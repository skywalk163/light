类 人类：
    属性 姓名。
    属性 年龄。
    
    段落 人类 接收 名 龄：
        己姓名 为 名。
        己年龄 为 龄。
    
    段落 打招呼：
        打印 "你好，我是 " 加 己姓名 加 "，今年 " 加 转文本(己年龄) 加 "岁。"。
    
    段落 获取年龄：
        返回 己年龄。
    
    段落 增长一岁：
        己年龄 为 己年龄 加 1。

打印 "=== 阶段2：类方法测试 ==="。

设 p 为 新建 人类("张三", 25)。
p.打招呼()。
打印 "年龄: " 加 转文本(p.获取年龄())。

p.增长一岁()。
打印 "增长一岁后年龄: " 加 转文本(p.获取年龄())。

设 p2 为 新建 人类("李四", 30)。
p2.打招呼()。
打印 "p2年龄: " 加 转文本(p2.获取年龄())。

打印 "=== 测试多个实例互不影响 ==="。
打印 "p年龄: " 加 转文本(p.获取年龄())。
打印 "p2年龄: " 加 转文本(p2.获取年龄())。

打印 "=== 测试完成 ==="。
