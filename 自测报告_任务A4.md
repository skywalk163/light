# 自测报告 · 任务 A4（工具并行与事件总线）

- 分支：`task-A4`，基线 `main = e1dbfc3a`（锚点 `pre-merge-round4`）
- 提交：`be1b09f7`（实现 + 测试）、本报告一条
- 开工检查：`git status --porcelain` = **0 行**；`.git/worktrees/wt-A4/index` **存在**（4,346,108 字节）

---

## 1. 改了哪些文件

| 文件 | 改动意图 |
|---|---|
| `stdlib/代理循环.light` | A4-1 注册表加第五参 `副作用`；A4-2 `分发工具` 拆成编排/并行/串行三段，只读批走线程；A4-4 修正两处注释 |
| `stdlib/事件总线.light` | A4-3 删掉重复贴两遍的过期设计注释，落「本轮不做异步发布」的裁决，补事件名冻结表 |
| `examples/harness/主程序.light` | 三个注册点补第五参（`read_file`/`list_dir` = 只读，`write_file` = 写）；加 `打印并行` 监听器 |
| `tests/test_tool_parallel_light.py` | 新建，26 条，覆盖 A4-1/A4-2/A4-3 的时序/顺序/隔离/失败四类判据 |

**没有碰**：`stdlib/代理工具集.light`、`stdlib/路径护栏.light`（B4）、`stdlib/流式.light`、`stdlib/SSE.light`（C4）、`src/`、`tools/ci/**`、任何门禁基线。
`stdlib` 里 `引 Python` 计数仍为 **0**（全 `stdlib/` grep 零命中）。

### 1.1 A4-1 定型的契约（B4 按此补传六个注册点）

```
己.注册工具(名称, 描述, 参数模式, 实现, 副作用 = "写")
```

- `副作用` ∈ `{"只读", "写", "执行"}`，其它值（含 `"read_only"`、`"readonly"`、`"只讀"`、`""`、`空`、数字）一律抛 `工具注册错误`
- 第五参可省，**省略即 `"写"`**
- `工具声明()` 的输出**不含**这个字段：只有 `name` / `description` / `parameters`
- 判定规则：`可整批并行(计划)` 要求 `长(计划) >= 2` 且 `己.并行上限 >= 2` 且**每一项**都是 `"只读"`

B4 的六个注册点：`read_file`/`list_dir`/`grep` → `"只读"`；`write_file`/`edit_file` → `"写"`；`run_command` → `"执行"`。

**契约未发现问题，无需主线裁决。** 实现中确认可行的两点：位置参数放末尾（放中间会让既有四参调用点静默错位）、带默认值的 `段落 ... 接收 a, b = "写":` 在光明里有 stdlib 先例（`断言工具轻量.light:18/26/34/42/53`）并实测编译通过。

### 1.2 并行度默认 4 的理由

工具执行是一次本地阻塞 I/O。线程数超过磁盘有效队列深度后吞吐不再涨；GIL 下工具实现里的纯 Python 部分本来就不并行；而模型一轮里同时发起的只读调用真实分布是 2～4 个。设成 1 等价于全串行（判定会拒绝）。超过上限时**分批 join**，不退化成串行——实测见 §3 的 0.602s。

### 1.3 三条不许悄悄放松的调度语义

1. **不重排**：混有 `"写"`/`"执行"` 的批整批串行，不做「先并行只读的再串行写的」。模型可能依赖顺序（先 write 再 read 同一文件）。
2. **回填顺序 == `tool_calls` 顺序**：结果存「下标 → 文本」的 `并行结果表`，回填按下标 0..n-1 走，全程不看「谁先完成」。这是结构性保证，不是靠约定。
3. **一个失败不拖垮整批**：`执行工具` 已把任何 Exception 转成可读文本，失败项照样占住自己的槽位照样回填。

**为什么解析校验与事件发布留在主线程**：事件总线不是线程安全的（`监听者表` 是普通 dict，发布时还会删一次性项录），而监听者常往 stdout 写中文——多线程同时写捕获流正是第三轮「3565 errors 幻影」的成因。只有 `执行工具` 进线程。

**为什么不用 `concurrent.futures`**：全仓 `.light` 对 `concurrent`/`futures`/`queue` 零使用零验证；`threading.Thread` + `join` 已在 `进程树.light:313-319`、`伪终端.light:234` 生产代码里跑了三轮。另外 `最终:`（finally）全仓只有 2 处且都是空块，靠它兜 `shutdown()` 没有活证据——所以用「分批 join + 预置哨兵槽位」，不依赖 finally。

---

## 2. 定向测试结果（输出落盘后直抄汇总行）

### 2.1 新建文件单跑

```
26 passed, 10 warnings in 9.60s
```

时序类原始输出（`-s` 捕获，非人工拼）：

```
[A4-2 时序] 并行 0.303s / 串行 0.904s（单工具 sleep 0.30s），比值 2.98
[A4-1 保守默认] 三个省略第五参的工具耗时 0.903s
[A4-2 并发上限] 上限=2 四个 sleep 0.30s 的只读工具，耗时 0.602s
[A4-2 隔离] 两只读+一写 混合批耗时 0.903s（三个 sleep 0.30s 串行）
```

### 2.2 A3 家族 + 新文件（`PYTHONUTF8=1`，本家族才带）

```
77 passed, 3 skipped, 10 warnings in 32.23s
```

3 skipped = `test_llm_client_light.py::TestRealAPI` 三条（无 `DEEPSEEK_API_KEY`），见 §6。
`test_harness_e2e.py` 四条全绿，说明主程序补第五参没有破坏整进程 e2e。

### 2.3 假测试门禁

```
[假测试门禁] 扫描 tests：命中 103 条违规
       字符串断言式         101 条
       上界断言式            2 条
[假测试门禁] 无新增违规。
[假测试门禁] 通过：存量冻结、无新增。
```

基线 103 条未动，新文件 0 条新增。

### 2.4 A4-3 判据 1（重复段落已消除）

```
事件总线.light 改前行数: 289
事件总线.light 改后行数: 268
```

差 **21 行**。改前 `:33-54` 与 `:63-84` 是同一段「D2-5 异步监听者语义设计」逐字重复（各 22 行），两份都删，换上一段更短的裁决记录 + 事件名冻结表。

---

## 3. 反跑证明（每条都是实测输出）

跑法：改坏一处 → 跑 `tests/test_tool_parallel_light.py` → `git checkout --` 恢复 → `git status --porcelain` 确认 0 行。

### 3.1 A4-1 反跑：默认值 `"写"` → `"只读"`

改 `段落 注册工具 接收 名称, 描述, 参数模式, 实现, 副作用 = "写":` 的默认值。

```
F............F............                                               [100%]
E   AssertionError: assert '只读' == '写'
      - 写
      + 只读
tests/test_tool_parallel_light.py:99: AssertionError: assert '只读' == '写'
E   AssertionError: 漏标第五参的工具被并行执行了（耗时 0.302s）
    assert 0.3021753000011813 > 0.9
---------------------------- Captured stdout call -----------------------------
[A4-1 保守默认] 三个省略第五参的工具耗时 0.302s
tests/test_tool_parallel_light.py:175: AssertionError: 漏标第五参的工具被并行执行了（耗时 0.302s）
2 failed, 24 passed, 10 warnings in 8.97s
```

两条都红：一条断字典内容，一条断**行为**（0.903s → 0.302s，说明默认值真的决定了调度路径，不只是写进字典）。

### 3.2 A4-2 时序判据反跑：线程池换回串行

在 `跑一批` 里 `t.start()` 后立刻 `t.join()`（起一个等一个）。

```
............F.F.F..F......                                               [100%]
E   AssertionError: 只读批没有真并行：并行 0.906s / 串行 0.903s（单工具 sleep 0.30s）
    assert 0.9057781999999861 < 0.5
---------------------------- Captured stdout call -----------------------------
[A4-2 时序] 并行 0.906s / 串行 0.903s（单工具 sleep 0.30s），比值 1.00
tests/test_tool_parallel_light.py:159
E   AssertionError: 并发上限=2 跑四任务耗时 1.208s，不符合「两批」预期
    assert 1.2082499999996799 < 0.9
[A4-2 并发上限] 上限=2 四个 sleep 0.30s 的只读工具，耗时 1.208s
E   AssertionError: 完成顺序没有被打乱（实际 ['slow', 'mid', 'fast']），本用例的前提不成立
    assert ['slow', 'mid', 'fast'] == ['fast', 'mid', 'slow']
tests/test_tool_parallel_light.py:216
E   AssertionError: 一个工具抛异常后整批退化成串行了（耗时 0.905s）
    assert 0.904655799999091 < 0.5
4 failed, 22 passed, 10 warnings in ...
```

比值从 2.98 掉到 **1.00**。注意第三条：顺序判据的**前提校验**也红了——它证明「完成顺序真的被打乱」这句断言不是装饰，串行实现下它当场失效。

### 3.3 A4-2 顺序判据反跑：回填改成按完成先后

删掉 `并行分发` 里按下标回填的循环，改在 `并行槽体` 里就地 `己.会话.追加工具(...)`。

```
............F...F..FF.....                                               [100%]
E   AssertionError: assert ['结果:r1', '结果:r3', '结果:r2'] == ['结果:r1', '结果:r2', '结果:r3']
      At index 1 diff: '结果:r3' != '结果:r2'
---------------------------- Captured stdout call -----------------------------
[A4-2 时序] 并行 0.303s / 串行 0.904s（单工具 sleep 0.30s），比值 2.98
tests/test_tool_parallel_light.py:163
E   AssertionError: assert ['fast', 'mid', 'slow'] == ['slow', 'mid', 'fast']
      At index 0 diff: 'fast' != 'slow'
tests/test_tool_parallel_light.py:219
E   AssertionError: assert ['boom', 'ok2', 'ok1'] == ['ok1', 'boom', 'ok2']
tests/test_tool_parallel_light.py:261
E   AssertionError: assert ['v_bad', 'v_ok'] == ['v_ok', 'v_bad']
tests/test_tool_parallel_light.py:286
4 failed, 22 passed, 10 warnings in ...
```

顺序判据红，**而时序判据仍绿（0.303s / 0.904s，比值 2.98）**——两条判据互相独立，各守一个性质。这正是「时序绿不代表顺序对」的实证：只有时序判据的话，这个真 bug 会整批放过。

### 3.4 A4-3 反跑：不发 `并行结束` 事件

```
......................FF..                                               [100%]
E   AssertionError: assert ['并行开始'] == ['并行开始', '并行结束']
      Right contains one more item: '并行结束'
tests/test_tool_parallel_light.py:312
E   AssertionError: assert [('开始', 0)] == [('开始', 0), ('结束', 3)]
      Right contains one more item: ('结束', 3)
tests/test_tool_parallel_light.py:328
2 failed, 24 passed, 10 warnings in ...
```

第二条同时钉住了「`并行结束` 必须在结果**全部回填之后**才发」：监听器看到的 tool 消息数是 3 而不是 0。

---

## 4. A4-4：改前 / 改后注释原文

### 4.1 文件头（原 `:17`）

改前：

```
#   - 网络错误指数退避重试（同步 time.sleep，待 A2 异步原语就绪后改异步睡眠）
```

改后：

```
#   - 网络错误指数退避重试：同步 time.sleep。
#     ⚠️ A4-4：这里以前写的是「待 A2 异步原语就绪后改异步睡眠」，那句话是**误导性的**
#     ——A2（异步 段落 / 并发等待 / 限时）早已合入 main，语言侧的 `异步 运行 主()`
#     真发射成 asyncio.run。挡路的从来不是异步原语，而是**传输层**：
#     stdlib/流式.light 的唯一读原语是阻塞 `s.recv`（只设 settimeout，never
#     setblocking(False)，全 stdlib 无 selectors/epoll）。在那之前把这 0.2 秒的退避
#     换成 异步睡眠，只会得到一个仍被 recv 堵死的事件循环。非阻塞传输层是 C4 的活；
#     本文件的循环异步化不在本轮范围内。详见下方 段落 睡眠 处的现状指针。
```

### 4.2 `段落 睡眠` 上方（原 `:377-384`）

原有那段判断说得对，**原文保留未动**，在其后追加现状指针：

```
  # ---- A4-4 现状指针（截至第四轮 A4 交付时）----
  # 非阻塞传输层是本轮 C4 的任务（总纲 §3、M7），**在本文件写下这段话时尚未交付**：
  # 本 worktree（task-A4，基线 main = e1dbfc3a）里 stdlib/流式.light 仍是
  # `settimeout` + 裸 `s.recv`，无 setblocking(False)、无 selector。
  # 所以上面那段判断在本轮依然成立，`time.sleep` 保持不变。
  # C4 合入后要不要把整条循环异步化，是**第五轮**的裁决，不在 A4 范围内——
  # 本轮明确只做「工具执行的线程并行」（见 分发工具），那条腿是本地 I/O，
  # 不依赖非阻塞 socket，两件事互不阻塞。
```

### 4.3 A4-3 的裁决：异步发布**不做**

三条理由已写进 `事件总线.light` 文件头：链路上无真 await 点（传输层阻塞）；现有唯一调用方 `代理循环` 是同步循环，加了就是零调用点的壳；发布顺序语义（注册序 + 中止传播 + 错误隔离）是被用例钉住的基线，异步版要原样复刻，那是一次真实重写。

按任务书口径，代码里**不留「将来要做」的悬空承诺**——删掉的是过期设计，写下的是「本轮不做 + 为什么」的事实陈述。缺口进本报告 §5 的移交清单，请 D4 记入 `docs/known_issues.md`（那是它的文件）。

---

## 5. 移交清单：光明写不出什么 → 临时绕法 → 期望的语言能力

| 光明写不出什么 | 本轮的临时绕法 | 期望的语言能力 |
|---|---|---|
| **没有可依赖的 `最终:`（finally）**。全仓仅 `流式.light:114`、`:263` 两处，且两处的 `最终:` 块体都是空的，真正的清理写在 try/finally 之外 | 不靠 finally 兜底：`预置槽位` 先把每个槽位填上可读的哨兵文本，线程异常退出时回填不会变成 KeyError | `尝试: / 捕获: / 最终:` 三段式里 `最终:` 能带缩进块体并保证执行。这是资源清理（关 socket、关文件、shutdown 线程池）的基础设施 |
| **`列表[下标] 为 值` 在 stdlib 无活证据**。有先例的文件（`examples/algorithms/*`、`积木库/blocks_v5/*`）都在 `E2E_EXCLUDED` 里，不在 CI 主链路 | 并行结果用**字典**（键 = 下标）而不是「预分配列表 + 按下标填」 | 让列表下标赋值进入 CI 覆盖，或明确文档化「stdlib 里请用 `.追加()`」 |
| **`[空] * N` 预分配全仓零命中** | 用 `当 i < n:` 循环逐个写字典键 | 序列重复运算符，或一个标准库的 `重复(值, 次数)` |
| **关键字实参只有 1 处真实用例**（`流式.light:108` 的 `server_hostname=目标`），且 parser 的 `_try_parse_keyword_arg` 是近期才补进括号分支的 | `threading.Thread(空, 己.并行槽体, 空, [槽号, 项])` 全用位置参数占位 `空`，`daemon` 走属性赋值 | 关键字实参进入 stdlib 常规用法并有回归覆盖。`Thread(target=..., args=...)` 比四个位置参数（其中两个是 `空`）可读得多 |
| **`concurrent.futures` 零验证**；`导入 a.b` 形式全仓只有 `urllib.parse` 一处先例 | 手写「分批 join」代替线程池 | 至少让 `导入 concurrent.futures` + `ThreadPoolExecutor` 有一条真跑用例。长点号链在光明里有历史坑（见 `代理循环.light:304-305` 记录的 `己.X.设置Y` 词法拆分） |
| **`继续`（continue）在 stdlib 零命中**（`跳过` 只有 `代理工具集.light:68` 一处） | `编排一项` 用「多个 `返回` 早退」代替 continue | 明确 continue 的关键字并纳入覆盖 |
| **字符串字面量里不许出现花括号**（A3 已登记：codegen 把字面量无条件发射成 f-string） | 全文回避 | 见 A3 移交清单 |

### 5.1 交 D4 记 `docs/known_issues.md`

1. 事件总线**无异步发布 / 异步监听者**（A4 本轮明确不做，三条理由见 `事件总线.light` 文件头）。前置条件是 C4 的非阻塞传输层。
2. 代理循环的 `段落 睡眠` 仍是 `time.sleep`；整条循环异步化是第五轮议题。

### 5.2 提请注意（不是缺陷，是口径差异，本轮未改）

- **总纲 §4.3 的事件名表与代码现状有两处不一致**：表里写 `工具开始`/`工具结束`/`工具失败`，代码实际只发一个 `工具调用`（`代理循环.light` 编排阶段），且 `test_agent_loop_light.py:187` 与 `examples/harness/主程序.light` 都订阅的是 `工具调用`。本轮**没有改事件名**——改了会破坏两个既有文件的现有断言，且不在 A4 范围。请主线裁决是补齐三个细粒度事件（第五轮）还是把总纲的表改成现状。
- **失败文本的措辞**：任务书 A4-2 第 5 点写「转成 `"工具调用失败: <原因>"`」，但现状实现里 schema 校验失败才是 `"工具调用失败: "`，**工具实现抛异常是 `"工具执行出错: "`**（`执行工具`）。我按现状语义走、没有统一措辞，测试断的是实际文本。两者区分是有意义的（前者模型该改参数重试，后者是环境问题），建议保留。

---

## 6. 已知未完成 / 未实测项

1. **POSIX 分支未实测**。本轮全部实测在 Windows 10 / PowerShell 5.1。并行实现只用 `threading.Thread` / `Lock` / `join`，无平台分支代码，但时序阈值（并行 <0.5s、串行 >0.9s）在 FreeBSD runner 上的余量未验证。风险点：CI 用 `pytest -n auto`，如果 runner 负载高，三个 `sleep(0.3)` 的并行批可能被调度拖过 0.5s。**若 CI 上这条软红了，先看是否是负载而非实现**——`比值 > 2.0` 那条断言对负载不敏感，可作为交叉判据。
2. **真实模型下的并行未验证**。`TestRealAPI` 三条 skip（无 key），所以「真实 DeepSeek 一轮里同时发起多个 `read_file`、走上并行路径」没有在线证据。离线侧由 `test_tool_parallel_light.py` 全覆盖。
3. **`并行上限` 的上界未压测**。只测了 2 和 4。设成很大的值（比如 64）时会一次起 64 个线程，没有测这种情况的内存与调度表现。
4. **未做「写工具真并发竞写」的负向证明**。即没有构造「两个 write 被错误并行导致数据损坏」的用例来反证隔离判据的必要性——隔离判据现在只证明「混批确实串行了」。
5. **B4 的六个注册点尚未补传**，所以 `代理工具集.light` 的工具目前全部走默认 `"写"` → 全部串行。这是设计上的安全默认，等 B4 合入后并行才会在真 harness 上生效。**这不是缺陷，但意味着「并行在真链路上有收益」这件事要等 B4 合入后才能端到端观测。**

---

## 7. skip / xfail 清单与掩盖分析

本任务**新增 0 条 skip、0 条 xfail**。`tests/test_tool_parallel_light.py` 26 条全部无条件执行。

存量 3 条 skip（`tests/test_llm_client_light.py::TestRealAPI`，`reason="未设置 DEEPSEEK_API_KEY"`）：

| 用例 | 跳过原因 | 掩盖了哪个未验证事实 |
|---|---|---|
| `test_stream_chat` | 无 `DEEPSEEK_API_KEY` | 真实 DeepSeek 对我们 SSE 分帧的接受度 |
| `test_nonstream_chat` | 同上 | 真实 DeepSeek 非流式响应体结构的接受度 |
| `test_真实模型接受tools声明并返回tool_calls` | 同上 | **真实模型认不认我们发的 `tools` 声明**——这是 A4-1「工具声明不带 `副作用` 字段」这条判据的在线对应面 |

**按第三轮口径核对结论：文件里 `:257-275` 的掩盖分析措辞已经是正确口径**，明确写着「掩盖的**只是**『对方认不认』，**不掩盖 tool_call 功能本身**：功能由 `tests/test_deepseek_mock.py` 守（无 skip、任何机器上都必须绿）」。**不需要改，本轮未动这个文件。**

对照总纲 §5.3（xfail 只许钉本轮明确不修的缺口）自查：这三条不是 xfail 而是环境 skip，且不在本轮裁决要修的范围（本轮硬修项是硬链接越界与 TOCTOU，都属 B4）。**无违规。**

补一条与 A4-1 直接相关的说明：`副作用` 字段**不会**增加这三条 skip 的掩盖面——因为它按设计根本不下发给模型，`test_工具声明逐字段只有name_description_parameters` 已经用 `json.dumps(声明, ensure_ascii=False)` 全序列化断言「副作用」三个字不出现在任何嵌套层。这条判据离线就能守死，不依赖真连。

---

## 8. A4-5 假绿排查结果（本家族逐个过了一遍）

- **`assert '<字符串>' in py_code` 形态**：本家族六个文件（`test_agent_loop_light` / `test_llm_client_light` / `test_event_bus_light` / `test_deepseek_mock` / `test_harness_e2e` / 新建的 `test_tool_parallel_light`）**零命中**。假测试门禁扫全 `tests/` 命中的 101 条字符串断言式全在其它文件（编译器测试家族），属 D4 的存量基线。
- **「订阅方签名少一参 → TypeError 被吞 → 事件列表恒空 → 上界断言空转」形态**：`test_agent_loop_light.py` 全文订阅点逐个核对（`:188`、`:249`、`:273`、`:286`、`:420`），**全部是两参**，第三轮修完没有残留。
- **上界断言**：`test_agent_loop_light.py` 剩下三处 `<=` 都不是空转形态——`:354 assert 用量 <= 400`（截断后用量不超预算，用量是数值不是集合长度）、`:356 assert len(剩下) < len(消息们)`（严格小于）、`:389 assert {...} <= 落地`（集合包含关系）。留原样。
- 新文件自身的判据全部是等值断言或带下界的双侧区间（`0.5 < 耗时 < 0.9`），无 `len(...) <= N`、无 `len(...) >= 0`、无 `returncode in [...]`。门禁实测 0 新增。

---

## 9. 环境与卫生

- 临时文件前缀 `_taskA4_`，**收尾已全部删除**；工作树 `git status --porcelain` = 0 行
- 端口：本文件不起服务器（直接调 `分发工具`），无端口占用
- `PYTHONUTF8=1` 只加在本家族六个文件的命令上，**没有**用在全量命令里
- 输出落盘姿势：PowerShell 的 `| Out-File -Encoding utf8` 会把 python 的 UTF-8 字节按 GBK 解码后再存，得到一份坏文件。可靠写法是让 cmd 做字节级重定向：
  `cmd /c "python -m pytest ... > 文件 2>&1"`。**建议其它三路照用。**
- 提交逐路径 `git add` 白名单，未用 `-A` / `commit -a`
