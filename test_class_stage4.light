打印 "=== 阶段4：类型判断 + isinstance 测试 ==="。

类 动物：
    属性 名字。
    
    段落 动物 接收 名：
        己名字 为 名。
    
    段落 说话：
        打印 己名字 加 "发出声音"。

类 狗 继承 动物：
    属性 品种。
    
    段落 狗 接收 名 品：
        父.动物(名)。
        己品种 为 品。
    
    段落 说话：
        打印 己名字 加 "汪汪叫"。

类 猫 继承 动物：
    属性 毛色。
    
    段落 猫 接收 名 毛：
        父.动物(名)。
        己毛色 为 毛。
    
    段落 说话：
        打印 己名字 加 "喵喵叫"。

类 金毛犬 继承 狗：
    属性 性格。
    
    段落 金毛犬 接收 名 性：
        父.狗(名, "金毛")。
        己性格 为 性。

打印 "--- 测试1：基础类型判断 ---"。
设 d 为 新建 狗("旺财", "中华田园犬")。
打印 "d 是 狗: " 加 转文本(是实例(d, "狗"))。
打印 "d 是 动物: " 加 转文本(是实例(d, "动物"))。
打印 "d 是 猫: " 加 转文本(是实例(d, "猫"))。

打印 "--- 测试2：继承关系判断 ---"。
设 c 为 新建 猫("咪咪", "橘色")。
打印 "c 是 猫: " 加 转文本(是实例(c, "猫"))。
打印 "c 是 动物: " 加 转文本(是实例(c, "动物"))。
打印 "c 是 狗: " 加 转文本(是实例(c, "狗"))。

打印 "--- 测试3：多层继承判断 ---"。
设 g 为 新建 金毛犬("大黄", "温顺")。
打印 "g 是 金毛犬: " 加 转文本(是实例(g, "金毛犬"))。
打印 "g 是 狗: " 加 转文本(是实例(g, "狗"))。
打印 "g 是 动物: " 加 转文本(是实例(g, "动物"))。
打印 "g 是 猫: " 加 转文本(是实例(g, "猫"))。

打印 "--- 测试4：对象类型名称获取 ---"。
打印 "d 的类型: " 加 取类型(d)。
打印 "c 的类型: " 加 取类型(c)。
打印 "g 的类型: " 加 取类型(g)。

打印 "--- 测试5：基础类型的类型名称 ---"。
设 n 为 42。
打印 "整数的类型: " 加 取类型(n)。

设 f 为 3.14。
打印 "浮点数的类型: " 加 取类型(f)。

设 s 为 "hello"。
打印 "文本的类型: " 加 取类型(s)。

设 b 为 真。
打印 "布尔的类型: " 加 取类型(b)。

设 lst 为 新建列表()。
打印 "列表的类型: " 加 取类型(lst)。

打印 "=== 阶段4测试完成 ==="。
