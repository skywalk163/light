# 光明标准库 v3.2 - 字符串工具模块

段落 反转 接收 原文：
    定义 结果 等于 ""
    定义 长度 等于 长度(原文)
    定义 索引 等于 长度 减 1
    当 索引 大于等于 0：
        结果 等于 结果 加 取字符(原文, 索引)
        索引 等于 索引 减 1
    返回 结果

段落 重复 接收 原文, 次数：
    定义 结果 等于 ""
    定义 计数 等于 0
    当 计数 小于 次数：
        结果 等于 结果 加 原文
        计数 等于 计数 加 1
    返回 结果

导出 反转, 重复
