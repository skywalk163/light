# 光明标准库 v3.2 - 列表工具模块
#
# 用法：
#   从 列表工具 导入 求和, 最大值, 反转列表。

# 列表求和
段落 求和 接收 列表：
    定义 总和 等于 0
    定义 计数 等于 0
    定义 长度值 等于 长度(列表)
    当 计数 小于 长度值：
        总和 等于 总和 加 列表[计数]
        计数 等于 计数 加 1
    返回 总和

# 列表最大值
段落 最大值 接收 列表：
    定义 结果 等于 列表[0]
    定义 计数 等于 1
    定义 长度值 等于 长度(列表)
    当 计数 小于 长度值：
        如果 列表[计数] 大于 结果：
            结果 等于 列表[计数]
        计数 等于 计数 加 1
    返回 结果

# 列表最小值
段落 最小值 接收 列表：
    定义 结果 等于 列表[0]
    定义 计数 等于 1
    定义 长度值 等于 长度(列表)
    当 计数 小于 长度值：
        如果 列表[计数] 小于 结果：
            结果 等于 列表[计数]
        计数 等于 计数 加 1
    返回 结果

# 列表平均值
段落 平均值 接收 列表：
    定义 长度值 等于 长度(列表)
    如果 长度值 等于 0：
        返回 0
    定义 总和 等于 求和(列表)
    返回 总和 除以 长度值

# 反转列表
段落 反转列表 接收 列表：
    定义 长度值 等于 长度(列表)
    定义 结果 等于 []
    定义 索引 等于 长度值 减 1
    当 索引 大于等于 0：
        追加(结果, 列表[索引])
        索引 等于 索引 减 1
    返回 结果

# 判断包含元素
段落 包含 接收 列表, 元素：
    定义 计数 等于 0
    定义 长度值 等于 长度(列表)
    当 计数 小于 长度值：
        如果 列表[计数] 等于 元素：
            返回 真
        计数 等于 计数 加 1
    返回 假

# 查找元素索引
段落 查找索引 接收 列表, 元素：
    定义 计数 等于 0
    定义 长度值 等于 长度(列表)
    当 计数 小于 长度值：
        如果 列表[计数] 等于 元素：
            返回 计数
        计数 等于 计数 加 1
    返回 减 1

# 列表连接
段落 连接 接收 列表甲, 列表乙：
    定义 结果 等于 []
    定义 索引 等于 0
    定义 长度甲 等于 长度(列表甲)
    当 索引 小于 长度甲：
        追加(结果, 列表甲[索引])
        索引 等于 索引 加 1
    索引 等于 0
    定义 长度乙 等于 长度(列表乙)
    当 索引 小于 长度乙：
        追加(结果, 列表乙[索引])
        索引 等于 索引 加 1
    返回 结果

# 范围生成
段落 范围 接收 起始, 结束：
    定义 结果 等于 []
    定义 当前 等于 起始
    当 当前 小于 结束：
        追加(结果, 当前)
        当前 等于 当前 加 1
    返回 结果

导出 求和, 最大值, 最小值, 平均值, 反转列表
导出 包含, 查找索引, 连接, 范围
