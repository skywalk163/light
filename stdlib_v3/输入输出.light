# 光明标准库 v3.2 - 输入输出模块
#
# 用法：
#   从 输入输出 导入 打印行, 读取行。

# 打印文本并换行
段落 打印行 接收 文本：
    打印(文本)

# 打印文本不换行
段落 打印不换行 接收 文本：
    写入输出(文本)

# 从标准输入读取一行
段落 读取行：
    返回 输入()

# 读取一行并转为整数
段落 读取整数：
    定义 行 等于 输入()
    返回 转整数(行)

# 读取一行并转为浮点数
段落 读取浮点数：
    定义 行 等于 输入()
    返回 转浮点数(行)

导出 打印行, 打印不换行, 读取行, 读取整数, 读取浮点数
