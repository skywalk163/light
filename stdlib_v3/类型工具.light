# 光明标准库 v3.2 - 类型工具模块
#
# 用法：
#   从 类型工具 导入 是整数, 是字符串, 类型名。

# 判断是否为整数
段落 是整数 接收 n：
    返回 类型(n) 等于 "整数"

# 判断是否为浮点数
段落 是浮点数 接收 n：
    返回 类型(n) 等于 "浮点数"

# 判断是否为字符串
段落 是字符串 接收 s：
    返回 类型(s) 等于 "字符串"

# 判断是否为列表
段落 是列表 接收 l：
    返回 类型(l) 等于 "列表"

# 将字符串转为整数
段落 转整数 接收 s：
    返回 转整数(s)

# 将字符串转为浮点数
段落 转浮点数 接收 s：
    返回 转浮点数(s)

# 将任意值转为字符串
段落 转字符串 接收 n：
    返回 转串(n)

# 返回值的类型名称
段落 类型名 接收 v：
    返回 类型(v)

导出 是整数, 是浮点数, 是字符串, 是列表
导出 转整数, 转浮点数, 转字符串, 类型名
