# 压缩算法

> gzip、zlib、lz4、zstd压缩与解压的段言封装

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 8 |
| FFI 声明 | 9 |

**关键词:** 压缩, gzip, zlib, lz4, zstd

## 导入方式

```duan
导入 压缩算法
```

或

```duan
导入 标准压缩算法
```

## 函数列表

共 8 个公开函数

> 压缩算法 — duanpub 桥接模块
> 
> 基于 Python gzip / zlib 标准库封装，函数名对齐 duanpub/packages/压缩算法/源.duan。
> 
> duanpub 原始包通过 C FFI 调用 zlib/lz4/zstd 库，
> 本桥接模块用 Python 标准库替代：
>   - gzip压缩/解压: Python gzip 模块
>   - zlib压缩/解压: Python zlib 模块
>   - lz4压缩/解压: 需要第三方 lz4 库 (skeleton)
>   - zstd压缩/解压: 需要第三方 zstd 库 (skeleton)

### gzip压缩

**参数:** `数据, 压缩级别`

gzip 压缩数据，返回字节

### gzip解压

**参数:** `数据`

gzip 解压数据，返回字节

### zlib压缩

**参数:** `数据, 压缩级别`

zlib 压缩数据，返回字节

### zlib解压

**参数:** `数据`

zlib 解压数据，返回字节

### lz4压缩

**参数:** `数据`

lz4 压缩数据

注意: 需要安装第三方 lz4 库 (pip install lz4)

### lz4解压

**参数:** `数据`

lz4 解压数据

注意: 需要安装第三方 lz4 库 (pip install lz4)

### zstd压缩

**参数:** `数据, 压缩级别`

zstd 压缩数据

注意: 需要安装第三方 zstd 库 (pip install zstandard)

### zstd解压

**参数:** `数据`

zstd 解压数据

注意: 需要安装第三方 zstd 库 (pip install zstandard)
