# 密码哈希

> bcrypt/scrypt/argon2id密码哈希，密码强度校验，常见密码黑名单，防时序攻击

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 4 |
| FFI 声明 | 7 |

**关键词:** 密码, 哈希, bcrypt, scrypt, argon2, 安全

## 导入方式

```duan
导入 密码哈希
```

或

```duan
导入 标准密码哈希
```

## 函数列表

共 4 个公开函数

> 密码哈希 — duanpub 桥接模块
> 
> 基于 Python hashlib 库封装，函数名对齐 duanpub/packages/密码哈希/源.duan。
> 
> duanpub 原始包通过 C FFI 调用 bcrypt/scrypt 密码哈希库，
> 本桥接模块用 Python hashlib 的 scrypt + pbkdf2 替代，
> 提供安全的密码哈希与验证功能。

### 哈希密码

**参数:** `密码, 盐值, 算法`

哈希密码，返回包含盐值的哈希字符串

格式: $algorithm$salt$hash（base64 编码）

### 验证密码

**参数:** `密码, 哈希字符串`

验证密码与哈希字符串是否匹配

### 生成盐值

**参数:** `长度`

生成密码盐值

### 获取算法名称

获取密码哈希算法名称
