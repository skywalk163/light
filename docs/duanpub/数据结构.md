# 数据结构

> 链表、栈、队列、堆、树、图、哈希表增强

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 52 |
| FFI 声明 | 0 |

**关键词:** 数据结构, 链表, 栈, 队列, 树, 图

## 导入方式

```duan
导入 数据结构
```

或

```duan
导入 标准数据结构
```

## 函数列表

共 52 个公开函数

> 数据结构 — duanpub 桥接模块
> 
> 基于 Python collections 库封装，函数名对齐 duanpub/packages/数据结构/源.duan。
> 
> duanpub 原始包通过 C FFI 实现链表、栈、队列、堆、树、图等数据结构，
> 本桥接模块用 Python 标准库实现，提供等价的数据结构功能。

### 创建链表节点

**参数:** `值`

创建链表节点

### 创建链表

创建空链表

### 链表追加

**参数:** `链表, 值`

向链表追加元素

### 链表插入

**参数:** `链表, 索引, 值`

在链表指定位置插入元素

### 链表删除

**参数:** `链表, 索引`

删除链表指定位置的元素

### 链表获取

**参数:** `链表, 索引`

获取链表指定位置的元素值

### 链表长度获取

**参数:** `链表`

获取链表长度

### 链表转为列表

**参数:** `链表`

将链表转为Python列表

### 创建栈

创建空栈

### 栈压入

**参数:** `栈, 值`

向栈中压入元素

### 栈弹出

**参数:** `栈`

从栈中弹出元素

### 栈窥视

**参数:** `栈`

查看栈顶元素但不弹出

### 栈大小

**参数:** `栈`

获取栈的大小

### 栈是否为空

**参数:** `栈`

检查栈是否为空

### 创建队列

创建空队列

### 队列入队

**参数:** `队列, 值`

向队列尾部入队

### 队列出队

**参数:** `队列`

从队列头部出队

### 队列窥视

**参数:** `队列`

查看队列头部元素但不移除

### 队列大小

**参数:** `队列`

获取队列的大小

### 队列是否为空

**参数:** `队列`

检查队列是否为空

### 创建最小堆

创建最小堆，返回堆对象

### 堆上浮

**参数:** `堆, 索引`

对堆中指定索引的元素执行上浮操作

### 堆下沉

**参数:** `堆, 索引`

对堆中指定索引的元素执行下沉操作

### 堆插入

**参数:** `堆, 值`

向堆中插入一个值

### 堆弹出

**参数:** `堆`

从堆中弹出最小值

### 堆窥视

**参数:** `堆`

查看堆顶元素但不弹出

### 堆是否为空

**参数:** `堆`

检查堆是否为空

### 创建树节点

**参数:** `值`

创建树节点

### 创建二叉树

创建空二叉树

### 二叉树插入

**参数:** `二叉树, 值`

向二叉搜索树中插入值

### 二叉树包含

**参数:** `二叉树, 值`

检查二叉树中是否包含指定值

### 中序遍递归

**参数:** `节点`

中序遍历递归辅助函数，返回值列表

### 二叉树中序遍

**参数:** `二叉树`

中序遍历二叉树，返回值列表

### 前序遍递归

**参数:** `节点`

前序遍历递归辅助函数，返回值列表

### 二叉树前序遍

**参数:** `二叉树`

前序遍历二叉树，返回值列表

### 后序遍递归

**参数:** `节点`

后序遍历递归辅助函数，返回值列表

### 二叉树后序遍

**参数:** `二叉树`

后序遍历二叉树，返回值列表

### 创建图

创建空图

### 图添加节点

**参数:** `图, 节点`

向图中添加节点

### 图添加边

**参数:** `图, 节点1, 节点2, 权重`

向图中添加边（无向图）

### 图邻居

**参数:** `图, 节点`

获取图中指定节点的邻居列表

### 图节点数

**参数:** `图`

获取图的节点数

### 图边数

**参数:** `图`

获取图的边数

### 图包含边

**参数:** `图, 节点1, 节点2`

检查图中是否存在边

### 创建哈希表增强

创建增强哈希表

### 哈希表设置

**参数:** `哈希表, 键, 值`

设置哈希表中的键值对

### 哈希表获取

**参数:** `哈希表, 键, 默认值`

获取哈希表中指定键的值

### 哈希表删除

**参数:** `哈希表, 键`

删除哈希表中指定键的键值对

### 哈希表包含

**参数:** `哈希表, 键`

检查哈希表中是否包含指定键

### 哈希表键列表

**参数:** `哈希表`

获取哈希表中所有键的列表

### 哈希表值列表

**参数:** `哈希表`

获取哈希表中所有值的列表

### 哈希表条目列表

**参数:** `哈希表`

获取哈希表中所有条目的列表
