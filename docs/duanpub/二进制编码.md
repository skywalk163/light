# 二进制编码

> Base64/Base32/Hex/URL编码/百分号编码

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 27 |
| FFI 声明 | 0 |

**关键词:** 编码, Base64, Base32, Hex, URL, 百分号

## 导入方式

```duan
导入 二进制编码
```

或

```duan
导入 标准二进制编码
```

## 函数列表

共 27 个公开函数

> 二进制编码 — duanpub 桥接模块
> 
> 基于 Python base64 / binascii / urllib.parse 库封装，
> 函数名对齐 duanpub/packages/二进制编码/源.duan。
> 
> duanpub 原始包通过 C FFI 实现编码/解码，
> 本桥接模块用 Python 标准库替代，提供等价的 Base64/Base32/Hex/URL/百分号编码功能。

### 字符串转字节

**参数:** `文本, 编码`

将字符串转为字节数组

### 字节转字符串

**参数:** `数据, 编码`

将字节数组转为字符串

### 是十六进制字符

**参数:** `ch`

判断字符是否为十六进制字符(0-9, a-f, A-F)

### 十六进制值

**参数:** `ch`

获取十六进制字符对应的数值

### 是空白字符

**参数:** `ch`

判断字符是否为空白字符

### 是URL安全字符

**参数:** `ch`

判断字符是否为URL安全字符

### 字节转十六进制

**参数:** `数据`

将字节转为十六进制小写字符串

### 字节转十六进制大写

**参数:** `数据`

将字节转为十六进制大写字符串

### Base64编码

**参数:** `数据`

Base64 编码，返回字节

### Base64编码字符串

**参数:** `数据`

Base64 编码，返回字符串

### Base64解码

**参数:** `数据`

Base64 解码，返回字节

### Base64解码为字符串

**参数:** `数据, 编码`

Base64 解码为字符串

### Base64URL编码

**参数:** `数据`

Base64 URL-safe 编码，返回字节

### Base64URL解码

**参数:** `数据`

Base64 URL-safe 解码，返回字节

### Base32编码

**参数:** `数据`

Base32 编码，返回字节

### Base32编码字符串

**参数:** `数据`

Base32 编码，返回字符串

### Base32解码

**参数:** `数据`

Base32 解码，返回字节

### Base32解码为字符串

**参数:** `数据, 编码`

Base32 解码为字符串

### Hex编码

**参数:** `数据`

Hex 编码（小写），返回字节

### Hex编码大写

**参数:** `数据`

Hex 编码（大写），返回字节

### Hex解码

**参数:** `数据`

Hex 解码，返回字节

### URL编码

**参数:** `字符串, safe`

URL 编码

### URL解码

**参数:** `字符串`

URL 解码

### URL编码查询

**参数:** `参数字典`

URL 查询参数编码

### URL解码查询

**参数:** `查询字符串`

URL 查询参数解码，返回字典

### 百分号编码

**参数:** `数据, safe`

百分号编码（同 URL编码）

### 百分号解码

**参数:** `字符串`

百分号解码（同 URL解码）
