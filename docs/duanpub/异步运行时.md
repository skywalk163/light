# 异步运行时

> 异步运行时：事件循环、Future/Promise、定时器、任务调度、异步IO

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 54 |
| FFI 声明 | 0 |

**关键词:** 异步, 事件循环, Future, Promise, 定时器, IO

## 导入方式

```duan
导入 异步运行时
```

或

```duan
导入 标准异步运行时
```

## 函数列表

共 54 个公开函数

> 异步运行时 — duanpub 桥接模块
> 
> 基于 Python asyncio 库封装，函数名对齐 duanpub/packages/异步运行时/源.duan。
> 
> duanpub 原始包通过 C FFI 实现异步运行时，
> 本桥接模块用 Python asyncio 模块替代，提供等价的 Future/Promise/EventLoop 功能。

### 创建Future

创建一个 Future 对象

### 创建Future完成

**参数:** `result`

创建一个已完成的 Future 对象

### 创建Future失败

**参数:** `exception`

创建一个已失败的 Future 对象

### 未来完成

**参数:** `fut`

设置 Future 为完成状态

### 未来拒绝

**参数:** `fut, exception`

设置 Future 为拒绝状态

### 未来等待

**参数:** `fut`

等待 Future 完成

### 未来等待超时

**参数:** `fut, timeout`

等待 Future 完成，超时返回 None

### 未来添加回调

**参数:** `fut, callback`

添加 Future 完成回调

### 未来添加错误回调

**参数:** `fut, callback`

添加 Future 错误回调

### 未来是否完成

**参数:** `fut`

检查 Future 是否完成

### 未来是否失败

**参数:** `fut`

检查 Future 是否失败

### 未来链式调用

**参数:** `fut, callback`

链式调用 Future

### 未来链式回调

**参数:** `fut, callback`

链式回调 Future

### 未来异常处理

**参数:** `fut, err_callback`

Future 异常处理

### 未来取消

**参数:** `fut`

取消 Future

### 未来获取结果

**参数:** `fut`

获取 Future 结果

### 创建Promise

创建一个 Promise 对象

### Promise执行

**参数:** `promise, executor`

执行 Promise

### Promise解决

**参数:** `promise, value`

解决 Promise

### Promise拒绝

**参数:** `promise, reason`

拒绝 Promise

### Promise然后

**参数:** `promise, callback`

Promise then 链式调用

### Promise捕获

**参数:** `promise, callback`

Promise catch 错误处理

### Promise最终

**参数:** `promise, callback`

Promise finally 最终处理

### Promise全部

**参数:** `promises`

等待所有 Promise 完成

### Promise竞速

**参数:** `promises`

Promise 竞速，返回第一个完成的

### Promise全部完成

**参数:** `promises`

等待所有 Promise 完成（忽略错误）

### Promise任意

**参数:** `promises`

Promise 任意一个完成

### Promise解决已完成

**参数:** `value`

返回一个已解决的 Promise

### Promise拒绝已完成

**参数:** `reason`

返回一个已拒绝的 Promise

### 创建事件循环

创建新的事件循环

### 事件循环运行

**参数:** `loop`

运行事件循环

### 事件循环运行单次

**参数:** `loop`

运行事件循环单次迭代

### 事件循环停止

**参数:** `loop`

停止事件循环

### 事件循环暂停

**参数:** `loop`

暂停事件循环

### 事件循环恢复

**参数:** `loop`

恢复事件循环

### 事件循环是否运行中

**参数:** `loop`

检查事件循环是否运行中

### 事件循环设置超时

**参数:** `loop, delay, callback`

设置超时定时器

### 事件循环设置间隔

**参数:** `loop, interval, callback`

设置间隔定时器

### 事件循环清除定时器

**参数:** `loop, timer_handle`

清除定时器

### 事件循环清除间隔

**参数:** `loop, interval_handle`

清除间隔定时器

### 事件循环立即执行

**参数:** `loop, callback`

立即执行回调

### 事件循环创建任务

**参数:** `loop, coro`

在事件循环中创建任务

### 事件循环任务执行器

**参数:** `loop, func`

在事件循环中执行任务

### 异步任务等待

**参数:** `task`

等待异步任务完成

### 异步任务取消

**参数:** `task`

取消异步任务

### 事件循环等待所有

**参数:** `loop, tasks`

等待所有任务完成

### 事件循环等待任意

**参数:** `loop, tasks`

等待任意任务完成

### 事件循环注册输入输出

**参数:** `loop, fd, callback`

注册 IO 输入输出监听

### 事件循环注销输入输出

**参数:** `loop, fd`

注销 IO 输入输出监听

### 事件循环输入输出轮询

**参数:** `loop, timeout`

IO 轮询

### 事件循环睡眠

**参数:** `loop, seconds`

事件循环睡眠

### 事件循环睡眠回调

**参数:** `loop, seconds, callback`

睡眠后执行回调

### 事件循环延迟

**参数:** `loop, seconds`

事件循环延迟

### 事件循环获取统计

**参数:** `loop`

获取事件循环统计信息
