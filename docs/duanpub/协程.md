# 协程

> 协程库：协程调度、挂起恢复、通道通信、生成器、选择器

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 37 |
| FFI 声明 | 0 |

**关键词:** 协程, 调度, 通道, 生成器, 异步

## 导入方式

```duan
导入 协程
```

或

```duan
导入 标准协程
```

## 函数列表

共 37 个公开函数

> 协程 — duanpub 桥接模块
> 
> 基于 Python asyncio 库封装，函数名对齐 duanpub/packages/协程/源.duan。
> 
> duanpub 原始包通过 C FFI 实现协程调度、通道通信、生成器、选择器，
> 本桥接模块用 Python asyncio 模块替代，提供等价的协程功能。

### createScheduler

创建协程调度器

### createChannel

创建无缓冲通道

### createBufferedChannel

**参数:** `capacity`

创建有缓冲通道

### createSelector

创建选择器

### createGenerator

**参数:** `gen_func`

创建生成器

### schedulerCreateCoro

**参数:** `scheduler, coro_func`

在调度器中创建协程

### schedulerStartCoro

**参数:** `scheduler, coro_id`

启动协程

### schedulerSuspendCoro

**参数:** `scheduler, coro_id`

挂起协程

### schedulerResumeCoro

**参数:** `scheduler, coro_id`

恢复协程

### schedulerCancelCoro

**参数:** `scheduler, coro_id`

取消协程

### schedulerRunStep

**参数:** `scheduler`

运行协程调度器一步

### schedulerRunAll

**参数:** `scheduler`

运行所有协程直到完成

### schedulerRun

**参数:** `scheduler`

运行调度器

### schedulerStop

**参数:** `scheduler`

停止调度器

### schedulerGetCoro

**参数:** `scheduler, coro_id`

获取协程信息

### schedulerGetAllCoros

**参数:** `scheduler`

获取所有协程

### schedulerGetStats

**参数:** `scheduler`

获取调度器统计

### schedulerWaitCoro

**参数:** `scheduler, coro_id`

等待单个协程完成

### schedulerWaitAll

**参数:** `scheduler`

等待所有协程完成

### schedulerWaitAny

**参数:** `scheduler`

等待任意协程完成

### schedulerYield

**参数:** `scheduler`

让出协程执行权

### channelSend

**参数:** `ch, value`

向通道发送数据

### channelRecv

**参数:** `ch`

从通道接收数据

### channelTrySend

**参数:** `ch, value`

尝试向通道发送数据（非阻塞）

### channelTryRecv

**参数:** `ch`

尝试从通道接收数据（非阻塞）

### channelClose

**参数:** `ch`

关闭通道

### channelIsClosed

**参数:** `ch`

检查通道是否已关闭

### channelLen

**参数:** `ch`

获取通道当前长度

### channelCap

**参数:** `ch`

获取通道容量

### channelClear

**参数:** `ch`

清空通道

### selectorAddSend

**参数:** `sel, ch, value`

添加发送选择分支

### selectorAddRecv

**参数:** `sel, ch`

添加接收选择分支

### selectorSetDefault

**参数:** `sel, callback`

设置默认选择分支

### selectorSelect

**参数:** `sel`

执行选择，返回第一个就绪的通道操作结果

### generatorNext

**参数:** `gen`

获取生成器下一个值

### generatorCollect

**参数:** `gen`

收集生成器所有值

### generatorReset

**参数:** `gen`

重置生成器
