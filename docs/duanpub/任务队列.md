# 任务队列

> 任务队列系统：异步任务、延迟任务、定时任务、重试策略、优先级、并发控制

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 21 |
| FFI 声明 | 0 |

**关键词:** 任务队列, 异步, 延迟, 定时, 重试

## 导入方式

```duan
导入 任务队列
```

或

```duan
导入 标准任务队列
```

## 函数列表

共 21 个公开函数

> 任务队列 — duanpub 桥接模块
> 
> 基于 Python queue / threading / time 库封装，
> 函数名对齐 duanpub/packages/任务队列/源.duan。
> 
> duanpub 原始包通过 C FFI 实现任务队列系统，
> 本桥接模块用 Python 标准库替代，提供等价的异步/延迟/定时任务功能。

### 取时间戳

获取当前时间戳（秒）

### 创建任务队列

创建一个新的任务队列

### 创建任务

**参数:** `任务函数, 参数, 优先级, 重试次数`

创建一个任务对象

### 任务队列添加任务

**参数:** `队列, 任务`

向队列添加一个任务

### 任务队列添加任务简便

**参数:** `队列, 任务函数, 参数`

简便方式添加任务

### 任务队列添加延迟任务

**参数:** `队列, 任务函数, 延迟秒数, 参数`

添加延迟任务

### 任务队列添加定时任务

**参数:** `队列, 任务函数, 定时时间, 参数`

添加定时任务（定时时间为时间戳）

### 队列触发事件

**参数:** `队列, 事件名`

触发队列事件

### 任务队列处理下一个

**参数:** `队列`

处理队列中的下一个任务，返回是否成功

### 任务队列处理所有

**参数:** `队列`

处理队列中所有任务

### 任务队列处理所有异步

**参数:** `队列`

异步处理所有任务（在新线程中）

### 任务队列暂停

**参数:** `队列`

暂停队列处理

### 任务队列恢复

**参数:** `队列`

恢复队列处理

### 任务队列停止

**参数:** `队列`

停止队列处理

### 任务队列获取统计

**参数:** `队列`

获取队列统计信息

### 任务队列获取任务

**参数:** `队列, 任务id`

根据任务ID获取任务

### 任务队列重试失败任务

**参数:** `队列`

重试所有失败任务

### 任务队列清理已完成

**参数:** `队列`

清理已完成的任务

### 任务队列注册事件监听器

**参数:** `队列, 事件名, 回调函数`

注册事件监听器

### 任务队列处理定时任务

**参数:** `队列`

处理到期的定时任务，将其加入主队列

### 检查定时

**参数:** `队列`

检查定时任务状态，返回已到期数量
