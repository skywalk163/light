# 日期序列

> 日期序列库：时间戳、时区、日期范围、重采样、偏移量、工作日历

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 开发工具 |
| 优先级 | 🔹 扩展包 |
| 公开函数 | 63 |
| FFI 声明 | 0 |

**关键词:** 日期, 时间戳, 时区, 日期范围, 重采样, 工作日历

## 导入方式

```duan
导入 日期序列
```

或

```duan
导入 标准日期序列
```

## 函数列表

共 63 个公开函数

> 日期序列 — duanpub 桥接模块
> 
> 基于 Python datetime / time / calendar 库封装，
> 函数名对齐 duanpub/packages/日期序列/源.duan。
> 
> duanpub 原始包通过 C FFI 实现日期序列功能，
> 本桥接模块用 Python 标准库替代，提供等价的日期序列处理功能。

### 创建时间戳

**参数:** `年, 月, 日, 时, 分, 秒, 微秒, 时区`

创建时间戳

### 创建时间戳从毫秒

**参数:** `毫秒`

从毫秒时间戳创建

### 创建时间戳从字符串

**参数:** `字符串, 格式`

从字符串解析创建时间戳

### 创建时间戳当前

创建当前时间的时间戳

### 创建时间戳今天

创建今天零点的时间戳

### ts格式化

**参数:** `ts, 格式`

格式化时间戳

### tsToISO8601

**参数:** `ts`

转为 ISO 8601 格式

### tsToUnix

**参数:** `ts`

转为 Unix 时间戳（秒）

### tsToMs

**参数:** `ts`

转为 Unix 时间戳（毫秒）

### ts添加

**参数:** `ts, 持续时间`

时间戳加法

### ts减去

**参数:** `ts, 持续时间`

时间戳减法

### ts差值

**参数:** `ts1, ts2`

计算两个时间戳的差值，返回 Duration

### tsIsBefore

**参数:** `ts1, ts2`

判断是否早于

### tsIsAfter

**参数:** `ts1, ts2`

判断是否晚于

### tsIsEqual

**参数:** `ts1, ts2`

判断是否相等

### ts获取星期

**参数:** `ts`

获取星期（1-7，1=星期一）

### ts获取星期名称

**参数:** `ts`

获取星期名称（中文）

### ts获取月份名称

**参数:** `ts`

获取月份名称（中文）

### ts获取月份天数

**参数:** `ts`

获取月份的天数

### ts是否闰年

**参数:** `ts`

判断是否闰年

### tsIsWorkday

**参数:** `ts`

判断是否为工作日

### ts获取季度

**参数:** `ts`

获取季度（1-4）

### ts获取周数

**参数:** `ts`

获取周数（ISO 周数）

### ts复制

**参数:** `ts`

复制时间戳

### ts设置时区

**参数:** `ts, 时区`

设置时区

### ts转为UTC

**参数:** `ts`

转为 UTC 时间

### ts添加天数

**参数:** `ts, 天数`

### ts添加小时

**参数:** `ts, 小时`

### ts添加分钟

**参数:** `ts, 分钟`

### ts添加秒

**参数:** `ts, 秒`

### ts添加月数

**参数:** `ts, 月数`

添加月数

### ts添加年数

**参数:** `ts, 年数`

### 创建日期范围

**参数:** `开始, 结束`

创建日期范围

### 创建日期范围从数量

**参数:** `开始, 数量, 单位, 步长`

从开始时间和数量创建日期范围

### 创建工作日范围

**参数:** `开始, 结束`

创建工作日范围（仅包含工作日）

### dr获取时间戳列表

**参数:** `dr`

获取日期范围内所有时间戳列表

### dr长度

**参数:** `dr`

获取日期范围天数

### dr包含

**参数:** `dr, ts`

判断日期范围是否包含指定时间戳

### 创建持续时间

**参数:** `秒, 分钟, 小时, 天数`

创建持续时间

### durToMs

**参数:** `dur`

持续时间转为毫秒

### dur总秒

*暂无详细文档*

### dur总分钟

*暂无详细文档*

### dur总小时

*暂无详细文档*

### dur总天数

**参数:** `dur`

获取总天数

### dur格式化

**参数:** `dur, 格式`

格式化持续时间

### 创建时区

**参数:** `偏移小时, 偏移分钟`

创建时区

### tz获取当前偏移

**参数:** `tz`

获取当前时区偏移（小时）

### tzIsDST

**参数:** `tz`

判断是否为夏令时

### 创建日期偏移

**参数:** `年, 月, 日`

创建日期偏移

### do应用

**参数:** `offset, ts`

应用日期偏移到时间戳

### 创建重采样器

**参数:** `数据, 频率`

创建重采样器

### rs求和

**参数:** `rs`

重采样求和

### rs平均值

**参数:** `rs`

重采样平均值

### rs最小值

**参数:** `rs`

重采样最小值

### rs最大值

**参数:** `rs`

重采样最大值

### rs首值

**参数:** `rs`

重采样首值

### rs末值

**参数:** `rs`

重采样末值

### rs计数

**参数:** `rs`

重采样计数

### rs标准差

**参数:** `rs`

重采样标准差

### rs前向填充

**参数:** `rs`

重采样前向填充

### rs后向填充

**参数:** `rs`

重采样后向填充

### rs线性插值

**参数:** `rs`

重采样线性插值

### rs应用

**参数:** `rs, 函数`

重采样应用自定义函数
