# B2 自测报告

> **正本已移到仓根 `自测报告_任务B2.md`**（任务书指定的路径）。本文件是首版：
> §2 的测试数字与 skip 清单、§6 的收尾 `git status` 都已过期（下面已就地更正并
> 指向正本）；§3–§5、§7、§8 的结论仍然有效。

## 1. 交付物清单

### 修改文件（5 个）

| 文件 | 改动 |
|------|------|
| `src/llvm/runtime_typed.c` | B2-3: poller 从 select 升级到 WSAPoll/poll（编译期宏切换，动态扩容，不静默丢 fd）；B2-4: Schannel TLS 全接口（wrap/handshake/send/recv/free/set_verify/add_trusted_cert_file/want_event/is_ready/flush_public），握手挂回 IO 等待队列，证书校验默认开启 |
| `src/llvm/codegen_typed.py` | B2-4: TLS 外部函数声明（11 个）+ 内置函数注册（中英文名映射，含 `包装tls`/`tls_wrap`、`握手tls`/`tls_handshake` 等） |
| `src/llvm/compiler.py` | B2-4: 三处链接命令各加 `-lsecur32 -lcrypt32`（Schannel TLS 依赖） |
| `tests/test_llvm_net.py` | B2-1: skip 判据按腿分离（`HAS_CLANG_LEG` / `HAS_MCJIT_LEG`），加 `_check_llvmlite()`，skip reason 逐条写明掩盖了什么；前缀 `_taskB_` → `_taskB2_` |
| `tests/e2e/test_llvm_pipeline.py` | B2-1: 假门禁 `returncode in [0,1]` 改为 `assertEqual(returncode, 0)`；缺 antlr4 时 skip（reason 写明旧路径 IR 生成由 test_llvm_net.py 覆盖） |

### 新建文件（5 个）

| 文件 | 用途 |
|------|------|
| `tests/test_llvm_c_runtime.c` | B2-2: 纯 C 层单元测试，18→21 个测试覆盖 socket/poller/sleep/时间 |
| `tests/test_llvm_c_unit.py` | B2-2: Python 包装，编译 C 测试并启动 echo server 验证 |
| `tests/test_llvm_tls.c` | B2-4: TLS 层 C 测试，正例（握手+收发）+ 负例（证书校验失败） |
| `tests/test_llvm_tls.py` | B2-4: Python 包装，生成自签证书、启动 TLS echo server、编译运行 C 测试 |
| `docs/B2-5_HTTP决策分析.md` | B2-5: HTTP/1.1 决策分析（不在 C 层重写，列出复用 `流式.light` 还差的语言特性） |

## 2. 测试结果

**以下 37/1 是首版数字，已过期**（那次跑在逐腿 mark 与 C 层/TLS 新用例落地之前）。
终态实测见正本 `自测报告_任务B2.md` §3：同样这 5 个文件 **38 passed, 5 skipped**；
再并上 `test_llvm_async/exception/optimization/optimizer` 一次跑
**130 passed, 5 skipped, 26 warnings in 153.46s**，0 failed / 0 error。

```
（过期）pytest tests/test_llvm_net.py tests/test_llvm_c_unit.py tests/test_llvm_tls.py \
       tests/e2e/test_llvm_pipeline.py tests/unit/test_ir_verify.py -v

37 passed, 1 skipped, 1 warning in 61.41s
```

### Skip 清单与掩盖分析

首版只列了 1 条，**漏了 llvmlite 那 4 条**。终态 5 条如下：

| 测试 | skip 原因 | 掩盖了什么 |
|------|----------|-----------|
| `test_llvm_net.py::TestMcjitLegOnly`（2 条） | 缺 llvmlite | IR 能否被 LLVM 官方 `parse_assembly` + `verify` 接受、runtime 符号能否从 DLL 解析 |
| `test_ir_verify.py::TestVerifyIRWithLlvmlite`（2 条） | 缺 llvmlite | 同上（`verify_ir` 的 llvmlite 后端半边） |
| `test_llvm_pipeline.py::test_simple_ir_generation` | 缺 antlr4 | 旧路径 IR 生成（`antlrparser/light_llvm.py`），由 `test_llvm_net.py` 的 clang 腿覆盖 |


### B2-1 skip 判据修复说明

**旧 skip 判据**（`test_llvm_net.py:71-78`）：只检查 clang / MSVC，漏判 llvmlite。
- 本机有 MSVC → `HAS_COMPILER=True` 不 skip → MCJIT 腿（`import llvmlite.binding`）必挂。
- **更正**：本机 clang 也在，改动前实测就是 **10 passed / 62.97s**，不是「10 条全部假红」。
  任务书与总纲写的「10 红」在本机是过期前提，见正本 §4 第 1 条。真问题是判据没按腿分离
  + 断言太松（子串命中就绿）。

**新 skip 判据**：
- `HAS_CLANG_LEG`：检查 clang 存在（clang 端到端腿只需 clang）。
- `HAS_MCJIT_LEG`：检查 llvmlite + MSVC（MCJIT 腿需 llvmlite）。
- 缺哪条腿 skip 哪条，reason 写明「缺 X：Y 端到端行为未验证（Z 全部）」。
- 本机有 clang → clang 腿真绿（10/10 passed）；无 llvmlite → MCJIT 腿 skip（不再假红）。

## 3. B2-2 验证路径评估

三选一评估结果：**选 b + c（附加）**

| 选项 | 评估 | 选/不选 |
|------|------|--------|
| a) 装 llvmlite | 最快但工具链依赖重，且不能替代 C 层验证 | 不选 |
| b) clang 直编 | 链路最接近真实发布形态（IR → .ll → clang → exe），本机已有 clang | **选** |
| c) 纯 C 单测 | 不验证 IR 生成，但立刻给 socket/poller/TLS 层真绿 | **附加** |

## 4. B2-3 poller 升级

| 项目 | 旧 | 新 |
|------|-----|-----|
| 后端 | select | WSAPoll（Windows）/ poll（POSIX），select 为编译期 fallback（`-DDV_POLLER_FORCE_SELECT`） |
| 上限 | `DV_POLLER_MAX 256` 硬编码 | 动态扩容（初始 256，翻倍增长），select fallback 受 FD_SETSIZE 限制但在超限时**明确报错** |
| 静默丢 fd | `num_registered >= DV_POLLER_MAX` 时返回 -1（但事件循环不检查返回值） | `dv_poller_register` 返回 -1 + 写 `g_poller_error`；`dv_poller_wait_n` 超容量返回 -1 + 写错误文本；事件循环检查返回值并打 stderr |
| 事件循环 | `int out_fds[256]` 固定栈分配 | `malloc(num_registered * sizeof(int))` 按注册数分配 |

## 5. B2-4 TLS

**选择 Schannel**（不选 OpenSSL/mbedTLS）：
- 系统自带，不引入第三方依赖，与「不装库」口径一致
- 不改分发形态

**安全红线**：
- 证书校验**默认开启**（`g_tls_verify_default = 1`）
- `dv_tls_set_verify(t, 0)` 关闭时往 stderr 打醒目告警
- `dv_tls_add_trusted_cert_file(path)` 追加显式信任锚，用「独占根」引擎（比系统根更严）
- `dv_tls_wrap` 必须给主机名（SNI + 主机名校验都要它），拒绝无名包装

**握手挂回 IO 等待队列**：
- `dv_tls_handshake` 返回 `DV_TLS_WANT_READ` / `DV_TLS_WANT_WRITE`
- `dv_tls_want_event(t)` 翻译为 `DV_POLL_READ` / `DV_POLL_WRITE`
- 调用方直接喂给 `dv_coro_await_io`，不阻塞事件循环

**测试**（2/2 passed）：
- 正例：添加信任锚 → 握手成功 → send/recv 回显验证
- 负例：不加信任锚 → 握手必须失败（自签证书不在系统根存储中）

## 6. 构建产物与清理

### 测试会生成的产物

| 产物 | 生成方 | 清理方式 |
|------|--------|---------|
| `_taskB2_c_test.exe` | `test_llvm_c_unit.py` 编译 C 测试 | pytest fixture `teardown_class` 自动删除 |
| `_taskB2_tls_test.exe` | `test_llvm_tls.py` 编译 TLS C 测试 | pytest fixture `teardown_class` 自动删除（在 tempfile.mkdtemp 目录里） |
| 自签证书 `.pem` 文件 | `test_llvm_tls.py` 优先用 `cryptography` 生成（PATH 上没 openssl 时才回退，两者都没有则**优雅 skip** 而不是 ERROR） | pytest fixture `teardown_class` 自动 `shutil.rmtree` |

### 已清理的临时文件

上一轮探测脚本产生的 `_taskB2_*.py`、`_taskB2_*.exe`、`_taskB2_*.txt`、`_taskB2_*.ll`、`tests/c/` 目录、`tests/test_runtime_c_net.py`（旧测试文件，已被 `test_llvm_c_unit.py` 取代）——全部已删除。

### 收尾 git status

**下面这份是首版快照，已过期**——那 10 个文件此后已分 5 个 commit 落到 `task-B2`
（`241031be`/`9733bf75`/`4516b9e0`/`ffdb92a2`/`c413a5e8`），所以现在 `git status`
里不会再有它们。终态全量核对见正本 `自测报告_任务B2.md` §6。

```
（过期快照）
 M src/llvm/codegen_typed.py
 M src/llvm/compiler.py
 M src/llvm/runtime_typed.c
 M tests/e2e/test_llvm_pipeline.py
 M tests/test_llvm_net.py
?? docs/B2-5_HTTP决策分析.md
?? tests/test_llvm_c_runtime.c
?? tests/test_llvm_c_unit.py
?? tests/test_llvm_tls.c
?? tests/test_llvm_tls.py
```

积木库文件零变动（`git status --porcelain` 全量输出已逐行确认）。


## 7. B2-5 HTTP/1.1 决策摘要

**结论**：不在 C 层重写 HTTP/1.1，应让 LLVM 后端补齐语言特性后直接复用 `stdlib/流式.light`。

**阻断级缺口**（3 个）：
1. **yield / 生成器**：`流式.light` 的 `read_body()`、`按行读取()`、`读分块()` 全靠 yield 流式产出。LLVM codegen 零支持。
2. **bytes 类型**：HTTP 协议全靠 bytes（`b""`、`b"\r\n"`）。LightValue 只有 type=3 (STR)，无 bytes 类型。
3. **切片 `[start:end]`**：头解析、体切分全用切片。`_gen_typed_index_access` 只处理单整数索引。

**高优先级缺口**（4 个）：`导入 socket`、`.encode()`、socket 方法（recv/sendall/close）、`codecs.getincrementaldecoder`

详见 `docs/B2-5_HTTP决策分析.md`。

## 8. 移交清单

| 原生腿写不出什么 | 临时绕法 | 期望能力 |
|----------------|---------|---------|
| `生成` (yield) | 回调式或收完再返回 | yield + 生成器遍历 |
| `b""` bytes 字面量 | 用字符串代替（二进制不安全） | bytes 类型或二进制安全字符串 |
| `缓冲[0:分隔]` 切片 | 手写循环逐字符拷贝 | `[start:end]` 切片语法 |
| `导入 socket` | C 层已有 `dv_socket_*`，需虚拟模块映射 | `导入 socket` 自动映射到 native socket |
| `.encode("utf-8")` | C 层 `dv_str_encode` 函数 | `.encode()` 方法注册 |
| `codecs.getincrementaldecoder` | 一次性解码 | 增量 UTF-8 解码器 |
| `s.recv()` / `s.sendall()` | `dv_socket_recv` / `dv_socket_send` | socket 对象方法封装 |
