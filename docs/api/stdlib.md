# 标准库 API 参考

> 自动生成 - 请勿手动编辑

---

## 模块: FFI

### 函数 `加载库(路径)`

```light
段落 加载库 接收 路径：文本, 别名：文本：
```

光明 C FFI 模块 - 外部函数接口
提供动态库加载、类型映射、函数调用等功能

### 函数 `获取库(别名)`

```light
段落 获取库 接收 别名：文本：
```

### 函数 `获取类型(类型名)`

```light
段落 获取类型 接收 类型名：文本：
```

### 函数 `声明函数(库别名)`

```light
段落 声明函数 接收 库别名：文本, 函数名：文本, 参数类型列表, 返回类型：文本：
```

### 函数 `调用函数(库别名)`

```light
段落 调用函数 接收 库别名：文本, 函数名：文本, 参数列表：
```

### 函数 `编码文本(甲)`

```light
段落 编码文本 接收 甲：文本：
```

### 函数 `解码文本(甲)`

```light
段落 解码文本 接收 甲：
```

### 函数 `内存分配(大小)`

```light
段落 内存分配 接收 大小：整数：
```

### 函数 `内存释放(指针)`

```light
段落 内存释放 接收 指针：
```

### 函数 `取地址(变量)`

```light
段落 取地址 接收 变量：
```

### 函数 `创建结构体(结构体类, 字段值)`

```light
段落 创建结构体 接收 结构体类, 字段值：
```

### 函数 `获取字段(结构体实例, 字段名)`

```light
段落 获取字段 接收 结构体实例, 字段名：文本：
```

### 函数 `设置字段(结构体实例, 字段名)`

```light
段落 设置字段 接收 结构体实例, 字段名：文本, 值：
```

### 函数 `创建回调(回调类型, 回调函数)`

```light
段落 创建回调 接收 回调类型, 回调函数：
```

### 函数 `关闭所有库：()`

```light
段落 关闭所有库：
```

---

## 模块: 中文文本

### 函数 `中文分词(文本):()`

```light
段落 中文分词(文本):
```

光明标准库 - 中文文本模块
提供中文分词、拼音转换、简繁转换、标点处理、笔画部首、中文数字等功能。
依赖: pypinyin, opencc-python-reimplemented
用法：
从 中文文本 导入 转拼音, 简转繁, 数字转中文。
中文分词：按非中文字符切分

### 函数 `中文逐字(文本):()`

```light
段落 中文逐字(文本):
```

中文逐字：拆分为单个汉字

### 函数 `汉字数量(文本):()`

```light
段落 汉字数量(文本):
```

汉字数量

### 函数 `转拼音(文本,()`

```light
段落 转拼音(文本, 首字母模式=假):
```

转拼音

### 函数 `转拼音列表(文本):()`

```light
段落 转拼音列表(文本):
```

转拼音列表

### 函数 `转带声调拼音(文本):()`

```light
段落 转带声调拼音(文本):
```

转带声调拼音

### 函数 `简转繁(文本):()`

```light
段落 简转繁(文本):
```

简转繁

### 函数 `繁转简(文本):()`

```light
段落 繁转简(文本):
```

繁转简

### 函数 `中文标点转英文(文本):()`

```light
段落 中文标点转英文(文本):
```

中文标点转英文

### 函数 `英文标点转中文(文本):()`

```light
段落 英文标点转中文(文本):
```

英文标点转中文

### 函数 `笔画数(字符):()`

```light
段落 笔画数(字符):
```

笔画数

### 函数 `部首(字符):()`

```light
段落 部首(字符):
```

部首

### 函数 `数字转中文(数值,()`

```light
段落 数字转中文(数值, 大写=假):
```

数字转中文

### 函数 `中文转数字(文本):()`

```light
段落 中文转数字(文本):
```

中文转数字

---

## 模块: 列表工具

### 函数 `求和。()`

```light
段落 求和。
```

光明标准库 - 列表工具模块（纯光明实现）
用法：
从 列表工具 导入 求和, 最大值, 反转列表。
列表求和

### 函数 `最大值。()`

```light
段落 最大值。
```

列表最大值

### 函数 `最小值。()`

```light
段落 最小值。
```

列表最小值

### 函数 `平均值。()`

```light
段落 平均值。
```

列表平均值

### 函数 `反转列表。()`

```light
段落 反转列表。
```

反转列表

### 函数 `包含。()`

```light
段落 包含。
```

判断包含元素

### 函数 `查找索引。()`

```light
段落 查找索引。
```

查找元素索引

### 函数 `计数。()`

```light
段落 计数。
```

计数元素出现次数

### 函数 `连接。()`

```light
段落 连接。
```

列表连接

### 函数 `范围。()`

```light
段落 范围。
```

范围生成

---

## 模块: 历法

### 函数 `公历转农历(年,()`

```light
段落 公历转农历(年, 月, 日):
```

光明标准库 - 历法模块
提供公历农历转换、节气查询、天干地支、生肖、农历月日名等功能。
依赖: lunardate
用法：
从 历法 导入 公历转农历, 干支纪年, 生肖。
公历转农历

### 函数 `农历转公历(年,()`

```light
段落 农历转公历(年, 月, 日, 闰月=假):
```

农历转公历

### 函数 `节气(年,()`

```light
段落 节气(年, 序号):
```

节气

### 函数 `节气列表(年):()`

```light
段落 节气列表(年):
```

节气列表

### 函数 `查找节气(年,()`

```light
段落 查找节气(年, 名称):
```

查找节气

### 函数 `天干(序号):()`

```light
段落 天干(序号):
```

天干

### 函数 `地支(序号):()`

```light
段落 地支(序号):
```

地支

### 函数 `干支(序号):()`

```light
段落 干支(序号):
```

干支

### 函数 `干支纪年(年):()`

```light
段落 干支纪年(年):
```

干支纪年

### 函数 `干支纪月(年,()`

```light
段落 干支纪月(年, 月):
```

干支纪月

### 函数 `干支纪日(年,()`

```light
段落 干支纪日(年, 月, 日):
```

干支纪日

### 函数 `生肖(年):()`

```light
段落 生肖(年):
```

生肖

### 函数 `农历月名(月,()`

```light
段落 农历月名(月, 闰月=假):
```

农历月名

### 函数 `农历日名(日):()`

```light
段落 农历日名(日):
```

农历日名

### 函数 `农历完整日期(年,()`

```light
段落 农历完整日期(年, 月, 日, 闰月=假):
```

农历完整日期

---

## 模块: 排版

### 函数 `插入中英文空格(文本):()`

```light
段落 插入中英文空格(文本):
```

光明标准库 - 排版模块
提供中英文混排空格、全半角转换、标点规范、引号规范、排版检查等功能。
参考《中文文案排版指北》。
用法：
从 排版 导入 插入中英文空格, 自动排版, 检查排版。
中英文之间插入空格

### 函数 `转全角(文本):()`

```light
段落 转全角(文本):
```

转全角

### 函数 `转半角(文本):()`

```light
段落 转半角(文本):
```

转半角

### 函数 `规范化标点(文本):()`

```light
段落 规范化标点(文本):
```

规范化标点

### 函数 `规范化引号(文本):()`

```light
段落 规范化引号(文本):
```

规范化引号

### 函数 `检查排版(文本):()`

```light
段落 检查排版(文本):
```

检查排版

### 函数 `自动排版(文本):()`

```light
段落 自动排版(文本):
```

自动排版

---
