# 时间管理 API

> 模块路径：`stdlib/时间管理.py`
> 导入方式：`从 时间管理 导入 函数名` 或 `导入 时间管理`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `睡眠(秒数)` | 睡眠指定秒数 |
| `睡眠毫秒(毫秒数)` | 睡眠指定毫秒数 |
| `精确睡眠(秒数)` | 更精确的睡眠（使用高精度计时器） |
| `计时(函数)` | 计时装饰器 |
| `计时函数(函数)` | 计时函数执行，返回(结果, 耗时秒数) |
| `多次计时(函数, 次数)` | 多次计时函数，返回统计信息 |
| `时间戳()` | 获取当前时间戳（秒） |
| `时间戳毫秒()` | 获取当前时间戳（毫秒） |
| `性能计数器()` | 获取高精度性能计数器值 |
| `性能计数器纳秒()` | 获取高精度性能计数器值（纳秒） |
| `进程时间()` | 获取进程CPU时间（秒） |
| `线程时间()` | 获取线程CPU时间（秒） |
| `格式化耗时(秒数)` | 将秒数格式化为可读字符串 |
| `时间戳纳秒()` | 获取当前时间戳（纳秒级整数） |
| `休眠(秒数)` | 休眠指定秒数 |
| `等待(秒数)` | 等待指定秒数（休眠的别名） |
| `单调时间()` | 获取单调时钟值（不会因为系统时间调整而倒退） |
| `测量执行时间(函数)` | 测量函数执行时间 |
| `本地时间(时间戳)` | 获取本地时间元组 |
| `UTC时间(时间戳)` | 获取UTC时间元组 |
| `时间元组转时间戳(时间元组)` | 时间元组转时间戳 |
| `格式化时间(格式, 时间戳)` | 格式化时间 |
| `解析时间(时间字符串, 格式)` | 解析时间字符串为时间元组 |
| `时区偏移()` | 获取本地时区与UTC的偏移量（秒） |
| `时区名称()` | 获取本地时区名称 |
| `夏令时()` | 当前是否为夏令时 |
| `创建秒表(自动启动)` | 创建秒表（便捷函数） |
| `__init__(self)` |  |
| `开始(self)` | 开始计时 |
| `结束(self)` | 结束计时，返回经过的秒数 |
| `经过时间(self)` | 获取当前经过的时间（不停止计时） |
| `重置(self)` | 重置计时器 |
| `打点(self, 名称)` | 记录一个时间点 |
| `获取记录(self)` | 获取所有打点记录 |
| `打印记录(self)` | 打印所有打点记录 |
| `__enter__(self)` |  |
| `__exit__(self, 异常类型, 异常值, 追溯)` |  |
| `包装器()` |  |
| `__init__(self, 延迟秒数, 回调函数)` |  |
| `开始(self)` | 启动定时器 |
| `_执行回调(self)` | 执行回调函数 |
| `取消(self)` | 取消定时器 |
| `是否运行中(self)` | 检查定时器是否在运行 |
| `__init__(self, 间隔秒数, 回调函数)` |  |
| `开始(self)` | 启动周期定时器 |
| `_运行循环(self)` | 运行循环 |
| `停止(self)` | 停止周期定时器 |
| `是否运行中(self)` | 检查定时器是否在运行 |
| `__init__(self, 总秒数)` |  |
| `开始(self)` | 开始倒计时 |
| `暂停(self)` | 暂停倒计时 |
| `继续(self)` | 继续倒计时 |
| `重置(self, 新总秒数)` | 重置倒计时 |
| `剩余时间(self)` | 获取剩余时间 |
| `已过时间(self)` | 获取已过时间 |
| `是否结束(self)` | 检查倒计时是否结束 |
| `是否运行中(self)` | 检查倒计时是否在运行 |
| `等待结束(self)` | 等待倒计时结束 |
| `__init__(self, 自动启动)` |  |
| `启动(self)` | 启动秒表 |
| `停止(self)` | 停止秒表，返回累计时间 |
| `重置(self)` | 重置秒表 |
| `读取(self)` | 读取当前经过时间（不停止） |
| `打点(self, 名称)` | 记录一个时间点，返回从开始到现在的时间 |

---

## 函数详情

### `睡眠(秒数: float)`

睡眠指定秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒数` | `float` |  |

---

### `睡眠毫秒(毫秒数: int)`

睡眠指定毫秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `毫秒数` | `int` |  |

---

### `精确睡眠(秒数: float)`

更精确的睡眠（使用高精度计时器）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒数` | `float` |  |

---

### `计时(函数: Callable) -> Callable`

计时装饰器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `Callable` |  |

**返回：** `Callable`

---

### `计时函数(函数: Callable) -> tuple`

计时函数执行，返回(结果, 耗时秒数)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `Callable` |  |

**返回：** `tuple`

---

### `多次计时(函数: Callable, 次数: int = 10) -> Dict[str, float]`

多次计时函数，返回统计信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `Callable` |  |
| `次数` | `int` | （默认：10） |

**返回：** `Dict[str, float]`

---

### `时间戳() -> float`

获取当前时间戳（秒）

**参数：**

无参数。

**返回：** `float`

---

### `时间戳毫秒() -> int`

获取当前时间戳（毫秒）

**参数：**

无参数。

**返回：** `int`

---

### `性能计数器() -> float`

获取高精度性能计数器值

**参数：**

无参数。

**返回：** `float`

---

### `性能计数器纳秒() -> int`

获取高精度性能计数器值（纳秒）

**参数：**

无参数。

**返回：** `int`

---

### `进程时间() -> float`

获取进程CPU时间（秒）

**参数：**

无参数。

**返回：** `float`

---

### `线程时间() -> float`

获取线程CPU时间（秒）

**参数：**

无参数。

**返回：** `float`

---

### `格式化耗时(秒数: float) -> str`

将秒数格式化为可读字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒数` | `float` |  |

**返回：** `str`

---

### `时间戳纳秒() -> int`

获取当前时间戳（纳秒级整数）

**参数：**

无参数。

**返回：** `int`

---

### `休眠(秒数: float) -> None`

休眠指定秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒数` | `float` |  |

**返回：** `None`

---

### `等待(秒数: float) -> None`

等待指定秒数（休眠的别名）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒数` | `float` |  |

**返回：** `None`

---

### `单调时间() -> float`

获取单调时钟值（不会因为系统时间调整而倒退）

返回:
    单调时钟值（秒）

**参数：**

无参数。

**返回：** `float`

---

### `测量执行时间(函数) -> tuple`

测量函数执行时间

参数:
    函数: 要测量的函数
    参数: 函数位置参数
    关键字参数: 函数关键字参数

返回:
    (返回值, 耗时秒数)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `None` |  |

**返回：** `tuple`

---

### `本地时间(时间戳: float = None) -> time.struct_time`

获取本地时间元组

参数:
    时间戳: 时间戳，None表示当前时间

返回:
    时间元组

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `时间戳` | `float` | （默认：None） |

**返回：** `time.struct_time`

---

### `UTC时间(时间戳: float = None) -> time.struct_time`

获取UTC时间元组

参数:
    时间戳: 时间戳，None表示当前时间

返回:
    UTC时间元组

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `时间戳` | `float` | （默认：None） |

**返回：** `time.struct_time`

---

### `时间元组转时间戳(时间元组: time.struct_time) -> float`

时间元组转时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `时间元组` | `time.struct_time` |  |

**返回：** `float`

---

### `格式化时间(格式: str = '%Y-%m-%d %H:%M:%S', 时间戳: float = None) -> str`

格式化时间

参数:
    格式: 格式化字符串
    时间戳: 时间戳，None表示当前时间

返回:
    格式化后的时间字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `格式` | `str` | （默认：'%Y-%m-%d %H:%M:%S'） |
| `时间戳` | `float` | （默认：None） |

**返回：** `str`

---

### `解析时间(时间字符串: str, 格式: str = '%Y-%m-%d %H:%M:%S') -> time.struct_time`

解析时间字符串为时间元组

参数:
    时间字符串: 时间字符串
    格式: 格式化字符串

返回:
    时间元组

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `时间字符串` | `str` |  |
| `格式` | `str` | （默认：'%Y-%m-%d %H:%M:%S'） |

**返回：** `time.struct_time`

---

### `时区偏移() -> int`

获取本地时区与UTC的偏移量（秒）

返回:
    偏移秒数，负数表示在UTC以东（如中国为-28800）

**参数：**

无参数。

**返回：** `int`

---

### `时区名称() -> str`

获取本地时区名称

**参数：**

无参数。

**返回：** `str`

---

### `夏令时() -> bool`

当前是否为夏令时

**参数：**

无参数。

**返回：** `bool`

---

### `创建秒表(自动启动: bool = True) -> 秒表`

创建秒表（便捷函数）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `自动启动` | `bool` | （默认：True） |

**返回：** `秒表`

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `开始(self)`

开始计时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `结束(self) -> float`

结束计时，返回经过的秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `float`

---

### `经过时间(self) -> float`

获取当前经过的时间（不停止计时）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `float`

---

### `重置(self)`

重置计时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `打点(self, 名称: str = '')`

记录一个时间点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `str` | （默认：''） |

---

### `获取记录(self) -> list`

获取所有打点记录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `list`

---

### `打印记录(self)`

打印所有打点记录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__enter__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__exit__(self, 异常类型, 异常值, 追溯)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `异常类型` | `None` |  |
| `异常值` | `None` |  |
| `追溯` | `None` |  |

---

### `包装器()`

**参数：**

无参数。

---

### `__init__(self, 延迟秒数: float, 回调函数: Callable)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `延迟秒数` | `float` |  |
| `回调函数` | `Callable` |  |

---

### `开始(self)`

启动定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `_执行回调(self)`

执行回调函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `取消(self)`

取消定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `是否运行中(self) -> bool`

检查定时器是否在运行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `bool`

---

### `__init__(self, 间隔秒数: float, 回调函数: Callable)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `间隔秒数` | `float` |  |
| `回调函数` | `Callable` |  |

---

### `开始(self)`

启动周期定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `_运行循环(self)`

运行循环

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `停止(self)`

停止周期定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `是否运行中(self) -> bool`

检查定时器是否在运行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `bool`

---

### `__init__(self, 总秒数: float)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `总秒数` | `float` |  |

---

### `开始(self)`

开始倒计时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `暂停(self)`

暂停倒计时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `继续(self)`

继续倒计时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `重置(self, 新总秒数: float = None)`

重置倒计时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `新总秒数` | `float` | （默认：None） |

---

### `剩余时间(self) -> float`

获取剩余时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `float`

---

### `已过时间(self) -> float`

获取已过时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `float`

---

### `是否结束(self) -> bool`

检查倒计时是否结束

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `bool`

---

### `是否运行中(self) -> bool`

检查倒计时是否在运行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `bool`

---

### `等待结束(self)`

等待倒计时结束

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 自动启动: bool = True)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `自动启动` | `bool` | （默认：True） |

---

### `启动(self) -> None`

启动秒表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `None`

---

### `停止(self) -> float`

停止秒表，返回累计时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `float`

---

### `重置(self) -> None`

重置秒表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `None`

---

### `读取(self) -> float`

读取当前经过时间（不停止）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `float`

---

### `打点(self, 名称: str = None) -> float`

记录一个时间点，返回从开始到现在的时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `str` | （默认：None） |

**返回：** `float`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `结束时间` | `time.perf_counter() + 秒数` |
| `开始` | `time.perf_counter()` |
| `结果` | `函数(*参数, **关键字参数)` |
| `结束` | `time.perf_counter()` |
| `时间列表` | `[]` |
| `结果` | `None` |
| `开始` | `time.perf_counter()` |
| `结果` | `函数(*参数, **关键字参数)` |
| `结束` | `time.perf_counter()` |
| `当前时间` | `self.经过时间()` |
| `开始` | `time.perf_counter()` |
| `结果` | `函数(*参数, **关键字参数)` |
| `结束` | `time.perf_counter()` |
| `开始` | `time.perf_counter()` |
| `结果` | `函数(*参数, **关键字参数)` |
| `结束` | `time.perf_counter()` |
| `已过` | `time.perf_counter() - self._开始时间` |
| `当前时间` | `self.读取()` |
| `分钟` | `int(秒数 // 60)` |
| `秒` | `秒数 % 60` |
| `小时` | `int(秒数 // 3600)` |
| `剩余` | `秒数 % 3600` |
| `分钟` | `int(剩余 // 60)` |
| `秒` | `剩余 % 60` |
