# 任务队列 API

> 模块路径：`stdlib/任务队列.py`
> 导入方式：`从 任务队列 导入 函数名` 或 `导入 任务队列`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `取时间戳()` | 获取当前时间戳（秒） |
| `创建任务队列()` | 创建一个新的任务队列 |
| `创建任务(任务函数, 参数, 优先级, 重试次数)` | 创建一个任务对象 |
| `任务队列添加任务(队列, 任务)` | 向队列添加一个任务 |
| `任务队列添加任务简便(队列, 任务函数, 参数)` | 简便方式添加任务 |
| `任务队列添加延迟任务(队列, 任务函数, 延迟秒数, 参数)` | 添加延迟任务 |
| `任务队列添加定时任务(队列, 任务函数, 定时时间, 参数)` | 添加定时任务（定时时间为时间戳） |
| `队列触发事件(队列, 事件名)` | 触发队列事件 |
| `任务队列注册事件监听器(队列, 事件名, 回调函数)` | 注册事件监听器 |
| `任务队列处理下一个(队列)` | 处理队列中的下一个任务，返回是否成功 |
| `任务队列处理所有(队列)` | 处理队列中所有任务 |
| `任务队列处理所有异步(队列)` | 异步处理所有任务（在新线程中） |
| `任务队列暂停(队列)` | 暂停队列处理 |
| `任务队列恢复(队列)` | 恢复队列处理 |
| `任务队列停止(队列)` | 停止队列处理 |
| `任务队列获取统计(队列)` | 获取队列统计信息 |
| `任务队列获取任务(队列, 任务id)` | 根据任务ID获取任务 |
| `任务队列重试失败任务(队列)` | 重试所有失败任务 |
| `任务队列清理已完成(队列)` | 清理已完成的任务 |
| `任务队列处理定时任务(队列)` | 处理到期的定时任务，将其加入主队列 |
| `检查定时(队列)` | 检查定时任务状态，返回已到期数量 |
| `__init__(self, id, 任务函数, 参数, 延迟, 定时时间, 优先级, 重试次数)` |  |
| `__init__(self)` |  |
| `_run()` |  |

---

## 函数详情

### `取时间戳()`

获取当前时间戳（秒）

**参数：**

无参数。

---

### `创建任务队列()`

创建一个新的任务队列

**参数：**

无参数。

---

### `创建任务(任务函数, 参数 = None, 优先级 = 0, 重试次数 = 0)`

创建一个任务对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `任务函数` | `None` |  |
| `参数` | `None` | （默认：None） |
| `优先级` | `None` | （默认：0） |
| `重试次数` | `None` | （默认：0） |

---

### `任务队列添加任务(队列, 任务)`

向队列添加一个任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `任务` | `None` |  |

---

### `任务队列添加任务简便(队列, 任务函数, 参数 = None)`

简便方式添加任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `任务函数` | `None` |  |
| `参数` | `None` | （默认：None） |

---

### `任务队列添加延迟任务(队列, 任务函数, 延迟秒数, 参数 = None)`

添加延迟任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `任务函数` | `None` |  |
| `延迟秒数` | `None` |  |
| `参数` | `None` | （默认：None） |

---

### `任务队列添加定时任务(队列, 任务函数, 定时时间, 参数 = None)`

添加定时任务（定时时间为时间戳）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `任务函数` | `None` |  |
| `定时时间` | `None` |  |
| `参数` | `None` | （默认：None） |

---

### `队列触发事件(队列, 事件名)`

触发队列事件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `事件名` | `None` |  |

---

### `任务队列注册事件监听器(队列, 事件名, 回调函数)`

注册事件监听器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `事件名` | `None` |  |
| `回调函数` | `None` |  |

---

### `任务队列处理下一个(队列)`

处理队列中的下一个任务，返回是否成功

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列处理所有(队列)`

处理队列中所有任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列处理所有异步(队列)`

异步处理所有任务（在新线程中）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列暂停(队列)`

暂停队列处理

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列恢复(队列)`

恢复队列处理

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列停止(队列)`

停止队列处理

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列获取统计(队列)`

获取队列统计信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列获取任务(队列, 任务id)`

根据任务ID获取任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `任务id` | `None` |  |

---

### `任务队列重试失败任务(队列)`

重试所有失败任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列清理已完成(队列)`

清理已完成的任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `任务队列处理定时任务(队列)`

处理到期的定时任务，将其加入主队列

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `检查定时(队列)`

检查定时任务状态，返回已到期数量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `__init__(self, id, 任务函数, 参数 = None, 延迟 = 0, 定时时间 = None, 优先级 = 0, 重试次数 = 0)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `id` | `None` |  |
| `任务函数` | `None` |  |
| `参数` | `None` | （默认：None） |
| `延迟` | `None` | （默认：0） |
| `定时时间` | `None` | （默认：None） |
| `优先级` | `None` | （默认：0） |
| `重试次数` | `None` | （默认：0） |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `_run()`

**参数：**

无参数。

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `任务` | `创建任务(任务函数, 参数)` |
| `任务` | `创建任务(任务函数, 参数)` |
| `任务` | `创建任务(任务函数, 参数)` |
| `now` | `_time.time()` |
| `count` | `0` |
| `t` | `_threading.Thread(target=_run)` |
| `now` | `_time.time()` |
| `count` | `0` |
| `监听器列表` | `队列.事件监听器.get(事件名, [])` |
| `ready_delayed` | `[t for t in 队列.延迟队列 if now - t.创建时间 >= t.延迟]` |
| `ready_timed` | `[t for t in 队列.定时任务列表 if t.定时时间 and now >= t.定时时间]` |
| `任务` | `队列.队列.popleft()` |
| `failed` | `list(队列.失败列表)` |
| `count` | `len(队列.已完成列表)` |
| `ready` | `[t for t in 队列.定时任务列表 if t.定时时间 and now >= t.定时时间]` |
