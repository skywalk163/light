# 模板渲染 API

> 模块路径：`stdlib/模板渲染.py`
> 导入方式：`从 模板渲染 导入 函数名` 或 `导入 模板渲染`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `范围(开始, 结束)` | 生成范围列表 |
| `整数转字符串(值)` | 整数转字符串 |
| `内部包含(文本, 子串)` | 检查是否包含子串 |
| `内部以开始(文本, 前缀)` | 检查是否以指定前缀开始 |
| `内部分割字符串(文本, 分隔符)` | 分割字符串 |
| `内部去除空白(文本)` | 去除首尾空白 |
| `内部转为小写(文本)` | 转为小写 |
| `内部转为大写(文本)` | 转为大写 |
| `内部查找字符(文本, 字符)` | 查找字符位置 |
| `内部值转字符串(值)` | 值转字符串 |
| `内部HTML转义(文本)` | HTML 转义 |
| `内部HTML反转义(文本)` | HTML 反转义 |
| `内部替换所有(文本, 旧, 新)` | 替换所有 |
| `内部URL编码(文本)` | URL 编码 |
| `内部转为大写过滤器(值)` | 大写过滤器 |
| `内部转为小写过滤器(值)` | 小写过滤器 |
| `内部首字母大写过滤器(值)` | 首字母大写过滤器 |
| `内部截断过滤器(值, 长度)` | 截断过滤器 |
| `内部默认值过滤器(值, 默认)` | 默认值过滤器 |
| `内部去除空白过滤器(值)` | 去除空白过滤器 |
| `内部JSON编码过滤器(值)` | JSON 编码过滤器 |
| `内部URL编码过滤器(值)` | URL 编码过滤器 |
| `内部HTML转义过滤器(值)` | HTML 转义过滤器 |
| `内部换行转BR过滤器(值)` | 换行转 BR 过滤器 |
| `内部数字格式化过滤器(值, 小数位)` | 数字格式化过滤器 |
| `engineRegisterFilter(engine, name, func)` | 注册模板过滤器 |
| `engineRegisterGlobal(engine, name, value)` | 注册全局变量 |
| `engineRender(engine, template_name, context)` | 渲染模板文件 |
| `engineRenderString(engine, template_string, context)` | 渲染模板字符串 |
| `engineClearCache(engine)` | 清空模板缓存 |
| `renderTemplateContent(内容, 上下文, 引擎)` | 渲染模板内容 |
| `renderRawBlock(内容, 上下文, 引擎)` | 渲染原始文本块 |
| `renderComments(内容, 上下文, 引擎)` | 渲染注释（忽略） |
| `renderExtendsBlock(内容, 上下文, 引擎)` | 渲染继承块（简化实现，返回空） |
| `extractBlockDefs(内容)` | 提取块定义（简化实现，返回空字典） |
| `renderBlockDefs(块定义, 块名, 上下文, 引擎)` | 渲染块定义 |
| `renderIncludeBlock(内容, 上下文, 引擎)` | 渲染包含块 |
| `renderIfBlock(内容, 条件, 上下文, 引擎)` | 渲染条件块 |
| `renderForBlock(内容, 变量名, 列表, 上下文, 引擎)` | 渲染循环块 |
| `renderVariables(内容, 上下文, 引擎)` | 渲染变量 |
| `findBlockEnd(内容, 起始位置, 块类型)` | 查找块结束位置（简化实现） |
| `findBlockInString(内容, 块名)` | 在字符串中查找块（简化实现） |
| `evaluateCondition(条件, 上下文)` | 评估条件表达式 |
| `evaluateSimpleCondition(条件, 上下文)` | 评估简单条件 |
| `创建tpl引擎()` | 创建模板引擎 |
| `__init__(self)` |  |
| `register_filter(self, name, func)` |  |
| `register_global(self, name, value)` |  |
| `clear_cache(self)` |  |

---

## 函数详情

### `范围(开始, 结束)`

生成范围列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `开始` | `None` |  |
| `结束` | `None` |  |

---

### `整数转字符串(值)`

整数转字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部包含(文本, 子串)`

检查是否包含子串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `子串` | `None` |  |

---

### `内部以开始(文本, 前缀)`

检查是否以指定前缀开始

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `前缀` | `None` |  |

---

### `内部分割字符串(文本, 分隔符)`

分割字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `分隔符` | `None` |  |

---

### `内部去除空白(文本)`

去除首尾空白

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `内部转为小写(文本)`

转为小写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `内部转为大写(文本)`

转为大写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `内部查找字符(文本, 字符)`

查找字符位置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `字符` | `None` |  |

---

### `内部值转字符串(值)`

值转字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部HTML转义(文本)`

HTML 转义

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `内部HTML反转义(文本)`

HTML 反转义

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `内部替换所有(文本, 旧, 新)`

替换所有

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `旧` | `None` |  |
| `新` | `None` |  |

---

### `内部URL编码(文本)`

URL 编码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `内部转为大写过滤器(值)`

大写过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部转为小写过滤器(值)`

小写过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部首字母大写过滤器(值)`

首字母大写过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部截断过滤器(值, 长度 = 100)`

截断过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |
| `长度` | `None` | （默认：100） |

---

### `内部默认值过滤器(值, 默认 = '')`

默认值过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |
| `默认` | `None` | （默认：''） |

---

### `内部去除空白过滤器(值)`

去除空白过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部JSON编码过滤器(值)`

JSON 编码过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部URL编码过滤器(值)`

URL 编码过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部HTML转义过滤器(值)`

HTML 转义过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部换行转BR过滤器(值)`

换行转 BR 过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `内部数字格式化过滤器(值, 小数位 = 2)`

数字格式化过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |
| `小数位` | `None` | （默认：2） |

---

### `engineRegisterFilter(engine, name, func)`

注册模板过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `engine` | `None` |  |
| `name` | `None` |  |
| `func` | `None` |  |

---

### `engineRegisterGlobal(engine, name, value)`

注册全局变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `engine` | `None` |  |
| `name` | `None` |  |
| `value` | `None` |  |

---

### `engineRender(engine, template_name, context = None)`

渲染模板文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `engine` | `None` |  |
| `template_name` | `None` |  |
| `context` | `None` | （默认：None） |

---

### `engineRenderString(engine, template_string, context = None)`

渲染模板字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `engine` | `None` |  |
| `template_string` | `None` |  |
| `context` | `None` | （默认：None） |

---

### `engineClearCache(engine)`

清空模板缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `engine` | `None` |  |

---

### `renderTemplateContent(内容, 上下文, 引擎 = None)`

渲染模板内容

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `renderRawBlock(内容, 上下文, 引擎 = None)`

渲染原始文本块

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `renderComments(内容, 上下文, 引擎 = None)`

渲染注释（忽略）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `renderExtendsBlock(内容, 上下文, 引擎 = None)`

渲染继承块（简化实现，返回空）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `extractBlockDefs(内容)`

提取块定义（简化实现，返回空字典）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |

---

### `renderBlockDefs(块定义, 块名, 上下文, 引擎 = None)`

渲染块定义

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `块定义` | `None` |  |
| `块名` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `renderIncludeBlock(内容, 上下文, 引擎 = None)`

渲染包含块

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `renderIfBlock(内容, 条件, 上下文, 引擎 = None)`

渲染条件块

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `条件` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `renderForBlock(内容, 变量名, 列表, 上下文, 引擎 = None)`

渲染循环块

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `变量名` | `None` |  |
| `列表` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `renderVariables(内容, 上下文, 引擎 = None)`

渲染变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `上下文` | `None` |  |
| `引擎` | `None` | （默认：None） |

---

### `findBlockEnd(内容, 起始位置, 块类型 = '')`

查找块结束位置（简化实现）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `起始位置` | `None` |  |
| `块类型` | `None` | （默认：''） |

---

### `findBlockInString(内容, 块名)`

在字符串中查找块（简化实现）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |
| `块名` | `None` |  |

---

### `evaluateCondition(条件, 上下文)`

评估条件表达式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件` | `None` |  |
| `上下文` | `None` |  |

---

### `evaluateSimpleCondition(条件, 上下文)`

评估简单条件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件` | `None` |  |
| `上下文` | `None` |  |

---

### `创建tpl引擎()`

创建模板引擎

**参数：**

无参数。

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `register_filter(self, name, func)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `name` | `None` |  |
| `func` | `None` |  |

---

### `register_global(self, name, value)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `name` | `None` |  |
| `value` | `None` |  |

---

### `clear_cache(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `s` | `str(值)` |
| `s` | `str(值)` |
| `template` | `engine._cache.get(template_name)` |
| `result` | `[]` |
| `tpl` | `_string.Template(内容)` |
| `mapping` | `dict(上下文)` |
| `ctx` | `dict(上下文)` |
| `parts` | `条件.split('==', 1)` |
| `left` | `上下文.get(parts[0].strip(), parts[0].strip())` |
| `right` | `上下文.get(parts[1].strip(), parts[1].strip())` |
| `parts` | `条件.split('!=', 1)` |
| `left` | `上下文.get(parts[0].strip(), parts[0].strip())` |
| `right` | `上下文.get(parts[1].strip(), parts[1].strip())` |
| `parts` | `条件.split('>', 1)` |
| `parts` | `条件.split('<', 1)` |
| `left` | `float(上下文.get(parts[0].strip(), parts[0].strip()))` |
| `right` | `float(上下文.get(parts[1].strip(), parts[1].strip()))` |
| `left` | `float(上下文.get(parts[0].strip(), parts[0].strip()))` |
| `right` | `float(上下文.get(parts[1].strip(), parts[1].strip()))` |
