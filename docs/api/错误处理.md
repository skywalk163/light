# 错误处理 API

> 模块路径：`stdlib/错误处理.py`
> 导入方式：`从 错误处理 导入 函数名` 或 `导入 错误处理`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建错误(消息)` | 创建一个错误对象 |
| `包装错误(错误, 上下文)` | 包装错误，添加上下文信息 |
| `错误链_获取(错误)` | 获取错误链，返回错误消息列表 |
| `结果成功(值)` | 创建一个成功的 Result |
| `结果失败(错误)` | 创建一个失败的 Result |
| `是成功_检查(result)` | 检查 Result 是否成功 |
| `是失败_检查(result)` | 检查 Result 是否失败 |
| `取值_获取(result)` | 获取 Result 的值，失败时抛出异常 |
| `或默认_获取(result, 默认值)` | 获取 Result 的值，失败时返回默认值 |
| `错误消息_获取(result)` | 获取 Result 的错误消息 |
| `然后_链式(result, 函数)` | 链式调用：如果 Result 成功则应用函数，否则返回原失败 |
| `设置Panic钩子(钩子函数)` | 设置 Panic 钩子函数 |
| `触发Panic(消息)` | 触发 Panic，调用钩子函数 |
| `恢复Panic钩子()` | 恢复 Panic 钩子为默认值 |
| `__init__(self, success, value, error)` |  |
| `is_success(self)` |  |
| `is_failure(self)` |  |
| `get_value(self)` |  |
| `get_error(self)` |  |
| `__init__(self, message, cause)` |  |
| `__str__(self)` |  |

---

## 函数详情

### `创建错误(消息)`

创建一个错误对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `None` |  |

---

### `包装错误(错误, 上下文)`

包装错误，添加上下文信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `错误` | `None` |  |
| `上下文` | `None` |  |

---

### `错误链_获取(错误)`

获取错误链，返回错误消息列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `错误` | `None` |  |

---

### `结果成功(值)`

创建一个成功的 Result

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `结果失败(错误)`

创建一个失败的 Result

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `错误` | `None` |  |

---

### `是成功_检查(result)`

检查 Result 是否成功

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `result` | `None` |  |

---

### `是失败_检查(result)`

检查 Result 是否失败

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `result` | `None` |  |

---

### `取值_获取(result)`

获取 Result 的值，失败时抛出异常

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `result` | `None` |  |

---

### `或默认_获取(result, 默认值)`

获取 Result 的值，失败时返回默认值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `result` | `None` |  |
| `默认值` | `None` |  |

---

### `错误消息_获取(result)`

获取 Result 的错误消息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `result` | `None` |  |

---

### `然后_链式(result, 函数)`

链式调用：如果 Result 成功则应用函数，否则返回原失败

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `result` | `None` |  |
| `函数` | `None` |  |

---

### `设置Panic钩子(钩子函数)`

设置 Panic 钩子函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `钩子函数` | `None` |  |

---

### `触发Panic(消息)`

触发 Panic，调用钩子函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `None` |  |

---

### `恢复Panic钩子()`

恢复 Panic 钩子为默认值

**参数：**

无参数。

---

### `__init__(self, success, value = None, error = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `success` | `None` |  |
| `value` | `None` | （默认：None） |
| `error` | `None` | （默认：None） |

---

### `is_success(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `is_failure(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `get_value(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `get_error(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, message, cause = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `message` | `None` |  |
| `cause` | `None` | （默认：None） |

---

### `__str__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `result` | `[]` |
| `current` | `错误` |
| `error` | `result.get_error()` |
| `current` | `current.cause` |
| `错误` | `_Error(错误)` |
