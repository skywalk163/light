# 异步运行时 API

> 模块路径：`stdlib/异步运行时.py`
> 导入方式：`从 异步运行时 导入 函数名` 或 `导入 异步运行时`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建Future()` | 创建一个 Future 对象 |
| `创建Future完成(result)` | 创建一个已完成的 Future 对象 |
| `创建Future失败(exception)` | 创建一个已失败的 Future 对象 |
| `未来完成(fut)` | 设置 Future 为完成状态 |
| `未来拒绝(fut, exception)` | 设置 Future 为拒绝状态 |
| `未来等待(fut)` | 等待 Future 完成 |
| `未来等待超时(fut, timeout)` | 等待 Future 完成，超时返回 None |
| `未来添加回调(fut, callback)` | 添加 Future 完成回调 |
| `未来添加错误回调(fut, callback)` | 添加 Future 错误回调 |
| `未来是否完成(fut)` | 检查 Future 是否完成 |
| `未来是否失败(fut)` | 检查 Future 是否失败 |
| `未来链式调用(fut, callback)` | 链式调用 Future |
| `未来链式回调(fut, callback)` | 链式回调 Future |
| `未来异常处理(fut, err_callback)` | Future 异常处理 |
| `未来取消(fut)` | 取消 Future |
| `未来获取结果(fut)` | 获取 Future 结果 |
| `创建Promise()` | 创建一个 Promise 对象 |
| `Promise执行(promise, executor)` | 执行 Promise |
| `Promise解决(promise, value)` | 解决 Promise |
| `Promise拒绝(promise, reason)` | 拒绝 Promise |
| `Promise然后(promise, callback)` | Promise then 链式调用 |
| `Promise捕获(promise, callback)` | Promise catch 错误处理 |
| `Promise最终(promise, callback)` | Promise finally 最终处理 |
| `Promise全部(promises)` | 等待所有 Promise 完成 |
| `Promise竞速(promises)` | Promise 竞速，返回第一个完成的 |
| `Promise全部完成(promises)` | 等待所有 Promise 完成（忽略错误） |
| `Promise任意(promises)` | Promise 任意一个完成 |
| `Promise解决已完成(value)` | 返回一个已解决的 Promise |
| `Promise拒绝已完成(reason)` | 返回一个已拒绝的 Promise |
| `创建事件循环()` | 创建新的事件循环 |
| `事件循环运行(loop)` | 运行事件循环 |
| `事件循环运行单次(loop)` | 运行事件循环单次迭代 |
| `事件循环停止(loop)` | 停止事件循环 |
| `事件循环暂停(loop)` | 暂停事件循环 |
| `事件循环恢复(loop)` | 恢复事件循环 |
| `事件循环是否运行中(loop)` | 检查事件循环是否运行中 |
| `事件循环设置超时(loop, delay, callback)` | 设置超时定时器 |
| `事件循环设置间隔(loop, interval, callback)` | 设置间隔定时器 |
| `事件循环清除定时器(loop, timer_handle)` | 清除定时器 |
| `事件循环清除间隔(loop, interval_handle)` | 清除间隔定时器 |
| `事件循环立即执行(loop, callback)` | 立即执行回调 |
| `事件循环创建任务(loop, coro)` | 在事件循环中创建任务 |
| `事件循环任务执行器(loop, func)` | 在事件循环中执行任务 |
| `异步任务等待(task)` | 等待异步任务完成 |
| `异步任务取消(task)` | 取消异步任务 |
| `事件循环等待所有(loop, tasks)` | 等待所有任务完成 |
| `事件循环等待任意(loop, tasks)` | 等待任意任务完成 |
| `事件循环注册输入输出(loop, fd, callback)` | 注册 IO 输入输出监听 |
| `事件循环注销输入输出(loop, fd)` | 注销 IO 输入输出监听 |
| `事件循环输入输出轮询(loop, timeout)` | IO 轮询 |
| `事件循环睡眠(loop, seconds)` | 事件循环睡眠 |
| `事件循环睡眠回调(loop, seconds, callback)` | 睡眠后执行回调 |
| `事件循环延迟(loop, seconds)` | 事件循环延迟 |
| `事件循环获取统计(loop)` | 获取事件循环统计信息 |
| `__init__(self)` |  |
| `then(self, on_fulfilled)` |  |
| `catch(self, on_rejected)` |  |
| `finally_(self, on_finally)` |  |
| `_err_cb(f)` |  |
| `resolve(value)` |  |
| `reject(error)` |  |
| `_repeat()` |  |

---

## 函数详情

### `创建Future()`

创建一个 Future 对象

**参数：**

无参数。

---

### `创建Future完成(result)`

创建一个已完成的 Future 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `result` | `None` |  |

---

### `创建Future失败(exception)`

创建一个已失败的 Future 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `exception` | `None` |  |

---

### `未来完成(fut)`

设置 Future 为完成状态

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |

---

### `未来拒绝(fut, exception)`

设置 Future 为拒绝状态

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |
| `exception` | `None` |  |

---

### `未来等待(fut)`

等待 Future 完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |

---

### `未来等待超时(fut, timeout)`

等待 Future 完成，超时返回 None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |
| `timeout` | `None` |  |

---

### `未来添加回调(fut, callback)`

添加 Future 完成回调

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |
| `callback` | `None` |  |

---

### `未来添加错误回调(fut, callback)`

添加 Future 错误回调

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |
| `callback` | `None` |  |

---

### `未来是否完成(fut)`

检查 Future 是否完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |

---

### `未来是否失败(fut)`

检查 Future 是否失败

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |

---

### `未来链式调用(fut, callback)`

链式调用 Future

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |
| `callback` | `None` |  |

---

### `未来链式回调(fut, callback)`

链式回调 Future

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |
| `callback` | `None` |  |

---

### `未来异常处理(fut, err_callback)`

Future 异常处理

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |
| `err_callback` | `None` |  |

---

### `未来取消(fut)`

取消 Future

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |

---

### `未来获取结果(fut)`

获取 Future 结果

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `fut` | `None` |  |

---

### `创建Promise()`

创建一个 Promise 对象

**参数：**

无参数。

---

### `Promise执行(promise, executor)`

执行 Promise

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promise` | `None` |  |
| `executor` | `None` |  |

---

### `Promise解决(promise, value)`

解决 Promise

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promise` | `None` |  |
| `value` | `None` |  |

---

### `Promise拒绝(promise, reason)`

拒绝 Promise

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promise` | `None` |  |
| `reason` | `None` |  |

---

### `Promise然后(promise, callback)`

Promise then 链式调用

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promise` | `None` |  |
| `callback` | `None` |  |

---

### `Promise捕获(promise, callback)`

Promise catch 错误处理

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promise` | `None` |  |
| `callback` | `None` |  |

---

### `Promise最终(promise, callback)`

Promise finally 最终处理

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promise` | `None` |  |
| `callback` | `None` |  |

---

### `Promise全部(promises)`

等待所有 Promise 完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promises` | `None` |  |

---

### `Promise竞速(promises)`

Promise 竞速，返回第一个完成的

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promises` | `None` |  |

---

### `Promise全部完成(promises)`

等待所有 Promise 完成（忽略错误）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promises` | `None` |  |

---

### `Promise任意(promises)`

Promise 任意一个完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `promises` | `None` |  |

---

### `Promise解决已完成(value)`

返回一个已解决的 Promise

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `value` | `None` |  |

---

### `Promise拒绝已完成(reason)`

返回一个已拒绝的 Promise

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `reason` | `None` |  |

---

### `创建事件循环()`

创建新的事件循环

**参数：**

无参数。

---

### `事件循环运行(loop)`

运行事件循环

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |

---

### `事件循环运行单次(loop)`

运行事件循环单次迭代

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |

---

### `事件循环停止(loop)`

停止事件循环

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |

---

### `事件循环暂停(loop)`

暂停事件循环

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |

---

### `事件循环恢复(loop)`

恢复事件循环

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |

---

### `事件循环是否运行中(loop)`

检查事件循环是否运行中

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |

---

### `事件循环设置超时(loop, delay, callback)`

设置超时定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `delay` | `None` |  |
| `callback` | `None` |  |

---

### `事件循环设置间隔(loop, interval, callback)`

设置间隔定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `interval` | `None` |  |
| `callback` | `None` |  |

---

### `事件循环清除定时器(loop, timer_handle)`

清除定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `timer_handle` | `None` |  |

---

### `事件循环清除间隔(loop, interval_handle)`

清除间隔定时器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `interval_handle` | `None` |  |

---

### `事件循环立即执行(loop, callback)`

立即执行回调

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `callback` | `None` |  |

---

### `事件循环创建任务(loop, coro)`

在事件循环中创建任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `coro` | `None` |  |

---

### `事件循环任务执行器(loop, func)`

在事件循环中执行任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `func` | `None` |  |

---

### `异步任务等待(task)`

等待异步任务完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `task` | `None` |  |

---

### `异步任务取消(task)`

取消异步任务

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `task` | `None` |  |

---

### `事件循环等待所有(loop, tasks)`

等待所有任务完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `tasks` | `None` |  |

---

### `事件循环等待任意(loop, tasks)`

等待任意任务完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `tasks` | `None` |  |

---

### `事件循环注册输入输出(loop, fd, callback)`

注册 IO 输入输出监听

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `fd` | `None` |  |
| `callback` | `None` |  |

---

### `事件循环注销输入输出(loop, fd)`

注销 IO 输入输出监听

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `fd` | `None` |  |

---

### `事件循环输入输出轮询(loop, timeout = None)`

IO 轮询

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `timeout` | `None` | （默认：None） |

---

### `事件循环睡眠(loop, seconds)`

事件循环睡眠

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `seconds` | `None` |  |

---

### `事件循环睡眠回调(loop, seconds, callback)`

睡眠后执行回调

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `seconds` | `None` |  |
| `callback` | `None` |  |

---

### `事件循环延迟(loop, seconds)`

事件循环延迟

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |
| `seconds` | `None` |  |

---

### `事件循环获取统计(loop)`

获取事件循环统计信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `loop` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `then(self, on_fulfilled)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `on_fulfilled` | `None` |  |

---

### `catch(self, on_rejected)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `on_rejected` | `None` |  |

---

### `finally_(self, on_finally)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `on_finally` | `None` |  |

---

### `_err_cb(f)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `f` | `None` |  |

---

### `resolve(value)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `value` | `None` |  |

---

### `reject(error)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `error` | `None` |  |

---

### `_repeat()`

**参数：**

无参数。

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `loop` | `_asyncio.get_event_loop()` |
| `fut` | `_asyncio.get_event_loop().create_future()` |
| `fut` | `_asyncio.get_event_loop().create_future()` |
| `loop` | `_asyncio.get_event_loop()` |
| `futures` | `[p._future for p in promises]` |
| `loop` | `_asyncio.get_event_loop()` |
| `results` | `loop.run_until_complete(_asyncio.gather(*futures, return_exceptions=True))` |
| `futures` | `[p._future for p in promises]` |
| `loop` | `_asyncio.get_event_loop()` |
| `p` | `_Promise()` |
| `p` | `_Promise()` |
| `exc` | `reason if isinstance(reason, Exception) else Exception(str(reason))` |
| `loop` | `_asyncio.new_event_loop()` |
| `loop` | `_asyncio.get_event_loop()` |
| `exc` | `reason if isinstance(reason, Exception) else Exception(str(reason))` |
