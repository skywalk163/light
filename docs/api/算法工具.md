# 算法工具 API

> 模块路径：`stdlib/算法工具.py`
> 导入方式：`从 算法工具 导入 函数名` 或 `导入 算法工具`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建优先队列()` | 创建优先队列 |
| `优先队列上浮(queue, 项)` | 优先队列上浮操作 |
| `优先队列下沉(queue, 项)` | 优先队列下沉操作 |
| `优先队列插入(queue, 优先级, 值)` | 向优先队列插入元素 |
| `优先队列弹出(queue)` | 从优先队列弹出元素 |
| `优先队列是否为空(queue)` | 检查优先队列是否为空 |
| `并查集查找(uf, x)` | 并查集查找操作 |
| `并查集合并(uf, x, y)` | 并查集合并操作 |
| `背包问题(重量列表, 价值列表, 容量)` | 0-1 背包问题，返回最大价值 |
| `最长公共子序列(序列1, 序列2)` | 计算最长公共子序列（LCS）长度 |
| `全排列(数据)` | 生成所有排列 |
| `全排列回溯(数据)` | 使用回溯法生成所有排列 |
| `子集(数据)` | 生成所有子集 |
| `子集回溯(数据)` | 使用回溯法生成所有子集 |
| `Dijkstra(图, 起点)` | Dijkstra 最短路径算法 |
| `最小生成树(图)` | Prim 最小生成树算法 |
| `按权重排(数据, 权重函数)` | 按权重函数排序 |
| `贪心找零(面额列表, 金额)` | 贪心算法找零，返回面额列表 |
| `降序排(数据)` | 降序排序 |
| `__init__(self)` |  |
| `上浮(self, 项)` |  |
| `下沉(self, 项)` |  |
| `插入(self, 优先级, 值)` | 插入元素，优先级越小越靠前 |
| `弹出(self)` | 弹出优先级最高的元素 |
| `是否为空(self)` | 检查队列是否为空 |
| `__init__(self)` |  |
| `查找(self, x)` | 查找元素所属集合（带路径压缩） |
| `合并(self, x, y)` | 合并两个集合 |
| `backtrack(path)` |  |
| `backtrack(start, path)` |  |

---

## 函数详情

### `创建优先队列()`

创建优先队列

**参数：**

无参数。

---

### `优先队列上浮(queue, 项)`

优先队列上浮操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `queue` | `None` |  |
| `项` | `None` |  |

---

### `优先队列下沉(queue, 项)`

优先队列下沉操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `queue` | `None` |  |
| `项` | `None` |  |

---

### `优先队列插入(queue, 优先级, 值)`

向优先队列插入元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `queue` | `None` |  |
| `优先级` | `None` |  |
| `值` | `None` |  |

---

### `优先队列弹出(queue)`

从优先队列弹出元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `queue` | `None` |  |

---

### `优先队列是否为空(queue)`

检查优先队列是否为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `queue` | `None` |  |

---

### `并查集查找(uf, x)`

并查集查找操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `uf` | `None` |  |
| `x` | `None` |  |

---

### `并查集合并(uf, x, y)`

并查集合并操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `uf` | `None` |  |
| `x` | `None` |  |
| `y` | `None` |  |

---

### `背包问题(重量列表, 价值列表, 容量)`

0-1 背包问题，返回最大价值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `重量列表` | `None` |  |
| `价值列表` | `None` |  |
| `容量` | `None` |  |

---

### `最长公共子序列(序列1, 序列2)`

计算最长公共子序列（LCS）长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `序列1` | `None` |  |
| `序列2` | `None` |  |

---

### `全排列(数据)`

生成所有排列

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `全排列回溯(数据)`

使用回溯法生成所有排列

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `子集(数据)`

生成所有子集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `子集回溯(数据)`

使用回溯法生成所有子集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `Dijkstra(图, 起点)`

Dijkstra 最短路径算法

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |
| `起点` | `None` |  |

---

### `最小生成树(图)`

Prim 最小生成树算法

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |

---

### `按权重排(数据, 权重函数)`

按权重函数排序

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `权重函数` | `None` |  |

---

### `贪心找零(面额列表, 金额)`

贪心算法找零，返回面额列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `面额列表` | `None` |  |
| `金额` | `None` |  |

---

### `降序排(数据)`

降序排序

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `上浮(self, 项)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `项` | `None` |  |

---

### `下沉(self, 项)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `项` | `None` |  |

---

### `插入(self, 优先级, 值)`

插入元素，优先级越小越靠前

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `优先级` | `None` |  |
| `值` | `None` |  |

---

### `弹出(self)`

弹出优先级最高的元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `是否为空(self)`

检查队列是否为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `查找(self, x)`

查找元素所属集合（带路径压缩）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `x` | `None` |  |

---

### `合并(self, x, y)`

合并两个集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `x` | `None` |  |
| `y` | `None` |  |

---

### `backtrack(path)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `None` |  |

---

### `backtrack(start, path)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `start` | `None` |  |
| `path` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `root_x` | `self.查找(x)` |
| `root_y` | `self.查找(y)` |
| `n` | `len(重量列表)` |
| `dp` | `[0] * (容量 + 1)` |
| `dp` | `[[0] * (n + 1) for _ in range(m + 1)]` |
| `result` | `[]` |
| `used` | `[False] * len(数据)` |
| `result` | `[]` |
| `result` | `[]` |
| `distances` | `{节点: float('inf') for 节点 in 图}` |
| `pq` | `[(0, 起点)]` |
| `visited` | `set()` |
| `start` | `next(iter(图))` |
| `visited` | `{start}` |
| `edges` | `[]` |
| `total_weight` | `0` |
| `edge_heap` | `[]` |
| `面额列表` | `sorted(面额列表, reverse=True)` |
| `result` | `[]` |
| `remaining` | `金额` |
| `idx` | `self._数据.index(项)` |
| `idx` | `self._数据.index(项)` |
| `distance` | `current_dist + weight` |
