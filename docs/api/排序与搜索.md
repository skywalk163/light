# 排序与搜索 API

> 模块路径：`stdlib/排序与搜索.py`
> 导入方式：`从 排序与搜索 导入 函数名` 或 `导入 排序与搜索`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `选取中值(列表)` | 选取列表的中位数 |
| `快排(列表)` | 快速排序，返回新列表 |
| `归并排(列表)` | 归并排序，返回新列表 |
| `归并合并(左列表, 右列表)` | 合并两个已排序的列表 |
| `插入排(列表)` | 插入排序，返回新列表 |
| `二分查(有序列表, 目标值)` | 在有序列表中二分查找目标值，返回索引，未找到返回-1 |
| `模糊查(列表, 目标值, 容差)` | 在列表中模糊查找目标值（允许误差），返回索引，未找到返回-1 |
| `查找子串(文本, 子串)` | 在文本中查找子串，返回起始索引，未找到返回-1 |
| `按位置排(列表, 位置列表)` | 按指定位置列表重新排列列表元素 |
| `TopK(列表, K, 反转)` | 返回列表中前K个最大或最小的元素 |
| `创建最小堆()` | 创建最小堆，返回堆对象 |
| `堆上浮(堆, 索引)` | 对堆中指定索引的元素执行上浮操作 |
| `堆下沉(堆, 索引)` | 对堆中指定索引的元素执行下沉操作 |
| `堆插入(堆, 值)` | 向堆中插入一个值 |
| `堆弹出(堆)` | 从堆中弹出最小值 |
| `堆窥视(堆)` | 查看堆顶元素但不弹出 |
| `堆是否为空(堆)` | 检查堆是否为空 |
| `反转列表(列表)` | 反转列表，返回新列表 |
| `__init__(self)` |  |

---

## 函数详情

### `选取中值(列表)`

选取列表的中位数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

---

### `快排(列表)`

快速排序，返回新列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

---

### `归并排(列表)`

归并排序，返回新列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

---

### `归并合并(左列表, 右列表)`

合并两个已排序的列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `左列表` | `None` |  |
| `右列表` | `None` |  |

---

### `插入排(列表)`

插入排序，返回新列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

---

### `二分查(有序列表, 目标值)`

在有序列表中二分查找目标值，返回索引，未找到返回-1

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `有序列表` | `None` |  |
| `目标值` | `None` |  |

---

### `模糊查(列表, 目标值, 容差 = 0.1)`

在列表中模糊查找目标值（允许误差），返回索引，未找到返回-1

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `目标值` | `None` |  |
| `容差` | `None` | （默认：0.1） |

---

### `查找子串(文本, 子串)`

在文本中查找子串，返回起始索引，未找到返回-1

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `子串` | `None` |  |

---

### `按位置排(列表, 位置列表)`

按指定位置列表重新排列列表元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `位置列表` | `None` |  |

---

### `TopK(列表, K, 反转 = False)`

返回列表中前K个最大或最小的元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `K` | `None` |  |
| `反转` | `None` | （默认：False） |

---

### `创建最小堆()`

创建最小堆，返回堆对象

**参数：**

无参数。

---

### `堆上浮(堆, 索引)`

对堆中指定索引的元素执行上浮操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |
| `索引` | `None` |  |

---

### `堆下沉(堆, 索引)`

对堆中指定索引的元素执行下沉操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |
| `索引` | `None` |  |

---

### `堆插入(堆, 值)`

向堆中插入一个值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |
| `值` | `None` |  |

---

### `堆弹出(堆)`

从堆中弹出最小值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |

---

### `堆窥视(堆)`

查看堆顶元素但不弹出

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |

---

### `堆是否为空(堆)`

检查堆是否为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |

---

### `反转列表(列表)`

反转列表，返回新列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `sorted_list` | `sorted(列表)` |
| `n` | `len(sorted_list)` |
| `pivot` | `列表[0]` |
| `left` | `[x for x in 列表[1:] if x <= pivot]` |
| `right` | `[x for x in 列表[1:] if x > pivot]` |
| `mid` | `len(列表) // 2` |
| `left` | `归并排(列表[:mid])` |
| `right` | `归并排(列表[mid:])` |
| `result` | `[]` |
| `i` | `0` |
| `j` | `0` |
| `result` | `list(列表)` |
| `idx` | `_bisect.bisect_left(有序列表, 目标值)` |
| `result` | `[None] * len(列表)` |
| `K` | `len(列表)` |
| `key` | `result[i]` |
| `j` | `i - 1` |
