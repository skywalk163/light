# 对象池与缓存 API

> 模块路径：`stdlib/对象池缓存.py`
> 导入方式：`从 对象池缓存 导入 函数名` 或 `导入 对象池缓存`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `对象上下文管理器(池)` | 创建对象上下文管理器 |
| `缓存装饰器(最大容量)` | 缓存装饰器 |
| `LRU缓存装饰器(最大容量)` | LRU缓存装饰器 |
| `记忆化(函数)` | 记忆化装饰器 |
| `创建对象池(创建函数, 最大大小, 初始化数量)` | 创建对象池 |
| `创建LRU缓存(最大容量)` | 创建LRU缓存 |
| `创建简单缓存(过期时间)` | 创建简单缓存 |
| `创建定时缓存(刷新函数, 刷新间隔)` | 创建定时缓存 |
| `创建缓存()` | 创建一个简单的字典缓存 |
| `设置缓存(缓存, 键, 值)` | 设置缓存值 |
| `获取缓存(缓存, 键, 默认值)` | 获取缓存值 |
| `清除缓存(缓存)` | 清除所有缓存 |
| `__init__(self, 创建函数, 最大大小, 初始化数量, 销毁函数)` |  |
| `获取(self)` | 获取对象 |
| `释放(self, 对象)` | 释放对象 |
| `清理(self)` | 清理所有对象 |
| `可用数量(self)` | 获取可用对象数量 |
| `已用数量(self)` | 获取已使用对象数量 |
| `总数量(self)` | 获取总对象数量 |
| `__enter__(self)` | 上下文管理器入口 |
| `__exit__(self, exc_type, exc_val, exc_tb)` | 上下文管理器出口 |
| `__init__(self, 池)` |  |
| `__enter__(self)` |  |
| `__exit__(self, exc_type, exc_val, exc_tb)` |  |
| `__init__(self, 最大容量)` |  |
| `获取(self, 键, 默认值)` | 获取缓存值 |
| `设置(self, 键, 值)` | 设置缓存值 |
| `删除(self, 键)` | 删除缓存项 |
| `包含(self, 键)` | 检查是否包含键 |
| `清空(self)` | 清空缓存 |
| `大小(self)` | 获取缓存大小 |
| `命中率(self)` | 获取命中率 |
| `统计信息(self)` | 获取统计信息 |
| `keys(self)` | 获取所有键 |
| `values(self)` | 获取所有值 |
| `items(self)` | 获取所有键值对 |
| `__init__(self, 过期时间)` |  |
| `获取(self, 键, 默认值)` | 获取缓存值 |
| `设置(self, 键, 值)` | 设置缓存值 |
| `删除(self, 键)` | 删除缓存项 |
| `包含(self, 键)` | 检查是否包含键 |
| `清空(self)` | 清空缓存 |
| `清理过期(self)` | 清理过期项 |
| `大小(self)` | 获取缓存大小 |
| `__init__(self, 最大大小, 过期时间)` |  |
| `获取(self, 键, 默认值)` | 获取缓存值 |
| `设置(self, 键, 值)` | 设置缓存值 |
| `删除(self, 键)` | 删除缓存项 |
| `包含(self, 键)` | 检查是否包含键 |
| `清空(self)` | 清空缓存 |
| `大小(self)` | 获取缓存大小 |
| `装饰器(函数)` |  |
| `包装()` |  |
| `__init__(self, 刷新函数, 刷新间隔)` |  |
| `获取(self)` | 获取缓存值 |
| `刷新(self)` | 手动刷新 |
| `清除(self)` | 清除缓存 |
| `__init__(self, 内存最大容量, 缓存目录)` |  |
| `获取(self, 键, 默认值)` | 获取缓存值 |
| `设置(self, 键, 值)` | 设置缓存值 |
| `删除(self, 键)` | 删除缓存项 |
| `清空(self)` | 清空缓存 |
| `大小(self)` | 获取缓存大小 |
| `包装()` |  |

---

## 函数详情

### `对象上下文管理器(池: 对象池) -> 对象上下文`

创建对象上下文管理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `池` | `对象池` |  |

**返回：** `对象上下文`

---

### `缓存装饰器(最大容量: int = 128)`

缓存装饰器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `最大容量` | `int` | （默认：128） |

---

### `LRU缓存装饰器(最大容量: int = 128)`

LRU缓存装饰器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `最大容量` | `int` | （默认：128） |

---

### `记忆化(函数)`

记忆化装饰器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `None` |  |

---

### `创建对象池(创建函数: Callable, 最大大小: int = 10, 初始化数量: int = 0) -> 对象池`

创建对象池

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `创建函数` | `Callable` |  |
| `最大大小` | `int` | （默认：10） |
| `初始化数量` | `int` | （默认：0） |

**返回：** `对象池`

---

### `创建LRU缓存(最大容量: int = 128) -> LRU缓存`

创建LRU缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `最大容量` | `int` | （默认：128） |

**返回：** `LRU缓存`

---

### `创建简单缓存(过期时间: float = None) -> 简单缓存`

创建简单缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `过期时间` | `float` | （默认：None） |

**返回：** `简单缓存`

---

### `创建定时缓存(刷新函数: Callable, 刷新间隔: float = 60) -> 定时缓存`

创建定时缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `刷新函数` | `Callable` |  |
| `刷新间隔` | `float` | （默认：60） |

**返回：** `定时缓存`

---

### `创建缓存() -> dict`

创建一个简单的字典缓存

**参数：**

无参数。

**返回：** `dict`

---

### `设置缓存(缓存: dict, 键, 值) -> None`

设置缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `缓存` | `dict` |  |
| `键` | `None` |  |
| `值` | `None` |  |

**返回：** `None`

---

### `获取缓存(缓存: dict, 键, 默认值: Any = None)`

获取缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `缓存` | `dict` |  |
| `键` | `None` |  |
| `默认值` | `Any` | （默认：None） |

---

### `清除缓存(缓存: dict) -> None`

清除所有缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `缓存` | `dict` |  |

**返回：** `None`

---

### `__init__(self, 创建函数: Callable, 最大大小: int = 10, 初始化数量: int = 0, 销毁函数: Callable = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `创建函数` | `Callable` |  |
| `最大大小` | `int` | （默认：10） |
| `初始化数量` | `int` | （默认：0） |
| `销毁函数` | `Callable` | （默认：None） |

---

### `获取(self) -> Any`

获取对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `Any`

---

### `释放(self, 对象: Any)`

释放对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `对象` | `Any` |  |

---

### `清理(self)`

清理所有对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `可用数量(self) -> int`

获取可用对象数量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `int`

---

### `已用数量(self) -> int`

获取已使用对象数量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `int`

---

### `总数量(self) -> int`

获取总对象数量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `int`

---

### `__enter__(self)`

上下文管理器入口

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__exit__(self, exc_type, exc_val, exc_tb)`

上下文管理器出口

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `exc_type` | `None` |  |
| `exc_val` | `None` |  |
| `exc_tb` | `None` |  |

---

### `__init__(self, 池: 对象池)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `池` | `对象池` |  |

---

### `__enter__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__exit__(self, exc_type, exc_val, exc_tb)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `exc_type` | `None` |  |
| `exc_val` | `None` |  |
| `exc_tb` | `None` |  |

---

### `__init__(self, 最大容量: int = 128)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `最大容量` | `int` | （默认：128） |

---

### `获取(self, 键: Any, 默认值: Any = None) -> Any`

获取缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `默认值` | `Any` | （默认：None） |

**返回：** `Any`

---

### `设置(self, 键: Any, 值: Any)`

设置缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `值` | `Any` |  |

---

### `删除(self, 键: Any) -> bool`

删除缓存项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |

**返回：** `bool`

---

### `包含(self, 键: Any) -> bool`

检查是否包含键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |

**返回：** `bool`

---

### `清空(self)`

清空缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `大小(self) -> int`

获取缓存大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `int`

---

### `命中率(self) -> float`

获取命中率

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `float`

---

### `统计信息(self) -> Dict[str, Any]`

获取统计信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `Dict[str, Any]`

---

### `keys(self) -> List[Any]`

获取所有键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `List[Any]`

---

### `values(self) -> List[Any]`

获取所有值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `List[Any]`

---

### `items(self) -> List[Tuple[Any, Any]]`

获取所有键值对

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `List[Tuple[Any, Any]]`

---

### `__init__(self, 过期时间: float = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `过期时间` | `float` | （默认：None） |

---

### `获取(self, 键: Any, 默认值: Any = None) -> Any`

获取缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `默认值` | `Any` | （默认：None） |

**返回：** `Any`

---

### `设置(self, 键: Any, 值: Any)`

设置缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `值` | `Any` |  |

---

### `删除(self, 键: Any) -> bool`

删除缓存项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |

**返回：** `bool`

---

### `包含(self, 键: Any) -> bool`

检查是否包含键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |

**返回：** `bool`

---

### `清空(self)`

清空缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `清理过期(self)`

清理过期项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `大小(self) -> int`

获取缓存大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `int`

---

### `__init__(self, 最大大小: int = 1000, 过期时间: float = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `最大大小` | `int` | （默认：1000） |
| `过期时间` | `float` | （默认：None） |

---

### `获取(self, 键: Any, 默认值: Any = None) -> Any`

获取缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `默认值` | `Any` | （默认：None） |

**返回：** `Any`

---

### `设置(self, 键: Any, 值: Any)`

设置缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `值` | `Any` |  |

---

### `删除(self, 键: Any) -> bool`

删除缓存项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |

**返回：** `bool`

---

### `包含(self, 键: Any) -> bool`

检查是否包含键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |

**返回：** `bool`

---

### `清空(self)`

清空缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `大小(self) -> int`

获取缓存大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `int`

---

### `装饰器(函数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `None` |  |

---

### `包装()`

**参数：**

无参数。

---

### `__init__(self, 刷新函数: Callable, 刷新间隔: float = 60)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `刷新函数` | `Callable` |  |
| `刷新间隔` | `float` | （默认：60） |

---

### `获取(self) -> Any`

获取缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `Any`

---

### `刷新(self) -> Any`

手动刷新

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `Any`

---

### `清除(self)`

清除缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 内存最大容量: int = 100, 缓存目录: str = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `内存最大容量` | `int` | （默认：100） |
| `缓存目录` | `str` | （默认：None） |

---

### `获取(self, 键: Any, 默认值: Any = None) -> Any`

获取缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `默认值` | `Any` | （默认：None） |

**返回：** `Any`

---

### `设置(self, 键: Any, 值: Any)`

设置缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |
| `值` | `Any` |  |

---

### `删除(self, 键: Any) -> bool`

删除缓存项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `Any` |  |

**返回：** `bool`

---

### `清空(self)`

清空缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `大小(self) -> int`

获取缓存大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `int`

---

### `包装()`

**参数：**

无参数。

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `缓存` | `{}` |
| `总次数` | `self._命中计数 + self._未命中计数` |
| `当前时间` | `time.monotonic()` |
| `过期键` | `[]` |
| `值` | `self._LRU缓存.获取(键)` |
| `值` | `self._简单缓存.获取(键)` |
| `结果1` | `self._LRU缓存.删除(键)` |
| `结果2` | `self._简单缓存.删除(键)` |
| `缓存` | `LRU缓存(最大容量)` |
| `当前时间` | `time.time()` |
| `值` | `self._内存缓存.获取(键)` |
| `对象` | `self._可用对象.pop()` |
| `对象` | `self._创建函数()` |
| `值` | `self._缓存.pop(键)` |
| `键` | `参数` |
| `结果` | `缓存.获取(键)` |
| `结果` | `函数(*参数)` |
