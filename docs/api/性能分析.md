# 性能分析 API

> 模块路径：`stdlib/性能分析.py`
> 导入方式：`从 性能分析 导入 函数名` 或 `导入 性能分析`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `_采样工作()` |  |
| `开始CPU采样(interval)` | 开始 CPU 采样 |
| `停止CPU采样()` | 停止 CPU 采样 |
| `获取CPU采样()` | 获取 CPU 采样数据 |
| `按值降序排序(data)` | 按值降序排序 |
| `排序节点递归(nodes, key_func)` | 递归排序节点 |
| `生成火焰图节点(name, count, children)` | 生成火焰图节点 |
| `生成火焰图(samples)` | 从采样数据生成火焰图 |
| `生成火焰图SVG(flame_data)` | 生成火焰图 SVG |
| `最大深度计算(node, depth)` | 计算节点最大深度 |
| `绘制节点SVG(node, x, y, width, height, depth)` | 绘制节点 SVG |
| `生成节点颜色(name)` | 根据名称生成节点颜色 |
| `转义SVG(text)` | 转义 SVG 文本 |
| `生成火焰图HTML(flame_data)` | 生成火焰图 HTML |
| `字符串替换(text, old, new)` | 字符串替换 |
| `字符串长度(text)` | 获取字符串长度 |
| `创建内存快照(name)` | 创建内存快照 |
| `获取内存使用量()` | 获取当前内存使用量（字节） |
| `获取内存峰值()` | 获取内存峰值使用量 |
| `比较内存快照(name1, name2)` | 比较两个内存快照 |
| `内存泄漏检测()` | 内存泄漏检测 |
| `开始计时(name)` | 开始计时 |
| `结束计时(name)` | 结束计时 |
| `计时器耗时(name)` | 获取计时器已耗时 |
| `测量执行时间(func)` | 测量函数执行时间 |
| `测量多次(func, repeat)` | 多次测量函数执行时间 |
| `创建性能报告()` | 创建性能报告 |
| `添加CPU采样(report, samples)` | 添加 CPU 采样数据到报告 |
| `添加内存快照(report, snapshot)` | 添加内存快照到报告 |
| `添加计时器(report, name, elapsed)` | 添加计时器数据到报告 |
| `生成报告(report)` | 生成性能报告 |
| `生成文本报告(report)` | 生成文本格式的性能报告 |
| `生成HTML报告(report)` | 生成 HTML 格式的性能报告 |
| `格式化字节(bytes_val)` | 格式化字节大小 |
| `检测热点函数(samples)` | 检测热点函数 |
| `检测热点代码行(samples)` | 检测热点代码行 |
| `生成调用树(samples)` | 生成调用树 |
| `高精度计时器()` | 获取高精度计时器值 |
| `当前线程ID()` | 获取当前线程 ID |
| `当前进程ID()` | 获取当前进程 ID |
| `__init__(self)` |  |

---

## 函数详情

### `_采样工作()`

**参数：**

无参数。

---

### `开始CPU采样(interval = 0.01)`

开始 CPU 采样

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `interval` | `None` | （默认：0.01） |

---

### `停止CPU采样()`

停止 CPU 采样

**参数：**

无参数。

---

### `获取CPU采样()`

获取 CPU 采样数据

**参数：**

无参数。

---

### `按值降序排序(data)`

按值降序排序

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `data` | `None` |  |

---

### `排序节点递归(nodes, key_func = None)`

递归排序节点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `nodes` | `None` |  |
| `key_func` | `None` | （默认：None） |

---

### `生成火焰图节点(name, count, children = None)`

生成火焰图节点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `None` |  |
| `count` | `None` |  |
| `children` | `None` | （默认：None） |

---

### `生成火焰图(samples)`

从采样数据生成火焰图

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `samples` | `None` |  |

---

### `生成火焰图SVG(flame_data)`

生成火焰图 SVG

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `flame_data` | `None` |  |

---

### `最大深度计算(node, depth = 0)`

计算节点最大深度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `node` | `None` |  |
| `depth` | `None` | （默认：0） |

---

### `绘制节点SVG(node, x, y, width, height, depth)`

绘制节点 SVG

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `node` | `None` |  |
| `x` | `None` |  |
| `y` | `None` |  |
| `width` | `None` |  |
| `height` | `None` |  |
| `depth` | `None` |  |

---

### `生成节点颜色(name)`

根据名称生成节点颜色

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `None` |  |

---

### `转义SVG(text)`

转义 SVG 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `None` |  |

---

### `生成火焰图HTML(flame_data)`

生成火焰图 HTML

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `flame_data` | `None` |  |

---

### `字符串替换(text, old, new)`

字符串替换

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `None` |  |
| `old` | `None` |  |
| `new` | `None` |  |

---

### `字符串长度(text)`

获取字符串长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `None` |  |

---

### `创建内存快照(name = 'default')`

创建内存快照

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `None` | （默认：'default'） |

---

### `获取内存使用量()`

获取当前内存使用量（字节）

**参数：**

无参数。

---

### `获取内存峰值()`

获取内存峰值使用量

**参数：**

无参数。

---

### `比较内存快照(name1 = 'default', name2 = None)`

比较两个内存快照

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name1` | `None` | （默认：'default'） |
| `name2` | `None` | （默认：None） |

---

### `内存泄漏检测()`

内存泄漏检测

**参数：**

无参数。

---

### `开始计时(name = 'default')`

开始计时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `None` | （默认：'default'） |

---

### `结束计时(name = 'default')`

结束计时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `None` | （默认：'default'） |

---

### `计时器耗时(name = 'default')`

获取计时器已耗时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `None` | （默认：'default'） |

---

### `测量执行时间(func)`

测量函数执行时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `func` | `None` |  |

---

### `测量多次(func, repeat = 3)`

多次测量函数执行时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `func` | `None` |  |
| `repeat` | `None` | （默认：3） |

---

### `创建性能报告()`

创建性能报告

**参数：**

无参数。

---

### `添加CPU采样(report, samples)`

添加 CPU 采样数据到报告

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `report` | `None` |  |
| `samples` | `None` |  |

---

### `添加内存快照(report, snapshot)`

添加内存快照到报告

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `report` | `None` |  |
| `snapshot` | `None` |  |

---

### `添加计时器(report, name, elapsed)`

添加计时器数据到报告

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `report` | `None` |  |
| `name` | `None` |  |
| `elapsed` | `None` |  |

---

### `生成报告(report)`

生成性能报告

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `report` | `None` |  |

---

### `生成文本报告(report)`

生成文本格式的性能报告

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `report` | `None` |  |

---

### `生成HTML报告(report)`

生成 HTML 格式的性能报告

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `report` | `None` |  |

---

### `格式化字节(bytes_val)`

格式化字节大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `bytes_val` | `None` |  |

---

### `检测热点函数(samples)`

检测热点函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `samples` | `None` |  |

---

### `检测热点代码行(samples)`

检测热点代码行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `samples` | `None` |  |

---

### `生成调用树(samples)`

生成调用树

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `samples` | `None` |  |

---

### `高精度计时器()`

获取高精度计时器值

**参数：**

无参数。

---

### `当前线程ID()`

获取当前线程 ID

**参数：**

无参数。

---

### `当前进程ID()`

获取当前进程 ID

**参数：**

无参数。

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `max_d` | `depth` |
| `text` | `str(text)` |
| `text` | `text.replace('&', '&amp;')` |
| `text` | `text.replace('<', '&lt;')` |
| `text` | `text.replace('>', '&gt;')` |
| `text` | `text.replace('"', '&quot;')` |
| `root` | `{'name': 'root', 'count': 0, 'children': []}` |
| `child_depth` | `最大深度计算(child, depth + 1)` |
| `color` | `生成节点颜色(node['name'])` |
| `svg` | `f'<rect x="{x}" y="{y}" width="{width}" height="{height}" fill="{color}" stroke="#fff" stroke-width="1"/>'` |
| `label` | `转义SVG(node['name'][:30])` |
| `hash_val` | `hash(str(name))` |
| `r` | `(hash_val & 255) % 180 + 40` |
| `g` | `(hash_val >> 8 & 255) % 180 + 40` |
| `b` | `(hash_val >> 16 & 255) % 180 + 40` |
| `svg_parts` | `['<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 800">']` |
| `y` | `0` |
| `html` | `'<html><body style="margin:0;background:#111">'` |
| `snapshot` | `_tracemalloc.take_snapshot()` |
| `snapshot` | `_tracemalloc.take_snapshot()` |
| `stats` | `snapshot.statistics('lineno')` |
| `total` | `sum((stat.size for stat in stats))` |
| `total` | `获取内存使用量()` |
| `snap1` | `_内存快照[name1]` |
| `usage` | `获取内存使用量()` |
| `elapsed` | `_time.perf_counter() - _计时器存储[name]` |
| `start` | `_time.perf_counter()` |
| `result` | `func(*args, **kwargs)` |
| `elapsed` | `_time.perf_counter() - start` |
| `times` | `[]` |
| `last_result` | `None` |
| `lines` | `['=== 性能报告 ===']` |
| `html` | `'<html><body><h1>性能报告</h1>'` |
| `hotspots` | `{}` |
| `sorted_hotspots` | `sorted(hotspots.items(), key=lambda x: x[1], reverse=True)` |
| `hotspots` | `{}` |
| `sorted_hotspots` | `sorted(hotspots.items(), key=lambda x: x[1], reverse=True)` |
| `frame` | `_sys._getframe(2)` |
| `filename` | `frame.f_code.co_filename` |
| `lineno` | `frame.f_lineno` |
| `funcname` | `frame.f_code.co_name` |
| `name` | `f"{s.get('filename', 'unknown')}:{s.get('funcname', 'unknown')}"` |
| `found` | `False` |
| `max_d` | `child_depth` |
| `snap2` | `_内存快照[name2]` |
| `diff` | `snap2.compare_to(snap1, 'lineno')` |
| `start` | `_time.perf_counter()` |
| `last_result` | `func(*args, **kwargs)` |
| `elapsed` | `_time.perf_counter() - start` |
| `name` | `f"{s.get('filename', '')}:{s.get('funcname', '')}"` |
| `line` | `f"{s.get('filename', '')}:{s.get('lineno', 0)}"` |
| `found` | `True` |
