# 日志系统 API

> 模块路径：`stdlib/日志系统增强.py`
> 导入方式：`从 日志系统增强 导入 函数名` 或 `导入 日志系统增强`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `获取日志记录器(名称)` | 获取日志记录器 |
| `创建日志记录器(名称, 级别, 控制台输出, 文件输出, 文件名)` | 创建配置好的日志记录器 |
| `快速配置(级别, 控制台, 文件, 文件名)` | 快速配置日志 |
| `日志上下文(记录器, 消息, 级别)` | 创建日志上下文管理器 |
| `获取全局日志管理器()` | 获取全局日志管理器 |
| `设置全局日志级别(级别)` | 设置全局日志级别 |
| `配置全局日志(级别, 控制台, 文件, 文件名)` | 配置全局日志 |
| `_输出(级别, 消息)` |  |
| `调试(消息)` | 输出调试级别日志 |
| `信息(消息)` | 输出信息级别日志 |
| `警告(消息)` | 输出警告级别日志 |
| `错误(消息)` | 输出错误级别日志 |
| `致命(消息)` | 输出致命级别日志 |
| `设置级别(级别)` | 设置日志级别 |
| `获取级别()` | 获取当前日志级别 |
| `设置格式(格式字符串)` | 设置日志格式 |
| `设置输出文件(文件路径)` | 设置日志输出文件 |
| `启用控制台输出(启用)` | 启用或禁用控制台输出 |
| `禁用控制台输出()` | 禁用控制台输出 |
| `启用文件输出(文件路径)` | 启用文件输出 |
| `禁用文件输出()` | 禁用文件输出 |
| `输出到标准错误()` | 将控制台输出重定向到标准错误 |
| `输出到标准输出()` | 将控制台输出重定向到标准输出 |
| `带上下文日志(级别, 消息)` | 带上下文的日志输出 |
| `调试上下文(消息)` | 带上下文的调试日志 |
| `信息上下文(消息)` | 带上下文的信息日志 |
| `警告上下文(消息)` | 带上下文的警告日志 |
| `错误上下文(消息)` | 带上下文的错误日志 |
| `致命上下文(消息)` | 带上下文的致命日志 |
| `日志异常(级别)` | 记录异常信息 |
| `调试异常()` | 记录调试级别的异常信息 |
| `信息异常()` | 记录信息级别的异常信息 |
| `警告异常()` | 记录警告级别的异常信息 |
| `错误异常()` | 记录错误级别的异常信息 |
| `致命异常()` | 记录致命级别的异常信息 |
| `日志函数(级别)` | 获取指定级别的日志函数 |
| `级别数值(级别)` | 获取级别的数值 |
| `级别名称(数值)` | 获取数值对应的级别名称 |
| `日志轮转(文件路径, 最大大小, 备份数量)` | 日志轮转 |
| `设置日志轮转(文件路径, 最大大小, 备份数量)` | 设置日志轮转 |
| `添加格式化器(名称, 格式化函数)` | 添加自定义格式化器 |
| `使用格式化器(名称)` | 使用指定的格式化器 |
| `创建简单格式化器(格式字符串)` | 创建简单格式化器 |
| `创建带颜色格式化器()` | 创建带颜色的格式化器 |
| `创建JSON格式化器()` | 创建JSON格式化器 |
| `重置格式化器()` | 重置为默认格式化器 |
| `获取所有级别()` | 获取所有可用的日志级别 |
| `获取格式化器列表()` | 获取所有已注册的格式化器 |
| `打印日志配置()` | 打印当前日志配置 |
| `创建标准格式(包含时间, 包含级别, 包含模块, 包含线程)` | 创建标准日志格式 |
| `创建JSON格式()` | 创建JSON格式 |
| `创建简洁格式()` | 创建简洁格式 |
| `创建详细格式()` | 创建详细格式 |
| `创建控制台处理器(级别, 格式化器)` | 创建控制台处理器 |
| `创建文件处理器(文件名, 级别, 格式化器, 编码)` | 创建文件处理器 |
| `创建滚动文件处理器(文件名, 级别, 格式化器, 最大大小, 备份数量, 编码)` | 创建按大小滚动的文件处理器 |
| `创建时间滚动处理器(文件名, 级别, 格式化器, 间隔, 备份数量, 编码)` | 创建按时间滚动的文件处理器 |
| `__init__(self, 允许级别列表, 拒绝级别列表, 包含关键词, 排除关键词)` |  |
| `filter(self, 记录)` | 过滤日志记录 |
| `__init__(self, 名称, 级别)` |  |
| `添加处理器(self, 处理器)` | 添加处理器 |
| `添加过滤器(self, 过滤器)` | 添加过滤器 |
| `设置级别(self, 级别)` | 设置日志级别 |
| `调试(self, 消息)` | 记录DEBUG级别日志 |
| `信息(self, 消息)` | 记录INFO级别日志 |
| `警告(self, 消息)` | 记录WARNING级别日志 |
| `错误(self, 消息)` | 记录ERROR级别日志 |
| `严重(self, 消息)` | 记录CRITICAL级别日志 |
| `异常(self, 消息)` | 记录异常日志 |
| `记录(self, 级别, 消息)` | 记录指定级别日志 |
| `获取记录器(self)` | 获取原始记录器 |
| `__init__(self)` |  |
| `设置全局级别(self, 级别)` | 设置全局日志级别 |
| `获取记录器(self, 名称)` | 获取或创建日志记录器 |
| `配置控制台日志(self, 级别, 格式化器)` | 配置控制台日志 |
| `配置文件日志(self, 文件名, 级别, 格式化器, 滚动, 最大大小, 备份数量)` | 配置文件日志 |
| `配置时间滚动日志(self, 文件名, 级别, 格式化器, 间隔, 备份数量)` | 配置时间滚动日志 |
| `添加全局过滤器(self, 过滤器)` | 添加全局过滤器 |
| `__init__(self, 记录器, 消息, 级别)` |  |
| `__enter__(self)` | 进入上下文 |
| `__exit__(self, exc_type, exc_val, exc_tb)` | 退出上下文 |
| `创建结构化消息(事件)` | 创建结构化消息 |
| `创建API日志(方法, 路径, 状态码, 耗时)` | 创建API日志 |
| `创建数据库日志(操作, 表名, 影响行数, 耗时)` | 创建数据库日志 |
| `创建性能日志(操作, 耗时, 内存)` | 创建性能日志 |
| `_自定义输出(级别, 消息)` |  |
| `格式化函数(级别, 时间, 消息)` |  |
| `格式化函数(级别, 时间, 消息)` |  |
| `格式化函数(级别, 时间, 消息)` |  |

---

## 函数详情

### `获取日志记录器(名称: str = __name__) -> 日志记录器`

获取日志记录器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` | （默认：__name__） |

**返回：** `日志记录器`

---

### `创建日志记录器(名称: str = __name__, 级别: int = logging.DEBUG, 控制台输出: bool = True, 文件输出: bool = False, 文件名: str = 'app.log') -> 日志记录器`

创建配置好的日志记录器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` | （默认：__name__） |
| `级别` | `int` | （默认：logging.DEBUG） |
| `控制台输出` | `bool` | （默认：True） |
| `文件输出` | `bool` | （默认：False） |
| `文件名` | `str` | （默认：'app.log'） |

**返回：** `日志记录器`

---

### `快速配置(级别: int = logging.INFO, 控制台: bool = True, 文件: bool = False, 文件名: str = 'app.log')`

快速配置日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `int` | （默认：logging.INFO） |
| `控制台` | `bool` | （默认：True） |
| `文件` | `bool` | （默认：False） |
| `文件名` | `str` | （默认：'app.log'） |

---

### `日志上下文(记录器: 日志记录器, 消息: str, 级别: int = logging.INFO) -> 日志上下文管理器`

创建日志上下文管理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `记录器` | `日志记录器` |  |
| `消息` | `str` |  |
| `级别` | `int` | （默认：logging.INFO） |

**返回：** `日志上下文管理器`

---

### `获取全局日志管理器() -> 日志管理器`

获取全局日志管理器

**参数：**

无参数。

**返回：** `日志管理器`

---

### `设置全局日志级别(级别: int)`

设置全局日志级别

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `int` |  |

---

### `配置全局日志(级别: int = logging.INFO, 控制台: bool = True, 文件: bool = False, 文件名: str = 'app.log')`

配置全局日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `int` | （默认：logging.INFO） |
| `控制台` | `bool` | （默认：True） |
| `文件` | `bool` | （默认：False） |
| `文件名` | `str` | （默认：'app.log'） |

---

### `_输出(级别: str, 消息: str) -> None`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |
| `消息` | `str` |  |

**返回：** `None`

---

### `调试(消息: str) -> None`

输出调试级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `信息(消息: str) -> None`

输出信息级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `警告(消息: str) -> None`

输出警告级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `错误(消息: str) -> None`

输出错误级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `致命(消息: str) -> None`

输出致命级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `设置级别(级别: str) -> None`

设置日志级别

参数:
    级别: '调试'、'信息'、'警告'、'错误'、'致命'

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |

**返回：** `None`

---

### `获取级别() -> str`

获取当前日志级别

**参数：**

无参数。

**返回：** `str`

---

### `设置格式(格式字符串: str) -> None`

设置日志格式

可用占位符:
    {级别} - 日志级别
    {时间} - 当前时间
    {消息} - 日志消息

默认格式: '[{级别}] {时间} - {消息}'

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `格式字符串` | `str` |  |

**返回：** `None`

---

### `设置输出文件(文件路径: Optional[str] = None) -> None`

设置日志输出文件

参数:
    文件路径: 输出文件路径，为空则关闭文件输出

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `Optional[str]` | （默认：None） |

**返回：** `None`

---

### `启用控制台输出(启用: bool = True) -> None`

启用或禁用控制台输出

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `启用` | `bool` | （默认：True） |

**返回：** `None`

---

### `禁用控制台输出() -> None`

禁用控制台输出

**参数：**

无参数。

**返回：** `None`

---

### `启用文件输出(文件路径: str) -> None`

启用文件输出

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |

**返回：** `None`

---

### `禁用文件输出() -> None`

禁用文件输出

**参数：**

无参数。

**返回：** `None`

---

### `输出到标准错误() -> None`

将控制台输出重定向到标准错误

**参数：**

无参数。

**返回：** `None`

---

### `输出到标准输出() -> None`

将控制台输出重定向到标准输出

**参数：**

无参数。

**返回：** `None`

---

### `带上下文日志(级别: str, 消息: str) -> None`

带上下文的日志输出

参数:
    级别: 日志级别
    消息: 日志消息
    上下文: 额外的上下文信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |
| `消息` | `str` |  |

**返回：** `None`

---

### `调试上下文(消息: str) -> None`

带上下文的调试日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `信息上下文(消息: str) -> None`

带上下文的信息日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `警告上下文(消息: str) -> None`

带上下文的警告日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `错误上下文(消息: str) -> None`

带上下文的错误日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `致命上下文(消息: str) -> None`

带上下文的致命日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `消息` | `str` |  |

**返回：** `None`

---

### `日志异常(级别: str = '错误') -> None`

记录异常信息

参数:
    级别: 日志级别，默认为'错误'

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` | （默认：'错误'） |

**返回：** `None`

---

### `调试异常() -> None`

记录调试级别的异常信息

**参数：**

无参数。

**返回：** `None`

---

### `信息异常() -> None`

记录信息级别的异常信息

**参数：**

无参数。

**返回：** `None`

---

### `警告异常() -> None`

记录警告级别的异常信息

**参数：**

无参数。

**返回：** `None`

---

### `错误异常() -> None`

记录错误级别的异常信息

**参数：**

无参数。

**返回：** `None`

---

### `致命异常() -> None`

记录致命级别的异常信息

**参数：**

无参数。

**返回：** `None`

---

### `日志函数(级别: str) -> Callable`

获取指定级别的日志函数

参数:
    级别: 日志级别

返回:
    日志函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |

**返回：** `Callable`

---

### `级别数值(级别: str) -> int`

获取级别的数值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |

**返回：** `int`

---

### `级别名称(数值: int) -> str`

获取数值对应的级别名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数值` | `int` |  |

**返回：** `str`

---

### `日志轮转(文件路径: str, 最大大小: int = 1024 * 1024, 备份数量: int = 5) -> None`

日志轮转

参数:
    文件路径: 日志文件路径
    最大大小: 单个日志文件最大大小（字节），默认1MB
    备份数量: 保留的备份文件数量，默认5

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `最大大小` | `int` | （默认：1024 * 1024） |
| `备份数量` | `int` | （默认：5） |

**返回：** `None`

---

### `设置日志轮转(文件路径: str, 最大大小: int = 1024 * 1024, 备份数量: int = 5) -> None`

设置日志轮转

参数:
    文件路径: 日志文件路径
    最大大小: 单个日志文件最大大小（字节），默认1MB
    备份数量: 保留的备份文件数量，默认5

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `最大大小` | `int` | （默认：1024 * 1024） |
| `备份数量` | `int` | （默认：5） |

**返回：** `None`

---

### `添加格式化器(名称: str, 格式化函数: Callable) -> None`

添加自定义格式化器

参数:
    名称: 格式化器名称
    格式化函数: 格式化函数，接收(级别, 时间, 消息)参数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |
| `格式化函数` | `Callable` |  |

**返回：** `None`

---

### `使用格式化器(名称: str) -> None`

使用指定的格式化器

参数:
    名称: 格式化器名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |

**返回：** `None`

---

### `创建简单格式化器(格式字符串: str) -> Callable`

创建简单格式化器

参数:
    格式字符串: 格式字符串，支持{级别}、{时间}、{消息}占位符

返回:
    格式化函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `格式字符串` | `str` |  |

**返回：** `Callable`

---

### `创建带颜色格式化器() -> Callable`

创建带颜色的格式化器

返回:
    格式化函数

**参数：**

无参数。

**返回：** `Callable`

---

### `创建JSON格式化器() -> Callable`

创建JSON格式化器

返回:
    格式化函数

**参数：**

无参数。

**返回：** `Callable`

---

### `重置格式化器() -> None`

重置为默认格式化器

**参数：**

无参数。

**返回：** `None`

---

### `获取所有级别() -> list`

获取所有可用的日志级别

**参数：**

无参数。

**返回：** `list`

---

### `获取格式化器列表() -> list`

获取所有已注册的格式化器

**参数：**

无参数。

**返回：** `list`

---

### `打印日志配置() -> None`

打印当前日志配置

**参数：**

无参数。

**返回：** `None`

---

### `创建标准格式(包含时间: bool = True, 包含级别: bool = True, 包含模块: bool = True, 包含线程: bool = False) -> logging.Formatter`

创建标准日志格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `包含时间` | `bool` | （默认：True） |
| `包含级别` | `bool` | （默认：True） |
| `包含模块` | `bool` | （默认：True） |
| `包含线程` | `bool` | （默认：False） |

**返回：** `logging.Formatter`

---

### `创建JSON格式() -> logging.Formatter`

创建JSON格式

**参数：**

无参数。

**返回：** `logging.Formatter`

---

### `创建简洁格式() -> logging.Formatter`

创建简洁格式

**参数：**

无参数。

**返回：** `logging.Formatter`

---

### `创建详细格式() -> logging.Formatter`

创建详细格式

**参数：**

无参数。

**返回：** `logging.Formatter`

---

### `创建控制台处理器(级别: int = logging.DEBUG, 格式化器: logging.Formatter = None) -> logging.Handler`

创建控制台处理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `int` | （默认：logging.DEBUG） |
| `格式化器` | `logging.Formatter` | （默认：None） |

**返回：** `logging.Handler`

---

### `创建文件处理器(文件名: str, 级别: int = logging.DEBUG, 格式化器: logging.Formatter = None, 编码: str = 'utf-8') -> logging.Handler`

创建文件处理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件名` | `str` |  |
| `级别` | `int` | （默认：logging.DEBUG） |
| `格式化器` | `logging.Formatter` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `logging.Handler`

---

### `创建滚动文件处理器(文件名: str, 级别: int = logging.DEBUG, 格式化器: logging.Formatter = None, 最大大小: int = 1024 * 1024 * 10, 备份数量: int = 5, 编码: str = 'utf-8') -> logging.handlers.RotatingFileHandler`

创建按大小滚动的文件处理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件名` | `str` |  |
| `级别` | `int` | （默认：logging.DEBUG） |
| `格式化器` | `logging.Formatter` | （默认：None） |
| `最大大小` | `int` | （默认：1024 * 1024 * 10） |
| `备份数量` | `int` | （默认：5） |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `logging.handlers.RotatingFileHandler`

---

### `创建时间滚动处理器(文件名: str, 级别: int = logging.DEBUG, 格式化器: logging.Formatter = None, 间隔: str = 'D', 备份数量: int = 7, 编码: str = 'utf-8') -> logging.handlers.TimedRotatingFileHandler`

创建按时间滚动的文件处理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件名` | `str` |  |
| `级别` | `int` | （默认：logging.DEBUG） |
| `格式化器` | `logging.Formatter` | （默认：None） |
| `间隔` | `str` | （默认：'D'） |
| `备份数量` | `int` | （默认：7） |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `logging.handlers.TimedRotatingFileHandler`

---

### `__init__(self, 允许级别列表: list = None, 拒绝级别列表: list = None, 包含关键词: list = None, 排除关键词: list = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `允许级别列表` | `list` | （默认：None） |
| `拒绝级别列表` | `list` | （默认：None） |
| `包含关键词` | `list` | （默认：None） |
| `排除关键词` | `list` | （默认：None） |

---

### `filter(self, 记录: logging.LogRecord) -> bool`

过滤日志记录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `记录` | `logging.LogRecord` |  |

**返回：** `bool`

---

### `__init__(self, 名称: str = __name__, 级别: int = logging.DEBUG)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `str` | （默认：__name__） |
| `级别` | `int` | （默认：logging.DEBUG） |

---

### `添加处理器(self, 处理器: logging.Handler)`

添加处理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `处理器` | `logging.Handler` |  |

---

### `添加过滤器(self, 过滤器: logging.Filter)`

添加过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `过滤器` | `logging.Filter` |  |

---

### `设置级别(self, 级别: int)`

设置日志级别

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `级别` | `int` |  |

---

### `调试(self, 消息: str)`

记录DEBUG级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `消息` | `str` |  |

---

### `信息(self, 消息: str)`

记录INFO级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `消息` | `str` |  |

---

### `警告(self, 消息: str)`

记录WARNING级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `消息` | `str` |  |

---

### `错误(self, 消息: str)`

记录ERROR级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `消息` | `str` |  |

---

### `严重(self, 消息: str)`

记录CRITICAL级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `消息` | `str` |  |

---

### `异常(self, 消息: str)`

记录异常日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `消息` | `str` |  |

---

### `记录(self, 级别: int, 消息: str)`

记录指定级别日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `级别` | `int` |  |
| `消息` | `str` |  |

---

### `获取记录器(self) -> logging.Logger`

获取原始记录器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `logging.Logger`

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `设置全局级别(self, 级别: int)`

设置全局日志级别

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `级别` | `int` |  |

---

### `获取记录器(self, 名称: str) -> 日志记录器`

获取或创建日志记录器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `str` |  |

**返回：** `日志记录器`

---

### `配置控制台日志(self, 级别: int = logging.INFO, 格式化器: logging.Formatter = None)`

配置控制台日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `级别` | `int` | （默认：logging.INFO） |
| `格式化器` | `logging.Formatter` | （默认：None） |

---

### `配置文件日志(self, 文件名: str, 级别: int = logging.DEBUG, 格式化器: logging.Formatter = None, 滚动: bool = True, 最大大小: int = 1024 * 1024 * 10, 备份数量: int = 5)`

配置文件日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件名` | `str` |  |
| `级别` | `int` | （默认：logging.DEBUG） |
| `格式化器` | `logging.Formatter` | （默认：None） |
| `滚动` | `bool` | （默认：True） |
| `最大大小` | `int` | （默认：1024 * 1024 * 10） |
| `备份数量` | `int` | （默认：5） |

---

### `配置时间滚动日志(self, 文件名: str, 级别: int = logging.DEBUG, 格式化器: logging.Formatter = None, 间隔: str = 'D', 备份数量: int = 7)`

配置时间滚动日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件名` | `str` |  |
| `级别` | `int` | （默认：logging.DEBUG） |
| `格式化器` | `logging.Formatter` | （默认：None） |
| `间隔` | `str` | （默认：'D'） |
| `备份数量` | `int` | （默认：7） |

---

### `添加全局过滤器(self, 过滤器: logging.Filter)`

添加全局过滤器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `过滤器` | `logging.Filter` |  |

---

### `__init__(self, 记录器: 日志记录器, 消息: str, 级别: int = logging.INFO)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `记录器` | `日志记录器` |  |
| `消息` | `str` |  |
| `级别` | `int` | （默认：logging.INFO） |

---

### `__enter__(self)`

进入上下文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__exit__(self, exc_type, exc_val, exc_tb)`

退出上下文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `exc_type` | `None` |  |
| `exc_val` | `None` |  |
| `exc_tb` | `None` |  |

---

### `创建结构化消息(事件: str) -> str`

创建结构化消息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `事件` | `str` |  |

**返回：** `str`

---

### `创建API日志(方法: str, 路径: str, 状态码: int, 耗时: float) -> str`

创建API日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `方法` | `str` |  |
| `路径` | `str` |  |
| `状态码` | `int` |  |
| `耗时` | `float` |  |

**返回：** `str`

---

### `创建数据库日志(操作: str, 表名: str, 影响行数: int, 耗时: float) -> str`

创建数据库日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `操作` | `str` |  |
| `表名` | `str` |  |
| `影响行数` | `int` |  |
| `耗时` | `float` |  |

**返回：** `str`

---

### `创建性能日志(操作: str, 耗时: float, 内存: float = None) -> str`

创建性能日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `操作` | `str` |  |
| `耗时` | `float` |  |
| `内存` | `float` | （默认：None） |

**返回：** `str`

---

### `_自定义输出(级别: str, 消息: str) -> None`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |
| `消息` | `str` |  |

**返回：** `None`

---

### `格式化函数(级别: str, 时间: str, 消息: str) -> str`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |
| `时间` | `str` |  |
| `消息` | `str` |  |

**返回：** `str`

---

### `格式化函数(级别: str, 时间: str, 消息: str) -> str`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |
| `时间` | `str` |  |
| `消息` | `str` |  |

**返回：** `str`

---

### `格式化函数(级别: str, 时间: str, 消息: str) -> str`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `级别` | `str` |  |
| `时间` | `str` |  |
| `消息` | `str` |  |

**返回：** `str`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `DEBUG` | `logging.DEBUG` |
| `INFO` | `logging.INFO` |
| `WARNING` | `logging.WARNING` |
| `ERROR` | `logging.ERROR` |
| `CRITICAL` | `logging.CRITICAL` |
| `级别名称` | `{DEBUG: 'DEBUG', INFO: 'INFO', WARNING: 'WARNING', ERROR: 'ERROR', CRITICAL: 'CRITICAL'}` |
| `记录器` | `日志记录器(名称, 级别)` |
| `处理器列表` | `[]` |
| `级别值` | `_LOG_LEVELS.get(级别, 0)` |
| `时间_str` | `_time.strftime('%Y-%m-%d %H:%M:%S', _time.localtime())` |
| `行` | `_format.format(级别=级别, 时间=时间_str, 消息=消息)` |
| `级别值` | `_LOG_LEVELS.get(级别, 0)` |
| `时间_str` | `_time.strftime('%Y-%m-%d %H:%M:%S', _time.localtime())` |
| `上下文_str` | `' '.join((f'{k}={v}' for (k, v) in 上下文.items()))` |
| `行` | `_format.format(级别=级别, 时间=时间_str, 消息=消息)` |
| `异常信息` | `traceback.format_exc()` |
| `函数映射` | `{'调试': 调试, '信息': 信息, '警告': 警告, '错误': 错误, '致命': 致命}` |
| `文件大小` | `os.path.getsize(文件路径)` |
| `格式化函数` | `_log_formatters[名称]` |
| `颜色映射` | `{'调试': '\x1b[36m', '信息': '\x1b[32m', '警告': '\x1b[33m', '错误': '\x1b[31m', '致命': '\x1b[41m'}` |
| `重置` | `'\x1b[0m'` |
| `配置` | `{'当前级别': 获取级别(), '级别数值': _current_level, '输出格式': _format, '控制台输出': _enable_console, '文件输出': _output_file is not None, '格式化器': list(_log_formatters.keys())}` |
| `格式部分` | `[]` |
| `处理器` | `logging.StreamHandler(sys.stdout)` |
| `处理器` | `logging.FileHandler(文件名, encoding=编码)` |
| `处理器` | `logging.handlers.RotatingFileHandler(文件名, maxBytes=最大大小, backupCount=备份数量, encoding=编码)` |
| `处理器` | `logging.handlers.TimedRotatingFileHandler(文件名, when=间隔, backupCount=备份数量, encoding=编码)` |
| `格式化器` | `格式化器 or 日志格式化器.创建标准格式()` |
| `处理器` | `日志处理器.创建控制台处理器(级别, 格式化器)` |
| `格式化器` | `格式化器 or 日志格式化器.创建详细格式()` |
| `格式化器` | `格式化器 or 日志格式化器.创建详细格式()` |
| `处理器` | `日志处理器.创建时间滚动处理器(文件名, 级别, 格式化器, 间隔, 备份数量)` |
| `格式化器` | `日志格式化器.创建标准格式()` |
| `处理器` | `日志处理器.创建控制台处理器(级别, 格式化器)` |
| `格式化器` | `日志格式化器.创建详细格式()` |
| `处理器` | `日志处理器.创建滚动文件处理器(文件名, 级别, 格式化器)` |
| `消息部分` | `[f'事件={事件}']` |
| `消息` | `f'性能 {操作} 耗时={耗时:.3f}秒'` |
| `消息` | `f'{消息} [{上下文_str}]'` |
| `旧文件` | `f'{文件路径}.{i}'` |
| `新文件` | `f'{文件路径}.{i + 1}'` |
| `级别值` | `_LOG_LEVELS.get(级别, 0)` |
| `时间_str` | `_time.strftime('%Y-%m-%d %H:%M:%S', _time.localtime())` |
| `行` | `格式化函数(级别, 时间_str, 消息)` |
| `颜色` | `颜色映射.get(级别, '')` |
| `包含` | `any((关键词 in 记录.getMessage() for 关键词 in self._包含关键词))` |
| `处理器` | `日志处理器.创建滚动文件处理器(文件名, 级别, 格式化器, 最大大小, 备份数量)` |
| `处理器` | `日志处理器.创建文件处理器(文件名, 级别, 格式化器)` |
