# HTTP服务端 API

> 模块路径：`stdlib/HTTP服务端.py`
> 导入方式：`从 HTTP服务端 导入 函数名` 或 `导入 HTTP服务端`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建服务器(主机, 端口)` | 创建 HTTP 服务器 |
| `添加路由(服务器, 方法, 路径, 处理函数)` | 添加路由 |
| `添加静态文件(服务器, URL前缀, 目录路径)` | 添加静态文件服务 |
| `启动服务器(服务器, 阻塞)` | 启动服务器 |
| `停止服务器(服务器)` | 停止服务器 |
| `创建CORS中间件(允许来源, 允许方法, 允许头)` | 创建 CORS 中间件 |
| `创建日志中间件()` | 创建访问日志中间件 |
| `创建静态文件中间件(URL前缀, 目录路径)` | 创建静态文件中间件 |
| `创建Web应用(主机, 端口)` | 创建完整的 Web 应用（含路由、中间件支持） |
| `运行简单服务器(处理函数, 主机, 端口)` | 运行一个简单的 HTTP 服务器，所有请求由同一函数处理 |
| `创建JSONAPI服务器(路由字典, 主机, 端口)` | 创建 JSON API 服务器 |
| `__init__(self)` |  |
| `__init__(self)` |  |
| `设置头(self, 名称, 值)` | 设置响应头 |
| `写文本(self, 文本, 状态码, 内容类型)` | 写入文本响应 |
| `写JSON(self, 数据, 状态码)` | 写入 JSON 响应 |
| `写HTML(self, html, 状态码)` | 写入 HTML 响应 |
| `写文件(self, 文件路径)` | 写入文件响应 |
| `重定向(self, 位置, 状态码)` | 重定向 |
| `设置Cookie(self, 名称, 值, 路径, 过期时间, 安全, HTTPOnly)` | 设置 Cookie |
| `__init__(self, 方法, 路径模式, 处理函数)` |  |
| `匹配(self, 路径)` | 尝试匹配路径，成功返回路径参数字典，失败返回 None |
| `__init__(self)` |  |
| `注册(self, 方法, 路径, 处理函数)` | 注册路由 |
| `GET(self, 路径, 处理函数)` | 注册 GET 路由（装饰器风格） |
| `POST(self, 路径, 处理函数)` | 注册 POST 路由 |
| `PUT(self, 路径, 处理函数)` | 注册 PUT 路由 |
| `DELETE(self, 路径, 处理函数)` | 注册 DELETE 路由 |
| `PATCH(self, 路径, 处理函数)` | 注册 PATCH 路由 |
| `匹配(self, 方法, 路径)` | 匹配路由，返回 (处理函数, 路径参数) |
| `log_message(self, format)` |  |
| `_解析请求(self)` | 将原始请求解析为 HTTP请求 对象 |
| `_发送响应(self, 响应)` | 发送 HTTP响应 对象到客户端 |
| `_处理请求(self)` | 处理单个请求：路由匹配 -> 中间件 -> 处理函数 |
| `_执行路由匹配(self, req, resp, server)` | 执行路由匹配并调用处理函数 |
| `do_GET(self)` |  |
| `do_POST(self)` |  |
| `do_PUT(self)` |  |
| `do_DELETE(self)` |  |
| `do_PATCH(self)` |  |
| `do_HEAD(self)` |  |
| `do_OPTIONS(self)` |  |
| `__init__(self, 允许来源, 允许方法, 允许头)` |  |
| `请求前(self, req, resp)` | 处理 CORS 预检请求和添加响应头 |
| `请求后(self, req, resp)` | 记录访问日志 |
| `__init__(self, URL前缀, 目录路径)` |  |
| `请求前(self, req, resp)` | 在路由匹配前检查静态文件 |
| `__init__(self, 服务器)` |  |
| `GET(self, 路径, 处理函数)` | 注册 GET 路由（装饰器风格） |
| `POST(self, 路径, 处理函数)` | 注册 POST 路由 |
| `PUT(self, 路径, 处理函数)` | 注册 PUT 路由 |
| `DELETE(self, 路径, 处理函数)` | 注册 DELETE 路由 |
| `PATCH(self, 路径, 处理函数)` | 注册 PATCH 路由 |
| `使用中间件(self, 中间件)` |  |
| `静态文件(self, URL前缀, 目录路径)` |  |
| `启动(self, 阻塞)` |  |
| `停止(self)` |  |
| `地址(self)` |  |

---

## 函数详情

### `创建服务器(主机 = '0.0.0.0', 端口 = 8080)`

创建 HTTP 服务器

Args:
    主机: 绑定地址，默认 "0.0.0.0"（所有接口）
    端口: 监听端口，默认 8080

Returns:
    HTTPServer 实例

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` | （默认：'0.0.0.0'） |
| `端口` | `None` | （默认：8080） |

---

### `添加路由(服务器, 方法, 路径, 处理函数)`

添加路由

Args:
    服务器: 由 创建服务器 返回的服务器实例
    方法: HTTP 方法（GET/POST/PUT/DELETE/PATCH）
    路径: 路径模式，支持 {参数名} 占位符
    处理函数: 处理函数，签名 fn(req, resp)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `服务器` | `None` |  |
| `方法` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` |  |

---

### `添加静态文件(服务器, URL前缀, 目录路径)`

添加静态文件服务

Args:
    服务器: 由 创建服务器 返回的服务器实例
    URL前缀: URL 前缀，如 /static
    目录路径: 本地文件系统目录路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `服务器` | `None` |  |
| `URL前缀` | `None` |  |
| `目录路径` | `None` |  |

---

### `启动服务器(服务器, 阻塞 = True)`

启动服务器

Args:
    服务器: 由 创建服务器 返回的服务器实例
    阻塞: 是否阻塞当前线程，默认为 True

Returns:
    阻塞模式下返回 None；非阻塞模式下返回线程对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `服务器` | `None` |  |
| `阻塞` | `None` | （默认：True） |

---

### `停止服务器(服务器)`

停止服务器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `服务器` | `None` |  |

---

### `创建CORS中间件(允许来源 = '*', 允许方法 = 'GET,POST,PUT,DELETE,OPTIONS', 允许头 = 'Content-Type')`

创建 CORS 中间件

Args:
    允许来源: 允许的跨域来源，默认 "*"
    允许方法: 允许的 HTTP 方法
    允许头: 允许的自定义头

Returns:
    CORS 中间件对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `允许来源` | `None` | （默认：'*'） |
| `允许方法` | `None` | （默认：'GET,POST,PUT,DELETE,OPTIONS'） |
| `允许头` | `None` | （默认：'Content-Type'） |

---

### `创建日志中间件()`

创建访问日志中间件

Returns:
    日志中间件对象

**参数：**

无参数。

---

### `创建静态文件中间件(URL前缀, 目录路径)`

创建静态文件中间件

Args:
    URL前缀: URL 前缀，如 /static
    目录路径: 本地文件系统目录路径

Returns:
    静态文件中间件对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `URL前缀` | `None` |  |
| `目录路径` | `None` |  |

---

### `创建Web应用(主机 = '0.0.0.0', 端口 = 8080)`

创建完整的 Web 应用（含路由、中间件支持）

返回的 Web 应用对象支持以下方法：
- GET/POST/PUT/DELETE/PATCH(路径, 处理函数)：注册路由
- 使用中间件(中间件)：添加中间件
- 静态文件(URL前缀, 目录路径)：添加静态文件服务
- 启动(阻塞=True)：启动服务器
- 停止()：停止服务器

Args:
    主机: 绑定地址，默认 "0.0.0.0"
    端口: 监听端口，默认 8080

Returns:
    Web 应用对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` | （默认：'0.0.0.0'） |
| `端口` | `None` | （默认：8080） |

---

### `运行简单服务器(处理函数, 主机 = '0.0.0.0', 端口 = 8080)`

运行一个简单的 HTTP 服务器，所有请求由同一函数处理

处理函数签名: fn(req, resp)
其中 req 是 HTTP请求 对象，resp 是 HTTP响应 对象

Args:
    处理函数: 请求处理函数
    主机: 绑定地址，默认 "0.0.0.0"
    端口: 监听端口，默认 8080

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `处理函数` | `None` |  |
| `主机` | `None` | （默认：'0.0.0.0'） |
| `端口` | `None` | （默认：8080） |

---

### `创建JSONAPI服务器(路由字典, 主机 = '0.0.0.0', 端口 = 8080)`

创建 JSON API 服务器

路由字典格式:
{
    "GET /users": 处理函数,
    "POST /users": 处理函数,
    "GET /users/{id}": 处理函数,
    "PUT /users/{id}": 处理函数,
    "DELETE /users/{id}": 处理函数,
}

处理函数签名: fn(req, resp)
其中 req 是 HTTP请求 对象，resp 是 HTTP响应 对象

返回的服务器默认开启 CORS 和日志中间件

Args:
    路由字典: 路由配置字典
    主机: 绑定地址，默认 "0.0.0.0"
    端口: 监听端口，默认 8080

Returns:
    Web 应用对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路由字典` | `None` |  |
| `主机` | `None` | （默认：'0.0.0.0'） |
| `端口` | `None` | （默认：8080） |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `设置头(self, 名称, 值)`

设置响应头

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` |  |
| `值` | `None` |  |

---

### `写文本(self, 文本, 状态码 = 200, 内容类型 = 'text/plain; charset=utf-8')`

写入文本响应

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文本` | `None` |  |
| `状态码` | `None` | （默认：200） |
| `内容类型` | `None` | （默认：'text/plain; charset=utf-8'） |

---

### `写JSON(self, 数据, 状态码 = 200)`

写入 JSON 响应

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `数据` | `None` |  |
| `状态码` | `None` | （默认：200） |

---

### `写HTML(self, html, 状态码 = 200)`

写入 HTML 响应

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `html` | `None` |  |
| `状态码` | `None` | （默认：200） |

---

### `写文件(self, 文件路径)`

写入文件响应

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件路径` | `None` |  |

---

### `重定向(self, 位置, 状态码 = 302)`

重定向

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `位置` | `None` |  |
| `状态码` | `None` | （默认：302） |

---

### `设置Cookie(self, 名称, 值, 路径 = '/', 过期时间 = None, 安全 = False, HTTPOnly = False)`

设置 Cookie

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` |  |
| `值` | `None` |  |
| `路径` | `None` | （默认：'/'） |
| `过期时间` | `None` | （默认：None） |
| `安全` | `None` | （默认：False） |
| `HTTPOnly` | `None` | （默认：False） |

---

### `__init__(self, 方法 = 'GET', 路径模式 = '/', 处理函数 = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `方法` | `None` | （默认：'GET'） |
| `路径模式` | `None` | （默认：'/'） |
| `处理函数` | `None` | （默认：None） |

---

### `匹配(self, 路径)`

尝试匹配路径，成功返回路径参数字典，失败返回 None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `注册(self, 方法, 路径, 处理函数)`

注册路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `方法` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` |  |

---

### `GET(self, 路径, 处理函数 = None)`

注册 GET 路由（装饰器风格）

支持两种用法:
- 直接调用: 路由表.GET("/path", 处理函数)
- 装饰器:   @路由表.GET("/path")

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `POST(self, 路径, 处理函数 = None)`

注册 POST 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `PUT(self, 路径, 处理函数 = None)`

注册 PUT 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `DELETE(self, 路径, 处理函数 = None)`

注册 DELETE 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `PATCH(self, 路径, 处理函数 = None)`

注册 PATCH 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `匹配(self, 方法, 路径)`

匹配路由，返回 (处理函数, 路径参数)

返回:
    (处理函数, 路径参数字典) 或 (None, {})

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `方法` | `None` |  |
| `路径` | `None` |  |

---

### `log_message(self, format)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `format` | `None` |  |

---

### `_解析请求(self)`

将原始请求解析为 HTTP请求 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `_发送响应(self, 响应)`

发送 HTTP响应 对象到客户端

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `响应` | `None` |  |

---

### `_处理请求(self)`

处理单个请求：路由匹配 -> 中间件 -> 处理函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `_执行路由匹配(self, req, resp, server)`

执行路由匹配并调用处理函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `req` | `None` |  |
| `resp` | `None` |  |
| `server` | `None` |  |

---

### `do_GET(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `do_POST(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `do_PUT(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `do_DELETE(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `do_PATCH(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `do_HEAD(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `do_OPTIONS(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 允许来源 = '*', 允许方法 = 'GET,POST,PUT,DELETE,OPTIONS', 允许头 = 'Content-Type')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `允许来源` | `None` | （默认：'*'） |
| `允许方法` | `None` | （默认：'GET,POST,PUT,DELETE,OPTIONS'） |
| `允许头` | `None` | （默认：'Content-Type'） |

---

### `请求前(self, req, resp)`

处理 CORS 预检请求和添加响应头

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `req` | `None` |  |
| `resp` | `None` |  |

---

### `请求后(self, req, resp)`

记录访问日志

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `req` | `None` |  |
| `resp` | `None` |  |

---

### `__init__(self, URL前缀, 目录路径)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `URL前缀` | `None` |  |
| `目录路径` | `None` |  |

---

### `请求前(self, req, resp)`

在路由匹配前检查静态文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `req` | `None` |  |
| `resp` | `None` |  |

---

### `__init__(self, 服务器)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `服务器` | `None` |  |

---

### `GET(self, 路径, 处理函数 = None)`

注册 GET 路由（装饰器风格）

支持两种用法:
- 直接调用: app.GET("/path", 处理函数)
- 装饰器:   @app.GET("/path")

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `POST(self, 路径, 处理函数 = None)`

注册 POST 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `PUT(self, 路径, 处理函数 = None)`

注册 PUT 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `DELETE(self, 路径, 处理函数 = None)`

注册 DELETE 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `PATCH(self, 路径, 处理函数 = None)`

注册 PATCH 路由

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `路径` | `None` |  |
| `处理函数` | `None` | （默认：None） |

---

### `使用中间件(self, 中间件)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `中间件` | `None` |  |

---

### `静态文件(self, URL前缀, 目录路径)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `URL前缀` | `None` |  |
| `目录路径` | `None` |  |

---

### `启动(self, 阻塞 = True)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `阻塞` | `None` | （默认：True） |

---

### `停止(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `地址(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `server` | `创建服务器(主机, 端口)` |
| `app` | `创建Web应用(主机, 端口)` |
| `cookie` | `f'{名称}={值}; Path={路径}'` |
| `m` | `self._正则.match(路径)` |
| `方法` | `方法.upper()` |
| `req` | `HTTP请求()` |
| `parsed` | `urllib.parse.urlparse(self.path)` |
| `content_length` | `int(self.headers.get('Content-Length', 0))` |
| `req` | `self._解析请求()` |
| `resp` | `HTTP响应()` |
| `server` | `http.server.HTTPServer((主机, 端口), _请求处理器)` |
| `server` | `http.server.HTTPServer((主机, 端口), _请求处理器)` |
| `部分` | `路由描述.strip().split(None, 1)` |
| `方法` | `方法.upper()` |
| `文本` | `_json.dumps(数据, ensure_ascii=False)` |
| `路径` | `路径.rstrip('/')` |
| `路径` | `路径.rstrip('/')` |
| `server` | `self.server` |
| `t` | `threading.Thread(target=服务器.serve_forever, daemon=True)` |
| `相对路径` | `req.路径[len(self.URL前缀):].lstrip('/')` |
| `完整路径` | `os.path.join(self.目录路径, 相对路径)` |
| `内容类型` | `'application/octet-stream'` |
| `过期时间` | `过期时间.strftime('%a, %d %b %Y %H:%M:%S GMT')` |
| `参数` | `路由.匹配(路径)` |
| `result` | `中间件.请求前(req, resp)` |
| `相对路径` | `req.路径[len(url_prefix):].lstrip('/')` |
| `完整路径` | `os.path.join(目录, 相对路径)` |
