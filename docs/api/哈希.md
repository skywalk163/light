# 哈希计算 API

> 模块路径：`stdlib/哈希.py`
> 导入方式：`从 哈希 导入 函数名` 或 `导入 哈希`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `字节转十六进制(数据)` | 字节转十六进制字符串 |
| `十六进制转字节(十六进制字符串)` | 十六进制字符串转字节 |
| `字节转Base64(数据)` | 字节转 Base64 字符串 |
| `创建哈希(算法)` | 创建哈希上下文，返回 哈希上下文 对象 |
| `哈希上下文更新(ctx, 数据)` | 更新哈希上下文（追加数据） |
| `哈希上下文完成(ctx)` | 完成哈希计算，返回十六进制摘要字符串 |
| `哈希上下文完成字节(ctx)` | 完成哈希计算，返回字节摘要 |
| `哈希上下文重置(ctx)` | 重置哈希上下文 |
| `哈希上下文释放(ctx)` | 释放哈希上下文 |
| `哈希上下文获取摘要大小(ctx)` | 获取哈希上下文的摘要大小（字节） |
| `哈希(数据, 算法)` | 计算字符串哈希，返回十六进制摘要 |
| `MD5哈希(数据)` | 计算 MD5 哈希，返回十六进制摘要 |
| `SHA1哈希(数据)` | 计算 SHA1 哈希，返回十六进制摘要 |
| `SHA256哈希(数据)` | 计算 SHA256 哈希，返回十六进制摘要 |
| `SHA512哈希(数据)` | 计算 SHA512 哈希，返回十六进制摘要 |
| `SHA3_256哈希(数据)` | 计算 SHA3-256 哈希，返回十六进制摘要 |
| `BLAKE3哈希(数据)` | 计算 BLAKE3 哈希（回退到 blake2b），返回十六进制摘要 |
| `哈希字节(数据, 算法)` | 计算字节哈希，返回十六进制摘要 |
| `创建HMAC(密钥, 算法)` | 创建 HMAC 上下文，返回 HMAC上下文 对象 |
| `hmac_ctx更新(ctx, 数据)` | 更新 HMAC 上下文 |
| `hmac_ctx完成(ctx)` | 完成 HMAC 计算，返回十六进制摘要 |
| `hmac_ctx完成字节(ctx)` | 完成 HMAC 计算，返回字节摘要 |
| `hmac_ctx重置(ctx)` | 重置 HMAC 上下文 |
| `hmac_ctx释放(ctx)` | 释放 HMAC 上下文 |
| `HMAC哈希(密钥, 数据, 算法)` | 一次性计算 HMAC，返回十六进制摘要 |
| `HMAC哈希字节(密钥, 数据, 算法)` | 一次性计算 HMAC，返回字节摘要 |
| `文件哈希(文件路径, 算法)` | 计算文件哈希，返回十六进制摘要 |
| `文件哈希字节(文件路径, 算法)` | 计算文件哈希，返回字节摘要 |
| `哈希相等(哈希1, 哈希2)` | 安全比较两个哈希值（防时序攻击） |
| `获取算法名称(算法)` | 获取算法标准名称 |
| `获取算法摘要大小(算法)` | 获取算法摘要大小（字节） |
| `获取所有算法()` | 获取所有可用的哈希算法名称列表 |
| `__init__(self, 算法, 摘要大小)` |  |
| `__init__(self, 算法)` |  |

---

## 函数详情

### `字节转十六进制(数据)`

字节转十六进制字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `十六进制转字节(十六进制字符串)`

十六进制字符串转字节

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `十六进制字符串` | `None` |  |

---

### `字节转Base64(数据)`

字节转 Base64 字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `创建哈希(算法)`

创建哈希上下文，返回 哈希上下文 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `算法` | `None` |  |

---

### `哈希上下文更新(ctx, 数据)`

更新哈希上下文（追加数据）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |
| `数据` | `None` |  |

---

### `哈希上下文完成(ctx)`

完成哈希计算，返回十六进制摘要字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `哈希上下文完成字节(ctx)`

完成哈希计算，返回字节摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `哈希上下文重置(ctx)`

重置哈希上下文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `哈希上下文释放(ctx)`

释放哈希上下文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `哈希上下文获取摘要大小(ctx)`

获取哈希上下文的摘要大小（字节）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `哈希(数据, 算法 = 'sha256')`

计算字符串哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `MD5哈希(数据)`

计算 MD5 哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `SHA1哈希(数据)`

计算 SHA1 哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `SHA256哈希(数据)`

计算 SHA256 哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `SHA512哈希(数据)`

计算 SHA512 哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `SHA3_256哈希(数据)`

计算 SHA3-256 哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `BLAKE3哈希(数据)`

计算 BLAKE3 哈希（回退到 blake2b），返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `哈希字节(数据, 算法 = 'sha256')`

计算字节哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `创建HMAC(密钥, 算法 = 'sha256')`

创建 HMAC 上下文，返回 HMAC上下文 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `密钥` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `hmac_ctx更新(ctx, 数据)`

更新 HMAC 上下文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |
| `数据` | `None` |  |

---

### `hmac_ctx完成(ctx)`

完成 HMAC 计算，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `hmac_ctx完成字节(ctx)`

完成 HMAC 计算，返回字节摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `hmac_ctx重置(ctx)`

重置 HMAC 上下文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `hmac_ctx释放(ctx)`

释放 HMAC 上下文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ctx` | `None` |  |

---

### `HMAC哈希(密钥, 数据, 算法 = 'sha256')`

一次性计算 HMAC，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `密钥` | `None` |  |
| `数据` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `HMAC哈希字节(密钥, 数据, 算法 = 'sha256')`

一次性计算 HMAC，返回字节摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `密钥` | `None` |  |
| `数据` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `文件哈希(文件路径, 算法 = 'sha256')`

计算文件哈希，返回十六进制摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `文件哈希字节(文件路径, 算法 = 'sha256')`

计算文件哈希，返回字节摘要

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `哈希相等(哈希1, 哈希2)`

安全比较两个哈希值（防时序攻击）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希1` | `None` |  |
| `哈希2` | `None` |  |

---

### `获取算法名称(算法)`

获取算法标准名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `算法` | `None` |  |

---

### `获取算法摘要大小(算法)`

获取算法摘要大小（字节）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `算法` | `None` |  |

---

### `获取所有算法()`

获取所有可用的哈希算法名称列表

**参数：**

无参数。

---

### `__init__(self, 算法 = '', 摘要大小 = 0)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `算法` | `None` | （默认：''） |
| `摘要大小` | `None` | （默认：0） |

---

### `__init__(self, 算法 = '')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `算法` | `None` | （默认：''） |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `h` | `_hashlib.new(算法)` |
| `ctx` | `哈希上下文(算法=h.name, 摘要大小=h.digest_size)` |
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `密钥` | `密钥.encode('utf-8')` |
| `哈希函数` | `getattr(_hashlib, 算法, None)` |
| `ctx` | `HMAC上下文(算法=算法)` |
| `数据` | `数据.encode('utf-8')` |
| `密钥` | `密钥.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `哈希函数` | `getattr(_hashlib, 算法, None)` |
| `密钥` | `密钥.encode('utf-8')` |
| `数据` | `数据.encode('utf-8')` |
| `哈希函数` | `getattr(_hashlib, 算法, None)` |
| `h` | `_hashlib.new(算法)` |
| `h` | `_hashlib.new(算法)` |
| `哈希1` | `哈希1.encode('utf-8')` |
| `哈希2` | `哈希2.encode('utf-8')` |
| `h` | `_hashlib.new(算法)` |
| `h` | `_hashlib.new(算法)` |
| `块` | `f.read(8192)` |
| `块` | `f.read(8192)` |
