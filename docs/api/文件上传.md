# 文件上传 API

> 模块路径：`stdlib/文件上传.py`
> 导入方式：`从 文件上传 导入 函数名` 或 `导入 文件上传`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `内部取时间戳()` | 获取当前时间戳 |
| `内部包含(字符串, 子串)` | 检查字符串是否包含子串 |
| `范围(开始, 结束, 步长)` | 生成范围列表 |
| `内部分割(字符串, 分隔符)` | 分割字符串 |
| `内部转为小写(字符串)` | 转为小写 |
| `内部整数转字符串(整数)` | 整数转字符串 |
| `内部字符串转整数(字符串)` | 字符串转整数 |
| `内部下一个随机数()` | 生成下一个随机数 |
| `内部随机字符串(长度)` | 生成随机字符串 |
| `创建上传配置(存储目录, 最大文件大小, 允许类型, 覆盖已存在, 唯一文件名)` | 创建上传配置对象 |
| `内部提取边界(content_type)` | 从 Content-Type 中提取 boundary |
| `内部解析内容部署(content_disposition)` | 解析 Content-Disposition 头 |
| `内部解析多部分头(头文本)` | 解析多部分头部 |
| `内部魔术字节检测(文件路径)` | 检测文件魔术字节，返回文件类型签名 |
| `内部生成唯一文件名(原始文件名)` | 生成唯一文件名 |
| `检测文件类型(文件路径)` | 检测文件类型 |
| `检测文件类型从内容(内容)` | 从文件内容检测文件类型 |
| `验证文件类型(文件路径, 允许类型列表)` | 验证文件类型是否在允许列表中 |
| `验证文件大小(文件路径, 最大大小)` | 验证文件大小是否在限制内 |
| `生成存储路径(配置, 文件名)` | 生成存储路径 |
| `解析多部分(数据, 边界)` | 解析多部分表单数据 |
| `解析多部分流式(数据, 边界)` | 流式解析多部分表单数据 |
| `保存文件(部分, 存储路径)` | 保存文件部分到磁盘 |
| `保存所有文件(parts, 配置)` | 保存所有文件部分 |
| `创建本地存储驱动(根目录)` | 创建本地存储驱动 |
| `创建内存存储驱动()` | 创建内存存储驱动 |
| `创建断点续传(文件标识)` | 创建断点续传管理器 |
| `断点续传添加分块(uploader, 分块编号, 数据)` | 添加分块数据 |
| `断点续传获取缺失分块(uploader, 总块数)` | 获取缺失的分块编号列表 |
| `断点续传是否完成(uploader, 总块数)` | 检查上传是否完成 |
| `断点续传合并分块(uploader, 总块数, 输出路径)` | 合并分块为完整文件 |
| `断点续传获取进度(uploader, 总块数)` | 获取上传进度 |
| `__init__(self, 存储目录, 最大文件大小, 允许类型, 覆盖已存在, 唯一文件名)` |  |
| `__init__(self)` |  |
| `__init__(self, 根目录)` |  |
| `保存(self, 文件路径, 数据)` |  |
| `读取(self, 文件路径)` |  |
| `删除(self, 文件路径)` |  |
| `__init__(self)` |  |
| `保存(self, 文件路径, 数据)` |  |
| `读取(self, 文件路径)` |  |
| `删除(self, 文件路径)` |  |
| `__init__(self, 文件标识)` |  |
| `添加分块(self, 分块编号, 数据)` |  |
| `获取缺失分块(self, 总块数)` |  |
| `是否完成(self, 总块数)` |  |
| `合并分块(self, 总块数, 输出路径)` |  |
| `获取进度(self, 总块数)` |  |

---

## 函数详情

### `内部取时间戳()`

获取当前时间戳

**参数：**

无参数。

---

### `内部包含(字符串, 子串)`

检查字符串是否包含子串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符串` | `None` |  |
| `子串` | `None` |  |

---

### `范围(开始, 结束, 步长 = 1)`

生成范围列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `开始` | `None` |  |
| `结束` | `None` |  |
| `步长` | `None` | （默认：1） |

---

### `内部分割(字符串, 分隔符)`

分割字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符串` | `None` |  |
| `分隔符` | `None` |  |

---

### `内部转为小写(字符串)`

转为小写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符串` | `None` |  |

---

### `内部整数转字符串(整数)`

整数转字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `整数` | `None` |  |

---

### `内部字符串转整数(字符串)`

字符串转整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符串` | `None` |  |

---

### `内部下一个随机数()`

生成下一个随机数

**参数：**

无参数。

---

### `内部随机字符串(长度 = 8)`

生成随机字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `长度` | `None` | （默认：8） |

---

### `创建上传配置(存储目录 = './uploads', 最大文件大小 = 10 * 1024 * 1024, 允许类型 = None, 覆盖已存在 = False, 唯一文件名 = True)`

创建上传配置对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `存储目录` | `None` | （默认：'./uploads'） |
| `最大文件大小` | `None` | （默认：10 * 1024 * 1024） |
| `允许类型` | `None` | （默认：None） |
| `覆盖已存在` | `None` | （默认：False） |
| `唯一文件名` | `None` | （默认：True） |

---

### `内部提取边界(content_type)`

从 Content-Type 中提取 boundary

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `content_type` | `None` |  |

---

### `内部解析内容部署(content_disposition)`

解析 Content-Disposition 头

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `content_disposition` | `None` |  |

---

### `内部解析多部分头(头文本)`

解析多部分头部

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `头文本` | `None` |  |

---

### `内部魔术字节检测(文件路径)`

检测文件魔术字节，返回文件类型签名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `内部生成唯一文件名(原始文件名)`

生成唯一文件名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `原始文件名` | `None` |  |

---

### `检测文件类型(文件路径)`

检测文件类型

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `检测文件类型从内容(内容)`

从文件内容检测文件类型

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `内容` | `None` |  |

---

### `验证文件类型(文件路径, 允许类型列表)`

验证文件类型是否在允许列表中

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |
| `允许类型列表` | `None` |  |

---

### `验证文件大小(文件路径, 最大大小)`

验证文件大小是否在限制内

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |
| `最大大小` | `None` |  |

---

### `生成存储路径(配置, 文件名)`

生成存储路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置` | `None` |  |
| `文件名` | `None` |  |

---

### `解析多部分(数据, 边界)`

解析多部分表单数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `边界` | `None` |  |

---

### `解析多部分流式(数据, 边界)`

流式解析多部分表单数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `边界` | `None` |  |

---

### `保存文件(部分, 存储路径)`

保存文件部分到磁盘

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `部分` | `None` |  |
| `存储路径` | `None` |  |

---

### `保存所有文件(parts, 配置)`

保存所有文件部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `parts` | `None` |  |
| `配置` | `None` |  |

---

### `创建本地存储驱动(根目录)`

创建本地存储驱动

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `根目录` | `None` |  |

---

### `创建内存存储驱动()`

创建内存存储驱动

**参数：**

无参数。

---

### `创建断点续传(文件标识)`

创建断点续传管理器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件标识` | `None` |  |

---

### `断点续传添加分块(uploader, 分块编号, 数据)`

添加分块数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `uploader` | `None` |  |
| `分块编号` | `None` |  |
| `数据` | `None` |  |

---

### `断点续传获取缺失分块(uploader, 总块数)`

获取缺失的分块编号列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `uploader` | `None` |  |
| `总块数` | `None` |  |

---

### `断点续传是否完成(uploader, 总块数)`

检查上传是否完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `uploader` | `None` |  |
| `总块数` | `None` |  |

---

### `断点续传合并分块(uploader, 总块数, 输出路径)`

合并分块为完整文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `uploader` | `None` |  |
| `总块数` | `None` |  |
| `输出路径` | `None` |  |

---

### `断点续传获取进度(uploader, 总块数)`

获取上传进度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `uploader` | `None` |  |
| `总块数` | `None` |  |

---

### `__init__(self, 存储目录 = './uploads', 最大文件大小 = 10 * 1024 * 1024, 允许类型 = None, 覆盖已存在 = False, 唯一文件名 = True)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `存储目录` | `None` | （默认：'./uploads'） |
| `最大文件大小` | `None` | （默认：10 * 1024 * 1024） |
| `允许类型` | `None` | （默认：None） |
| `覆盖已存在` | `None` | （默认：False） |
| `唯一文件名` | `None` | （默认：True） |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 根目录)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `根目录` | `None` |  |

---

### `保存(self, 文件路径, 数据)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件路径` | `None` |  |
| `数据` | `None` |  |

---

### `读取(self, 文件路径)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件路径` | `None` |  |

---

### `删除(self, 文件路径)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件路径` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `保存(self, 文件路径, 数据)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件路径` | `None` |  |
| `数据` | `None` |  |

---

### `读取(self, 文件路径)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件路径` | `None` |  |

---

### `删除(self, 文件路径)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件路径` | `None` |  |

---

### `__init__(self, 文件标识)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `文件标识` | `None` |  |

---

### `添加分块(self, 分块编号, 数据)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `分块编号` | `None` |  |
| `数据` | `None` |  |

---

### `获取缺失分块(self, 总块数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `总块数` | `None` |  |

---

### `是否完成(self, 总块数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `总块数` | `None` |  |

---

### `合并分块(self, 总块数, 输出路径)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `总块数` | `None` |  |
| `输出路径` | `None` |  |

---

### `获取进度(self, 总块数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `总块数` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `parts` | `content_type.split(';')` |
| `result` | `{}` |
| `parts` | `content_disposition.split(';')` |
| `headers` | `{}` |
| `ext` | `_os.path.splitext(原始文件名)[1]` |
| `part` | `part.strip()` |
| `part` | `part.strip()` |
| `line` | `line.strip()` |
| `magic` | `内部魔术字节检测(文件路径)` |
| `文件类型` | `检测文件类型(文件路径)` |
| `size` | `_os.path.getsize(文件路径)` |
| `路径` | `_os.path.join(配置.存储目录, 文件名)` |
| `parts` | `[]` |
| `delimiter` | `b'--' + 边界` |
| `raw_parts` | `数据.split(delimiter)` |
| `目录` | `_os.path.dirname(存储路径)` |
| `saved` | `[]` |
| `full_path` | `_os.path.join(self.根目录, 文件路径)` |
| `dir_name` | `_os.path.dirname(full_path)` |
| `full_path` | `_os.path.join(self.根目录, 文件路径)` |
| `full_path` | `_os.path.join(self.根目录, 文件路径)` |
| `内容` | `内容.encode('utf-8')` |
| `文件名` | `内部生成唯一文件名(文件名)` |
| `数据` | `数据.encode('utf-8')` |
| `边界` | `边界.encode('utf-8')` |
| `header_end` | `raw_part.find(b'\r\n\r\n')` |
| `header_bytes` | `raw_part[:header_end]` |
| `body_bytes` | `raw_part[header_end + 4:]` |
| `headers` | `{}` |
| `part` | `文件部分()` |
| `cd` | `headers.get('content-disposition', '')` |
| `cd_info` | `内部解析内容部署(cd)` |
| `raw_part` | `raw_part[2:]` |
| `raw_part` | `raw_part[:-4]` |
| `路径` | `生成存储路径(配置, part.文件名)` |
| `raw_part` | `raw_part[:-3]` |
| `raw_part` | `raw_part[:-2]` |
