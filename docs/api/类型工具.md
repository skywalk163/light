# 类型工具 API

> 模块路径：`stdlib/类型工具.py`
> 导入方式：`从 类型工具 导入 函数名` 或 `导入 类型工具`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `获取数字字符映射()` | 获取数字字符到数值的映射字典 |
| `获取数字到字符()` | 获取数值到字符的映射字典 |
| `型别检查(值)` | 获取值的类型名称 |
| `是整数(值)` | 判断是否为整数 |
| `是浮点数(值)` | 判断是否为浮点数 |
| `是字符串(值)` | 判断是否为字符串 |
| `是布尔(值)` | 判断是否为布尔值 |
| `是列表(值)` | 判断是否为列表 |
| `是字典(值)` | 判断是否为字典 |
| `是空(值)` | 判断值是否为空（None, 空字符串, 空列表, 空字典） |
| `转整数(值)` | 将值转为整数 |
| `字符串转整数(字符串, 基数)` | 将字符串转为整数，支持指定基数 |
| `转浮点数(值)` | 将值转为浮点数 |
| `字符串转浮点数(字符串)` | 将字符串转为浮点数 |
| `转字符串(值)` | 将值转为字符串 |
| `整数转字符串(整数)` | 将整数转为字符串 |
| `浮点数转字符串(浮点数)` | 将浮点数转为字符串 |
| `转布尔(值)` | 将值转为布尔值 |
| `可空(值)` | 创建一个可空值 |
| `可空_有值(nullable)` | 检查可空是否有值 |
| `可空_取值(nullable, 默认值)` | 获取可空的值，无值时返回默认值 |
| `可空_或缺省(nullable, 默认值)` | 可空取值或返回默认值 |
| `可空_或计算(nullable, 计算函数)` | 可空取值或执行计算函数 |
| `型别配(值, 类型映射)` | 根据值的类型匹配执行对应的处理函数。 |
| `__init__(self, value, has_value)` |  |
| `has_value(self)` |  |
| `get_value(self)` |  |

---

## 函数详情

### `获取数字字符映射()`

获取数字字符到数值的映射字典

**参数：**

无参数。

---

### `获取数字到字符()`

获取数值到字符的映射字典

**参数：**

无参数。

---

### `型别检查(值)`

获取值的类型名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `是整数(值)`

判断是否为整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `是浮点数(值)`

判断是否为浮点数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `是字符串(值)`

判断是否为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `是布尔(值)`

判断是否为布尔值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `是列表(值)`

判断是否为列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `是字典(值)`

判断是否为字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `是空(值)`

判断值是否为空（None, 空字符串, 空列表, 空字典）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `转整数(值)`

将值转为整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `字符串转整数(字符串, 基数 = 10)`

将字符串转为整数，支持指定基数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符串` | `None` |  |
| `基数` | `None` | （默认：10） |

---

### `转浮点数(值)`

将值转为浮点数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `字符串转浮点数(字符串)`

将字符串转为浮点数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符串` | `None` |  |

---

### `转字符串(值)`

将值转为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `整数转字符串(整数)`

将整数转为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `整数` | `None` |  |

---

### `浮点数转字符串(浮点数)`

将浮点数转为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `浮点数` | `None` |  |

---

### `转布尔(值)`

将值转为布尔值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `可空(值 = None)`

创建一个可空值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` | （默认：None） |

---

### `可空_有值(nullable)`

检查可空是否有值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `nullable` | `None` |  |

---

### `可空_取值(nullable, 默认值 = None)`

获取可空的值，无值时返回默认值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `nullable` | `None` |  |
| `默认值` | `None` | （默认：None） |

---

### `可空_或缺省(nullable, 默认值)`

可空取值或返回默认值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `nullable` | `None` |  |
| `默认值` | `None` |  |

---

### `可空_或计算(nullable, 计算函数)`

可空取值或执行计算函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `nullable` | `None` |  |
| `计算函数` | `None` |  |

---

### `型别配(值, 类型映射)`

根据值的类型匹配执行对应的处理函数。
类型映射: {类型名: 处理函数, ...}

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |
| `类型映射` | `None` |  |

---

### `__init__(self, value = None, has_value = False)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `value` | `None` | （默认：None） |
| `has_value` | `None` | （默认：False） |

---

### `has_value(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `get_value(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `t` | `type(值)` |
| `type_name` | `type(值).__name__` |
| `handler` | `类型映射.get(type_name)` |
| `handler` | `类型映射.get('default')` |
