# 证书 API

> 模块路径：`stdlib/证书.py`
> 导入方式：`从 证书 导入 函数名` 或 `导入 证书`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `_parse_subject_dict(主体文本)` | 解析主体字符串为字典 |
| `解析证书(pem文本)` | 解析 PEM 格式证书，返回 Certificate 对象 |
| `解析证书DER(der数据)` | 解析 DER 格式证书 |
| `cert_get_subject(cert)` | 获取证书主体 |
| `cert_get_issuer(cert)` | 获取证书颁发者 |
| `cert_get_serial(cert)` | 获取证书序列号 |
| `cert_get_validity(cert)` | 获取证书有效期 |
| `cert_get_fingerprint(cert, 算法)` | 获取证书指纹 |
| `cert_get_pub_key(cert)` | 获取证书公钥 |
| `cert_get_pub_key_algo(cert)` | 获取证书公钥算法 |
| `cert_get_sig_algo(cert)` | 获取证书签名算法 |
| `cert_get_version(cert)` | 获取证书版本 |
| `cert_get_raw_pem(cert)` | 获取证书原始 PEM 文本 |
| `cert_is_self_signed(cert)` | 判断证书是否自签名 |
| `cert_is_expired(cert)` | 判断证书是否已过期 |
| `cert_days_left(cert)` | 获取证书剩余有效天数 |
| `cert_verify(cert, ca_cert)` | 验证证书（简化实现） |
| `生成自签名证书(通用名称, 组织, 国家, 有效天数)` | 生成自签名证书（简化实现） |
| `生成密钥对(算法, 位数)` | 生成密钥对（简化实现） |
| `生成CSR(通用名称, 私钥, 组织, 国家)` | 生成证书签名请求（CSR） |
| `csr_get_raw_pem(csr)` | 获取 CSR 原始 PEM 文本 |
| `csr_get_subject(csr)` | 获取 CSR 主体 |
| `csr_get_pub_pem(csr)` | 获取 CSR 公钥 PEM |
| `检查CRL(cert, crl)` | 检查证书是否在 CRL 中 |
| `加载CRL(crl文本)` | 加载 CRL（简化实现） |
| `crl_is_revoked(crl, cert)` | 检查证书是否被吊销 |
| `crl_get_revoke_date(crl, cert)` | 获取证书吊销日期 |
| `crl_get_revoke_ason(crl)` | 获取 CRL 更新日期 |
| `crl_get_err(crl)` | 获取 CRL 错误信息 |
| `cert_pem_to_der(pem文本)` | PEM 转 DER |
| `PEM转DER(pem文本)` | PEM 转 DER |
| `DER转PEM(der数据)` | DER 转 PEM |
| `verify_is_valid(验证结果)` | 检查验证结果是否有效 |
| `verify_get_err(验证结果)` | 获取验证错误信息 |
| `verify_get_chain_depth(验证结果)` | 获取验证链深度 |
| `verify_get_verify_path(验证结果)` | 获取验证路径 |
| `释放证书(cert)` | 释放证书资源 |
| `__init__(self)` |  |

---

## 函数详情

### `_parse_subject_dict(主体文本)`

解析主体字符串为字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主体文本` | `None` |  |

---

### `解析证书(pem文本)`

解析 PEM 格式证书，返回 Certificate 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pem文本` | `None` |  |

---

### `解析证书DER(der数据)`

解析 DER 格式证书

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `der数据` | `None` |  |

---

### `cert_get_subject(cert)`

获取证书主体

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_issuer(cert)`

获取证书颁发者

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_serial(cert)`

获取证书序列号

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_validity(cert)`

获取证书有效期

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_fingerprint(cert, 算法 = 'sha256')`

获取证书指纹

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |
| `算法` | `None` | （默认：'sha256'） |

---

### `cert_get_pub_key(cert)`

获取证书公钥

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_pub_key_algo(cert)`

获取证书公钥算法

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_sig_algo(cert)`

获取证书签名算法

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_version(cert)`

获取证书版本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_get_raw_pem(cert)`

获取证书原始 PEM 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_is_self_signed(cert)`

判断证书是否自签名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_is_expired(cert)`

判断证书是否已过期

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_days_left(cert)`

获取证书剩余有效天数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `cert_verify(cert, ca_cert = None)`

验证证书（简化实现）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |
| `ca_cert` | `None` | （默认：None） |

---

### `生成自签名证书(通用名称 = 'localhost', 组织 = '', 国家 = '', 有效天数 = 365)`

生成自签名证书（简化实现）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `通用名称` | `None` | （默认：'localhost'） |
| `组织` | `None` | （默认：''） |
| `国家` | `None` | （默认：''） |
| `有效天数` | `None` | （默认：365） |

---

### `生成密钥对(算法 = 'RSA', 位数 = 2048)`

生成密钥对（简化实现）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `算法` | `None` | （默认：'RSA'） |
| `位数` | `None` | （默认：2048） |

---

### `生成CSR(通用名称, 私钥 = None, 组织 = None, 国家 = None)`

生成证书签名请求（CSR）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `通用名称` | `None` |  |
| `私钥` | `None` | （默认：None） |
| `组织` | `None` | （默认：None） |
| `国家` | `None` | （默认：None） |

---

### `csr_get_raw_pem(csr)`

获取 CSR 原始 PEM 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `csr` | `None` |  |

---

### `csr_get_subject(csr)`

获取 CSR 主体

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `csr` | `None` |  |

---

### `csr_get_pub_pem(csr)`

获取 CSR 公钥 PEM

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `csr` | `None` |  |

---

### `检查CRL(cert, crl)`

检查证书是否在 CRL 中

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |
| `crl` | `None` |  |

---

### `加载CRL(crl文本)`

加载 CRL（简化实现）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `crl文本` | `None` |  |

---

### `crl_is_revoked(crl, cert)`

检查证书是否被吊销

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `crl` | `None` |  |
| `cert` | `None` |  |

---

### `crl_get_revoke_date(crl, cert)`

获取证书吊销日期

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `crl` | `None` |  |
| `cert` | `None` |  |

---

### `crl_get_revoke_ason(crl)`

获取 CRL 更新日期

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `crl` | `None` |  |

---

### `crl_get_err(crl)`

获取 CRL 错误信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `crl` | `None` |  |

---

### `cert_pem_to_der(pem文本)`

PEM 转 DER

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pem文本` | `None` |  |

---

### `PEM转DER(pem文本)`

PEM 转 DER

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pem文本` | `None` |  |

---

### `DER转PEM(der数据)`

DER 转 PEM

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `der数据` | `None` |  |

---

### `verify_is_valid(验证结果)`

检查验证结果是否有效

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `验证结果` | `None` |  |

---

### `verify_get_err(验证结果)`

获取验证错误信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `验证结果` | `None` |  |

---

### `verify_get_chain_depth(验证结果)`

获取验证链深度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `验证结果` | `None` |  |

---

### `verify_get_verify_path(验证结果)`

获取验证路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `验证结果` | `None` |  |

---

### `释放证书(cert)`

释放证书资源

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cert` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `result` | `{}` |
| `part` | `part.strip()` |
| `cert` | `Certificate()` |
| `b64_data` | `_b64.b64encode(der数据).decode('ascii')` |
| `pem_text` | `f'-----BEGIN CERTIFICATE-----\n{b64_data}\n-----END CERTIFICATE-----'` |
| `delta` | `cert._not_after - _datetime.datetime.now()` |
| `now` | `_datetime.datetime.now()` |
| `not_after` | `now + _datetime.timedelta(days=有效天数)` |
| `cert` | `Certificate()` |
| `pem_data` | `f'-----BEGIN CERTIFICATE-----\nMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA\n-----END CERTIFICATE-----'` |
| `subject` | `f'CN={通用名称}'` |
| `pem_data` | `f'-----BEGIN CERTIFICATE REQUEST-----\nMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA\n-----END CERTIFICATE REQUEST-----'` |
| `csr` | `Certificate()` |
| `lines` | `pem文本.strip().split('\n')` |
| `b64_lines` | `[line for line in lines if not line.startswith('-----')]` |
| `b64_data` | `''.join(b64_lines)` |
| `b64_data` | `_b64.b64encode(der数据).decode('ascii')` |
| `lines` | `['-----BEGIN CERTIFICATE-----']` |
| `pem文本` | `pem文本.encode('utf-8')` |
| `ctx` | `_ssl.create_default_context()` |
| `pem_path` | `f.name` |
