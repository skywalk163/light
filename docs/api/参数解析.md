# 命令行参数解析 API

> 模块路径：`stdlib/参数解析.py`
> 导入方式：`从 参数解析 导入 函数名` 或 `导入 参数解析`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `简单解析(参数定义, 参数列表)` | 简单参数解析（便捷函数） |
| `快速获取(参数名, 默认值, 参数列表)` | 快速获取单个参数值 |
| `__init__(self, 程序名, 描述, 版本)` | 初始化参数解析器 |
| `添加参数(self, 名称, 短名称, 类型, 默认值, 描述, 必需, 取值列表, 多值, 标志, 计数)` | 添加命令行参数 |
| `添加位置参数(self, 名称, 类型, 默认值, 描述, 可选, 多值)` | 添加位置参数 |
| `添加子命令(self, 名称, 描述)` | 添加子命令 |
| `解析(self, 参数列表)` | 解析命令行参数 |
| `获取(self, 参数名, 默认值)` | 获取解析后的参数值 |
| `打印帮助(self)` | 打印帮助信息 |
| `帮助文本(self)` | 获取帮助文本 |
| `用法文本(self)` | 获取用法文本 |

---

## 函数详情

### `简单解析(参数定义: List[Dict[str, Any]], 参数列表: List[str] = None) -> Dict[str, Any]`

简单参数解析（便捷函数）

参数:
    参数定义: 参数定义列表，每个元素是字典
    参数列表: 要解析的参数列表

返回:
    参数字典

示例:
    参数定义 = [
        {'名称': '文件', '描述': '输入文件'},
        {'名称': '--输出', '短名称': '-o', '描述': '输出文件'},
        {'名称': '-- verbose', '短名称': '-v', '标志': True, '描述': '详细输出'},
    ]

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `参数定义` | `List[Dict[str, Any]]` |  |
| `参数列表` | `List[str]` | （默认：None） |

**返回：** `Dict[str, Any]`

---

### `快速获取(参数名: str, 默认值: Any = None, 参数列表: List[str] = None) -> Any`

快速获取单个参数值

参数:
    参数名: 参数名（如 '--output' 或 'filename'）
    默认值: 默认值
    参数列表: 参数列表

返回:
    参数值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `参数名` | `str` |  |
| `默认值` | `Any` | （默认：None） |
| `参数列表` | `List[str]` | （默认：None） |

**返回：** `Any`

---

### `__init__(self, 程序名: str = None, 描述: str = '', 版本: str = None)`

初始化参数解析器

参数:
    程序名: 程序名称
    描述: 程序描述
    版本: 版本号

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `程序名` | `str` | （默认：None） |
| `描述` | `str` | （默认：''） |
| `版本` | `str` | （默认：None） |

---

### `添加参数(self, 名称: str, 短名称: str = None, 类型: type = str, 默认值: Any = None, 描述: str = '', 必需: bool = False, 取值列表: List[Any] = None, 多值: bool = False, 标志: bool = False, 计数: bool = False) -> None`

添加命令行参数

参数:
    名称: 参数名称（长选项）
    短名称: 短选项名称
    类型: 参数类型
    默认值: 默认值
    描述: 参数描述
    必需: 是否必需
    取值列表: 允许的取值列表
    多值: 是否接受多个值
    标志: 是否为布尔标志
    计数: 是否为计数参数（如 -vvv）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `str` |  |
| `短名称` | `str` | （默认：None） |
| `类型` | `type` | （默认：str） |
| `默认值` | `Any` | （默认：None） |
| `描述` | `str` | （默认：''） |
| `必需` | `bool` | （默认：False） |
| `取值列表` | `List[Any]` | （默认：None） |
| `多值` | `bool` | （默认：False） |
| `标志` | `bool` | （默认：False） |
| `计数` | `bool` | （默认：False） |

**返回：** `None`

---

### `添加位置参数(self, 名称: str, 类型: type = str, 默认值: Any = None, 描述: str = '', 可选: bool = False, 多值: bool = False) -> None`

添加位置参数

参数:
    名称: 参数名称
    类型: 参数类型
    默认值: 默认值
    描述: 参数描述
    可选: 是否可选
    多值: 是否接受多个值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `str` |  |
| `类型` | `type` | （默认：str） |
| `默认值` | `Any` | （默认：None） |
| `描述` | `str` | （默认：''） |
| `可选` | `bool` | （默认：False） |
| `多值` | `bool` | （默认：False） |

**返回：** `None`

---

### `添加子命令(self, 名称: str, 描述: str = '') -> '参数解析器'`

添加子命令

参数:
    名称: 子命令名称
    描述: 子命令描述

返回:
    子命令的参数解析器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `str` |  |
| `描述` | `str` | （默认：''） |

**返回：** `'参数解析器'`

---

### `解析(self, 参数列表: List[str] = None) -> Dict[str, Any]`

解析命令行参数

参数:
    参数列表: 参数列表，默认为sys.argv[1:]

返回:
    参数字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `参数列表` | `List[str]` | （默认：None） |

**返回：** `Dict[str, Any]`

---

### `获取(self, 参数名: str, 默认值: Any = None) -> Any`

获取解析后的参数值

参数:
    参数名: 参数名称
    默认值: 不存在时的默认值

返回:
    参数值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `参数名` | `str` |  |
| `默认值` | `Any` | （默认：None） |

**返回：** `Any`

---

### `打印帮助(self) -> None`

打印帮助信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `None`

---

### `帮助文本(self) -> str`

获取帮助文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `str`

---

### `用法文本(self) -> str`

获取用法文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `str`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `解析器` | `参数解析器()` |
| `解析器` | `参数解析器()` |
| `结果` | `解析器.解析(参数列表)` |
| `参数列表` | `[]` |
| `是否位置参数` | `False` |
| `kwargs` | `{'help': 描述}` |
| `kwargs` | `{'help': 描述, 'type': 类型}` |
| `子解析器` | `self._子解析器.add_parser(名称, help=描述)` |
| `子封装` | `参数解析器.__new__(参数解析器)` |
| `args` | `self._解析器.parse_args(参数列表)` |
| `目标名` | `名称.lstrip('--').replace('-', '_')` |
| `目标名` | `名称.lstrip('-').replace('-', '_')` |
| `目标名` | `名称.replace('-', '_')` |
| `是否位置参数` | `True` |
