# FFI 外部函数接口 API

> 模块路径：`stdlib/FFI.py`
> 导入方式：`从 FFI 导入 函数名` 或 `导入 FFI`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `加载库(路径, 别名)` | 加载动态库：加载库(路径, 别名) |
| `获取库(别名)` | 获取已加载的库：获取库(别名) |
| `注册类型(名称, 类型)` | 注册用户自定义类型（struct/union/funcptr/typedef）供 获取类型 查找 |
| `获取类型(类型名)` | 获取对应的 ctypes 类型。优先查找 FFI_TYPE_MAP，再查找用户自定义类型注册表， |
| `获取类型或空(类型名)` | 获取类型，找不到时返回 None 而非默认值 |
| `声明函数(库别名, 函数名, 参数类型, 返回类型)` | 声明外部函数 |
| `调用函数(库别名, 函数名, 参数)` | 调用外部函数 |
| `编码文本(s)` | 将文本编码为字节串 |
| `解码文本(b)` | 将字节串解码为文本 |
| `内存分配(大小)` | 分配内存块 |
| `内存释放(ptr)` | 释放内存（Python 自动管理） |
| `取地址(变量)` | 获取变量的指针 |
| `创建结构体(结构体类)` | 创建结构体实例 |
| `获取字段(结构体实例, 字段名)` | 获取结构体字段值 |
| `设置字段(结构体实例, 字段名, 值)` | 设置结构体字段值 |
| `创建回调(回调类型, 回调函数)` | 创建 C 回调函数 |
| `取地址(变量)` | 获取变量的指针（ctypes 指针） |
| `解引用(指针)` | 解引用指针获取值 |
| `指针偏移(指针, 偏移量)` | 指针偏移：返回指针 + 偏移量 的地址 |
| `创建数组(类型, 大小)` | 创建 C 类型数组，支持字符串类型名或 ctypes 类对象 |
| `设置数组(数组, 索引, 值)` | 设置数组元素值 |
| `分配内存(大小)` | 分配内存块 |
| `释放内存(指针)` | 释放内存 |
| `设指针值(指针, 值)` | 通过指针写入值 |
| `获取FFI错误()` | 获取最后一次 FFI 错误消息 |
| `_set_ffi_error(error)` | 设置 FFI 错误（内部使用） |
| `获取系统错误码()` | 获取系统 errno |
| `设系统错误码(值)` | 设置系统 errno |
| `关闭所有库()` | 关闭所有已加载的库 |
| `创建回调函数(回调类型, 段言函数)` | 创建C回调函数指针 |
| `创建结构体值(结构体类)` | 创建结构体实例（用于按值传递） |
| `创建枚举(枚举名, 值字典)` | 动态创建C语言兼容的枚举类 |
| `创建联合体(联合体名, 字段列表)` | 动态创建C语言联合体类型 |
| `解析库路径(平台映射)` | 根据当前平台自动选择库文件路径 |
| `变长参数调用(库别名, 函数名, 固定参数, 可变参数)` | 调用变长参数函数 |
| `获取平台()` | 获取当前平台标识 |
| `查找库(库名)` | 使用 ctypes.util.find_library 查找系统库 |
| `取地址增强(变量)` | 获取变量的指针（增强版：支持结构体按值传递） |
| `结构体大小(结构体类)` | 获取结构体的大小（字节） |
| `字段偏移(结构体类, 字段名)` | 获取结构体字段的偏移量 |
| `结构体转字节(结构体实例)` | 将结构体序列化为字节串 |
| `字节转结构体(数据, 结构体类)` | 从字节串反序列化为结构体 |
| `注册回调(名称, 回调对象)` | 注册回调函数，防止被GC回收 |
| `注销回调(键)` | 注销回调函数 |
| `获取回调(键)` | 获取已注册的回调函数 |
| `列出回调()` | 列出所有已注册的回调键 |
| `清理回调()` | 清理所有已注册的回调 |
| `设置调试()` | 设置FFI调试配置 |
| `启用调试()` | 启用FFI调试 |
| `禁用调试()` | 禁用FFI调试 |
| `获取日志()` | 获取FFI调试日志 |
| `清空日志()` | 清空FFI调试日志 |
| `FFI调试()` | FFI调试（别名） |
| `FFI禁用调试()` | FFI禁用调试（别名） |
| `FFI获取日志()` | FFI获取日志（别名） |
| `_debug_log_call(函数名, 参数, 返回值)` | 内部：记录函数调用 |
| `_debug_log_type(类型名, 详情)` | 内部：记录类型信息 |
| `_debug_log_memory(操作, 大小)` | 内部：记录内存操作 |
| `位域设置(结构体实例, 字段名, 值)` | 设置位域字段的值 |
| `位域获取(结构体实例, 字段名)` | 获取位域字段的值 |
| `创建函数指针(签名类型)` | 创建C函数指针类型 |
| `创建类型别名(别名, 基础类型)` | 创建C类型别名（返回基础类型） |
| `定义宏(名称, 值)` | 定义C预处理器宏 |
| `获取宏(名称)` | 获取宏的值 |
| `列出宏()` | 列出所有已定义的宏 |
| `清理宏()` | 清理所有宏定义 |
| `__init__(self, path)` |  |
| `load(self)` | 加载动态库 |
| `get_function(self, name)` | 获取库中的函数 |
| `declare_function(self, name, arg_types, restype)` | 声明并设置函数签名 |
| `close(self)` | 关闭动态库句柄 |
| `__init__(self)` |  |
| `load_library(self, path, alias)` | 加载动态库并注册别名 |
| `get_library(self, alias)` | 获取已加载的库 |
| `close_all(self)` | 关闭所有已加载的库 |

---

## 函数详情

### `加载库(路径: str, 别名: str) -> FFILibrary`

加载动态库：加载库(路径, 别名)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |
| `别名` | `str` |  |

**返回：** `FFILibrary`

---

### `获取库(别名: str) -> Optional[FFILibrary]`

获取已加载的库：获取库(别名)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `别名` | `str` |  |

**返回：** `Optional[FFILibrary]`

---

### `注册类型(名称: str, 类型: Any)`

注册用户自定义类型（struct/union/funcptr/typedef）供 获取类型 查找

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |
| `类型` | `Any` |  |

---

### `获取类型(类型名: str) -> Any`

获取对应的 ctypes 类型。优先查找 FFI_TYPE_MAP，再查找用户自定义类型注册表，
最后尝试通过 eval 解析全局作用域中的类型名（如 struct/union 类名）。

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `类型名` | `str` |  |

**返回：** `Any`

---

### `获取类型或空(类型名: str) -> Optional[Any]`

获取类型，找不到时返回 None 而非默认值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `类型名` | `str` |  |

**返回：** `Optional[Any]`

---

### `声明函数(库别名: str, 函数名: str, 参数类型: List[str], 返回类型: str) -> Any`

声明外部函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `库别名` | `str` |  |
| `函数名` | `str` |  |
| `参数类型` | `List[str]` |  |
| `返回类型` | `str` |  |

**返回：** `Any`

---

### `调用函数(库别名: str, 函数名: str, 参数: List[Any]) -> Any`

调用外部函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `库别名` | `str` |  |
| `函数名` | `str` |  |
| `参数` | `List[Any]` |  |

**返回：** `Any`

---

### `编码文本(s: str) -> bytes`

将文本编码为字节串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `s` | `str` |  |

**返回：** `bytes`

---

### `解码文本(b) -> str`

将字节串解码为文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `b` | `None` |  |

**返回：** `str`

---

### `内存分配(大小: int) -> Any`

分配内存块

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `大小` | `int` |  |

**返回：** `Any`

---

### `内存释放(ptr: Any)`

释放内存（Python 自动管理）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ptr` | `Any` |  |

---

### `取地址(变量) -> Any`

获取变量的指针

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `变量` | `None` |  |

**返回：** `Any`

---

### `创建结构体(结构体类: Any) -> Any`

创建结构体实例

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体类` | `Any` |  |

**返回：** `Any`

---

### `获取字段(结构体实例: Any, 字段名: str) -> Any`

获取结构体字段值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体实例` | `Any` |  |
| `字段名` | `str` |  |

**返回：** `Any`

---

### `设置字段(结构体实例: Any, 字段名: str, 值: Any)`

设置结构体字段值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体实例` | `Any` |  |
| `字段名` | `str` |  |
| `值` | `Any` |  |

---

### `创建回调(回调类型, 回调函数) -> Any`

创建 C 回调函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `回调类型` | `None` |  |
| `回调函数` | `None` |  |

**返回：** `Any`

---

### `取地址(变量) -> Any`

获取变量的指针（ctypes 指针）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `变量` | `None` |  |

**返回：** `Any`

---

### `解引用(指针) -> Any`

解引用指针获取值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |

**返回：** `Any`

---

### `指针偏移(指针, 偏移量: int) -> Any`

指针偏移：返回指针 + 偏移量 的地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |
| `偏移量` | `int` |  |

**返回：** `Any`

---

### `创建数组(类型, 大小: int) -> Any`

创建 C 类型数组，支持字符串类型名或 ctypes 类对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `类型` | `None` |  |
| `大小` | `int` |  |

**返回：** `Any`

---

### `设置数组(数组, 索引: int, 值: Any)`

设置数组元素值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数组` | `None` |  |
| `索引` | `int` |  |
| `值` | `Any` |  |

---

### `分配内存(大小: int) -> Any`

分配内存块

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `大小` | `int` |  |

**返回：** `Any`

---

### `释放内存(指针)`

释放内存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |

---

### `设指针值(指针, 值)`

通过指针写入值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |
| `值` | `None` |  |

---

### `获取FFI错误() -> str`

获取最后一次 FFI 错误消息

**参数：**

无参数。

**返回：** `str`

---

### `_set_ffi_error(error)`

设置 FFI 错误（内部使用）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `error` | `None` |  |

---

### `获取系统错误码() -> int`

获取系统 errno

**参数：**

无参数。

**返回：** `int`

---

### `设系统错误码(值: int)`

设置系统 errno

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `int` |  |

---

### `关闭所有库()`

关闭所有已加载的库

**参数：**

无参数。

---

### `创建回调函数(回调类型, 段言函数)`

创建C回调函数指针

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `回调类型` | `None` |  |
| `段言函数` | `None` |  |

---

### `创建结构体值(结构体类)`

创建结构体实例（用于按值传递）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体类` | `None` |  |

---

### `创建枚举(枚举名: str, 值字典: Dict[str, int]) -> type`

动态创建C语言兼容的枚举类

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `枚举名` | `str` |  |
| `值字典` | `Dict[str, int]` |  |

**返回：** `type`

---

### `创建联合体(联合体名: str, 字段列表: List[Tuple[str, Any]]) -> type`

动态创建C语言联合体类型

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `联合体名` | `str` |  |
| `字段列表` | `List[Tuple[str, Any]]` |  |

**返回：** `type`

---

### `解析库路径(平台映射: Dict[str, str]) -> str`

根据当前平台自动选择库文件路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `平台映射` | `Dict[str, str]` |  |

**返回：** `str`

---

### `变长参数调用(库别名: str, 函数名: str, 固定参数: List[Any], 可变参数: List[Any]) -> Any`

调用变长参数函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `库别名` | `str` |  |
| `函数名` | `str` |  |
| `固定参数` | `List[Any]` |  |
| `可变参数` | `List[Any]` |  |

**返回：** `Any`

---

### `获取平台() -> str`

获取当前平台标识

**参数：**

无参数。

**返回：** `str`

---

### `查找库(库名: str) -> Optional[str]`

使用 ctypes.util.find_library 查找系统库

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `库名` | `str` |  |

**返回：** `Optional[str]`

---

### `取地址增强(变量) -> Any`

获取变量的指针（增强版：支持结构体按值传递）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `变量` | `None` |  |

**返回：** `Any`

---

### `结构体大小(结构体类) -> int`

获取结构体的大小（字节）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体类` | `None` |  |

**返回：** `int`

---

### `字段偏移(结构体类, 字段名: str) -> int`

获取结构体字段的偏移量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体类` | `None` |  |
| `字段名` | `str` |  |

**返回：** `int`

---

### `结构体转字节(结构体实例) -> bytes`

将结构体序列化为字节串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体实例` | `None` |  |

**返回：** `bytes`

---

### `字节转结构体(数据: bytes, 结构体类) -> Any`

从字节串反序列化为结构体

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |
| `结构体类` | `None` |  |

**返回：** `Any`

---

### `注册回调(名称: str, 回调对象: Any) -> str`

注册回调函数，防止被GC回收

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |
| `回调对象` | `Any` |  |

**返回：** `str`

---

### `注销回调(键: str) -> bool`

注销回调函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `键` | `str` |  |

**返回：** `bool`

---

### `获取回调(键: str) -> Optional[Any]`

获取已注册的回调函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `键` | `str` |  |

**返回：** `Optional[Any]`

---

### `列出回调() -> List[str]`

列出所有已注册的回调键

**参数：**

无参数。

**返回：** `List[str]`

---

### `清理回调()`

清理所有已注册的回调

**参数：**

无参数。

---

### `设置调试()`

设置FFI调试配置

**参数：**

无参数。

---

### `启用调试()`

启用FFI调试

**参数：**

无参数。

---

### `禁用调试()`

禁用FFI调试

**参数：**

无参数。

---

### `获取日志() -> List[str]`

获取FFI调试日志

**参数：**

无参数。

**返回：** `List[str]`

---

### `清空日志()`

清空FFI调试日志

**参数：**

无参数。

---

### `FFI调试()`

FFI调试（别名）

**参数：**

无参数。

---

### `FFI禁用调试()`

FFI禁用调试（别名）

**参数：**

无参数。

---

### `FFI获取日志() -> List[str]`

FFI获取日志（别名）

**参数：**

无参数。

**返回：** `List[str]`

---

### `_debug_log_call(函数名: str, 参数: tuple, 返回值: Any = None)`

内部：记录函数调用

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数名` | `str` |  |
| `参数` | `tuple` |  |
| `返回值` | `Any` | （默认：None） |

---

### `_debug_log_type(类型名: str, 详情: str)`

内部：记录类型信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `类型名` | `str` |  |
| `详情` | `str` |  |

---

### `_debug_log_memory(操作: str, 大小: int = 0)`

内部：记录内存操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `操作` | `str` |  |
| `大小` | `int` | （默认：0） |

---

### `位域设置(结构体实例, 字段名: str, 值: int)`

设置位域字段的值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体实例` | `None` |  |
| `字段名` | `str` |  |
| `值` | `int` |  |

---

### `位域获取(结构体实例, 字段名: str) -> int`

获取位域字段的值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `结构体实例` | `None` |  |
| `字段名` | `str` |  |

**返回：** `int`

---

### `创建函数指针(签名类型) -> type`

创建C函数指针类型

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `签名类型` | `None` |  |

**返回：** `type`

---

### `创建类型别名(别名: str, 基础类型: Any) -> Any`

创建C类型别名（返回基础类型）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `别名` | `str` |  |
| `基础类型` | `Any` |  |

**返回：** `Any`

---

### `定义宏(名称: str, 值: str)`

定义C预处理器宏

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |
| `值` | `str` |  |

---

### `获取宏(名称: str) -> Optional[str]`

获取宏的值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |

**返回：** `Optional[str]`

---

### `列出宏() -> Dict[str, str]`

列出所有已定义的宏

**参数：**

无参数。

**返回：** `Dict[str, str]`

---

### `清理宏()`

清理所有宏定义

**参数：**

无参数。

---

### `__init__(self, path: str)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `path` | `str` |  |

---

### `load(self) -> 'FFILibrary'`

加载动态库

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `'FFILibrary'`

---

### `get_function(self, name: str) -> Any`

获取库中的函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `name` | `str` |  |

**返回：** `Any`

---

### `declare_function(self, name: str, arg_types: List[Any], restype: Any) -> Any`

声明并设置函数签名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `name` | `str` |  |
| `arg_types` | `List[Any]` |  |
| `restype` | `Any` |  |

**返回：** `Any`

---

### `close(self)`

关闭动态库句柄

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `load_library(self, path: str, alias: str) -> FFILibrary`

加载动态库并注册别名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `path` | `str` |  |
| `alias` | `str` |  |

**返回：** `FFILibrary`

---

### `get_library(self, alias: str) -> Optional[FFILibrary]`

获取已加载的库

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `alias` | `str` |  |

**返回：** `Optional[FFILibrary]`

---

### `close_all(self)`

关闭所有已加载的库

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `FFI_TYPE_MAP` | `{'整数': ctypes.c_int, '小数': ctypes.c_double, '浮数': ctypes.c_double, '文本': ctypes.c_char_p, '串': ctypes.c_char_p, '布尔': ctypes.c_bool, '空': ctypes.c_void_p, '数': ctypes.c_double, '无': None, 'int': ctypes.c_int, 'double': ctypes.c_double, 'float': ctypes.c_float, 'char': ctypes.c_char, 'void': None, 'long': ctypes.c_long, 'unsigned': ctypes.c_uint, 'size_t': ctypes.c_size_t}` |
| `lib` | `_ffi_manager.get_library(库别名)` |
| `arg_types` | `[获取类型(t) for t in 参数类型]` |
| `restype` | `获取类型(返回类型)` |
| `lib` | `_ffi_manager.get_library(库别名)` |
| `func` | `lib.get_function(函数名)` |
| `instance` | `结构体类()` |
| `instance` | `结构体类()` |
| `enum_type` | `type(枚举名, (), 值字典)` |
| `current_platform` | `platform.system().lower()` |
| `lib` | `_ffi_manager.get_library(库别名)` |
| `func` | `lib.get_function(函数名)` |
| `all_args` | `list(固定参数) + list(可变参数)` |
| `instance` | `结构体类()` |
| `key` | `f'{名称}_{_callback_counter}'` |
| `func` | `self.get_function(name)` |
| `lib` | `FFILibrary(path)` |
| `ctype` | `获取类型(类型)` |
| `ctype` | `类型` |
| `plat_key` | `'win'` |
| `args_str` | `', '.join((repr(a) for a in 参数))` |
| `entry` | `f'[FFI调用] {函数名}({args_str})'` |
| `plat_key` | `'mac'` |
| `search_paths` | `[path, os.path.join('/usr/lib', path), os.path.join('/usr/local/lib', path)]` |
| `plat_key` | `'linux'` |
| `plat_key` | `current_platform` |
