# 日期序列 API

> 模块路径：`stdlib/日期序列.py`
> 导入方式：`从 日期序列 导入 函数名` 或 `导入 日期序列`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建时间戳(年, 月, 日, 时, 分, 秒, 微秒, 时区)` | 创建时间戳 |
| `创建时间戳从毫秒(毫秒)` | 从毫秒时间戳创建 |
| `创建时间戳从字符串(字符串, 格式)` | 从字符串解析创建时间戳 |
| `创建时间戳当前()` | 创建当前时间的时间戳 |
| `创建时间戳今天()` | 创建今天零点的时间戳 |
| `ts格式化(ts, 格式)` | 格式化时间戳 |
| `tsToISO8601(ts)` | 转为 ISO 8601 格式 |
| `tsToUnix(ts)` | 转为 Unix 时间戳（秒） |
| `tsToMs(ts)` | 转为 Unix 时间戳（毫秒） |
| `ts添加(ts, 持续时间)` | 时间戳加法 |
| `ts减去(ts, 持续时间)` | 时间戳减法 |
| `ts差值(ts1, ts2)` | 计算两个时间戳的差值，返回 Duration |
| `tsIsBefore(ts1, ts2)` | 判断是否早于 |
| `tsIsAfter(ts1, ts2)` | 判断是否晚于 |
| `tsIsEqual(ts1, ts2)` | 判断是否相等 |
| `ts获取星期(ts)` | 获取星期（1-7，1=星期一） |
| `ts获取星期名称(ts)` | 获取星期名称（中文） |
| `ts获取月份名称(ts)` | 获取月份名称（中文） |
| `ts获取月份天数(ts)` | 获取月份的天数 |
| `ts是否闰年(ts)` | 判断是否闰年 |
| `tsIsWorkday(ts)` | 判断是否为工作日 |
| `ts获取季度(ts)` | 获取季度（1-4） |
| `ts获取周数(ts)` | 获取周数（ISO 周数） |
| `ts复制(ts)` | 复制时间戳 |
| `ts设置时区(ts, 时区)` | 设置时区 |
| `ts转为UTC(ts)` | 转为 UTC 时间 |
| `ts添加天数(ts, 天数)` |  |
| `ts添加小时(ts, 小时)` |  |
| `ts添加分钟(ts, 分钟)` |  |
| `ts添加秒(ts, 秒)` |  |
| `ts添加月数(ts, 月数)` | 添加月数 |
| `ts添加年数(ts, 年数)` |  |
| `创建日期范围(开始, 结束)` | 创建日期范围 |
| `创建日期范围从数量(开始, 数量, 单位, 步长)` | 从开始时间和数量创建日期范围 |
| `创建工作日范围(开始, 结束)` | 创建工作日范围（仅包含工作日） |
| `dr获取时间戳列表(dr)` | 获取日期范围内所有时间戳列表 |
| `dr长度(dr)` | 获取日期范围天数 |
| `dr包含(dr, ts)` | 判断日期范围是否包含指定时间戳 |
| `创建持续时间(秒, 分钟, 小时, 天数)` | 创建持续时间 |
| `durToMs(dur)` | 持续时间转为毫秒 |
| `dur总秒数(dur)` | 获取总秒数 |
| `dur总分钟数(dur)` | 获取总分钟数 |
| `dur总小时数(dur)` | 获取总小时数 |
| `dur总天数(dur)` | 获取总天数 |
| `dur格式化(dur, 格式)` | 格式化持续时间 |
| `创建时区(偏移小时, 偏移分钟)` | 创建时区 |
| `tz获取当前偏移(tz)` | 获取当前时区偏移（小时） |
| `tzIsDST(tz)` | 判断是否为夏令时 |
| `创建日期偏移(年, 月, 日)` | 创建日期偏移 |
| `do应用(offset, ts)` | 应用日期偏移到时间戳 |
| `创建重采样器(数据, 频率)` | 创建重采样器 |
| `rs求和(rs)` | 重采样求和 |
| `rs平均值(rs)` | 重采样平均值 |
| `rs最小值(rs)` | 重采样最小值 |
| `rs最大值(rs)` | 重采样最大值 |
| `rs首值(rs)` | 重采样首值 |
| `rs末值(rs)` | 重采样末值 |
| `rs计数(rs)` | 重采样计数 |
| `rs标准差(rs)` | 重采样标准差 |
| `rs前向填充(rs)` | 重采样前向填充 |
| `rs后向填充(rs)` | 重采样后向填充 |
| `rs线性插值(rs)` | 重采样线性插值 |
| `rs应用(rs, 函数)` | 重采样应用自定义函数 |
| `_resample_groups(rs)` | 将数据按频率分组 |
| `_resample_apply(rs, func)` | 对重采样组应用函数 |
| `_std_dev(values)` | 计算标准差 |
| `__init__(self, dt)` |  |
| `to_datetime(self)` |  |
| `__init__(self, start, end)` |  |
| `start(self)` |  |
| `end(self)` |  |
| `__init__(self, seconds)` |  |
| `_to_timedelta(self)` |  |
| `__init__(self, tz)` |  |
| `__init__(self, years, months, days)` |  |
| `__init__(self, 数据, 频率)` |  |

---

## 函数详情

### `创建时间戳(年, 月, 日, 时 = 0, 分 = 0, 秒 = 0, 微秒 = 0, 时区 = None)`

创建时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `年` | `None` |  |
| `月` | `None` |  |
| `日` | `None` |  |
| `时` | `None` | （默认：0） |
| `分` | `None` | （默认：0） |
| `秒` | `None` | （默认：0） |
| `微秒` | `None` | （默认：0） |
| `时区` | `None` | （默认：None） |

---

### `创建时间戳从毫秒(毫秒)`

从毫秒时间戳创建

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `毫秒` | `None` |  |

---

### `创建时间戳从字符串(字符串, 格式 = '%Y-%m-%d %H:%M:%S')`

从字符串解析创建时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符串` | `None` |  |
| `格式` | `None` | （默认：'%Y-%m-%d %H:%M:%S'） |

---

### `创建时间戳当前()`

创建当前时间的时间戳

**参数：**

无参数。

---

### `创建时间戳今天()`

创建今天零点的时间戳

**参数：**

无参数。

---

### `ts格式化(ts, 格式 = '%Y-%m-%d %H:%M:%S')`

格式化时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `格式` | `None` | （默认：'%Y-%m-%d %H:%M:%S'） |

---

### `tsToISO8601(ts)`

转为 ISO 8601 格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `tsToUnix(ts)`

转为 Unix 时间戳（秒）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `tsToMs(ts)`

转为 Unix 时间戳（毫秒）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts添加(ts, 持续时间)`

时间戳加法

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `持续时间` | `None` |  |

---

### `ts减去(ts, 持续时间)`

时间戳减法

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `持续时间` | `None` |  |

---

### `ts差值(ts1, ts2)`

计算两个时间戳的差值，返回 Duration

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts1` | `None` |  |
| `ts2` | `None` |  |

---

### `tsIsBefore(ts1, ts2)`

判断是否早于

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts1` | `None` |  |
| `ts2` | `None` |  |

---

### `tsIsAfter(ts1, ts2)`

判断是否晚于

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts1` | `None` |  |
| `ts2` | `None` |  |

---

### `tsIsEqual(ts1, ts2)`

判断是否相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts1` | `None` |  |
| `ts2` | `None` |  |

---

### `ts获取星期(ts)`

获取星期（1-7，1=星期一）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts获取星期名称(ts)`

获取星期名称（中文）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts获取月份名称(ts)`

获取月份名称（中文）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts获取月份天数(ts)`

获取月份的天数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts是否闰年(ts)`

判断是否闰年

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `tsIsWorkday(ts)`

判断是否为工作日

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts获取季度(ts)`

获取季度（1-4）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts获取周数(ts)`

获取周数（ISO 周数）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts复制(ts)`

复制时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts设置时区(ts, 时区)`

设置时区

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `时区` | `None` |  |

---

### `ts转为UTC(ts)`

转为 UTC 时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |

---

### `ts添加天数(ts, 天数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `天数` | `None` |  |

---

### `ts添加小时(ts, 小时)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `小时` | `None` |  |

---

### `ts添加分钟(ts, 分钟)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `分钟` | `None` |  |

---

### `ts添加秒(ts, 秒)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `秒` | `None` |  |

---

### `ts添加月数(ts, 月数)`

添加月数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `月数` | `None` |  |

---

### `ts添加年数(ts, 年数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ts` | `None` |  |
| `年数` | `None` |  |

---

### `创建日期范围(开始, 结束)`

创建日期范围

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `开始` | `None` |  |
| `结束` | `None` |  |

---

### `创建日期范围从数量(开始, 数量, 单位 = 'days', 步长 = 1)`

从开始时间和数量创建日期范围

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `开始` | `None` |  |
| `数量` | `None` |  |
| `单位` | `None` | （默认：'days'） |
| `步长` | `None` | （默认：1） |

---

### `创建工作日范围(开始, 结束)`

创建工作日范围（仅包含工作日）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `开始` | `None` |  |
| `结束` | `None` |  |

---

### `dr获取时间戳列表(dr)`

获取日期范围内所有时间戳列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dr` | `None` |  |

---

### `dr长度(dr)`

获取日期范围天数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dr` | `None` |  |

---

### `dr包含(dr, ts)`

判断日期范围是否包含指定时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dr` | `None` |  |
| `ts` | `None` |  |

---

### `创建持续时间(秒 = 0, 分钟 = 0, 小时 = 0, 天数 = 0)`

创建持续时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒` | `None` | （默认：0） |
| `分钟` | `None` | （默认：0） |
| `小时` | `None` | （默认：0） |
| `天数` | `None` | （默认：0） |

---

### `durToMs(dur)`

持续时间转为毫秒

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dur` | `None` |  |

---

### `dur总秒数(dur)`

获取总秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dur` | `None` |  |

---

### `dur总分钟数(dur)`

获取总分钟数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dur` | `None` |  |

---

### `dur总小时数(dur)`

获取总小时数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dur` | `None` |  |

---

### `dur总天数(dur)`

获取总天数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dur` | `None` |  |

---

### `dur格式化(dur, 格式 = '%H:%M:%S')`

格式化持续时间

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dur` | `None` |  |
| `格式` | `None` | （默认：'%H:%M:%S'） |

---

### `创建时区(偏移小时, 偏移分钟 = 0)`

创建时区

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `偏移小时` | `None` |  |
| `偏移分钟` | `None` | （默认：0） |

---

### `tz获取当前偏移(tz)`

获取当前时区偏移（小时）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `tz` | `None` |  |

---

### `tzIsDST(tz)`

判断是否为夏令时

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `tz` | `None` |  |

---

### `创建日期偏移(年 = 0, 月 = 0, 日 = 0)`

创建日期偏移

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `年` | `None` | （默认：0） |
| `月` | `None` | （默认：0） |
| `日` | `None` | （默认：0） |

---

### `do应用(offset, ts)`

应用日期偏移到时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `offset` | `None` |  |
| `ts` | `None` |  |

---

### `创建重采样器(数据, 频率)`

创建重采样器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `频率` | `None` |  |

---

### `rs求和(rs)`

重采样求和

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs平均值(rs)`

重采样平均值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs最小值(rs)`

重采样最小值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs最大值(rs)`

重采样最大值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs首值(rs)`

重采样首值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs末值(rs)`

重采样末值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs计数(rs)`

重采样计数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs标准差(rs)`

重采样标准差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs前向填充(rs)`

重采样前向填充

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs后向填充(rs)`

重采样后向填充

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs线性插值(rs)`

重采样线性插值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `rs应用(rs, 函数)`

重采样应用自定义函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |
| `函数` | `None` |  |

---

### `_resample_groups(rs)`

将数据按频率分组

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |

---

### `_resample_apply(rs, func)`

对重采样组应用函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `rs` | `None` |  |
| `func` | `None` |  |

---

### `_std_dev(values)`

计算标准差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `values` | `None` |  |

---

### `__init__(self, dt = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `dt` | `None` | （默认：None） |

---

### `to_datetime(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, start, end)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `start` | `None` |  |
| `end` | `None` |  |

---

### `start(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `end(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, seconds = 0)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `seconds` | `None` | （默认：0） |

---

### `_to_timedelta(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, tz)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `tz` | `None` |  |

---

### `__init__(self, years = 0, months = 0, days = 0)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `years` | `None` | （默认：0） |
| `months` | `None` | （默认：0） |
| `days` | `None` | （默认：0） |

---

### `__init__(self, 数据, 频率)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `数据` | `None` |  |
| `频率` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `now` | `_datetime.datetime.now()` |
| `delta` | `ts1._dt - ts2._dt` |
| `weekday` | `ts._dt.weekday()` |
| `名称` | `['星期一', '星期二', '星期三', '星期四', '星期五', '星期六', '星期日']` |
| `名称` | `['一月', '二月', '三月', '四月', '五月', '六月', '七月', '八月', '九月', '十月', '十一月', '十二月']` |
| `new_ts` | `ts复制(ts)` |
| `new_ts` | `ts复制(ts)` |
| `total_months` | `ts._dt.year * 12 + (ts._dt.month - 1) + 月数` |
| `year` | `total_months // 12` |
| `month` | `total_months % 12 + 1` |
| `day` | `min(ts._dt.day, _calendar.monthrange(year, month)[1])` |
| `end_dt` | `开始._dt + delta` |
| `timestamps` | `[]` |
| `current` | `开始._dt` |
| `end_dt` | `结束._dt` |
| `timestamps` | `[]` |
| `current` | `dr._start._dt` |
| `end_dt` | `dr._end._dt` |
| `delta` | `dr._end._dt - dr._start._dt` |
| `total` | `秒 + 分钟 * 60 + 小时 * 3600 + 天数 * 86400` |
| `total_sec` | `int(dur._total_seconds)` |
| `hours` | `total_sec // 3600` |
| `minutes` | `total_sec % 3600 // 60` |
| `seconds` | `total_sec % 60` |
| `offset` | `_datetime.timedelta(hours=偏移小时, minutes=偏移分钟)` |
| `offset` | `tz._tz.utcoffset(None)` |
| `result` | `[]` |
| `last_val` | `None` |
| `groups` | `list(_resample_groups(rs))` |
| `result` | `[]` |
| `next_val` | `None` |
| `groups` | `list(_resample_groups(rs))` |
| `result` | `[]` |
| `valid_indices` | `[i for (i, g) in enumerate(groups) if g]` |
| `data` | `rs._数据` |
| `freq` | `rs._频率` |
| `n` | `max(1, int(freq))` |
| `groups` | `[data[i:i + n] for i in range(0, len(data), n)]` |
| `mean` | `sum(values) / len(values)` |
| `variance` | `sum(((x - mean) ** 2 for x in values)) / len(values)` |
| `dt` | `_datetime.datetime(年, 月, 日, 时, 分, 秒, 微秒)` |
| `dt` | `_datetime.datetime.fromtimestamp(毫秒 / 1000.0)` |
| `dt` | `_datetime.datetime.strptime(字符串, 格式)` |
| `delta` | `_datetime.timedelta(seconds=持续时间._total_seconds)` |
| `delta` | `_datetime.timedelta(seconds=持续时间._total_seconds)` |
| `delta` | `_datetime.timedelta(days=步长 * (数量 - 1))` |
| `dt` | `dt.replace(tzinfo=时区)` |
| `delta` | `_datetime.timedelta(seconds=持续时间)` |
| `delta` | `_datetime.timedelta(seconds=持续时间)` |
| `delta` | `_datetime.timedelta(hours=步长 * (数量 - 1))` |
| `last_val` | `group[-1]` |
| `next_val` | `groups[i][0]` |
| `before` | `[v for v in valid_indices if v < i]` |
| `after` | `[v for v in valid_indices if v > i]` |
| `delta` | `_datetime.timedelta(weeks=步长 * (数量 - 1))` |
| `delta` | `_datetime.timedelta(days=步长 * (数量 - 1))` |
| `b_idx` | `before[-1]` |
| `a_idx` | `after[0]` |
| `b_val` | `groups[b_idx][-1]` |
| `a_val` | `groups[a_idx][0]` |
| `ratio` | `(i - b_idx) / (a_idx - b_idx)` |
