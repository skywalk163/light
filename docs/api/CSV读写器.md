# CSV 读写 API

> 模块路径：`stdlib/CSV读写器.py`
> 导入方式：`从 CSV读写器 导入 函数名` 或 `导入 CSV读写器`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `读取CSV(文件路径, 编码, 分隔符, 表头)` | 读取CSV文件 |
| `读取CSV列表(文件路径, 编码, 分隔符, 表头)` | 读取CSV文件为列表列表 |
| `写入CSV(文件路径, 数据, 编码, 分隔符)` | 写入CSV文件（字典列表） |
| `写入CSV列表(文件路径, 数据, 编码, 分隔符)` | 写入CSV文件（列表列表） |
| `CSV转字典(文本, 分隔符)` | CSV文本转字典列表 |
| `字典转CSV(数据, 分隔符)` | 字典列表转CSV文本 |
| `CSV转列表(文本, 分隔符)` | CSV文本转列表列表 |
| `列表转CSV(数据, 分隔符)` | 列表列表转CSV文本 |
| `_解析CSV行(行, 分隔符)` | 解析CSV单行 |
| `获取CSV表头(文件路径, 编码, 分隔符)` | 获取CSV表头 |
| `获取CSV行数(文件路径, 编码)` | 获取CSV行数 |
| `获取CSV列数(文件路径, 编码, 分隔符)` | 获取CSV列数 |
| `CSV添加行(文件路径, 行, 编码, 分隔符)` | 向CSV文件添加行 |
| `CSV添加字典行(文件路径, 字典, 编码, 分隔符)` | 向CSV文件添加字典行 |
| `CSV筛选(文件路径, 条件函数, 编码, 分隔符)` | 筛选CSV数据 |
| `CSV排序(文件路径, 键, 升序, 编码, 分隔符)` | 排序CSV数据 |
| `CSV分组(文件路径, 键, 编码, 分隔符)` | 分组CSV数据 |
| `CSV聚合(文件路径, 分组键, 聚合键, 聚合函数, 编码, 分隔符)` | 聚合CSV数据 |
| `CSV转JSON(文件路径, 输出文件, 编码, 分隔符)` | CSV转JSON |
| `JSON转CSV(文件路径, 输出文件, 编码, 分隔符)` | JSON转CSV |
| `CSV合并()` | 合并多个CSV文件 |
| `CSV分割(文件路径, 每文件行数, 输出前缀, 编码, 分隔符)` | 分割CSV文件 |
| `CSV验证(文件路径, 编码, 分隔符)` | 验证CSV文件格式 |
| `CSV统计(文件路径, 编码, 分隔符)` | 统计CSV文件信息 |
| `CSV删除列(文件路径, 列名, 输出文件, 编码, 分隔符)` | 删除CSV列 |
| `CSV重命名列(文件路径, 旧列名, 新列名, 输出文件, 编码, 分隔符)` | 重命名CSV列 |
| `CSV去重(文件路径, 键, 输出文件, 编码, 分隔符)` | CSV去重 |
| `CSV填充缺失值(文件路径, 填充值, 输出文件, 编码, 分隔符)` | 填充CSV缺失值 |
| `CSV转HTML表格(文件路径, 编码, 分隔符)` | CSV转HTML表格 |
| `CSV转Markdown表格(文件路径, 编码, 分隔符)` | CSV转Markdown表格 |
| `读取TSV(文件路径, 编码)` | 读取TSV文件（制表符分隔） |
| `写入TSV(文件路径, 数据, 表头, 编码)` | 写入TSV文件（制表符分隔） |

---

## 函数详情

### `读取CSV(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',', 表头: bool = True) -> List[Dict[str, Any]]`

读取CSV文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |
| `表头` | `bool` | （默认：True） |

**返回：** `List[Dict[str, Any]]`

---

### `读取CSV列表(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',', 表头: bool = True) -> List[List[str]]`

读取CSV文件为列表列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |
| `表头` | `bool` | （默认：True） |

**返回：** `List[List[str]]`

---

### `写入CSV(文件路径: str, 数据: List[Dict[str, Any]], 编码: str = 'utf-8', 分隔符: str = ',')`

写入CSV文件（字典列表）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `数据` | `List[Dict[str, Any]]` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

---

### `写入CSV列表(文件路径: str, 数据: List[List[str]], 编码: str = 'utf-8', 分隔符: str = ',')`

写入CSV文件（列表列表）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `数据` | `List[List[str]]` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

---

### `CSV转字典(文本: str, 分隔符: str = ',') -> List[Dict[str, Any]]`

CSV文本转字典列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `分隔符` | `str` | （默认：','） |

**返回：** `List[Dict[str, Any]]`

---

### `字典转CSV(数据: List[Dict[str, Any]], 分隔符: str = ',') -> str`

字典列表转CSV文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Dict[str, Any]]` |  |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `CSV转列表(文本: str, 分隔符: str = ',') -> List[List[str]]`

CSV文本转列表列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `分隔符` | `str` | （默认：','） |

**返回：** `List[List[str]]`

---

### `列表转CSV(数据: List[List[str]], 分隔符: str = ',') -> str`

列表列表转CSV文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[List[str]]` |  |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `_解析CSV行(行: str, 分隔符: str = ',') -> List[str]`

解析CSV单行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `行` | `str` |  |
| `分隔符` | `str` | （默认：','） |

**返回：** `List[str]`

---

### `获取CSV表头(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',') -> List[str]`

获取CSV表头

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `List[str]`

---

### `获取CSV行数(文件路径: str, 编码: str = 'utf-8') -> int`

获取CSV行数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `int`

---

### `获取CSV列数(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',') -> int`

获取CSV列数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `int`

---

### `CSV添加行(文件路径: str, 行: List[Any], 编码: str = 'utf-8', 分隔符: str = ',')`

向CSV文件添加行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `行` | `List[Any]` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

---

### `CSV添加字典行(文件路径: str, 字典: Dict[str, Any], 编码: str = 'utf-8', 分隔符: str = ',')`

向CSV文件添加字典行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `字典` | `Dict[str, Any]` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

---

### `CSV筛选(文件路径: str, 条件函数: callable, 编码: str = 'utf-8', 分隔符: str = ',') -> List[Dict[str, Any]]`

筛选CSV数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `条件函数` | `callable` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `List[Dict[str, Any]]`

---

### `CSV排序(文件路径: str, 键: str, 升序: bool = True, 编码: str = 'utf-8', 分隔符: str = ',') -> List[Dict[str, Any]]`

排序CSV数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `键` | `str` |  |
| `升序` | `bool` | （默认：True） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `List[Dict[str, Any]]`

---

### `CSV分组(文件路径: str, 键: str, 编码: str = 'utf-8', 分隔符: str = ',') -> Dict[str, List[Dict[str, Any]]]`

分组CSV数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `键` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `Dict[str, List[Dict[str, Any]]]`

---

### `CSV聚合(文件路径: str, 分组键: str, 聚合键: str, 聚合函数: callable, 编码: str = 'utf-8', 分隔符: str = ',') -> Dict[str, Any]`

聚合CSV数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `分组键` | `str` |  |
| `聚合键` | `str` |  |
| `聚合函数` | `callable` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `Dict[str, Any]`

---

### `CSV转JSON(文件路径: str, 输出文件: str = None, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

CSV转JSON

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `输出文件` | `str` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `JSON转CSV(文件路径: str, 输出文件: str = None, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

JSON转CSV

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `输出文件` | `str` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `CSV合并() -> str`

合并多个CSV文件

**参数：**

无参数。

**返回：** `str`

---

### `CSV分割(文件路径: str, 每文件行数: int = 1000, 输出前缀: str = 'split_', 编码: str = 'utf-8', 分隔符: str = ',') -> List[str]`

分割CSV文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `每文件行数` | `int` | （默认：1000） |
| `输出前缀` | `str` | （默认：'split_'） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `List[str]`

---

### `CSV验证(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',') -> bool`

验证CSV文件格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `bool`

---

### `CSV统计(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',') -> Dict[str, Any]`

统计CSV文件信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `Dict[str, Any]`

---

### `CSV删除列(文件路径: str, 列名: str, 输出文件: str = None, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

删除CSV列

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `列名` | `str` |  |
| `输出文件` | `str` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `CSV重命名列(文件路径: str, 旧列名: str, 新列名: str, 输出文件: str = None, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

重命名CSV列

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `旧列名` | `str` |  |
| `新列名` | `str` |  |
| `输出文件` | `str` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `CSV去重(文件路径: str, 键: str = None, 输出文件: str = None, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

CSV去重

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `键` | `str` | （默认：None） |
| `输出文件` | `str` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `CSV填充缺失值(文件路径: str, 填充值: Any = '', 输出文件: str = None, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

填充CSV缺失值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `填充值` | `Any` | （默认：''） |
| `输出文件` | `str` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `CSV转HTML表格(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

CSV转HTML表格

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `CSV转Markdown表格(文件路径: str, 编码: str = 'utf-8', 分隔符: str = ',') -> str`

CSV转Markdown表格

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |
| `分隔符` | `str` | （默认：','） |

**返回：** `str`

---

### `读取TSV(文件路径: str, 编码: str = 'utf-8') -> List[Dict[str, str]]`

读取TSV文件（制表符分隔）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `List[Dict[str, str]]`

---

### `写入TSV(文件路径: str, 数据: List[Dict[str, Any]], 表头: List[str] = None, 编码: str = 'utf-8') -> None`

写入TSV文件（制表符分隔）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `str` |  |
| `数据` | `List[Dict[str, Any]]` |  |
| `表头` | `List[str]` | （默认：None） |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `None`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `表头` | `list(数据[0].keys())` |
| `行` | `文本.strip().split('\n')` |
| `表头` | `行[0].split(分隔符)` |
| `结果` | `[]` |
| `表头` | `list(数据[0].keys())` |
| `行` | `[分隔符.join(表头)]` |
| `行` | `文本.strip().split('\n')` |
| `行` | `[]` |
| `结果` | `[]` |
| `当前` | `''` |
| `在引号内` | `False` |
| `i` | `0` |
| `表头` | `获取CSV表头(文件路径, 编码, 分隔符)` |
| `表头` | `获取CSV表头(文件路径, 编码, 分隔符)` |
| `行` | `[]` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `结果` | `{}` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `分组` | `{}` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `JSON内容` | `json.dumps(数据, indent=2, ensure_ascii=False)` |
| `CSV内容` | `字典转CSV(数据, 分隔符)` |
| `合并数据` | `[]` |
| `表头` | `None` |
| `CSV内容` | `字典转CSV(合并数据, 分隔符)` |
| `数据` | `读取CSV列表(文件路径, 编码, 分隔符)` |
| `表头` | `数据[0] if 数据 else []` |
| `数据行` | `数据[1:] if len(数据) > 1 else []` |
| `输出文件列表` | `[]` |
| `文件索引` | `0` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `统计` | `{'行数': len(数据), '列数': len(数据[0].keys()) if 数据 else 0, '表头': list(数据[0].keys()) if 数据 else []}` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `CSV内容` | `字典转CSV(数据, 分隔符)` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `CSV内容` | `字典转CSV(数据, 分隔符)` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `CSV内容` | `字典转CSV(数据, 分隔符)` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `CSV内容` | `字典转CSV(数据, 分隔符)` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `表头` | `list(数据[0].keys())` |
| `表头行` | `''.join((f'<th>{列}</th>' for 列 in 表头))` |
| `行内容` | `[]` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `表头` | `list(数据[0].keys())` |
| `对齐线` | `'|' + '|'.join(['---'] * len(表头)) + '|'` |
| `行` | `[f"|{'|'.join(表头)}|", 对齐线]` |
| `阅读器` | `csv.DictReader(f, delimiter=分隔符)` |
| `阅读器` | `csv.reader(f, delimiter=分隔符)` |
| `写入器` | `csv.DictWriter(f, fieldnames=表头, delimiter=分隔符)` |
| `写入器` | `csv.writer(f, delimiter=分隔符)` |
| `单元格` | `_解析CSV行(行内容, 分隔符)` |
| `单元格` | `[]` |
| `单元格` | `[]` |
| `字符` | `行[i]` |
| `第一行` | `f.readline()` |
| `写入器` | `csv.writer(f, delimiter=分隔符)` |
| `分组键` | `行.get(键, '')` |
| `键` | `行.get(分组键, '')` |
| `值` | `行.get(聚合键, '')` |
| `数据` | `json.load(f)` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `分割数据` | `[表头] + 数据行[i:i + 每文件行数]` |
| `输出文件` | `f'{输出前缀}{文件索引}.csv'` |
| `数据` | `读取CSV(文件路径, 编码, 分隔符)` |
| `数值列` | `[]` |
| `非空计数` | `0` |
| `已见` | `set()` |
| `结果` | `[]` |
| `数据` | `结果` |
| `已见` | `set()` |
| `结果` | `[]` |
| `数据` | `结果` |
| `单元格` | `''.join((f"<td>{行.get(列, '')}</td>" for 列 in 表头))` |
| `单元格` | `[]` |
| `值` | `项.get(列, '')` |
| `值` | `str(值)` |
| `在引号内` | `not 在引号内` |
| `值` | `float(值)` |
| `表头` | `list(数据[0].keys()) if 数据 else []` |
| `值` | `行.get(列, '')` |
| `值` | `行.get(键, '')` |
| `键值` | `tuple(sorted(行.items()))` |
| `值` | `str(行内容.get(列, ''))` |
| `值` | `值.replace('|', '\\|')` |
| `值` | `值.replace('"', '""')` |
| `值` | `值.replace('"', '""')` |
| `当前` | `''` |
| `值` | `0` |
