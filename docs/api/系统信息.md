# 系统信息 API

> 模块路径：`stdlib/系统信息.py`
> 导入方式：`从 系统信息 导入 函数名` 或 `导入 系统信息`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `获取CPU信息()` | 获取CPU信息字典 |
| `获取CPU核心数()` | 获取CPU核心数 |
| `获取CPU使用率()` | 获取CPU使用率百分比（简化实现，依赖psutil） |
| `获取CPU每核使用率()` | 获取每核CPU使用率列表（简化实现，依赖psutil） |
| `获取内存信息()` | 获取内存信息字典（依赖psutil） |
| `获取内存总量()` | 获取内存总量（字节） |
| `获取内存可用()` | 获取可用内存（字节） |
| `获取内存使用率()` | 获取内存使用率百分比 |
| `获取磁盘信息(路径)` | 获取指定路径的磁盘信息 |
| `获取磁盘全部()` | 获取所有磁盘分区信息汇总 |
| `获取所有磁盘信息()` | 获取所有磁盘分区的信息列表 |
| `获取网口全部()` | 获取所有网口名称列表 |
| `获取网络接口信息(接口名)` | 获取指定网络接口的详细信息 |
| `获取所有网络接口信息()` | 获取所有网络接口的信息字典 |
| `获取系统概览()` | 获取系统概要信息 |
| `获取主机名()` | 获取系统主机名 |
| `获取操作系统名称()` | 获取操作系统名称 |
| `获取操作系统版本()` | 获取操作系统版本字符串 |
| `获取系统架构()` | 获取系统架构类型 |
| `获取当前用户()` | 获取当前用户名 |
| `获取启动时间()` | 获取系统启动时间戳（依赖psutil） |
| `获取运行时间()` | 获取系统已运行时间（秒） |
| `格式化字节(字节数)` | 将字节数格式化为人类可读的字符串 |
| `格式化运行时间(秒数)` | 将运行时间秒数格式化为人类可读的字符串 |

---

## 函数详情

### `获取CPU信息()`

获取CPU信息字典

**参数：**

无参数。

---

### `获取CPU核心数()`

获取CPU核心数

**参数：**

无参数。

---

### `获取CPU使用率()`

获取CPU使用率百分比（简化实现，依赖psutil）

**参数：**

无参数。

---

### `获取CPU每核使用率()`

获取每核CPU使用率列表（简化实现，依赖psutil）

**参数：**

无参数。

---

### `获取内存信息()`

获取内存信息字典（依赖psutil）

**参数：**

无参数。

---

### `获取内存总量()`

获取内存总量（字节）

**参数：**

无参数。

---

### `获取内存可用()`

获取可用内存（字节）

**参数：**

无参数。

---

### `获取内存使用率()`

获取内存使用率百分比

**参数：**

无参数。

---

### `获取磁盘信息(路径)`

获取指定路径的磁盘信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `获取磁盘全部()`

获取所有磁盘分区信息汇总

**参数：**

无参数。

---

### `获取所有磁盘信息()`

获取所有磁盘分区的信息列表

**参数：**

无参数。

---

### `获取网口全部()`

获取所有网口名称列表

**参数：**

无参数。

---

### `获取网络接口信息(接口名)`

获取指定网络接口的详细信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `接口名` | `None` |  |

---

### `获取所有网络接口信息()`

获取所有网络接口的信息字典

**参数：**

无参数。

---

### `获取系统概览()`

获取系统概要信息

**参数：**

无参数。

---

### `获取主机名()`

获取系统主机名

**参数：**

无参数。

---

### `获取操作系统名称()`

获取操作系统名称

**参数：**

无参数。

---

### `获取操作系统版本()`

获取操作系统版本字符串

**参数：**

无参数。

---

### `获取系统架构()`

获取系统架构类型

**参数：**

无参数。

---

### `获取当前用户()`

获取当前用户名

**参数：**

无参数。

---

### `获取启动时间()`

获取系统启动时间戳（依赖psutil）

**参数：**

无参数。

---

### `获取运行时间()`

获取系统已运行时间（秒）

**参数：**

无参数。

---

### `格式化字节(字节数)`

将字节数格式化为人类可读的字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字节数` | `None` |  |

---

### `格式化运行时间(秒数)`

将运行时间秒数格式化为人类可读的字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒数` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `units` | `['B', 'KB', 'MB', 'GB', 'TB', 'PB']` |
| `unit_index` | `0` |
| `size` | `float(字节数)` |
| `days` | `int(秒数 // 86400)` |
| `hours` | `int(秒数 % 86400 // 3600)` |
| `minutes` | `int(秒数 % 3600 // 60)` |
| `seconds` | `int(秒数 % 60)` |
| `parts` | `[]` |
| `mem` | `_psutil.virtual_memory()` |
| `usage` | `_psutil.disk_usage(路径)` |
| `total` | `0` |
| `used` | `0` |
| `free` | `0` |
| `result` | `[]` |
| `stats` | `_psutil.net_io_counters(pernic=True)` |
| `addrs` | `_psutil.net_if_addrs().get(接口名, [])` |
| `stats` | `_psutil.net_if_stats().get(接口名)` |
| `io` | `_psutil.net_io_counters(pernic=True).get(接口名)` |
| `addr_list` | `[]` |
| `result` | `{}` |
| `boot_time` | `_psutil.boot_time()` |
| `boot` | `获取启动时间()` |
| `秒数` | `0` |
| `usage` | `_psutil.disk_usage(part.mountpoint)` |
| `usage` | `_psutil.disk_usage(part.mountpoint)` |
