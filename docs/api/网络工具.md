# 网络工具 API

> 模块路径：`stdlib/网络工具.py`
> 导入方式：`从 网络工具 导入 函数名` 或 `导入 网络工具`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `解析IPv4(地址字符串)` | 解析IPv4地址为整数 |
| `地址转字符串(地址)` | 将IP地址对象转为字符串 |
| `地址转整数(地址)` | 将IP地址字符串转为整数 |
| `整数转地址(整数)` | 将整数转为IP地址字符串 |
| `验证IPv4(地址)` | 验证是否为有效的IPv4地址 |
| `验证IPv6(地址)` | 验证是否为有效的IPv6地址 |
| `获取本地IP列表()` | 获取本地所有IP地址列表 |
| `是私有IP(地址)` | 判断是否为私有IP地址 |
| `是回环IP(地址)` | 判断是否为回环IP地址 |
| `是广播IP(地址)` | 判断是否为广播IP地址 |
| `是组播IP(地址)` | 判断是否为组播IP地址 |
| `创建子网(地址, 前缀长度)` | 创建子网，返回子网对象 |
| `解析CIDR(CIDR字符串)` | 解析CIDR表示法，返回(网络地址, 前缀长度) |
| `子网包含IP(子网, IP地址)` | 判断子网是否包含指定IP |
| `子网分割(子网, 子网前缀长度)` | 将子网分割为更小的子网，返回子网列表 |
| `获取子网掩码(子网)` | 获取子网掩码字符串 |
| `前缀长度到掩码(前缀长度)` | 将前缀长度转换为子网掩码字符串 |
| `掩码到前缀长度(掩码)` | 将子网掩码转换为前缀长度 |
| `扫描端口(主机, 端口, 超时秒)` | 扫描单个端口是否开放，返回True/False |
| `扫描端口范围(主机, 起始端口, 结束端口, 超时秒)` | 扫描端口范围，返回开放端口列表 |
| `扫描常用端口(主机, 超时秒)` | 扫描常用服务端口，返回{端口: 服务名}字典 |
| `获取常用服务名称(端口)` | 获取常用端口对应的服务名称 |
| `Ping主机(主机, 超时秒)` | Ping主机，返回是否可达（布尔） |
| `Ping主机多次(主机, 次数, 超时秒)` | Ping主机多次，返回(成功次数, 平均延迟) |
| `获取主机名(主机)` | 获取主机名或IP对应的主机名 |
| `获取主机名地址(主机名)` | 获取主机名对应的IP地址 |
| `检查网络连通性(主机, 超时秒)` | 检查网络连通性，返回True/False |
| `Traceroute追踪(主机, 最大跳数, 超时秒)` | Traceroute追踪路由，返回跳数列表 |

---

## 函数详情

### `解析IPv4(地址字符串)`

解析IPv4地址为整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址字符串` | `None` |  |

---

### `地址转字符串(地址)`

将IP地址对象转为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `地址转整数(地址)`

将IP地址字符串转为整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `整数转地址(整数)`

将整数转为IP地址字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `整数` | `None` |  |

---

### `验证IPv4(地址)`

验证是否为有效的IPv4地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `验证IPv6(地址)`

验证是否为有效的IPv6地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `获取本地IP列表()`

获取本地所有IP地址列表

**参数：**

无参数。

---

### `是私有IP(地址)`

判断是否为私有IP地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `是回环IP(地址)`

判断是否为回环IP地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `是广播IP(地址)`

判断是否为广播IP地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `是组播IP(地址)`

判断是否为组播IP地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |

---

### `创建子网(地址, 前缀长度)`

创建子网，返回子网对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `地址` | `None` |  |
| `前缀长度` | `None` |  |

---

### `解析CIDR(CIDR字符串)`

解析CIDR表示法，返回(网络地址, 前缀长度)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `CIDR字符串` | `None` |  |

---

### `子网包含IP(子网, IP地址)`

判断子网是否包含指定IP

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `子网` | `None` |  |
| `IP地址` | `None` |  |

---

### `子网分割(子网, 子网前缀长度)`

将子网分割为更小的子网，返回子网列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `子网` | `None` |  |
| `子网前缀长度` | `None` |  |

---

### `获取子网掩码(子网)`

获取子网掩码字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `子网` | `None` |  |

---

### `前缀长度到掩码(前缀长度)`

将前缀长度转换为子网掩码字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `前缀长度` | `None` |  |

---

### `掩码到前缀长度(掩码)`

将子网掩码转换为前缀长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `掩码` | `None` |  |

---

### `扫描端口(主机, 端口, 超时秒 = 1)`

扫描单个端口是否开放，返回True/False

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` |  |
| `端口` | `None` |  |
| `超时秒` | `None` | （默认：1） |

---

### `扫描端口范围(主机, 起始端口, 结束端口, 超时秒 = 1)`

扫描端口范围，返回开放端口列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` |  |
| `起始端口` | `None` |  |
| `结束端口` | `None` |  |
| `超时秒` | `None` | （默认：1） |

---

### `扫描常用端口(主机, 超时秒 = 1)`

扫描常用服务端口，返回{端口: 服务名}字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` |  |
| `超时秒` | `None` | （默认：1） |

---

### `获取常用服务名称(端口)`

获取常用端口对应的服务名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `端口` | `None` |  |

---

### `Ping主机(主机, 超时秒 = 2)`

Ping主机，返回是否可达（布尔）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` |  |
| `超时秒` | `None` | （默认：2） |

---

### `Ping主机多次(主机, 次数 = 4, 超时秒 = 2)`

Ping主机多次，返回(成功次数, 平均延迟)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` |  |
| `次数` | `None` | （默认：4） |
| `超时秒` | `None` | （默认：2） |

---

### `获取主机名(主机 = None)`

获取主机名或IP对应的主机名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` | （默认：None） |

---

### `获取主机名地址(主机名)`

获取主机名对应的IP地址

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机名` | `None` |  |

---

### `检查网络连通性(主机 = '8.8.8.8', 超时秒 = 3)`

检查网络连通性，返回True/False

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` | （默认：'8.8.8.8'） |
| `超时秒` | `None` | （默认：3） |

---

### `Traceroute追踪(主机, 最大跳数 = 30, 超时秒 = 3)`

Traceroute追踪路由，返回跳数列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `主机` | `None` |  |
| `最大跳数` | `None` | （默认：30） |
| `超时秒` | `None` | （默认：3） |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `结果` | `[]` |
| `开放端口` | `[]` |
| `常用端口` | `{21: 'FTP', 22: 'SSH', 23: 'Telnet', 25: 'SMTP', 53: 'DNS', 80: 'HTTP', 110: 'POP3', 143: 'IMAP', 443: 'HTTPS', 445: 'SMB', 993: 'IMAPS', 995: 'POP3S', 1433: 'MSSQL', 1521: 'Oracle', 3306: 'MySQL', 3389: 'RDP', 5432: 'PostgreSQL', 6379: 'Redis', 8080: 'HTTP-Proxy', 8443: 'HTTPS-Alt', 27017: 'MongoDB'}` |
| `结果` | `{}` |
| `服务映射` | `{21: 'FTP', 22: 'SSH', 23: 'Telnet', 25: 'SMTP', 53: 'DNS', 80: 'HTTP', 110: 'POP3', 143: 'IMAP', 443: 'HTTPS', 445: 'SMB', 993: 'IMAPS', 995: 'POP3S', 1433: 'MSSQL', 1521: 'Oracle', 3306: 'MySQL', 3389: 'RDP', 5432: 'PostgreSQL', 6379: 'Redis', 8080: 'HTTP-Proxy', 8443: 'HTTPS-Alt', 27017: 'MongoDB'}` |
| `成功次数` | `0` |
| `总延迟` | `0.0` |
| `平均延迟` | `总延迟 / 成功次数 if 成功次数 > 0 else 0.0` |
| `结果` | `[]` |
| `hostname` | `_socket.gethostname()` |
| `ip` | `_ipaddress.ip_address(地址)` |
| `ip` | `_ipaddress.ip_address(地址)` |
| `ip` | `_ipaddress.ip_address(地址)` |
| `ip` | `_ipaddress.ip_address(地址)` |
| `网络` | `_ipaddress.ip_network(CIDR字符串, strict=False)` |
| `sock` | `_socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)` |
| `result` | `sock.connect_ex((主机, 端口))` |
| `param` | `'-n' if _platform.system().lower() == 'windows' else '-c'` |
| `result` | `_subprocess.run(['ping', param, '1', '-w', str(int(超时秒 * 1000)), 主机], capture_output=True, text=True, timeout=超时秒 + 2)` |
| `sock` | `_socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)` |
| `result` | `sock.connect_ex((主机, 53))` |
| `dest_ip` | `_socket.gethostbyname(主机)` |
| `addr` | `info[4][0]` |
| `sock` | `_socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)` |
| `result` | `sock.connect_ex((主机, port))` |
| `start` | `_time.time()` |
| `recv_sock` | `_socket.socket(_socket.AF_INET, _socket.SOCK_RAW, _socket.IPPROTO_ICMP)` |
| `send_sock` | `_socket.socket(_socket.AF_INET, _socket.SOCK_DGRAM, _socket.IPPROTO_UDP)` |
| `port` | `33434 + ttl` |
| `start` | `_time.time()` |
| `hop_addr` | `''` |
| `elapsed` | `(_time.time() - start) * 1000` |
| `hop_addr` | `addr[0] if addr else '*'` |
