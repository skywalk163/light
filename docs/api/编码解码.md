# 编码解码 API

> 模块路径：`stdlib/编码解码.py`
> 导入方式：`从 编码解码 导入 函数名` 或 `导入 编码解码`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `Base64编码(数据, 编码)` | Base64编码（字符串输入） |
| `Base64解码(数据, 编码)` | Base64解码（返回字符串） |
| `Base64编码二进制(数据)` | Base64编码（二进制输入） |
| `Base64解码二进制(数据)` | Base64解码（二进制输入输出） |
| `Base64URL编码(数据, 编码)` | URL安全的Base64编码 |
| `Base64URL解码(数据, 编码)` | URL安全的Base64解码 |
| `Base32编码(数据, 编码)` | Base32编码 |
| `Base32解码(数据, 编码)` | Base32解码 |
| `Base16编码(数据, 编码)` | Base16编码（十六进制） |
| `Base16解码(数据, 编码)` | Base16解码（十六进制） |
| `十六进制编码(数据, 编码)` | 十六进制编码（小写） |
| `十六进制解码(数据, 编码)` | 十六进制解码 |
| `十六进制编码大写(数据, 编码)` | 十六进制编码（大写） |
| `二进制转十六进制(数据)` | 二进制转十六进制 |
| `十六进制转二进制(数据)` | 十六进制转二进制 |
| `URL编码(文本, 编码)` | URL编码 |
| `URL解码(文本, 编码)` | URL解码 |
| `URL编码全字符(文本, 编码)` | URL编码（包括安全字符） |
| `URL查询串编码(参数, 编码)` | 编码URL查询字符串 |
| `URL查询串解码(查询串, 编码)` | 解码URL查询字符串 |
| `Unicode转ASCII(文本)` | Unicode转ASCII（转义） |
| `ASCII转Unicode(文本)` | ASCII转Unicode（还原转义） |
| `GBK转UTF8(数据)` | GBK转UTF-8 |
| `UTF8转GBK(数据)` | UTF-8转GBK |
| `GB2312转UTF8(数据)` | GB2312转UTF-8 |
| `UTF8转GB2312(数据)` | UTF-8转GB2312 |
| `GB18030转UTF8(数据)` | GB18030转UTF-8 |
| `UTF8转GB18030(数据)` | UTF-8转GB18030 |
| `BIG5转UTF8(数据)` | BIG5转UTF-8 |
| `UTF8转BIG5(数据)` | UTF-8转BIG5 |
| `ISO8859_1转UTF8(数据)` | ISO-8859-1转UTF-8 |
| `UTF8转ISO8859_1(数据)` | UTF-8转ISO-8859-1 |
| `字符集转换(数据, 源编码, 目标编码)` | 通用字符集转换 |
| `字符集转换为字符串(数据, 源编码, 目标编码)` | 字符集转换为字符串 |
| `检测编码(数据, 候选编码)` | 检测文本编码 |
| `HTML实体编码(文本)` | HTML实体编码 |
| `HTML实体解码(文本)` | HTML实体解码 |
| `Unicode转中文(文本)` | Unicode转义转中文 |
| `中文转Unicode(文本)` | 中文转Unicode转义 |
| `字节转字符串(数据, 编码)` | 字节转字符串 |
| `字符串转字节(数据, 编码)` | 字符串转字节 |
| `文本转十六进制(文本, 编码)` | 文本转十六进制 |
| `十六进制转文本(数据, 编码)` | 十六进制转文本 |
| `二进制转字符串(数据, 编码)` | 二进制转字符串 |
| `字符串转二进制(数据, 编码)` | 字符串转二进制 |

---

## 函数详情

### `Base64编码(数据: str, 编码: str = 'utf-8') -> str`

Base64编码（字符串输入）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `Base64解码(数据: str, 编码: str = 'utf-8') -> str`

Base64解码（返回字符串）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `Base64编码二进制(数据: bytes) -> bytes`

Base64编码（二进制输入）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `bytes`

---

### `Base64解码二进制(数据: bytes) -> bytes`

Base64解码（二进制输入输出）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `bytes`

---

### `Base64URL编码(数据: str, 编码: str = 'utf-8') -> str`

URL安全的Base64编码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `Base64URL解码(数据: str, 编码: str = 'utf-8') -> str`

URL安全的Base64解码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `Base32编码(数据: str, 编码: str = 'utf-8') -> str`

Base32编码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `Base32解码(数据: str, 编码: str = 'utf-8') -> str`

Base32解码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `Base16编码(数据: str, 编码: str = 'utf-8') -> str`

Base16编码（十六进制）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `Base16解码(数据: str, 编码: str = 'utf-8') -> str`

Base16解码（十六进制）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `十六进制编码(数据: str, 编码: str = 'utf-8') -> str`

十六进制编码（小写）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `十六进制解码(数据: str, 编码: str = 'utf-8') -> str`

十六进制解码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `十六进制编码大写(数据: str, 编码: str = 'utf-8') -> str`

十六进制编码（大写）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `二进制转十六进制(数据: bytes) -> str`

二进制转十六进制

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `str`

---

### `十六进制转二进制(数据: str) -> bytes`

十六进制转二进制

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |

**返回：** `bytes`

---

### `URL编码(文本: str, 编码: str = 'utf-8') -> str`

URL编码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `URL解码(文本: str, 编码: str = 'utf-8') -> str`

URL解码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `URL编码全字符(文本: str, 编码: str = 'utf-8') -> str`

URL编码（包括安全字符）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `URL查询串编码(参数: dict, 编码: str = 'utf-8') -> str`

编码URL查询字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `参数` | `dict` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `URL查询串解码(查询串: str, 编码: str = 'utf-8') -> dict`

解码URL查询字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `查询串` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `dict`

---

### `Unicode转ASCII(文本: str) -> str`

Unicode转ASCII（转义）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `ASCII转Unicode(文本: str) -> str`

ASCII转Unicode（还原转义）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `GBK转UTF8(数据: bytes) -> str`

GBK转UTF-8

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `str`

---

### `UTF8转GBK(数据: str) -> bytes`

UTF-8转GBK

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |

**返回：** `bytes`

---

### `GB2312转UTF8(数据: bytes) -> str`

GB2312转UTF-8

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `str`

---

### `UTF8转GB2312(数据: str) -> bytes`

UTF-8转GB2312

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |

**返回：** `bytes`

---

### `GB18030转UTF8(数据: bytes) -> str`

GB18030转UTF-8

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `str`

---

### `UTF8转GB18030(数据: str) -> bytes`

UTF-8转GB18030

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |

**返回：** `bytes`

---

### `BIG5转UTF8(数据: bytes) -> str`

BIG5转UTF-8

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `str`

---

### `UTF8转BIG5(数据: str) -> bytes`

UTF-8转BIG5

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |

**返回：** `bytes`

---

### `ISO8859_1转UTF8(数据: bytes) -> str`

ISO-8859-1转UTF-8

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |

**返回：** `str`

---

### `UTF8转ISO8859_1(数据: str) -> bytes`

UTF-8转ISO-8859-1

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |

**返回：** `bytes`

---

### `字符集转换(数据: bytes, 源编码: str, 目标编码: str) -> bytes`

通用字符集转换

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |
| `源编码` | `str` |  |
| `目标编码` | `str` |  |

**返回：** `bytes`

---

### `字符集转换为字符串(数据: bytes, 源编码: str, 目标编码: str) -> str`

字符集转换为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |
| `源编码` | `str` |  |
| `目标编码` | `str` |  |

**返回：** `str`

---

### `检测编码(数据: bytes, 候选编码: list = None) -> str`

检测文本编码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |
| `候选编码` | `list` | （默认：None） |

**返回：** `str`

---

### `HTML实体编码(文本: str) -> str`

HTML实体编码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `HTML实体解码(文本: str) -> str`

HTML实体解码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `Unicode转中文(文本: str) -> str`

Unicode转义转中文

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `中文转Unicode(文本: str) -> str`

中文转Unicode转义

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `字节转字符串(数据: bytes, 编码: str = 'utf-8') -> str`

字节转字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `字符串转字节(数据: str, 编码: str = 'utf-8') -> bytes`

字符串转字节

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `bytes`

---

### `文本转十六进制(文本: str, 编码: str = 'utf-8') -> str`

文本转十六进制

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `十六进制转文本(数据: str, 编码: str = 'utf-8') -> str`

十六进制转文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `二进制转字符串(数据: bytes, 编码: str = 'utf-8') -> str`

二进制转字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `bytes` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `字符串转二进制(数据: str, 编码: str = 'utf-8') -> bytes`

字符串转二进制

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `str` |  |
| `编码` | `str` | （默认：'utf-8'） |

**返回：** `bytes`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `结果` | `urllib.parse.parse_qs(查询串, encoding=编码)` |
| `映射` | `{'&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;'}` |
| `映射` | `{'&amp;': '&', '&lt;': '<', '&gt;': '>', '&quot;': '"', '&#39;': "'", '&nbsp;': ' '}` |
| `候选编码` | `['utf-8', 'gbk', 'gb2312', 'gb18030', 'big5', 'iso-8859-1']` |
| `文本` | `文本.replace(原始, 替换)` |
| `文本` | `文本.replace(原始, 替换)` |
