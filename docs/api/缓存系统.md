# 缓存系统 API

> 模块路径：`stdlib/缓存系统.py`
> 导入方式：`从 缓存系统 导入 函数名` 或 `导入 缓存系统`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `新建缓存配置(max_size, ttl, policy)` | 创建缓存配置 |
| `新建内存缓存(config)` | 创建内存缓存实例 |
| `缓存获取(cache, key)` | 从缓存获取值 |
| `缓存设置(cache, key, value, ttl)` | 设置缓存值 |
| `缓存删除(cache, key)` | 删除缓存值 |
| `缓存是否存在(cache, key)` | 检查缓存key是否存在且未过期 |
| `缓存清空(cache)` | 清空缓存 |
| `缓存获取统计(cache)` | 获取缓存统计信息 |
| `缓存获取大小(cache)` | 获取缓存当前大小 |
| `__init__(self, config)` |  |
| `_is_expired(self, key)` |  |
| `_evict_if_needed(self)` |  |
| `_remove(self, key)` |  |

---

## 函数详情

### `新建缓存配置(max_size = 100, ttl = 300, policy = 'lru')`

创建缓存配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `max_size` | `None` | （默认：100） |
| `ttl` | `None` | （默认：300） |
| `policy` | `None` | （默认：'lru'） |

---

### `新建内存缓存(config = None)`

创建内存缓存实例

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `config` | `None` | （默认：None） |

---

### `缓存获取(cache, key)`

从缓存获取值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cache` | `None` |  |
| `key` | `None` |  |

---

### `缓存设置(cache, key, value, ttl = None)`

设置缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cache` | `None` |  |
| `key` | `None` |  |
| `value` | `None` |  |
| `ttl` | `None` | （默认：None） |

---

### `缓存删除(cache, key)`

删除缓存值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cache` | `None` |  |
| `key` | `None` |  |

---

### `缓存是否存在(cache, key)`

检查缓存key是否存在且未过期

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cache` | `None` |  |
| `key` | `None` |  |

---

### `缓存清空(cache)`

清空缓存

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cache` | `None` |  |

---

### `缓存获取统计(cache)`

获取缓存统计信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cache` | `None` |  |

---

### `缓存获取大小(cache)`

获取缓存当前大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `cache` | `None` |  |

---

### `__init__(self, config = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `config` | `None` | （默认：None） |

---

### `_is_expired(self, key)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `key` | `None` |  |

---

### `_evict_if_needed(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `_remove(self, key)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `key` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `policy` | `self._config['policy']` |
| `oldest` | `min(self._access_count.keys(), key=lambda k: self._access_count[k])` |
| `ttl` | `ttl if ttl is not None else cache._config['ttl']` |
| `total` | `cache._hits + cache._misses` |
| `hit_rate` | `cache._hits / total if total > 0 else 0` |
| `least` | `min(self._access_count.keys(), key=lambda k: self._access_count[k])` |
| `oldest_exp` | `min(self._expiry.keys(), key=lambda k: self._expiry[k])` |
