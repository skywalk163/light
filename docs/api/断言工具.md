# 断言测试 API

> 模块路径：`stdlib/断言工具.py`
> 导入方式：`从 断言工具 导入 函数名` 或 `导入 断言工具`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `断言(条件, 消息)` | 基本断言 |
| `断言为真(条件, 消息)` | 断言条件为真 |
| `断言为假(条件, 消息)` | 断言条件为假 |
| `断言为无(值, 消息)` | 断言值为None |
| `断言不为无(值, 消息)` | 断言值不为None |
| `断言相等(实际值, 期望值, 消息)` | 断言两个值相等 |
| `断言不相等(实际值, 期望值, 消息)` | 断言两个值不相等 |
| `断言浅相等(实际值, 期望值, 消息)` | 断言两个值浅相等 |
| `断言深相等(实际值, 期望值, 消息)` | 断言两个值深相等 |
| `断言大于(实际值, 期望值, 消息)` | 断言实际值大于期望值 |
| `断言大于等于(实际值, 期望值, 消息)` | 断言实际值大于等于期望值 |
| `断言小于(实际值, 期望值, 消息)` | 断言实际值小于期望值 |
| `断言小于等于(实际值, 期望值, 消息)` | 断言实际值小于等于期望值 |
| `断言接近(实际值, 期望值, 公差, 消息)` | 断言两个浮点数接近 |
| `断言在范围内(实际值, 最小值, 最大值, 消息)` | 断言值在范围内 |
| `断言正数(值, 消息)` | 断言值为正数 |
| `断言负数(值, 消息)` | 断言值为负数 |
| `断言零(值, 消息)` | 断言值为零 |
| `断言字符串相等(实际值, 期望值, 忽略大小写, 消息)` | 断言字符串相等 |
| `断言包含子串(实际值, 子串, 忽略大小写, 消息)` | 断言字符串包含子串 |
| `断言不包含子串(实际值, 子串, 忽略大小写, 消息)` | 断言字符串不包含子串 |
| `断言以开头(实际值, 前缀, 忽略大小写, 消息)` | 断言字符串以指定前缀开头 |
| `断言以结尾(实际值, 后缀, 忽略大小写, 消息)` | 断言字符串以指定后缀结尾 |
| `断言为空字符串(值, 消息)` | 断言字符串为空 |
| `断言不为空字符串(值, 消息)` | 断言字符串不为空 |
| `断言空白字符串(值, 消息)` | 断言字符串为空白 |
| `断言非空白字符串(值, 消息)` | 断言字符串非空白 |
| `断言匹配正则(实际值, 模式, 消息)` | 断言字符串匹配正则表达式 |
| `断言包含(容器, 元素, 消息)` | 断言容器包含元素 |
| `断言不包含(容器, 元素, 消息)` | 断言容器不包含元素 |
| `断言长度(序列, 期望长度, 消息)` | 断言序列长度 |
| `断言为空(序列, 消息)` | 断言序列为空 |
| `断言不为空(序列, 消息)` | 断言序列不为空 |
| `断言子集(实际集合, 期望子集, 消息)` | 断言实际集合是期望子集 |
| `断言超集(实际集合, 期望超集, 消息)` | 断言实际集合是期望超集 |
| `断言集合相等(实际集合, 期望集合, 消息)` | 断言两个集合相等 |
| `断言交集非空(集合1, 集合2, 消息)` | 断言两个集合交集非空 |
| `断言无交集(集合1, 集合2, 消息)` | 断言两个集合无交集 |
| `断言类型(对象, 期望类型, 消息)` | 断言对象是指定类型 |
| `断言实例(对象, 期望类型, 消息)` | 断言对象是指定类型的实例 |
| `断言子类(实际类型, 期望父类, 消息)` | 断言类型是另一个类型的子类 |
| `断言可调用(对象, 消息)` | 断言对象可调用 |
| `断言抛出异常(函数, 异常类型, 消息)` | 断言函数抛出异常 |
| `断言不抛出异常(函数, 消息)` | 断言函数不抛出异常 |
| `断言抛出特定异常(函数, 异常类型, 异常消息, 消息)` | 断言函数抛出特定异常和消息 |
| `断言满足条件(值, 条件函数, 消息)` | 断言值满足条件 |
| `断言所有满足条件(序列, 条件函数, 消息)` | 断言序列中所有元素满足条件 |
| `断言任一满足条件(序列, 条件函数, 消息)` | 断言序列中任一元素满足条件 |
| `断言属性存在(对象, 属性名, 消息)` | 断言对象存在指定属性 |
| `断言属性不存在(对象, 属性名, 消息)` | 断言对象不存在指定属性 |
| `断言属性值(对象, 属性名, 期望值, 消息)` | 断言对象属性值 |
| `断言文件存在(路径, 消息)` | 断言文件存在 |
| `断言文件不存在(路径, 消息)` | 断言文件不存在 |
| `断言目录存在(路径, 消息)` | 断言目录存在 |
| `断言目录不存在(路径, 消息)` | 断言目录不存在 |
| `必为真(条件, 消息)` | 断言条件为真（便捷方法） |
| `必为假(条件, 消息)` | 断言条件为假（便捷方法） |
| `必相等(实际值, 期望值, 消息)` | 断言相等（便捷方法） |
| `必不相等(实际值, 期望值, 消息)` | 断言不相等（便捷方法） |
| `必包含(容器, 元素, 消息)` | 断言包含（便捷方法） |
| `必不包含(容器, 元素, 消息)` | 断言不包含（便捷方法） |
| `必为无(值, 消息)` | 断言为None（便捷方法） |
| `必不为无(值, 消息)` | 断言不为None（便捷方法） |
| `必抛出异常(函数, 异常类型, 消息)` | 断言抛出异常（便捷方法） |
| `期望(实际值)` | 创建链式断言器 |
| `_测试框架_断言相等(实际, 期望, 消息)` | 测试框架用断言相等（计数版，不抛异常） |
| `_测试框架_断言不等(实际, 期望, 消息)` | 测试框架用断言不等（计数版，不抛异常） |
| `_测试框架_断言为真(值, 消息)` | 测试框架用断言为真（计数版，不抛异常） |
| `_测试框架_断言为假(值, 消息)` | 测试框架用断言为假（计数版，不抛异常） |
| `_测试框架_断言为空(值, 消息)` | 测试框架用断言为空（计数版，不抛异常） |
| `_测试框架_断言不为空(值, 消息)` | 测试框架用断言不为空（计数版，不抛异常） |
| `_测试框架_断言近似(实际, 期望, 精度, 消息)` | 测试框架用断言近似（计数版，不抛异常） |
| `测试开始(名称)` | 开始测试套件 |
| `测试结束()` | 结束测试套件并输出结果 |
| `测试运行(测试函数)` | 运行单个测试函数 |
| `__init__(self, 消息, 期望值, 实际值)` |  |
| `__init__(self, 实际值)` |  |
| `为真(self)` | 断言为真 |
| `为假(self)` | 断言为假 |
| `为无(self)` | 断言为None |
| `不为无(self)` | 断言不为None |
| `等于(self, 期望值)` | 断言相等 |
| `不等于(self, 期望值)` | 断言不相等 |
| `大于(self, 期望值)` | 断言大于 |
| `小于(self, 期望值)` | 断言小于 |
| `包含(self, 元素)` | 断言包含 |
| `不包含(self, 元素)` | 断言不包含 |
| `长度为(self, 期望长度)` | 断言长度 |
| `为空(self)` | 断言为空 |
| `不为空(self)` | 断言不为空 |
| `类型为(self, 期望类型)` | 断言类型 |
| `匹配正则(self, 模式)` | 断言匹配正则 |

---

## 函数详情

### `断言(条件: bool, 消息: str = '')`

基本断言

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件` | `bool` |  |
| `消息` | `str` | （默认：''） |

---

### `断言为真(条件: bool, 消息: str = '')`

断言条件为真

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件` | `bool` |  |
| `消息` | `str` | （默认：''） |

---

### `断言为假(条件: bool, 消息: str = '')`

断言条件为假

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件` | `bool` |  |
| `消息` | `str` | （默认：''） |

---

### `断言为无(值: Any, 消息: str = '')`

断言值为None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言不为无(值: Any, 消息: str = '')`

断言值不为None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言相等(实际值: Any, 期望值: Any, 消息: str = '')`

断言两个值相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `Any` |  |
| `期望值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言不相等(实际值: Any, 期望值: Any, 消息: str = '')`

断言两个值不相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `Any` |  |
| `期望值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言浅相等(实际值: Any, 期望值: Any, 消息: str = '')`

断言两个值浅相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `Any` |  |
| `期望值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言深相等(实际值: Any, 期望值: Any, 消息: str = '')`

断言两个值深相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `Any` |  |
| `期望值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言大于(实际值: float, 期望值: float, 消息: str = '')`

断言实际值大于期望值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `float` |  |
| `期望值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言大于等于(实际值: float, 期望值: float, 消息: str = '')`

断言实际值大于等于期望值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `float` |  |
| `期望值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言小于(实际值: float, 期望值: float, 消息: str = '')`

断言实际值小于期望值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `float` |  |
| `期望值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言小于等于(实际值: float, 期望值: float, 消息: str = '')`

断言实际值小于等于期望值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `float` |  |
| `期望值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言接近(实际值: float, 期望值: float, 公差: float = 0.0001, 消息: str = '')`

断言两个浮点数接近

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `float` |  |
| `期望值` | `float` |  |
| `公差` | `float` | （默认：0.0001） |
| `消息` | `str` | （默认：''） |

---

### `断言在范围内(实际值: float, 最小值: float, 最大值: float, 消息: str = '')`

断言值在范围内

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `float` |  |
| `最小值` | `float` |  |
| `最大值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言正数(值: float, 消息: str = '')`

断言值为正数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言负数(值: float, 消息: str = '')`

断言值为负数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言零(值: float, 消息: str = '')`

断言值为零

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `float` |  |
| `消息` | `str` | （默认：''） |

---

### `断言字符串相等(实际值: str, 期望值: str, 忽略大小写: bool = False, 消息: str = '')`

断言字符串相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `str` |  |
| `期望值` | `str` |  |
| `忽略大小写` | `bool` | （默认：False） |
| `消息` | `str` | （默认：''） |

---

### `断言包含子串(实际值: str, 子串: str, 忽略大小写: bool = False, 消息: str = '')`

断言字符串包含子串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `str` |  |
| `子串` | `str` |  |
| `忽略大小写` | `bool` | （默认：False） |
| `消息` | `str` | （默认：''） |

---

### `断言不包含子串(实际值: str, 子串: str, 忽略大小写: bool = False, 消息: str = '')`

断言字符串不包含子串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `str` |  |
| `子串` | `str` |  |
| `忽略大小写` | `bool` | （默认：False） |
| `消息` | `str` | （默认：''） |

---

### `断言以开头(实际值: str, 前缀: str, 忽略大小写: bool = False, 消息: str = '')`

断言字符串以指定前缀开头

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `str` |  |
| `前缀` | `str` |  |
| `忽略大小写` | `bool` | （默认：False） |
| `消息` | `str` | （默认：''） |

---

### `断言以结尾(实际值: str, 后缀: str, 忽略大小写: bool = False, 消息: str = '')`

断言字符串以指定后缀结尾

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `str` |  |
| `后缀` | `str` |  |
| `忽略大小写` | `bool` | （默认：False） |
| `消息` | `str` | （默认：''） |

---

### `断言为空字符串(值: str, 消息: str = '')`

断言字符串为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言不为空字符串(值: str, 消息: str = '')`

断言字符串不为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言空白字符串(值: str, 消息: str = '')`

断言字符串为空白

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言非空白字符串(值: str, 消息: str = '')`

断言字符串非空白

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言匹配正则(实际值: str, 模式: str, 消息: str = '')`

断言字符串匹配正则表达式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `str` |  |
| `模式` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言包含(容器: Iterable, 元素: Any, 消息: str = '')`

断言容器包含元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `容器` | `Iterable` |  |
| `元素` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言不包含(容器: Iterable, 元素: Any, 消息: str = '')`

断言容器不包含元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `容器` | `Iterable` |  |
| `元素` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言长度(序列: Iterable, 期望长度: int, 消息: str = '')`

断言序列长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `序列` | `Iterable` |  |
| `期望长度` | `int` |  |
| `消息` | `str` | （默认：''） |

---

### `断言为空(序列: Iterable, 消息: str = '')`

断言序列为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `序列` | `Iterable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言不为空(序列: Iterable, 消息: str = '')`

断言序列不为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `序列` | `Iterable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言子集(实际集合: Iterable, 期望子集: Iterable, 消息: str = '')`

断言实际集合是期望子集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际集合` | `Iterable` |  |
| `期望子集` | `Iterable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言超集(实际集合: Iterable, 期望超集: Iterable, 消息: str = '')`

断言实际集合是期望超集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际集合` | `Iterable` |  |
| `期望超集` | `Iterable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言集合相等(实际集合: Iterable, 期望集合: Iterable, 消息: str = '')`

断言两个集合相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际集合` | `Iterable` |  |
| `期望集合` | `Iterable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言交集非空(集合1: Iterable, 集合2: Iterable, 消息: str = '')`

断言两个集合交集非空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Iterable` |  |
| `集合2` | `Iterable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言无交集(集合1: Iterable, 集合2: Iterable, 消息: str = '')`

断言两个集合无交集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Iterable` |  |
| `集合2` | `Iterable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言类型(对象: Any, 期望类型: type, 消息: str = '')`

断言对象是指定类型

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `对象` | `Any` |  |
| `期望类型` | `type` |  |
| `消息` | `str` | （默认：''） |

---

### `断言实例(对象: Any, 期望类型: type, 消息: str = '')`

断言对象是指定类型的实例

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `对象` | `Any` |  |
| `期望类型` | `type` |  |
| `消息` | `str` | （默认：''） |

---

### `断言子类(实际类型: type, 期望父类: type, 消息: str = '')`

断言类型是另一个类型的子类

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际类型` | `type` |  |
| `期望父类` | `type` |  |
| `消息` | `str` | （默认：''） |

---

### `断言可调用(对象: Any, 消息: str = '')`

断言对象可调用

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `对象` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言抛出异常(函数: Callable, 异常类型: type = Exception, 消息: str = '')`

断言函数抛出异常

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `Callable` |  |
| `异常类型` | `type` | （默认：Exception） |
| `消息` | `str` | （默认：''） |

---

### `断言不抛出异常(函数: Callable, 消息: str = '')`

断言函数不抛出异常

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `Callable` |  |
| `消息` | `str` | （默认：''） |

---

### `断言抛出特定异常(函数: Callable, 异常类型: type, 异常消息: str, 消息: str = '')`

断言函数抛出特定异常和消息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `Callable` |  |
| `异常类型` | `type` |  |
| `异常消息` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言满足条件(值: Any, 条件函数: Callable[[Any], bool], 消息: str = '')`

断言值满足条件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `条件函数` | `Callable[[Any], bool]` |  |
| `消息` | `str` | （默认：''） |

---

### `断言所有满足条件(序列: Iterable, 条件函数: Callable[[Any], bool], 消息: str = '')`

断言序列中所有元素满足条件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `序列` | `Iterable` |  |
| `条件函数` | `Callable[[Any], bool]` |  |
| `消息` | `str` | （默认：''） |

---

### `断言任一满足条件(序列: Iterable, 条件函数: Callable[[Any], bool], 消息: str = '')`

断言序列中任一元素满足条件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `序列` | `Iterable` |  |
| `条件函数` | `Callable[[Any], bool]` |  |
| `消息` | `str` | （默认：''） |

---

### `断言属性存在(对象: Any, 属性名: str, 消息: str = '')`

断言对象存在指定属性

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `对象` | `Any` |  |
| `属性名` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言属性不存在(对象: Any, 属性名: str, 消息: str = '')`

断言对象不存在指定属性

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `对象` | `Any` |  |
| `属性名` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言属性值(对象: Any, 属性名: str, 期望值: Any, 消息: str = '')`

断言对象属性值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `对象` | `Any` |  |
| `属性名` | `str` |  |
| `期望值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `断言文件存在(路径: str, 消息: str = '')`

断言文件存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言文件不存在(路径: str, 消息: str = '')`

断言文件不存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言目录存在(路径: str, 消息: str = '')`

断言目录存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `断言目录不存在(路径: str, 消息: str = '')`

断言目录不存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |
| `消息` | `str` | （默认：''） |

---

### `必为真(条件: bool, 消息: str = '')`

断言条件为真（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件` | `bool` |  |
| `消息` | `str` | （默认：''） |

---

### `必为假(条件: bool, 消息: str = '')`

断言条件为假（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件` | `bool` |  |
| `消息` | `str` | （默认：''） |

---

### `必相等(实际值: Any, 期望值: Any, 消息: str = '')`

断言相等（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `Any` |  |
| `期望值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `必不相等(实际值: Any, 期望值: Any, 消息: str = '')`

断言不相等（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `Any` |  |
| `期望值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `必包含(容器: Iterable, 元素: Any, 消息: str = '')`

断言包含（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `容器` | `Iterable` |  |
| `元素` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `必不包含(容器: Iterable, 元素: Any, 消息: str = '')`

断言不包含（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `容器` | `Iterable` |  |
| `元素` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `必为无(值: Any, 消息: str = '')`

断言为None（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `必不为无(值: Any, 消息: str = '')`

断言不为None（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

---

### `必抛出异常(函数: Callable, 异常类型: type = Exception, 消息: str = '')`

断言抛出异常（便捷方法）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `Callable` |  |
| `异常类型` | `type` | （默认：Exception） |
| `消息` | `str` | （默认：''） |

---

### `期望(实际值: Any) -> 链式断言器`

创建链式断言器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际值` | `Any` |  |

**返回：** `链式断言器`

---

### `_测试框架_断言相等(实际: Any, 期望: Any, 消息: str = '') -> None`

测试框架用断言相等（计数版，不抛异常）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际` | `Any` |  |
| `期望` | `Any` |  |
| `消息` | `str` | （默认：''） |

**返回：** `None`

---

### `_测试框架_断言不等(实际: Any, 期望: Any, 消息: str = '') -> None`

测试框架用断言不等（计数版，不抛异常）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际` | `Any` |  |
| `期望` | `Any` |  |
| `消息` | `str` | （默认：''） |

**返回：** `None`

---

### `_测试框架_断言为真(值: Any, 消息: str = '') -> None`

测试框架用断言为真（计数版，不抛异常）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

**返回：** `None`

---

### `_测试框架_断言为假(值: Any, 消息: str = '') -> None`

测试框架用断言为假（计数版，不抛异常）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

**返回：** `None`

---

### `_测试框架_断言为空(值: Any, 消息: str = '') -> None`

测试框架用断言为空（计数版，不抛异常）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

**返回：** `None`

---

### `_测试框架_断言不为空(值: Any, 消息: str = '') -> None`

测试框架用断言不为空（计数版，不抛异常）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `Any` |  |
| `消息` | `str` | （默认：''） |

**返回：** `None`

---

### `_测试框架_断言近似(实际: float, 期望: float, 精度: float = 1e-09, 消息: str = '') -> None`

测试框架用断言近似（计数版，不抛异常）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `实际` | `float` |  |
| `期望` | `float` |  |
| `精度` | `float` | （默认：1e-09） |
| `消息` | `str` | （默认：''） |

**返回：** `None`

---

### `测试开始(名称: str) -> None`

开始测试套件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |

**返回：** `None`

---

### `测试结束() -> None`

结束测试套件并输出结果

**参数：**

无参数。

**返回：** `None`

---

### `测试运行(测试函数: Callable) -> None`

运行单个测试函数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `测试函数` | `Callable` |  |

**返回：** `None`

---

### `__init__(self, 消息: str = '', 期望值: Any = None, 实际值: Any = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `消息` | `str` | （默认：''） |
| `期望值` | `Any` | （默认：None） |
| `实际值` | `Any` | （默认：None） |

---

### `__init__(self, 实际值: Any)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `实际值` | `Any` |  |

---

### `为真(self) -> '链式断言器'`

断言为真

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `'链式断言器'`

---

### `为假(self) -> '链式断言器'`

断言为假

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `'链式断言器'`

---

### `为无(self) -> '链式断言器'`

断言为None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `'链式断言器'`

---

### `不为无(self) -> '链式断言器'`

断言不为None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `'链式断言器'`

---

### `等于(self, 期望值: Any) -> '链式断言器'`

断言相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `期望值` | `Any` |  |

**返回：** `'链式断言器'`

---

### `不等于(self, 期望值: Any) -> '链式断言器'`

断言不相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `期望值` | `Any` |  |

**返回：** `'链式断言器'`

---

### `大于(self, 期望值: float) -> '链式断言器'`

断言大于

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `期望值` | `float` |  |

**返回：** `'链式断言器'`

---

### `小于(self, 期望值: float) -> '链式断言器'`

断言小于

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `期望值` | `float` |  |

**返回：** `'链式断言器'`

---

### `包含(self, 元素: Any) -> '链式断言器'`

断言包含

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `元素` | `Any` |  |

**返回：** `'链式断言器'`

---

### `不包含(self, 元素: Any) -> '链式断言器'`

断言不包含

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `元素` | `Any` |  |

**返回：** `'链式断言器'`

---

### `长度为(self, 期望长度: int) -> '链式断言器'`

断言长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `期望长度` | `int` |  |

**返回：** `'链式断言器'`

---

### `为空(self) -> '链式断言器'`

断言为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `'链式断言器'`

---

### `不为空(self) -> '链式断言器'`

断言不为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `'链式断言器'`

---

### `类型为(self, 期望类型: type) -> '链式断言器'`

断言类型

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `期望类型` | `type` |  |

**返回：** `'链式断言器'`

---

### `匹配正则(self, 模式: str) -> '链式断言器'`

断言匹配正则

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `模式` | `str` |  |

**返回：** `'链式断言器'`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `实际` | `实际值.lower() if 忽略大小写 else 实际值` |
| `期望` | `期望值.lower() if 忽略大小写 else 期望值` |
| `实际` | `实际值.lower() if 忽略大小写 else 实际值` |
| `子串` | `子串.lower() if 忽略大小写 else 子串` |
| `实际` | `实际值.lower() if 忽略大小写 else 实际值` |
| `子串` | `子串.lower() if 忽略大小写 else 子串` |
| `实际` | `实际值.lower() if 忽略大小写 else 实际值` |
| `前缀` | `前缀.lower() if 忽略大小写 else 前缀` |
| `实际` | `实际值.lower() if 忽略大小写 else 实际值` |
| `后缀` | `后缀.lower() if 忽略大小写 else 后缀` |
| `实际长度` | `len(序列)` |
| `实际` | `set(实际集合)` |
| `期望` | `set(期望子集)` |
| `实际` | `set(实际集合)` |
| `期望` | `set(期望超集)` |
| `实际` | `set(实际集合)` |
| `期望` | `set(期望集合)` |
| `集合1` | `set(集合1)` |
| `集合2` | `set(集合2)` |
| `集合1` | `set(集合1)` |
| `集合2` | `set(集合2)` |
| `实际值` | `getattr(对象, 属性名)` |
| `总数` | `_通过数 + _失败数` |
| `实际JSON` | `json.dumps(实际值, sort_keys=True)` |
| `期望JSON` | `json.dumps(期望值, sort_keys=True)` |
