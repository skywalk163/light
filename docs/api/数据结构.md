# 数据结构 API

> 模块路径：`stdlib/数据结构.py`
> 导入方式：`从 数据结构 导入 函数名` 或 `导入 数据结构`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建链表节点(值)` | 创建链表节点 |
| `创建链表()` | 创建空链表 |
| `链表追加(链表, 值)` | 向链表追加元素 |
| `链表插入(链表, 索引, 值)` | 在链表指定位置插入元素 |
| `链表删除(链表, 索引)` | 删除链表指定位置的元素 |
| `链表获取(链表, 索引)` | 获取链表指定位置的元素值 |
| `链表长度获取(链表)` | 获取链表长度 |
| `链表转为列表(链表)` | 将链表转为Python列表 |
| `创建栈()` | 创建空栈 |
| `栈压入(栈, 值)` | 向栈中压入元素 |
| `栈弹出(栈)` | 从栈中弹出元素 |
| `栈窥视(栈)` | 查看栈顶元素但不弹出 |
| `栈大小(栈)` | 获取栈的大小 |
| `栈是否为空(栈)` | 检查栈是否为空 |
| `创建队列()` | 创建空队列 |
| `队列入队(队列, 值)` | 向队列尾部入队 |
| `队列出队(队列)` | 从队列头部出队 |
| `队列窥视(队列)` | 查看队列头部元素但不移除 |
| `队列大小(队列)` | 获取队列的大小 |
| `队列是否为空(队列)` | 检查队列是否为空 |
| `创建最小堆()` | 创建最小堆，返回堆对象 |
| `堆上浮(堆, 索引)` | 对堆中指定索引的元素执行上浮操作 |
| `堆下沉(堆, 索引)` | 对堆中指定索引的元素执行下沉操作 |
| `堆插入(堆, 值)` | 向堆中插入一个值 |
| `堆弹出(堆)` | 从堆中弹出最小值 |
| `堆窥视(堆)` | 查看堆顶元素但不弹出 |
| `堆是否为空(堆)` | 检查堆是否为空 |
| `创建树节点(值)` | 创建树节点 |
| `创建二叉树()` | 创建空二叉树 |
| `二叉树插入(二叉树, 值)` | 向二叉搜索树中插入值 |
| `二叉树包含(二叉树, 值)` | 检查二叉树中是否包含指定值 |
| `中序遍递归(节点)` | 中序遍历递归辅助函数，返回值列表 |
| `二叉树中序遍(二叉树)` | 中序遍历二叉树，返回值列表 |
| `前序遍递归(节点)` | 前序遍历递归辅助函数，返回值列表 |
| `二叉树前序遍(二叉树)` | 前序遍历二叉树，返回值列表 |
| `后序遍递归(节点)` | 后序遍历递归辅助函数，返回值列表 |
| `二叉树后序遍(二叉树)` | 后序遍历二叉树，返回值列表 |
| `创建图()` | 创建空图 |
| `图添加节点(图, 节点)` | 向图中添加节点 |
| `图添加边(图, 节点1, 节点2, 权重)` | 向图中添加边（无向图） |
| `图邻居(图, 节点)` | 获取图中指定节点的邻居列表 |
| `图节点数(图)` | 获取图的节点数 |
| `图边数(图)` | 获取图的边数 |
| `图包含边(图, 节点1, 节点2)` | 检查图中是否存在边 |
| `创建哈希表增强()` | 创建增强哈希表 |
| `哈希表设置(哈希表, 键, 值)` | 设置哈希表中的键值对 |
| `哈希表获取(哈希表, 键, 默认值)` | 获取哈希表中指定键的值 |
| `哈希表删除(哈希表, 键)` | 删除哈希表中指定键的键值对 |
| `哈希表包含(哈希表, 键)` | 检查哈希表中是否包含指定键 |
| `哈希表键列表(哈希表)` | 获取哈希表中所有键的列表 |
| `哈希表值列表(哈希表)` | 获取哈希表中所有值的列表 |
| `哈希表条目列表(哈希表)` | 获取哈希表中所有条目的列表 |
| `__init__(self, 值)` |  |
| `__init__(self)` |  |
| `__init__(self)` |  |
| `__init__(self)` |  |
| `__init__(self)` |  |
| `__init__(self, 值)` |  |
| `__init__(self)` |  |
| `__init__(self)` |  |
| `__init__(self)` |  |

---

## 函数详情

### `创建链表节点(值)`

创建链表节点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `创建链表()`

创建空链表

**参数：**

无参数。

---

### `链表追加(链表, 值)`

向链表追加元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `链表` | `None` |  |
| `值` | `None` |  |

---

### `链表插入(链表, 索引, 值)`

在链表指定位置插入元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `链表` | `None` |  |
| `索引` | `None` |  |
| `值` | `None` |  |

---

### `链表删除(链表, 索引)`

删除链表指定位置的元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `链表` | `None` |  |
| `索引` | `None` |  |

---

### `链表获取(链表, 索引)`

获取链表指定位置的元素值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `链表` | `None` |  |
| `索引` | `None` |  |

---

### `链表长度获取(链表)`

获取链表长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `链表` | `None` |  |

---

### `链表转为列表(链表)`

将链表转为Python列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `链表` | `None` |  |

---

### `创建栈()`

创建空栈

**参数：**

无参数。

---

### `栈压入(栈, 值)`

向栈中压入元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `栈` | `None` |  |
| `值` | `None` |  |

---

### `栈弹出(栈)`

从栈中弹出元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `栈` | `None` |  |

---

### `栈窥视(栈)`

查看栈顶元素但不弹出

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `栈` | `None` |  |

---

### `栈大小(栈)`

获取栈的大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `栈` | `None` |  |

---

### `栈是否为空(栈)`

检查栈是否为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `栈` | `None` |  |

---

### `创建队列()`

创建空队列

**参数：**

无参数。

---

### `队列入队(队列, 值)`

向队列尾部入队

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |
| `值` | `None` |  |

---

### `队列出队(队列)`

从队列头部出队

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `队列窥视(队列)`

查看队列头部元素但不移除

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `队列大小(队列)`

获取队列的大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `队列是否为空(队列)`

检查队列是否为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `队列` | `None` |  |

---

### `创建最小堆()`

创建最小堆，返回堆对象

**参数：**

无参数。

---

### `堆上浮(堆, 索引)`

对堆中指定索引的元素执行上浮操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |
| `索引` | `None` |  |

---

### `堆下沉(堆, 索引)`

对堆中指定索引的元素执行下沉操作

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |
| `索引` | `None` |  |

---

### `堆插入(堆, 值)`

向堆中插入一个值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |
| `值` | `None` |  |

---

### `堆弹出(堆)`

从堆中弹出最小值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |

---

### `堆窥视(堆)`

查看堆顶元素但不弹出

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |

---

### `堆是否为空(堆)`

检查堆是否为空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `堆` | `None` |  |

---

### `创建树节点(值)`

创建树节点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

---

### `创建二叉树()`

创建空二叉树

**参数：**

无参数。

---

### `二叉树插入(二叉树, 值)`

向二叉搜索树中插入值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `二叉树` | `None` |  |
| `值` | `None` |  |

---

### `二叉树包含(二叉树, 值)`

检查二叉树中是否包含指定值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `二叉树` | `None` |  |
| `值` | `None` |  |

---

### `中序遍递归(节点)`

中序遍历递归辅助函数，返回值列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `节点` | `None` |  |

---

### `二叉树中序遍(二叉树)`

中序遍历二叉树，返回值列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `二叉树` | `None` |  |

---

### `前序遍递归(节点)`

前序遍历递归辅助函数，返回值列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `节点` | `None` |  |

---

### `二叉树前序遍(二叉树)`

前序遍历二叉树，返回值列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `二叉树` | `None` |  |

---

### `后序遍递归(节点)`

后序遍历递归辅助函数，返回值列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `节点` | `None` |  |

---

### `二叉树后序遍(二叉树)`

后序遍历二叉树，返回值列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `二叉树` | `None` |  |

---

### `创建图()`

创建空图

**参数：**

无参数。

---

### `图添加节点(图, 节点)`

向图中添加节点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |
| `节点` | `None` |  |

---

### `图添加边(图, 节点1, 节点2, 权重 = 1)`

向图中添加边（无向图）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |
| `节点1` | `None` |  |
| `节点2` | `None` |  |
| `权重` | `None` | （默认：1） |

---

### `图邻居(图, 节点)`

获取图中指定节点的邻居列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |
| `节点` | `None` |  |

---

### `图节点数(图)`

获取图的节点数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |

---

### `图边数(图)`

获取图的边数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |

---

### `图包含边(图, 节点1, 节点2)`

检查图中是否存在边

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `图` | `None` |  |
| `节点1` | `None` |  |
| `节点2` | `None` |  |

---

### `创建哈希表增强()`

创建增强哈希表

**参数：**

无参数。

---

### `哈希表设置(哈希表, 键, 值)`

设置哈希表中的键值对

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希表` | `None` |  |
| `键` | `None` |  |
| `值` | `None` |  |

---

### `哈希表获取(哈希表, 键, 默认值 = None)`

获取哈希表中指定键的值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希表` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：None） |

---

### `哈希表删除(哈希表, 键)`

删除哈希表中指定键的键值对

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希表` | `None` |  |
| `键` | `None` |  |

---

### `哈希表包含(哈希表, 键)`

检查哈希表中是否包含指定键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希表` | `None` |  |
| `键` | `None` |  |

---

### `哈希表键列表(哈希表)`

获取哈希表中所有键的列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希表` | `None` |  |

---

### `哈希表值列表(哈希表)`

获取哈希表中所有值的列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希表` | `None` |  |

---

### `哈希表条目列表(哈希表)`

获取哈希表中所有条目的列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `哈希表` | `None` |  |

---

### `__init__(self, 值)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `值` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 值)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `值` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `new_node` | `链表节点(值)` |
| `new_node` | `链表节点(值)` |
| `cur` | `链表.头` |
| `result` | `[]` |
| `cur` | `链表.头` |
| `new_node` | `树节点(值)` |
| `cur` | `二叉树.根` |
| `cur` | `二叉树.根` |
| `cur` | `链表.头` |
| `cur` | `链表.头` |
| `cur` | `链表.头` |
| `cur` | `cur.下一个` |
| `cur` | `cur.下一个` |
| `cur` | `cur.下一个` |
| `cur` | `cur.下一个` |
| `cur` | `cur.下一个` |
| `cur` | `cur.左` |
| `cur` | `cur.右` |
| `cur` | `cur.左` |
| `cur` | `cur.右` |
