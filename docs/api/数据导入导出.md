# 数据导入导出 API

> 模块路径：`stdlib/数据导入导出.py`
> 导入方式：`从 数据导入导出 导入 函数名` 或 `导入 数据导入导出`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建读取选项(分隔符, 引号字符, 转义字符, 有表头, 编码)` | 创建 CSV 读取选项对象 |
| `创建写入选项(分隔符, 引号字符, 行终止符, 编码)` | 创建 CSV 写入选项对象 |
| `读取CSV文本(文本, 选项)` | 读取 CSV 文本，返回二维列表 |
| `写入CSV文本(数据, 选项)` | 将二维列表写入 CSV 文本 |
| `读取TSV(文本, 选项)` | 读取 TSV 文本 |
| `写入TSV(数据)` | 将二维列表写入 TSV 文本 |
| `skip_whitespace(文本)` | 跳过空白字符，返回非空白起始索引 |
| `parse_json_string(文本)` | 解析 JSON 字符串 |
| `parse_json_number(文本)` | 解析 JSON 数字 |
| `parse_json_bool(文本)` | 解析 JSON 布尔值 |
| `parse_json_null(文本)` | 解析 JSON null |
| `parse_json_array(文本)` | 解析 JSON 数组 |
| `parse_json_object(文本)` | 解析 JSON 对象 |
| `parse_json_value(文本)` | 解析 JSON 值 |
| `读取JSON文本(文本)` | 读取 JSON 文本 |
| `json_to_string(值, 缩进)` | 将 JSON 值转换为字符串 |
| `写入JSON文本(值, 缩进)` | 将值写入为 JSON 文本 |
| `读取JSON行(文本)` | 读取 JSON Lines 格式（每行一个 JSON 对象） |
| `写入JSON行(数据)` | 写入 JSON Lines 格式 |
| `parse_xml(文本)` | 解析 XML 文本，返回 XMLNode 对象 |
| `XMLNode_获取属性(node, 名称)` | 获取 XML 节点属性 |
| `XMLNode_获取子节点(node, 名称)` | 获取 XML 子节点 |
| `XMLNode_获取所有子节点(node)` | 获取所有 XML 子节点 |
| `XMLNode_获取文本(node)` | 获取 XML 节点文本 |
| `读取XML文本(文本)` | 读取 XML 文本，返回 XMLNode |
| `parse_yaml(文本)` | 解析 YAML 文本（回退到 JSON 解析，YAML 是 JSON 超集） |
| `读取YAML文本(文本)` | 读取 YAML 文本 |
| `写入YAML文本(数据)` | 将数据写入 YAML 格式 |
| `读取Excel(文件路径, 工作表名)` | 读取 Excel 文件（回退到 CSV 解析） |
| `读取Excel多工作表(文件路径)` | 读取 Excel 文件的多工作表 |
| `写入Excel(文件路径, 数据, 工作表名)` | 写入 Excel 文件（以 CSV 格式写入） |
| `创建工作簿()` | 创建 Excel 工作簿对象 |
| `ExcelWorkbook_添加工作表(wb, 名称, 数据)` | 向工作簿添加工作表 |
| `ExcelWorkbook_获取工作表(wb, 名称)` | 获取工作簿中的工作表 |
| `ExcelWorkbook_获取工作表名称列表(wb)` | 获取工作簿中所有工作表名称 |
| `读取固定宽度(文本, 宽度列表)` | 读取固定宽度文本 |
| `写入固定宽度(数据, 宽度列表)` | 写入固定宽度文本 |
| `读取INI(文本)` | 读取 INI 格式文本，返回字典 |
| `写入INI(数据)` | 将字典写入 INI 格式文本 |
| `读取属性文件(文本)` | 读取属性文件（key=value 格式） |
| `写入属性文件(数据)` | 将字典写入属性文件格式 |
| `读取Markdown表格(文本)` | 读取 Markdown 表格，返回二维列表 |
| `写入Markdown表格(数据)` | 将二维列表写入 Markdown 表格格式 |
| `检测文件格式(文件路径)` | 检测文件格式，返回格式名称 |
| `检测CSV分隔符(文本)` | 检测 CSV 分隔符 |
| `检测编码(文件路径)` | 检测文件编码 |
| `获取文件信息(文件路径)` | 获取文件信息 |
| `__init__(self, 分隔符, 引号字符, 转义字符, 有表头, 编码)` |  |
| `__init__(self, 分隔符, 引号字符, 行终止符, 编码)` |  |
| `__init__(self, elem)` |  |
| `获取属性(self, 名称)` |  |
| `获取子节点(self, 名称)` |  |
| `获取所有子节点(self)` |  |
| `获取文本(self)` |  |
| `__init__(self)` |  |
| `添加工作表(self, 名称, 数据)` |  |
| `获取工作表(self, 名称)` |  |
| `获取工作表名称列表(self)` |  |

---

## 函数详情

### `创建读取选项(分隔符 = ',', 引号字符 = '"', 转义字符 = None, 有表头 = True, 编码 = 'utf-8')`

创建 CSV 读取选项对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `分隔符` | `None` | （默认：','） |
| `引号字符` | `None` | （默认：'"'） |
| `转义字符` | `None` | （默认：None） |
| `有表头` | `None` | （默认：True） |
| `编码` | `None` | （默认：'utf-8'） |

---

### `创建写入选项(分隔符 = ',', 引号字符 = '"', 行终止符 = '\r\n', 编码 = 'utf-8')`

创建 CSV 写入选项对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `分隔符` | `None` | （默认：','） |
| `引号字符` | `None` | （默认：'"'） |
| `行终止符` | `None` | （默认：'\r\n'） |
| `编码` | `None` | （默认：'utf-8'） |

---

### `读取CSV文本(文本, 选项 = None)`

读取 CSV 文本，返回二维列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `选项` | `None` | （默认：None） |

---

### `写入CSV文本(数据, 选项 = None)`

将二维列表写入 CSV 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `选项` | `None` | （默认：None） |

---

### `读取TSV(文本, 选项 = None)`

读取 TSV 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `选项` | `None` | （默认：None） |

---

### `写入TSV(数据)`

将二维列表写入 TSV 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `skip_whitespace(文本)`

跳过空白字符，返回非空白起始索引

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_json_string(文本)`

解析 JSON 字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_json_number(文本)`

解析 JSON 数字

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_json_bool(文本)`

解析 JSON 布尔值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_json_null(文本)`

解析 JSON null

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_json_array(文本)`

解析 JSON 数组

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_json_object(文本)`

解析 JSON 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_json_value(文本)`

解析 JSON 值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `读取JSON文本(文本)`

读取 JSON 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `json_to_string(值, 缩进 = None)`

将 JSON 值转换为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |
| `缩进` | `None` | （默认：None） |

---

### `写入JSON文本(值, 缩进 = None)`

将值写入为 JSON 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |
| `缩进` | `None` | （默认：None） |

---

### `读取JSON行(文本)`

读取 JSON Lines 格式（每行一个 JSON 对象）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `写入JSON行(数据)`

写入 JSON Lines 格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `parse_xml(文本)`

解析 XML 文本，返回 XMLNode 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `XMLNode_获取属性(node, 名称)`

获取 XML 节点属性

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `node` | `None` |  |
| `名称` | `None` |  |

---

### `XMLNode_获取子节点(node, 名称)`

获取 XML 子节点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `node` | `None` |  |
| `名称` | `None` |  |

---

### `XMLNode_获取所有子节点(node)`

获取所有 XML 子节点

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `node` | `None` |  |

---

### `XMLNode_获取文本(node)`

获取 XML 节点文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `node` | `None` |  |

---

### `读取XML文本(文本)`

读取 XML 文本，返回 XMLNode

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `parse_yaml(文本)`

解析 YAML 文本（回退到 JSON 解析，YAML 是 JSON 超集）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `读取YAML文本(文本)`

读取 YAML 文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `写入YAML文本(数据)`

将数据写入 YAML 格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `读取Excel(文件路径, 工作表名 = None)`

读取 Excel 文件（回退到 CSV 解析）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |
| `工作表名` | `None` | （默认：None） |

---

### `读取Excel多工作表(文件路径)`

读取 Excel 文件的多工作表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `写入Excel(文件路径, 数据, 工作表名 = 'Sheet1')`

写入 Excel 文件（以 CSV 格式写入）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |
| `数据` | `None` |  |
| `工作表名` | `None` | （默认：'Sheet1'） |

---

### `创建工作簿()`

创建 Excel 工作簿对象

**参数：**

无参数。

---

### `ExcelWorkbook_添加工作表(wb, 名称, 数据)`

向工作簿添加工作表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `wb` | `None` |  |
| `名称` | `None` |  |
| `数据` | `None` |  |

---

### `ExcelWorkbook_获取工作表(wb, 名称)`

获取工作簿中的工作表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `wb` | `None` |  |
| `名称` | `None` |  |

---

### `ExcelWorkbook_获取工作表名称列表(wb)`

获取工作簿中所有工作表名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `wb` | `None` |  |

---

### `读取固定宽度(文本, 宽度列表)`

读取固定宽度文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `宽度列表` | `None` |  |

---

### `写入固定宽度(数据, 宽度列表)`

写入固定宽度文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |
| `宽度列表` | `None` |  |

---

### `读取INI(文本)`

读取 INI 格式文本，返回字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `写入INI(数据)`

将字典写入 INI 格式文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `读取属性文件(文本)`

读取属性文件（key=value 格式）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `写入属性文件(数据)`

将字典写入属性文件格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `读取Markdown表格(文本)`

读取 Markdown 表格，返回二维列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `写入Markdown表格(数据)`

将二维列表写入 Markdown 表格格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` |  |

---

### `检测文件格式(文件路径)`

检测文件格式，返回格式名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `检测CSV分隔符(文本)`

检测 CSV 分隔符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `检测编码(文件路径)`

检测文件编码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `获取文件信息(文件路径)`

获取文件信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `__init__(self, 分隔符 = ',', 引号字符 = '"', 转义字符 = None, 有表头 = True, 编码 = 'utf-8')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `分隔符` | `None` | （默认：','） |
| `引号字符` | `None` | （默认：'"'） |
| `转义字符` | `None` | （默认：None） |
| `有表头` | `None` | （默认：True） |
| `编码` | `None` | （默认：'utf-8'） |

---

### `__init__(self, 分隔符 = ',', 引号字符 = '"', 行终止符 = '\r\n', 编码 = 'utf-8')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `分隔符` | `None` | （默认：','） |
| `引号字符` | `None` | （默认：'"'） |
| `行终止符` | `None` | （默认：'\r\n'） |
| `编码` | `None` | （默认：'utf-8'） |

---

### `__init__(self, elem = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `elem` | `None` | （默认：None） |

---

### `获取属性(self, 名称)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` |  |

---

### `获取子节点(self, 名称)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` |  |

---

### `获取所有子节点(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `获取文本(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `添加工作表(self, 名称, 数据)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` |  |
| `数据` | `None` |  |

---

### `获取工作表(self, 名称)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` |  |

---

### `获取工作表名称列表(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `s` | `文本.strip().lower()` |
| `sep` | `选项.分隔符 if 选项 else ','` |
| `reader` | `_csv.reader(_io.StringIO(文本), delimiter=sep)` |
| `output` | `_io.StringIO()` |
| `sep` | `选项.分隔符 if 选项 else ','` |
| `writer` | `_csv.writer(output, delimiter=sep, lineterminator='\n')` |
| `reader` | `_csv.reader(_io.StringIO(文本), delimiter='\t')` |
| `output` | `_io.StringIO()` |
| `writer` | `_csv.writer(output, delimiter='\t', lineterminator='\n')` |
| `result` | `_json.loads(文本)` |
| `result` | `_json.loads(文本)` |
| `result` | `[]` |
| `lines` | `[_json.dumps(item, ensure_ascii=False) for item in 数据]` |
| `root` | `_ET.fromstring(文本)` |
| `result` | `{}` |
| `lines` | `[]` |
| `lines` | `[]` |
| `workbook` | `ExcelWorkbook()` |
| `result` | `[]` |
| `lines` | `[]` |
| `config` | `_configparser.ConfigParser()` |
| `result` | `{}` |
| `config` | `_configparser.ConfigParser()` |
| `output` | `_io.StringIO()` |
| `result` | `{}` |
| `lines` | `[]` |
| `lines` | `[line.strip() for line in 文本.split('\n') if line.strip()]` |
| `data_lines` | `[line for line in lines if not line.startswith('|') or not line.replace('|', '').replace('-', '').replace(':', '').strip() == '' or line.count('|') > 2]` |
| `data_lines` | `[]` |
| `result` | `[]` |
| `lines` | `[]` |
| `ext` | `ext.lower()` |
| `格式映射` | `{'.csv': 'csv', '.tsv': 'tsv', '.json': 'json', '.xml': 'xml', '.yaml': 'yaml', '.yml': 'yaml', '.ini': 'ini', '.properties': 'properties', '.md': 'markdown', '.xlsx': 'excel', '.xls': 'excel', '.txt': 'text'}` |
| `候选` | `[',', '\t', ';', '|']` |
| `最佳` | `','` |
| `最佳得分` | `-1` |
| `first_line` | `文本.split('\n')[0] if '\n' in 文本 else 文本` |
| `stat` | `_os.stat(文件路径)` |
| `line` | `line.strip()` |
| `child` | `self._elem.find(名称)` |
| `line` | `line.strip()` |
| `val_str` | `写入YAML文本(val).strip()` |
| `val_str` | `写入YAML文本(item).strip()` |
| `reader` | `_csv.reader(f)` |
| `reader` | `_csv.reader(f)` |
| `data` | `[row for row in reader]` |
| `writer` | `_csv.writer(f)` |
| `line` | `line.rstrip('\r')` |
| `row` | `[]` |
| `pos` | `0` |
| `parts` | `[]` |
| `line` | `line.strip()` |
| `row` | `[cell.strip() for cell in line.strip('|').split('|')]` |
| `得分` | `first_line.count(d)` |
| `raw` | `f.read(4)` |
| `key` | `key.strip()` |
| `val` | `val.strip()` |
| `val` | `str(row[i]) if i < len(row) else ''` |
| `content` | `line[1:-1].replace('|', '').strip()` |
| `最佳得分` | `得分` |
| `最佳` | `d` |
| `val` | `val[1:-1]` |
| `val` | `val[:w]` |
| `val` | `True` |
| `val` | `False` |
| `val` | `None` |
| `val` | `float(val)` |
| `val` | `int(val)` |
