# 配置管理 API

> 模块路径：`stdlib/配置管理.py`
> 导入方式：`从 配置管理 导入 函数名` 或 `导入 配置管理`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建配置源(名称)` | 创建配置源 |
| `从字典加载(配置源, 字典数据)` | 从字典加载配置 |
| `从文件加载(配置源, 文件路径, 格式)` | 从文件加载配置（支持 ini/json/yaml/toml） |
| `_加载_ini(配置源, 文件路径)` | 内部：从 INI 文件加载配置 |
| `_加载_json(配置源, 文件路径)` | 内部：从 JSON 文件加载配置 |
| `_加载_yaml(配置源, 文件路径)` | 内部：从 YAML 文件加载配置 |
| `_加载_toml(配置源, 文件路径)` | 内部：从 TOML 文件加载配置 |
| `_展平字典(目标, 数据, 前缀)` | 内部：递归展平字典为点号分隔的键值对 |
| `从环境变量加载(配置源, 前缀)` | 从环境变量加载配置 |
| `获取(配置源, 键, 默认值)` | 获取配置项 |
| `设置(配置源, 键, 值)` | 设置配置项 |
| `获取整数(配置源, 键, 默认值)` | 获取整数配置 |
| `获取浮点数(配置源, 键, 默认值)` | 获取浮点数配置 |
| `获取布尔(配置源, 键, 默认值)` | 获取布尔配置 |
| `获取列表(配置源, 键, 分隔符, 默认值)` | 获取列表配置 |
| `获取字典(配置源, 键, 默认值)` | 获取字典配置（JSON 格式） |
| `包含(配置源, 键)` | 检查配置项是否存在 |
| `删除(配置源, 键)` | 删除配置项 |
| `全部键(配置源)` | 获取所有配置键 |
| `导出字典(配置源)` | 导出配置为字典 |
| `保存到文件(配置源, 文件路径, 格式)` | 保存配置到文件 |
| `_保存_ini(配置源, 文件路径)` | 内部：保存配置为 INI 文件 |
| `_保存_json(配置源, 文件路径)` | 内部：保存配置为 JSON 文件 |
| `_保存_yaml(配置源, 文件路径)` | 内部：保存配置为 YAML 文件 |
| `_保存_toml(配置源, 文件路径)` | 内部：保存配置为 TOML 文件 |
| `_恢复嵌套字典(扁平数据)` | 内部：将点号分隔的扁平键恢复为嵌套字典 |
| `刷新(配置源)` | 重新加载配置 |
| `创建配置(名称)` | 创建配置管理器（快捷方式） |
| `创建INI配置(文件路径)` | 创建基于 INI 文件的配置 |
| `创建JSON配置(文件路径)` | 创建基于 JSON 文件的配置 |
| `创建分层配置(字典数据)` | 创建分层配置（支持点号分隔的键路径） |
| `__init__(self, 键, 值, 描述, 类型)` |  |
| `__init__(self, 名称)` |  |
| `全部键(self)` |  |
| `导出字典(self)` |  |
| `__init__(self, 名称)` |  |
| `添加源(self, 配置源, 优先级)` | 添加配置源（优先级越高越优先） |
| `移除源(self, 配置源名称)` | 移除配置源 |
| `获取(self, 键, 默认值)` | 获取配置（按优先级从高到低查找） |
| `设置(self, 键, 值)` | 设置配置到最高优先级源 |
| `注册监听器(self, 回调函数)` | 注册配置变更监听器 |
| `通知变更(self, 键, 旧值, 新值)` | 通知配置变更 |

---

## 函数详情

### `创建配置源(名称 = '默认')`

创建配置源

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `None` | （默认：'默认'） |

---

### `从字典加载(配置源, 字典数据)`

从字典加载配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `字典数据` | `None` |  |

---

### `从文件加载(配置源, 文件路径, 格式 = 'ini')`

从文件加载配置（支持 ini/json/yaml/toml）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |
| `格式` | `None` | （默认：'ini'） |

---

### `_加载_ini(配置源, 文件路径)`

内部：从 INI 文件加载配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_加载_json(配置源, 文件路径)`

内部：从 JSON 文件加载配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_加载_yaml(配置源, 文件路径)`

内部：从 YAML 文件加载配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_加载_toml(配置源, 文件路径)`

内部：从 TOML 文件加载配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_展平字典(目标, 数据, 前缀)`

内部：递归展平字典为点号分隔的键值对

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `目标` | `None` |  |
| `数据` | `None` |  |
| `前缀` | `None` |  |

---

### `从环境变量加载(配置源, 前缀 = '')`

从环境变量加载配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `前缀` | `None` | （默认：''） |

---

### `获取(配置源, 键, 默认值 = None)`

获取配置项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：None） |

---

### `设置(配置源, 键, 值)`

设置配置项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |
| `值` | `None` |  |

---

### `获取整数(配置源, 键, 默认值 = 0)`

获取整数配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：0） |

---

### `获取浮点数(配置源, 键, 默认值 = 0.0)`

获取浮点数配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：0.0） |

---

### `获取布尔(配置源, 键, 默认值 = False)`

获取布尔配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：False） |

---

### `获取列表(配置源, 键, 分隔符 = ',', 默认值 = None)`

获取列表配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |
| `分隔符` | `None` | （默认：','） |
| `默认值` | `None` | （默认：None） |

---

### `获取字典(配置源, 键, 默认值 = None)`

获取字典配置（JSON 格式）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：None） |

---

### `包含(配置源, 键)`

检查配置项是否存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |

---

### `删除(配置源, 键)`

删除配置项

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `键` | `None` |  |

---

### `全部键(配置源)`

获取所有配置键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |

---

### `导出字典(配置源)`

导出配置为字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |

---

### `保存到文件(配置源, 文件路径, 格式 = 'ini')`

保存配置到文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |
| `格式` | `None` | （默认：'ini'） |

---

### `_保存_ini(配置源, 文件路径)`

内部：保存配置为 INI 文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_保存_json(配置源, 文件路径)`

内部：保存配置为 JSON 文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_保存_yaml(配置源, 文件路径)`

内部：保存配置为 YAML 文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_保存_toml(配置源, 文件路径)`

内部：保存配置为 TOML 文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |
| `文件路径` | `None` |  |

---

### `_恢复嵌套字典(扁平数据)`

内部：将点号分隔的扁平键恢复为嵌套字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `扁平数据` | `None` |  |

---

### `刷新(配置源)`

重新加载配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `配置源` | `None` |  |

---

### `创建配置(名称 = '默认')`

创建配置管理器（快捷方式）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `None` | （默认：'默认'） |

---

### `创建INI配置(文件路径)`

创建基于 INI 文件的配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `创建JSON配置(文件路径)`

创建基于 JSON 文件的配置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文件路径` | `None` |  |

---

### `创建分层配置(字典数据)`

创建分层配置（支持点号分隔的键路径）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典数据` | `None` |  |

---

### `__init__(self, 键 = '', 值 = '', 描述 = '', 类型 = '字符串')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `None` | （默认：''） |
| `值` | `None` | （默认：''） |
| `描述` | `None` | （默认：''） |
| `类型` | `None` | （默认：'字符串'） |

---

### `__init__(self, 名称 = '默认')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` | （默认：'默认'） |

---

### `全部键(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `导出字典(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 名称 = '默认')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` | （默认：'默认'） |

---

### `添加源(self, 配置源, 优先级 = 0)`

添加配置源（优先级越高越优先）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `配置源` | `None` |  |
| `优先级` | `None` | （默认：0） |

---

### `移除源(self, 配置源名称)`

移除配置源

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `配置源名称` | `None` |  |

---

### `获取(self, 键, 默认值 = None)`

获取配置（按优先级从高到低查找）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：None） |

---

### `设置(self, 键, 值)`

设置配置到最高优先级源

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `None` |  |
| `值` | `None` |  |

---

### `注册监听器(self, 回调函数)`

注册配置变更监听器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `回调函数` | `None` |  |

---

### `通知变更(self, 键, 旧值, 新值)`

通知配置变更

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `None` |  |
| `旧值` | `None` |  |
| `新值` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `parser` | `_configparser.ConfigParser()` |
| `parser` | `_configparser.ConfigParser()` |
| `sections` | `{}` |
| `data` | `_恢复嵌套字典(配置源._数据)` |
| `data` | `_恢复嵌套字典(配置源._数据)` |
| `data` | `_恢复嵌套字典(配置源._数据)` |
| `result` | `{}` |
| `源` | `创建配置源('ini源')` |
| `管理器` | `配置管理器('ini配置')` |
| `源` | `创建配置源('json源')` |
| `管理器` | `配置管理器('json配置')` |
| `源` | `创建配置源('分层源')` |
| `管理器` | `配置管理器('分层配置')` |
| `格式` | `格式.lower()` |
| `data` | `_json.load(f)` |
| `data` | `_yaml.safe_load(f)` |
| `data` | `_tomllib.load(f)` |
| `value` | `配置源._数据.get(键)` |
| `value` | `配置源._数据.get(键)` |
| `value` | `配置源._数据.get(键)` |
| `默认值` | `[]` |
| `value` | `配置源._数据.get(键)` |
| `默认值` | `{}` |
| `value` | `配置源._数据.get(键)` |
| `格式` | `格式.lower()` |
| `parts` | `key.split('.')` |
| `当前` | `result` |
| `最高源` | `self._配置源列表[0][1]` |
| `旧值` | `获取(最高源, 键)` |
| `新键` | `前缀 + '.' + key if 前缀 else key` |
| `当前` | `当前[part]` |
| `value` | `获取(配置源, 键)` |
