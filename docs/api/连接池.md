# 连接池 API

> 模块路径：`stdlib/连接池.py`
> 导入方式：`从 连接池 导入 函数名` 或 `导入 连接池`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建连接池(factory, min_size, max_size)` | 创建连接池 |
| `连接池获取连接(pool, timeout)` | 从连接池获取连接 |
| `连接池释放连接(pool, conn)` | 释放连接回连接池 |
| `连接池关闭(pool)` | 关闭连接池 |
| `连接池获取统计(pool)` | 获取连接池统计信息 |
| `连接池设置最小连接数(pool, min_size)` | 设置连接池最小连接数 |
| `__init__(self, factory, min_size, max_size)` |  |
| `_create_conn(self)` | 创建新连接 |

---

## 函数详情

### `创建连接池(factory, min_size = 1, max_size = 10)`

创建连接池

Args:
    factory: 创建连接的可调用对象
    min_size: 最小连接数
    max_size: 最大连接数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `factory` | `None` |  |
| `min_size` | `None` | （默认：1） |
| `max_size` | `None` | （默认：10） |

---

### `连接池获取连接(pool, timeout = None)`

从连接池获取连接

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pool` | `None` |  |
| `timeout` | `None` | （默认：None） |

---

### `连接池释放连接(pool, conn)`

释放连接回连接池

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pool` | `None` |  |
| `conn` | `None` |  |

---

### `连接池关闭(pool)`

关闭连接池

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pool` | `None` |  |

---

### `连接池获取统计(pool)`

获取连接池统计信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pool` | `None` |  |

---

### `连接池设置最小连接数(pool, min_size)`

设置连接池最小连接数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `pool` | `None` |  |
| `min_size` | `None` |  |

---

### `__init__(self, factory, min_size = 1, max_size = 10)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `factory` | `None` |  |
| `min_size` | `None` | （默认：1） |
| `max_size` | `None` | （默认：10） |

---

### `_create_conn(self)`

创建新连接

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `current` | `pool._pool.qsize() + pool._active` |
| `conn` | `self._factory()` |
| `conn` | `pool._pool.get(timeout=timeout)` |
| `conn` | `factory()` |
| `conn` | `pool._pool.get_nowait()` |
| `conn` | `pool._create_conn()` |
| `conn` | `pool._create_conn()` |
