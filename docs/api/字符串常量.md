# 字符串常量 API

> 模块路径：`stdlib/字符串常量.py`
> 导入方式：`从 字符串常量 导入 函数名` 或 `导入 字符串常量`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `是字母(字符)` | 检查字符是否为字母 |
| `是数字(字符)` | 检查字符是否为数字 |
| `是字母数字(字符)` | 检查字符是否为字母或数字 |
| `是小写(字符)` | 检查字符是否为小写字母 |
| `是大写(字符)` | 检查字符是否为大写字母 |
| `是空白(字符)` | 检查字符是否为空白字符 |
| `是可打印(字符)` | 检查字符是否为可打印字符 |
| `是标点(字符)` | 检查字符是否为标点符号 |
| `是十六进制(字符)` | 检查字符是否为十六进制数字 |
| `是八进制(字符)` | 检查字符是否为八进制数字 |
| `字符列表全部是(文本, 检测函数)` | 检查字符串中所有字符是否都满足条件 |
| `字符列表有一个是(文本, 检测函数)` | 检查字符串中是否至少有一个字符满足条件 |
| `首字母大写(文本)` | 首字母大写 |
| `标题大小写(文本)` | 标题大小写（每个单词首字母大写） |
| `全大写(文本)` | 转换为全大写 |
| `全小写(文本)` | 转换为全小写 |
| `交换大小写(文本)` | 交换大小写 |
| `去除两端空白(文本, 字符)` | 去除两端空白或指定字符 |
| `去除左端空白(文本, 字符)` | 去除左端空白或指定字符 |
| `去除右端空白(文本, 字符)` | 去除右端空白或指定字符 |
| `居中填充(文本, 宽度, 填充字符)` | 居中填充 |
| `左对齐(文本, 宽度, 填充字符)` | 左对齐填充 |
| `右对齐(文本, 宽度, 填充字符)` | 右对齐填充 |
| `补零(文本, 宽度)` | 左侧补零 |
| `连接(分隔符, 字符串列表)` | 用分隔符连接字符串列表 |
| `分割(文本, 分隔符, 最大分割次数)` | 分割字符串 |
| `右分割(文本, 分隔符, 最大分割次数)` | 从右侧分割字符串 |
| `按行分割(文本, 保留换行符)` | 按行分割 |
| `替换(文本, 旧子串, 新子串, 替换次数)` | 替换子串 |
| `包含(文本, 子串)` | 检查是否包含子串 |
| `以开头(文本, 前缀)` | 检查是否以前缀开头 |
| `以结尾(文本, 后缀)` | 检查是否以后缀结尾 |
| `查找(文本, 子串, 起始, 结束)` | 查找子串位置，未找到返回-1 |
| `反向查找(文本, 子串, 起始, 结束)` | 从右侧查找子串位置 |
| `计数(文本, 子串, 起始, 结束)` | 统计子串出现次数 |

---

## 函数详情

### `是字母(字符: str) -> bool`

检查字符是否为字母

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是数字(字符: str) -> bool`

检查字符是否为数字

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是字母数字(字符: str) -> bool`

检查字符是否为字母或数字

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是小写(字符: str) -> bool`

检查字符是否为小写字母

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是大写(字符: str) -> bool`

检查字符是否为大写字母

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是空白(字符: str) -> bool`

检查字符是否为空白字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是可打印(字符: str) -> bool`

检查字符是否为可打印字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是标点(字符: str) -> bool`

检查字符是否为标点符号

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是十六进制(字符: str) -> bool`

检查字符是否为十六进制数字

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `是八进制(字符: str) -> bool`

检查字符是否为八进制数字

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字符` | `str` |  |

**返回：** `bool`

---

### `字符列表全部是(文本: str, 检测函数) -> bool`

检查字符串中所有字符是否都满足条件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `检测函数` | `None` |  |

**返回：** `bool`

---

### `字符列表有一个是(文本: str, 检测函数) -> bool`

检查字符串中是否至少有一个字符满足条件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `检测函数` | `None` |  |

**返回：** `bool`

---

### `首字母大写(文本: str) -> str`

首字母大写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `标题大小写(文本: str) -> str`

标题大小写（每个单词首字母大写）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `全大写(文本: str) -> str`

转换为全大写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `全小写(文本: str) -> str`

转换为全小写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `交换大小写(文本: str) -> str`

交换大小写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |

**返回：** `str`

---

### `去除两端空白(文本: str, 字符: str = None) -> str`

去除两端空白或指定字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `字符` | `str` | （默认：None） |

**返回：** `str`

---

### `去除左端空白(文本: str, 字符: str = None) -> str`

去除左端空白或指定字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `字符` | `str` | （默认：None） |

**返回：** `str`

---

### `去除右端空白(文本: str, 字符: str = None) -> str`

去除右端空白或指定字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `字符` | `str` | （默认：None） |

**返回：** `str`

---

### `居中填充(文本: str, 宽度: int, 填充字符: str = ' ') -> str`

居中填充

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `宽度` | `int` |  |
| `填充字符` | `str` | （默认：' '） |

**返回：** `str`

---

### `左对齐(文本: str, 宽度: int, 填充字符: str = ' ') -> str`

左对齐填充

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `宽度` | `int` |  |
| `填充字符` | `str` | （默认：' '） |

**返回：** `str`

---

### `右对齐(文本: str, 宽度: int, 填充字符: str = ' ') -> str`

右对齐填充

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `宽度` | `int` |  |
| `填充字符` | `str` | （默认：' '） |

**返回：** `str`

---

### `补零(文本: str, 宽度: int) -> str`

左侧补零

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `宽度` | `int` |  |

**返回：** `str`

---

### `连接(分隔符: str, 字符串列表: List[str]) -> str`

用分隔符连接字符串列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `分隔符` | `str` |  |
| `字符串列表` | `List[str]` |  |

**返回：** `str`

---

### `分割(文本: str, 分隔符: str = None, 最大分割次数: int = -1) -> List[str]`

分割字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `分隔符` | `str` | （默认：None） |
| `最大分割次数` | `int` | （默认：-1） |

**返回：** `List[str]`

---

### `右分割(文本: str, 分隔符: str, 最大分割次数: int = -1) -> List[str]`

从右侧分割字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `分隔符` | `str` |  |
| `最大分割次数` | `int` | （默认：-1） |

**返回：** `List[str]`

---

### `按行分割(文本: str, 保留换行符: bool = False) -> List[str]`

按行分割

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `保留换行符` | `bool` | （默认：False） |

**返回：** `List[str]`

---

### `替换(文本: str, 旧子串: str, 新子串: str, 替换次数: int = -1) -> str`

替换子串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `旧子串` | `str` |  |
| `新子串` | `str` |  |
| `替换次数` | `int` | （默认：-1） |

**返回：** `str`

---

### `包含(文本: str, 子串: str) -> bool`

检查是否包含子串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `子串` | `str` |  |

**返回：** `bool`

---

### `以开头(文本: str, 前缀: str) -> bool`

检查是否以前缀开头

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `前缀` | `str` |  |

**返回：** `bool`

---

### `以结尾(文本: str, 后缀: str) -> bool`

检查是否以后缀结尾

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `后缀` | `str` |  |

**返回：** `bool`

---

### `查找(文本: str, 子串: str, 起始: int = 0, 结束: int = -1) -> int`

查找子串位置，未找到返回-1

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `子串` | `str` |  |
| `起始` | `int` | （默认：0） |
| `结束` | `int` | （默认：-1） |

**返回：** `int`

---

### `反向查找(文本: str, 子串: str, 起始: int = 0, 结束: int = -1) -> int`

从右侧查找子串位置

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `子串` | `str` |  |
| `起始` | `int` | （默认：0） |
| `结束` | `int` | （默认：-1） |

**返回：** `int`

---

### `计数(文本: str, 子串: str, 起始: int = 0, 结束: int = -1) -> int`

统计子串出现次数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `str` |  |
| `子串` | `str` |  |
| `起始` | `int` | （默认：0） |
| `结束` | `int` | （默认：-1） |

**返回：** `int`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `小写字母` | `string.ascii_lowercase` |
| `大写字母` | `string.ascii_uppercase` |
| `字母` | `string.ascii_letters` |
| `数字` | `string.digits` |
| `十六进制数字` | `string.hexdigits` |
| `八进制数字` | `string.octdigits` |
| `标点符号` | `string.punctuation` |
| `可打印字符` | `string.printable` |
| `空白字符` | `string.whitespace` |
| `换行符` | `'\n'` |
| `制表符` | `'\t'` |
| `回车符` | `'\r'` |
| `空字符串` | `''` |
