# 数学运算 API

> 模块路径：`stdlib/数学.py`
> 导入方式：`从 数学 导入 函数名` 或 `导入 数学`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `绝对值(x)` | 绝对值 |
| `最大值(甲, 乙)` | 最大值 |
| `最小值(甲, 乙)` | 最小值 |
| `幂(底数, 指数)` | 幂运算：底数 ^ 指数 |
| `平方根(x)` | 平方根 |
| `正弦(x)` | 正弦（弧度） |
| `余弦(x)` | 余弦（弧度） |
| `正切(x)` | 正切（弧度） |
| `弧度转角度(弧度)` | 弧度转角度 |
| `角度转弧度(角度)` | 角度转弧度 |
| `向上取整(x)` | 向上取整 |
| `向下取整(x)` | 向下取整 |
| `四舍五入(x, 小数位数)` | 四舍五入 |
| `随机整数(最小值, 最大值)` | 随机整数 [最小值, 最大值] |
| `随机浮点()` | 随机浮点数 [0.0, 1.0) |
| `随机选择(列表)` | 从列表中随机选择一个元素 |
| `圆周率()` | 圆周率 π |
| `自然常数()` | 自然常数 e |
| `阶乘(n)` | 阶乘 n! |
| `对数(x, 底数)` | 对数：log_底数(x) |
| `自然对数(x)` | 自然对数 ln(x) |
| `常用对数(x)` | 常用对数 log10(x) |
| `对数2(x)` | 以2为底的对数 log2(x) |
| `立方根(x)` | 立方根 |
| `指数(x)` | 指数函数 e^x |
| `双曲正弦(x)` | 双曲正弦 sinh(x) |
| `双曲余弦(x)` | 双曲余弦 cosh(x) |
| `双曲正切(x)` | 双曲正切 tanh(x) |
| `反双曲正弦(x)` | 反双曲正弦 asinh(x) |
| `反双曲余弦(x)` | 反双曲余弦 acosh(x) |
| `反双曲正切(x)` | 反双曲正切 atanh(x) |
| `反正弦(x)` | 反正弦 arcsin(x)（弧度） |
| `反余弦(x)` | 反余弦 arccos(x)（弧度） |
| `反正切(x)` | 反正切 arctan(x)（弧度） |
| `反正切2(y, x)` | 反正切2 atan2(y, x)（弧度） |
| `余切(x)` | 余切 cot(x) |
| `正割(x)` | 正割 sec(x) |
| `余割(x)` | 余割 csc(x) |
| `双曲余切(x)` | 双曲余切 coth(x) |
| `双曲正割(x)` | 双曲正割 sech(x) |
| `双曲余割(x)` | 双曲余割 csch(x) |
| `弧度(x)` | 角度转弧度（别名） |
| `角度(x)` | 弧度转角度（别名） |
| `双阶乘(n)` | 双阶乘 n!! |
| `排列(n, k)` | 排列数 P(n, k) |
| `组合(n, k)` | 组合数 C(n, k) |
| `二项式系数(n, k)` | 二项式系数（组合数别名） |
| `欧拉常数()` | 欧拉常数 γ ≈ 0.5772 |
| `黄金比例()` | 黄金比例 φ ≈ 1.618 |
| `弧度π()` | π 弧度（180度） |
| `弧度π2()` | π/2 弧度（90度） |
| `弧度π4()` | π/4 弧度（45度） |
| `弧度2π()` | 2π 弧度（360度） |
| `度分秒转弧度(度, 分, 秒)` | 度分秒转弧度 |
| `弧度转度分秒(弧度)` | 弧度转度分秒 |
| `复数实部(z)` | 复数实部 |
| `复数虚部(z)` | 复数虚部 |
| `复数模(z)` | 复数模（绝对值） |
| `复数辐角(z)` | 复数辐角（弧度） |
| `复数共轭(z)` | 复数共轭 |
| `复数极坐标转直角(模, 辐角)` | 极坐标转直角坐标 |
| `复数直角转极坐标(z)` | 直角坐标转极坐标 (模, 辐角) |
| `复数指数(z)` | 复数指数函数 e^z |
| `复数对数(z)` | 复数对数 |
| `复数平方根(z)` | 复数平方根 |
| `复数正弦(z)` | 复数正弦 |
| `复数余弦(z)` | 复数余弦 |
| `复数正切(z)` | 复数正切 |
| `双曲正弦h(x)` | 双曲正弦（简写） |
| `双曲余弦h(x)` | 双曲余弦（简写） |
| `双曲正切h(x)` | 双曲正切（简写） |
| `随机范围(最小值, 最大值)` | 生成指定范围内的随机浮点数 |
| `随机打乱(列表)` | 随机打乱列表（原地修改） |
| `随机样本(列表, k)` | 从列表中随机抽取k个不重复元素 |
| `设置随机种子(种子)` | 设置随机数种子 |
| `正态随机(均值, 标准差)` | 生成正态分布随机数 |
| `泊松随机(均值)` | 生成泊松分布随机数 |
| `指数随机(均值)` | 生成指数分布随机数 |
| `平均数(数据)` | 算术平均数 |
| `中位数(数据)` | 中位数 |
| `众数(数据)` | 众数（出现最多的值） |
| `标准差(数据)` | 总体标准差 |
| `样本标准差(数据)` | 样本标准差（自由度 n-1） |
| `方差(数据)` | 总体方差 |
| `样本方差(数据)` | 样本方差（自由度 n-1） |
| `范围(数据)` | 范围（最大值 - 最小值） |
| `求和(数据)` | 求和 |
| `累积和(数据)` | 累积和 |
| `线性回归(x数据, y数据)` | 线性回归（斜率、截距、相关系数） |
| `平方(x)` | 计算平方 |
| `是奇数(n)` | 判断是否为奇数 |
| `是偶数(n)` | 判断是否为偶数 |
| `是素数(n)` | 判断是否为素数 |
| `最大公约数(甲, 乙)` | 计算最大公约数 |
| `最小公倍数(甲, 乙)` | 计算最小公倍数 |
| `累加(n)` | 计算1到n的累加和 |

---

## 函数详情

### `绝对值(x: Number) -> Number`

绝对值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `Number`

---

### `最大值(甲: Number, 乙: Number) -> Number`

最大值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `甲` | `Number` |  |
| `乙` | `Number` |  |

**返回：** `Number`

---

### `最小值(甲: Number, 乙: Number) -> Number`

最小值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `甲` | `Number` |  |
| `乙` | `Number` |  |

**返回：** `Number`

---

### `幂(底数: Number, 指数: Number) -> float`

幂运算：底数 ^ 指数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `底数` | `Number` |  |
| `指数` | `Number` |  |

**返回：** `float`

---

### `平方根(x: Number) -> float`

平方根

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `正弦(x: Number) -> float`

正弦（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `余弦(x: Number) -> float`

余弦（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `正切(x: Number) -> float`

正切（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `弧度转角度(弧度: Number) -> float`

弧度转角度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `弧度` | `Number` |  |

**返回：** `float`

---

### `角度转弧度(角度: Number) -> float`

角度转弧度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `角度` | `Number` |  |

**返回：** `float`

---

### `向上取整(x: Number) -> int`

向上取整

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `int`

---

### `向下取整(x: Number) -> int`

向下取整

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `int`

---

### `四舍五入(x: Number, 小数位数: int = 0) -> float`

四舍五入

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |
| `小数位数` | `int` | （默认：0） |

**返回：** `float`

---

### `随机整数(最小值: int, 最大值: int) -> int`

随机整数 [最小值, 最大值]

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `最小值` | `int` |  |
| `最大值` | `int` |  |

**返回：** `int`

---

### `随机浮点() -> float`

随机浮点数 [0.0, 1.0)

**参数：**

无参数。

**返回：** `float`

---

### `随机选择(列表: List) -> object`

从列表中随机选择一个元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `List` |  |

**返回：** `object`

---

### `圆周率() -> float`

圆周率 π

**参数：**

无参数。

**返回：** `float`

---

### `自然常数() -> float`

自然常数 e

**参数：**

无参数。

**返回：** `float`

---

### `阶乘(n: int) -> int`

阶乘 n!

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |

**返回：** `int`

---

### `对数(x: Number, 底数: Number = math.e) -> float`

对数：log_底数(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |
| `底数` | `Number` | （默认：math.e） |

**返回：** `float`

---

### `自然对数(x: Number) -> float`

自然对数 ln(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `常用对数(x: Number) -> float`

常用对数 log10(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `对数2(x: Number) -> float`

以2为底的对数 log2(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `立方根(x: Number) -> float`

立方根

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `指数(x: Number) -> float`

指数函数 e^x

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲正弦(x: Number) -> float`

双曲正弦 sinh(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲余弦(x: Number) -> float`

双曲余弦 cosh(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲正切(x: Number) -> float`

双曲正切 tanh(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `反双曲正弦(x: Number) -> float`

反双曲正弦 asinh(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `反双曲余弦(x: Number) -> float`

反双曲余弦 acosh(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `反双曲正切(x: Number) -> float`

反双曲正切 atanh(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `反正弦(x: Number) -> float`

反正弦 arcsin(x)（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `反余弦(x: Number) -> float`

反余弦 arccos(x)（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `反正切(x: Number) -> float`

反正切 arctan(x)（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `反正切2(y: Number, x: Number) -> float`

反正切2 atan2(y, x)（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `y` | `Number` |  |
| `x` | `Number` |  |

**返回：** `float`

---

### `余切(x: Number) -> float`

余切 cot(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `正割(x: Number) -> float`

正割 sec(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `余割(x: Number) -> float`

余割 csc(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲余切(x: Number) -> float`

双曲余切 coth(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲正割(x: Number) -> float`

双曲正割 sech(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲余割(x: Number) -> float`

双曲余割 csch(x)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `弧度(x: Number) -> float`

角度转弧度（别名）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `角度(x: Number) -> float`

弧度转角度（别名）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双阶乘(n: int) -> int`

双阶乘 n!!

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |

**返回：** `int`

---

### `排列(n: int, k: int) -> int`

排列数 P(n, k)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |
| `k` | `int` |  |

**返回：** `int`

---

### `组合(n: int, k: int) -> int`

组合数 C(n, k)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |
| `k` | `int` |  |

**返回：** `int`

---

### `二项式系数(n: int, k: int) -> int`

二项式系数（组合数别名）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |
| `k` | `int` |  |

**返回：** `int`

---

### `欧拉常数() -> float`

欧拉常数 γ ≈ 0.5772

**参数：**

无参数。

**返回：** `float`

---

### `黄金比例() -> float`

黄金比例 φ ≈ 1.618

**参数：**

无参数。

**返回：** `float`

---

### `弧度π() -> float`

π 弧度（180度）

**参数：**

无参数。

**返回：** `float`

---

### `弧度π2() -> float`

π/2 弧度（90度）

**参数：**

无参数。

**返回：** `float`

---

### `弧度π4() -> float`

π/4 弧度（45度）

**参数：**

无参数。

**返回：** `float`

---

### `弧度2π() -> float`

2π 弧度（360度）

**参数：**

无参数。

**返回：** `float`

---

### `度分秒转弧度(度: int, 分: int = 0, 秒: float = 0) -> float`

度分秒转弧度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `度` | `int` |  |
| `分` | `int` | （默认：0） |
| `秒` | `float` | （默认：0） |

**返回：** `float`

---

### `弧度转度分秒(弧度: float) -> tuple`

弧度转度分秒

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `弧度` | `float` |  |

**返回：** `tuple`

---

### `复数实部(z: complex) -> float`

复数实部

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `float`

---

### `复数虚部(z: complex) -> float`

复数虚部

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `float`

---

### `复数模(z: complex) -> float`

复数模（绝对值）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `float`

---

### `复数辐角(z: complex) -> float`

复数辐角（弧度）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `float`

---

### `复数共轭(z: complex) -> complex`

复数共轭

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `complex`

---

### `复数极坐标转直角(模: float, 辐角: float) -> complex`

极坐标转直角坐标

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模` | `float` |  |
| `辐角` | `float` |  |

**返回：** `complex`

---

### `复数直角转极坐标(z: complex) -> tuple`

直角坐标转极坐标 (模, 辐角)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `tuple`

---

### `复数指数(z: complex) -> complex`

复数指数函数 e^z

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `complex`

---

### `复数对数(z: complex) -> complex`

复数对数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `complex`

---

### `复数平方根(z: complex) -> complex`

复数平方根

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `complex`

---

### `复数正弦(z: complex) -> complex`

复数正弦

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `complex`

---

### `复数余弦(z: complex) -> complex`

复数余弦

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `complex`

---

### `复数正切(z: complex) -> complex`

复数正切

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `z` | `complex` |  |

**返回：** `complex`

---

### `双曲正弦h(x: Number) -> float`

双曲正弦（简写）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲余弦h(x: Number) -> float`

双曲余弦（简写）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `双曲正切h(x: Number) -> float`

双曲正切（简写）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `float`

---

### `随机范围(最小值: float, 最大值: float) -> float`

生成指定范围内的随机浮点数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `最小值` | `float` |  |
| `最大值` | `float` |  |

**返回：** `float`

---

### `随机打乱(列表: List) -> None`

随机打乱列表（原地修改）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `List` |  |

**返回：** `None`

---

### `随机样本(列表: List, k: int) -> List`

从列表中随机抽取k个不重复元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `List` |  |
| `k` | `int` |  |

**返回：** `List`

---

### `设置随机种子(种子: int = None) -> None`

设置随机数种子

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `种子` | `int` | （默认：None） |

**返回：** `None`

---

### `正态随机(均值: float = 0.0, 标准差: float = 1.0) -> float`

生成正态分布随机数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `均值` | `float` | （默认：0.0） |
| `标准差` | `float` | （默认：1.0） |

**返回：** `float`

---

### `泊松随机(均值: float) -> int`

生成泊松分布随机数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `均值` | `float` |  |

**返回：** `int`

---

### `指数随机(均值: float) -> float`

生成指数分布随机数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `均值` | `float` |  |

**返回：** `float`

---

### `平均数(数据: List[Number]) -> float`

算术平均数

参数:
    数据: 数字列表

返回:
    平均数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `float`

---

### `中位数(数据: List[Number]) -> float`

中位数

参数:
    数据: 数字列表

返回:
    中位数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `float`

---

### `众数(数据: List[Number]) -> Number`

众数（出现最多的值）

参数:
    数据: 数字列表

返回:
    众数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `Number`

---

### `标准差(数据: List[Number]) -> float`

总体标准差

参数:
    数据: 数字列表

返回:
    标准差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `float`

---

### `样本标准差(数据: List[Number]) -> float`

样本标准差（自由度 n-1）

参数:
    数据: 样本数据列表

返回:
    样本标准差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `float`

---

### `方差(数据: List[Number]) -> float`

总体方差

参数:
    数据: 数字列表

返回:
    方差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `float`

---

### `样本方差(数据: List[Number]) -> float`

样本方差（自由度 n-1）

参数:
    数据: 样本数据列表

返回:
    样本方差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `float`

---

### `范围(数据: List[Number]) -> float`

范围（最大值 - 最小值）

参数:
    数据: 数字列表

返回:
    范围值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `float`

---

### `求和(数据: List[Number]) -> Number`

求和

参数:
    数据: 数字列表

返回:
    总和

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `Number`

---

### `累积和(数据: List[Number]) -> List[Number]`

累积和

参数:
    数据: 数字列表

返回:
    累积和列表（每个元素为到该位置为止的和）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `List[Number]` |  |

**返回：** `List[Number]`

---

### `线性回归(x数据: List[Number], y数据: List[Number]) -> dict`

线性回归（斜率、截距、相关系数）

参数:
    x数据: X 轴数据列表
    y数据: Y 轴数据列表

返回:
    {'斜率': slope, '截距': intercept, '相关系数': r}

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x数据` | `List[Number]` |  |
| `y数据` | `List[Number]` |  |

**返回：** `dict`

---

### `平方(x: Number) -> Number`

计算平方

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `x` | `Number` |  |

**返回：** `Number`

---

### `是奇数(n: int) -> bool`

判断是否为奇数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |

**返回：** `bool`

---

### `是偶数(n: int) -> bool`

判断是否为偶数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |

**返回：** `bool`

---

### `是素数(n: int) -> bool`

判断是否为素数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |

**返回：** `bool`

---

### `最大公约数(甲: int, 乙: int) -> int`

计算最大公约数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `甲` | `int` |  |
| `乙` | `int` |  |

**返回：** `int`

---

### `最小公倍数(甲: int, 乙: int) -> int`

计算最小公倍数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `甲` | `int` |  |
| `乙` | `int` |  |

**返回：** `int`

---

### `累加(n: int) -> int`

计算1到n的累加和

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |

**返回：** `int`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `Number` | `Union[int, float]` |
| `result` | `1` |
| `i` | `n` |
| `result` | `1` |
| `result` | `1` |
| `度` | `math.degrees(弧度)` |
| `整数度` | `int(度)` |
| `分` | `(度 - 整数度) * 60` |
| `整数分` | `int(分)` |
| `秒` | `(分 - 整数分) * 60` |
| `result` | `[]` |
| `total` | `0` |
| `n` | `len(x数据)` |
| `i` | `3` |
| `k` | `n - k` |
| `result` | `result * (n - i) // (i + 1)` |
| `r` | `1.0` |
| `r` | `sum(((x数据[i] - mx) * (y数据[i] - my) for i in range(n))) / (n * sx * sy)` |
