# 协程 API

> 模块路径：`stdlib/协程.py`
> 导入方式：`从 协程 导入 函数名` 或 `导入 协程`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `createScheduler()` | 创建协程调度器 |
| `schedulerCreateCoro(scheduler, coro_func)` | 在调度器中创建协程 |
| `schedulerStartCoro(scheduler, coro_id)` | 启动协程 |
| `schedulerSuspendCoro(scheduler, coro_id)` | 挂起协程 |
| `schedulerResumeCoro(scheduler, coro_id)` | 恢复协程 |
| `schedulerCancelCoro(scheduler, coro_id)` | 取消协程 |
| `schedulerRunStep(scheduler)` | 运行协程调度器一步 |
| `schedulerRunAll(scheduler)` | 运行所有协程直到完成 |
| `schedulerRun(scheduler)` | 运行调度器 |
| `schedulerStop(scheduler)` | 停止调度器 |
| `schedulerGetCoro(scheduler, coro_id)` | 获取协程信息 |
| `schedulerGetAllCoros(scheduler)` | 获取所有协程 |
| `schedulerGetStats(scheduler)` | 获取调度器统计 |
| `schedulerWaitCoro(scheduler, coro_id)` | 等待单个协程完成 |
| `schedulerWaitAll(scheduler)` | 等待所有协程完成 |
| `schedulerWaitAny(scheduler)` | 等待任意协程完成 |
| `schedulerYield(scheduler)` | 让出协程执行权 |
| `createChannel()` | 创建无缓冲通道 |
| `createBufferedChannel(capacity)` | 创建有缓冲通道 |
| `channelSend(ch, value)` | 向通道发送数据 |
| `channelRecv(ch)` | 从通道接收数据 |
| `channelTrySend(ch, value)` | 尝试向通道发送数据（非阻塞） |
| `channelTryRecv(ch)` | 尝试从通道接收数据（非阻塞） |
| `channelClose(ch)` | 关闭通道 |
| `channelIsClosed(ch)` | 检查通道是否已关闭 |
| `channelLen(ch)` | 获取通道当前长度 |
| `channelCap(ch)` | 获取通道容量 |
| `channelClear(ch)` | 清空通道 |
| `createSelector()` | 创建选择器 |
| `selectorAddSend(sel, ch, value)` | 添加发送选择分支 |
| `selectorAddRecv(sel, ch)` | 添加接收选择分支 |
| `selectorSetDefault(sel, callback)` | 设置默认选择分支 |
| `selectorSelect(sel)` | 执行选择，返回第一个就绪的通道操作结果 |
| `createGenerator(gen_func)` | 创建生成器 |
| `generatorNext(gen)` | 获取生成器下一个值 |
| `generatorCollect(gen)` | 收集生成器所有值 |
| `generatorReset(gen)` | 重置生成器 |
| `__init__(self)` |  |
| `_next_id(self)` |  |
| `__init__(self, capacity)` |  |
| `__init__(self)` |  |
| `__init__(self, gen_func)` |  |
| `next(self)` |  |

---

## 函数详情

### `createScheduler()`

创建协程调度器

**参数：**

无参数。

---

### `schedulerCreateCoro(scheduler, coro_func)`

在调度器中创建协程

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |
| `coro_func` | `None` |  |

---

### `schedulerStartCoro(scheduler, coro_id)`

启动协程

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |
| `coro_id` | `None` |  |

---

### `schedulerSuspendCoro(scheduler, coro_id)`

挂起协程

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |
| `coro_id` | `None` |  |

---

### `schedulerResumeCoro(scheduler, coro_id)`

恢复协程

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |
| `coro_id` | `None` |  |

---

### `schedulerCancelCoro(scheduler, coro_id)`

取消协程

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |
| `coro_id` | `None` |  |

---

### `schedulerRunStep(scheduler)`

运行协程调度器一步

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerRunAll(scheduler)`

运行所有协程直到完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerRun(scheduler)`

运行调度器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerStop(scheduler)`

停止调度器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerGetCoro(scheduler, coro_id)`

获取协程信息

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |
| `coro_id` | `None` |  |

---

### `schedulerGetAllCoros(scheduler)`

获取所有协程

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerGetStats(scheduler)`

获取调度器统计

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerWaitCoro(scheduler, coro_id)`

等待单个协程完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |
| `coro_id` | `None` |  |

---

### `schedulerWaitAll(scheduler)`

等待所有协程完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerWaitAny(scheduler)`

等待任意协程完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `schedulerYield(scheduler)`

让出协程执行权

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `scheduler` | `None` |  |

---

### `createChannel()`

创建无缓冲通道

**参数：**

无参数。

---

### `createBufferedChannel(capacity)`

创建有缓冲通道

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `capacity` | `None` |  |

---

### `channelSend(ch, value)`

向通道发送数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |
| `value` | `None` |  |

---

### `channelRecv(ch)`

从通道接收数据

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |

---

### `channelTrySend(ch, value)`

尝试向通道发送数据（非阻塞）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |
| `value` | `None` |  |

---

### `channelTryRecv(ch)`

尝试从通道接收数据（非阻塞）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |

---

### `channelClose(ch)`

关闭通道

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |

---

### `channelIsClosed(ch)`

检查通道是否已关闭

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |

---

### `channelLen(ch)`

获取通道当前长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |

---

### `channelCap(ch)`

获取通道容量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |

---

### `channelClear(ch)`

清空通道

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `ch` | `None` |  |

---

### `createSelector()`

创建选择器

**参数：**

无参数。

---

### `selectorAddSend(sel, ch, value)`

添加发送选择分支

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `sel` | `None` |  |
| `ch` | `None` |  |
| `value` | `None` |  |

---

### `selectorAddRecv(sel, ch)`

添加接收选择分支

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `sel` | `None` |  |
| `ch` | `None` |  |

---

### `selectorSetDefault(sel, callback)`

设置默认选择分支

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `sel` | `None` |  |
| `callback` | `None` |  |

---

### `selectorSelect(sel)`

执行选择，返回第一个就绪的通道操作结果

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `sel` | `None` |  |

---

### `createGenerator(gen_func)`

创建生成器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `gen_func` | `None` |  |

---

### `generatorNext(gen)`

获取生成器下一个值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `gen` | `None` |  |

---

### `generatorCollect(gen)`

收集生成器所有值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `gen` | `None` |  |

---

### `generatorReset(gen)`

重置生成器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `gen` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `_next_id(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, capacity = 0)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `capacity` | `None` | （默认：0） |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, gen_func)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `gen_func` | `None` |  |

---

### `next(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `statuses` | `{}` |
| `coro_id` | `scheduler._next_id()` |
| `info` | `scheduler._coros[coro_id]` |
| `coro` | `info['func'](*info['args'])` |
| `info` | `scheduler._coros[coro_id]` |
| `info` | `scheduler._coros[coro_id]` |
| `info` | `scheduler._coros[coro_id]` |
| `tasks` | `[]` |
| `s` | `info['status']` |
| `info` | `scheduler._coros[coro_id]` |
| `loop` | `_asyncio.get_event_loop()` |
| `loop` | `_asyncio.get_event_loop()` |
| `value` | `next(gen._gen)` |
| `results` | `list(gen._gen)` |
| `task` | `scheduler._loop.create_task(coro)` |
| `val` | `ch._queue.get_nowait()` |
