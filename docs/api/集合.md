# 集合操作 API

> 模块路径：`stdlib/集合.py`
> 导入方式：`从 集合 导入 函数名` 或 `导入 集合`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建集合(可迭代对象)` | 创建集合 |
| `集合转列表(集合)` | 集合转列表 |
| `列表转集合(列表)` | 列表转集合 |
| `集合转元组(集合)` | 集合转元组 |
| `集合转字符串(集合, 分隔符)` | 集合转字符串 |
| `并集(集合1, 集合2)` | 计算两个集合的并集 |
| `交集(集合1, 集合2)` | 计算两个集合的交集 |
| `差集(集合1, 集合2)` | 计算集合1相对于集合2的差集 |
| `对称差(集合1, 集合2)` | 计算两个集合的对称差 |
| `多个并集()` | 计算多个集合的并集 |
| `多个交集()` | 计算多个集合的交集 |
| `是否子集(集合1, 集合2)` | 判断集合1是否是集合2的子集 |
| `是否真子集(集合1, 集合2)` | 判断集合1是否是集合2的真子集 |
| `是否超集(集合1, 集合2)` | 判断集合1是否是集合2的超集 |
| `是否真超集(集合1, 集合2)` | 判断集合1是否是集合2的真超集 |
| `是否不相交(集合1, 集合2)` | 判断两个集合是否不相交 |
| `添加元素(集合, 元素)` | 向集合添加元素 |
| `移除元素(集合, 元素)` | 从集合移除元素（不存在时报错） |
| `丢弃元素(集合, 元素)` | 从集合丢弃元素（不存在时不报错） |
| `弹出元素(集合)` | 从集合弹出一个元素 |
| `清空集合(集合)` | 清空集合 |
| `集合长度(集合)` | 获取集合长度 |
| `集合包含(集合, 元素)` | 判断集合是否包含元素 |
| `集合相等(集合1, 集合2)` | 判断两个集合是否相等 |
| `集合不等(集合1, 集合2)` | 判断两个集合是否不等 |
| `复制集合(集合)` | 复制集合 |
| `冻结集合(可迭代对象)` | 创建冻结集合 |
| `集合的幂集(集合)` | 计算集合的幂集 |
| `集合的笛卡尔积(集合1, 集合2)` | 计算两个集合的笛卡尔积 |
| `集合的补集(集合, 全集)` | 计算集合相对于全集的补集 |
| `集合差集的大小(集合1, 集合2)` | 计算差集的大小 |
| `集合交集的大小(集合1, 集合2)` | 计算交集的大小 |
| `集合并集的大小(集合1, 集合2)` | 计算并集的大小 |
| `杰卡德相似度(集合1, 集合2)` | 计算两个集合的杰卡德相似度 |
| `集合的唯一元素(列表)` | 从列表中获取唯一元素集合 |
| `集合的重复元素(列表)` | 从列表中获取重复元素集合 |

---

## 函数详情

### `创建集合(可迭代对象 = None)`

创建集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `可迭代对象` | `None` | （默认：None） |

---

### `集合转列表(集合: Set[Any])`

集合转列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |

---

### `列表转集合(列表: List[Any])`

列表转集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `List[Any]` |  |

---

### `集合转元组(集合: Set[Any])`

集合转元组

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |

---

### `集合转字符串(集合: Set[Any], 分隔符: str = ',')`

集合转字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |
| `分隔符` | `str` | （默认：','） |

---

### `并集(集合1: Set[Any], 集合2: Set[Any])`

计算两个集合的并集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `交集(集合1: Set[Any], 集合2: Set[Any])`

计算两个集合的交集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `差集(集合1: Set[Any], 集合2: Set[Any])`

计算集合1相对于集合2的差集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `对称差(集合1: Set[Any], 集合2: Set[Any])`

计算两个集合的对称差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `多个并集()`

计算多个集合的并集

**参数：**

无参数。

---

### `多个交集()`

计算多个集合的交集

**参数：**

无参数。

---

### `是否子集(集合1: Set[Any], 集合2: Set[Any])`

判断集合1是否是集合2的子集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `是否真子集(集合1: Set[Any], 集合2: Set[Any])`

判断集合1是否是集合2的真子集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `是否超集(集合1: Set[Any], 集合2: Set[Any])`

判断集合1是否是集合2的超集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `是否真超集(集合1: Set[Any], 集合2: Set[Any])`

判断集合1是否是集合2的真超集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `是否不相交(集合1: Set[Any], 集合2: Set[Any])`

判断两个集合是否不相交

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `添加元素(集合: Set[Any], 元素: Any)`

向集合添加元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |
| `元素` | `Any` |  |

---

### `移除元素(集合: Set[Any], 元素: Any)`

从集合移除元素（不存在时报错）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |
| `元素` | `Any` |  |

---

### `丢弃元素(集合: Set[Any], 元素: Any)`

从集合丢弃元素（不存在时不报错）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |
| `元素` | `Any` |  |

---

### `弹出元素(集合: Set[Any])`

从集合弹出一个元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |

---

### `清空集合(集合: Set[Any])`

清空集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |

---

### `集合长度(集合: Set[Any])`

获取集合长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |

---

### `集合包含(集合: Set[Any], 元素: Any)`

判断集合是否包含元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |
| `元素` | `Any` |  |

---

### `集合相等(集合1: Set[Any], 集合2: Set[Any])`

判断两个集合是否相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `集合不等(集合1: Set[Any], 集合2: Set[Any])`

判断两个集合是否不等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `复制集合(集合: Set[Any])`

复制集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |

---

### `冻结集合(可迭代对象 = None)`

创建冻结集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `可迭代对象` | `None` | （默认：None） |

---

### `集合的幂集(集合: Set[Any])`

计算集合的幂集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |

---

### `集合的笛卡尔积(集合1: Set[Any], 集合2: Set[Any])`

计算两个集合的笛卡尔积

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `集合的补集(集合: Set[Any], 全集: Set[Any])`

计算集合相对于全集的补集

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合` | `Set[Any]` |  |
| `全集` | `Set[Any]` |  |

---

### `集合差集的大小(集合1: Set[Any], 集合2: Set[Any])`

计算差集的大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `集合交集的大小(集合1: Set[Any], 集合2: Set[Any])`

计算交集的大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `集合并集的大小(集合1: Set[Any], 集合2: Set[Any])`

计算并集的大小

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `杰卡德相似度(集合1: Set[Any], 集合2: Set[Any])`

计算两个集合的杰卡德相似度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `集合1` | `Set[Any]` |  |
| `集合2` | `Set[Any]` |  |

---

### `集合的唯一元素(列表: List[Any])`

从列表中获取唯一元素集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `List[Any]` |  |

---

### `集合的重复元素(列表: List[Any])`

从列表中获取重复元素集合

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `List[Any]` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `结果` | `set(集合列表[0])` |
| `结果` | `set(集合列表[0])` |
| `元素列表` | `list(集合)` |
| `幂集` | `[set()]` |
| `交集大小` | `len(集合1 & 集合2)` |
| `并集大小` | `len(集合1 | 集合2)` |
| `出现次数` | `{}` |
