# 集合工具 API

> 模块路径：`stdlib/集合工具.py`
> 导入方式：`从 集合工具 导入 函数名` 或 `导入 集合工具`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建默认字典(默认工厂)` | 创建默认字典 |
| `创建有序字典(数据)` | 创建有序字典 |
| `创建计数器(可迭代对象)` | 创建计数器 |
| `创建双端队列(可迭代对象, 最大长度)` | 创建双端队列 |
| `命名元组(类型名, 字段名列表)` | 创建命名元组类型 |
| `链字典()` | 链字典（ChainMap） |
| `用户字典()` | 创建UserDict（可自定义字典行为） |
| `用户列表()` | 创建UserList（可自定义列表行为） |
| `用户字符串()` | 创建UserString（可自定义字符串行为） |
| `统计频率(可迭代对象)` | 统计频率（返回普通字典） |
| `可迭代对象(可迭代对象, n)` | 滑动窗口n-gram |
| `__init__(self, 默认工厂)` |  |
| `到普通字典(self)` | 转换为普通字典 |
| `移动到开头(self, 键, last)` | 移动元素到开头或末尾 |
| `弹出末尾(self, last)` | 弹出末尾或开头的元素 |
| `到普通字典(self)` | 转换为普通字典 |
| `最常见(self, n)` | 获取最常见的n个元素 |
| `元素列表(self)` | 返回所有元素（按计数次重复） |
| `更新计数(self, 可迭代对象)` | 更新计数 |
| `相减(self, 可迭代对象)` | 相减计数 |
| `到字典(self)` | 转换为普通字典 |
| `左入队(self)` | 从左侧添加元素 |
| `右入队(self)` | 从右侧添加元素 |
| `左出队(self)` | 从左侧弹出元素 |
| `右出队(self)` | 从右侧弹出元素 |
| `左扩展(self, 可迭代对象)` | 从左侧扩展 |
| `右扩展(self, 可迭代对象)` | 从右侧扩展 |
| `旋转(self, n)` | 旋转队列 |
| `转为列表(self)` | 转换为列表 |

---

## 函数详情

### `创建默认字典(默认工厂 = None) -> 默认字典`

创建默认字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `默认工厂` | `None` | （默认：None） |

**返回：** `默认字典`

---

### `创建有序字典(数据 = None) -> 有序字典`

创建有序字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `None` | （默认：None） |

**返回：** `有序字典`

---

### `创建计数器(可迭代对象 = None) -> 计数器`

创建计数器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `可迭代对象` | `None` | （默认：None） |

**返回：** `计数器`

---

### `创建双端队列(可迭代对象 = None, 最大长度 = None) -> 双端队列`

创建双端队列

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `可迭代对象` | `None` | （默认：None） |
| `最大长度` | `None` | （默认：None） |

**返回：** `双端队列`

---

### `命名元组(类型名: str, 字段名列表: list)`

创建命名元组类型

参数:
    类型名: 类型名称
    字段名列表: 字段名列表

返回:
    命名元组类

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `类型名` | `str` |  |
| `字段名列表` | `list` |  |

---

### `链字典()`

链字典（ChainMap）

将多个字典链接为一个视图

**参数：**

无参数。

---

### `用户字典()`

创建UserDict（可自定义字典行为）

**参数：**

无参数。

---

### `用户列表()`

创建UserList（可自定义列表行为）

**参数：**

无参数。

---

### `用户字符串()`

创建UserString（可自定义字符串行为）

**参数：**

无参数。

---

### `统计频率(可迭代对象: Iterable[Any]) -> Dict[Any, int]`

统计频率（返回普通字典）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `可迭代对象` | `Iterable[Any]` |  |

**返回：** `Dict[Any, int]`

---

### `可迭代对象(可迭代对象: Iterable[Any], n: int = 2) -> List[Tuple[Any, ...]]`

滑动窗口n-gram

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `可迭代对象` | `Iterable[Any]` |  |
| `n` | `int` | （默认：2） |

**返回：** `List[Tuple[Any, ...]]`

---

### `__init__(self, 默认工厂 = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `默认工厂` | `None` | （默认：None） |

---

### `到普通字典(self) -> dict`

转换为普通字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `dict`

---

### `移动到开头(self, 键, last = True)`

移动元素到开头或末尾

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `键` | `None` |  |
| `last` | `None` | （默认：True） |

---

### `弹出末尾(self, last = True)`

弹出末尾或开头的元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `last` | `None` | （默认：True） |

---

### `到普通字典(self) -> dict`

转换为普通字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `dict`

---

### `最常见(self, n = None)`

获取最常见的n个元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `n` | `None` | （默认：None） |

---

### `元素列表(self)`

返回所有元素（按计数次重复）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `更新计数(self, 可迭代对象 = None)`

更新计数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `可迭代对象` | `None` | （默认：None） |

---

### `相减(self, 可迭代对象 = None)`

相减计数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `可迭代对象` | `None` | （默认：None） |

---

### `到字典(self) -> dict`

转换为普通字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `dict`

---

### `左入队(self)`

从左侧添加元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `右入队(self)`

从右侧添加元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `左出队(self)`

从左侧弹出元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `右出队(self)`

从右侧弹出元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `左扩展(self, 可迭代对象)`

从左侧扩展

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `可迭代对象` | `None` |  |

---

### `右扩展(self, 可迭代对象)`

从右侧扩展

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `可迭代对象` | `None` |  |

---

### `旋转(self, n = 1)`

旋转队列

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `n` | `None` | （默认：1） |

---

### `转为列表(self) -> list`

转换为列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

**返回：** `list`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `d` | `deque(maxlen=n)` |
| `result` | `[]` |
