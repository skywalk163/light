# 单元测试框架 API

> 模块路径：`stdlib/单元测试框架.py`
> 导入方式：`从 单元测试框架 导入 函数名` 或 `导入 单元测试框架`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建测试套件()` | 创建测试套件 |
| `添加测试用例(套件, 测试用例)` | 向套件添加测试用例 |
| `运行测试套件(套件)` | 运行测试套件，返回 测试结果 对象 |
| `创建测试运行器(详细)` | 创建测试运行器 |
| `运行测试(测试用例类, 详细)` | 运行单个测试用例类中的所有测试方法，返回 测试结果 对象 |
| `运行所有测试(测试用例列表, 详细)` | 运行多个测试用例，返回 测试结果 对象 |
| `发现测试(路径, 模式)` | 自动发现测试，返回测试套件 |
| `创建Mock(规格, 名称)` | 创建 Mock 对象 |
| `修补(目标, 新值)` | 临时修补对象（上下文管理器） |
| `间谍(目标)` | 创建 Spy 对象，记录调用但不改变行为 |
| `参数化(参数列表)` | 参数化测试装饰器 |
| `基准测试(函数, 迭代次数)` | 运行基准测试，返回每次调用的平均耗时（秒） |
| `测量执行时间(函数)` | 测量函数执行时间（秒） |
| `__init__(self, 名称)` |  |
| `断言等于(self, 预期, 实际, 消息)` | 断言两个值相等 |
| `断言不等于(self, 预期, 实际, 消息)` | 断言两个值不相等 |
| `断言真(self, 表达式, 消息)` | 断言表达式为真 |
| `断言假(self, 表达式, 消息)` | 断言表达式为假 |
| `断言大于(self, a, b, 消息)` | 断言 a > b |
| `断言大于等于(self, a, b, 消息)` | 断言 a >= b |
| `断言小于(self, a, b, 消息)` | 断言 a < b |
| `断言小于等于(self, a, b, 消息)` | 断言 a <= b |
| `断言包含(self, 容器, 元素, 消息)` | 断言容器包含元素 |
| `断言不包含(self, 容器, 元素, 消息)` | 断言容器不包含元素 |
| `断言为None(self, 值, 消息)` | 断言值为 None |
| `断言不为None(self, 值, 消息)` | 断言值不为 None |
| `断言抛出(self, 异常类型, 可调用对象)` | 断言调用抛出指定异常 |
| `断言几乎相等(self, 预期, 实际, 位数, 消息)` | 断言浮点数近似相等 |
| `__init__(self)` |  |
| `是否全部通过(self)` | 检查是否所有测试通过 |
| `获取摘要(self)` | 获取测试结果摘要文本 |
| `获取报告(self)` | 获取完整测试报告 |
| `__init__(self, 目标, 新值)` |  |
| `__enter__(self)` |  |
| `__exit__(self, exc_type, exc_val, exc_tb)` |  |
| `__init__(self, 目标)` |  |
| `__getattr__(self, 名称)` |  |
| `装饰器(测试函数)` |  |
| `记录调用()` |  |
| `包装器(self)` |  |

---

## 函数详情

### `创建测试套件()`

创建测试套件

**参数：**

无参数。

---

### `添加测试用例(套件, 测试用例)`

向套件添加测试用例

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `套件` | `None` |  |
| `测试用例` | `None` |  |

---

### `运行测试套件(套件)`

运行测试套件，返回 测试结果 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `套件` | `None` |  |

---

### `创建测试运行器(详细 = True)`

创建测试运行器

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `详细` | `None` | （默认：True） |

---

### `运行测试(测试用例类, 详细 = True)`

运行单个测试用例类中的所有测试方法，返回 测试结果 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `测试用例类` | `None` |  |
| `详细` | `None` | （默认：True） |

---

### `运行所有测试(测试用例列表, 详细 = True)`

运行多个测试用例，返回 测试结果 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `测试用例列表` | `None` |  |
| `详细` | `None` | （默认：True） |

---

### `发现测试(路径 = '.', 模式 = '*测试*.py')`

自动发现测试，返回测试套件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` | （默认：'.'） |
| `模式` | `None` | （默认：'*测试*.py'） |

---

### `创建Mock(规格 = None, 名称 = None)`

创建 Mock 对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `规格` | `None` | （默认：None） |
| `名称` | `None` | （默认：None） |

---

### `修补(目标, 新值 = None)`

临时修补对象（上下文管理器）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `目标` | `None` |  |
| `新值` | `None` | （默认：None） |

---

### `间谍(目标)`

创建 Spy 对象，记录调用但不改变行为

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `目标` | `None` |  |

---

### `参数化(参数列表)`

参数化测试装饰器

用法:
    @参数化([(1, 2), (3, 4)])
    def 测试加法(self, a, b):
        ...

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `参数列表` | `None` |  |

---

### `基准测试(函数, 迭代次数 = 1000)`

运行基准测试，返回每次调用的平均耗时（秒）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `None` |  |
| `迭代次数` | `None` | （默认：1000） |

---

### `测量执行时间(函数)`

测量函数执行时间（秒）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `函数` | `None` |  |

---

### `__init__(self, 名称 = '')`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` | （默认：''） |

---

### `断言等于(self, 预期, 实际, 消息 = '')`

断言两个值相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `预期` | `None` |  |
| `实际` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言不等于(self, 预期, 实际, 消息 = '')`

断言两个值不相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `预期` | `None` |  |
| `实际` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言真(self, 表达式, 消息 = '')`

断言表达式为真

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `表达式` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言假(self, 表达式, 消息 = '')`

断言表达式为假

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `表达式` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言大于(self, a, b, 消息 = '')`

断言 a > b

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `a` | `None` |  |
| `b` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言大于等于(self, a, b, 消息 = '')`

断言 a >= b

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `a` | `None` |  |
| `b` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言小于(self, a, b, 消息 = '')`

断言 a < b

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `a` | `None` |  |
| `b` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言小于等于(self, a, b, 消息 = '')`

断言 a <= b

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `a` | `None` |  |
| `b` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言包含(self, 容器, 元素, 消息 = '')`

断言容器包含元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `容器` | `None` |  |
| `元素` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言不包含(self, 容器, 元素, 消息 = '')`

断言容器不包含元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `容器` | `None` |  |
| `元素` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言为None(self, 值, 消息 = '')`

断言值为 None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `值` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言不为None(self, 值, 消息 = '')`

断言值不为 None

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `值` | `None` |  |
| `消息` | `None` | （默认：''） |

---

### `断言抛出(self, 异常类型, 可调用对象)`

断言调用抛出指定异常

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `异常类型` | `None` |  |
| `可调用对象` | `None` |  |

---

### `断言几乎相等(self, 预期, 实际, 位数 = 7, 消息 = '')`

断言浮点数近似相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `预期` | `None` |  |
| `实际` | `None` |  |
| `位数` | `None` | （默认：7） |
| `消息` | `None` | （默认：''） |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `是否全部通过(self)`

检查是否所有测试通过

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `获取摘要(self)`

获取测试结果摘要文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `获取报告(self)`

获取完整测试报告

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 目标, 新值 = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `目标` | `None` |  |
| `新值` | `None` | （默认：None） |

---

### `__enter__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__exit__(self, exc_type, exc_val, exc_tb)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `exc_type` | `None` |  |
| `exc_val` | `None` |  |
| `exc_tb` | `None` |  |

---

### `__init__(self, 目标)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `目标` | `None` |  |

---

### `__getattr__(self, 名称)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `名称` | `None` |  |

---

### `装饰器(测试函数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `测试函数` | `None` |  |

---

### `记录调用()`

**参数：**

无参数。

---

### `包装器(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `结果` | `测试结果()` |
| `verbosity` | `2 if 详细 else 1` |
| `结果` | `测试结果()` |
| `总结果` | `测试结果()` |
| `运行器` | `_unittest.TextTestRunner(stream=_io.StringIO(), verbosity=0)` |
| `unittest_结果` | `运行器.run(套件)` |
| `运行器` | `_unittest.TextTestRunner(stream=_io.StringIO(), verbosity=2 if 详细 else 1)` |
| `unittest_结果` | `运行器.run(套件)` |
| `子结果` | `运行测试(测试类, 详细=详细)` |
| `加载器` | `_unittest.TestLoader()` |
| `套件` | `加载器.discover(路径, pattern=模式)` |
| `lines` | `[]` |
| `原始方法` | `getattr(self.目标, 名称)` |
| `开始` | `_time.perf_counter()` |
| `结束` | `_time.perf_counter()` |
| `总耗时` | `结束 - 开始` |
| `平均耗时` | `总耗时 / 迭代次数` |
| `开始` | `_time.perf_counter()` |
| `结果` | `函数(*参数, **关键字参数)` |
| `结束` | `_time.perf_counter()` |
| `耗时` | `结束 - 开始` |
| `msg` | `'期望抛出异常 ' + str(异常类型) + '，但未抛出任何异常'` |
| `套件` | `_unittest.TestLoader().loadTestsFromTestCase(测试用例类)` |
| `套件` | `_unittest.TestLoader().loadTestsFromTestCase(测试用例类.__class__)` |
| `msg` | `'期望抛出异常 ' + str(异常类型) + '，但实际抛出了 ' + type(e).__name__` |
