# 线程与并发 API

> 模块路径：`stdlib/线程.py`
> 导入方式：`从 线程 导入 函数名` 或 `导入 线程`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `创建线程(目标函数, 参数, 名称)` | 创建并启动一个新线程，返回线程对象 |
| `等待线程(线程)` | 等待线程结束 |
| `分离线程(线程)` | 分离线程，使其独立运行 |
| `获取线程号(线程)` | 获取线程的标识号 |
| `当前线程号()` | 获取当前线程的标识号 |
| `线程休眠(秒数)` | 当前线程休眠指定秒数 |
| `线程让出()` | 让出当前线程的CPU时间片 |
| `创建互斥锁()` | 创建互斥锁，返回互斥锁对象 |
| `互斥锁锁定(互斥锁)` | 锁定互斥锁 |
| `互斥锁尝试锁定(互斥锁)` | 尝试锁定互斥锁，立即返回True/False |
| `互斥锁解锁(互斥锁)` | 解锁互斥锁 |
| `互斥锁销毁(互斥锁)` | 销毁互斥锁 |
| `创建信号量(初始值)` | 创建信号量，返回信号量对象 |
| `信号量阻塞(信号量)` | 阻塞直到信号量可用 |
| `信号量发布(信号量)` | 发布信号量，增加一个可用计数 |
| `信号量销毁(信号量)` | 销毁信号量 |
| `创建条件变量()` | 创建条件变量，返回条件变量对象 |
| `条件变量阻塞(条件变量)` | 阻塞等待条件变量通知 |
| `条件变量通知(条件变量)` | 通知一个等待的条件变量 |
| `条件变量通知全部(条件变量)` | 通知所有等待的条件变量 |
| `条件变量销毁(条件变量)` | 销毁条件变量 |
| `原子加法(指针, 增量)` | 原子加法，返回新值 |
| `原子减法(指针, 减量)` | 原子减法，返回新值 |
| `原子存储(指针, 值)` | 原子存储值 |
| `原子加载(指针)` | 原子加载值 |
| `原子比较交换(指针, 预期值, 新值)` | 原子比较交换，成功返回True，失败返回False |
| `线程池工作线程(线程池)` | 获取线程池的工作线程数 |
| `创建线程池(最大线程数)` | 创建线程池，返回线程池对象 |
| `线程池提交任务(线程池, 任务函数)` | 提交任务到线程池，返回Future对象 |
| `线程池等待全部(线程池)` | 等待线程池中所有任务完成 |
| `线程池关停(线程池)` | 关停线程池 |
| `__init__(self, 线程)` |  |
| `__init__(self)` |  |
| `__init__(self, 初始值)` |  |
| `__init__(self)` |  |
| `__init__(self, 初始值)` |  |
| `__init__(self, 最大线程数)` |  |

---

## 函数详情

### `创建线程(目标函数, 参数 = None, 名称 = '')`

创建并启动一个新线程，返回线程对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `目标函数` | `None` |  |
| `参数` | `None` | （默认：None） |
| `名称` | `None` | （默认：''） |

---

### `等待线程(线程)`

等待线程结束

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `线程` | `None` |  |

---

### `分离线程(线程)`

分离线程，使其独立运行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `线程` | `None` |  |

---

### `获取线程号(线程)`

获取线程的标识号

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `线程` | `None` |  |

---

### `当前线程号()`

获取当前线程的标识号

**参数：**

无参数。

---

### `线程休眠(秒数)`

当前线程休眠指定秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒数` | `None` |  |

---

### `线程让出()`

让出当前线程的CPU时间片

**参数：**

无参数。

---

### `创建互斥锁()`

创建互斥锁，返回互斥锁对象

**参数：**

无参数。

---

### `互斥锁锁定(互斥锁)`

锁定互斥锁

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `互斥锁` | `None` |  |

---

### `互斥锁尝试锁定(互斥锁)`

尝试锁定互斥锁，立即返回True/False

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `互斥锁` | `None` |  |

---

### `互斥锁解锁(互斥锁)`

解锁互斥锁

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `互斥锁` | `None` |  |

---

### `互斥锁销毁(互斥锁)`

销毁互斥锁

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `互斥锁` | `None` |  |

---

### `创建信号量(初始值 = 1)`

创建信号量，返回信号量对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `初始值` | `None` | （默认：1） |

---

### `信号量阻塞(信号量)`

阻塞直到信号量可用

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `信号量` | `None` |  |

---

### `信号量发布(信号量)`

发布信号量，增加一个可用计数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `信号量` | `None` |  |

---

### `信号量销毁(信号量)`

销毁信号量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `信号量` | `None` |  |

---

### `创建条件变量()`

创建条件变量，返回条件变量对象

**参数：**

无参数。

---

### `条件变量阻塞(条件变量)`

阻塞等待条件变量通知

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件变量` | `None` |  |

---

### `条件变量通知(条件变量)`

通知一个等待的条件变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件变量` | `None` |  |

---

### `条件变量通知全部(条件变量)`

通知所有等待的条件变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件变量` | `None` |  |

---

### `条件变量销毁(条件变量)`

销毁条件变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `条件变量` | `None` |  |

---

### `原子加法(指针, 增量)`

原子加法，返回新值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |
| `增量` | `None` |  |

---

### `原子减法(指针, 减量)`

原子减法，返回新值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |
| `减量` | `None` |  |

---

### `原子存储(指针, 值)`

原子存储值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |
| `值` | `None` |  |

---

### `原子加载(指针)`

原子加载值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |

---

### `原子比较交换(指针, 预期值, 新值)`

原子比较交换，成功返回True，失败返回False

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `指针` | `None` |  |
| `预期值` | `None` |  |
| `新值` | `None` |  |

---

### `线程池工作线程(线程池)`

获取线程池的工作线程数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `线程池` | `None` |  |

---

### `创建线程池(最大线程数)`

创建线程池，返回线程池对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `最大线程数` | `None` |  |

---

### `线程池提交任务(线程池, 任务函数)`

提交任务到线程池，返回Future对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `线程池` | `None` |  |
| `任务函数` | `None` |  |

---

### `线程池等待全部(线程池)`

等待线程池中所有任务完成

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `线程池` | `None` |  |

---

### `线程池关停(线程池)`

关停线程池

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `线程池` | `None` |  |

---

### `__init__(self, 线程)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `线程` | `None` |  |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 初始值 = 1)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `初始值` | `None` | （默认：1） |

---

### `__init__(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, 初始值 = 0)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `初始值` | `None` | （默认：0） |

---

### `__init__(self, 最大线程数)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `最大线程数` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `a` | `_原子存储[指针]` |
| `a` | `_原子存储[指针]` |
| `a` | `_原子存储[指针]` |
| `a` | `_原子存储[指针]` |
| `参数` | `()` |
| `线程` | `_threading.Thread(target=目标函数, args=参数, name=名称)` |
