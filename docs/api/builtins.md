# 内置函数 API

> 模块路径：`stdlib/builtins.py`
> 导入方式：`从 builtins 导入 函数名` 或 `导入 builtins`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `读取文件(path, encoding)` | 读取文件内容 |
| `_读文件(path)` | 内部用：读取文件内容（简化版） |
| `写入文件(path, content, encoding)` | 写入文件内容 |
| `追加文件(path, content, encoding)` | 追加内容到文件 |
| `文件存在(path)` | 检查文件是否存在 |
| `目录存在(path)` | 检查目录是否存在 |
| `路径存在(path)` | 检查路径是否存在（文件或目录） |
| `创建目录(path)` | 创建目录 |
| `删除文件(path)` | 删除文件 |
| `删除目录(path)` | 删除空目录 |
| `列出目录(path)` | 列出目录内容 |
| `文件大小(path)` | 获取文件大小（字节） |
| `绝对路径(path)` | 获取绝对路径 |
| `连接路径()` | 连接多个路径 |
| `目录名(path)` | 获取路径的目录部分 |
| `文件名(path)` | 获取路径的文件名部分 |
| `扩展名(path)` | 获取文件扩展名 |
| `分割路径(path)` | 分割路径为(目录, 文件名) |
| `分割扩展名(path)` | 分割路径为(主名, 扩展名) |
| `环境变量(name, default)` | 获取环境变量 |
| `设置环境变量(name, value)` | 设置环境变量 |
| `参数列表()` | 获取命令行参数列表 |
| `退出程序(code)` | 退出程序 |
| `当前目录()` | 获取当前工作目录 |
| `切换目录(path)` | 切换工作目录 |
| `执行命令(command)` | 执行系统命令 |
| `读取行()` | 从标准输入读取一行 |
| `读取N字节(字节数)` | 从标准输入读取指定数量的字节 |
| `写入输出(text)` | 向标准输出写入文本（不含换行） |
| `打印输出(text)` | 向标准输出打印文本并换行 |
| `刷新输出()` | 强制刷新标准输出缓冲区 |
| `写入错误(text)` | 向标准错误写入文本 |
| `打印错误(text)` | 向标准错误打印文本并换行 |
| `解析JSON(text)` | 解析 JSON 字符串为段言值 |
| `序列化JSON(value, 缩进)` | 将段言值序列化为 JSON 字符串 |
| `美化JSON(value)` | 美化 JSON 输出（带缩进） |
| `转整数(text)` | 将字符串转换为整数 |
| `转浮点(text)` | 将字符串转换为浮点数 |
| `转字符串(value)` | 将值转换为字符串 |
| `字符串长度(text)` | 获取字符串长度 |
| `显示宽度(text)` | 获取字符串显示宽度（中文/全角字符计 2，其余计 1） |
| `字符串获取(text, index)` | 获取字符串中指定位置的字符 |
| `截取(text, start, end)` | 截取字符串的一部分 |
| `分割字符串(text, separator)` | 分割字符串 |
| `连接字符串(parts, separator)` | 连接字符串列表 |
| `替换字符串(text, old, new)` | 替换字符串 |
| `去除空白(text)` | 去除首尾空白 |
| `转大写(text)` | 转换为大写 |
| `转小写(text)` | 转换为小写 |
| `字符串包含(text, substring)` | 检查字符串是否包含子串 |
| `开头(text, prefix)` | 检查字符串是否以指定前缀开头 |
| `结尾(text, suffix)` | 检查字符串是否以指定后缀结尾 |
| `查找子串(text, substring)` | 查找子串位置，未找到返回-1 |
| `替换字符串次数(text, old, new, count)` | 替换字符串，指定替换次数 |
| `截取到末尾(text, start)` | 从指定位置截取到字符串末尾 |
| `字符串计数(text, substring)` | 统计子串出现次数 |
| `字符串重复(text, times)` | 重复字符串指定次数 |
| `字符串反转(text)` | 反转字符串 |
| `转标题(text)` | 转换为标题格式（首字母大写） |
| `去除左侧空白(text)` | 去除左侧空白 |
| `去除右侧空白(text)` | 去除右侧空白 |
| `字符串对齐居中(text, width, fillchar)` | 居中对齐字符串 |
| `字符串对齐左(text, width, fillchar)` | 左对齐字符串 |
| `字符串对齐右(text, width, fillchar)` | 右对齐字符串 |
| `列()` | 创建包含指定元素的列表 |
| `列表创建()` | 创建空列表 |
| `列表长度(列表)` | 获取列表长度 |
| `列表获取(列表, 索引)` | 获取列表中指定索引的元素 |
| `列表追加(列表, 元素)` | 向列表追加元素 |
| `列表弹出(列表, 索引)` | 从列表弹出元素 |
| `列表插入(列表, 索引, 元素)` | 在指定索引处插入元素 |
| `列表排序(列表, 反向)` | 排序列表（原地修改） |
| `列表反转(列表)` | 反转列表（原地修改） |
| `列表包含(列表, 元素)` | 检查列表是否包含元素 |
| `字典创建()` | 创建空字典 |
| `字典设置(字典, 键, 值)` | 设置字典键值 |
| `字典删除(字典, 键)` | 删除字典键值 |
| `字典键列表(字典)` | 获取字典的所有键 |
| `字典值列表(字典)` | 获取字典的所有值 |
| `字典项列表(字典)` | 获取字典的所有键值对 |
| `字典包含键(字典, 键)` | 检查字典是否包含键 |
| `字典获取(字典, 键, 默认值)` | 从字典获取值，不存在则返回默认值 |
| `是整数(值)` | 检查是否为整数 |
| `是浮点(值)` | 检查是否为浮点数 |
| `是字符串(值)` | 检查是否为字符串 |
| `是列表(值)` | 检查是否为列表 |
| `是字典(值)` | 检查是否为字典 |
| `是空(值)` | 检查是否为空值 |
| `是字母(char)` | 检查字符是否为字母 |
| `是数字(char)` | 检查字符是否为数字 |
| `是空白(char)` | 检查字符是否为空格或空白字符 |
| `时间戳()` | 获取当前 Unix 时间戳（秒） |
| `格式化时间(时间对象, 格式)` | 将时间戳或时间字符串格式化为指定格式 |
| `随机整数(最小, 最大)` | 生成范围内的随机整数 |
| `随机浮点()` | 生成 [0.0, 1.0) 范围内的随机浮点数 |
| `随机选择(列表)` | 从列表中随机选择一个元素 |
| `阶乘(n)` | 计算 n 的阶乘 |
| `平均数(数据)` | 计算列表的平均值 |
| `中位数(数据)` | 计算列表的中位数 |
| `众数(数据)` | 计算列表的众数（出现次数最多的值） |
| `方差(数据)` | 计算总体方差 |
| `标准差(数据)` | 计算总体标准差 |
| `样本方差(数据)` | 计算样本方差（分母 n-1） |
| `样本标准差(数据)` | 计算样本标准差（分母 n-1） |
| `求和(数据)` | 计算列表中所有数值的和 |
| `累积和(数据)` | 计算列表的累积和 |
| `圆周率()` | 返回圆周率 π 的近似值 |
| `自然常数()` | 返回自然常数 e 的近似值 |
| `角度转弧度(角度)` | 角度转弧度 |
| `弧度转角度(弧度)` | 弧度转角度 |

---

## 函数详情

### `读取文件(path: str, encoding: str = 'utf-8') -> str`

读取文件内容

参数:
    path: 文件路径
    encoding: 编码（默认utf-8）

返回:
    文件内容

异常:
    RuntimeError: 文件读取失败

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |
| `encoding` | `str` | （默认：'utf-8'） |

**返回：** `str`

---

### `_读文件(path: str) -> str`

内部用：读取文件内容（简化版）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `str`

---

### `写入文件(path: str, content: str, encoding: str = 'utf-8') -> None`

写入文件内容

参数:
    path: 文件路径
    content: 文件内容
    encoding: 编码（默认utf-8）

异常:
    RuntimeError: 文件写入失败

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |
| `content` | `str` |  |
| `encoding` | `str` | （默认：'utf-8'） |

**返回：** `None`

---

### `追加文件(path: str, content: str, encoding: str = 'utf-8') -> None`

追加内容到文件

参数:
    path: 文件路径
    content: 追加内容
    encoding: 编码（默认utf-8）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |
| `content` | `str` |  |
| `encoding` | `str` | （默认：'utf-8'） |

**返回：** `None`

---

### `文件存在(path: str) -> bool`

检查文件是否存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `bool`

---

### `目录存在(path: str) -> bool`

检查目录是否存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `bool`

---

### `路径存在(path: str) -> bool`

检查路径是否存在（文件或目录）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `bool`

---

### `创建目录(path: str) -> None`

创建目录

参数:
    path: 目录路径

说明:
    自动创建所有父目录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `None`

---

### `删除文件(path: str) -> None`

删除文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `None`

---

### `删除目录(path: str) -> None`

删除空目录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `None`

---

### `列出目录(path: str = '.') -> List[str]`

列出目录内容

参数:
    path: 目录路径（默认当前目录）

返回:
    文件名列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` | （默认：'.'） |

**返回：** `List[str]`

---

### `文件大小(path: str) -> int`

获取文件大小（字节）

参数:
    path: 文件路径

返回:
    文件大小（字节）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `int`

---

### `绝对路径(path: str) -> str`

获取绝对路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `str`

---

### `连接路径() -> str`

连接多个路径

**参数：**

无参数。

**返回：** `str`

---

### `目录名(path: str) -> str`

获取路径的目录部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `str`

---

### `文件名(path: str) -> str`

获取路径的文件名部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `str`

---

### `扩展名(path: str) -> str`

获取文件扩展名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `str`

---

### `分割路径(path: str) -> tuple`

分割路径为(目录, 文件名)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `tuple`

---

### `分割扩展名(path: str) -> tuple`

分割路径为(主名, 扩展名)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `tuple`

---

### `环境变量(name: str, default: str = None) -> Optional[str]`

获取环境变量

参数:
    name: 环境变量名
    default: 默认值

返回:
    环境变量值或默认值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `str` |  |
| `default` | `str` | （默认：None） |

**返回：** `Optional[str]`

---

### `设置环境变量(name: str, value: str) -> None`

设置环境变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `name` | `str` |  |
| `value` | `str` |  |

**返回：** `None`

---

### `参数列表() -> List[str]`

获取命令行参数列表

**参数：**

无参数。

**返回：** `List[str]`

---

### `退出程序(code: int = 0) -> None`

退出程序

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `code` | `int` | （默认：0） |

**返回：** `None`

---

### `当前目录() -> str`

获取当前工作目录

**参数：**

无参数。

**返回：** `str`

---

### `切换目录(path: str) -> None`

切换工作目录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `path` | `str` |  |

**返回：** `None`

---

### `执行命令(command: str) -> int`

执行系统命令

参数:
    command: 命令字符串

返回:
    退出码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `command` | `str` |  |

**返回：** `int`

---

### `读取行() -> str`

从标准输入读取一行

返回:
    读取的字符串（不含换行符）

**参数：**

无参数。

**返回：** `str`

---

### `读取N字节(字节数: int) -> str`

从标准输入读取指定数量的字节

参数:
    字节数: 要读取的字节数

返回:
    读取的字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字节数` | `int` |  |

**返回：** `str`

---

### `写入输出(text: str) -> None`

向标准输出写入文本（不含换行）

参数:
    text: 要写入的文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `None`

---

### `打印输出(text: str) -> None`

向标准输出打印文本并换行

参数:
    text: 要打印的文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `None`

---

### `刷新输出() -> None`

强制刷新标准输出缓冲区

**参数：**

无参数。

**返回：** `None`

---

### `写入错误(text: str) -> None`

向标准错误写入文本

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `None`

---

### `打印错误(text: str) -> None`

向标准错误打印文本并换行

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `None`

---

### `解析JSON(text: str) -> object`

解析 JSON 字符串为段言值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `object`

---

### `序列化JSON(value: object, 缩进: Optional[int] = None) -> str`

将段言值序列化为 JSON 字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `value` | `object` |  |
| `缩进` | `Optional[int]` | （默认：None） |

**返回：** `str`

---

### `美化JSON(value: object) -> str`

美化 JSON 输出（带缩进）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `value` | `object` |  |

**返回：** `str`

---

### `转整数(text: str) -> int`

将字符串转换为整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `int`

---

### `转浮点(text: str) -> float`

将字符串转换为浮点数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `float`

---

### `转字符串(value) -> str`

将值转换为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `value` | `None` |  |

**返回：** `str`

---

### `字符串长度(text: str) -> int`

获取字符串长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `int`

---

### `显示宽度(text: str) -> int`

获取字符串的终端显示宽度。中文、日文等全角（CJK）及宽字符计为 2，其余字符（ASCII、半角符号等）计为 1。用于终端表格/边框对齐，避免 `字符串长度` 把全角字符按 1 个字符计算导致右侧错位。

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` | 待测量显示宽度的字符串 |

**返回：** `int`（显示宽度）

**示例：**

```python
显示宽度("中文abc")   # => 7  （中、文 各 2，a、b、c 各 1）
显示宽度("hello")     # => 5
显示宽度("A中")       # => 3  （A 为 1，中 为 2）
```

> 说明：与 `字符串长度` 不同，后者返回字符个数（如 `"中文abc"` 长度为 5），而 `显示宽度` 返回实际占据的列数（7）。

---

### `字符串获取(text: str, index: int) -> str`

获取字符串中指定位置的字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `index` | `int` |  |

**返回：** `str`

---

### `截取(text: str, start: int, end: int) -> str`

截取字符串的一部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `start` | `int` |  |
| `end` | `int` |  |

**返回：** `str`

---

### `分割字符串(text: str, separator: str = None) -> List[str]`

分割字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `separator` | `str` | （默认：None） |

**返回：** `List[str]`

---

### `连接字符串(parts: List[str], separator: str = '') -> str`

连接字符串列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `parts` | `List[str]` |  |
| `separator` | `str` | （默认：''） |

**返回：** `str`

---

### `替换字符串(text: str, old: str, new: str) -> str`

替换字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `old` | `str` |  |
| `new` | `str` |  |

**返回：** `str`

---

### `去除空白(text: str) -> str`

去除首尾空白

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `str`

---

### `转大写(text: str) -> str`

转换为大写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `str`

---

### `转小写(text: str) -> str`

转换为小写

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `str`

---

### `字符串包含(text: str, substring: str) -> bool`

检查字符串是否包含子串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `substring` | `str` |  |

**返回：** `bool`

---

### `开头(text: str, prefix: str) -> bool`

检查字符串是否以指定前缀开头

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `prefix` | `str` |  |

**返回：** `bool`

---

### `结尾(text: str, suffix: str) -> bool`

检查字符串是否以指定后缀结尾

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `suffix` | `str` |  |

**返回：** `bool`

---

### `查找子串(text: str, substring: str) -> int`

查找子串位置，未找到返回-1

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `substring` | `str` |  |

**返回：** `int`

---

### `替换字符串次数(text: str, old: str, new: str, count: int = -1) -> str`

替换字符串，指定替换次数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `old` | `str` |  |
| `new` | `str` |  |
| `count` | `int` | （默认：-1） |

**返回：** `str`

---

### `截取到末尾(text: str, start: int) -> str`

从指定位置截取到字符串末尾

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `start` | `int` |  |

**返回：** `str`

---

### `字符串计数(text: str, substring: str) -> int`

统计子串出现次数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `substring` | `str` |  |

**返回：** `int`

---

### `字符串重复(text: str, times: int) -> str`

重复字符串指定次数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `times` | `int` |  |

**返回：** `str`

---

### `字符串反转(text: str) -> str`

反转字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `str`

---

### `转标题(text: str) -> str`

转换为标题格式（首字母大写）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `str`

---

### `去除左侧空白(text: str) -> str`

去除左侧空白

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `str`

---

### `去除右侧空白(text: str) -> str`

去除右侧空白

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |

**返回：** `str`

---

### `字符串对齐居中(text: str, width: int, fillchar: str = ' ') -> str`

居中对齐字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `width` | `int` |  |
| `fillchar` | `str` | （默认：' '） |

**返回：** `str`

---

### `字符串对齐左(text: str, width: int, fillchar: str = ' ') -> str`

左对齐字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `width` | `int` |  |
| `fillchar` | `str` | （默认：' '） |

**返回：** `str`

---

### `字符串对齐右(text: str, width: int, fillchar: str = ' ') -> str`

右对齐字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `text` | `str` |  |
| `width` | `int` |  |
| `fillchar` | `str` | （默认：' '） |

**返回：** `str`

---

### `列() -> list`

创建包含指定元素的列表

**参数：**

无参数。

**返回：** `list`

---

### `列表创建() -> list`

创建空列表

**参数：**

无参数。

**返回：** `list`

---

### `列表长度(列表) -> int`

获取列表长度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

**返回：** `int`

---

### `列表获取(列表, 索引)`

获取列表中指定索引的元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `索引` | `None` |  |

---

### `列表追加(列表, 元素) -> None`

向列表追加元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `元素` | `None` |  |

**返回：** `None`

---

### `列表弹出(列表, 索引: int = -1)`

从列表弹出元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `索引` | `int` | （默认：-1） |

---

### `列表插入(列表, 索引, 元素) -> None`

在指定索引处插入元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `索引` | `None` |  |
| `元素` | `None` |  |

**返回：** `None`

---

### `列表排序(列表, 反向: bool = False) -> None`

排序列表（原地修改）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `反向` | `bool` | （默认：False） |

**返回：** `None`

---

### `列表反转(列表) -> None`

反转列表（原地修改）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

**返回：** `None`

---

### `列表包含(列表, 元素) -> bool`

检查列表是否包含元素

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |
| `元素` | `None` |  |

**返回：** `bool`

---

### `字典创建() -> dict`

创建空字典

**参数：**

无参数。

**返回：** `dict`

---

### `字典设置(字典, 键, 值) -> None`

设置字典键值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典` | `None` |  |
| `键` | `None` |  |
| `值` | `None` |  |

**返回：** `None`

---

### `字典删除(字典, 键) -> None`

删除字典键值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典` | `None` |  |
| `键` | `None` |  |

**返回：** `None`

---

### `字典键列表(字典) -> list`

获取字典的所有键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典` | `None` |  |

**返回：** `list`

---

### `字典值列表(字典) -> list`

获取字典的所有值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典` | `None` |  |

**返回：** `list`

---

### `字典项列表(字典) -> list`

获取字典的所有键值对

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典` | `None` |  |

**返回：** `list`

---

### `字典包含键(字典, 键) -> bool`

检查字典是否包含键

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典` | `None` |  |
| `键` | `None` |  |

**返回：** `bool`

---

### `字典获取(字典, 键, 默认值 = None)`

从字典获取值，不存在则返回默认值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `字典` | `None` |  |
| `键` | `None` |  |
| `默认值` | `None` | （默认：None） |

---

### `是整数(值) -> bool`

检查是否为整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

**返回：** `bool`

---

### `是浮点(值) -> bool`

检查是否为浮点数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

**返回：** `bool`

---

### `是字符串(值) -> bool`

检查是否为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

**返回：** `bool`

---

### `是列表(值) -> bool`

检查是否为列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

**返回：** `bool`

---

### `是字典(值) -> bool`

检查是否为字典

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

**返回：** `bool`

---

### `是空(值) -> bool`

检查是否为空值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `值` | `None` |  |

**返回：** `bool`

---

### `是字母(char: str) -> bool`

检查字符是否为字母

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `char` | `str` |  |

**返回：** `bool`

---

### `是数字(char: str) -> bool`

检查字符是否为数字

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `char` | `str` |  |

**返回：** `bool`

---

### `是空白(char: str) -> bool`

检查字符是否为空格或空白字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `char` | `str` |  |

**返回：** `bool`

---

### `时间戳() -> float`

获取当前 Unix 时间戳（秒）

返回:
    浮点数时间戳

**参数：**

无参数。

**返回：** `float`

---

### `格式化时间(时间对象: Union[str, float], 格式: str = '%Y-%m-%d %H:%M:%S') -> str`

将时间戳或时间字符串格式化为指定格式

参数:
    时间对象: Unix 时间戳（浮点数）或 'YYYY-MM-DD HH:MM:SS' 格式字符串
    格式: 目标格式模板

返回:
    格式化后的时间字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `时间对象` | `Union[str, float]` |  |
| `格式` | `str` | （默认：'%Y-%m-%d %H:%M:%S'） |

**返回：** `str`

---

### `随机整数(最小: int, 最大: int) -> int`

生成范围内的随机整数

参数:
    最小: 最小值（包含）
    最大: 最大值（包含）

返回:
    随机整数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `最小` | `int` |  |
| `最大` | `int` |  |

**返回：** `int`

---

### `随机浮点() -> float`

生成 [0.0, 1.0) 范围内的随机浮点数

返回:
    随机浮点数

**参数：**

无参数。

**返回：** `float`

---

### `随机选择(列表) -> Optional[object]`

从列表中随机选择一个元素

参数:
    列表: 源列表

返回:
    随机选中的元素，列表为空返回空

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `列表` | `None` |  |

**返回：** `Optional[object]`

---

### `阶乘(n: int) -> int`

计算 n 的阶乘

参数:
    n: 非负整数

返回:
    n!

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `n` | `int` |  |

**返回：** `int`

---

### `平均数(数据: list) -> float`

计算列表的平均值

参数:
    数据: 数值列表

返回:
    平均值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `float`

---

### `中位数(数据: list) -> float`

计算列表的中位数

参数:
    数据: 数值列表

返回:
    中位数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `float`

---

### `众数(数据: list)`

计算列表的众数（出现次数最多的值）

参数:
    数据: 数值列表

返回:
    众数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

---

### `方差(数据: list) -> float`

计算总体方差

参数:
    数据: 数值列表

返回:
    方差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `float`

---

### `标准差(数据: list) -> float`

计算总体标准差

参数:
    数据: 数值列表

返回:
    标准差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `float`

---

### `样本方差(数据: list) -> float`

计算样本方差（分母 n-1）

参数:
    数据: 数值列表

返回:
    样本方差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `float`

---

### `样本标准差(数据: list) -> float`

计算样本标准差（分母 n-1）

参数:
    数据: 数值列表

返回:
    样本标准差

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `float`

---

### `求和(数据: list) -> float`

计算列表中所有数值的和

参数:
    数据: 数值列表

返回:
    总和

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `float`

---

### `累积和(数据: list) -> list`

计算列表的累积和

参数:
    数据: 数值列表

返回:
    累积和列表

示例:
    累积和([1, 2, 3, 4])  # [1, 3, 6, 10]

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `数据` | `list` |  |

**返回：** `list`

---

### `圆周率() -> float`

返回圆周率 π 的近似值

**参数：**

无参数。

**返回：** `float`

---

### `自然常数() -> float`

返回自然常数 e 的近似值

**参数：**

无参数。

**返回：** `float`

---

### `角度转弧度(角度: float) -> float`

角度转弧度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `角度` | `float` |  |

**返回：** `float`

---

### `弧度转角度(弧度: float) -> float`

弧度转角度

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `弧度` | `float` |  |

**返回：** `float`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `result` | `[]` |
| `total` | `0` |
| `dir_path` | `os.path.dirname(path)` |
| `dt` | `_datetime_class.fromtimestamp(时间对象)` |
| `dt` | `_datetime_class.strptime(时间对象, fmt)` |
