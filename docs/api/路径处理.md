# 路径处理 API

> 模块路径：`stdlib/路径处理.py`
> 导入方式：`从 路径处理 导入 函数名` 或 `导入 路径处理`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `绝对路径(路径)` | 返回路径的绝对路径 |
| `相对路径(路径, 起始)` | 返回相对于起始路径的相对路径 |
| `拼接路径(路径片段1, 路径片段2)` | 拼接两个路径 |
| `拼接路径2()` | 拼接多个路径片段 |
| `获取文件名(路径)` | 获取路径中的文件名部分 |
| `获取目录名(路径)` | 获取路径中的目录名部分 |
| `获取扩展名(路径)` | 获取路径中的文件扩展名（含点号） |
| `获取基础名(路径)` | 获取路径中的文件名（不含扩展名） |
| `获取父目录(路径)` | 获取路径的父目录路径 |
| `分割路径(路径)` | 将路径分割为(目录, 文件名) |
| `分割扩展名(路径)` | 将路径分割为(主路径, 扩展名) |
| `分割全部(路径)` | 将路径分割为所有组成部分的列表 |
| `规范化路径(路径)` | 规范化路径格式 |
| `获取相对路径(目标路径, 起始路径)` | 获取从起始路径到目标路径的相对路径 |
| `globMatch(模式, 路径)` | 检查路径是否匹配glob模式 |
| `路径是否相等(路径1, 路径2)` | 比较两个路径是否相等（规范化后比较） |
| `更改扩展名(路径, 新扩展名)` | 更改文件扩展名 |
| `主目录()` | 获取当前用户的主目录路径 |
| `当前目录()` | 获取当前工作目录路径 |
| `临时目录()` | 获取系统临时目录路径 |

---

## 函数详情

### `绝对路径(路径)`

返回路径的绝对路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `相对路径(路径, 起始 = None)`

返回相对于起始路径的相对路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |
| `起始` | `None` | （默认：None） |

---

### `拼接路径(路径片段1, 路径片段2)`

拼接两个路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径片段1` | `None` |  |
| `路径片段2` | `None` |  |

---

### `拼接路径2()`

拼接多个路径片段

**参数：**

无参数。

---

### `获取文件名(路径)`

获取路径中的文件名部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `获取目录名(路径)`

获取路径中的目录名部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `获取扩展名(路径)`

获取路径中的文件扩展名（含点号）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `获取基础名(路径)`

获取路径中的文件名（不含扩展名）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `获取父目录(路径)`

获取路径的父目录路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `分割路径(路径)`

将路径分割为(目录, 文件名)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `分割扩展名(路径)`

将路径分割为(主路径, 扩展名)

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `分割全部(路径)`

将路径分割为所有组成部分的列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `规范化路径(路径)`

规范化路径格式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |

---

### `获取相对路径(目标路径, 起始路径)`

获取从起始路径到目标路径的相对路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `目标路径` | `None` |  |
| `起始路径` | `None` |  |

---

### `globMatch(模式, 路径)`

检查路径是否匹配glob模式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `路径` | `None` |  |

---

### `路径是否相等(路径1, 路径2)`

比较两个路径是否相等（规范化后比较）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径1` | `None` |  |
| `路径2` | `None` |  |

---

### `更改扩展名(路径, 新扩展名)`

更改文件扩展名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `None` |  |
| `新扩展名` | `None` |  |

---

### `主目录()`

获取当前用户的主目录路径

**参数：**

无参数。

---

### `当前目录()`

获取当前工作目录路径

**参数：**

无参数。

---

### `临时目录()`

获取系统临时目录路径

**参数：**

无参数。

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `basename` | `_path.basename(路径)` |
| `parts` | `[]` |
| `新扩展名` | `'.' + 新扩展名` |
