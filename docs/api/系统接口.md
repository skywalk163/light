# 系统接口 API

> 模块路径：`stdlib/系统接口.py`
> 导入方式：`从 系统接口 导入 函数名` 或 `导入 系统接口`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `获取环境变量(名称, 默认值)` | 获取环境变量 |
| `设置环境变量(名称, 值)` | 设置环境变量 |
| `删除环境变量(名称)` | 删除环境变量 |
| `环境变量存在(名称)` | 检查环境变量是否存在 |
| `获取所有环境变量()` | 获取所有环境变量字典 |
| `当前工作目录()` | 获取当前工作目录 |
| `切换工作目录(路径)` | 切换工作目录 |
| `路径存在(路径)` | 检查路径是否存在 |
| `是文件(路径)` | 检查是否为文件 |
| `是目录(路径)` | 检查是否为目录 |
| `是绝对路径(路径)` | 检查是否为绝对路径 |
| `绝对路径(路径)` | 获取绝对路径 |
| `规范化路径(路径)` | 规范化路径 |
| `连接路径()` | 连接多个路径片段 |
| `取目录名(路径)` | 获取路径中的目录部分 |
| `取文件名(路径)` | 获取路径中的文件名部分 |
| `取文件扩展名(路径)` | 获取文件扩展名（包含点号） |
| `取文件名无扩展(路径)` | 获取不含扩展名的文件名 |
| `分割路径(路径)` | 分割路径为目录和文件名 |
| `分割扩展名(路径)` | 分割扩展名 |
| `文件大小(路径)` | 获取文件大小（字节） |
| `文件修改时间(路径)` | 获取文件修改时间戳 |
| `文件创建时间(路径)` | 获取文件创建时间戳 |
| `文件访问时间(路径)` | 获取文件访问时间戳 |
| `创建目录(路径, 递归)` | 创建目录 |
| `删除目录(路径, 递归)` | 删除目录 |
| `列出目录(路径)` | 列出目录内容 |
| `遍历目录(路径)` | 递归遍历目录 |
| `重命名(旧路径, 新路径)` | 重命名文件或目录 |
| `删除文件(路径)` | 删除文件 |
| `复制文件(源路径, 目标路径)` | 复制文件 |
| `进程ID()` | 获取当前进程ID |
| `父进程ID()` | 获取父进程ID |
| `退出(退出码)` | 退出程序 |
| `获取命令行参数()` | 获取命令行参数列表 |
| `获取脚本路径()` | 获取当前脚本路径 |
| `标准输入()` | 获取标准输入 |
| `标准输出()` | 获取标准输出 |
| `标准错误()` | 获取标准错误 |
| `操作系统()` | 获取操作系统名称 |
| `操作系统版本()` | 获取操作系统版本 |
| `操作系统详细信息()` | 获取操作系统详细信息 |
| `计算机名()` | 获取计算机名 |
| `处理器架构()` | 获取处理器架构 |
| `处理器型号()` | 获取处理器型号 |
| `Python版本()` | 获取Python版本 |
| `Python实现()` | 获取Python实现（CPython, PyPy等） |
| `Python编译器()` | 获取Python编译器 |
| `是否Windows()` | 是否为Windows系统 |
| `是否Linux()` | 是否为Linux系统 |
| `是否Mac()` | 是否为macOS系统 |
| `用户名()` | 获取当前用户名 |
| `用户主目录()` | 获取用户主目录 |
| `临时目录()` | 获取系统临时目录 |
| `执行系统命令(命令)` | 执行系统命令（直接输出到控制台） |
| `路径分隔符()` | 获取路径分隔符（Windows为\，Linux为/） |
| `环境变量分隔符()` | 获取环境变量分隔符（Windows为;，Linux为:） |
| `行分隔符()` | 获取行分隔符 |
| `CPU核心数()` | 获取CPU核心数 |
| `内存使用情况()` | 获取内存使用情况 |
| `内存总量()` | 获取系统内存总量（字节） |
| `内存可用()` | 获取系统可用内存（字节） |
| `线程数()` | 获取当前进程的线程数 |

---

## 函数详情

### `获取环境变量(名称: str, 默认值: str = None) -> Optional[str]`

获取环境变量

参数:
    名称: 环境变量名
    默认值: 不存在时的默认值

返回:
    环境变量值

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |
| `默认值` | `str` | （默认：None） |

**返回：** `Optional[str]`

---

### `设置环境变量(名称: str, 值: str) -> None`

设置环境变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |
| `值` | `str` |  |

**返回：** `None`

---

### `删除环境变量(名称: str) -> None`

删除环境变量

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |

**返回：** `None`

---

### `环境变量存在(名称: str) -> bool`

检查环境变量是否存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `名称` | `str` |  |

**返回：** `bool`

---

### `获取所有环境变量() -> Dict[str, str]`

获取所有环境变量字典

**参数：**

无参数。

**返回：** `Dict[str, str]`

---

### `当前工作目录() -> str`

获取当前工作目录

**参数：**

无参数。

**返回：** `str`

---

### `切换工作目录(路径: str) -> None`

切换工作目录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `None`

---

### `路径存在(路径: str) -> bool`

检查路径是否存在

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `bool`

---

### `是文件(路径: str) -> bool`

检查是否为文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `bool`

---

### `是目录(路径: str) -> bool`

检查是否为目录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `bool`

---

### `是绝对路径(路径: str) -> bool`

检查是否为绝对路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `bool`

---

### `绝对路径(路径: str) -> str`

获取绝对路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `str`

---

### `规范化路径(路径: str) -> str`

规范化路径

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `str`

---

### `连接路径() -> str`

连接多个路径片段

**参数：**

无参数。

**返回：** `str`

---

### `取目录名(路径: str) -> str`

获取路径中的目录部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `str`

---

### `取文件名(路径: str) -> str`

获取路径中的文件名部分

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `str`

---

### `取文件扩展名(路径: str) -> str`

获取文件扩展名（包含点号）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `str`

---

### `取文件名无扩展(路径: str) -> str`

获取不含扩展名的文件名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `str`

---

### `分割路径(路径: str) -> tuple`

分割路径为目录和文件名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `tuple`

---

### `分割扩展名(路径: str) -> tuple`

分割扩展名

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `tuple`

---

### `文件大小(路径: str) -> int`

获取文件大小（字节）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `int`

---

### `文件修改时间(路径: str) -> float`

获取文件修改时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `float`

---

### `文件创建时间(路径: str) -> float`

获取文件创建时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `float`

---

### `文件访问时间(路径: str) -> float`

获取文件访问时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `float`

---

### `创建目录(路径: str, 递归: bool = True) -> None`

创建目录

参数:
    路径: 目录路径
    递归: 是否递归创建父目录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |
| `递归` | `bool` | （默认：True） |

**返回：** `None`

---

### `删除目录(路径: str, 递归: bool = False) -> None`

删除目录

参数:
    路径: 目录路径
    递归: 是否递归删除

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |
| `递归` | `bool` | （默认：False） |

**返回：** `None`

---

### `列出目录(路径: str = '.') -> List[str]`

列出目录内容

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` | （默认：'.'） |

**返回：** `List[str]`

---

### `遍历目录(路径: str) -> List[tuple]`

递归遍历目录

返回:
    [(目录路径, 子目录列表, 文件列表), ...]

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `List[tuple]`

---

### `重命名(旧路径: str, 新路径: str) -> None`

重命名文件或目录

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `旧路径` | `str` |  |
| `新路径` | `str` |  |

**返回：** `None`

---

### `删除文件(路径: str) -> None`

删除文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `路径` | `str` |  |

**返回：** `None`

---

### `复制文件(源路径: str, 目标路径: str) -> None`

复制文件

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `源路径` | `str` |  |
| `目标路径` | `str` |  |

**返回：** `None`

---

### `进程ID() -> int`

获取当前进程ID

**参数：**

无参数。

**返回：** `int`

---

### `父进程ID() -> int`

获取父进程ID

**参数：**

无参数。

**返回：** `int`

---

### `退出(退出码: int = 0) -> None`

退出程序

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `退出码` | `int` | （默认：0） |

**返回：** `None`

---

### `获取命令行参数() -> List[str]`

获取命令行参数列表

**参数：**

无参数。

**返回：** `List[str]`

---

### `获取脚本路径() -> str`

获取当前脚本路径

**参数：**

无参数。

**返回：** `str`

---

### `标准输入() -> Any`

获取标准输入

**参数：**

无参数。

**返回：** `Any`

---

### `标准输出() -> Any`

获取标准输出

**参数：**

无参数。

**返回：** `Any`

---

### `标准错误() -> Any`

获取标准错误

**参数：**

无参数。

**返回：** `Any`

---

### `操作系统() -> str`

获取操作系统名称

**参数：**

无参数。

**返回：** `str`

---

### `操作系统版本() -> str`

获取操作系统版本

**参数：**

无参数。

**返回：** `str`

---

### `操作系统详细信息() -> str`

获取操作系统详细信息

**参数：**

无参数。

**返回：** `str`

---

### `计算机名() -> str`

获取计算机名

**参数：**

无参数。

**返回：** `str`

---

### `处理器架构() -> str`

获取处理器架构

**参数：**

无参数。

**返回：** `str`

---

### `处理器型号() -> str`

获取处理器型号

**参数：**

无参数。

**返回：** `str`

---

### `Python版本() -> str`

获取Python版本

**参数：**

无参数。

**返回：** `str`

---

### `Python实现() -> str`

获取Python实现（CPython, PyPy等）

**参数：**

无参数。

**返回：** `str`

---

### `Python编译器() -> str`

获取Python编译器

**参数：**

无参数。

**返回：** `str`

---

### `是否Windows() -> bool`

是否为Windows系统

**参数：**

无参数。

**返回：** `bool`

---

### `是否Linux() -> bool`

是否为Linux系统

**参数：**

无参数。

**返回：** `bool`

---

### `是否Mac() -> bool`

是否为macOS系统

**参数：**

无参数。

**返回：** `bool`

---

### `用户名() -> str`

获取当前用户名

**参数：**

无参数。

**返回：** `str`

---

### `用户主目录() -> str`

获取用户主目录

**参数：**

无参数。

**返回：** `str`

---

### `临时目录() -> str`

获取系统临时目录

**参数：**

无参数。

**返回：** `str`

---

### `执行系统命令(命令: str) -> int`

执行系统命令（直接输出到控制台）

参数:
    命令: 命令字符串

返回:
    退出码

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `命令` | `str` |  |

**返回：** `int`

---

### `路径分隔符() -> str`

获取路径分隔符（Windows为\，Linux为/）

**参数：**

无参数。

**返回：** `str`

---

### `环境变量分隔符() -> str`

获取环境变量分隔符（Windows为;，Linux为:）

**参数：**

无参数。

**返回：** `str`

---

### `行分隔符() -> str`

获取行分隔符

**参数：**

无参数。

**返回：** `str`

---

### `CPU核心数() -> int`

获取CPU核心数

**参数：**

无参数。

**返回：** `int`

---

### `内存使用情况() -> Dict[str, int]`

获取内存使用情况

返回:
    {'rss': 常驻内存, 'vms': 虚拟内存} 字节

**参数：**

无参数。

**返回：** `Dict[str, int]`

---

### `内存总量() -> int`

获取系统内存总量（字节）

**参数：**

无参数。

**返回：** `int`

---

### `内存可用() -> int`

获取系统可用内存（字节）

**参数：**

无参数。

**返回：** `int`

---

### `线程数() -> int`

获取当前进程的线程数

**参数：**

无参数。

**返回：** `int`

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `进程` | `psutil.Process()` |
| `内存信息` | `进程.memory_info()` |
