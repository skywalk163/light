# 日期时间 API

> 模块路径：`stdlib/日期时间.py`
> 导入方式：`从 日期时间 导入 函数名` 或 `导入 日期时间`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `当前时间戳()` | 当前 Unix 时间戳（秒） |
| `当前毫秒时间戳()` | 当前毫秒时间戳 |
| `当前微秒时间戳()` | 当前微秒时间戳 |
| `当前时间()` | 当前本地时间，返回 日期时间对象 |
| `当前UTC时间()` | 当前 UTC 时间，返回 日期时间对象 |
| `时间戳转日期时间(时间戳)` | Unix 时间戳转 日期时间对象 |
| `时间戳转UTC(时间戳)` | Unix 时间戳转 UTC 日期时间对象 |
| `日期时间转时间戳(dt)` | 日期时间对象转 Unix 时间戳 |
| `创建日期时间(年, 月, 日, 时, 分, 秒)` | 创建日期时间对象 |
| `创建UTC日期时间(年, 月, 日, 时, 分, 秒)` | 创建 UTC 日期时间对象 |
| `格式化时间(dt, 格式)` | 格式化日期时间对象为字符串 |
| `格式化ISO8601(dt)` | 格式化为 ISO8601 字符串 |
| `格式化RFC3339(dt)` | 格式化为 RFC3339 字符串 |
| `格式化中文日期(dt)` | 格式化为中文日期字符串 |
| `解析时间(文本, 格式)` | 解析时间字符串为 日期时间对象 |
| `获取星期名称(dt)` | 获取星期名称（星期一~星期日） |
| `获取月份名称(月)` | 获取月份名称 |
| `获取月份天数(年, 月)` | 获取指定年月的天数 |
| `是闰年(年)` | 判断是否为闰年 |
| `获取星期(dt)` | 获取星期几（0=星期一, 6=星期日） |
| `获取年日(dt)` | 获取今天是当年第几天（1-366） |
| `时间相加(dt, 间隔)` | 时间加时间间隔，返回新 日期时间对象 |
| `时间相减(dt1, dt2)` | 时间相减，返回 时间间隔 |
| `时间加天数(dt, 天数)` | 时间加天数，返回新 日期时间对象 |
| `时间加小时(dt, 小时)` | 时间加小时，返回新 日期时间对象 |
| `时间加分钟(dt, 分钟)` | 时间加分钟，返回新 日期时间对象 |
| `时间比较(dt1, dt2)` | 比较两个时间，返回 -1/0/1（dt1早于/等于/晚于dt2） |
| `时间等于(dt1, dt2)` | 判断两个时间是否相等 |
| `时间早于(dt1, dt2)` | 判断 dt1 是否早于 dt2 |
| `创建时间间隔(天, 秒, 微秒, 毫秒, 分钟, 小时, 周)` | 创建时间间隔对象 |
| `时间间隔总秒数(间隔)` | 获取时间间隔的总秒数 |
| `时间间隔总毫秒数(间隔)` | 获取时间间隔的总毫秒数 |
| `时间间隔天数(间隔)` | 获取时间间隔的天数 |
| `时间间隔小时数(间隔)` | 获取时间间隔的总小时数 |
| `时间间隔分钟数(间隔)` | 获取时间间隔的总分钟数 |
| `开始计时()` | 开始计时，返回当前时间戳 |
| `结束计时(开始时间)` | 结束计时，返回耗时秒数 |
| `休眠(秒)` | 休眠指定秒数 |
| `毫秒休眠(毫秒)` | 休眠指定毫秒数 |
| `__init__(self, dt)` |  |
| `年(self)` |  |
| `月(self)` |  |
| `日(self)` |  |
| `时(self)` |  |
| `分(self)` |  |
| `秒(self)` |  |
| `__init__(self, td)` |  |

---

## 函数详情

### `当前时间戳()`

当前 Unix 时间戳（秒）

**参数：**

无参数。

---

### `当前毫秒时间戳()`

当前毫秒时间戳

**参数：**

无参数。

---

### `当前微秒时间戳()`

当前微秒时间戳

**参数：**

无参数。

---

### `当前时间()`

当前本地时间，返回 日期时间对象

**参数：**

无参数。

---

### `当前UTC时间()`

当前 UTC 时间，返回 日期时间对象

**参数：**

无参数。

---

### `时间戳转日期时间(时间戳)`

Unix 时间戳转 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `时间戳` | `None` |  |

---

### `时间戳转UTC(时间戳)`

Unix 时间戳转 UTC 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `时间戳` | `None` |  |

---

### `日期时间转时间戳(dt)`

日期时间对象转 Unix 时间戳

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |

---

### `创建日期时间(年, 月, 日, 时 = 0, 分 = 0, 秒 = 0)`

创建日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `年` | `None` |  |
| `月` | `None` |  |
| `日` | `None` |  |
| `时` | `None` | （默认：0） |
| `分` | `None` | （默认：0） |
| `秒` | `None` | （默认：0） |

---

### `创建UTC日期时间(年, 月, 日, 时 = 0, 分 = 0, 秒 = 0)`

创建 UTC 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `年` | `None` |  |
| `月` | `None` |  |
| `日` | `None` |  |
| `时` | `None` | （默认：0） |
| `分` | `None` | （默认：0） |
| `秒` | `None` | （默认：0） |

---

### `格式化时间(dt, 格式 = '%Y-%m-%d %H:%M:%S')`

格式化日期时间对象为字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |
| `格式` | `None` | （默认：'%Y-%m-%d %H:%M:%S'） |

---

### `格式化ISO8601(dt)`

格式化为 ISO8601 字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |

---

### `格式化RFC3339(dt)`

格式化为 RFC3339 字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |

---

### `格式化中文日期(dt)`

格式化为中文日期字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |

---

### `解析时间(文本, 格式 = '%Y-%m-%d %H:%M:%S')`

解析时间字符串为 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |
| `格式` | `None` | （默认：'%Y-%m-%d %H:%M:%S'） |

---

### `获取星期名称(dt)`

获取星期名称（星期一~星期日）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |

---

### `获取月份名称(月)`

获取月份名称

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `月` | `None` |  |

---

### `获取月份天数(年, 月)`

获取指定年月的天数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `年` | `None` |  |
| `月` | `None` |  |

---

### `是闰年(年)`

判断是否为闰年

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `年` | `None` |  |

---

### `获取星期(dt)`

获取星期几（0=星期一, 6=星期日）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |

---

### `获取年日(dt)`

获取今天是当年第几天（1-366）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |

---

### `时间相加(dt, 间隔)`

时间加时间间隔，返回新 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |
| `间隔` | `None` |  |

---

### `时间相减(dt1, dt2)`

时间相减，返回 时间间隔

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt1` | `None` |  |
| `dt2` | `None` |  |

---

### `时间加天数(dt, 天数)`

时间加天数，返回新 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |
| `天数` | `None` |  |

---

### `时间加小时(dt, 小时)`

时间加小时，返回新 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |
| `小时` | `None` |  |

---

### `时间加分钟(dt, 分钟)`

时间加分钟，返回新 日期时间对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt` | `None` |  |
| `分钟` | `None` |  |

---

### `时间比较(dt1, dt2)`

比较两个时间，返回 -1/0/1（dt1早于/等于/晚于dt2）

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt1` | `None` |  |
| `dt2` | `None` |  |

---

### `时间等于(dt1, dt2)`

判断两个时间是否相等

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt1` | `None` |  |
| `dt2` | `None` |  |

---

### `时间早于(dt1, dt2)`

判断 dt1 是否早于 dt2

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `dt1` | `None` |  |
| `dt2` | `None` |  |

---

### `创建时间间隔(天 = 0, 秒 = 0, 微秒 = 0, 毫秒 = 0, 分钟 = 0, 小时 = 0, 周 = 0)`

创建时间间隔对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `天` | `None` | （默认：0） |
| `秒` | `None` | （默认：0） |
| `微秒` | `None` | （默认：0） |
| `毫秒` | `None` | （默认：0） |
| `分钟` | `None` | （默认：0） |
| `小时` | `None` | （默认：0） |
| `周` | `None` | （默认：0） |

---

### `时间间隔总秒数(间隔)`

获取时间间隔的总秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `间隔` | `None` |  |

---

### `时间间隔总毫秒数(间隔)`

获取时间间隔的总毫秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `间隔` | `None` |  |

---

### `时间间隔天数(间隔)`

获取时间间隔的天数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `间隔` | `None` |  |

---

### `时间间隔小时数(间隔)`

获取时间间隔的总小时数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `间隔` | `None` |  |

---

### `时间间隔分钟数(间隔)`

获取时间间隔的总分钟数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `间隔` | `None` |  |

---

### `开始计时()`

开始计时，返回当前时间戳

**参数：**

无参数。

---

### `结束计时(开始时间)`

结束计时，返回耗时秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `开始时间` | `None` |  |

---

### `休眠(秒)`

休眠指定秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `秒` | `None` |  |

---

### `毫秒休眠(毫秒)`

休眠指定毫秒数

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `毫秒` | `None` |  |

---

### `__init__(self, dt = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `dt` | `None` | （默认：None） |

---

### `年(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `月(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `日(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `时(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `分(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `秒(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

### `__init__(self, td = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `td` | `None` | （默认：None） |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `星期名称` | `['星期一', '星期二', '星期三', '星期四', '星期五', '星期六', '星期日']` |
| `月份名称` | `['一月', '二月', '三月', '四月', '五月', '六月', '七月', '八月', '九月', '十月', '十一月', '十二月']` |
