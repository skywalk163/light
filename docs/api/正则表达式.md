# 正则表达式 API

> 模块路径：`stdlib/正则表达式.py`
> 导入方式：`从 正则表达式 导入 函数名` 或 `导入 正则表达式`

---

## 函数列表

| 函数 | 说明 |
|------|------|
| `RegexObjNew(模式, 标志)` | 创建正则对象 |
| `RegexObjIsMatch(regex, 文本)` | 判断是否匹配 |
| `RegexObjSearch(regex, 文本)` | 搜索第一个匹配，返回 MatchResult |
| `RegexObjSearchAll(regex, 文本)` | 搜索所有匹配，返回 MatchResult 列表 |
| `RegexObjReplace(regex, 文本, 替换内容, 次数)` | 替换匹配内容 |
| `RegexObjSplit(regex, 文本, 最大分割)` | 按正则分割字符串 |
| `RegexObjFree(regex)` | 释放正则对象 |
| `MatchResultGroup(match, 组号)` | 获取匹配结果的捕获组 |
| `创建正则(模式, 忽略大小写_, 多行, 点匹配换行)` | 创建编译后的正则对象 |
| `是否匹配(模式, 文本, 标志)` | 判断文本是否匹配正则模式 |
| `搜索(模式, 文本, 标志)` | 搜索第一个匹配，返回 MatchResult |
| `全部搜索(模式, 文本, 标志)` | 搜索所有匹配，返回 MatchResult 列表 |
| `替换(模式, 文本, 替换内容, 次数, 标志)` | 替换所有匹配内容 |
| `分割(模式, 文本, 最大分割, 标志)` | 按正则分割字符串 |
| `提取所有(模式, 文本, 标志)` | 提取所有匹配的字符串列表 |
| `转义特殊字符(文本)` | 转义正则表达式中的特殊字符 |
| `__init__(self, 匹配成功, 整体匹配, 起始位置, 结束位置, 捕获组)` |  |
| `__init__(self, 模式, 标志)` |  |
| `_compile(self)` |  |

---

## 函数详情

### `RegexObjNew(模式, 标志 = 0)`

创建正则对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `标志` | `None` | （默认：0） |

---

### `RegexObjIsMatch(regex, 文本)`

判断是否匹配

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `regex` | `None` |  |
| `文本` | `None` |  |

---

### `RegexObjSearch(regex, 文本)`

搜索第一个匹配，返回 MatchResult

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `regex` | `None` |  |
| `文本` | `None` |  |

---

### `RegexObjSearchAll(regex, 文本)`

搜索所有匹配，返回 MatchResult 列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `regex` | `None` |  |
| `文本` | `None` |  |

---

### `RegexObjReplace(regex, 文本, 替换内容, 次数 = 0)`

替换匹配内容

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `regex` | `None` |  |
| `文本` | `None` |  |
| `替换内容` | `None` |  |
| `次数` | `None` | （默认：0） |

---

### `RegexObjSplit(regex, 文本, 最大分割 = 0)`

按正则分割字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `regex` | `None` |  |
| `文本` | `None` |  |
| `最大分割` | `None` | （默认：0） |

---

### `RegexObjFree(regex)`

释放正则对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `regex` | `None` |  |

---

### `MatchResultGroup(match, 组号 = 0)`

获取匹配结果的捕获组

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `match` | `None` |  |
| `组号` | `None` | （默认：0） |

---

### `创建正则(模式, 忽略大小写_ = False, 多行 = False, 点匹配换行 = False)`

创建编译后的正则对象

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `忽略大小写_` | `None` | （默认：False） |
| `多行` | `None` | （默认：False） |
| `点匹配换行` | `None` | （默认：False） |

---

### `是否匹配(模式, 文本, 标志 = 0)`

判断文本是否匹配正则模式

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `文本` | `None` |  |
| `标志` | `None` | （默认：0） |

---

### `搜索(模式, 文本, 标志 = 0)`

搜索第一个匹配，返回 MatchResult

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `文本` | `None` |  |
| `标志` | `None` | （默认：0） |

---

### `全部搜索(模式, 文本, 标志 = 0)`

搜索所有匹配，返回 MatchResult 列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `文本` | `None` |  |
| `标志` | `None` | （默认：0） |

---

### `替换(模式, 文本, 替换内容, 次数 = 0, 标志 = 0)`

替换所有匹配内容

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `文本` | `None` |  |
| `替换内容` | `None` |  |
| `次数` | `None` | （默认：0） |
| `标志` | `None` | （默认：0） |

---

### `分割(模式, 文本, 最大分割 = 0, 标志 = 0)`

按正则分割字符串

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `文本` | `None` |  |
| `最大分割` | `None` | （默认：0） |
| `标志` | `None` | （默认：0） |

---

### `提取所有(模式, 文本, 标志 = 0)`

提取所有匹配的字符串列表

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `模式` | `None` |  |
| `文本` | `None` |  |
| `标志` | `None` | （默认：0） |

---

### `转义特殊字符(文本)`

转义正则表达式中的特殊字符

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `文本` | `None` |  |

---

### `__init__(self, 匹配成功 = False, 整体匹配 = '', 起始位置 = 0, 结束位置 = 0, 捕获组 = None)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `匹配成功` | `None` | （默认：False） |
| `整体匹配` | `None` | （默认：''） |
| `起始位置` | `None` | （默认：0） |
| `结束位置` | `None` | （默认：0） |
| `捕获组` | `None` | （默认：None） |

---

### `__init__(self, 模式 = '', 标志 = 0)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |
| `模式` | `None` | （默认：''） |
| `标志` | `None` | （默认：0） |

---

### `_compile(self)`

**参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `self` | `None` |  |

---

## 常量

| 常量名 | 值 |
|--------|-----|
| `忽略大小写` | `_re.IGNORECASE` |
| `多行模式` | `_re.MULTILINE` |
| `点号匹配换行` | `_re.DOTALL` |
| `Unicode匹配` | `_re.UNICODE` |
| `ASCII匹配` | `_re.ASCII` |
| `VERBOSE` | `_re.VERBOSE` |
| `obj` | `RegexObj(模式=模式, 标志=标志)` |
| `m` | `regex._regex.search(文本)` |
| `捕获组` | `list(m.groups())` |
| `结果` | `[]` |
| `标志` | `0` |
| `捕获组` | `list(m.groups())` |
| `m` | `_re.search(模式, 文本, 标志)` |
| `结果` | `[]` |
