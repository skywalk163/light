# v7 合并后失败用例根因聚类工单

- 基线 commit：`7593616b`
- 跑分：`144 failed / 3239 passed / 99 skipped / 6 errors`（3482 收集）
- 复现命令：`python -m pytest tests -q --no-header -p no:cacheprovider --tb=short -rf`

> **⚠ 基线数字必须每轮自测，不要沿用本文档里的历史数字。**
> 第 4 轮同机自测 `24809c76`：**72 failed / 3313 passed / 99 skipped / 6 errors**（collected 3484，269.91s）。
> 而上一轮记录的是 `77 failed / 3308 passed / 99 skipped / 6 errors`（同为 3484）——**同 HEAD 同机差 5 例**。
> `skipped / errors / collected` 三项完全一致，只有 failed/passed 在抖。已定位一个抖动源：
> `test_duan_compile_and_run_product[L1_baihua/10_引Python画笑脸.light]` 的 Python 块调 `turtle` +
> `s.exitonclick()`，会阻塞到 180s 超时，跑不跑通取决于机器负载。**不要单独跑这个用例**。
> 结论：拿旧数字做减法会凭空多算/少算若干例，必须 `git stash` 自测同会话基线。
- 背景：段言(duan)/光明(light) 两库合并。**绝大多数失败是「duan 带进来、从未在 light 编译器上跑过」的测试，不要假设它们曾经通过过。**

## 全局硬规则（分发时必须随单转达）

1. **禁止改断言让测试变绿。** 若断言确实写错，必须写明错在哪、正确期望是什么、依据（引规范文档 `文件:行号`）。
2. **禁止改被测输入去迁就编译器。** 例如不要改 `bootstrap/bootstrap_level5.light` 来绕过 parser——它是被测输入。
3. **每张单收尾必须跑全量回归**，不能只看本簇转绿。词法/代码生成类改动尤其容易打穿现在绿着的用例。
4. **PowerShell 5.1 环境**：没有 `&&`（用 `;`），没有 heredoc；pytest 中文输出会被 GBK 解码成乱码，必须 `| Out-File -Encoding utf8 <文件>` 落盘再读。
5. 临时文件写 `.scratch/`（已 gitignore），用完删除；提交前 `git status --short` 必须干净。
6. 跑自举测试会重新生成 `bootstrap/bootstrap_level3.py`、`bootstrap_level4.py`（被 git 跟踪）。跑完用 `git checkout -- <文件>` 还原，**不要留下未提交的产物改动**。

## 已定口径（用户裁决，执行者不得自行更改）

- **遍历循环语序消歧：按连接词区分。** 前置工作是先收窄 `之` 的用法——目前 `之` 在 L0 和 L1 都能用，是消歧的障碍。必须先定「哪个连接词归哪种语序」，再动 parser。
- **类型检查 VARIABLE 档语义：以 light 现行实现为准**，即只在「注解与推断类型冲突」时告警（`V001`），对未注解变量不发声。因此 `tests/test_level6_types.py` 里期望「缺注解 → 告警」的断言**需要重写**，且必须在提交信息里留痕说明这是口径变更、依据是本决策。

---

# 优先级 P0 —— 静默错编（编译通过但跑出错误结果）

这类最危险：不报错，结果是错的。目前只是因为循环变量/参数恰好触发 NameError 或 arity 检查才暴露。

## 单 01 · 遍历循环族（28 例，最大簇）

**受影响**：`tests/e2e/test_e2e_chain.py` 24 例 + `tests/test_core_coverage.py` 2 例 + `tests/e2e/test_e2e_chain.py` 元组解包 2 例

三个子缺口，**必须同一人做、按顺序做**：

### 01-A 语序颠倒（6 例，静默错编，最高优先）

- 代表用例：`test_duan_run[L1_baihua/05_遍循环.light]`、`[L1_baihua/07_字典.light]`、`[L1_baihua/09_异常.light]`（各含 run + product）
- 症状：`examples/L1_baihua/05_遍循环.light:7` 源码 `遍 水果 之 f:`，产物生成 `for 水果 in f:` —— **变量与可迭代对象被调换**
- 根因：`src/parser_stmt.py:2048-2052` 注释写死「语序：遍 变量 之 可迭代对象」，无条件把第一个 token 当变量
- 规范依据：`docs/L1_白话体语法规范_v4.0.md:566` 明确 L1 是 `遍 可迭代对象 之 变量`，`:568` 举例 `遍 典 之 k` → `for k in 典`。而 L0（`examples/L0_core/04_遍_循环.light:7` `遍 果 于 水果：`）是变量在前，L0 全绿 —— parser 只实现了 L0 一侧
- 判定：**编译器缺陷**
- 难度：中等（受口径约束，见上）

### 01-B `为` 未列入连接词（18 例）

- 代表用例：`test_duan_run[L3_domain/demo1_sql.light]`、`[L4_python/all_in_one_demo.light]`、`test_duan_compile_and_run_product[E阶段_L3L4原生语法/E1_L3_SQL原生.light]`
- 报错：`原因: 遍历循环期望'在'、'之'、'于'或'中的'，但得到 「为」`
- 根因：`src/parser_stmt.py:2046` `FOREACH_CONNECTORS = frozenset({'之','在','于','中的'})` 缺 `为`；`:2061` 不在集合里直接 `_error`
- 规范依据：`docs/L1_白话体语法规范_v4.0.md:601` 明说「将 `为` 识别为连接词（等价于 `之`）」；`docs/guide/常见问题.md:244` 同口径
- 判定：**编译器缺陷，且编译器与自己的规范文档矛盾**
- 难度：简单（加一个元素），但 **必须在 01-A 之后做** —— 否则只是把「解析报错」换成「静默错编」

### 01-C 多变量/元组解包目标（4 例）

- 用例：`tests/test_core_coverage.py::TestStmtCoverage::test_foreach_dict`（:165）、`test_foreach_tuple_unpack`（:171）；`test_duan_run[Z阶段_可选类型系统/Z7...]`、`[L4_python/demo5_sklearn_iris.light]`
- 报错：`遍历循环期望'在'、'之'、'于'或'中的'，但得到 ，`
- 根因跨三层：
  - `src/parser_stmt.py:2049-2061` 只读一个循环变量
  - `src/ast_nodes_v3.py` `ForeachStmt.__slots__=('variable','iterable','body','is_async')`，`variable` 是单字符串
  - 两个代码生成器都需发射 `for a, b in ...`
- 附带决策：`遍历 键,值 之 字典` 是否自动补 `.items()`
- 判定：**编译器缺陷**；难度中等，属完整小特性，**不要只补 parser**

### ⚠ 本单已完工（v7 收尾第 4 轮复跑确认）

修复在 `4827b537`（标题：`修复：单 01 遍历循环族 —— 语序消歧、为 连接词、元组解包`）落地。第 4 轮按用例 ID 逐个复跑：

- **01-A 语序颠倒**：`test_duan_run[L1_baihua/05_遍循环.light]`、`[07_字典.light]`、`[09_异常.light]`
  的 run + product 六例**全绿**（靶向 `-k "L1_baihua"` 跑 28 例得 25 passed，3 个红见下）。
- **01-B `为` 连接词**：`test_duan_run[L3_domain/demo1_sql.light]` 全绿。
  `[E阶段_L3L4原生语法/E1_L3_SQL原生.light]` 仍红，但**根因已经换了**：现在报
  `[运行错误] name 'l3_sql_成绩_q4' is not defined`（产物已跑到「查询 q4」那一步才炸），
  不再是原来的 `原因: 遍历循环期望'在'、'之'、'于'或'中的'，但得到 「为」`。
  即遍历循环这一层已通，剩下的是 L3 SQL 参数化查询的变量命名缺陷，**属另一个根因，不要计进本单**。
- **01-C 元组解包**：`tests/test_core_coverage.py::TestStmtCoverage::test_foreach_dict`、
  `test_foreach_tuple_unpack` 全绿（该文件当前零红）。

**靶向 `-k` 过滤时的两个坑**（第 4 轮踩到）：

1. `-k "L1_baihua"` 会把 `L1_baihua/10_引Python画笑脸.light` 选进来，它的 Python 块调 `turtle` +
   `s.exitonclick()`，阻塞到 180s 超时才失败，单跑 28 例花了 175s。**这个用例不要单独跑。**
   它在全量跑里反而是绿的——这正是 failed 数在 72/77 之间抖动的一个来源。
2. `hello.light` 的 run + product 两例仍红，**与本单无关**，是单 02 紧凑写法族的残留，见单 09-B 的补充。

---

## 单 02 · 紧凑写法（无空格）分词族（15 例）

**受影响**：`tests/test_level6_indent.py` 4 + `tests/test_level6_lexer.py` 3 + `tests/test_lexer.py` 1 + `tests/unit/test_lexer.py` 1 + `tests/unit/test_parser.py` 1 + `tests/_test_null_safety.py` 1 + `tests/e2e/test_e2e_chain.py` 4

实测 实际 vs 期望：

- `设x为10` → `IDENTIFIER '设x'` + `为` + `10`；应为 `设`(KW) `x`(ID) `为`(KW) `10`(NUM)
- `如果x大于0` → `IDENTIFIER '如果x'` + …；应切开
- `遍历i从1到10` → `IDENTIFIER '遍历i'` + …
- `打印x` → `IDENTIFIER '打印x'`
- `left自right` → `IDENTIFIER 'left'` + `IDENTIFIER '自right'`
- `甲加1。` → `IDENTIFIER '甲加1'` + `PERIOD`（期望 4 个 token，实得 2）
- `n乘阶乘(n-1)` → `n` + `IDENTIFIER '乘阶'` + `KEYWORD 乘` + …（`tests/unit/test_parser.py:65`：`'n乘阶' != 'n'`）

**根因（单点）**：`src/lexer.py:1309-1315`，`_tokenize_identifier_or_keyword` 的「汉字后紧跟 ASCII」后缀合并分支——`_tokenize_chinese_sequence` 已经正确切出 `KEYWORD 设/如果/打印/自`，这个分支又把紧邻 ASCII 后缀无条件粘回去，并把 token 类型从 KEYWORD 降级成 IDENTIFIER。

第二个缺口：纯汉字序列不在内部运算符动词处切分（`甲加`、`乘阶乘`，`加`/`乘` 在 `src/keywords.py` 的 `VERB_ARITY` 里）。

**规范依据**：`docs/superpowers/specs/2026-07-01-level6-type-annotation-design.md`（light 自己的规格，非 duan）2.1 节「最长前缀匹配」、2.2 节把 `设/为/如果/遍历/加/减/乘/除` 全列为支持无空格分词、2.4 节逐 token 给出期望，与 `tests/test_level6_lexer.py:41-53` 断言完全一致。另 `docs/bug_fixes_compact_notation.md` 记录 duan 侧修过同族问题（Bug 1、Bug 4），其修复要点在 light 的 `src/lexer.py:1383-1397` 与 `:500-508` 都还在 —— **不是机制缺失，是 1309-1315 这个后加分支把已修好的路径推翻了一半**。

**重要澄清**：`tests/test_level6_indent.py` 那 4 例**不是缩进问题**。INDENT/DEDENT 生成正确（同文件 2 例通过），4 个失败全部卡在 `tokens[indent_idx+1].type == KEYWORD` 而该位置是 `打印x`/`打印i`/`打印c`。缩进代码一行都不用动。

**风险提示**：1313 行那个合并是为 `分割Pointer_内部`、`读取LSP消息`、`阶段1标题` 这类混排名字加的。收窄条件时必须让 `user_definitions`（`src/lexer.py:500-508` 已预扫描）当唯一放行依据。

> ### ⚠ 本单已完成（见提交 `单 02 紧凑写法分词族 + 单 15 L4 嵌入块缩进回归闭合`），下列工单原判**经实测推翻**，后来人不要照原判理解
>
> 1. **`src/lexer.py:1382-1397`（原 `:1344-1359`）是死代码。** 该 `elif tokens and tokens[-1].type == KEYWORD:` 缩进 20，与 `:1304` 的 `if tokens:` 同级 —— 进入该 elif 的前提是 `tokens` 为假，条件却要求 `tokens` 为真，永假。所以「`读取N字节` 靠这段的 `ALL_VERB_ARITY` 校验保护」**从未生效过**；它实际是靠 1309-1315 合并 `N` + 1317-1342 续接 `字节` 得到的。本次收窄把这份「已知名校验」搬进了活路径。死代码本身**保留未删**（provably unreachable，删它属独立的结构清理，不与本单混提）。
> 2. **`分割Pointer_内部` 与 `阶段1标题` 根本不走 KEYWORD 分支**，上面的风险提示点错了例子：`分割` 在 `STDLIB_VERB_ARITY` 而不在 `VERB_ARITY`，`:1656-1660` 输出的是 IDENTIFIER，走 `:1306-1307`；`阶段1标题` 的 ASCII 后缀首字符是数字，`suffix[0].isalpha()` 本来就为假，靠 `:1275-1279` 的混排白名单整体返回。**收窄 KEYWORD 分支对这两个例子零影响。**
> 3. **真正会被打穿的是「点号后的成员方法名」，工单完全没提。** `examples/L3_domain/demo4_echarts.light:64` `l3_chart.导出JSON()`、`:69` `导出HTML(...)`、`examples/chat_bot/主.light:42` `.段言到Python(...)` —— `导出`/`到` 都是关键字，只凭 `user_definitions` 放行会把它们拆开。因此放行条件必须补「紧邻前一个源字符是 `.`」这一档。**注意不能写 `tokens[-2].type == DOT`**：`_tokenize_identifier_or_keyword` 的 `tokens` 是 `:1262` 新建的局部列表，看不到外层已发射的 DOT。
> 4. **`n乘阶` 不是 lexer 产出的 token**，是 `src/parser_expr.py:1050-1058` 的相邻 IDENTIFIER 合并把 lexer 吐的 `n` + `乘阶` 粘起来的。但根因仍在 lexer：`乘阶乘` 在**词首运算符**处被当成复合词前缀（原意保护 `加法`/`减法`），跳过首个 `乘` 后扫到第二个才切。收窄为「运算符之后的剩余部分本身是已知名字（`阶乘` ∈ `STDLIB_VERB_ARITY`）时不算复合词前缀」即可，**parser_expr 无需任何改动**。
> 5. **`甲加1` 的根因是「词尾运算符豁免」只看纯汉字序列右边界、不看其后紧邻字符**（`src/lexer.py` 探测循环与输出循环两处「`scan_pos + sub_len >= len(full_identifier)`」）。判据补成「汉字序列后紧跟 ASCII 数字则不算词尾」。
> 6. **收窄的已知边界**：只有**未声明的自由名字**会被拆开。实测 `设 衰减1 为 0.9`、`导出 衰减1`、`段落 优化 接收 衰减1：` 三种声明都让 `衰减1` 进 `user_definitions`、在 `:1275-1279` 整体返回。全仓仅 `积木库/blocks/函数/函数Adam.light:5` 与 `积木库/blocks_v4/函数/函数Adam.light:5` 的 `衰减1`/`衰减2` 命中该边界（既没声明也没导出，本来就是 NameError，且这两个文件是自动生成的快照、从未运行）。
> 7. **A/B 全仓 token 流实测**：以 HEAD 版与改后版 lexer 分别 tokenize 全仓 37255 个 `.light`，**37203 个 token 流逐 token 完全一致，仅 52 个文件有差异**，且 52 处全是「关键字被正确切开 / 运算符被正确切出 / 4 个嵌入块 LexerError → 正常闭合」，**没有任何文件从 OK 变成 ERR，也没有任何文件的 token 数量异常塌缩**。



**难度**：中等。**必须带全量回归**（`test_lexer.py`、`tests/unit/`、`test_level6_*`、e2e 产物链一起跑）。

**附带**：`tests/_test_null_safety.py::TestNullSafetyArithmetic::test_operation_without_unwrap`（:123）属同一根因。测试输入 `设值为空。设结果为值加1。` 无空格，RHS 被并成单个标识符 `值加1`，不生成 BinaryOp，所以可空运算检查无从触发。检查逻辑本身正确（`src/type_inferencer.py:1800-1823`，加空格后正常报错）。本单修好后此例自动转绿。

---

## 单 03 · 内置名劫持局部变量与成员方法（6 例）

**受影响**：`tests/test_context_manager.py` 2 + `tests/test_level6_types.py` 2 + `tests/e2e/test_e2e_chain.py` 2

实测错译：

- `设 映射 为 {"a":1}` 后 `映射["a"]` → 生成 `map()["a"]` → `TypeError: map() must have at least two arguments`
- `设 内容 为 f.读取()` → 生成 `内容 = input(f)`（对照 `f.抓取()` 正常）
- `计数.读取()` → 生成 `input(计数)` → `OSError: reading from stdin while output is captured`
- `examples/file_search/主.light:20` `文件名 包含 关键词` → 生成 `文件名 @@contains@@ 关键词` → `SyntaxError`（占位符泄漏，见单 08）

**根因**：`src/code_generator.py`

- `:303` `'映射': 'map'`、`:195` `'读取': 'input'`（同源 `src/verb_info.py:167/174`）
- `:2100` 内置替换只对用户自定义函数设护栏：`if expr.name in self.builtin_map and expr.name not in self._user_defined_functions` —— `_user_defined_functions` 只在 `:474/:477`（import 符号）与 `:1244`（段落定义）填充，**局部变量从不进去**
- `:2242-2243` 成员访问同理：`builtin_target and expr.member not in self.method_name_map` → `return f"{builtin_target}({obj}, {args_str})"`

`src/code_generator.py:435-449` 的 docstring 已把这个坑写清楚（「显式导入是用户最强的意图表达，必须压过内置映射」），护栏思路对，覆盖面只做了 import 一路。

**判定**：**编译器缺陷，用户可见的正确性问题**。任何用户拿 `映射`/`读取`/`范围`/`包含` 当变量名或方法名的程序都会静默错译 —— **优先级应远高于它在失败清单里只占 6 例**。

**难度**：中等。局部变量需按作用域收集；成员访问路径应直接禁止对 `obj.成员` 做 builtin 函数式改写（除非 `obj` 是内置命名空间）。`:2238` 注释说明「内置式优先」是有意设计，收窄时容易伤到 `追加→append` 这类，**必须跑 stdlib/积木回归**。

### ⚠ 本单已完工（v7 收尾第 3 轮核实）——上面整段描述的是「修复前」状态

修复已在 `ec65486a`（提交标题：修复：单 03 内置名劫持 + 单 18 GBK 撞 emoji（回收 8 例））落地，`git merge-base --is-ancestor ec65486a HEAD` 退出码 0。当前 HEAD 的实际状态：

- 局部变量走**独立集合** `_local_variables`（`src/code_generator.py:84`），不复用 `_user_defined_functions`（后者是模块级只增不减，`:500-503` 注释解释了为什么必须分开）
- 配套设施：`_bind_local`(`:505`)、`_push_local_scope`(`:530`)、`_pop_local_scope`(`:540`)、`_shadows_builtin`(`:544`)
- **10 个 `_bind_local` 插桩点**覆盖六类绑定：`:955` 普通赋值、`:973` 解包赋值、`:1092` VarDecl（注释明确写了「在 value 生成之后登记」以避免 `设 映射 为 映射(f,列)` 右侧自遮蔽）、`:1219` 遍历/for 循环变量、`:1335` 段落形参、`:1373` `捕获…为`、`:1837`/`:1852` `使用…为`、`:1883` 异步作用域结果、`:2037` 类方法形参
- 消费点 `:2216` `shadowed = self._shadows_builtin(expr.name)` → `:2219`
- 成员访问路径已按建议收窄到 `:2385-2392`，条件加了 `and obj == '_light_builtin'`，即只有内置命名空间对象才做函数式改写

**行号漂移**：`'映射': 'map'` 实为 `:310`（原写 `:303`）；`'读取': 'input'` 实为 `:202`（原写 `:195`）；`'范围': 'range'` 在 `:221`；护栏实为 `:2219`（原写 `:2100`）；成员访问实为 `:2385-2392`（原写 `:2242-2243`）；`_user_defined_functions` 写入点实为 `:481`/`:484`/`:1328`（原写 `:474`/`:477`/`:1244`），grep 确认**无第 4 处**。

**「收窄会伤到 `追加→append`」的担忧不成立（实测）**：`builtin_map` 共 203 条、`method_name_map` 共 22 条，同名交集 9 条（`包含/去除空白/反转/开头/截取/排序/结尾/转大写/转小写`）被 `expr.member not in method_name_map` 条件挡在函数式改写之外，走 `obj.append()` 式方法调用，与 `obj == '_light_builtin'` 门无关。`长度→len(obj)`、`包含→(x in obj)`、`连接→_light_join` 都在成员分支**之前**单独 return。

### ⚠ 原「6 例」归属全部有误（逐个单跑核实）

- **`tests/test_context_manager.py` 2 例**（`f.读取()` 相关）：`test_with_body`(:71-80)、`test_with_file_read`(:231-240) **已绿**。产物为 `内容 = f.读取()`，不再是 `input(f)`。该文件当前 6 个红是**另一个根因**：`TestAsyncWith::test_async_with_basic/await/return`、`TestWithAST::test_ast_async_with`、`TestWithCodeGen::test_codegen_async_with`、`TestWithEndToEnd::test_async_with_file` —— 见**单 07**（第 3 轮曾误当新缺陷另立「单 21」，第 4 轮核实与单 07 同一 bug、同一根因位置，已合并删除）。
- **`tests/test_level6_types.py` 2 例**：**归属错误**。该文件当前只有 1 个红（`TestTypeParser::test_parse_function_with_parens_is_single_tuple_param` :768，`assert 2 == 1`，TypeParser 剥括号破坏「元组参数 vs 联合参数」区分），与内置名劫持正交；grep 确认该文件里根本没有 `映射/读取/计数/内容` 这类被测输入。
- **`tests/e2e` 2 例**（`文件名 包含 关键词`）：**归属错误，属单 08**。真根因是中缀运算符占位符 `@@contains@@` 从未被 `operator_map` 转成 `in`：`包含` 在 `src/parser_core.py:270` 被映射成 `'@@contains@@'`，而 `BinaryOp` 分支（`:2186-2190`）只做 `operator_map.get(op, op)`，查不到就原样透传（实测 `'@@contains@@' in operator_map == False`，该表里没有任何含 `@@` 的键）。对照成员形式 `文.包含("a")` → `("a" in 文)` 正常，说明**成员形式已好、中缀形式仍漏**。工单自己的括注已写「见单 08」，却仍把它计进单 03 的例数。

**残留（已裁决不修）**：单遍生成下「先用后声明」仍会误伤 —— `打印(映射)。设 映射 为 {"a":1}。` 生成 `print(map())` + `映射 = {"a": 1}`。消除它需要给 CodeGenerator 加段落级绑定预扫描（单遍→两遍）。因为这本身就是 Python 里的 `UnboundLocalError`（用户错误代码），成本高收益低，**决定不做**。

**新发现（另立工单）**：形参名恰好是内置动词名时，`名[...]` 被解析成调用而非索引 —— `段落 甲 接收 映射: 打印(映射["k"])` → `def 甲(映射): print(映射(["k"]))`。对照局部变量档 `设 映射…` 与普通名形参 `表格` 都正确产出 `["k"]` 索引。这是 parser 层 `名[...]` 消歧缺陷（AST 就错了，codegen 的 shadow 是对的），见新增的**单 22**。


---

## 单 04 · 嵌套调用丢参数（2 例）

- 用例：`test_duan_run[L0_core/05_段_函数定义.light]` + product
- 症状：源码 `examples/L0_core/05_段_函数定义.light:28` 是 `印(求和(10, 20))`，`:14` 定义 `段 求和(a, b)：`，产物生成 `print(求和(10))` —— **实参 `20` 被吞掉**
- 报错：`求和() missing 1 required positional argument: 'b'`
- 判定：编译器缺陷，静默错编类（恰好触发 arity 检查才暴露）
- 难度：中等；适合独立分发

### ✅ 单 04 已完工（v7 收尾第 6 轮）

**标题是错的：「嵌套」不是触发条件。** 顶层裸调 `求和(10, 20)` 一行即复现，赋值 `设 x 为 求和(10,20)` 同样复现。
准确表述应为「**内置动词名作段名时，括号式调用被 arity 截断丢参**」。

**根因** `src/parser_expr.py:640`（原 `while not RPAREN and collected < arity`）。
`求和` 在 `src/keywords.py` 的 `VERB_ARITY` 里 arity=1，被 lexer 切成 KEYWORD 而非 IDENTIFIER，
走进定参分支；括号式收参循环用 arity 封顶只收 `10`，紧随的「跳过剩余 token 直到右括号」把 `, 20` 整段吞掉。
**不是只丢最后一个**：三参 `求和(1,2,3)` 会丢掉 arity 之后的全部。
三档取证已分别排除 lexer（token 流含两个实参与逗号，正确）与 code_generator（忠实反映错误 AST），
**parser 是第一个出错的档**。

**口径**：arity 上限的存在意义是给**无括号并置式**（`求和 10 20`）消歧——那种写法没有分隔符，
必须靠元数知道抓几个。而括号式的 `(` … `)` 与 `,` 已把边界写死，再套 arity 纯属误用。
故只去掉括号式那一侧的上限，无括号分支不动。单向放宽：实参数 ≤ arity 的旧输入产物逐字节不变。

**触发条件**（三条同时成立）：调用目标是 `VERB_ARITY` 内置动词 + 括号式 + 实参数 > 该动词 arity。
与实参类型无关（中文数字、变量名表现一致，工单猜的「数字相关」不成立）。

**工单漏了一处用户可见样例**：全仓扫描另有 `edu/课程5_函数/示例.light:4,23` 同根因命中（教程链路，pytest 分母外）。

**回归**：`test_duan_run[L0_core/05_段_函数定义.light]` 与 product 变体**双双转绿**（2 例，与工单一致）。

**另立单（正交，勿并入本单）**：定参括号分支尚不支持 `kwarg=value` 形态。
`examples/L2_wenyan/主程序.light:58` 的 `排序(学生列表, 依据 = 段(x) …)` 会撞上它。
该文件基线本就因 `:6` 「模块」保留字 ParseError（新单 B / L2 语法体系）到不了 codegen，本单不改善也不恶化它。


---

# 优先级 P1 —— 高杠杆低成本

## 单 05 · `TypeChecker` / `GradedTypeChecker` 符号错位（13 例，最高性价比）

**受影响**：`tests/test_level6_types.py` 9（`:438/455/468/482/505/1783/1791/1801/1811`）+ `tests/test_core_coverage.py` 4（`:287/294/301/308`）

**报错**：`TypeError: TypeChecker.check() got an unexpected keyword argument 'inferencer'` / `takes 2 positional arguments but 3 were given`

**根因**：`src/type_checker.py` 并存两个检查器

- `:620` `class TypeChecker` —— 注释「光明独立类型检查器（CLI 使用）」，`__init__(self, strict=False)`，`check(self, module)`
- `:819` `class GradedTypeChecker` —— 注释「分级类型检查器（编译器集成用）」，`__init__(self, config)`，`check(self, module, inferencer=None)`，`:847-856` 按 `TypeCheckLevel` 逐级 gate

测试导入 `TypeChecker` 却按 `GradedTypeChecker` 契约调用。`TypeChecker(config)` 不报错（config 被当成 `strict` 吃掉），到 `.check()` 才炸。

**实测验证**：把 `test_core_coverage` 那 4 例改用 `GradedTypeChecker(TypeCheckerConfig(...)).check(module, inf)`，全部通过。分级机制在 light 里**完整存在且工作正常**（`test_check_level_ordering`、`test_file_directive_parse` 本来就是绿的）。

**落地方式（取方式 1）**：改测试 import `TypeChecker → GradedTypeChecker`（两个文件的 import/构造），风险最低。方式 2（改源码把分级检查器以 `TypeChecker` 名导出）会与 light 既有同名遗留类撞名，需连带处理所有 `TypeChecker(strict=...)` 调用点，风险高。

**这不是「改断言变绿」**：断言（`assert results is not None`）与被测行为都不变，只是纠正指向了错误类的符号名。

**但 9 例中只有 4 例靠改符号就能全绿**，剩下 5 例还有语义层问题：

- VARIABLE 档：`src/type_checker.py:955-972` `_check_stmt_variables` 只在「有注解且与推断类型不兼容」时发 `V001`，对 `type_annotation=None` 不发声。测试要「缺注解 → 告警」。**按已定口径（以 light 现行实现为准），这些断言需重写**，并留痕说明依据。
- SIGNATURE 档：`:912-918` 注释写死「SIGNATURE 级别下总是产生警告」，测试的 `_make_segment`（`tests/test_level6_types.py:413-421`）默认 `return_type=None`，于是 S004 污染了假设「只含变量级告警」的结果集。需给 `_make_segment` 显式 `return_type`。

**难度**：符号层极低；语义层中等。**建议拆两张单**。

### ⚠ 本单已完工（v7 收尾第 4 轮核实）——上面整段描述的是「修复前」状态，13 例已全绿，本轮 0 例可回收

修复在 `9393cbf8`（标题：`修复：单 09 品牌陈旧期望 + 单 05 类型检查器符号错位 + 单 06 己(self) 未映射`）落地。
**符号层与语义层两档都已做完**，不存在工单 :193 建议的「剩一张单」：

- 符号层：`tests/test_level6_types.py:37-38` 已 `from type_checker import GradedTypeChecker, ...`（不再 import `TypeChecker`），
  10 处构造/调用点（`:444/:463/:479/:492/:506/:527/:1812/:1822/:1835/:1845/:1855`）全部按
  `GradedTypeChecker(config).check(module, inferencer=...)` 契约；`tests/test_core_coverage.py:274/:282` 与 `:314/:323` 同样已改。
- 语义层 SIGNATURE 档：`_make_segment`(`:413-427`) 的 `return_type` 默认值已从 `None` 改成 `'空'`，
  docstring 直接引 `src/type_checker.py:912-918` 解释 S004 为何会污染只想观察变量级告警的结果集
  —— 正是工单 `:191` 要求的那处修复。
- 语义层 VARIABLE 档：五处断言已按「以 light 现行实现为准」重写，且每处上方都留了
  `# 口径变更(v7 合并)：…原 duan 期望「缺注解→告警」已废止` 注释（`:433`/`:452`/`:512`/`:1815`/`:1828`）。
  其中 `:453` `test_variable_level_silent_on_missing_annotation` 与 `:1829` 同名用例是「反向断言」形态
  （断言 V001 **不**产生），比原 duan 断言更能测住 light 语义。

**靶向复跑证据**：`pytest tests/test_level6_types.py tests/test_core_coverage.py` = **1 failed / 247 passed**。
唯一的红是 `TestTypeParser::test_parse_function_with_parens_is_single_tuple_param`
（`tests/test_level6_types.py:776`，`assert 2 == 1`，`布尔.param_types` 得到 `[数, 元]` 两项而非一个元组参数），
**与单 05 正交** —— 工单自己在单 03 的 `:147` 已经查清这一点（TypeParser 剥括号破坏「元组参数 vs 联合参数」区分）。
全量基线里 `tests/test_core_coverage.py` 零红、`tests/test_level6_types.py` 仅此一红，可交叉验证。

**「13 例」的构成无法再对账（行号已失效）**：工单给的 9 个行号（`:438/455/468/482/505/1783/1791/1801/1811`）
是**修复前**那一版 `tests/test_level6_types.py` 的坐标，本单修复本身就重写了这些断言，行号已整体漂移
——例如现在的 `:1783`/`:1791` 落在 `TestTypeScenarioValid::test_class_field_annotation` 的三引号源码块里，
与类型检查器无关。按当前文件重新点名，真正属本单的是
`TestTypeCheckerWithAnnotations` 6 例（`:434/:453/:469/:485/:496/:513`）
+ `TestTypeScenarioChecker` 5 例（`:1808/:1816/:1829/:1839/:1849`）
+ `tests/test_core_coverage.py::TestTypeCheckerCoverage` 4 例（`:287/:294/:301/:308`）= **15 例**，全绿。
数量与工单的「13」对不上不影响结论，仅供后来人别再拿旧行号找用例。

---

## 单 06 · `己`(self) 未映射（11 例）

**受影响**：`tests/integration/test_class_system.py` 5 + `tests/e2e/test_module_e2e.py` 1 + `tests/unit/test_examples_run.py` 1 + `tests/e2e/test_e2e_chain.py` 4

**报错**：统一 `NameError: name '己' is not defined`

**实测产物**（`矩形` / `计数器` 类）：

```python
class 矩形:
    def __init__(self, 宽, 高):
        self.宽 = 宽          # 赋值目标位置正确
    def 面积(self):
        return (己.宽 * 己.高)  # 表达式读取位置错误，己 未定义
```

**根因**：`src/code_generator_unified.py:1170-1171`

```python
elif is_instance(expr, 'Identifier'):
    return self._sanitize_name(expr.name)   # 缺 己→self 处理
```

对照旧生成器 `src/code_generator.py:2041-2046` **是有的**：

```python
if self._in_class_method:
    if name == '己':          return 'self'
    if name.startswith('己.'): return 'self.' + name[2:]
```

`UnifiedCodeGenerator` 未移植这段。AST 侧佐证：赋值左值是 `Identifier('self.值')`（parser 的 SelfAssignment 通路已重写），表达式位置产出 `Identifier('己.值')`（`src/parser_expr.py:771`）。

**判定**：编译器缺陷，窄而深。同文件 5 个不用 `己` 的用例全绿；`class_example.light`、`class_complete.light`、`class_access_control.light` 三个示例也是通过的 —— **不是类系统整体坏了**。

**难度**：低。现成最小复现两个：`tests/e2e/test_module_e2e.py:182-196` 的 `矩形`（3 行）、`examples/student_management.light`（全量）。适合独立分发，优先做。

---

## 单 07 · `等待`(await) + 成员访问解析崩溃（6 例）—— 已完工（v7 收尾第 4 轮）

> **修复已落地**：`src/parser_expr.py` 的 `等待` 分支判据从 `peek2.type != LPAREN`
> 改成 `peek2.type not in (LPAREN, DOT)`，即 DOT 归入「后面是 await 表达式」一侧。
> 6 例红转绿，另加 7 例回归用例（`tests/test_context_manager.py::TestAwaitMemberAccess`）。
> 回归算术与红绿名单见本单末尾。下面保留原始分析，供理解根因。

**受影响**：`tests/test_context_manager.py` 的 `test_async_with_basic`(:99)、`test_async_with_await`(:108)、`test_async_with_return`(:127)、`test_ast_async_with`(:163)、`test_codegen_async_with`(:214)、`test_async_with_file`(:242)

**报错**：`parser_core.ParseError: 无法识别的语法元素：'.'`

**隔离实验结论**（决定性）：

- `使用 异步 … 为 f:` + 体 `输出(f)`（无 await）→ OK，生成 `async with open("t") as f:`
- `等待 读取(f)`（await + 普通调用）→ OK
- `等待 f.读取()`（await + 成员访问）→ **FAIL**
- 无 with 的裸 `等待 f.读取()` → 同样 FAIL

**所以 `async with` 完全实现、`await` 也实现，崩溃点是 `等待` + 成员访问的组合，与 with 无关。**

**根因**：`src/parser_expr.py:340-355`

```python
if tok.type == KEYWORD and tok.value == '等待':
    next_tok = self._peek(1)
    if next_tok and next_tok.type == IDENTIFIER:
        peek2 = self._peek(2)
        if peek2 and peek2.type != TokenType.LPAREN:   # ← bug
            # 合并成复合标识符 等待f，只吃两个 token，.读取() 被丢下
```

该启发式只认「`等待 标识符(`」一种形状，漏了「`等待 标识符.成员`」。

**修法**：`peek2` 为 DOT（及其它后缀 token）时应走 `AwaitExpr(self._parse_expr())`，而非折叠为复合标识符。注意别破坏既有「等待价值」复合标识符用例，加单测回归。

**难度**：中低。建议与单 03 交同一人（都涉及 `f.读取()`，一个在解析层、一个在生成层）。

### 第 4 轮补充：已吸收原「单 21」（重复工单，已删）+ token 流取证

第 3 轮把这同一个 bug 又立了一次「单 21 · `等待`(await) 表达式不接后缀成员访问」，
受影响用例、根因位置（`src/parser_expr.py:340-355`）完全相同，**已删除单 21，只留本单**。
从单 21 吸收一条本单原先没有的证据，并纠正它一处机理误判：

- **（吸收）与单 03 无关**：`等待 f.抓取()`（`抓取` 非内置名）同样炸。所以这 6 例不属内置名映射问题，
  第 1 轮把它们计进单 03 是误判。
- **（纠正）单 21 的机理描述是错的**。它写「直接 `expr = self._parse_expr()` 后包成 `AwaitExpr`，
  `_parse_expr` 走到 `f` 后遇到 `.` 无法续接 postfix」——实测根本走不到那一行。
  `等待 f.读取()` 的 token 流是
  `KEYWORD(等待) IDENTIFIER(f) DOT(.) KEYWORD(读取) LPAREN RPAREN EOF`，
  `peek2` 是 `DOT`，`:344` 的 `peek2.type != TokenType.LPAREN` 成立 → 走 `:345-348` 的**复合标识符分支**，
  只吃掉 `等待` + `f` 并返回 `Identifier('等待f')`，`.读取()` 整段被丢在流里没人消费，
  于是报 `无法识别的语法元素：'.'`（行1 列5，正指向那个 `.`）。**本单原来的描述才是对的。**
- **对照实测（同一脚本一次跑出）**：
  - `等待 读取(f)` → OK，产物 `await input(f)`（`peek2` 是 LPAREN，走 `:349-352`）
  - `打印(等待价值)` → OK，产物 `print(等待价值)`（`peek2` 是 RPAREN，复合标识符按预期保留）
  - `等待 f.读取()` / `等待 f.抓取()` / `使用 异步 … 为 f:` + `等待 f.读取()` → 三例全 FAIL，同一报错
- **修法的边界因此更明确**：只需把「能开启 postfix 链的 token」从复合标识符分支里排除掉，
  即 `peek2.type` 为 `DOT` 时也走 `AwaitExpr(self._parse_expr())`。
  `RPAREN`/`NEWLINE` 等仍留在复合标识符一侧，`等待价值` 这类用例零影响。
  是否把 `LBRACKET`（`等待 队列[0]`）一并纳入需单独决策——本轮未做，也没有用例要求。

### 第 4 轮落地记录：改动、爆炸半径、回归算术、红绿名单

**改动**（两个文件）：

- `src/parser_expr.py`：`等待` 分支判据 `peek2.type != TokenType.LPAREN`
  → `peek2.type not in (TokenType.LPAREN, TokenType.DOT)`，并把根因写进上方注释。
  **一行判据 + 注释，没有新增函数、没有动 AST 节点、没有动代码生成器。**
- `tests/test_context_manager.py`：新增 `TestAwaitMemberAccess` 7 例回归。
  其中 4 例测 await 侧（内置名成员 / 非内置名成员 / 链式成员 / 普通调用不变），
  3 例测复合标识符侧（RPAREN 尾 / NEWLINE 尾 / 运算符尾）。
  **两头都锁**，因为原先没有任何用例测 `等待价值` 这条复合标识符路径，
  只修 await 侧很容易把它反向打穿。

**爆炸半径证据**：本轮没动 lexer 也没动 code_generator，token 流逐字节不变，
所以不需要全仓 token 流 A/B。对应的证据是**源级构造扫描**：

- 全仓 `.light` 用 `等待\s+<名>\s*\.` 与 `等待<名>\s*\.` 两种正则扫，**零命中**。
- 含 `等待` 的 29 个 `.light` 文件里，`等待` 全部是标识符的一部分
  （`锁等待时间`/`CPU等待IO`/`磁盘IO等待`/`泊松过程等待时间`/`工具/等待.light`）
  或独立语句/调用，没有一处是 `等待 对象.成员`。
- 更强的一条：改动只在 `peek2 == DOT` 时改变行为，而该形状**在修复前 100% 报
  ParseError**（DOT 落在流里没人消费）。所以不存在「原本解析成功、现在解析成别的」
  的可能，只有「原本必炸、现在能过」。

**回归算术**（同机同会话自测，非沿用历史数字）：

- 前：`72 failed / 3313 passed / 99 skipped / 6 errors`，collected 3484（269.91s）
- 后：`68 failed / 3324 passed / 99 skipped / 6 errors`，collected 3491（487.87s）
- `skipped 99 → 99` 不变、`errors 6 → 6` 不变 ✓
- `collected 3484 → 3491`，**+7 恰好等于本轮新增的 7 例回归用例**，无收集塌缩 ✓
- 对账：`72 − 6(红转绿) + 2(新增打红) = 68` ✓；
  `3313 + 6(红转绿) − 2(新增打红) + 7(新增用例) = 3324` ✓

**红转绿（6，逐条 diff 得出，非算术推断）**：

- `tests/test_context_manager.py::TestAsyncWith::test_async_with_basic`
- `tests/test_context_manager.py::TestAsyncWith::test_async_with_await`
- `tests/test_context_manager.py::TestAsyncWith::test_async_with_return`
- `tests/test_context_manager.py::TestWithAST::test_ast_async_with`
- `tests/test_context_manager.py::TestWithCodeGen::test_codegen_async_with`
- `tests/test_context_manager.py::TestWithEndToEnd::test_async_with_file`

**新增打红（2，判定为环境抖动、非本次回归）**：

- `test_duan_run[L1_baihua/10_引Python画笑脸.light]`
- `test_duan_compile_and_run_product[L1_baihua/10_引Python画笑脸.light]`

判定依据三条，缺一不可：

1. 失败形态是 `subprocess.TimeoutExpired: ... timed out after 120 seconds`，
   不是编译/解析/断言失败。该例的 Python 块调 `turtle` + `s.exitonclick()`，
   会一直阻塞到超时，跑通与否只取决于机器负载（这一轮全量耗时 487s、上一轮 269s）。
2. `examples/L1_baihua/10_引Python画笑脸.light` 全文 **`等待` 零命中**，
   本次改动在语法上不可能触及它。
3. 同一个用例在第 4 轮里出现过三种结果：全量基线跑=绿、`-k L1_baihua` 靶向跑=红、
   全量复跑=红。**它就是 72/77 那 5 例抖动的主要来源。**

> 后续建议（未做，需决策）：给这个用例加 `_SKIP` 或把 `turtle` 块换成非阻塞收尾，
> 否则每一轮回归都要为它单独解释 2 例红。**改例子属于改被测输入，需授权。**

---

## 单 08 · `包含` 占位符 `@@contains@@` 泄漏到 Python（2 例）

- 用例：`test_duan_run[file_search/主.light]` + product
- 症状：`examples/file_search/主.light:20` `如果 关键词 等于 "" 或 文件名 包含 关键词:` → 产物 `if ((关键词 == "") or (文件名 @@contains@@ 关键词)):` → `SyntaxError: invalid syntax (行 234)`
- 根因：代码生成的内部占位符 `@@contains@@` 未被替换成 `in`
- 判定：编译器缺陷
- 难度：简单，孤立。**最适合当新手/低成本任务分发**

### ✅ 单 08 已完工（v7 收尾第 6 轮）

**工单根因描述会把人带进坑。** 「未被替换成 `in`」读起来像「往 `operator_map` 加一条 `'@@contains@@': 'in'` 就行」，
照做会产出 `文件名 in 关键词`——**语法通过、语义反了**，从响亮的 SyntaxError 退化成 rc=0 无输出的静默错答案，比现状更坏。
`包含` 是唯一一个**操作数需要交换**的比较运算符：`A 包含 B` 语义是「A 含有 B」→ Python `B in A`。

**根因**：占位符产生点 `src/parser_core.py:270`（有意设计，unified 后端 `code_generator_unified.py:1247-1248` 正确消费）；
漏替换点在 `src/code_generator.py` 的 `BinaryOp` 分支（`operator_map.get(op, op)` 兜底把未知运算符原样透传）。
占位符协议只在一个后端落实，src 后端的兜底路径把协议违约伪装成正常输出。
run 与 product 两档都走 src 后端（`cli/light_unified.py` `--backend` 默认 `src`），故两档同因同错。

**改点** `src/code_generator.py:2207` 加 `if expr.operator == '@@contains@@': return f"({right} in {left})"`。
括号必须留：`in` 是比较运算符，裸发射会与外层比较串成链式比较（`x in y == False` → `(x in y) and (y == False)`，恒假），
与成员形式 `:2382` 踩过同一个坑。三处方向佐证一致：`parser_core.py:270` 注释、`code_generator_unified.py:1248`、成员形式 `:2382`。

**同族占位符全量清单**：全仓 `@@...@@` 家族**只有 `@@contains@@` 一个成员**（27 个运算符逐个过 src 后端 compile，26 干净、仅 `包含` 泄漏）。工单这一点是对的。

**「孤立」被推翻**：src 后端逐个编译全仓 `.light`，产物含 `@@contains@@` 的有 **54 文件 64 处**（积木库 49 / examples 4 / demo 1）。
另三个 examples（chat_bot/markdown_editor/password_manager）在 `E2E_EXCLUDED` 内、积木库不在 pytest 覆盖内——**不是好的，是没被看的**。
**不要用分数变化验收本单**：pytest 上只回收 `file_search/主.light` run+product 2 例，但 49 个积木块产物由「不可编译」变为「可编译且语义正确」。

**回归**：`test_duan_run[file_search/主.light]` + product 双双转绿（2 例）。


---

## 单 09 · 品牌改名后的陈旧期望（5 例，最低垂果实）

合并已定「品牌统一为光明 / light」，源码改了，测试里硬编码的期望字符串没跟上。

### 09-A `tests/test_self_host.py::test_generated_code_contains_duan_identifiers`（:169-191）

```python
has_duan_compiler = '段言' in py_code or 'duan' in py_code.lower()
assert has_duan_compiler, "生成的代码应包含段言相关标识符"
```

实测 `包含 'Duan'/'段言': False`，其余三条断言（`:192-194` 词法/解析/codegen 函数）全绿 —— 自举链健康，只有品牌那条在测一个已作废的事实。

正确期望：`has_light_compiler = '光明' in py_code or 'light' in py_code.lower()`，断言消息与 docstring `:9` 同步。顺带 `:174` 的 `has_duan` 与 `:176` 的 `has_duan_compiler` 是重复变量，可收敛。

### 09-B `tests/unit/test_examples_run.py` 的 `EXAMPLE_EXPECTED` 4 条

实测逐条比对，**只差品牌词，其余字符全等**（含数值、缩进、标点）：

- `class_static.light`：期望 `学校名称：段言学院` / 实际 `光明学院`
- `type_annotation_demo.light`：`=== 段言类型注解示例 ===` / `光明`
- `bootstrap_eval.light`：`=== 段言自举示例：表达式求值器 ===` / `光明`
- `bootstrap_lexer.light`：`=== 段言自举示例：简单词法分析器 ===` / `光明`

正确期望：这 4 条 `段言` → `光明`，其余不动。

**难度**：极低，一行一处。**优先做，立刻回收 5 例。**

### ⚠ 第 4 轮复跑：品牌部分已完工，但 09-B 的测试仍红（换了根因，属单 02 残留）

修复在 `9393cbf8` 落地。第 4 轮逐条复跑：

- **09-A** `tests/test_self_host.py::test_generated_code_contains_duan_identifiers` → **已绿**
  （`test_self_host.py` 全量 5 例全过）。品牌断言已改成 `光明`/`light`。
- **09-B** `tests/unit/test_examples_run.py::TestExampleFilesRun::test_all_examples_output` → **仍红**，
  但**已不是品牌问题**：`EXAMPLE_EXPECTED` 里那 4 条字符串已全部改成 `光明`
  （`class_static.light:115`、`type_annotation_demo.light:160`、`bootstrap_eval.light:181`、
  `bootstrap_lexer.light:190`；grep 确认该文件已无 `段言` 残留），品牌那一档确已修好。当前唯一失败的 subTest 是
  `hello.light: 运行异常 -> name '和加i' is not defined`。

  **根因（token 级取证）**：`examples/hello.light:18` 是 `设和为和加i`（无空格）。lexer 把 RHS 切成
  `IDENTIFIER('和加i')` 而不是 `和`/`加`/`i` 三个 token，于是产物是 `和 = 和加i`，`和加i` 未定义。
  这是**单 02 紧凑写法族的残留**：单 02 的「词尾运算符豁免」例外只放行「运算符后紧跟 ASCII **数字**」
  （`src/lexer.py:1876-1888` `_tail_is_digit_operand`，刻意不放行字母），所以 `和加1` 能切开、
  `和加i`（字母尾）切不开。实测对照：
  - `设和为和加i` → `... IDENTIFIER('和加i')`（**红**）
  - `设和为和加1` → `... 和 加 NUMBER(1)`（单 02 已修，绿）
  - `设积为积乘n` / `设差为差减n` → 同样并成 `积乘n`/`差减n`（**红**）
  - `设 和 为 和 加 i`（带空格）→ 正常切开

  **为何不在本轮顺手修**：把例外从「数字尾」扩到「字母尾」会撞上单 02 刻意收窄的边界——
  `分割Pointer_内部`、`读取LSP消息` 这类合法混排标识符的 ASCII 尾恰恰是字母，扩了就会被拆穿。
  区分「`积乘n` 的 n 是循环变量」与「`读取LSP` 的 LSP 是名字的一部分」需要真正的作用域/声明信息，
  **属产品决策 + 单 02 的独立续作，不在单 05 收尾范围**。`hello.light` 是被测输入，按硬规则 2 不可改。
  → **09-B 应从「已完工」降级为「品牌已修、剩单 02 字母尾残留」，转挂单 02。**

---

# 优先级 P2 —— 真活儿

## 单 10 · `bootstrap_level5.light` 的 `结束` + DEDENT 配对（7 例）

**受影响**：`tests/test_self_host_bootstrap.py` 5（`:667/715/779/851/870`）+ `tests/test_e2e_full_coverage.py` 2（`:65/:82`）

**线索纠正**：只有 level5 挂。level3、level4、`bootstrap_v3.light` 都过（该文件 57 passed / 2 skipped）。

**报错**：`位置: 行 973, 列 1 / 原因: 无法识别的语法元素：'2'`

**核心发现**：那个 `'2'` **不是源码里的数字，是 DEDENT token 的 value（缩进宽度 2）**。tokenize dump：

```
line=972 col=1 type=DEDENT     value=4
line=972 col=5 type=IDENTIFIER value='结束'
line=973 col=1 type=DEDENT     value=2      ← 报错的就是它
line=973 col=3 type=IDENTIFIER value='结束'
```

对应源码 `bootstrap/bootstrap_level5.light:971-974`：

```
      返回 列表创建(out, try_end 加 1)
    结束          ← DEDENT(4) + 结束
  结束            ← DEDENT(2) + 结束，parser 在这里死
```

**根因**：`结束` 被词法器当 IDENTIFIER（非 KEYWORD），而 level5 是「缩进 + 显式 `结束`」双轨块结构。连续两个递减缩进的 `结束` 时，parser 消掉了 `结束` 却没消掉配对的 DEDENT，第二个 DEDENT 落到语句起始位置报错。

**辅证**：全文件「以 `：` 结尾的块开启行 = 427」，「单独成行的 `结束` = 392」，差 35 —— 两种闭合风格混用，parser 只按一种规则平衡。

**两步走**：

1. 先修 `src/parser_stmt.py:561` 的报错文案 —— INDENT/DEDENT token 必须打印类型而不是 `value`，否则后来人一样被 `'2'` 带偏
2. 真修在 `src/parser_core.py` 的 `结束` + DEDENT 配对逻辑

**难度**：中高，自成闭环（只碰 parser + 一个输入文件），适合独立分发。

---

## 单 11 · `lunardate` 硬依赖（8 例）

**受影响**：`tests/test_datetime.py` 8 例（该文件 107 收集，99 通过），代表 `test_公历转农历`、`test_中国节假日`

**症状**：`stdlib/日期时间.py:732-733`（`农历转公历` 在 `:740-741` 同构）

```python
if not _HAS_LUNAR:
    raise RuntimeError("农历转换需要 lunardate 库，请执行: pip install lunardate")
```

`pyproject.toml:30` 是 `dependencies = []`，`[project.optional-dependencies]`(:32-58) 只有 antlr/dev/llvm/repl/docs，**没有 lunardate** —— 这是个彻底的隐式依赖。

**结论：既不该装依赖，也不该改 skip。应补全内置农历换算，删掉第三方硬依赖。**

依据是这个模块**自己就带着完整农历数据表且已经在用**：

- `stdlib/日期时间.py:44-73` `_LUNAR_YEAR_INFO`，201 项，覆盖 1900-2100（实测长度 201）
- `:85-120` `_农历年份信息` / `_农历闰月` / `_农历闰月天数` / `_农历月天数` / `_农历年总天数`

实测（`_HAS_LUNAR=False` 状态下）`_农历月天数(2024,1)=30`、`_农历闰月(2024)=0`、`_农历年总天数(2024)=354` 全部正常出值。更关键的是 `:795-799` **已经写好了无 lunardate 时退回内置表的分支**：

```python
        else:
            腊月天数 = _农历月天数(农历年, 12)    ← 无 lunardate 时走内置表
```

**现状是自相矛盾的：同一个文件里「腊月有几天」不靠 lunardate 就能算，「正月初一是哪天」却非要 lunardate。** 缺的只是「农历年首 ↔ 公历日期」的基准偏移换算，用 `_LUNAR_YEAR_INFO` + 一个 1900-01-31 基准日即可补全，纯算术，无外部数据。

**为什么不装依赖**：`pyproject.toml:30` 的零依赖是刻意姿态（stdlib 全用标准库）。为一个中文语言的**核心卖点功能**（农历/节假日/春节）引入第三方包，等于把 `import 日期时间` 的可用性绑到 pip 源上。

**为什么不改 skip**：这 8 例测的是 `春节日期(2024) == 2024-02-10` 这类**标准库功能正确性**，不是可选增强。skip 掉等于把中文历法能力从覆盖里摘出去。skip 该留给「FFI 依赖 libc.so.6」那种真平台外部性（`tests/unit/test_examples_run.py:18-20` 就是这么划的）。

**积木库先例支持补实现而非 skip**：`积木库/评估/冒烟.py:24-27` 的口径是「缺依赖单列 + 不计入分母」，但 `:193-194` 同时留了追责钩子 —— `'pip install %s（索引未声明 依赖，建议补上）'`。也就是「单列出来 + 记成待补的账」，不是「当没这回事」。而且 `:26` 亲口点名 `公历转农历` 是那批第三方依赖块之一 —— 同一个功能，积木库侧已经记了账，标准库侧还在 raise。这条账现在该结掉。

**兜底方案**（若内置换算超预算，三件必须一起做）：`lunardate>=0.2.0` 进 `[project.optional-dependencies]` 新增 `lunar` 组 + 8 例加 `@pytest.mark.skipif(not _HAS_LUNAR, ...)` + `积木库/索引.json` 对应块补 `依赖` 字段。**只做 skip 是把缺口藏起来。**

**附带一个独立真 bug**（无论选哪个方案都该修）：`中国节假日()` 的 `except (ValueError, IndexError)`(:833) 漏了 `RuntimeError`，而 `:829/:831` 抛的正是 `RuntimeError`，穿透了 catch。该函数设计意图明显是「单个节日算不出就跳过」，应优雅降级成「只有公历节日」，而不是整个函数炸掉。

**难度**：低-中；适合独立分发。

---

## 单 12 · L0 单字关键字语法体系未实现（6 例）

**受影响**：`test_duan_run[L0_core/03_若_条件判断.light]`、`[L0_core/06_类_面向对象.light]`、`[L0_core/09_匹配_模式匹配.light]` 各 + product

三条**独立**的语法特性，**建议拆成 3 张单并行**：

- `examples/L0_core/03_若_条件判断.light:7` `若 分数 >= 90 则：` / `:15` `否：` → 报 `「则」是保留关键字，不能直接作为语句开头`、`「否」…`。缺「条件后置 `则`」与单字 `否` 分支
- `examples/L0_core/06_类_面向对象.light:9` `构 接收 名称, 年龄：` / `:5` `类 动物：` / `:21` `类 狗 承 动物：` → 报 `「接收」是保留关键字…`。缺单字 `构`/`性`/`承`/`新` 类语法
- `examples/L0_core/09_匹配_模式匹配.light:6` `匹 值：` / `:7` `例 数：` → 报 `发现 14 个语法错误 / 无法识别的语法元素：'：'`。缺 `匹`/`例` 模式匹配（最重）

**判定**：编译器缺陷。三个文件头注释都自我声明 `# v4.0 单字关键字「若」「则」「否」「或若」` 等，是 v4.0 关键字体系的**规范样例**。同目录 `01/02/04/08` 全绿 —— 单字体系是部分实现、部分缺失。

**难度**：困难，要动 `src/parser_stmt.py` 的语句分派与代码生成。

---

## 单 13 · `试/捕` 缺 `掷` 与 `错误信息`（2 例）

- 用例：`test_duan_run[L0_core/07_试_异常处理.light]` + product
- 报错：`name '错误信息' is not defined`、`name '掷' is not defined`，并且 `ZeroDivisionError` / `IndexError` **根本没被 试/捕 拦住**
- 判定：编译器缺陷（标准库/运行时缺口）
- 难度：中等

---

## 单 14 · `lsp_protocol.light` 用了已移除语法（2 例）

- 用例：`tests/test_lsp_protocol.py` 2 例（该文件全灭，2 collected / 2 failed），代表 `TestLSPProtocol::test_编译成功`
- 线索纠正：报错里的 `parser_co...` 只是 pytest 截断了 `parser_core.ParseError`，**不是 `lsp/lsp_protocol.py` 的问题**
- 判定：**源文件侧欠账**，parser 行为正确。已废弃语法（如「段落 名 接收 参数」已废弃，应改「函数 名 (参数)」或「段落 名 (参数)」）
- 规模：约 40 个错误点
- 难度：中低但量大；适合独立分发

---

# 优先级 P3 —— 环境与脚手架

## 单 15 · 例子文件缺 `结束引` 闭合（8 例）

- 用例：`J阶段_L4_C_Go_MoonBit/J1_C_快速求和`、`J2_Go_斐波那契`、`J3_MoonBit_快速排序`、`L0_core/10_导_模块导入`，各 run + product
- 报错：`词法错误 (行4, 列1): L4引用/嵌入块未闭合：缺少「结束嵌入」或「结束引」标记`（`src/lexer.py:1189` `_tokenize_embed_block`）
- ~~判定：**例子文件过时**~~ —— **原判已被用户裁决推翻**，见下

> ### ⚠ 已定口径 + 已完成（见提交 `单 02 紧凑写法分词族 + 单 15 L4 嵌入块缩进回归闭合`）
>
> **用户裁决：判为编译器缺陷 —— lexer 应支持缩进回归自动闭合。** 依据：`docs/知识库/语言规范.md:170-173` 的官方 L4 示例本身就省略 `结束引`（靠 `出 hello` 回到第 0 列收尾）；`docs/分层语法设计_v4.0.md:748` 明列「缩进回归」为合法闭合方式。
>
> **实现要点**：`_tokenize_embed_block` 是字符级扫描器，不经过 token 流、拿不到 INDENT/DEDENT，必须自己从 `i` 反向扫到行首算出开启行缩进。**不要用入参 `col - 1` 代替** —— 主循环换行段 `:540-546` 对 `\t` 是 `indent += 4` 而 `col` 只 `+= 1`，制表符缩进会失真。
>
> **闭合规则三档（顺序不可换）**：① 显式 `结束引`/`结束嵌入`（原逻辑零改动，含尾随句号消耗）→ ② 缩进回归：仅当「块体缩进 > 开启行缩进」时，首个缩进 ≤ 开启行的非空行即闭合点，**该行不消耗**，交回主循环当普通 light 语句 → ③ EOF：缩进式块体按缩进回归的极限情形正常闭合。空行/纯空白行原样收进块体、**不参与缩进判定**（J1 `:10`、J2 `:12`、J3 `:11` 块体内都有空行）。
>
> **零缩进块体不做任何启发式，仍报未闭合。** 理由：`引 Python:` 与块体同在第 0 列时，`import math` 与一条合法 light 语句在词法层不可区分。曾评估过「零缩进 + `出` 行收尾」这条替代规则，实测会误吞 —— 若把 `印("先打一行")` 排在 `出 result` 之前，它会被吞进 Python 块生成 `印(...)` 交给 Python 执行（`NameError: 印`）。那是顺序依赖，不是词法规则。
>
> **`examples/L0_core/10_导_模块导入.light` 的块体原本在第 0 列**（与 `引 Python:` 同缩进），落不进缩进回归规则。**经用户单独授权**（属硬规则 2「禁止改被测输入」的例外），给其 Python 块体加 4 空格缩进，与全部通过的 L4 示例主流写法一致（对照 `examples/L4_python/demo1_numpy_mean.light:4-10`）。产物侧安全：`src/code_generator.py:3231` 有 `textwrap.dedent(stmt.code).strip()`，缩进块体会被正确 dedent。
>
> **修完各例落点（实测）**：
> - J3 run + product：**已转绿**（该文件不用 `串`、不调用任何 MoonBit 函数）
> - J1 / J2 run + product 4 例：**仍红，卡在 `串(...)` 未定义**，与词法无关，另立单 19
> - `10_导` run + product 2 例：**仍红，但已不是词法问题** —— LexerError 消失后暴露下一层缺陷：`导 数学 自 数学`（`:5`）与 `导 字符串处理 自 字符串处理`（`:6`）解析失败，报 `期望'为'或'等于'，但得到 。`，属 L0 单字关键字体系（单 12 同族）
> - 工单原判「J1/J2/J3 修完还会撞上 gcc/go/moonbit 工具链依赖」**不成立**：`src/code_generator.py:3443` 的正则 `(?:int|float|double|void|long|char\s*\*)\s+(\w+)\s*\(` 在 Python 3 下 `\w` 匹配汉字，`double 快速求和(`、`int 阶乘(` 都能捕获，`:3450`/`:3452`/`:3454` 三条路径都往 `globals()` 写名字（最差是占位 lambda），不会 NameError；J2 走 `:3494` 的 `//export\s+(\w+)` 同理
> - 工单说的 `_SKIP` 清单**在当前 HEAD 不存在**（`tests/` 全目录 grep 零命中），单 16 若要走 skip 路线需先建该机制

## 单 19 · `串` 双用（类型名 + 转换函数）实现缺失（4 例，规范承诺未落地）

**受影响**：`test_duan_run[J阶段_L4_C_Go_MoonBit/J1_C_快速求和.light]` + product、`[J2_Go_斐波那契.light]` + product

**症状**：`examples/J阶段_L4_C_Go_MoonBit/J1_C_快速求和.light:18-19`、`J2_Go_斐波那契.light:24-25` 用 `串(快速求和(...))` 这种**函数调用形式**，产物运行期 `NameError: name '串' is not defined`。

**根因**：`串` 只被当类型名实现，转换函数一侧从未落地。

- `src/keywords.py:476` 把 `串` 列入 `BUILTIN_TYPES`
- 类型侧三处映射齐全：`src/code_generator.py:1124`（`_map_type`）、`:1783`（`_TYPE_NAME_MAP`）、`:2831`（FFI ctypes）
- **但 `builtin_map`（起于 `:195`）里没有 `'串'`** —— 只有 `:324 '转串'`、`:325 '转字符串'`、`:326 '到字符串'`、`:327 '转换字符串'` 四个长名别名，目标函数 `stdlib/builtins.py:423 def 转字符串(value) -> str` 是现成的
- `stdlib/builtins.py` 无 `def 串`；全仓 `def 串` 零命中。**duan 侧也从未实现过** —— 这是「规范写下起就没落地」的缺口，不是合并弄丢的

**规范依据（双用是明文承诺）**：`docs/L2_文言体语法规范_v4.0.md:285-288` 小节标题就叫「类型转换」，`:287` 一行内同时用了两种身份：

```light
设 文字: 串 = 串(123)
```

冒号后是类型注解，等号后是转换调用。同段 `:286` 的 `整(...)`、`:288` 的 `列(...)` 并列举例。该文档另有 9 处 `串(...)` 调用（`:193/:373/:483/:496/:607/:610/:828/:903/:904`）；`docs/分层语法设计_v4.0.md:373`、`docs/guide/常见问题.md:139`（注释直接写「正确：使用串()转换」）、`docs/v4_beginner_ecosystem_plan.md:216/495/499/509` 同口径。

**「类型名兼作转换函数」在 light 里已是既成惯例，不是刻意分离**：`整数`(`:224`)、`列表`(`:227`)、`字典`(`:228`)、`集合`(`:229`)、`布尔`(`:230`) 全部双用；短名 `列` 也已双用（`:363` + `stdlib/builtins.py:598 def 列`）。**`串` 是同批短类型名里唯一缺失的**，`整` 同样缺失（builtin_map 里只有 `'整数'` 和 `'转整数'`）。

**影响面远大于 4 例**：全仓 `.light` 有 **35 处 `串(` 调用点、分布 9 个文件 6 个目录**（已排除 `转串(`/`字符串(` 等长词）—— `examples/F阶段_标准库增强/F3_段言侧三个增强模块示例.light` 单文件 24 处，`examples/weather_app/{main,utils}.light` 3 处，`edu/课程{4,7,8,10}/示例.light` 8 处，`opensource_projects/{待办事项管理器,CLI记事本}/主.light` 2 处，J1/J2 4 处。核心库与自举链路走的是长名 `转字符串`/`转串`，**面向用户的 examples / edu 教程 / 开源示例走的是规范推荐的短名 `串(`**。

**最小改法**：`src/code_generator.py` 紧邻 `:324` 加 `'串': '_light_builtin.转字符串'`；一次补齐规范三兄弟再加 `'整': '_light_builtin.转整数'`（`stdlib/builtins.py:407`）。

**与类型注解路径不冲突**（三条论证）：① 注解走 `_map_type`(`:1117`) 与 `_TYPE_NAME_MAP`(`:1780`) 两个字符串→字符串映射，输入是 AST 节点的 `type_annotation` 裸字符串，**不读 `builtin_map`**；② `builtin_map` 只在 `ParagraphCall` 分支（`:2219-2220`）与成员调用（`:2385`）被查，且有局部变量遮蔽 / `_user_defined_functions` / 显式 import 三重护栏，类型注解位置永远不产出 `ParagraphCall`；③ `串` **不在** `ALL_KEYWORDS`，`BUILTIN_TYPES` 在 parser 里的消费点只有 `src/parser_stmt.py:2830` 和 `:2879` 两处形参位判定，平时 `串` 就是普通 IDENTIFIER。

**待实测确认**：是否需要同步在 `STDLIB_VERB_ARITY` 加 `'串': 1` 以支持无括号写法 `串 x`（规范里全是带括号的 `串(x)`，仓内 35 处调用点也全带括号，仅补 `builtin_map` 应已覆盖）。

**难度**：低。**不属单 02/15 的 lexer 范围**，需独立分发。

### ⚠ 本单已落地，但「4 例转绿」完全不成立（v7 收尾第 3 轮实测）

`'串': '_light_builtin.转字符串'` 与 `'整': '_light_builtin.转整数'` 已加在 `src/code_generator.py` 的 `'转串'` 一行之后。技术定位与行号全部核实成立，但**落地判断错了两处**：

1. **只加 `串`，J1/J2 四例一个都不转绿。** 实测（隔离掉其它改动）J1 仍报 `NameError: name '快速' is not defined`、J2 仍报 `NameError: name '斐波那契' is not defined`。真正的第二阻塞点是 L4 导出名撞内置动词被拆分/顶替 + Go 名未绑定 —— 见新增的**单 20**。`串` 是必要但远不充分的前置。

2. **`串` 的真实价值在 pytest 分母之外。** e2e 只扫 `examples/`（`tests/e2e/test_e2e_chain.py:79-85`，`:84` 还显式排除 `weather_app`），所以 35 个 `串(` 调用点里 e2e 可见的只有 F3 与 J1/J2；F3 在 HEAD 本就 ParseError（属其它单）。`edu/`、`opensource_projects/` 是顶层目录、根本不在 e2e 覆盖内。**加 `串` 的 pytest 转绿数 = 0**，它修的是面向用户的教程/示例链路。

**统计口径更正**：不是「35 处 / 9 文件 / 6 目录」。可复现扫描（排除 `转串`/`字符串`/`串接` 等长词、注释、字符串字面量）为 **45 次出现 / 35 个命中行 / 11 个文件 / 9 个末级目录**。「35」是 ripgrep 的**行**计数被当成了「处」；文件数工单自己的明细列的就是 11 个，标题写 9 自相矛盾；F3 实为 25 处（原写 24）。

**`整` 的落地说明**：全仓 `整(` 调用点 **0 处**、`整` 无括号 0 处、`整`/`串` 作变量名或形参名 0 处。所以加 `整` **不修任何用例、也不动任何现有绿用例**，纯粹是补齐规范三兄弟（`串`/`整`/`列`），已在代码注释里标注这一点。映射目标选 `_light_builtin.转整数` 而不是 `int`，是为了与已落地的短名 `列`（`:363 '列': '_light_builtin.列'`）保持同族；代价是错误类型与长名 `整数→int` 不同（RuntimeError vs ValueError），这个不一致是既有的、不由本单引入。

**`STDLIB_VERB_ARITY` 确认不动**：全仓 `串` 无括号出现 33 处，**全部**是类型注解位（`-> 串`、`: 串`、`设 x: 串`）或匹配模式（`例 串：`），没有一处是 `串 x` 这种无括号动词调用。类型注解/匹配走 `_TYPE_NAME_MAP` 与 parser 的 `BUILTIN_TYPES`，都不读 `builtin_map`、不需要 arity。

**三条「不冲突」论证经扩查仍成立**，且不存在第 4 条冲突路径：`type_inferencer.py:167/2318`、`type_checker.py:396/1134` 都以 `转字符串` 为准、`串` 只作类型名；REPL 补全 `completer.py:38` 只列 `串` 为类型；FFI `:2896-2910` 仅用 `串` 作类型判定。均不读 `code_generator.builtin_map`。实测佐证：含 `设 戊：串 为 "hello"` 的 `tests/test_level8_llvm.light` 在加 `串` 前后生成产物**逐字节相同**。

**工单未记的一条隐性前提**：`串` 同时在 `src/lexer.py` 的 `_COMPOUND_SAFE_SINGLE_KEYWORDS` 里，这才是 `串(123)` 能被正确切成 `串` + `(` 的原因。

**遗留（本轮刻意不做）**：`src/code_generator_unified.py`（ANTLR 后端）的 `builtin_map` 同样缺 `串`/`整`。e2e 与 CLI 走的是 src 后端，补不补不影响任何用例；为把本轮改动面压到最小，留到「两个后端映射表一致性」单独一轮统一对齐，届时应连带核对两表的全部差异，而不是只补这两个键。



---

## 单 20 · L4 引 C/Go 导出函数名被拆分/顶替（4 例）—— 已落地

**受影响**：`test_duan_run[J阶段_L4_C_Go_MoonBit/J1_C_快速求和.light]` + product、`[J2_Go_斐波那契.light]` + product

**症状**：J1 报 `NameError: name '快速' is not defined`，J2 报 `NameError: name '斐波那契' is not defined`。工单原先把这四例归给单 19（`串` 缺失），实测证明 `串` 只是**先炸的那一个**，补完 `串` 之后立刻露出下面两条真根因。

**根因一（词法 + 生成两层）：L4 块导出的函数名，在光明侧不被当作用户标识符。**

`引 C:` / `引 Go:` 块里定义的函数会在产物运行期被绑进模块 `globals()`，所以在光明侧就是「用户显式声明的名字」。但：

- 词法层：`Lexer._scan_user_definitions` 只认 `《》` 段落/类/方法、`导出/导入` 列表、`函数/段落`、`段 名(...)`、`定义/设` —— **完全不认 L4 块**。实测 `_scan_user_definitions(J1源码)` 只返回 `['运行']`。于是最长前缀匹配把 `快速求和` 切成 `IDENTIFIER 快速` + `KEYWORD 求和`（`求和` 在 `VERB_ARITY`），J1:18 的产物变成 `串(快速, sum(1))` —— **一个实参变成了两个**，比「名字被顶替」更糟。
- 生成层：`builtin_map` 无条件顶替 —— `阶乘` → `math.factorial`（`src/code_generator.py:244`）、`求和` → `sum`（`:213`），产物根本没调到外语函数。`_user_defined_functions` 护栏思路是对的，但只登记 import 符号与段落名，不认 L4 块。

注意：**「撞表」不是受害的必要条件**。`快速求和` 本身不在任何关键字/动词表里，但因为**尾部含 `求和`** 仍被切碎。所以白名单必须登记**全部**导出名，不能只登记撞表的那些。

**根因二：Go 路径只在编译成功分支绑定导出名。**

`src/code_generator.py` 的 Go 分支里，`globals()` 写入只有两处，且都在 `if _LIGHT_GO_OK:` → `try:` 成功分支内。`except`（编译失败）只 print 不绑定，`else`（无 go）只 print 不绑定；更糟的是**导出名提取本身也在 try 里面**，无 go 时连「要绑哪些名字」都不知道。对照 C 路径：提取无条件执行，绑定覆盖三分支（真实函数对象 / `[C:x 未加载]` / `[C:x 编译器未找到]`），所以 C 侧无 gcc 时也能跑通。

反证：把 C 函数名换成不撞内置的 `myfunc`、无 gcc，产物输出 `结果=[C:myfunc 编译器未找到]` 且 EXEC OK —— 证明只要名字不撞内置动词，无工具链也能跑通。

**判定**：编译器缺陷。用户按规范写 L4 引 C/Go，只要导出名撞了（或含）内置动词，程序就静默走错分支。**已否决「改 examples 改名」的方案**（属改被测输入），采用方案 B：解析期注册 + Go 占位绑定。

**落地（三处）**：

1. `src/lexer.py` 新增模块级 `extract_embed_export_names(language, code)`，C 用 `(?:int|float|double|void|long|char\s*\*)\s+(\w+)\s*\(`、Go 用 `//export\s+(\w+)`，与 code_generator 运行期绑定用的正则**逐字同源**。MoonBit 与 Python 返回空集 —— 它们运行期不往 `globals()` 写名字，登记了反而制造「白名单有名、运行期无绑定」的新 NameError（J3 靠这条保持现状）。
2. `Lexer._scan_user_definitions` 开头加**一遍独立扫描**（不改主循环一个字节）：`str.find` 找 `引`/`嵌入`，套用与真实分词器相同的前缀门槛（`嵌入` 两字；`引` 必须紧跟空格/制表/冒号/换行），再加**行首门槛**（前缀之前只能是空白），然后调用 `_tokenize_embed_block` 定界。
   - 行首门槛是必需的：预扫描是裸文本、不认字符串字面量，而 `examples/L4_python/all_in_one_demo.light:90` 与 `demo4_requests_http.light:30` 的字符串里就写着 `引 Python:`。真实分词器不会踩，因为字符串在更早的分支就被整体吞掉了。
   - 复用 `_tokenize_embed_block` 而不另写一套定界，是为了让单 15 刚加的缩进回归闭合、EOF 闭合、显式 `结束引/结束嵌入` 三条判据**同源**，不出现「预扫描认为块到这、分词认为块到那」的错位。
   - 只前进一个字符、**不跳过块体**：今天的主循环本来就会扫进块体并从中登记名字，跳过会减少既有白名单、可能打红既有绿。
3. `src/code_generator.py`：`_register_imported_names` 把 L4 导出名与 import 符号同级登记进 `_user_defined_functions`（必须在这个预扫描里，不能在 `_generate_embed_block` 里懒登记 —— 生成器是单遍，块写在调用点之后时懒登记来不及，实测「块在后」时懒登记失效、预扫描顺序无关）；Go 路径把导出名提取上移到 `if _LIGHT_GO_OK:` 之外，并在 `else` 之后补全量占位绑定（`if _LIGHT_GO_FN not in globals()` 保证不覆盖 build 成功时绑好的真实对象）。

**顺带修掉的一个真实崩溃**：C 路径原先 `del _LIGHT_C_CODE, _LIGHT_C_FN, _LIGHT_C_FUNCS` 会 del 循环变量 `_LIGHT_C_FN`；块内提不出任何函数声明时（如只有 `#define` 的 C 块）它从未被绑定，`del` 直接 `NameError` 打断整个产物。J1 有两个函数所以没触发。已改为不 del 循环变量，Go 侧新写的 `del` 同样避开这个形态。

**语义取舍（必须记住的代价）**：白名单是**模块级**，不是块级。词法层 `user_definitions` 对整份源码算一次；生成层 `_user_defined_functions` 每次 `generate()` 重置后只增不减。所以一旦某个 L4 导出名撞了内置名，**该内置名在整份文件内不可达，且静默** —— 合成用例实测：同文件内 `引 C:` 导出 `求和` 后，另一个段落里真心想调内置 `sum` 的 `求和(表)` 也会拿到 C 占位串，打印 `[C:求和 编译器未找到]` 而不是 `6`。

这个取舍与仓内既有口径一致，不是新发明：`src/code_generator.py:452` 的注释就写着「显式导入是用户最强的意图表达，必须压过内置映射」，`引 C:` 里写 `double 求和(...)` 与 `从《X》导入《求和》` 是同一类声明，遮蔽范围理应一致。要做到块级/调用点级需要真正的作用域符号表（parser + 类型系统改造），远超本单。**中间态建议**：导出名撞 `ALL_VERB_ARITY`/`builtin_map` 时发一条编译期 W 级告警，把「静默」变成「响一声」，成本几乎为零 —— 未做，待裁决。

**J1/J2 不受这个代价影响**（逐行核查）：J1 光明代码区里 `阶乘` 只出现在 :19、`求和` 在 J2 只出现在 :25，每个撞名导出恰好一个调用点、意图就是调外语函数；J1 内没有任何一处想要 `math.factorial`，J2 内没有任何一处想要 `sum`。零误伤。

**爆炸半径**：全仓含 L4 引/嵌入块的文件 **24 个 / 27 个块**（用 `_tokenize_embed_block` 定界，非正则近似）。语言分布：Python 13、C 1、Go 1、MoonBit 1、SQL 1、模式 1、公式 1、示例1-5 各 1。**只有 C/Go 两种语言产生具名导出**，共 4 个导出名，其中撞表 2 个（`阶乘` 在 `ALL_VERB_ARITY` 不在 `VERB_ARITY` → 只在生成层被顶替；`求和` 同时在 `VERB_ARITY` → 词法层也参与拆分，两层都要治）。

token 流差分（覆盖 24 个含 L4 块的文件）：**变化 2 / 24**。
- J1：62 → 61 token，`IDENTIFIER 快速` + `KEYWORD 求和` 合并为 `IDENTIFIER 快速求和`
- J2：61 → 61 token，`求和` 只是 `KEYWORD → IDENTIFIER` 换型，数量守恒（说明词法层没坏 J2，坏的是生成层 `builtin_map`）
- 其余 22 个逐 token 完全一致，**含两个字符串里带 `引 Python:` 的文件**（行首门槛生效）


---

## 单 22 · 形参名为内置动词名时 `名[...]` 被误解析为调用（0 例，边角但真实）

**症状**：
```light
段落 甲 接收 映射:
    打印(映射["k"])
```
生成 `def 甲(映射): print(映射(["k"]))` —— 索引访问被解析成**零参调用 + 后随列表**。

**对照**：局部变量档（`设 映射 为 …` 后 `映射["k"]`）与普通名形参（`表格["k"]`）都正确产出 `["k"]` 索引。只有「形参名恰好是内置动词名」时出错。

**判定**：parser 层 `名[...]` 消歧缺陷 —— **AST 就错了**，不是 codegen 的问题（单 03 的 `_shadows_builtin` 在这一档是对的）。

**难度**：未评估。**真实暴露面待评估** —— 这是构造出来的用例，全仓 examples 未必命中，可能不值得优先修。




## 单 16 · 第三方 Python 包缺失（6 例）

- `L4_python/demo1_numpy_mean`（numpy）、`demo3_matplotlib_plot`（matplotlib）、`demo5_sklearn_iris`（sklearn），各 run + product
- 判定：**环境缺依赖**（`demo4_requests_http` 全绿说明 requests 装了）
- 建议：直接进 `_SKIP`，或按积木库口径「单列 + 不计入分母」

## 单 17 · 产物不自洽：L3 运行时模块不在 sys.path（3 例）

- `L3_domain/demo4_echarts`（product）、`L3_domain/demo5_markdown`（product）
- 报错：`ModuleNotFoundError: No module named 'l3_echarts'` / `'l3_markdown'`
- 关键：`src/l3_echarts.py` 和 `src/l3_markdown.py` **在仓库里是存在的**。产物 `.py` import 不到，因为 `tests/e2e/test_e2e_chain.py:114` 用 `cwd=REPO_ROOT` 裸跑产物，`src/` 不在 `sys.path` 上，而 CLI 是靠 `cli/light_unified.py:36-42` 手动 insert 才找得到
- 判定：**编译器缺陷（产物自洽性）** —— `light compile` 生成的 `.py` 声称能独立运行，实际依赖调用方替它铺路径
- 难度：中等（产物头部注入运行时路径引导，或把 L3 helper 内联）
- **值得修**，它戳的是「产物可独立运行」这条产品承诺

## 单 18 · Windows 控制台 GBK 撞 emoji（4 例）

- 用例：`test_duan_run[cli_tool/file_organizer.light]`、`[kids/draw_shapes.light]` + product
- 报错：`'gbk' codec can't encode character '\U0001f4c1'`（源码 `examples/cli_tool/file_organizer.light:223` `打印("  📁 其他: " …)`、`examples/kids/draw_shapes.light:164` `🎨`）
- 佐证：日志里 `╔══════╗`（BMP 内、GBK 可编码）打印成功了，只有补充平面 emoji 炸 —— 纯粹是子进程 stdout 编码是 cp936
- 判定：**测试脚手架缺陷**，不是编译器也不是例子。`tests/e2e/test_e2e_chain.py:79-83` 的 `subprocess.run` 没传 `env`，子进程继承宿主 cp936；`:114-117` 跑产物处同样没设
- 修法：传 `env={**os.environ, 'PYTHONUTF8': '1', 'PYTHONIOENCODING': 'utf-8'}`
- 难度：简单，一处改动解决 4 例。**这是唯一一个「改测试」是正确解的簇**，但改的是执行环境配置，不是断言 —— `returncode == 0` 本身是对的

---

# 待核实

- **基线自身有 1 例抖动（v7 收尾第 3 轮发现）**：第 2 轮收尾记录的基线是 `82 failed / 3303 passed`，第 3 轮在同一台机器、同一 HEAD（`3a44a80d`）上把 `src/lexer.py`、`src/code_generator.py` 退回后复跑，得到 `81 failed / 3304 passed`（skipped 99、errors 6、collected 3484 均一致）。**说明有 1 个用例是环境相关或用例间干扰导致的抖动**，尚未定位是哪一个。教训：**每轮回归必须在同一会话内自测基线，不要沿用上一轮记录的数字**——第 3 轮如果直接拿 82 去减，会误以为多修了 1 例而去追查一个不存在的绿。可疑范围优先看下面 `test_stdlib_complete` 那 3 个 FileSystem 用例（已知只在全量跑时红）。


- `tests/test_stdlib_complete.py` 的 3 个 FileSystem 失败 **在单独跑时复现不出来**（单跑 21 passed，与 `test_filesystem.py` 合跑 82 passed）。怀疑是全量跑时的用例间干扰或临时目录竞争。**需要单独定位，不要按「Windows 路径问题」立单。**
- `tests/test_level6_types.py::TestTypeParser::test_parse_function_with_parens_is_single_tuple_param`（1 例）：TypeParser 剥括号破坏「元组参数 vs 联合参数」区分。证据未完整取回，需补查。
- 上述单 01–18 合计约 135 例，与 144 尚差约 9 例（含上面两条）。差额主要在 `test_e2e_chain` 的长尾单例，可在开工后按 `-rf` 清单逐个补入。

# 建议开工顺序

1. **单 09**（品牌陈旧期望，5 例）—— 一行一处，立刻回收
2. **单 05 符号层**（13 例中的 4 例全绿 + 9 例暴露真问题）—— 最高杠杆
3. **单 06**（`己`→self，11 例）—— 窄而深，两个现成最小复现
4. **单 03**（内置名劫持，6 例）—— 例数不多但是用户可见正确性缺陷
5. **单 01**（遍历循环族，28 例）—— 最大簇，但必须先按已定口径收窄 `之` 的用法
6. **单 02**（紧凑分词，15 例）—— 单点根因，但回归面最广，要留足回归时间

单 04 / 08 / 13 / 15 / 16 / 18 都是小而独立的，可随时并行插入。单 10 / 12 / 14 / 17 是真活儿，建议等前六项落地、跑分稳定后再开。

---

# 附录 · 第 4 轮 e2e 全面归因（HEAD 86af6416，同机自测）

派 General subagent 跑 `tests/e2e/test_e2e_chain.py`（deselect 掉 L1_baihua/10 那两个 turtle 阻塞例），
自测基线 **44 failed / 111 passed / 2 deselected**。逐条回解乱码、对齐源码行号后归因。
下列每一条我（主会话）都用 `.scratch\verify_claims.py` 亲自 token-dump 复核过关键项，非照抄。

## 环境实测（`importlib.util.find_spec` 逐个验证，非推测）

`numpy / pandas / matplotlib / sklearn / requests / lunardate / sympy / markdown` **全部未安装**；
`sqlite3` 在；`gcc` / `go` 均无。**纠正工单原判**：单 16 写「`demo4_requests_http` 全绿 ⇒ requests 已装」
是错的 —— requests 实测没装，那个用例绿是因为源码把 requests 调用包在 `试/捕` 里。
**不能用「某用例绿」反推包已装。**

## 按根因归类（44 例）

- **单 12 · L0 单字关键字（8 例）**：`03_若`(run+product)、`06_类`、`09_匹配`、`10_导`。
  纠正粒度：`若…则` **已实现**，`03` 挂在 `:9 或若…则` 与 `:15 否：`（缺 elif/else 分派，非重做 if）；
  `类`/`承`/`性` **已实现**，`06` 只挂在 `:9/:24 构 接收 …`（缺构造器写法）。
  `10_导` 的 LexerError 已消（单 15 词法已落地），残留 `导 X 自 Y` 解析失败，**转挂单 12**——
  **单 15 在 e2e 侧可回收 0 例**，工单「10_导 修好应真绿」不成立。
- **单 04 · 嵌套调用丢尾参（2 例）**：`05_段` `印(求和(10,20))`→`求和(10)`。工单正确。
- **单 13 · 试/捕缺 `掷`/`错误信息`（2 例）**：`07_试`。**纠正**：工单说「异常没被拦住」为假——
  三条「捕获到…」都打印了，try/except 生成正确生效；product traceback 里的
  `During handling of the above exception` 是 Python `__context__` 链式显示，被工单误读。
  真缺口只有 `掷`未映射`raise` + `错误信息`未在 except 绑定，比工单描述小。
- **单 08 · `@@contains@@` 泄漏（2 例）**：`file_search/主`。工单正确，产物 234 行占位符没替换；
  仅中缀 `A 包含 B` 泄漏，成员式 `A.包含(B)` 正常，可缩小修复面。
- **单 02 残留 · 运算符后接 ASCII 字母（2 例）**：`hello.light:18 设和为和加i`→`IDENTIFIER('和加i')`。
  即 09-B。同文件 `n乘阶乘` 已好，e2e 侧单 02 残留只剩字母尾这一种。
- **单 16 · 第三方包缺失（12 例）**：demo1(numpy)/demo2(pandas)/demo3_matplotlib/demo5_sklearn/
  all_in_one_demo(numpy)/**demo3_math(sympy)**。**纠正**：`sympy` 工单漏列，demo3_math 无处归属，须补入。
- **单 17 · 产物不自洽 `src/` 不在 sys.path（2 例，非 3）**：demo4_echarts、demo5_markdown 仅 product 红
  （run 走 CLI 有 `cli/light_unified.py` 铺路，product 裸跑没有）。产物引导段
  `src/code_generator.py:770-797` 只处理 `stdlib/`、无 `src/` 一档。工单标题「3 例」应改「2 例」。

## 新根因（工单里没有的，需新立单）

- **新单 A · 汉字数词切标识符（F3，编译期，回归面最大）**：`百分位数`→`CHINESE_NUM(100) IDENTIFIER(分位数)`。
  我复核实测更严重：`万能钥匙`→`CHINESE_NUM(10000) IDENTIFIER(能钥匙)`、`设甲为百分位数` 竟能 PARSE OK
  （静默错编）。lexer 见 `百/千/万` 即拆，撞上一切含数词的标识符，属**静默错编类，优先级高**。
  **纠正**：工单把 `F3` 归到单 11(lunardate) 是错的——F3 编译期就挂，装 lunardate 也不绿；
  **单 11 在 e2e 侧可回收 0 例**（它只影响 `tests/test_datetime.py`）。
  F3 另两个缺陷：具名实参 `f(步长天=1)` 未实现、列表推导式 `[表达式 对于 x 在 X]` 未实现（`于` 被切成两个 KEYWORD）。

  ### ✅ 新单 A 已完工（v7 收尾第 5 轮）

  **落点**：`src/lexer.py:1727` 起的「数字前缀就切」分支。
  同文件 `:1721` 的「整串都是中文数字」分支合法，必须留（`一百零一`/`三点一四` 靠它）。

  **口径矛盾（意外发现，已写进代码注释）**：lexer 有两条扫描路径，对同一串汉字给出**不同** token 流。
  慢速回退路径 `:2105-2127` / `:2117-2127` 只认 `num_len == len(full_identifier)`，`else` 整体输出标识符，
  **根本没有前缀切分**。故 `:1727` 的前缀切分是快速路径独有的越界行为，回退路径的口径才是设计本意。

  **本单口径**：`:1727` 收窄为「**仅当数词前缀之后紧跟关键字时才切**」。
  - `九十那么大` → 余部以关键字 `那么` 开头 → 照切（保住原用例）
  - `百分位数` / `万能钥匙` → 余部不以关键字开头 → 不切
  - `一百零一` / `三点一四` → 走 `:1721`，零影响
  单向收窄：原本会切的改后要么仍切要么不切，不存在「原本不切、改后反而切了」。

  **踩过的坑（务必别再犯）**：不切的那一侧**只能落空、不能自行 emit(full_identifier) 后 continue**。
  第一版那样写，绕过了 `:1762` 的用户定义前缀匹配、`:1810` 的 stdlib 名判定、`:1817` 起的嵌入关键字扫描，
  把 `二元运算符表等于_元` 整块吞成一个标识符、中间的 `等于` 关键字消失
  （`antlrparser/self_hosted/parser.light`，由全仓 token 比对抓出）。已固化为测试
  `tests/unit/test_lexer.py::test_number_prefix_word_keeps_embedded_keyword`。

  **影响面取证（全仓 37255 个 `.light`，stash 前后各转储一遍 token 流）**：
  差异文件 **29** 个、差异点 **37** 处、形状 **24** 种、**UNEXPECTED = 0**。
  每一处都是同一形状 `CHINESE_NUM(X) + IDENTIFIER(尾) → IDENTIFIER(整词)`，被修正的整词全是真实域标识符：
  `百分位数` `百分比` `百分位排名` `百分位对应分数` `四元数` `四阶矩` `四分位距` `二叉树节点` `二阶` `二分法`
  `二元运算符表` `二氧化碳吸收/释放/产生` `三分命中` `三分出手` `三阶矩` `五数汇总` `千粒重` `十六进制数字`
  `八进制数字` `一致` `一审案件` `零填充`。无任何「原本不切、改后反而切」的方向，与单向收窄论证一致。

  **回归算术（同机同会话自测）**：基线 3ade4f3c = 66 failed / 3324 passed / 99 skipped / 6 errors / collected 3492；
  本轮 = 66 failed / **3329** passed / 99 skipped / 6 errors / collected **3497**。
  collected +5、passed +5 恰为本单新增的 5 项 lexer 测试；**红转绿 0、新增打红 0**，
  FAILED 逐文件计数完全一致（44/8/2/1/2/5/3/1）。

  **诚实结论**：本单在测试盘面上**回收 0 例**——F3 还压着「具名实参」「列表推导式」两个缺陷，
  单修数词不足以让它转绿。本单的价值在于消掉 29 个文件里的 **37 处静默错译**：这些是不报错、
  产物语义却与源码不符的坑，比红用例更危险。

  **测量陷阱（第 5 轮新踩）**：跑全量回归**不能**设 `$env:PYTHONIOENCODING='utf-8'`。
  它会继承进被测 CLI 子进程、改掉子进程 stdout 编码，让三个用例假红：
  `test_full_programs.py::TestCLITools::test_light_unified_help`、
  `test_cross_platform.py::TestCrossPlatformCLI::test_encoding_cli`、
  `test_e2e_full_coverage.py::TestCompilerFullChain::test_full_pipeline_run`。
  同一 HEAD 设了该变量这 3 例红、`Remove-Item Env:PYTHONIOENCODING` 掉则 3 例全绿——已实测两侧坐实。
  另：经 PowerShell 管道再 `Out-File` 会把日志里的中文二次编码，两份日志的 FAILED 名字**逐字不可比**；
  对账时须先剥掉非 ASCII 再比（或让 python 自己写文件）。

- **新单 B · L2 文言体 OOP/模式匹配（4 例，`L2_wenyan/主程序`+`学生模块`）**：纯汉字标识符 `学生模块`
  被在内部关键字 `模块` 处切开（我复核确认 `学生模块`→`IDENTIFIER(学生) KEYWORD(模块)`，且裸用也炸，
  与单 02 的「汉字+ASCII」不同族）；另缺 `接`抽象方法、`设 名:类型` 无初值字段、`自之X=值`、
  `承…接…`、`配 真:`+`情况 <布尔>:`。是 L2 该层规范样例，全灭 ⇒ L2 OOP 合并后基本不可用。

  ### ✅ 新单 B 已完工（v7 收尾第 7 轮）· 4 例中 3 绿

  **改点**（5 个源文件）

  - `src/lexer.py`：`模块` / `标准库` / `打印` 三个词进 `IDENTIFIER_SAFE_KEYWORDS`（词尾/词中不切、词首仍是关键字）；
    修 `_skip_compound_safe_and_match` 的返回值缺陷。
  - `src/parser_stmt.py`：类体/接口体收 `段`、`设`（带/不带类型注解）；类体与接口体的 `else: break`
    改为**明确 ParseError**（裁决 A「strict」）；类头 `接` 认作 `实现` 同义词，`承`/`接` 均支持逗号多接口，
    `承 X 接 A, B:` 组合式可用；`配 真:` + 布尔守卫 `情况`、`其他` 默认支、`遍 <expr> 为 项:`、
    `导 X 出 A,B` → `from X import A,B`、try/捕/终 闭合修正；成员链后的索引后缀 `自之成绩[科目] = 分数`。
  - `src/parser_expr.py`：`段(参数) 返 <表达式>` lambda（`_parse_primary` 五元素匹配，不中就 `self.pos` 回退）；
    括号式调用收中文具名实参 `名 = 值`。
  - `src/code_generator.py`：`_MEMBER_SUFFIX_MAP` 补 `的项/的键/的值/的长度`；
    `_BUILTIN_KWARG_NAME_MAP` 做「(Python 被调名, 中文实参名)」配对翻译（`依据`→`key`、`逆序`/`倒序`→`reverse`）；
    `转成字符串` 进 builtin_map；`自`→`self`、调用侧 `构造`→`__init__`。
  - `src/code_generator_unified.py`：补 `InterfaceDefinition` 分支（此前 unified 后端静默丢接口）。

  **产品裁定** 4 个都在会话里拍过板：类体成员分派 = strict 报错；`模块`/`打印` 走安全词白名单；
  `接` 收作 `实现` 同义词；`倒序` 与 `逆序` 同为 sorted 的 `reverse`（漏收就会静默发射
  `sorted(xs, 倒序=True)` → 运行期 TypeError）。

  **影响面取证**（全仓 37255 个 `.light`，stash 前后各转储 token 流 + 产物）

  - token：差异 **12 文件 42 处 / 14 种形状 / UNEXPECTED = 0**。26 处是「合并·字面不变」
    （`段言标准库` `可打印` `可打印字符` `临时文件模块` `参数解析模块` `命令模块` `是可打印`
    `检查是否可打印` `文件匹配模块` `系统接口模块` `集合工具模块` `学生模块`）；
    另 18 处归类为 **base 字面失真 → after 忠实于源码**，即改前的 lexer 本来就在**改写源码文本**：
    `自之姓名` 被读成 `之之姓名`（16 处，`_skip_compound_safe_and_match` 返回值缺陷），
    `测试模块` 被读成 `测`+`模块`+`块`（2 处，丢了 `试` 又多个 `块`）。
    判据是把两侧字面拿回源码里找（窗口向后借最多 3 个两侧相同的 token）：源码里找不到的那侧就是错的。
  - 产物：差异 19 文件，**ERR→OK 3**（`学生模块` `主程序` `edu/课程6_列表与字典/示例`）、
    **ERR→ERR 16**（本来就红，仅报错文案变）、**OK→ERR 0**——无静默错编。

  **取证反过来咬了两处，都已处理**

  1. `bootstrap/release/stdlib/对象池缓存.light` 从干净 ParseError 退化成
     `UnboundLocalError: cannot access local variable 'IndexAccess'`。根因：`_parse_assignment_stmt`
     里原有一句**函数内** `from ast_nodes_v3 import IndexAccess`（旧 :917），
     它把 `IndexAccess` 变成整个函数的局部名，于是本轮新增的成员链索引后缀（:894）先执行时未绑定。
     本文件第 17 行已 `from ast_nodes_v3 import *`，那句函数内 import 纯属多余——已删；
     `_parse_self_assignment` 里同族的一句（旧 :702）同时清掉。改后该文件回到 ParseError。
  2. `examples/L0_core/06_类_面向对象.light` 的首个报错从「行 9 `接收`」前移到
     「行 6 `类体内不支持的成员声明：'性'`」。这是 strict 化**暴露**出的既有缺口：
     类体不认 v4.0 单字成员关键字 `性`(属性)/`构`(构造)。该文件改前改后都红，**没有绿转红**，
     但诊断现在指着真正的第一个问题而非下游症状。属 L0，不在本单范围 → 已立新单。

  **回归算术（同机同会话自测，`git stash push` 对照）**

  - 基线 = 62 failed / 3403 passed / 98 skipped / collected 3561(+2 skipped)
  - 本轮 = 59 failed / **3482** passed / 98 skipped / collected **3637**(+2 skipped)
  - skipped 两侧一致；collected **+76** 恰为本单新增的 76 项单测（`tests/test_context_manager.py`
    + `tests/unit/test_lexer.py`），passed +79 = 76 新增全绿 + 3 例红转绿，算术自洽。
  - **红转绿 3 条**：`test_duan_run[L2_wenyan/学生模块.light]`、
    `test_duan_compile_and_run_product[L2_wenyan/学生模块.light]`、
    `test_duan_run[L2_wenyan/主程序.light]`。
  - **新增打红 0 条。**

  **诚实结论：可回收 3 例，第 4 例留红。** `学生模块.light` 全链绿（run/compile/产物 rc=0）；
  `主程序.light` run 腿绿且输出全对（等级、平均分、`配` 分布、try/捕/抛/终、降序排名）。
  剩下 `test_duan_compile_and_run_product[L2_wenyan/主程序.light]` 仍红，根因**不在本单**：
  `duan compile` 产出的产物不自包含——跨模块 `导 学生模块` 在产物里变成裸 `import`，
  而 `_register_user_modules`（`cli/light_unified.py:130-220`）只在 `run` 路径做进程内注册，
  compile 路径不落地依赖模块。这是编译器缺陷，不是台架问题；排除该用例或改台架都属于
  「改断言让测试变绿」，故按第二条走：**留红 + 另立新单**。

  **单测** `tests/test_context_manager.py` + `tests/unit/test_lexer.py` 共 130 项全绿。
  其中 4 条断言在自测时被判**写错并已修正**（不是产物错）：3 条 `TestBracketCallKeywordArgs`
  期望中文实参名原样透传（`sorted(xs, 依据=f)`），那正是运行期 TypeError 的形状，正确期望是 `key=`；
  1 条 `test_类体内设带初值成为类字段` 把自己输入里的 `: 数` 注解漏掉了，正确期望是 `总数: float = 0`。
  修断言前先报了原因与依据并取得裁定，未做「凑绿」。

  ### 🆕 本轮新立的单（均已取证，未修）

  - **新单 D · `duan compile` 产物不自包含（跨模块 `导` 不落地依赖）**：`L2_wenyan/主程序.light`
    product 腿唯一红因。run 腿靠 `cli/light_unified.py:130-220` 的 `_register_user_modules`
    进程内注册依赖模块才通；compile 腿不做这件事，产物里只剩裸 `import 学生模块`。
    需求：compile 时解析 `导` 依赖图并把依赖模块一并产出到输出目录（或生成可定位的 sys.path 引导）。

    ### ✅ 新单 D 已完工（v7 收尾第 8 轮）· 回收 1 例

    **更正上面的描述**：产物里那句不是裸 `import 学生模块`，而是
    `from 学生模块 import 人, 学生, 可打印`（`src/code_generator.py:_generate_import_stmt`
    的 from-import 分支）。译法本身是对的，缺的是被导入的那个模块没落盘。

    **根因**：`_register_user_modules` 的产出是**进程内内存对象**——
    `exec` 出模块命名空间 → `types.ModuleType` → 塞 `sys.modules`。
    `run` 在同一进程里 `exec` 产物，命中 `sys.modules` 缓存所以能跑；
    `compile` 写完 `.py` 进程就退出，注册全部蒸发，磁盘上那句 from-import 成了悬空引用。
    这个机制天生只服务 in-process 执行，架构上无法为独立产物服务。

    e2e 把产物写到**系统临时目录**（`tests/e2e/test_e2e_chain.py` 的
    `TemporaryDirectory()` + `-o <tmp>/product.py`），所以连「恰好 cwd 有依赖」这条侥幸
    路径也没有：实测产物直接 `ModuleNotFoundError: No module named '学生模块'`。

    **改点**（只动 `cli/light_unified.py`）
    - 抽出 `_find_user_module_path(模块名, 源目录)`：在源文件目录、再退一级目录里找
      `<模块名>.light`。**run 与 compile 两条路径共用这一套查找判据**——两边各写一份
      正是本单的成因（run 找得到、compile 找不到，于是 run 绿、产物红）。
    - 抽出 `_iter_user_module_deps(源码, 源目录, seen)`：只认用户模块（跳过
      `KNOWN_STDLIB`、跳过 `language` 为 python/c 的外语导入、跳过磁盘上没有 `.light` 的），
      yield 前先入 `seen` 防循环依赖。`_register_user_modules` 改为复用它。
    - 新增 `_emit_user_modules(源码, 源目录, 输出目录, emitted)`：把依赖模块逐个
      parse+generate 落盘成 `<输出目录>/<模块名>.py`，**递归**处理依赖的依赖。
      依赖自己编不过时打 `[警告]` 而不是静默产出半残产物。
    - `compile_with_src` 写完主产物后调用它，并打印 `已随产物生成依赖模块: 学生模块.py`。

    **为什么落盘 `.py` 到产物同目录就够**：Python 执行脚本时把脚本所在目录放到
    `sys.path[0]`。产物前导码装的 `.light` 导入钩子搜索的是
    `[_light_stdlib, _light_file_dir, os.getcwd()]`（`src/code_generator.py:637`），
    **不含源文件目录**，所以指望钩子去 `examples/L2_wenyan/` 捞 `.light` 是走不通的；
    直接把编好的 `.py` 放成产物的兄弟文件最省事，也和既有先例一致
    （`cli/light_unified.py:pkg_build`、`src/incremental_build.py:_compile_files`
    都是产物与源同目录同名）。

    **过程事故（记下来，同一个坑第二次踩）**：`if output_file:` 这段文本在
    `compile_with_antlr` 与 `compile_with_src` 里**一字不差地各有一份**，
    按原文替换命中的是前者，于是补丁打进了没有 `source_file` 参数的 ANTLR 方法，
    还带错了缩进 → `IndentationError`，L2 四例全红。教训：在这个文件里按文本改之前
    先确认命中的是哪个方法，最省事的判据是把后续几行（`except SyntaxError` vs
    `except Exception`）一起纳入匹配串。

    **回归算术（同机同会话自测，`git stash push -- cli/light_unified.py` 对照）**
    - 基线 = 59 failed / 3482 passed / 98 skipped / collected 3637(+2 skipped)
    - 本轮 = **58** failed / **3483** passed / 98 skipped / collected 3637(+2 skipped)
    - collected 与 skipped 两侧完全一致（本单不新增测试）。
    - **红转绿 1 条**：`test_duan_compile_and_run_product[L2_wenyan/主程序.light]`
    - **新增打红 0 条。**

    **诚实结论：回收 1 例，新单 B 的 4 例至此全绿。** 未做的部分也说清：
    依赖发现仍只查「源目录 + 上一级目录」两处（沿用 run 路径的既有判据，本单没扩），
    所以放在更深子目录的依赖仍然找不到；`.light` 导入钩子的搜索路径没动。
    本单不新增单测——CLI 类在 `tests/` 下没有对应测试文件，按「不为改动新建测试文件」
    的约定，守卫就是那条红转绿的 e2e。

  - **新单 E · 类体不认 v4.0 单字成员关键字 `性` / `构`**：`examples/L0_core/06_类_面向对象.light`
    行 6 `性 名称`、行 9 `构 接收 …`。该文件头部注释自报用 v4.0 单字关键字
    「类/承/性/构/己/新」，但 `_parse_class_definition` 的成员分派白名单只有
    「属性/私属性/设/构造/函数/段落/段/私段落/@装饰器/过/结束」。strict 化后报错点前移暴露此缺口。

    ### ✅ 新单 E 已完工（v7 收尾第 9 轮）· 回收 2 例（e2e 两腿）

    **诊断**：整个文件其实用了一整套 v4.0 单字：`性`（属性）、`构`（构造）、
    `构 接收 …`（构造形参）、`己名称 为 名称`（self 赋值，已支持）、`段`（方法，已支持）、
    `父.构(…)`（super().__init__）、`新 狗(…)`（实例化）、`类 狗 承 动物`（继承，已支持）。
    token 探针（`.scratch/tok.py`）证实 `性`/`构`/`新` 在词法层是 **IDENTIFIER**，
    不是 KEYWORD。

    **关键取舍：`性`/`构`/`新` 有意不升为保留字，改走「按位置识别」。**
    这三个是超高频构词字（性能/性别、结构/构建、新增/更新/最新）。若加进
    `keywords.ALL_KEYWORDS`，词法层会把海量标识符切碎——血溅半径远大于本单收益，
    与单 C 的 `r` 前缀、单 B 的复合安全词同属一类判断。改为在解析器按上下文形状
    识别，词法零改动，因此无需跑 A/B token 流，只需跑产物 A/B。

    **改点（4 文件）**
    - `src/parser_stmt.py` 类体成员分派：新增 `IDENTIFIER('性')`→属性、
      `IDENTIFIER('构')`→构造两条分支（安全性：该位置的 IDENTIFIER 原本一律落到
      末尾 `self._error`，这两支只把「原本报错」变「能解析」，动不了任何既有能过的语义）。
    - `src/parser_stmt.py` `_parse_attribute_declaration` / `_parse_method_definition(is_constructor)`：
      各加一个「若当前是 IDENTIFIER `性`/`构` 就 consume，否则按老路 consume KEYWORD」的兼容口。
    - `src/parser_expr.py` `_parse_primary`：`IDENTIFIER('新') + IDENTIFIER(类名) + LPAREN`
      三 token 形状 → `ClassInstantiation`（只收带括号形式，无括号与「标识符并列」歧义，留给 `新建`）。
    - `src/code_generator.py` + `src/code_generator_unified.py`：把 `构` 并入
      `_CTOR_NAMES`（`('构造','初始化','构')`），并把 unified 的 `method_map` 也补 `'构':'__init__'`，
      使 `父.构(…)` 两后端一致地发 `super().__init__(…)`。`_generate_class_definition`
      里两处硬编码 `('构造','初始化')` 一并改读 `self._CTOR_NAMES`，消掉「定义识别与调用识别分叉」的隐患。

    **产物**（`06_类_面向对象.light`）：`class 动物` / `class 狗(动物)` /
    `def __init__(self, …)` / `super().__init__(名称, 年龄)` / `旺财 = 狗("旺财", 3, "金毛")`，
    exec rc=0。

    **两后端口径守卫**：改 `src._CTOR_NAMES` 当场打红
    `TestBackendParity::test_ctor_names_match`（断言两后端三张口径表逐项相等）。
    按硬规矩**未改断言**——断言是对的，是我漏改了 unified 一侧；补上 unified 的
    `_CTOR_NAMES` 与 `method_map` 后自然复绿。这正是这条守卫存在的意义。

    **全仓产物 A/B**（37255 个 `.light`，`.scratch/prodab.py` 快照 sha256 比对）：
    ERR→OK **1**（本例），OK→ERR **0**，OK→OK 产物变化 **0**，ERR→ERR 文本变化 **0**。
    零血溅，证实「按位置识别、不升关键字」的判断成立。

    **同机自测回归**（stash 基线，同一会话）：collected 3637 两侧一致，skipped 98 一致，
    58 failed → **56 failed**。红转绿 2：
    `test_e2e_chain.py::test_duan_run[…06_类_面向对象.light]`、
    `test_e2e_chain.py::test_duan_compile_and_run_product[…06_类_面向对象.light]`；
    新增打红 **0**。

    **补测**：`tests/test_context_manager.py::TestL0SingleCharClassKeywords` 7 例，
    既断产物形状（`super().__init__` 不是 `super().构`）也 exec 真跑；另含两条反向
    用例守「不该识别的别识别」：`设 新 为 7` 仍是普通变量、`性能`/`结构` 不被切碎。


  - **新单 F · `_skip_compound_safe_and_match` 同族的其余复合安全词**：本轮只修了返回值缺陷本身，
    诊断阶段扫到同族切分点约 161 处，未逐一坐实哪些是真错译。需要一轮专门取证。
  - **新单 G · `设 x, y: 类型` 多目标带注解未支持**（诊断期撞到，本单绕开）。

    ### ✅ 新单 G 已完工（v7 收尾第 10 轮）· 回收 0 例（语料零实例）

    **必须先说的事实：这个形状在被测输入里一例都没有。** 用两条互不相同的正则
    扫全仓 37255 个 `.light`（`^\s*设\s+\S+\s*,\s*\S+\s*(:|：)` 与
    `^\s*设[^。\n]*,[^。\n]*(:|：)[^。\n]*(为|=)`），命中 0。语料里真实存在的
    多目标写法是**无注解**的 `设 表头, 数据 为 读取CSV(文件)`（13 处 / 5 文件），
    而这个形状**本来就是好的**——实测 2 目标、3 目标、`为` 与 `=` 两种连接词
    全部正常发 `甲, 乙 = 造()`。所以本单不回收任何红，纯粹是补语法完整性。

    **规范也没裁定。** `docs/统一语法规范_v3.1.md:635-636` 只写了元组解构
    `设（甲，乙）为 值` 和列表解构 `设 [首，余] 为 值`，
    `docs/从段言到Python.md:67` 只写了 `设 x, y 为 10, 20` → `x, y = 10, 20`。
    多目标带注解在任何一份规范里都没有条款。

    **口径由用户裁定 = broadcast（注解广播到每个目标）。**
    定这个口径的硬约束是：Python **不允许**给解包目标加注解——
    `甲, 乙: float = f()` 是 SyntaxError。所以不存在「直译」这条路，
    只能拆成「每个目标一条纯注解行 + 一条解包语句」：
    ```
    设 甲, 乙: 数 为 造()。
    →
    甲: float
    乙: float
    甲, 乙 = 造()
    ```
    纯注解行不产生赋值、不覆盖随后解包出来的值，只登记类型。

    **改点（4 文件）**
    - `src/ast_nodes_v3.py` `DestructuringAssignment`：`__slots__` 与 `__init__`
      加 `type_annotation`（默认 None）。
    - `src/parser_stmt.py` 多目标分支：收完变量名后、要求 `为/等于/=` 之前，
      允许可选的 `: 类型`（复用单目标路径同一个 `_parse_type_annotation`）。
      原先撞到冒号只会报「期望'为'或'等于'，但得到 冒号」——位置对，
      但话把人往「你漏写了为」上引。
    - `src/code_generator.py` `DestructuringAssignment` 分支：有注解就先按
      `_map_type` 逐个目标发纯注解行。
    - `src/code_generator_unified.py` `_generate_destructuring`：同口径补上
      （用该文件的 `_map_return_type`，它与 src 的 `_map_type` 同表）。
      两后端一起改是单 E 的教训——只改一边下次就是 parity 守卫打红。

    **全仓产物 A/B**（37255 个 `.light`，逐文件 sha256）：
    ERR→OK 0 · OK→ERR 0 · OK→OK 产物变化 0 · ERR→ERR 文本变化 0。
    四个零正是预期：语料零实例，且无注解路径 `ann=None` 直接跳过广播，
    老行为一个字节都没动。

    **同机自测回归**（stash 基线，同一会话）：failed 56 → 56，skipped 98 一致，
    红转绿 0，新增打红 0。collected 3644 → 3647，**+3 的来源是我新加的 3 条测试**
    （基线跑时把测试文件一起 stash 了，故差值精确等于新增用例数）。

    **补测**：`tests/test_context_manager.py::TestMultiTargetAnnotation` 3 例
    （2 目标广播 / 3 目标广播 / 无注解形状产物不变），都 exec 真跑；
    并显式断言产物里**不出现** Python 非法的 `甲, 乙: float = ...`。

  - **新单 H · `b''` bytes 前缀**（新单 C 已备案的附带发现，`contrib/HTTP客户端.light`）。
  - **新单 I · `超` = super 未实现**。
  - **新单 J · `配 X 之:` 与 `若/否` 的规范口径需统一**（`配` 模式匹配存在两套写法，规范文档未裁定）。


- **新单 C · `r"…"` 原始字符串前缀未实现（2 例，demo2_regex）**：`r"…"`→`IDENTIFIER(r) STRING(…)`，
  parse OK 但生成 `r(...)` 调用 → 运行期 `name 'r' is not defined`。静默错编类。

  ### ✅ 新单 C 已完工（v7 收尾第 6 轮）

  **改点** `src/lexer.py` 主循环 f-string 特判旁，新增 `r`/`R` 紧贴引号的前缀判据，并给 `_tokenize_string` 加 `raw` 参数。
  判据与既有 `f` 前缀同源：字母**紧贴**引号才当前缀（`r"x"` 是前缀、`r "x"` / `设 r 为 1` 不是）。
  落点在 ASCII 分派处，**不碰** `_tokenize_chinese_sequence` 的两条汉字路径，故不涉及新单 A 注释警告的「绕过下游 :1762/:1810/:1817」。

  **raw 语义不能省**：`_tokenize_string:1017-1044` 会翻译 `\n \t \r \\ \x.. \0`（仅「未识别转义」如 `\d` 才原样保留）。
  只改调用形状而不加 raw，等于把「调用错译」换成「转义错译」。

  **前缀集合产品裁定：只做 `r`/`R`**。依据 `docs/L2_文言体语法规范_v4.0.md:93-96`（唯一把字符串前缀写进规范处，只列 `r`）。
  `f` 不动（裸串已由 codegen 自动插值，加 `f` 冗余）；`b` 另立单（bytes 语义需 codegen 配合，`contrib/HTTP客户端.light` 的 `b''` 归那单）；`u` 不做（Python 3 空操作）。
  规范文档 3 处自相矛盾（`段言-完整规范文档.md:3631` 说不支持 f-string，`syntax.md`/`L2 规范` 又给 f-string 示例）已备案，待统一。

  **影响面取证**（全仓 37255 个 `.light`，stash 前后各转储 token 流）：差异 **8 文件 64 处、UNEXPECTED 0**，全是 `IDENTIFIER(r)+STRING → STRING`。
  其中 61 处内容不变（纯合并两 token），3 处内容也变——正是 raw 生效处，全落在预判的
  `bootstrap/release/stdlib/字符串工具.light`（`\r?\n`、`[<>"\']`）与 `正则表达式.light`（`\r?\n`）。
  这 3 处旧值在正则里凑巧语义等价（`\x0d?` 的 `?` 仍作用于那个 CR 字符），换成 `\x`/`\0` 就会真坏；现在忠于源码。

  **诚实结论：可回收 1 例**。`demo2_regex` 现完整跑通（邮箱校验、手机号提取、命名组、日期替换全对）；
  工单记的另一例 `all_in_one_L3_demo.light` 因 8 个无关语法错误（`；`/裸数字行，属簇 11 未坐实那族）先挂，`r` 前缀只是夹带，修完仍挂。

  **附带发现（非本单，备案）**：`contrib/HTTP客户端.light` 的 `b''` 字节串当前同样被错译成 `b('')`，与本单同族。

- **簇 11（E 阶段/L3，约 6 例，取证被截断未完全取回）**：`E1_L3_SQL原生`、`E4_L4_沙箱隔离`、
  `all_in_one_L3_demo`。其中 E1_L3_SQL 第 4 轮另测到运行期 `name 'l3_sql_成绩_q4' is not defined`
  （遍历循环层已通、SQL 参数化命名缺陷）。**这三条 e2e 需补一轮取证**，本轮未坐实，勿凭工单归类。

## 例数对账

已精确归因 38 例（单12:8 + 单04:2 + 单13:2 + 单08:2 + 单02:2 + 单16:12 + 单17:2 + 新A:2 + 新B:4 + 新C:2），
余约 6 例在簇 11（E/L3），合计 44，与 e2e 自测基线一致。整仓 68 failed 中其余部分在非 e2e 文件
（`test_datetime` 8 = 单11、`test_self_host_bootstrap`+`test_e2e_full_coverage` = 单10、
`test_lsp_protocol` 2 = 单14、`test_stdlib_complete` 3 = 抖动待定、`test_level6_types` 1 = TypeParser），
另有 6 errors = `test_lightpub_bridge`（requests 缺包）。

**第 5 轮更正**：上面「新A:2」是**归因**数而非**可回收**数。新单 A 已修完，实测 e2e 侧
**可回收 0 例**——F3 还压着「具名实参」「列表推导式」两个缺陷。同理需警惕：本文所有
「N 例」若未标明是复跑坐实的可回收数，都只是归因数，勿当作修完就能减掉的红数。

**第 7 轮更正（新单 B）**：归因 4 例，**实测可回收 3 例**（`学生模块` run/product + `主程序` run），
第 4 例 `主程序` product 腿留红并转入新单 D。同机自测：62 failed → **59 failed**，
新增打红 0。本轮另新立 D–J 共 7 个单（D/E 有取证，F–J 为诊断期备案）。


