# 30 分钟入门段言

> **版本：** v6.0
> 段言是一门中文编程语言——关键字用中文书写，同时支持中文运算符和符号运算符，让零基础用户也能快速上手编程。
>
> 本教程专为 **从未写过代码** 的中文用户设计，跟着读完即可独立编写简单程序。

---

## 📋 教程地图

```
① 安装环境  ──→  ② 第一个程序  ──→  ③ REPL 练手  ──→  ④ 基础语法
                                                      │
                                                      ↓
⑧ 发布程序  ←──  ⑦ 调试排错  ←──  ⑥ 异常处理  ←──  ⑤ 高级语法
                                                      │
                                                      ↓
                                              ⑨ 从错误中学习
```

**预计时间：** 30 分钟  
**适合人群：** 完全零基础的中文用户  
**学完你将能：** 独立编写、调试、编译段言程序

---

## 🔰 第一部分：环境准备（5 分钟）

### 1.1 安装 Python——为什么段言需要它？

段言的语言本身是用 Python 编写的，所以它需要 Python 环境来运行。你可以把 Python 想象成「语言的土壤」——段言这棵「树」生长在 Python 的土壤上。

**为什么不用 C++ 或 Rust 写编译器？** 因为 Python 生态成熟、跨平台方便，而且段言的目标用户是中文初学者，降低安装门槛比追求极致性能更重要。

**操作步骤：**

1. 访问 [python.org](https://www.python.org/downloads/)，下载最新版 Python（3.10 或更高）
2. 运行安装程序，**务必勾选**「Add Python to PATH」
   > 为什么勾选这个？它让系统知道「python」这个命令在哪，否则你敲 `python` 时系统会说「找不到命令」
3. 一路「下一步」完成安装

**验证安装：** 打开终端（Windows 按 `Win+R`，输入 `cmd` 回车），输入：

```bash
python --version
```

看到类似 `Python 3.12.x` 就说明成功了。

---

### 1.2 安装段言

在终端中执行：

```bash
pip install duan
```

**为什么用 `pip`？** pip 是 Python 的包管理器——就像手机上的应用商店。你输入 `pip install 包名`，它就会从 PyPI（Python 包仓库）下载软件并自动安装。

**验证安装：**

```bash
duan --version
```

看到 `段言编译器 v6.x.x` 说明安装成功。

---

### 1.3 你的第一个段言程序

创建文件 `hello.duan`，写入：

```段言
打印 "你好，段言！"
```

**为什么文件后缀是 `.duan`？** 后缀名告诉计算机「这是一个段言源文件」，就像 `.docx` 表示 Word 文档一样。

运行它：

```bash
duan run hello.duan
```

看到 `你好，段言！` 输出——恭喜，你已经跑通了第一个段言程序！

> **为什么 `打印` 不需要括号？** 段言中 `打印` 是内置命令，加括号也可以（`打印("你好")`），两种写法等价。这是为了让零基础用户少记一个语法规则。

---

### ✅ 第一部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 安装 Python 并理解为什么需要它 | ☐ |
| 用 pip 安装段言 | ☐ |
| 创建并运行第一个段言程序 | ☐ |
| 理解 `duan run` 命令的作用 | ☐ |

---

## 🚀 第二部分：第一个完整程序（5 分钟）

### 2.1 变量——为什么需要「盒子」？

变量就像厨房里的储物盒——你把不同的东西放进不同的盒子，需要时打开盒子取出来。计算机用变量来「记住」数据。

```段言
设 名字 为 "张三"
设 年龄 为 25
设 身高 为 1.75

打印 名字
打印 年龄
打印 身高
```

**语法拆解：**
- `设` → 告诉段言「我要创建一个变量」
- `名字` → 给盒子贴标签（变量名）
- `为` → 把右边的值放进去
- `"张三"` → 带引号的是文本（字符串），不带引号的是数字

**为什么用 `设` 而不是 `var` 或 `let`？** 段言的目标是让中文母语者用母语思考。「设」在中文里就是「设定一个变量」的意思，不用先翻译成英文再理解。

---

### 2.2 字符串插值——把变量嵌入文字

如果想把变量嵌入到一段文字中，用 `f` 开头的字符串（f-string）：

```段言
设 名字 为 "段言"
设 版本 为 1.11

打印(f"语言：{名字}，版本：{版本}")
```

**为什么叫 f-string？** 这里的 `f` 代表「format」（格式化）。`{名字}` 就像一个占位符，运行时会被变量的实际值替换。输出：

```
语言：段言，版本：1.11
```

---

### 2.3 注释——给未来的自己写纸条

`#` 开头的是注释——程序会跳过它，完全不执行。注释是写给**人**看的：

```段言
# 计算年龄
设 当前年份 为 2026
设 出生年份 为 2000
设 年龄 为 当前年份 - 出生年份  # 结果：26
打印 年龄
```

**为什么需要注释？** 你写代码时头脑清醒，但一个月后回来看，可能完全想不起当初为什么这么写。注释就是给「未来的自己」留的纸条。

---

### 2.4 交互式输入——让程序和你对话

使用 `输入` 函数让程序等待用户输入：

```段言
设 名字 为 输入("请输入你的名字：")
打印(f"你好，{名字}！欢迎学习段言！")
```

**为什么需要 `输入`？** 没有它，程序只能输出固定的内容。有了它，程序可以根据用户的输入做出不同响应——这才是真正的「交互」。

---

### 2.5 句号是可选的

段言语句末尾的句号「。」是**可选的**——加了不影响运行，不加也完全没问题。本教程的示例一律不加句号，保持简洁。

**为什么设计成可选的？** 中文编程的初心是降低门槛。强制句号会让初学者多一个「忘记加句号」的报错，这和段言的设计理念不符。

---

### ✅ 第二部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 用 `设` 创建变量并理解其含义 | ☐ |
| 使用 f-string 做字符串插值 | ☐ |
| 用 `#` 写注释 | ☐ |
| 用 `输入` 获取用户输入 | ☐ |

---

## 💻 第三部分：REPL——边敲边学（5 分钟）

### 3.1 什么是 REPL？

REPL 是 **Read-Eval-Print Loop**（读取-求值-打印-循环）的缩写。简单说，就是一个「敲一行，执行一行」的交互式环境。

**为什么需要 REPL？** 你想试试某个语法对不对，不用创建文件、不用保存、不用 `duan run`——直接敲进去就能看到结果。这是学习新语言最快的方式。

启动 REPL：

```bash
duan repl
```

你会看到类似这样的提示符：

```
段言 REPL v6.0
>>> 
```

### 3.2 REPL 基础操作

试试在 `>>>` 后面敲这些：

```段言
>>> 打印 "你好，REPL！"
你好，REPL！

>>> 设 甲 为 10
>>> 设 乙 为 20
>>> 甲 + 乙
30

>>> 10 * 3 + 5
35
```

**为什么最后一个没写 `打印` 也有输出？** 在 REPL 中，输入一个表达式，它会自动计算并显示结果。这让你可以快速验证想法。

### 3.3 REPL 中的多行代码

复杂语句需要多行时，REPL 会自动识别：

```段言
>>> 如果 10 > 5：
...     打印 "10 大于 5"
... 否则：
...     打印 "这个不会被执行"
...
10 大于 5
```

看到 `...` 提示符表示 REPL 在等你继续输入。

### 3.4 REPL 实用技巧

```段言
>>> # 快速计算
>>> 1024 * 768
786432

>>> # 字符串操作
>>> "段言" + "真棒"
段言真棒

>>> # 查看帮助
>>> 帮助(打印)
打印(value) → 输出内容到屏幕
```

退出 REPL 按 `Ctrl+Z` 回车（Windows）或 `Ctrl+D`（Mac/Linux），也可以输入 `退出()`。

---

### ✅ 第三部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 启动和退出 REPL | ☐ |
| 在 REPL 中直接计算表达式 | ☐ |
| 在 REPL 中编写多行代码 | ☐ |
| 理解 REPL 和普通运行的区别 | ☐ |

---

## 🔢 第四部分：基本运算与条件判断（5 分钟）

### 4.1 算术运算——为什么有两种写法？

段言同时支持**符号运算符**和**中文运算符**，两者完全等价：

```段言
设 甲 为 10
设 乙 为 3

打印(甲 + 乙)       # 加法 → 13    等价于：甲 加 乙
打印(甲 - 乙)       # 减法 → 7     等价于：甲 减 乙
打印(甲 * 乙)       # 乘法 → 30    等价于：甲 乘 乙
打印(甲 / 乙)       # 除法 → 3.333  等价于：甲 除 乙
打印(甲 // 乙)      # 整除 → 3     等价于：甲 整除 乙
打印(甲 % 乙)       # 取余 → 1     等价于：甲 取余 乙
打印(甲 ** 乙)      # 幂 → 1000    等价于：甲 幂 乙
```

**为什么设计两种写法？** 新手可能记不住 `//` 表示整除、`%` 表示取余——但「整除」「取余」一看就懂。同时保留符号写法是为了让有编程经验的用户能快速上手。**推荐使用符号写法**，更简洁通用。

> 💡 **小提示：** `%` 取余在实际编程中非常常用——判断奇偶（`数 % 2 == 0`）、循环取数等场景都会用到。

---

### 4.2 比较运算

比较的结果是 `真`（成立）或 `假`（不成立）：

```段言
设 甲 为 10
设 乙 为 5

打印(甲 > 乙)        # 真    等价于：甲 大于 乙
打印(甲 < 乙)        # 假    等价于：甲 小于 乙
打印(甲 == 乙)       # 假    等价于：甲 等于 乙
打印(甲 != 乙)       # 真    等价于：甲 不等于 乙
打印(甲 >= 乙)       # 真    等价于：甲 大于等于 乙
```

**注意：** `==` 是「判断相等」，`=` 是「赋值」。这是编程初学者最容易犯的错误之一——后面「从错误中学习」部分会详细讲。

---

### 4.3 逻辑运算——组合多个条件

用 `且`（两个都满足）、`或`（满足其一即可）、`非`（取反）连接多个条件：

```段言
设 年龄 为 20
设 有身份证 为 真

如果 年龄 >= 18 且 有身份证：
    打印 "可以上网"
```

---

### 4.4 条件判断——让程序做决策

```段言
设 分数 为 85

如果 分数 >= 90：
    打印 "优秀"
否则如果 分数 >= 60：
    打印 "及格"
否则：
    打印 "不及格"
```

**为什么用冒号和缩进？** 冒号（`：`）告诉段言「接下来是条件成立时要执行的代码」，缩进（前面空两格或四格）标示哪些代码属于这个条件。这是 Python 和段言共同的风格——**强制缩进让代码结构一目了然**。

---

### 4.5 三元表达式——一行写完二选一

```段言
设 数 为 10
设 结果 为 "偶数" 如果 数 % 2 == 0 否则 "奇数"
打印 结果    # 输出: 偶数
```

**为什么叫「三元」？** 因为它有三个部分：条件（`数 % 2 == 0`）、条件成立时的值（`"偶数"`）、条件不成立时的值（`"奇数"`）。

---

### 4.6 循环——让计算机做重复的事

计算机最擅长的事情就是——**不厌其烦地重复**。

**当循环（while）——「只要条件成立，就一直做」：**

```段言
设 计数 为 1
当 计数 <= 5：
    打印(f"第{计数}次")
    计数 = 计数 + 1
```

**为什么叫「当」？** 你读出来就是「当计数小于等于5时，执行大括号里的代码」——和日常说话完全一致。

**遍历循环（for）——「对每个元素做一次」：**

```段言
遍历 项 在 1 到 5：
    打印 项
```

**遍历列表：**

```段言
设 水果 为 ["苹果", "香蕉", "橘子"]
遍历 水果 为 果：
    打印 果
```

**循环控制：**

- `跳出`：立刻结束整个循环（相当于 break）
- `跳过`：跳过当前这一轮，直接进入下一轮（相当于 continue）

```段言
遍历 项 在 1 到 10：
    如果 项 % 2 == 0：
        跳过           # 跳过偶数
    如果 项 > 7：
        跳出           # 超过 7 就停止
    打印 项            # 输出：1, 3, 5, 7
```

---

### ✅ 第四部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 算术运算（符号和中文两种写法） | ☐ |
| 比较运算和逻辑运算 | ☐ |
| `如果/否则如果/否则` 条件判断 | ☐ |
| 三元表达式 | ☐ |
| `当` 循环和 `遍历` 循环 | ☐ |
| `跳出` 和 `跳过` 控制循环 | ☐ |

---

## ⚡ 第五部分：段落（函数）——把代码打包（3 分钟）

### 5.1 定义和调用——为什么需要「段落」？

想象你每天都在写：

```段言
打印 "你好，张三！"
# ... 50行代码 ...
打印 "你好，张三！"
```

太重复了！函数就是把这些代码「打包」成一个名字，需要时叫一下名字就行：

```段言
段落 问好 接收 名字：
    打印(f"你好，{名字}！")

问好("张三")   # 输出：你好，张三！
问好("李四")   # 输出：你好，李四！
```

**为什么叫「段落」？** 文章由段落组成，程序也由一个个「段落」组成。每个段落做一件事，组合起来就是一个完整的程序。

**语法拆解：**
- `段落` → 关键字，告诉段言「我要定义一个函数」
- `问好` → 函数名，你自己起
- `接收 名字` → 参数列表，调用者传入的数据
- `：` → 后面缩进的代码是这个「段落」的内容
- `返回` → 把结果「抛」回给调用者

### 5.2 返回值

```段言
段落 加法 接收 a, b：
    返回 a + b

设 结果 为 加法(3, 5)
打印 结果    # 输出: 8
```

**为什么需要 `返回`？** 没有 `返回`，函数虽然计算了 `a + b`，但结果只在函数内部「消化」了，外面拿不到。`返回` 就是把结果「传递」出来。

### 5.3 默认参数——给参数一个「保底值」

```段言
段落 问候 接收 名字 = "世界"：
    打印(f"你好，{名字}！")

问候("张三")    # 你好，张三！
问候()          # 你好，世界！
```

**为什么需要默认参数？** 有些参数大多数时候用同一个值，只有少数情况需要改。默认参数让你少写代码。

### 5.4 递归——函数调用自己

```段言
段落 阶乘 接收 n：
    如果 n <= 1：
        返回 1
    返回 n * 阶乘(n - 1)

打印 阶乘(5)    # 输出: 120
```

**递归是怎么工作的？** `阶乘(5)` 会调用 `阶乘(4)`，`阶乘(4)` 会调用 `阶乘(3)`……直到 `阶乘(1)` 直接返回 1，然后结果一层层「传回来」：

```
阶乘(5) = 5 * 阶乘(4)
        = 5 * 4 * 阶乘(3)
        = 5 * 4 * 3 * 阶乘(2)
        = 5 * 4 * 3 * 2 * 阶乘(1)
        = 5 * 4 * 3 * 2 * 1
        = 120
```

**⚠️ 递归必须有「出口」**：也就是 `n <= 1` 时直接返回，否则会无限循环下去。

---

### ✅ 第五部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 用 `段落` 定义函数 | ☐ |
| 用 `返回` 返回结果 | ☐ |
| 使用默认参数 | ☐ |
| 理解递归的基本原理 | ☐ |

---

## 📦 第六部分：列表、字符串与字典（3 分钟）

### 6.1 列表——多个东西放一起

```段言
设 水果 为 ["苹果", "香蕉", "橘子"]
打印 水果[0]    # 输出：苹果（为什么从0开始？下文解释）
水果.追加("葡萄")
打印 长度(水果)  # 输出：4
```

**为什么索引从 0 开始？** 计算机内部，列表是一块连续的内存地址。`[0]` 表示「第一个元素的位置」，偏移 0 个单位。`[1]` 表示偏移 1 个单位。虽然初学者觉得从 1 开始更自然，但几乎所有编程语言都从 0 开始——这是编程的「通用约定」。

### 6.2 列表推导——一行代码生成列表

```段言
设 平方数 为 [x * x 遍历 x 之 范围(1, 6)]
打印 平方数    # 输出：[1, 4, 9, 16, 25]
```

**读法：** 「x 乘 x，遍历 x 从 1 到 5 的范围」——和中文的表达顺序完全一致。

带条件筛选：

```段言
设 偶数 为 [x 遍历 x 之 范围(1, 11) 若 x % 2 == 0]
打印 偶数  # [2, 4, 6, 8, 10]
```

### 6.3 字符串操作

```段言
设 名字 为 "段言"
打印 长度(名字)  # 输出：2
打印 名字[0]    # 输出：段
打印 名字 + "真棒"  # 输出：段言真棒
```

### 6.4 字典——键值对映射

```段言
设 学生 为 {"名字": "张三", "年龄": 25, "成绩": 95}
打印 学生["名字"]    # 输出：张三
打印(f"{学生['名字']}的年龄是{学生['年龄']}")
```

**为什么需要字典？** 列表用数字索引（0, 1, 2...），但数字不直观。字典让你用「名字」这样的文字来查找——就像查字典一样。

---

### ✅ 第六部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 创建和操作列表 | ☐ |
| 列表推导式 | ☐ |
| 基本的字符串操作 | ☐ |
| 创建和访问字典 | ☐ |

---

## 🏗️ 第七部分：类与对象（3 分钟）

### 7.1 面向对象——为什么需要类和对象？

写程序时，我们经常需要表示「现实世界的事物」——比如一个学生有名字、年龄、成绩，还能计算平均分。用之前学的变量可以做到，但会很零散：

```段言
设 张三的名字 为 "张三"
设 张三的年龄 为 20
设 张三的成绩 为 [85, 92, 78]
# ... 如果有10个学生，就有30个变量，太乱了！
```

**类** 就是「蓝图」，**对象** 就是根据蓝图造出来的「实物」：

```段言
类 学生：
    属性 名字, 年龄, 成绩

    构造 接收 名字, 年龄：
        己.名字 为 名字
        己.年龄 为 年龄
        己.成绩 为 []

    段落 添加成绩 接收 分数：
        己.成绩.追加(分数)

    段落 平均分 接收：
        如果 长度(己.成绩) == 0：
            返回 0
        设 总和 为 0
        遍历 己.成绩 为 分：
            总和 = 总和 + 分
        返回 总和 / 长度(己.成绩)

# 创建对象——根据「蓝图」造出「实物」
设 张三 为 学生("张三", 20)
张三.添加成绩(85)
张三.添加成绩(92)
打印(f"{张三.名字}的平均分是{张三.平均分()}")
```

**为什么要用 `己`？** `己` 相当于其他语言的 `this` 或 `self`，表示「当前对象自己」。`己.名字` 的意思是「这个学生自己的名字」。

### 7.2 继承——减少重复代码

当两个类有很多共同点，可以让一个类「继承」另一个类的功能：

```段言
类 动物：
    属性 名字

    构造 接收 名字：
        己.名字 为 名字

    段落 介绍 接收：
        打印(f"我叫{己.名字}")

类 狗 继承 动物：
    段落 叫 接收：
        打印 "汪汪！"

设 小狗 为 狗("旺财")
小狗.介绍()    # 我叫旺财（继承自动物）
小狗.叫()      # 汪汪！（自己的方法）
```

**为什么说「继承」？** 就像孩子继承父母的基因——`狗` 自动拥有 `动物` 的所有属性和方法，不需要重新写一遍。

### 7.3 静态方法——不需要对象就能调用的方法

```段言
类 数学工具：
    静态 段落 平方 接收 x：
        返回 x * x

打印 数学工具.平方(5)    # 输出: 25
```

**为什么需要静态方法？** 有些工具函数不需要创建对象就能用——`数学工具.平方(5)` 直接调用，不用先 `设 工具 为 数学工具()`。

---

### ✅ 第七部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 用 `类` 定义蓝图 | ☐ |
| 用 `构造` 初始化对象 | ☐ |
| 用 `己` 访问当前对象属性 | ☐ |
| 用 `继承` 复用代码 | ☐ |
| 使用静态方法 | ☐ |

---

## 🛡️ 第八部分：异常处理与文件操作（3 分钟）

### 8.1 异常处理——程序出错了怎么办？

程序运行中总会有意外——比如用户输入了不合法的数据、要读取的文件不存在。`尝试/捕获` 就是给程序穿上「防弹衣」：

```段言
尝试：
    设 结果 为 10 / 0
    打印 结果
捕获 错误：
    打印("出错了：" + 转字符串(错误))
最终：
    打印("无论是否异常都会执行")
```

**为什么需要 `最终`？** 有些操作无论成功还是失败都必须执行——比如关闭文件、释放资源。`最终` 保证了这一点。

### 8.2 主动抛出异常

```段言
段落 检查年龄 接收 年龄：
    如果 年龄 < 0：
        抛出 "年龄不能为负数"
    打印(f"年龄是{年龄}")

尝试：
    检查年龄(-5)
捕获 e：
    打印("捕获到异常：" + 转字符串(e))
```

### 8.3 文件操作

```段言
# 写入文件
写入文件("note.txt", "你好，段言！")
打印("文件已写入")

# 读取文件
设 内容 为 读取文件("note.txt")
打印(内容)

# 检查文件是否存在
如果 文件存在("note.txt")：
    打印("文件存在")
否则：
    打印("文件不存在")
```

**为什么用「写入文件/读取文件」而不是 `open/write/close`？** 段言把常见的文件操作封装成了直观的中文函数——初学者不需要理解「文件句柄」「缓冲区」等底层概念。

---

### ✅ 第八部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 用 `尝试/捕获` 处理异常 | ☐ |
| 用 `抛出` 主动触发异常 | ☐ |
| 用 `最终` 确保清理操作 | ☐ |
| 读写文件 | ☐ |

---

## 📚 第九部分：模块系统（2 分钟）

### 9.1 导入标准库——站在巨人的肩膀上

段言内置了很多标准库，让你不用从头写一切：

```段言
导入 数学

设 半径 为 5
设 面积 为 数学.圆周率 * 半径 * 半径
打印(f"圆的面积：{面积}")

设 随机数 为 数学.随机整数(1, 100)
打印(f"随机数：{随机数}")
```

### 9.2 从模块导入特定函数

```段言
从 数学 导入 平方根, 绝对值

打印(f"根号16 = {平方根(16)}")
打印(f"|-5| = {绝对值(-5)}")
```

### 9.3 创建自己的模块

创建 `工具.duan`：

```段言
段落 双倍 接收 数：
    返回 数 * 2

段落 问候 接收 名：
    返回 "你好，" + 名 + "！"

导出 双倍, 问候
```

在另一个文件中使用：

```段言
导入 工具

打印 工具.双倍(21)
打印 工具.问候("小明")
```

---

## 🔍 第十部分：调试——找出程序的 Bug（额外 3 分钟）

### 10.1 为什么需要调试器？

写完代码第一次运行就完美通过——这种情况几乎不存在。调试器让你可以**逐行执行**代码，观察每一行执行后变量的变化，找到问题所在。

### 10.2 使用 duan debug

段言内置了调试器，直接用 `duan debug` 命令：

```bash
duan debug hello.duan
```

**调试器常用命令：**

| 命令 | 作用 | 为什么用这个？ |
|------|------|---------------|
| `b 5` | 在第 5 行设置断点 | 让程序在指定行停下来，方便观察 |
| `d 5` | 删除第 5 行的断点 | 不再需要时清除 |
| `c` | 继续执行到下一个断点 | 跳过不需要细看的代码 |
| `n` | 单步跳过（不进入函数内部） | 如果函数是可信的，直接跳过 |
| `s` | 单步进入（进入函数内部） | 想看函数内部发生了什么 |
| `p 变量名` | 打印变量的当前值 | 检查变量是否如预期 |
| `w` | 显示调用栈 | 看当前是哪个函数调了哪个函数 |
| `l` | 显示当前源代码 | 确认当前执行到哪一行 |
| `vars` | 显示所有变量 | 全局查看所有变量的状态 |
| `q` | 退出调试 | 结束调试 |

### 10.3 实战调试示例

假设有这样一个程序 `bug.duan`：

```段言
段落 阶乘 接收 n：
    如果 n <= 1：
        返回 1
    返回 n * 阶乘(n - 1)

设 结果 为 阶乘(5)
打印(f"5! = {结果}")
```

运行调试：

```bash
duan debug bug.duan
```

```
[段言调试器] 已加载 bug.duan
> b 5                    # 在第5行设断点（返回语句）
[断点已设置] 第5行

> c                      # 开始执行
[断点触发] 第5行: 返回 n * 阶乘(n - 1)

> p n                    # 查看 n 的值
n = 5

> c                      # 继续，会反复触发断点
[断点触发] 第5行: 返回 n * 阶乘(n - 1)
> p n
n = 4

> c                      # 继续
[断点触发] 第5行: 返回 n * 阶乘(n - 1)
> p n
n = 3

> c
[断点触发] 第5行: 返回 n * 阶乘(n - 1)
> p n
n = 2

> c
[断点触发] 第5行: 返回 n * 阶乘(n - 1)
> p n
n = 1

> c                      # 继续，程序结束
5! = 120
```

你也可以直接启动调试 REPL，不指定文件：

```bash
duan debug
```

然后在调试器中加载文件进行调试。

---

## 🚢 第十一部分：编译与发布——把程序变成 EXE（额外 3 分钟）

### 11.1 为什么需要编译？

你写的 `.duan` 文件需要段言环境才能运行——别人得先安装段言才能跑你的程序。编译成 `.exe` 文件后，任何 Windows 用户（即使没有安装 Python 和段言）都能直接运行。

### 11.2 编译为 Python 代码

```bash
duan compile hello.duan -o hello.py
```

**为什么先编译成 Python？** 段言解释器本身是 Python 写的，编译成 Python 代码后，可以借助 Python 生态进一步打包成 EXE。

### 11.3 编译为独立的 EXE 文件

```bash
# 需要先安装 PyInstaller（只需一次）
pip install pyinstaller

# 编译为 exe
duan compile hello.duan -o hello.exe
```

**原理：** `duan compile` 先把段言代码翻译成 Python，然后 PyInstaller 把 Python 打包成一个独立的 .exe 文件。这个文件里包含了运行所需的一切，**无需任何环境**就能运行。

### 11.4 LLVM 原生编译——更快、更小

```bash
duan build hello.duan -o hello.exe
```

**为什么用 `duan build` 而不是 `duan compile`？** `duan build` 走的是 LLVM 后端——直接把段言代码编译成机器码，生成的原生可执行文件**启动更快、体积更小**，不需要 Python 运行时。

**`duan build` 和 `duan compile` 的区别：**

| 命令 | 后端 | 输出 | 是否需要 Python | 启动速度 |
|------|------|------|----------------|---------|
| `duan compile` | SRC/ANTLR | .py 或 .exe | 打包后不需要 | 中等 |
| `duan build` | LLVM | 原生 .exe | 不需要 | 最快 |

> **注意：** 如果代码中使用了类（`class`）等高级特性，需要使用 LLVM 后端才能正确编译。可以用 `duan ai check` 检测代码与后端的兼容性。

---

### ✅ 第十部分 + 第十一部分进度检查

| 学会了什么 | 状态 |
|-----------|------|
| 用 `duan debug` 设置断点调试 | ☐ |
| 用 `p` 命令查看变量值 | ☐ |
| 用 `c`、`n`、`s` 控制执行流程 | ☐ |
| 用 `duan compile` 编译为 Python | ☐ |
| 用 `duan build` 编译为原生 EXE | ☐ |
| 理解不同后端之间的区别 | ☐ |

---

## 🧠 第十二部分：从错误中学习——常见新手错误

错误是学习编程的最佳老师。下面列出段言新手最常见的错误，遇到时对照检查。

### 错误 1：忘了写 `为`

```段言
# ❌ 错误
设 名字 "张三"

# ✅ 正确
设 名字 为 "张三"
```

**为什么？** `设 X 为 Y` 是固定语法结构，`为` 是必须的关键字，漏掉它会报语法错误。

### 错误 2：混淆 `=` 和 `==`

```段言
# ❌ 错误：这是赋值，不是比较
如果 分数 = 100：
    打印 "满分"

# ✅ 正确：== 才是比较
如果 分数 == 100：
    打印 "满分"
```

**为什么？** `=` 是「把右边的值放进左边」，`==` 是「判断左右两边是否相等」。这个错误几乎每个编程初学者都会犯——**连有经验的程序员偶尔也会写错**。

### 错误 3：忘记冒号 `：`

```段言
# ❌ 错误
如果 年龄 >= 18
    打印 "成年"

# ✅ 正确
如果 年龄 >= 18：
    打印 "成年"
```

**为什么？** 冒号告诉段言「条件写完了，后面是要执行的代码块」。段言中 `如果`、`否则`、`遍历`、`当`、`段落`、`类` 后面都需要冒号。

### 错误 4：缩进不一致

```段言
# ❌ 错误：缩进混乱
如果 真：
    打印 "第一行"
  打印 "第二行"  # 缩进不对

# ✅ 正确：统一缩进
如果 真：
    打印 "第一行"
    打印 "第二行"
```

**为什么？** 段言用缩进（空格或 Tab）来标示代码块的归属。缩进不统一，段言无法判断某行代码属于哪个块。**建议统一使用 4 个空格**。

### 错误 5：变量名冲突

```段言
# ❌ 错误：使用内置函数名作变量名
设 打印 为 123
打印(打印)  # 报错：打印不再是函数了

# ✅ 正确：避免使用关键字
设 打印值 为 123
打印(打印值)
```

**为什么？** `打印`、`输入`、`长度`、`转字符串` 等是段言的内置函数，把它们当作变量名会覆盖内置函数。

### 错误 6：索引越界

```段言
# ❌ 错误
设 列表 为 [1, 2, 3]
打印 列表[5]  # 列表只有3个元素，索引5不存在

# ✅ 正确：先检查长度
设 列表 为 [1, 2, 3]
如果 长度(列表) > 5：
    打印 列表[5]
否则：
    打印(f"列表只有{长度(列表)}个元素，索引5不存在")
```

**为什么？** 列表索引从 0 开始，长度为 3 的列表，有效索引是 0、1、2。

### 错误 7：字符串引号不匹配

```段言
# ❌ 错误
设 消息 为 "你好'

# ✅ 正确：引号成对
设 消息 为 "你好"
设 消息2 为 '你好'
```

**为什么？** 字符串必须用成对的引号包裹。`"` 开始就要用 `"` 结束，`'` 开始就要用 `'` 结束。

### 错误 8：修改列表时用 `设`

```段言
# ❌ 错误：创建变量用「设」，修改列表元素用「=」
设 列表 为 [1, 2, 3]
设 列表[0] 为 10  # 在 SRC 后端不支持

# ✅ 正确：直接赋值
设 列表 为 [1, 2, 3]
列表[0] = 10
```

---

## 🎯 第十三部分：综合实战——猜数字游戏

把学到的知识串起来，写一个完整的猜数字游戏：

```段言
导入 数学

# 生成 1 到 100 的随机数
设 目标 为 数学.随机整数(1, 100)
设 尝试次数 为 0
设 猜中 为 假

打印 "=== 猜数字游戏 ==="
打印 "我已经想好了一个 1 到 100 之间的数字"
打印 "你能猜出来吗？"

当 非 猜中：
    设 猜测 为 转整数(输入("请猜一个数字："))
    尝试次数 = 尝试次数 + 1

    如果 猜测 == 目标：
        打印(f"🎉 恭喜！你猜中了！用了{尝试次数}次")
        猜中 = 真
    否则如果 猜测 < 目标：
        打印("太小了，再试试")
    否则：
        打印("太大了，再试试")

打印 "游戏结束！"
```

**运行：**

```bash
duan run guess.duan
```

**这个程序用到了哪些知识？**

| 知识点 | 对应代码 |
|-------|---------|
| 模块导入 | `导入 数学` |
| 变量 | `设 目标 为 ...` |
| 循环 | `当 非 猜中：` |
| 条件判断 | `如果/否则如果/否则` |
| 布尔值 | `真/假` |
| 用户输入 | `输入(...)` |
| 类型转换 | `转整数(...)` |
| 字符串插值 | `f"用了{尝试次数}次"` |

---

## 🧭 第十四部分：下一步去哪里？

### 14.1 更多学习资源

| 资源 | 说明 |
|------|------|
| `docs/syntax.md` | 完整语法手册（含所有 v6.0 特性） |
| `docs/tutorials/入门教程.md` | 入门教程（更多练习） |
| `docs/tutorials/进阶教程.md` | 进阶教程（FFI、Web、并发） |
| `docs/stdlib.md` | 标准库文档 |
| `examples/` 目录 | 可运行示例文件 |

### 14.2 更多 CLI 命令

```bash
duan tutorial              # 交互式教程（边学边练）
duan ast hello.duan        # 查看语法树（理解代码是如何被解析的）
duan tokens hello.duan     # 查看 Token 流（看代码如何被拆分）
duan check hello.duan      # 语法检查（快速定位语法错误）
duan ai card               # 查看语法速查卡
duan ai generate "需求"    # AI 辅助生成段言代码
```

### 14.3 更多语法特性（进阶）

| 特性 | 说明 | 示例 |
|------|------|------|
| 模式匹配 | 类似 switch-case | `匹配 值：情况 1：` |
| Lambda | 快速定义匿名函数 | `参数 x => x * x` |
| 异步编程 | 并发执行 | `异步 段落 / 等待` |
| 上下文管理器 | 资源管理 | `使用 打开文件("f") 为 文件：` |
| 管道操作符 | 数据流式处理 | `数据 \|> 函数1 \|> 函数2` |
| 列表推导 | 快速生成列表 | `[x*x 遍历 x 之 范围(1,6)]` |
| 泛型 | 类型参数化 | `段落 恒等[T] 接收 x：` |
| 接口与协议 | 抽象行为定义 | `协议 可打印：` |

### 14.4 推荐学习路径

```
入门教程（本文）→ 进阶教程 → 阅读 examples/ 示例 → 自己写小项目
```

**下一步小项目建议：**
- 🧮 命令行计算器
- 📝 待办事项管理
- 🌡️ 温度转换工具
- 🔢 斐波那契数列生成器
- 📊 学生成绩管理系统

---

> 编程是实践的艺术。打开终端，创建你的第一个 `.duan` 文件，用 `duan run` 跑起来——这是最快的学习方式。遇到错误不要怕，翻到「从错误中学习」章节对照检查，或者用 `duan debug` 一步一步看代码怎么跑的。

> **记住：** 每个程序员都是从写「Hello World」开始的。你已经在路上了，继续走下去！