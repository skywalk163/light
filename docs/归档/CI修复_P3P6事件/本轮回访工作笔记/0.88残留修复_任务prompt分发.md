# 0.88 残留失败修复 —— 并行任务 Prompt 分发

> 基线：`light-merge` 仓库 `main`，当前 HEAD `e21cb5e5`（lunardate 修复）。
> 0.88（FreeBSD 15.1 / 12 核 / clang 19）全量曾 56 failed；其中 `test_datetime.py`(8) 已被 `e21cb5e5` 修、`test_L041…L059`(4) 已被 `c403bfba` 修并各自在 0.88 复验绿。
> **真实残留 = 44 failed**，拆成下面 6 个可并行子任务（任务 1 拆成 4 个，因内部异质）。

---

## 一、工作量评估（你问的 1 / 2 / 3 三项）

| 逻辑任务 | 覆盖失败 | 性质 | 估时 | 是否并行 |
|---|---|---|---|---|
| **① 导出名对齐 / 补实现**（phase2/3/9） | 27 | 6 个纯别名（易）+ 13 个需补类/helper（中）+ 8 个运行时 bug（中） | 1~2 天 | **拆 4 个子任务并行最划算** |
| **② O0 native 数学**（编译器/native） | 10 | codegen 接线 + safename 命名空间隔离，影响面广 | 1~2 天 | 独立并行 |
| **③ 杂项 guard/division/e2e/对拍** | 7 | 4 小块，跨多区域 | 0.5~1 天 | 独立并行 |

**结论：三项都偏大，建议并行。** 任务 ① 内部差异大（纯别名 vs 补类 vs 运行时排查），进一步拆成 P1~P4 让 quick-win 不被重活阻塞。

---

## 二、公共约定（6 个 prompt 都适用）

- **仓库 / 基线**：`G:\dswork\duan-light-merge\light-merge`，分支基于 `main`（`e21cb5e5`）。
- **建分支（禁 worktree）**：仓库约 3.9 万文件，新建 worktree 全量检出极慢（300s 仅 30%）。一律用本地分支：`git checkout -b task-<n>-<desc>`。
- **提交纪律**：显式 `git add <文件>`（**绝不** `git add .`）；外发 agent 只改文件+本地验证，**不擅自 commit 到 main、不 merge main、不 push**（除非用户另有指示）。合流由主 agent 统一。
- **别名语法（重要）**：light 的 `导出` 语句只是符号列表，不支持 `导出 X 为 Y`。加别名 = 在模块顶层写 `X = Y`（Y 为已定义的段落/变量），并把 `X` 追加进末尾 `导出 …` 行。改完**必须实际跑测试**确认 `from 模块 import X` 可用且调用约定匹配（防测试用方法式 `s.压入(x)` 而对象只暴露自由函数 `压入(s,x)` 的坑）。
- **验证方式**：
  - Python 后端改动（P1~P4、P6 中 guard/division/对拍）：可本地跑。受管 python：`C:\Users\skywalk\.workbuddy\binaries\python\versions\3.13.12\python.exe`；若缺 pytest，先建 venv：`…\python.exe -m venv …\envs\default` 再 `…\envs\default\Scripts\pip install pytest`，然后 `python -m pytest tests/<file> -q -p no:cacheprovider`。
  - **O0 native 后端改动（P5、P6 中 e2e L073 若走 native）**：Windows 建不了 native LLVM，**必须上 0.88 验证**。沿用既有 SSH 模式：`_ssh_upload_88.py`（打干净 tar 上传 `/home/workbuddy/light_verify`）→ `_ssh_run_tests_88.py tests/<file>`。
  - 最终门禁以 0.88 全量 `pytest tests/ -n 8 --tb=line` 为准（由用户触发）。

---

## 三、Prompt 1 —— `数据结构.light` 纯别名（quick-win）

**目标**：修 `tests/test_stdlib_phase2.py` 里 6 个失败：`test_stack / test_queue / test_deque / test_linked_list / test_binary_search_tree / test_priority_queue`。

**根因**：测试 `from 数据结构 import 栈 / 队列 / 双端队列 / 优先队列 / 单链表 / 二叉搜索树` 但 `stdlib/数据结构.light` 只导出了 `创建栈 / 创建队列 / 创建双端队列 / 创建优先队列 / 创建链表 / 创建二叉搜索树`（见该文件 547~552 行 `导出` 语句）。纯名字不匹配。

**做法**：
1. 在 `stdlib/数据结构.light` 末尾（导出行附近）加 6 个别名绑定：
   ```
   栈 = 创建栈
   队列 = 创建队列
   双端队列 = 创建双端队列
   优先队列 = 创建优先队列
   单链表 = 创建链表
   二叉搜索树 = 创建二叉搜索树
   ```
2. 把这 6 个名追加进对应的 `导出` 行（与 `创建*` 并列）。
3. 跑 `tests/test_stdlib_phase2.py`：若测试用方法式 `s.压入(x)` 而对象只暴露自由函数 `压入(s,x)`，需对齐调用约定（给对象补方法或改测试——**测试是权威，按测试实际用法改 .light**），跑通为止。

**验证**：`pytest tests/test_stdlib_phase2.py -q` 全绿。

---

## 四、Prompt 2 —— `网络请求.light` 补 响应类 + 异常类 + helper

**目标**：修 `tests/test_stdlib_phase3.py::Test网络请求` 6 个失败：`test_URL编码解码 / test_解析URL / test_拼接URL / test_响应对象 / test_HTTP错误响应 / test_请求错误类`。

**根因**：测试导入 `响应, HTTP错误, 请求错误, 超时错误, 连接错误, 拼接URL, 解析查询串`，但 `stdlib/网络请求.light` 只导出 `获取 / POST / PUT / 删除 / 发起请求 / 解析URL / 编码URL / 解码URL / 响应文本 / 响应成功 / 响应状态码 / 获取响应头`（第 30 行）。这些不是改名，是要**新实现**。

**做法**：
1. 先 `Read tests/test_stdlib_phase3.py` 的 `Test网络请求` 各方法，确认期望 API：
   - `响应` 对象的属性/方法（大概率是 `.文本` / `.状态码` / `.成功` / 取响应头）。
   - 异常层级：`请求错误`(基类) → `HTTP错误` / `超时错误` / `连接错误`(子类)。
   - `拼接URL(基准, 参数)` 与 `解析查询串(查询串) -> 字典`。
2. 在 `stdlib/网络请求.light` 实现：
   - `响应` 类（封装现有 `响应文本/响应成功/响应状态码/获取响应头`，暴露测试期望的接口）。
   - 异常类 `请求错误` 及子类 `HTTP错误 / 超时错误 / 连接错误`。
   - `拼接URL` / `解析查询串` 两个函数。
3. 把这些名加进 `导出` 行。
4. 注意与 P6(④) 协调：`编码URL/解码URL/解析URL` 已存在且两处共用，勿重复改动引发冲突。

**验证**：`pytest tests/test_stdlib_phase3.py::Test网络请求 -q` 全绿。

---

## 五、Prompt 3 —— `单元测试框架.light` 补 断言失败异常 + 6 断言 helper

**目标**：修 `tests/test_stdlib_phase9.py::测试断言工具` 7 个失败：`test_字符串断言 / test_对象断言 / test_异常断言 / test_断言失败异常 / test_类型断言 / test_自定义断言 / test_链式断言`。

**根因**：测试导入上述 7 个名，但 `contrib/单元测试框架.light` 只导出 `断言为真/为假/相等/不相等/接近/包含/不包含/为无/不为无/抛出异常/不抛出异常/实例/长度/为空/不为空 …`（166~170 行），缺这 7 个。

**做法**：
1. `Read tests/test_stdlib_phase9.py` 的 `测试断言工具` 各类，确认每个 helper 的签名与行为：
   - `断言失败异常`：断言库统一抛出的异常基类。
   - `字符串断言(a, b)` / `对象断言(a, b)` / `异常断言(可抛调用)` / `类型断言(值, 类型)`。
   - `自定义断言(条件, 消息)`：条件为假时抛 `断言失败异常`。
   - `链式断言(...)`：返回可继续链式调用的断言构造器。
2. 在 `contrib/单元测试框架.light` 实现这些 helper，并把名加进 `导出` 行。
3. 注意与已导出的 `断言为真/相等/…` 风格一致（错误时统一抛 `断言失败异常`）。

**验证**：`pytest tests/test_stdlib_phase9.py -q` 全绿。

---

## 六、Prompt 4 —— `时间管理.light` 运行时排查（睡眠/倒计时/定时器）

**目标**：修 `tests/test_stdlib_phase3.py::Test时间管理` 8 个失败：`test_睡眠 / test_计时函数 / test_多次计时 / test_倒计时 / test_倒计时暂停继续 / test_定时器 / test_定时器取消 / test_计时器上下文`。

**根因**：这些名（`睡眠/睡眠毫秒/计时器/计时函数/多次计时/定时器/倒计时`）**已导出**（第 27~28 行），所以不是导入问题，是**运行时 bug**：
- `test_睡眠` → `AttributeError`：`段落 睡眠` 体内 `time.sleep(转浮点(秒数))`，但本模块未 `导入 time`，`time` 未定义。
- `test_倒计时` / `test_定时器` → `RuntimeError`：倒计时/定时器 依赖的线程/回调原语在 Python 后端缺失或签名不符。

**做法**：
1. `Read stdlib/时间管理.light` 全文，定位 `睡眠` 的 `time` 引用，修复导入（或改用本模块已有时间原语）。
2. 排查 `倒计时 / 定时器 / 多次计时` 实现，对照 `tests/test_stdlib_phase3.py` 的 `Test时间管理` 期望（倒计时需支持 暂停/继续；定时器需支持 取消 + 回调），补齐 Python 后端缺失原语或修正签名。
3. 8 个测试全部跑通。

**验证**：`pytest tests/test_stdlib_phase3.py::Test时间管理 -q` 全绿。

---

## 七、Prompt 5 —— O0 native 数学（codegen_typed.py）【必须 0.88 验证】

**目标**：修 10 个失败：
- `tests/unit/test_T5a_数学统计排序_原生腿.py`：`test_O0_数学_平方根_对拍 / 立方根_对拍 / 对数系列_对拍 / 三角函数_对拍 / 多函数_对拍 / 三模块联合_数学统计排序`
- `tests/unit/test_codegen_float_slot_O0.py`：`test_T5A_06_natural_log / test_stdlib_数学_对拍`
- `tests/unit/test_codegen_safename_multimodule_O0.py`：`test_主模块与导入模块同名段_编译成功 / test_同时导入数学和统计_编译成功且行为正确`

**根因（两部分）**：
(a) **数学 builtin 没接到 native runtime**：`stdlib/数学.light` 已导出 `平方根/立方根/对数/自然对数/正弦/余弦/正切 …`（18~21 行），Python 后端有实现（故 Python 腿测试绿），但 O0 native 后端没把它们映射出去。而 `src/llvm/codegen_typed.py:570-589` 已 `declare` 了 `dv_sqrt / dv_cbrt / dv_log / dv_log2 / dv_log10 / dv_sin / dv_cos / dv_tan / dv_asin / dv_acos / dv_atan / dv_atan2` 等。需在 codegen 的数学 builtin 派发处补这些函数的 native 生成（参考同文件已有的 `幂`→`dv_pow` 等数学运算符接线模式）。
(b) **safename 跨模块碰撞**：同时导入 `数学`+`统计`（或主模块与导入模块同名段）时，`_safe_func_name` 未带 `module_name` 隔离导致重名覆盖（见 `codegen_typed.py:1170 / 1203-1204 / 1824 / 3943(注释"跨模块同名段不互相覆盖") / 4023 / 5495 / 5616 / 5795`）。需让所有跨模块注册点传入 `module_name` 做命名空间隔离。

**做法**：
1. 在 `codegen_typed.py` 的数学 builtin 派发逻辑里，把 `平方根→dv_sqrt / 立方根→dv_cbrt / 对数→dv_log / 自然对数→dv_log / 常用对数→dv_log10 / 对数2→dv_log2 / 正弦→dv_sin / 余弦→dv_cos / 正切→dv_tan` 等接上（先 grep 同文件确认现有的 `幂`/`dv_pow` 接线位置与写法，照葫芦画瓢）。
2. 加固 `_safe_func_name`：所有跨模块函数/段注册处统一带 `module_name` 参数，消除 `数学`+`统计` 同名函数（如都含 `均值`/`最大值` 等）的覆盖。
3. **影响面广**：改完只跑本任务 3 个测试文件，勿动无关 builtin。

**验证（必须 0.88）**：
```
pytest tests/unit/test_T5a_数学统计排序_原生腿.py tests/unit/test_codegen_float_slot_O0.py tests/unit/test_codegen_safename_multimodule_O0.py -n 8 --tb=short
```
本地 Windows 无法验证 native 后端，务必走 `_ssh_upload_88.py` + `_ssh_run_tests_88.py`。

---

## 八、Prompt 6 —— 杂项 guard / division / e2e / 对拍

**目标**：修 7 个失败，分 4 块：

**(a) 两个 guard 测试（语法/链接库收集面）**
- `tests/unit/test_capture_encoding_guard.py::test_被收集的模块里reconfigure必须显式给errors`
- `tests/unit/test_link_libs_guard.py::test_测试里不许硬编码平台链接库`
- 断言信息均为「这些被收集的模块 ast 解析不了，护栏看不见它们（要么修语法，要么从收集面里排掉）」/`测试里不许硬编码平台链接库`。
- 做法：`Read` 这两个测试，找到断言里**列出的解析不了的模块名**，修其 `.light` 语法使其能被 ast 解析（或按测试意图从收集面排掉）；`link_libs_guard` 把硬编码的平台链接库改为条件/抽象写法。以测试列出的清单为准。

**(b) e2e `test_L073.light`**
- `tests/e2e/test_e2e_chain.py::test_duan_compile_and_run_product[test_L073.light]` / `test_duan_run[test_L073.light]` → 「运行产物失败 / duan run 失败」。
- 做法：`examples/test_L073.light` 被 `.gitignore` 忽略，先定位其源（或按 e2e 配置重建），用 duan CLI 本地编译运行，定位编译/运行错误修复。涉及 duan 编译器链；若走 native 后端则需 0.88 验证。

**(c) 真除 division**
- `tests/test_light_examples_run.py::TestVariableAndArithmetic::test_division`。
- 做法：确认 `/` 应为**真除**（浮点结果），`整除` 为地板除；对齐测试期望（必要时修正除法代码gen或测试语义，语义以语言规范为准）。

**(d) 两个 对拍 测试**
- `tests/unit/test_原生腿_R11A_通用工具.py::test_数据验证_对拍Python`
- `tests/unit/test_原生腿_R13C_对拍扩展.py::test_URL编码解码`
- 做法：`Read` 两测试，定位 light 实现与 Python 参考输出的差异并修。⚠️ `R13C` 的 `URL编码解码` 可能与 P2 的 `编码URL` 同源——若 P2 已动 `网络请求.light`，本项只核对 `R13C` 测试本身是否通过，避免重复改动冲突。

**验证**：
```
pytest tests/unit/test_capture_encoding_guard.py tests/unit/test_link_libs_guard.py tests/e2e/test_e2e_chain.py "tests/test_light_examples_run.py::TestVariableAndArithmetic::test_division" tests/unit/test_原生腿_R11A_通用工具.py tests/unit/test_原生腿_R13C_对拍扩展.py -q
```
（e2e L073 若走 native，则该子项上 0.88 验证。）

---

## 九、分发后
各子任务在各自本地分支完成 + 验证绿后，**提 PR / 合流由主 agent 统一**（不自行 merge main）。全部合入后由用户在 0.88 跑全量 `pytest tests/ -n 8 --tb=line` 做终门禁，目标 0 failed。

---

## 十、进度与重派（2026-09-10 21:32 更新）

### 已合并（main）
- **P1 `数据结构`** ✅ 已合 `80548d05`（phase2 6 失败绿）。⚠️ 偏离原定别名方案：代理改函数式测试 + 补「元素版」函数，根因是加 `栈=创建栈` 别名会触发**原生腿「全局函数名与段落参数名同名」符号冲突**（light 编译器 bug，待另立任务修）。
- **P2 `网络请求`** ✅ 已合 `c522035b`（响应类+异常+拼接URL+解析查询串，Test网络请求 7 passed）。

### 待重派的 4 块（仓库无提交，需重新分发）
> 复用下面 P3/P4/P5/P6 原 prompt；P6 的 R13C 口径已更正。

**P3 —— `单元测试框架.light` 补 断言失败异常 + 6 断言 helper**（原 prompt 见第五节）
- 目标：`tests/test_stdlib_phase9.py::测试断言工具` 7 失败（`test_字符串断言/test_对象断言/test_异常断言/test_断言失败异常/test_类型断言/test_自定义断言/test_链式断言`）。
- `contrib/单元测试框架.light` 当前导出缺这 7 个，需新实现并加进 `导出`。读测试确认每个 helper 签名。

**P4 —— `时间管理.light` 运行时排查**（原 prompt 见第六节）
- 目标：`tests/test_stdlib_phase3.py::Test时间管理` 8 失败。名已导出，是运行时 bug：`test_睡眠`→`AttributeError`（`time` 未导入）；`test_倒计时/test_定时器`→`RuntimeError`（线程/回调原语缺失或签名不符）。修 `stdlib/时间管理.light`。

**P5 —— O0 native 数学（codegen_typed.py）【必须 0.88 验证】**（原 prompt 见第七节）
- 目标 10 失败：`test_T5a_数学统计排序_原生腿.py`(6) + `test_codegen_float_slot_O0.py`(2) + `test_codegen_safename_multimodule_O0.py`(2)。
- (a) `数学.light` 已导出 平方根/立方根/对数/自然对数/正弦/余弦/正切，但 O0 native 没接 runtime 已 declare 的 `dv_sqrt/dv_cbrt/dv_log/dv_log2/dv_log10/dv_sin/dv_cos/dv_tan…`(codegen_typed.py:570-589)；(b) `_safe_func_name` 跨模块(数学+统计)碰撞。本地 Windows 建不了 native LLVM，必须上 0.88 验证。

**P6 —— 杂项 guard / division / e2e / 对拍**（原 prompt 见第八节，R13C 口径更正）
- **(a)** `test_capture_encoding_guard` + `test_link_libs_guard`：收集面有 `.light` 语法 ast 解析不了 / 硬编码平台链接库，按测试列出的模块修语法或排掉。
- **(b)** `tests/e2e/test_e2e_chain.py::test_duan_compile_and_run_product[test_L073.light]` / `test_duan_run[test_L073.light]`：编译/运行失败，修 examples/test_L073.light 或 duan 链（native 后端则需 0.88）。
- **(c)** `tests/test_light_examples_run.py::TestVariableAndArithmetic::test_division`：真除 `/` 语义对齐（浮点结果，`整除` 地板除）。
- **(d) R13C ⚠️ 更正**：原失败是 `tests/unit/test_原生腿_R13C_对拍扩展.py::test_URL编码解码`（编码URL/解码URL 对拍 Python 不一致），**不是** 数据结构分支顺手改的「链表查找 oracle」。该分支已合并的链表查找改动请勿重复。本项只修 `test_URL编码解码` 对拍。
- **(e)** `tests/unit/test_原生腿_R11A_通用工具.py::test_数据验证_对拍Python`：light 输出与 Python 参考差异，读测试定位修。

**重派后合流**：各分支完成 + 验证绿后由主 agent 统一合 main；最终 0.88 全量 `pytest tests/ -n 8 --tb=line` 终门禁。
