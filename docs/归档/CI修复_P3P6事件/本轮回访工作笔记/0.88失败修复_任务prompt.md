# 0.88（FreeBSD 15.1）全量 145 failed — 可并行修复任务 Prompt（共 12 个）

> **来源**：0.88 快机 `pytest tests/ -n 8` 全量（145 failed / 18 errors）→ 22 个失败文件 → 对其中 13 文件 + 9 新文件各做 `--tb=short` 隔离重跑，拿到精确报错后归类。
> **重要前提（所有任务通用）**：
> 1. **0.88 验证机是过期镜像**：它上面的 `light_verify/` 是早期打的 tar，部分源码已落后于本地工作树（例：`stdlib/统计.light` 本地已用 `_light_builtin.排序`，0.88 仍调 `sort`）。**开工第一步先本地复现**，若本地已绿，只需重传 0.88（见下）即可，不要重复改。
> 2. **仓库**：`G:/dswork/duan-light-merge/light-merge`（本地工作树 = 真相源）。改完本地验证后，用 `_ssh_upload_88.py` 重传再在 0.88 确认。
> 3. **分支纪律**：每人开 `task-Txx-<简述>` 本地分支，只动本任务文件，勿碰其它测试/stdlib；PR 合入前过 `check_regression.py`（只拦基线外**新增**失败，安全）。
> 4. **本地验证**：`cd light-merge && python -m pytest <文件> -p no:cacheprovider -q`（Windows 上原生 LLVM/process_tree/pure_light_hook 有平台噪声，聚焦本任务的具体报错即可）。
> 5. **0.88 确认**：`python3 -m pytest <文件> -p no:cacheprovider -q`（在 `/home/workbuddy/light_verify`）。

---

## T1 — 环境：上传 tar 补 `任务书/`（关 10 个，纯环境零代码）

**文件**：`tests/unit/test_ci_gates_round9.py`（10：Test真清单在册 6 + Test缺失内置清单在册 4）
**真实报错**：
- `FileNotFoundError: .../任务书/原生腿产品清单.json` / `分布式判据清单.json` / `自举地板清单.json`
- `AssertionError: 清单不见了：任务书/缺失内置清单.json`
**根因**：本地 `tar czf` 打包命令排除了 `任务书/` 目录（排除列表里写的是 `任务书`），0.88 上缺这些 JSON → 清单类门禁全红。gitea CI 是完整 checkout，本就通过，属快机专属环境假失败。
**修复**：改本地打包脚本/命令的排除列表，**不要再排除 `任务书/`**（与 `docs/`、`vscode-extension/` 一样必须带上）。重传 0.88 后单独跑该文件应转绿。
**验收**：`tests/unit/test_ci_gates_round9.py` 在 0.88 全绿。
**注意**：这是 0.88 专属，不需要改任何仓库源码；但若你顺手发现清单 JSON 内容本身有问题，另开任务。

---

## T2 — 测试隔离/全局污染修复（关 ~33 个，最难，单独一人）

**文件**：`tests/test_vscode_extension_completions.py`（18）、`tests/unit/test_native_leg_capability.py`（8）、`tests/unit/test_spec_docs_sync.py`（7，隔离重跑已通过→纯污染）、`tests/unit/test_doc_examples_gate.py`（约 2 个污染型）
**现象**：这三类文件**单独跑（含 `-n 8`）都通过**，但在**全量**里失败：
- vscode：`AssertionError: 扩展文件不存在: /home/workbuddy/light_verify/vscode-extension/extension.js`（路径绝对、文件确实存在）
- native_leg：`能力清单 JSON 不见了: tests/unit/../../docs/原生腿能力清单.json`（**相对路径**，依赖 cwd）
- spec_docs_sync：隔离通过，全量挂 → 经典 cwd/状态污染
**根因**：全量里排在前面的某个测试改了 cwd 或泄漏了 monkeypatch/fixture 状态，使后续用例路径解析失败。
**修复指引**：
1. `test_native_leg_capability.py` 的清单加载：把 `tests/unit/../../docs/原生腿能力清单.json` 改成基于 `__file__` 的**绝对路径**，消除 cwd 依赖（治标先让 native_leg 转绿）。
2. 定位污染源：在 0.88 上 `pytest tests/ -n 0 -p no:cacheprovider -q` 单进程确定性复现，然后二分/逐个目录加入这三个文件重跑，找出**哪个测试**让 cwd 变脏或 `monkeypatch` 未还原（重点嫌疑：多个文件 import 期 `os.chdir` 到仓库根、`test_module_system*.py`/`test_missing_stdlib.py` 里 `os.chdir` 到临时目录的用例——确认其 `restore` 在所有异常路径都执行）。修法：用 `monkeypatch.chdir` 或 fixture `yield`+`finally` 严格还原，禁止裸 `os.chdir`。
3. vscode 用绝对路径仍挂，说明还有更全局泄漏（可能 `os.path` 被 mock 或未还原），重点查有无测试 mock 了 `os`/`pathlib`。
**验收**：0.88 全量里这三个文件不再出现 FAIL/ERROR。
**注意**：这是"全量才暴露"的隔离 bug，别当业务逻辑 bug 硬改。

---

## T3 — `数学.light` 特性补齐（关 5 个）

**文件**：`tests/test_stdlib_complete.py`
**真实报错**：`ImportError: cannot import name '排列'/'复数实部'/'黄金比例'/'双曲正弦'/'反正弦' from '数学'`
**现实**：`stdlib/数学.light` 当前导出 `绝对值 最大值 最小值 幂 平方根 立方根 正弦 余弦 正切 弧度转角度 角度转弧度 向上取整 向下取整 四舍五入 圆周率 自然常数 阶乘 对数 自然对数 常用对数 对数2 指数` + 英文别名 `pi pow sqrt sin cos tan floor ceil round random`。**无上述 5 个**。
**修复**：在 `数学.light` 补实现并导出：
- `黄金比例` → 常量 φ = (1+√5)/2
- `双曲正弦` → `sinh(x)`；可加 `双曲余弦` 等
- `反正弦` → `arcsin(x)`（需确认解释器有无 math 模块绑定，参考 `正弦/余弦` 实现方式）
- `复数实部` → 复数类型实部取值（确认 light 是否支持复数；若不支持复数类型，则与任务负责人确认是否降级为 xfail）
- `排列` → 排列组合函数（如 `排列(列表)` 返回全排列）
**验收**：`tests/test_stdlib_complete.py` 的 import 处全绿。

---

## T4 — `数据结构.light` 命名/缺失实现（关 6 个）

**文件**：`tests/test_stdlib_phase2.py`
**真实报错**：`ImportError: cannot import name`：
- `栈` → Did you mean `创建栈`；`队列` → 创建队列；`双端队列` → Did you mean `双端队列空`(即 `创建双端队列`)；`优先队列` → Did you mean `优先队首`(即 `创建优先队列`)；`二叉搜索树` → Did you mean `创建二叉搜索树`；`单链表` → **无近似，真缺失**
**现实**：`数据结构.light` 导出的是动词形构造器 `创建栈/创建队列/创建双端队列/创建优先队列/创建二叉搜索树`（均存在），但测试 import 用短名词 `栈/队列/双端队列/优先队列/二叉搜索树`，且 `单链表` 未实现。
**修复决策**：
- 短名 `栈/队列/双端队列/优先队列/二叉搜索树`：二选一——(a) 在 `数据结构.light` 补短名导出别名（如 `段落 栈 接收 ...: 返回 创建栈(...)`）；(b) 改测试 import 为 `创建X`。**优先 (b) 最小改动**，除非复刻规范明确要短名。
- `单链表`：若属应实现的数据结构，按模块 `创建X` 风格实现并导出；否则改测试移除。
**验收**：`tests/test_stdlib_phase2.py` 全绿。

---

## T5 — 小模块命名对齐：网络请求/进度条/数据验证/编码解码/断言工具（关 ~13 个）

**文件**：`tests/test_stdlib_phase3.py`（部分）、`tests/test_stdlib_phase4.py`（字符集转字符串 1）、`tests/test_stdlib_phase9.py`（断言失败异常 1）
**真实报错与对应现实导出**（已核查 stdlib 真实导出名）：
- 网络请求：`响应`(测试想要响应对象；stdlib 返回 dict，无 `响应` 创建器)、`拼接URL`→**已有 `合并URL`**、`解析查询串`→**已有 `URL查询串解码`/`URL查询串编码`**、`请求错误`→**已有 `错误响应`**
- 进度条：`进度条`→**已有 `创建进度条`**；`多阶段进度条`→**已有 `创建多阶段进度条`**
- 数据验证：`验证结果`→**已有 `创建验证结果`**；`数据模式`→**已有 `创建数据模式`**
- 编码解码：`字符集转换为字符串`→**已有 `字符集转字符串`**（line 23 导出）
- 断言工具：`断言失败异常`→**已有 `断言失败`**（是抛错函数，非异常类；测试应 `尝试: ... 捕获 断言失败:` 或直接调 `断言失败(消息)`）
**修复决策**：命名不同的（拼接URL/验证结果/数据模式/字符集转换为字符串/断言失败异常）→ 改测试 import/调用为真实导出名（最小改动）；真缺特性（`响应`/`解析查询串`）→ 按规范在 stdlib 补实现并导出，或确认测试期望是否合理。
**验收**：上述三文件 ImportError 全消除。

---

## T6 — `日期` 模块 LunarDate camelCase（关 8 个）

**文件**：`tests/test_datetime.py`
**真实报错**：`AttributeError: type object 'LunarDate' has no attribute 'from_solar_date'. Did you mean: 'fromSolarDate'`；`'LunarDate' object has no attribute 'to_solar_date'. Did you mean: 'toSolarDate'`
**根因**：底层 `lunardate`（0.88 装 `py312-lunardate`）API 是 camelCase `toSolarDate`/`fromSolarDate`，某层 wrapper/测试调 snake_case。
**修复**：定位调用点（在 `日期` stdlib wrapper 还是测试自身）——统一为 camelCase，或在 wrapper 暴露 snake_case 别名 `to_solar_date`/`from_solar_date`。**注意 0.88 与 gitea(1.5) 的 lunardate 版本是否一致**，避免一处修好另一处又挂。
**验收**：`tests/test_datetime.py` 全绿。

---

## T7 — `phase4` 对象式 API 改为函数式（关 ~17 个）

**文件**：`tests/test_stdlib_phase4.py`（31 个中的对象式部分）
**真实报错**：`'dict' object has no attribute '是否有效'`、`'bool' object has no attribute '是否有效'`、`'dict' object has no attribute '设置'/'获取'/'验证'/'_总数'`、`'bool' object has no attribute 'get'`
**根因**：测试把 stdlib 返回的对象当**带方法的对象**（`结果.是否有效()`、`状态.设置(...)`），但 stdlib 实际返回**普通 dict / bool**，方法以函数式提供（`是否有效(结果)`、`设置当前(状态, 值)`）。
**修复决策**（按复刻规范二选一）：
- (A) 改测试：把 `X.方法(...)` 改成函数式 `方法(X, ...)`（风险低，推荐先试）。涉及 `数据验证`(验证结果→用 `是否有效(结果)` 等)、`进度条`(状态→用 `设置当前(状态, 值)`)、`缓存管理器` 等。
- (B) 改 stdlib：让构造返回带方法的类（改动大，需确认是否目标设计）。
**验收**：`tests/test_stdlib_phase4.py` 的 AttributeError 全消除（该文件还剩 bytes/str、ord、kwarg 等见 T11）。

---

## T8 — `phase13`/comprehensive/pure_light_hook 方言 NameError（关 ~17 个）

**文件**：`tests/test_stdlib_phase13.py`（14）、`tests/test_stdlib_comprehensive.py`（2）、`tests/test_pure_light_hook.py`（2）
**真实报错**：`NameError: name '列表' is not defined`×7、`name '无' is not defined`×3、`name 'split'/'去除空格'/'replace'/'find' is not defined`
**根因**：测试用了一批解释器未定义的标识符——`列表`(应用 `[]` 或构造器)、`无`(None，light 里是关键字 `空`)、`split/replace/find/去除空格`(字符串方法，误当裸全局名，应为 `文本.分割()`/`文本.去除空白()` 等方法式或对应 builtin)。
**修复**：按 light 实际 builtin/方法替换。先查 `src/keywords.py` 注册表确认正确写法：`分割`(2参)/`去除空白`(1参) 已注册；`空` 是字面量关键字（line 280）；`列表` → 用 `[]` 字面量或模块构造函数。**逐文件修完让 NameError 消失**，不要盲目猜名字，每个替换都要在本地能跑通。
**验收**：上述三文件 NameError 全消除。

---

## T9 — 原生腿能力边界：标 xfail（关 ~7 个，瞬间消红）

**文件**：`tests/test_stdlib_phase3.py`（时间管理：`倒计时`/`定时器`/`多次计时`/`计时函数` 报 `原生腿不可用：回调需函数值动态调用`）、`tests/test_stdlib_phase9.py`（`断言可调用`/`断言满足条件`/`链式断言器` 报 `能力边界：原生腿函数值不可调用…降级 stub`）
**根因**：**已知语言限制**——原生腿（native backend）暂不支持把函数作为值传递/动态调用。测试在验证解释器该能力，当前必败。
**修复**：**不要当 bug 硬改代码**，在测试上标预期失败，让 CI 不红但保留追踪：
```python
@pytest.mark.xfail(strict=False, reason="原生腿暂不支持函数值作为值传递（见 docs/原生腿能力边界.md）")
```
或整类/整文件加 `pytest.mark.xfail`。待原生腿支持函数值后再摘标记。
**验收**：这些用例从 FAILED 变为 XFAIL（不计失败）。

---

## T10 — `统计.light` 缺 sort builtin（关 16 个，先确认本地是否已修）

**文件**：`tests/test_metrics_light.py`（16）
**真实报错**：`AttributeError: module 'light_builtins' has no attribute 'sort'`（在 `stdlib/统计.light` 百分位数调用 `sort(...)`）
**先确认**：**本地工作树 `统计.light` 的 `百分位数` 已改为 `_light_builtin.排序(数据)`（`排序` 是已注册关键字，src/keywords.py:366/457）**。若本地 `python -m pytest tests/test_metrics_light.py` 已绿 → **只重传 0.88** 即可（0.88 是旧 tar）。若本地仍报 `sort` → 修法：把 `统计.light` 里所有 `sort(...)` 改成 `_light_builtin.排序(...)`（或列表 `.排序()` 方法），不要引入英文 `sort`。
**验收**：`tests/test_metrics_light.py` 在 0.88 转绿。

---

## T11 — 真 bug 簇：R13C / R13B / llvm（关 ~9 个）

**文件**：
- `tests/unit/test_原生腿_R13C_对拍扩展.py`（2）：URL 编码对非 ASCII（`光明`）结果不一致（`实际='path/to?q=光明' 期望='path/to?q=奥楥'` → 编码实现 bug）；数值被转成 str（`实际=['1','0.571429','1','0'] 期望=[1.0,1.0,1.0,0.0]`)
- `tests/unit/test_原生腿_R13B_能力扩展.py`（2）：`Test行政区划扩展` → `AttributeError: 'ChinaRegion' object has no attribute '_region_code_map'`（真缺属性/初始化）；`Test网络请求扩展.test_O0_HTTPS_需网络环境` → 应无网时 `pytest.skip`（见 T12）
- `tests/test_llvm_c3_expr.py`（4）：`Failed: DID NOT RAISE NotImplementedError`（某未实现分支应抛 NotImplementedError 却没抛）、`stdout 不符`、数值差（`+ 1.33333` 除法精度）、`双后端 stdout 不一致`
- `tests/test_llvm_optimizer.py`（1）：`Test槽位池按真实用量分配.test_溢出仍然硬报错不悄悄放大` → `RuntimeError not raised`（溢出未硬报错）
**修复**：逐条定位 stdlib/编译器代码修：
- R13C URL 编码对非 ASCII 按 UTF-8 percent-encoding 实现；数值型断言确认类型（不要转 str）。
- R13B `ChinaRegion` 补 `_region_code_map` 初始化（查 `stdlib/行政区划*.light` 或对应扩展模块）。
- llvm_c3_expr：未实现分支补 `raise NotImplementedError`；除法精度对齐 Python 语义。
- llvm_optimizer：溢出分支补 `raise RuntimeError`。
**验收**：对应用例在 0.88 转绿。

---

## T12 — 文档门禁 / 网络 skip / 环境小修（关 ~4 个）

**文件**：
- `tests/unit/test_doc_examples_gate.py::test_不许新增ROT`（1，隔离也失败 → 真问题）：`AssertionError: Lists differ: [] != [('docs/dataset_deepseek_r1_100_examples.m...', ...)]` → 文档里出现了不应新增的 ROT 示例，需审查文档内容或更新门禁白名单（与文档负责人确认）。
- `tests/unit/test_lightpub_doc_importability.py`（1）：`标成text的导入块必须真的导不进来` → 文档里 ```text 代码块被误判可导入，修门禁逻辑或文档标注。
- `tests/unit/test_light_import_hook_stdlib.py::test_import_re_resolves_to_cpython_but_alias_loads_light`（1）：`import re 应命中 CPython re，实际: /usr/local/lib/python3.12/re/__init__.py` → 断言硬编码了 CPython 路径含 `Lib`，FreeBSD 路径不含，属环境特定；改为宽松断言或 `pytest.mark.skipif(platform)`。
- `tests/unit/test_原生腿_R13B_能力扩展.py::Test网络请求扩展.test_O0_HTTPS_需网络环境`（1）：无网环境应 `pytest.skip(reason="需网络")`，不要当失败。
**验收**：对应用例在 0.88 转绿或变 skip/xfail。

---

## 收敛预期
- T1（任务书）做完关 10；T10（统计）本地若已修只需重传，关 16。
- T2（污染）做完关 vscode18+native_leg8+spec_docs_sync7+部分 doc_examples_gate ≈ 33。
- T3–T9、T11–T12 做完后，0.88 全量接近 0 红（剩 T9 标 xfail 的已知限制）。
- **总原则**：每个任务独立分支、独立文件；合入前本地 + 0.88 双验证；统一走 `check_regression.py` 基线闸门。
