# C 外部函数接口（FFI）绑定指南

光明支持通过 C FFI 直接调用 C 动态库（.so/.dll），实现与 C 语言生态的无缝互操作。本文档全面介绍光明 C FFI 的语法、特性和最佳实践。

## 目录

- [概念概述](#概念概述)
- [快速入门](#快速入门)
- [第一阶段：基础 FFI](#第一阶段基础-ffi)
- [第二阶段：指针与数组](#第二阶段指针与数组)
- [第三阶段：高级特性](#第三阶段高级特性)
- [第四阶段：企业级特性](#第四阶段企业级特性)
- [类型映射表](#类型映射表)
- [错误处理](#错误处理)
- [完整示例](#完整示例)
- [测试与验证](#测试与验证)

## 概念概述

光明 C FFI 基于 Python ctypes 实现，将光明语法映射为对应的 ctypes 操作。代码生成器将 FFI 声明编译为 Python 代码，运行时模块（stdlib/FFI.py）提供辅助函数。

### 成熟度演进

| 阶段 | 成熟度 | 核心功能 |
|------|--------|----------|
| 第一阶段 | 35% | 加载库、函数声明、结构体、回调、类型映射 |
| 第二阶段 | 60% | 指针操作、数组操作、内存管理、错误处理 |
| 第三阶段 | 80% | 枚举、联合体、变长参数、回调创建、跨平台路径 |
| 第四阶段 | 90%+ | typedef、位域、函数指针、回调生命周期、调试、宏 |

## 快速入门

### 加载库并调用函数

```光明
加载库 "libm.so" 为 math。
外部 段落 平方根 接收 输入: 小数 返回 小数 在 math。

设 结果 为 平方根(16.0)
打印 结果  # 输出: 4.0
```

### 在 Windows 上加载 DLL

```光明
加载库 "kernel32.dll" 为 kernel。
外部 段落 获取当前进程ID 返回 整数 在 kernel。

设 pid 为 获取当前进程ID()
打印 pid  # 输出当前进程ID
```

## 第一阶段：基础 FFI

### 加载动态库

```光明
加载库 "库路径" 为 库别名
```

- `库路径`：动态库文件路径
- `库别名`：后续引用该库的标识符

### 声明 C 函数

```光明
外部 段落 函数名 接收 参数1: 类型, 参数2: 类型 返回 返回类型 在 库别名
```

自定义 C 函数名：

```光明
外部 段落 打印 为 "printf" 接收 格式: 文本 返回 整数 在 libc
```

### 定义 C 结构体

```光明
外部 结构体 点 {
    x: 小数,
    y: 小数
}
```

### 定义回调类型

```光明
外部 回调 比较器 接收 甲: 整数, 乙: 整数 返回 整数
```

## 第二阶段：指针与数组

### 指针操作

```光明
外部 段落 分配内存 接收 大小: 整数 返回 指针 在 libc
外部 段落 释放内存 接收 指针 在 libc

设 ptr 为 分配内存(1024)
设指针值(ptr, 42)
设 值 为 解引用(ptr)  # 值 = 42
释放内存(ptr)
```

内置函数：

| 函数 | 说明 |
|------|------|
| `取地址(变量)` | 获取变量指针 |
| `解引用(指针)` | 解引用指针 |
| `指针偏移(指针, 偏移量)` | 指针偏移 |
| `设指针值(指针, 值)` | 设置指针指向的值 |

### 数组操作

```光明
设 arr 为 创建数组(整数, 5)
设置数组(arr, 0, 10)
设置数组(arr, 1, 20)
```

| 函数 | 说明 |
|------|------|
| `创建数组(类型, 大小)` | 创建 C 数组 |
| `设置数组(数组, 索引, 值)` | 设置数组元素 |

### 内存管理

```光明
设 buf 为 分配内存(256)
设指针值(buf, 100)
打印 解引用(buf)  # 100
释放内存(buf)
```

| 函数 | 说明 |
|------|------|
| `分配内存(大小)` | 分配内存块 |
| `释放内存(指针)` | 释放内存块 |

### 错误处理

```光明
尝试：
    设 结果 为 可能要失败的FFI函数()
捕获 外部错误 为 甲：
    打印 "FFI 调用失败: " + 甲
```

| 函数 | 说明 |
|------|------|
| `FFI错误()` | 获取最近的 FFI 错误信息 |
| `系统错误码()` | 获取系统 errno |
| `设系统错误码(值)` | 设置系统 errno |

## 第三阶段：高级特性

### C 枚举映射

```光明
外部 枚举 颜色 {
    红 = 0,
    绿 = 1,
    蓝 = 2
}

外部 枚举 信号 {
    SIGINT = 2,
    SIGKILL = 9,
    SIGTERM = 15
}
```

### C 联合体

```光明
外部 联合体 数值 {
    整数: 整数,
    小数: 小数
}
```

### 变长参数

```光明
外部 变长参数 段落 格式化 为 "sprintf" 接收 缓冲区: 文本 返回 整数 在 libc
```

### 回调创建

```光明
外部 回调 回调类型 接收 整数 返回 整数

段落 我的回调 接收 输入：
    返回 输入 + 1

设 cb 为 创建回调(回调类型, 我的回调)
```

### 结构体按值传递

```光明
设 pt 为 创建结构体值(点, x=3.0, y=4.0)
```

### 跨平台库路径

```光明
设 路径 为 解析库路径({
    "win": "kernel32.dll",
    "linux": "libc.so.6",
    "mac": "libc.dylib"
})
```

### 结构体工具

| 函数 | 说明 |
|------|------|
| `结构体大小(结构体类)` | 获取字节大小 |
| `字段偏移(结构体类, 字段名)` | 获取字段偏移量 |
| `结构体转字节(实例)` | 序列化为字节串 |
| `字节转结构体(数据, 类)` | 反序列化 |

## 第四阶段：企业级特性

### 类型别名（typedef）

```光明
外部 类型别名 尺寸 为 整数
外部 类型别名 宽字符 为 文本
```

### 位域（Bitfield）

```光明
外部 位域 标志 : 整数 {
    读: 1,
    写: 1,
    执行: 1
}

外部 位域 寄存器 : 整数 {
    模式: 2,
    启用: 1,
    速率: 3
}
```

位域操作：

```光明
位域设置(标志, '读', 1)
设 状态 为 位域获取(标志, '读')
```

### 函数指针类型

```光明
外部 函数指针 比较器 接收 甲: 整数, 乙: 整数 返回 整数
外部 函数指针 无参回调 接收 返回 无
```

### 回调生命周期管理

```光明
设 键 为 注册回调("我的回调", 回调函数指针)
设 cb 为 获取回调(键)
打印 列出回调()  # 所有已注册回调
注销回调(键)       # 释放回调
```

回调注册表防止 Python GC 回收 ctypes 回调对象，确保 C 代码持有的回调指针始终有效。

| 函数 | 说明 |
|------|------|
| `注册回调(名称, 对象)` | 注册回调，返回键 |
| `注销回调(键)` | 注销回调 |
| `获取回调(键)` | 获取已注册回调 |
| `列出回调()` | 列出所有回调键 |
| `清理回调()` | 清理所有回调 |

### FFI 调试系统

```光明
外部 调试 {
    开启,
    记录调用,
    记录类型,
    追踪内存
}
```

运行时控制：

```光明
启用调试()
FFI禁用调试()
设 日志 为 FFI获取日志()
清空日志()
```

### C 预处理器宏

```光明
外部 宏 缓冲区大小 为 1024
外部 宏 版本 为 "1.0.0"
```

宏管理：

```光明
定义宏("最大连接", "100")
设 值 为 获取宏("最大连接")  # "100"
打印 列出宏()  # 所有宏定义
清理宏()
```

## 类型映射表

| 光明类型 | ctypes 类型 | C 类型 | 说明 |
|----------|-------------|--------|------|
| 整数 | c_int | int | 32 位有符号整数 |
| 小数 | c_double | double | 双精度浮点数 |
| 文本 | c_char_p | char* | 字符串指针 |
| 布尔 | c_bool | bool | 布尔值 |
| 字节 | c_char | char | 单字节字符 |
| 长整数 | c_long | long | 长整数 |
| 无符号整数 | c_uint | unsigned int | 无符号整数 |
| 浮点 | c_float | float | 单精度浮点数 |
| 无 | None | void | 无返回值 |
| 指针 | c_void_p | void* | 通用指针 |
| 短整数 | c_short | short | 短整数 |

## 错误处理

### 异常捕获

```光明
尝试：
    加载库 "不存在的库.so" 为 lib
捕获 外部错误 为 错误：
    打印 "加载失败: " + 错误
```

### 错误码查询

```光明
设 码 为 系统错误码()
设 信息 为 FFI错误()
```

## 完整示例

### 调用 libm 数学库

```光明
加载库 "libm.so" 为 math

外部 段落 平方根 接收 输入: 小数 返回 小数 在 math
外部 段落 幂运算 为 "pow" 接收 底数: 小数, 指数: 小数 返回 小数 在 math
外部 段落 正弦 为 "sin" 接收 角度: 小数 返回 小数 在 math

设 根 为 平方根(25.0)
打印 "sqrt(25) = " + 根  # 5.0

设 幂 为 幂运算(2.0, 10.0)
打印 "2^10 = " + 幂  # 1024.0

设 值 为 正弦(3.14159 / 2)
打印 "sin(pi/2) = " + 值  # ~1.0
```

### 结构体传参

```光明
外部 结构体 矩形 {
    宽: 小数,
    高: 小数
}

外部 段落 计算面积 接收 矩形 返回 小数 在 geom

设 矩形 为 创建结构体值(矩形, 宽=10.0, 高=5.0)
设 面积 为 计算面积(矩形)
打印 面积  # 50.0
```

### 回调排序

```光明
外部 回调 排序比较器 接收 甲: 整数, 乙: 整数 返回 整数

段落 升序比较 接收 甲, 乙：
    如果 甲 < 乙：
        返回-1
    如果 甲 > 乙：
        返回 1
    返回 0

设 比较器 为 创建回调(排序比较器, 升序比较)
设 键 为 注册回调("排序", 比较器)
```

## 测试与验证

光明 C FFI 提供了完整的测试覆盖，84 个测试用例分布在 4 个阶段中：

```bash
# 运行所有 FFI 测试
python -m pytest tests/test_ffi.py tests/test_ffi_phase2.py tests/test_ffi_phase3.py tests/test_ffi_phase4.py -v

# 单独运行某个阶段
python -m pytest tests/test_ffi.py -v           # 第一阶段（16 个测试）
python -m pytest tests/test_ffi_phase2.py -v    # 第二阶段（17 个测试）
python -m pytest tests/test_ffi_phase3.py -v    # 第三阶段（23 个测试）
python -m pytest tests/test_ffi_phase4.py -v    # 第四阶段（28 个测试）
```

## 架构说明

### 文件结构

```
src/
├── ast_nodes_v3.py      # FFI AST 节点定义（v3.2 语法）
├── ast_nodes.py         # 旧 AST 兼容节点（LLVM 后端）
├── light_parser_v3.py    # 解析器导出
├── keywords.py          # FFI 关键词定义
├── lexer.py             # 词法分析器（复合词）
├── parser_stmt.py       # FFI 语句解析器
├── code_generator.py    # Python 代码生成器
└── llvm/codegen.py      # LLVM 后端（占位）

stdlib/
└── FFI.py               # FFI 运行时模块（~700 行）

tests/
├── test_ffi.py          # 第一阶段测试（16 个）
├── test_ffi_phase2.py   # 第二阶段测试（17 个）
├── test_ffi_phase3.py   # 第三阶段测试（23 个）
└── test_ffi_phase4.py   # 第四阶段测试（28 个）
```

### 编译流程

1. **词法分析**：`lexer.py` 识别 FFI 关键词和复合词
2. **语法解析**：`parser_stmt.py` 的 `_parse_ffi_decl` 分发到各子解析方法
3. **AST 构建**：`ast_nodes_v3.py` 中的 FFI 节点实例化
4. **代码生成**：`code_generator.py` 生成 Python ctypes 代码
5. **运行时**：`stdlib/FFI.py` 提供辅助函数和回调注册表