# 光明编程语言 - 统一语法规范 v3.1

**版本**：v3.1  
**日期**：2026-06-24  
**状态**：纯缩进式语法 + 句号可选 + 范围表达式 + 列表解构  
**依据**：Light 递归下降解析器 + 端到端测试验证

---

## 重要变更（v2.0 → v3.1）

| 变更项 | v2.0（旧） | v3.1（新） | 说明 |
|--------|-----------|-----------|------|
| 块结束标记 | `结束。`（必需） | 移除 | 采用纯缩进式语法，类似 Python |
| 句号 | 语句必须以句号结尾 | 句号可选 | 缩进和关键字自然分隔语句 |
| 解构赋值 | 仅支持元组 `(甲, 乙)` | 支持元组和列表 `[首, 余]` | 增加列表解构语法 |
| 范围表达式 | 无 | `1至10` / `1到10步2` | 生成 `range(start, end+1, step)` |
| 无括号调用 | 部分支持 | 完全支持 | 动词调用可省略括号 |

---

## 核心设计原则

### 1. 纯缩进式语法
使用冒号 `：` 开始块，使用缩进来表示块层级，不再需要「结束」关键字。

```光明
段落 平方 接收 数值：
  返回 数值 乘以 数值

如果 甲 大于 10：
  打印 "大于10"
否则：
  打印 "不大于10"

类 动物：
  属性 名称
  段 说话：
    打印 "叫声"
```

### 2. 句号可选
语句结尾的句号 `。` 是可选的，用于增强可读性而非语法必需。

```光明
# 两种写法都正确
设 甲 为 10
设 乙 为 20。
```

### 3. 中文无空格
中文关键字和标识符之间不需要空格，保持中文书写的自然流畅。

```光明
如果甲大于十那么打印甲
```

---

## 一、词法规范

### 1.1 注释

```光明
# 井号行注释
// 双斜杠行注释
```

块注释用三个反引号包围：
```光明
```
这是块注释
```
```

### 1.2 关键字

**双字关键字**（按类别）：

| 类别 | 关键字 |
|------|--------|
| 定义声明 | `设`、`为`、`定义`、`常量`、`类型`、`导入`、`导出`、`从`、`等于` |
| 条件判断 | `如果`、`那么`、`否则`、`否则若`、`若`、`则` |
| 循环控制 | `遍历`、`当`、`跳出`、`跳过`、`在`、`对`、`中的` |
| 段落定义 | `段落`（或`段`）、`接收`、`返回`、`函数`、`输入`、`参数` |
| 泛型 | 类型参数用 `[]` 包围，如 `T`、`K, V` |
| 异常处理 | `尝试`、`捕获`、`抛出`、`最终` |
| 模式匹配 | `匹配`、`情况` |
| 类与对象 | `类`、`继承`、`实现`、`属性`、`私属性`、`构造`、`新建`、`接口`、`方法` |
| 上下文 | `使用`、`标注` |
| 装饰器 | `抽象` |

**单字保留字**：

| 字 | 含义 | 备注 |
|----|------|------|
| `真` | True | 布尔字面量 |
| `假` | False | 布尔字面量 |
| `空` | None | 空值字面量 |
| `且` | 逻辑与 | 推荐写法 |
| `或` | 逻辑或 | |
| `与` | 逻辑与 | `且`的别名 |
| `非` | 逻辑非 | |
| `并` | 管道连接符 | `甲 并 乙` 等效于 `甲 -> 乙` |
| `之` | 属性提取符/遍历连接符 | `对象之属性`、`遍历 甲 之 列表` |
| `于` | 遍历连接符 | `遍历 甲 于 列表`（等效于`之`） |
| `的` | 定语标记 | `对象的属性`（等效于`之`） |
| `己` | self 引用 | 类方法中使用 |
| `父` | super() 调用 | 类方法中使用 |
| `至` | 范围表达式 | `1至10` |
| `到` | 范围表达式 | `1到10` |
| `步` | 范围步长 | `1到10步2` |

> **注意**：`与` 和 `且` 均可用作逻辑与运算符。`且` 是推荐写法，`与` 是兼容别名。

### 1.3 运算符关键字

| 类别 | 中文关键字 | 对应符号 | 示例 |
|------|-----------|---------|------|
| 加法 | `加上` | `+` | `甲 加上 乙` |
| 减法 | `减去` | `-` | `甲 减去 乙` |
| 乘法 | `乘以` | `*` | `甲 乘以 乙` |
| 除法 | `除以` | `/` | `甲 除以 乙` |
| 取模 | `取余` | `%` | `甲 取余 乙` |
| 整除 | `整除` | `//` | `甲 整除 乙` |
| 幂运算 | `幂` | `**` | `甲 幂 乙` |
| 大于 | `大于` | `>` | `甲 大于 乙` |
| 小于 | `小于` | `<` | `甲 小于 乙` |
| 等于 | `等于` | `==` | `甲 等于 乙` |
| 不等于 | `不等于` | `!=` | `甲 不等于 乙` |
| 大于等于 | `大于等于` | `>=` | `甲 大于等于 乙` |
| 小于等于 | `小于等于` | `<=` | `甲 小于等于 乙` |

### 1.4 符号

| 符号 | 名称 | 用途 |
|------|------|------|
| `=` | 等号 | 赋值 |
| `+` | 加号 | 加法 |
| `-` | 减号 | 减法 / 负号 |
| `*` | 乘号 | 乘法 |
| `/` | 除号 | 除法 |
| `%` | 百分号 | 取模 |
| `**` | 双星号 | 幂运算 |
| `==` | 双等号 | 等于比较 |
| `!=` | 不等号 | 不等于比较 |
| `>` | 大于号 | 大于比较 |
| `<` | 小于号 | 小于比较 |
| `>=` | 大于等于 | 大于等于比较 |
| `<=` | 小于等于 | 小于等于比较 |
| `&&` | 双与号 | 逻辑与（待实现） |
| `\|\|` | 双或号 | 逻辑或（待实现） |
| `->` | 箭头 | 管道操作符 |
| `@` | at符号 | 装饰器 |
| `#` | 井号 | 行注释 |
| `//` | 双斜杠 | 行注释 |
| ``` ``` ``` | 三反引号 | 块注释 |

### 1.5 括号与分隔符

| 符号 | 名称 | 用途 |
|------|------|------|
| `（` `）` | 中文圆括号 | 元组解构、表达式分组 |
| `(` `)` | 英文圆括号 | 函数调用参数 |
| `［` `］` / `[` `]` | 方括号 | 列表字面量、列表解构、索引访问 |
| `【` `】` | 中文方括号 | 字典字面量 |
| `《` `》` | 书名号 | 段名引用 |
| `，` / `,` | 逗号 | 参数分隔、元素分隔 |
| `。` | 句号 | 语句结束（可选） |
| `：` / `:` | 冒号 | 块开始、类型注解 |
| `；` / `;` | 分号 | 多语句同行（不推荐） |
| `.` | 点号 | 成员访问 |

### 1.6 字面量

#### 数字字面量
```光明
设 甲 为 123          # 整数
设 乙 为 3.14         # 浮点数
设 丙 为 -456         # 负数
设 丁 为 十           # 中文数字（零到十）
```

#### 字符串字面量
```光明
"你好"       # 双引号字符串
'世界'       # 单引号字符串
"你好，{名字}" # 字符串插值
```

#### 列表字面量
```text
[1, 2, 3]           # 英文逗号
［1，2，3］         # 中文方括号+中文逗号
列 1 2 3            # 无括号列表（动词形式）
```

#### 字典字面量
```光明
设 人 为 【"名"："张三"，"龄"：20】  # 中文方括号字典
```

---

## 二、语句规范

### 2.1 变量声明

```光明
设 甲 为 10
设 乙 为 "你好"
设 丙 为 真
```

**带类型注解**（可选）：
```光明
设 甲: 整数 为 10
```

### 2.2 赋值语句

```光明
甲 = 20
甲 等于 30
甲 为 40
```

**复合赋值**：
```光明
设 甲 为 甲 加上 1
设 甲 为 甲 减去 1
设 甲 为 甲 乘以 2
设 甲 为 甲 除以 2
```

### 2.3 解构赋值

#### 元组解构
```光明
设（甲，乙）为 元组
```

#### 列表解构
```光明
设 [首，余] 为 列表
```

### 2.4 条件语句

```光明
如果 条件：
  语句1
  语句2

如果 条件：
  语句
否则：
  语句

如果 条件1：
  语句1
否则若 条件2：
  语句2
否则：
  语句3
```

**单行形式**：
```光明
如果 条件 那么 语句
```

### 2.5 循环语句

#### 当循环（while）
```光明
当 条件：
  语句
```

#### 遍历循环（for）
```光明
遍历 项 之 列表：
  打印 项

遍历 项 于 列表：
  打印 项

遍历 索引, 值 之 列表：
  打印 索引, 值
```

#### 范围遍历
```光明
遍历 i 之 1至10：
  打印 i

遍历 i 之 1到10步2：
  打印 i
```

### 2.6 段落定义

#### 标准形式
```光明
函数 名(参数1, 参数2):
  语句
  返回 值
```

#### 书名号形式
```光明
《名》段(参数1, 参数2)：
  语句
  返回 值
```

#### 无参数
```光明
函数 问候():
  打印 "你好"
```

#### 泛型段落
使用方括号 `[]` 定义类型参数：

```光明
函数 恒等[T](x):
  返回 x

函数 映射[T, U](列表, 函数):
  返回 列表
```

### 2.7 函数调用

#### 带括号调用
```光明
打印("你好")
平方(5)
```

#### 无括号调用（动词形式）
```光明
打印 "你好"
排序 列表
列 1 2 3
```

#### 方法调用
```光明
对象.方法(参数)
对象之方法(参数)
```

### 2.8 返回语句

```光明
返回           # 无返回值
返回 值        # 返回一个值
返回 甲, 乙    # 返回多个值（元组）
```

### 2.9 循环控制

```光明
跳出           # break
跳过           # continue
```

### 2.10 导入导出

```光明
导入 模块
导入 模块1, 模块2
从 模块 导入 函数1, 函数2
导出 符号1, 符号2
导出 全部
```

---

## 三、表达式规范

### 3.1 算术表达式

```光明
甲 加上 乙
甲 减去 乙
甲 乘以 乙
甲 除以 乙
甲 取余 乙
甲 幂 乙
甲 整除 乙
```

### 3.2 比较表达式

```光明
甲 大于 乙
甲 小于 乙
甲 等于 乙
甲 不等于 乙
甲 大于等于 乙
甲 小于等于 乙
```

### 3.3 逻辑表达式

```光明
设 结果 为 甲 且 乙
设 结果 为 甲 与 乙
设 结果 为 甲 或 乙
设 结果 为 非 甲
```

### 3.4 范围表达式

```光明
设 甲 为 1至10          # range(1, 11)
设 乙 为 1到10          # range(1, 11)
设 丙 为 1到10步2       # range(1, 11, 2)
```

> **注意**：范围是包含结束值的，`1至10` 包含 1 到 10 的所有整数。

### 3.5 三元条件表达式

```光明
如果 条件 那么 值1 否则 值2
```

### 3.6 管道操作

```text
数据 -> 处理 -> 输出
数据 并 处理 并 输出
```

### 3.7 成员访问

```光明
打印 对象.名称
打印 对象.方法()
打印 对象之名称
打印 对象的名称
```

### 3.8 索引访问

```光明
列表[0]
字典["键"]
```

### 3.9 列表推导

```光明
设 结果 为 [表达式 遍历 变量 之 列表]
设 结果 为 [表达式 遍历 变量 之 列表 若 条件]
```

**示例**：
```光明
设 平方表 为 [甲 乘以 甲 遍历 甲 之 数列]
设 正数表 为 [甲 遍历 甲 之 数列 若 甲 大于 0]
```

### 3.10 字典推导

```光明
设 结果 为 【键：值 遍历 变量 之 列表】
设 结果 为 【键：值 遍历 变量 之 列表 若 条件】
```

### 3.11 匿名函数（Lambda）

```光明
设 匿名段落 为 接收 参数：返回 表达式
```

**示例**：
```光明
设 平方 为 接收 甲：返回 甲 乘以 甲
```

---

## 四、类与对象

### 4.1 类定义

```光明
类 类名：
  属性 属性名
  属性 属性名2
  
  构造 接收 参数：
    己属性名 为 参数
  
  函数 方法名(参数):
    语句
```

### 4.1.1 泛型类
使用方括号 `[]` 定义类型参数：

```光明
类 栈[T]：
  属性 数据

  构造：
    己数据 为 []

  段落 入栈 接收 值：
    列表追加(己数据, 值)

  段落 出栈：
    返回 己数据之弹出
```

### 4.2 类继承

```光明
类 子类 继承 父类：
  # 子类内容
```

### 4.3 接口实现

```光明
类 类名 实现 接口甲, 接口乙：
  # 实现接口方法
```

### 4.4 接口定义

```光明
接口 接口名：
  函数 方法名(参数)
  属性 属性名
```

### 4.5 接口继承

```光明
接口 子接口 继承 父接口1, 父接口2：
  # 子接口新增方法
```

### 4.6 对象创建

```光明
令 对象 = 新建 类名(参数1, 参数2)
```

### 4.7 self 引用

```光明
打印 己属性名      # 访问属性
打印 己方法名()    # 调用方法
打印 父.方法名()   # 调用父类方法
```

---

## 五、异常处理

### 5.1 try-catch

```光明
尝试：
  可能出错的语句
捕获 异常变量：
  处理异常
最终：
  总会执行的语句
```

### 5.2 抛出异常

```光明
抛出 异常
抛出 "错误信息"
```

---

## 六、模式匹配

```光明
匹配 值：
  情况 模式1：
    语句
  情况 模式2：
    语句
  情况 _：
    默认语句
```

---

## 七、装饰器

```光明
@装饰器名 标注 《段落名》段(参数)：
  语句
```

**示例**：
```光明
@日志 标注 《平方》段(数值)：
  返回 数值 乘以 数值
```

---

## 八、上下文管理器

```光明
使用 表达式：
  语句

使用 表达式 为 变量：
  语句
```

---

## 九、语法速查表

| 结构 | 标准语法 | 示例 | 句号 |
|------|---------|------|------|
| 变量声明 | `设 名 为 值` | `设 甲 为 10` | 可选 |
| 赋值 | `名 = 值` | `甲 = 20` | 可选 |
| 元组解构 | `设（甲，乙）为 值` | `设（甲，乙）为 元组` | 可选 |
| 列表解构 | `设 [首，余] 为 值` | `设 [首，余] 为 列表` | 可选 |
| 段落定义 | `段落 名 接收 参数：...` | `段落 平方 接收 数：返回 数 乘 数` | - |
| 函数调用 | `名(参数)` | `平方(5)` | 可选 |
| 无括号调用 | `动词 参数1 参数2` | `打印 "你好"` | 可选 |
| 条件 | `如果 条件：...否则：...` | `如果 甲 大于 10：...否则：...` | - |
| 当循环 | `当 条件：...` | `当 甲 小于 5：...` | - |
| 遍历 | `遍历 项 之 列表：...` | `遍历 甲 之 列表：打印(甲)` | - |
| 范围遍历 | `遍历 i 之 1至10：...` | `遍历 i 之 1至10：打印(i)` | - |
| 范围步长 | `遍历 i 之 1到10步2：...` | `遍历 i 之 1到10步2：打印(i)` | - |
| 列表 | `[1，2，3]` | `设 甲 为 [1，2，3]` | 可选 |
| 字典 | `【"键"："值"】` | `设 甲 为 【"名"："张三"】` | 可选 |
| 列表推导 | `[表达式 遍历 变量 之 列表]` | `[甲 乘以 甲 遍历 甲 之 数列]` | - |
| 字典推导 | `【键：值 遍历 变量 之 列表】` | `【甲：甲 遍历 甲 之 数列】` | - |
| 匿名函数 | `接收 参数：返回 表达式` | `接收 甲：返回 甲 乘 甲` | - |
| 三元条件 | `如果 条件 那么 值1 否则 值2` | `设 甲 为 如果 乙 那么 1 否则 0` | 可选 |
| 类定义 | `类 名 [继承 父]：...` | `类 狗 继承 动物：` | - |
| 接口定义 | `接口 名 [继承 父]：...` | `接口 可打印：方法 显示()` | - |
| 对象创建 | `新建 类名(参数)` | `设 甲 为 新建 狗("旺财")` | 可选 |
| 属性访问 | `对象.属性` / `对象之属性` | `设 甲 为 对象.名称` | - |
| 管道 | `甲 -> 乙` / `甲 并 乙` | `设 甲 为 数据 -> 处理` | 可选 |
| 异常处理 | `尝试：...捕获 变量：...` | `尝试：...捕获 e：...` | - |
| 模式匹配 | `匹配 值：情况 ...` | `匹配 甲：情况 1：...` | - |
| 装饰器 | `@名 标注 段落定义` | `@日志 标注 《甲》段()：...` | - |
| 上下文 | `使用 表达式 [为 变量]：...` | `使用 甲 为 乙：...` | - |
| 导入 | `导入 模块` / `从 模块 导入 符号` | `导入 数学` | 可选 |
| 导出 | `导出 符号` | `导出 平方` | 可选 |
| 返回 | `返回 值` | `返回 结果` | 可选 |
| 跳出 | `跳出` | `跳出` | 可选 |
| 跳过 | `跳过` | `跳过` | 可选 |

---

## 十、语法风格统一表

### 推荐写法 vs 兼容写法

| 不推荐（旧语法） | 推荐（新语法） | 说明 |
|-----------------|---------------|------|
| `结束。` | （省略，用缩进） | 纯缩进式语法 |
| `打印 甲。` | `打印(甲)。` 或 `打印 甲` | 动词调用两种均可 |
| `新建 类名 参数` | `新建 类名(参数)` | 类实例化推荐括号形式 |
| `遍历 甲 于 列表` | `遍历 甲 之 列表` | `之` 是推荐写法 |
| `段 平方(数值)：` | `段落 平方 接收 数值：` | 标准段落定义形式 |
| `使用 表达式 作为 变量` | `使用 表达式 为 变量` | `为` 是标准关键字 |

---

## 十一、完整示例

### 阶乘函数

```光明
段落 阶乘 接收 n：
  如果 n 小于等于 1：
    返回 1
  返回 n 乘以 阶乘(n 减去 1)

打印 阶乘(5)
```

### 斐波那契数列

```光明
段落 斐波那契 接收 n：
  如果 n 小于等于 1：
    返回 n
  返回 斐波那契(n 减去 1) 加上 斐波那契(n 减去 2)

遍历 i 之 1至10：
  打印 斐波那契(i)
```

### 类示例

```光明
类 动物：
  属性 名称
  
  构造 接收 名称：
    己名称 为 名称
  
  段落 说话：
    打印 "动物叫声"

类 狗 继承 动物：
  段落 说话：
    打印 "汪汪汪"

设 旺财 为 新建 狗("旺财")
旺财.说话()
```

### 列表解构

```text
设 [首，次，...余] 为 [1, 2, 3, 4, 5]
打印 首    # 1
打印 次    # 2
打印 余    # [3, 4, 5]
```

### 范围遍历

```光明
# 打印 1 到 10
遍历 i 之 1至10：
  打印 i

# 打印 1 到 10 的奇数
遍历 i 之 1到10步2：
  打印 i
```

---

## 十二、向后兼容说明

v3.1 语法完全向后兼容 v2.0 语法：

1. **句号兼容**：有句号的代码仍然可以正常运行，句号是可选的
2. **结束兼容**：虽然不再推荐使用「结束」，但如果需要可以通过配置开启（默认关闭）
3. **运算符兼容**：符号运算符（+、-、*、/ 等）和中文运算符都支持
4. **括号兼容**：中文括号和英文括号都支持

---

**文档版本**：v3.1  
**最后更新**：2026-06-24  
**维护者**：Light Language Team
