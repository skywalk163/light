# Light Level 5 语法规范

**版本**: v5.0
**日期**: 2026-07-01
**状态**: 已实现（异常处理 + 模块系统）
**基于**: Light Level 4（完整面向对象支持）

---

## 一、新增特性概览

### 1.1 异常处理

- `尝试`/`捕获`/`最终` 块
- `抛出` 语句
- 中文异常类型别名

### 1.2 模块系统

- `导入` 模块
- `导出` 符号（独立语句 + 内联）
- 编译期内联机制

---

## 二、异常处理

### 2.1 语法

```
尝试：
    可能出错的代码()
捕获 值错误 as e：
    输出("值错误：" + 字符串(e))
捕获 类型错误：
    输出("类型错误")
捕获：
    输出("其他错误")
最终：
    清理资源()
```

### 2.2 抛出语句

```
抛出 "错误消息"
抛出 错误对象
```

### 2.3 异常类型映射

| 中文别名 | Python 类型 |
|---------|-----------|
| 异常 | Exception |
| 值错误 | ValueError |
| 类型错误 | TypeError |
| 键错误 | KeyError |
| 索引错误 | IndexError |
| 除零错误 | ZeroDivisionError |
| 属性错误 | AttributeError |
| 名称错误 | NameError |
| 文件错误 | FileNotFoundError |
| 运行错误 | RuntimeError |

---

## 三、模块系统

### 3.1 导入语法

```
导入 模块名
导入 子目录/模块名
```

### 3.2 导出语法

```
# 独立导出语句
导出 函数1
导出 函数2

# 内联导出
段 函数1 导出 接收 参数：
    ...
```

### 3.3 搜索路径

1. 当前目录
2. 当前目录/模块/
3. 当前目录/modules/
4. 当前目录/test_modules/
5. 标准库目录

---

## 四、新增关键字

共计 40 个关键字（Level 4 的 34 + 模块系统 2 + 异常处理 4）：

`设`, `段落`, `段`, `返回`, `结束`, `为`, `如果`, `否则`, `当`, `接收`, `加`, `减`, `乘`, `除`, `取模`, `等于`, `小于`, `大于`, `小于等于`, `大于等于`, `不等于`, `且`, `或`, `非`, `遍历`, `在`, `类`, `属性`, `己`, `继承`, `父`, `尝试`, `捕获`, `最终`, `抛出`, `导入`, `导出`

---

## 五、使用示例

### 5.1 异常处理

```
尝试：
    设 结果 为 1 除 0
捕获 除零错误：
    输出("除零错误")
捕获：
    输出("其他错误")
最终：
    输出("清理")
```

### 5.2 模块系统

math_utils.light:
```
段 加法 导出 接收 a, b：
    返回 a 加 b
结束。
```

main.light:
```
导入 math_utils
设 r 为 加法(3, 4)
输出(r)
```

---

## 六、实现状态

| 特性 | 状态 | 说明 |
|------|------|------|
| 异常处理 | ✅ 完成 | try/catch/finally/throw |
| 模块系统 | ✅ 完成 | 导入/导出/内联 |
| 自举验证 | ⚠️ 限制 | 需 Level 5 编译器编译自身 |

---

## 七、后续计划

- Level 6: 类型注解
- 自定义异常类型
- 动态导入
- 包管理
