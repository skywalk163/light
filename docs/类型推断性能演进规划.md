# 光明类型推断器性能演进规划

> 版本: v1.0 | 日期: 2026-08-04 | 基于 v4.2.0 基准数据

---

## 0. 汇报摘要：阶段目标对比总表

### 表 A：阶段概览

| 阶段 | 对应版本 | 目标规模 | 推断时间目标 | 触发条件 | 优化项数 | 累计风险 |
|------|---------|---------|-------------|---------|---------|---------|
| 0（当前） | v4.2.0 | <200 段 | <20 ms | — | — | — |
| 1 | v4.3.0 | 200–500 段 | <50 ms | 实测 >30ms 或 规模达 200 段 | 3 | 低 |
| 2 | v4.4.0 | 500–2000 段 | <100 ms | 实测 >100ms 或 IDE 延迟感知 | 3 | 中 |
| 3 | v5.0.0 | 2000+ 段 | <200 ms | 实测 >200ms 或 规模达 2000 段 | 3 | 高 |

### 表 B：当前基准数据（v4.2.0）

| 指标 | 数据 |
|------|------|
| 100 段推断时间 | ~8.8 ms |
| 时间增长曲线 | 接近线性 O(n) |
| 内存占用（100 段） | ~166 KB |
| 最热路径 | `is_instance`（4,095 次）、`unify`（436 次）、`apply_substitution`（1,395 次） |

### 表 C：各阶段优化项明细

| 阶段 | 优化项 | 针对热点 | 预期总提升 | 风险 | 改动文件 |
|------|-------|---------|-----------|------|---------|
| 1 | `is_instance` → 整数分派 | 语句分发（4,095 次） | 15–25% | 低 | `ast_nodes.py`、`type_inferencer.py` |
| 1 | `TypeSubstitution` CoW | 对象创建（617 次） | 5–10% | 低 | `type_system.py` |
| 1 | `apply_substitution` 单例短路 | 替换调用（1,395 次） | 3–5% | 极低 | `type_system.py` |
| **1 合计** | | | **25–40%** | | |
| 2 | 调用图拓扑迭代 | HM 两轮冗余 | 30–40% | 中 | `type_inferencer.py` |
| 2 | 增量推断缓存 | 全量重推断 | 50–80%（IDE 场景） | 中 | `type_inferencer.py` |
| 2 | `unify` 快速路径 | 跨类型合一 | 15–20% | 低 | `type_system.py` |
| **2 合计** | | | **60–85%**（IDE 场景） | | |
| 3 | 并行段推断 | 串行推断 | 40–60% | 高 | `type_inferencer.py` |
| 3 | 符号表全局索引 | 深层作用域查找 | 50–70% | 低 | `type_system.py` |
| 3 | 类型解析 LRU 缓存 | 重复解析注解 | 90% | 极低 | `type_system.py` |
| **3 合计** | | | **70–90%** | | |

### 表 D：推断时间随规模增长预估

| 模块规模 | 阶段 0（当前） | 阶段 1 后（预估） | 阶段 2 后（预估） | 阶段 3 后（预估） |
|---------|-------------|-----------------|-----------------|-----------------|
| 10 段 | ~1.8 ms | ~1.2 ms | <1 ms | <1 ms |
| 50 段 | ~5.8 ms | ~4.0 ms | ~2.5 ms | ~1.5 ms |
| 100 段 | ~8.8 ms | ~6.0 ms | ~3.5 ms | ~2.0 ms |
| 500 段 | ~45 ms（估） | ~30 ms | ~15 ms | ~8 ms |
| 2000 段 | ~180 ms（估） | ~120 ms | ~60 ms | ~30 ms |

---

## 1. 当前基准（v4.2.0）

| 指标 | 数据 |
|------|------|
| 100 段模块推断时间 | ~8.8 ms |
| 50 个类型变量推断时间 | ~5.1 ms |
| 30 层嵌套调用推断时间 | ~6.4 ms |
| 100 段内存占用 | ~166 KB |
| 时间增长曲线 | 接近线性（O(n)） |
| 最热路径 | `is_instance`（4,095 次/100段）、`unify`（436 次）、`apply_substitution`（1,395 次） |

**结论**：当前规模（<200 段）性能充裕，无需优化。

---

## 2. 目标定义

| 阶段 | 目标规模 | 推断时间目标 | 触发条件 |
|------|---------|-------------|---------|
| 阶段 0（当前） | <200 段 | <20 ms | — |
| 阶段 1 | 200–500 段 | <50 ms | 实测 >30ms 时启动 |
| 阶段 2 | 500–2000 段 | <100 ms | 阶段 1 完成后评估 |
| 阶段 3 | 2000+ 段 | <200 ms | 阶段 2 完成后评估 |

---

## 3. 阶段 1：低风险优化（200–500 段）

### 3.1 `is_instance` 改为 `type_id` 整数分派

**现状**：[type_inferencer.py](file:///d:/traework/light/src/type_inferencer.py#L53-L57) 每次 `type(node).__name__` 做字符串比较，占函数调用总次数的 **25%**。

**方案**：为所有 AST 节点添加 `_ast_type_id` 整数常量，用 `node._ast_type_id == AST_ID_RETURN_STMT` 替代 `is_instance(node, 'ReturnStatement')`。

**改动范围**：
- `ast_nodes.py`：为每个节点类添加 `_ast_type_id` 类属性
- `type_inferencer.py`：`_infer_statement` 和 `_infer_expr` 中的 `is_instance` 调用替换为 `_ast_type_id` 整数比较

**预期收益**：`_infer_statement` / `_infer_expr` 分发开销降低 **60–70%**，总推断时间降低 **15–25%**。

**风险**：低。`_ast_type_id` 在类定义时静态赋值，不依赖运行时状态。

### 3.2 `TypeSubstitution.clone()` 改为 Copy-on-Write

**现状**：[type_system.py](file:///d:/traework/light/src/type_system.py#L947-L949) 每次 `_unify_type_var` 调用 `subs.clone()` 创建新字典，`TypeSubstitution.__init__` 被调用 **617 次/100段**。

**方案**：`TypeSubstitution` 内部维护一个 `_frozen` 标记和版本号。`clone()` 返回一个"浅代理"对象，只在真正写入时才复制底层字典。

```python
class TypeSubstitution:
    def __init__(self, mapping=None, _parent=None):
        self._mapping = dict(mapping) if mapping else {}
        self._parent = _parent  # CoW 父对象
        self._dirty = set()     # 本层修改的 key

    def clone(self):
        return TypeSubstitution(_parent=self)

    def __setitem__(self, name, t):
        self._dirty.add(name)
        self._mapping[name] = t

    def __contains__(self, name):
        if name in self._dirty:
            return name in self._mapping
        if self._parent:
            return name in self._parent
        return name in self._mapping
```

**预期收益**：`TypeSubstitution` 创建开销降低 **80%**，总推断时间降低 **5–10%**。

**风险**：低。接口不变，仅内部实现优化。

### 3.3 `apply_substitution` 单例短路

**现状**：`TYPE_NUMBER`、`TYPE_STRING` 等单例类型在 `apply_substitution` 中仍走完整调用链，但实际永远返回自身。

**方案**：在 `Type.apply_substitution` 基类中增加快速路径：

```python
class Type:
    def apply_substitution(self, subs):
        if not subs:  # 空替换直接返回
            return self
        return self  # 基类默认：单例类型无需替换
```

`_SingletonType` 子类直接继承此行为，无需额外代码。

**预期收益**：`apply_substitution` 调用开销降低 **10–15%**，总推断时间降低 **3–5%**。

**风险**：极低。单例类型确实不需要替换。

---

## 4. 阶段 2：结构性优化（500–2000 段）

### 4.1 基于调用图的 HM 迭代优化

**现状**：[type_inferencer.py](file:///d:/traework/light/src/type_inferencer.py#L408) 固定两轮迭代所有段。对于无相互调用的段，第二轮是浪费。

**方案**：在 `_pre_scan_definitions` 阶段构建调用图（段名 → 被调用段名集合），用拓扑排序确定迭代次数：

- 无环子图：1 轮
- 有环子图（相互递归）：2 轮
- 深度嵌套链：按拓扑层级分组，逐层推断

**预期收益**：对于无相互调用的大模块，推断时间降低 **30–40%**（省掉一轮迭代）。

**风险**：中。需要正确处理间接调用（高阶函数）、动态分发等场景。

### 4.2 增量推断缓存

**现状**：每次 `infer()` 调用都重新推断整个模块，即使只有少数段发生变化。

**方案**：为每个段维护 `(源码哈希, 依赖段签名哈希) → 推断结果` 缓存。仅重新推断发生变化的段及其依赖链。

**改动范围**：
- `TypeInferencer` 增加 `_segment_cache: Dict[str, CacheEntry]`
- `infer()` 新增 `incremental=True` 参数

**预期收益**：IDE 场景下（单文件编辑后重新推断），时间降低 **50–80%**。

**风险**：中。需要正确处理跨段类型变量传播和缓存失效。

### 4.3 `unify` 快速路径扩展

**现状**：[type_system.py](file:///d:/traework/light/src/type_system.py#L771-L912) 已用 `_type_id` 整数分派，但 `GenericTypeInstance ↔ ListType/DictType` 的跨类型合一仍需逐字段检查。

**方案**：为常见合一模式（如 `列表[T] ~ 列表[数]`、`T ~ 数`）添加内联快速路径，跳过 `apply_substitution` + `collect_type_vars` 的完整调用。

**预期收益**：`unify` 调用开销降低 **15–20%**。

**风险**：低。增加分支但不改变逻辑。

---

## 5. 阶段 3：架构级优化（2000+ 段）

### 5.1 并行段推断

**现状**：`_hm_infer_module` 中所有段按顺序推断。

**方案**：对于调用图中无依赖关系的段，使用 `concurrent.futures` 并行推断。每个段在独立的作用域副本中推断，最后合并符号表。

**预期收益**：多核环境下推断时间降低 **40–60%**（取决于并行度）。

**风险**：高。需要确保 `TypeSymbolTable` 和 `TypeSubstitution` 的线程安全，以及 `type_cache` 的隔离。

### 5.2 符号表索引优化

**现状**：[type_system.py](file:///d:/traework/light/src/type_system.py#L1027-L1032) `TypeSymbolTable.lookup` 逐层遍历作用域栈，O(d) 复杂度（d = 嵌套深度）。

**方案**：为全局符号（类名、段名、枚举名）维护一个扁平化的 `_global_index: Dict[str, TypedSymbol]`，避免深层作用域穿透。

**预期收益**：深层嵌套模块中符号查找开销降低 **50–70%**。

**风险**：低。全局符号与局部作用域隔离，不会冲突。

### 5.3 类型表达式解析缓存

**现状**：`TypeParser.parse()` 每次重新解析类型注解字符串（如 `列表[字典[串: 数]]`）。

**方案**：为 `TypeParser` 添加 LRU 缓存（`@lru_cache(maxsize=512)`），相同类型字符串直接返回缓存结果。

**预期收益**：类型注解解析开销降低 **90%**（类型注解在模块内高度重复）。

**风险**：极低。`TypeParser.parse()` 是纯函数。

---

## 6. 实施路线图

```
v4.2.0（当前）         v4.3.0                v4.4.0                v5.0.0
    │                    │                     │                     │
    ├─ 阶段 0            ├─ 阶段 1             ├─ 阶段 2             ├─ 阶段 3
    │  <200 段           │  200–500 段         │  500–2000 段        │  2000+ 段
    │  无需优化           │                     │                     │
    │                    │  3.1 is_instance    │  4.1 调用图优化      │  5.1 并行推断
    │                    │  3.2 CoW 替换       │  4.2 增量缓存        │  5.2 符号表索引
    │                    │  3.3 单例短路       │  4.3 unify 快速路径  │  5.3 类型缓存
    │                    │                     │                     │
    └────────────────────┴─────────────────────┴─────────────────────┴──────────→
```

### 触发条件

- **阶段 1**：实测 100 段推断 >30ms，或项目规模达到 200 段
- **阶段 2**：实测 500 段推断 >100ms，或 IDE 场景下推断延迟感知明显
- **阶段 3**：实测 2000 段推断 >200ms，或项目规模达到 2000 段

### 每个阶段完成后必须执行的验证

1. 运行全量回归测试（当前 449 项）
2. 运行 `bench_type_infer.py` 更新基准数据
3. 对比 `profile_type_infer.out` 确认热点变化
4. 更新本文档的基准数据

---

## 7. 风险与注意事项

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 优化引入类型推断精度下降 | 阶段 2、3 | 保持全量回归测试，任何精度变化视为失败 |
| 并行推断的符号表竞争 | 阶段 3 | 使用不可变符号表快照 + 写时合并 |
| 增量缓存失效逻辑错误 | 阶段 2 | 为增量模式增加 `--no-incremental` 全量回退开关 |
| 过早优化浪费开发时间 | 所有阶段 | 严格按触发条件执行，不提前优化 |

---

## 9. 任务分解表：代码修改清单

### 9.1 阶段 1（v4.3.0）：低风险优化

#### 任务 1.1：`is_instance` → `_ast_type_id` 整数分派

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 1.1a | `src/ast_nodes.py` | 第 14 行后 | 新增 `AST_TYPE_ID` 常量枚举（约 100 个常量） |
| 1.1b | `src/ast_nodes.py` | 每个 AST 节点类（约 100 处） | 为每个类添加 `_ast_type_id = AST_TYPE_ID_XXX` 类属性 |
| 1.1c | `src/type_inferencer.py` | 第 53–57 行 | 删除 `is_instance()` 辅助函数 |
| 1.1d | `src/type_inferencer.py` | 第 758–803 行 | `_infer_statement` 中 14 处 `is_instance(node, 'X')` → `node._ast_type_id == AST_ID_X` |
| 1.1e | `src/type_inferencer.py` | 第 889–913 行 | `_infer_assignment` 中 2 处替换 |
| 1.1f | `src/type_inferencer.py` | 第 1095–1480 行 | `_infer_expr` 中 22 处替换 |
| 1.1g | `src/type_inferencer.py` | 第 1533 行 | `_infer_function_call` 中 1 处替换 |
| 1.1h | `src/type_checker.py` | 全文 | 同步替换 `type_checker.py` 中的 `is_instance` 调用（如有） |
| 1.1i | `tests/` | 全量回归 | 运行 `python -m pytest tests/ -x -q` 确认 449 项通过 |

**预估工作量**：2–3 小时 | **验证方式**：回归测试全绿 + `bench_type_infer.py` 耗时下降 ≥15%

#### 任务 1.2：`TypeSubstitution` Copy-on-Write

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 1.2a | `src/type_system.py` | 第 710–756 行 | 重写 `TypeSubstitution` 类 |
| 1.2b | — | `__init__` | 新增 `_parent: Optional[TypeSubstitution]` 参数，`_local: Dict[str, Type]` 本地修改 |
| 1.2c | — | `clone()` | 改为 `return TypeSubstitution(_parent=self)`，不再复制字典 |
| 1.2d | — | `__contains__` | 改为链表查找：先查 `_local`，再递归查 `_parent` |
| 1.2e | — | `__getitem__` | 同上，链表查找 |
| 1.2f | — | `__setitem__` | 写入 `_local`（CoW 触发点） |
| 1.2g | — | `bind()` | 适配新结构 |
| 1.2h | — | `items()` | 合并 `_parent` 和 `_local` 的键值对 |
| 1.2i | — | `compose()` | 适配新结构 |
| 1.2j | — | `__bool__` | 检查 `_local` 或 `_parent` 是否有内容 |
| 1.2k | — | `__repr__` | 适配新结构 |
| 1.2l | `src/type_system.py` | 第 915–949 行 | `_unify_type_var` 中的 `subs.clone()` → `subs.clone()`（接口不变，内部已优化） |
| 1.2m | `tests/` | 全量回归 | 运行 `python -m pytest tests/ -x -q` |

**预估工作量**：2–3 小时 | **验证方式**：回归测试全绿 + `bench_type_infer.py` 耗时下降 ≥5%

#### 任务 1.3：`apply_substitution` 单例短路

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 1.3a | `src/type_system.py` | 第 67–69 行 | `Type.apply_substitution()` 基类增加 `if not subs: return self` 快速路径 |
| 1.3b | `src/type_system.py` | 第 94–99 行 | 确认 `_SingletonType` 子类（`NumberType`、`StringType`、`BooleanType`、`NullType`、`AnyType`、`UnknownType`）继承正确 |
| 1.3c | `tests/` | 全量回归 | 运行 `python -m pytest tests/ -x -q` |

**预估工作量**：0.5 小时 | **验证方式**：回归测试全绿，`profile_type_infer.py` 中 `apply_substitution` 调用次数不变但单次耗时下降

---

### 9.2 阶段 2（v4.4.0）：结构性优化

#### 任务 2.1：基于调用图的 HM 迭代优化

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 2.1a | `src/type_inferencer.py` | `TypeInferencer.__init__` | 新增 `_call_graph: Dict[str, Set[str]]` 调用图字段 |
| 2.1b | `src/type_inferencer.py` | `_pre_scan_definitions` | 在第一轮扫描时，对每个段体做轻量 AST 遍历，收集段名 → 被调用段名集合 |
| 2.1c | `src/type_inferencer.py` | 新增方法 `_topo_sort_segments` | 实现拓扑排序：入度为 0 的段入队，BFS 逐层输出 |
| 2.1d | `src/type_inferencer.py` | 新增方法 `_find_scc` | 检测强连通分量（相互递归的段），标记为需要第 2 轮迭代 |
| 2.1e | `src/type_inferencer.py` | `_hm_infer_module` 第 408 行 | 将 `for _ in range(2)` 改为：先按拓扑序推断无环段（1 轮），再对 SCC 内的段做第 2 轮 |
| 2.1f | `src/type_inferencer.py` | `_infer_function_call` | 函数调用时同步更新 `_call_graph` |
| 2.1g | `tests/` | 新增 `test_call_graph.py` | 测试用例：无环模块验证 1 轮推断、有环模块验证 2 轮、混合模块验证正确性 |

**预估工作量**：4–6 小时 | **验证方式**：新增测试全绿 + `bench_type_infer.py` 中无相互调用的大模块耗时下降 ≥30%

#### 任务 2.2：增量推断缓存

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 2.2a | `src/type_inferencer.py` | 新增 `@dataclass SegmentCacheEntry` | 字段：`func_type: FunctionType`、`source_hash: str`、`dep_hashes: Dict[str, str]` |
| 2.2b | `src/type_inferencer.py` | `TypeInferencer.__init__` | 新增 `_segment_cache: Dict[str, SegmentCacheEntry]` |
| 2.2c | `src/type_inferencer.py` | `infer()` 签名 | 新增 `incremental: bool = False` 参数 |
| 2.2d | `src/type_inferencer.py` | `infer()` 方法体 | 增量模式下：计算每个段的源码哈希 + 依赖段签名哈希，命中缓存则跳过推断 |
| 2.2e | `src/type_inferencer.py` | 新增方法 `_compute_segment_hash` | 对段定义 AST 做 `hash(str(segment))` 或 `hashlib.sha256` |
| 2.2f | `src/type_inferencer.py` | 新增方法 `_invalidate_dependents` | 当段签名变化时，递归标记所有依赖方缓存失效 |
| 2.2g | `src/type_inferencer.py` | `_hm_infer_module` | 推断完成后将结果写入 `_segment_cache` |
| 2.2h | `tests/` | 新增 `test_incremental_infer.py` | 测试用例：首次推断 → 修改一个段 → 增量推断 → 验证只有被修改段及其依赖被重新推断 |

**预估工作量**：4–6 小时 | **验证方式**：新增测试全绿 + 增量推断耗时 < 全量推断的 50%

#### 任务 2.3：`unify` 快速路径扩展

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 2.3a | `src/type_system.py` | `unify()` 第 786 行前 | 新增快速路径：`t1 is t2` 直接返回 `subs`（同一对象引用） |
| 2.3b | `src/type_system.py` | `unify()` 第 800 行后 | 同 `_type_id` 分支内，新增 `TypeVar ~ 具体类型` 的内联处理（跳过 `_unify_type_var` 的 `collect_type_vars` 调用） |
| 2.3c | `src/type_system.py` | `unify()` 第 803 行后 | 列表/字典/集合合一：若 `element_type` 都是具体类型（非 TypeVar），直接 `is_subtype_of` 比较，跳过 `unify` 递归 |
| 2.3d | `src/type_system.py` | `_unify_type_var` 第 936–945 行 | 发生检查：只在 `other` 包含 TypeVar 时才调用 `collect_type_vars`，纯具体类型直接跳过 |
| 2.3e | `tests/` | 全量回归 | 运行 `python -m pytest tests/ -x -q` |

**预估工作量**：2–3 小时 | **验证方式**：回归测试全绿 + `profile_type_infer.py` 中 `unify` 累积耗时下降 ≥15%

---

### 9.3 阶段 3（v5.0.0）：架构级优化

#### 任务 3.1：并行段推断

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 3.1a | `src/type_inferencer.py` | 顶部导入 | 新增 `from concurrent.futures import ThreadPoolExecutor, as_completed` |
| 3.1b | `src/type_inferencer.py` | `TypeInferencer` | 新增 `_parallel: bool = False` 配置项和 `_max_workers: int = 4` |
| 3.1c | `src/type_inferencer.py` | 新增方法 `_infer_segment_isolated` | 在独立的作用域副本中推断单个段，返回 `(段名, func_type, errors)` |
| 3.1d | `src/type_inferencer.py` | `_hm_infer_module` | 将拓扑排序后的段按层级分组，同层无依赖段提交到 `ThreadPoolExecutor` 并行推断 |
| 3.1e | `src/type_inferencer.py` | 符号表合并 | 并行推断完成后，按拓扑序逐层合并符号表（避免竞争） |
| 3.1f | `src/type_system.py` | `TypeSymbolTable` | 新增 `snapshot()` 方法返回不可变快照，`merge(snapshot)` 合并外部快照 |
| 3.1g | `tests/` | 新增 `test_parallel_infer.py` | 测试用例：并行推断结果与串行一致、线程安全、异常处理 |

**预估工作量**：6–8 小时 | **验证方式**：新增测试全绿 + 多核环境下 `bench_type_infer.py` 耗时下降 ≥40%

#### 任务 3.2：符号表全局索引

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 3.2a | `src/type_system.py` | `TypeSymbolTable.__init__` | 新增 `_global_index: Dict[str, TypedSymbol]` 扁平索引 |
| 3.2b | `src/type_system.py` | `TypeSymbolTable.define` | 当 `symbol_type` 为 `'function'`/`'class'`/`'enum'`/`'trait'` 时，同步写入 `_global_index` |
| 3.2c | `src/type_system.py` | `TypeSymbolTable.lookup` | 优先查 `_global_index`（O(1)），未命中再回退到作用域栈遍历 |
| 3.2d | `src/type_system.py` | `TypeSymbolTable.exit_scope` | 退出作用域时清理该作用域在 `_global_index` 中注册的符号 |
| 3.2e | `tests/` | 全量回归 | 运行 `python -m pytest tests/ -x -q` |

**预估工作量**：2–3 小时 | **验证方式**：回归测试全绿 + `profile_type_infer.py` 中 `TypeSymbolTable.lookup` 累积耗时下降 ≥50%

#### 任务 3.3：类型表达式解析 LRU 缓存

| 步骤 | 文件 | 位置 | 修改内容 |
|------|------|------|---------|
| 3.3a | `src/type_system.py` | 顶部导入 | 新增 `from functools import lru_cache` |
| 3.3b | `src/type_system.py` | `TypeParser.parse` 第 1130 行 | 将核心解析逻辑提取为 `_parse_impl(expr: str) -> Type` 静态方法 |
| 3.3c | `src/type_system.py` | `TypeParser` | 新增 `@lru_cache(maxsize=512)` 的 `_parse_cached` 包装方法 |
| 3.3d | `src/type_system.py` | `TypeParser.parse` | 改为调用 `self._parse_cached(expr)` |
| 3.3e | `src/type_inferencer.py` | `TypeInferencer.__init__` | 确认 `TypeParser` 实例复用（目前已复用，无需改动） |
| 3.3f | `tests/` | 全量回归 | 运行 `python -m pytest tests/ -x -q` |

**预估工作量**：1–2 小时 | **验证方式**：回归测试全绿 + `profile_type_infer.py` 中 `TypeParser.parse` 调用次数大幅下降

---

### 9.4 任务总览

| 阶段 | 任务数 | 改动文件 | 预估工时 | 新增测试 |
|------|-------|---------|---------|---------|
| 阶段 1 | 3 项 / 25 子步骤 | `ast_nodes.py`、`type_system.py`、`type_inferencer.py`、`type_checker.py` | 4.5–6.5 小时 | 0 |
| 阶段 2 | 3 项 / 20 子步骤 | `type_inferencer.py`、`type_system.py` | 10–15 小时 | 3 个测试文件 |
| 阶段 3 | 3 项 / 18 子步骤 | `type_inferencer.py`、`type_system.py` | 9–13 小时 | 1 个测试文件 |
| **合计** | **9 项 / 63 子步骤** | **4 个文件** | **23.5–34.5 小时** | **4 个测试文件** |

---

## 8. 附录：基准测试工具

- `d:\traework\light\profile_type_infer.py` — cProfile 性能分析脚本
- `d:\traework\light\bench_type_infer.py` — 多维度基准测试脚本
- `d:\traework\light\profile_type_infer.out` — 当前基准原始数据

运行方式：
```powershell
d:\miniconda3\python d:\traework\light\profile_type_infer.py
d:\miniconda3\python d:\traework\light\bench_type_infer.py
```