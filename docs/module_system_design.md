# 光明模块系统设计文档

**版本**: v0.9.0  
**日期**: 2026-06-10  
**状态**: 设计中

---

## 一、设计目标

1. **代码组织**：支持将编译器拆分为多个模块文件
2. **命名空间**：避免不同模块间的命名冲突
3. **依赖管理**：明确模块间的依赖关系
4. **自举支持**：为光明编译器自举提供基础设施

---

## 二、语法设计

### 2.1 基本导入

```光明
# 导入整个模块
导入《模块名》。

# 导入并使用
导入《数学库》。
设 结果 为 《数学库.平方根》(16)。
```

### 2.2 选择性导入

```光明
# 从模块导入特定函数
从《数学库》导入《平方根》，《绝对值》。

# 直接使用
设 结果 为 《平方根》(16)。
```

### 2.3 别名导入

```光明
# 模块别名
导入《数学库》为数学。

# 使用别名
设 结果 为 《数学.平方根》(16)。

# 函数别名
从《数学库》导入《平方根》为开方。
```

### 2.4 导出

```光明
# 导出函数
导出《函数名》。

# 导出多个
导出《函数1》，《函数2》，《函数3》。

# 导出所有段落
导出全部。
```

### 2.5 完整示例

**文件：math_utils.light**
```光明
# 数学工具模块

导出《平方》，《立方》。

《平方》段(数)：
  返回数乘数。

《立方》段(数)：
  返回数乘数乘数。

# 内部函数，不导出
《辅助计算》段(数)：
  返回数加一。
```

**文件：main.light**
```光明
# 主程序

从《math_utils》导入《平方》，《立方》。

设 甲 为 5。
设 平方结果 为 《平方》(甲)。
设 立方结果 为 《立方》(甲)。

打印(平方结果)。
打印(立方结果)。
```

---

## 三、语义规则

### 3.1 模块查找规则

1. **当前目录优先**：先在当前目录查找 `<模块名>.light`
2. **标准库路径**：在 `DUAN_PATH` 环境变量指定的路径查找
3. **错误处理**：找不到模块时报错

### 3.2 命名空间规则

```光明
# 方式1：模块名作为前缀
导入《数学》。
设 结果 为 《数学.平方根》(16)。

# 方式2：直接使用（选择性导入）
从《数学》导入《平方根》。
设 结果 为 《平方根》(16)。
```

### 3.3 导出规则

- 默认情况下，段落不导出（模块私有）
- 使用 `导出` 语句显式导出
- `导出全部` 导出所有段落

### 3.4 循环依赖

- 检测循环依赖并报错
- 错误信息：`错误：检测到循环依赖：A → B → C → A`

---

## 四、实现架构

### 4.1 新增组件

```
src/
├── module_resolver.py    # 模块解析器
├── module_loader.py      # 模块加载器
├── symbol_table.py       # 符号表（扩展）
└── ...
```

### 4.2 编译流程

```
主模块 (main.light)
    ↓
模块解析器 (查找导入语句)
    ↓
依赖图构建
    ↓
拓扑排序
    ↓
逐个编译模块
    ↓
链接符号
    ↓
生成Python代码
```

### 4.3 核心类设计

```python
# module_resolver.py

@dataclass
class Module:
    """模块信息"""
    name: str              # 模块名
    path: str              # 文件路径
    imports: List[Import]  # 导入列表
    exports: Set[str]      # 导出符号
    symbols: Dict[str, Any] # 所有符号

@dataclass  
class Import:
    """导入信息"""
    module_name: str       # 模块名
    symbols: List[str]     # 导入的符号（空表示导入全部）
    alias: Optional[str]   # 别名

class ModuleResolver:
    """模块解析器"""
    
    def resolve(self, main_file: str) -> List[Module]:
        """解析主文件及其所有依赖"""
        
    def find_module(self, module_name: str, from_dir: str) -> str:
        """查找模块文件路径"""
        
    def detect_circular_dependency(self, graph: Dict) -> List[str]:
        """检测循环依赖"""
```

---

## 五、代码生成

### 5.1 Python导入映射

**光明代码**：
```光明
导入《数学库》。
从《工具》导入《打印》，《读取》。
```

**生成的Python代码**：
```python
# 导入模块
import 数学库

# 从模块导入
from 工具 import 打印, 读取
```

### 5.2 模块间调用

**光明代码**：
```光明
导入《数学库》。
设 结果 为 《数学库.平方根》(16)。
```

**生成的Python代码**：
```python
import 数学库

结果 = 数学库.平方根(16)
```

---

## 六、关键字

新增关键字：

| 关键字 | 用途 | 示例 |
|--------|------|------|
| `导入` | 导入模块 | `导入《数学库》。` |
| `从` | 指定来源模块 | `从《数学库》导入...` |
| `导出` | 导出符号 | `导出《平方根》。` |
| `为` | 别名 | `导入《数学库》为数学。` |

---

## 七、错误处理

### 7.1 错误类型

```python
class ModuleError(LightError):
    """模块相关错误"""

class ModuleNotFoundError(ModuleError):
    """模块未找到"""

class CircularDependencyError(ModuleError):
    """循环依赖"""

class SymbolNotFoundError(ModuleError):
    """符号未找到"""
```

### 7.2 错误信息示例

```
错误：模块未找到：'数学库'
  位置：main.light:3
  搜索路径：
    - ./数学库.light (不存在)
    - /usr/lib/light/数学库.light (不存在)

错误：检测到循环依赖
  main.light → utils.light → math.light → main.light

错误：符号未导出
  模块 '数学库' 未导出符号 '内部函数'
  位置：main.light:5
```

---

## 八、标准库

### 8.1 内置模块

| 模块名 | 功能 |
|--------|------|
| `系统` | 文件I/O、命令行参数 |
| `数学` | 数学函数 |
| `文本` | 字符串操作 |
| `列表` | 列表操作 |
| `字典` | 字典操作 |

### 8.2 示例：系统模块

```光明
# 使用系统模块
导入《系统》。

# 文件操作
设 内容 为 《系统.读取文件》("input.light")。
《系统.写入文件》("output.py", 内容)。

# 命令行参数
设 参数列表 为 《系统.参数列表》()。
```

---

## 九、实现步骤

### 步骤1：扩展词法分析器（1天）

- 添加新关键字：`导入`、`从`、`导出`、`为`
- 添加书名号《》作为模块名/符号名定界符

### 步骤2：扩展语法分析器（2天）

- 新增 `ImportStmt` AST节点
- 新增 `ExportStmt` AST节点
- 解析导入/导出语句

### 步骤3：实现模块解析器（3天）

- 实现 `ModuleResolver` 类
- 实现模块查找逻辑
- 实现循环依赖检测

### 步骤4：扩展代码生成器（2天）

- 生成Python导入语句
- 处理模块间符号引用

### 步骤5：测试（2天）

- 单模块测试
- 多模块测试
- 循环依赖测试
- 错误处理测试

---

## 十、参考

- Python模块系统
- Node.js CommonJS/ES Modules
- Rust模块系统
- 言语言(yan)模块系统设计
