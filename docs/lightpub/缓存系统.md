# 缓存系统

> 多级缓存（内存/Redis/文件），LRU/LFU/TTL淘汰策略

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 数据库 |
| 优先级 | 🔹 扩展包 |
| 实现状态 | ✅ 已实现，可直接导入 |
| 公开函数 | 9 |
| FFI 声明 | 9 |

**关键词:** 缓存, LRU, LFU, TTL, 多级, 淘汰

## 导入方式

```light
导入 缓存系统
```

或

```light
导入 标准缓存系统
```

## 函数列表

共 9 个公开函数

> 缓存系统 — lightpub 桥接模块
> 
> 基于 Python functools 库封装，函数名对齐上游 duanpub（段言时期）packages/缓存系统/源.duan。
> 
> 上游 duanpub 原始包通过 C FFI 实现多级缓存/LRU/LFU/TTL淘汰策略，
> 本桥接模块用 Python functools.lru_cache 和 dict 替代，提供等价的缓存功能。

### 新建缓存配置

**参数:** `max_size, ttl, policy`

创建缓存配置

### 新建内存缓存

**参数:** `config`

创建内存缓存实例

### 缓存获取

**参数:** `cache, key`

从缓存获取值

### 缓存设置

**参数:** `cache, key, value, ttl`

设置缓存值

### 缓存删除

**参数:** `cache, key`

删除缓存值

### 缓存是否存在

**参数:** `cache, key`

检查缓存key是否存在且未过期

### 缓存清空

**参数:** `cache`

清空缓存

### 缓存获取统计

**参数:** `cache`

获取缓存统计信息

### 缓存获取大小

**参数:** `cache`

获取缓存当前大小
