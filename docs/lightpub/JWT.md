# JWT

> JSON Web Token生成、验证、签名

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 安全加密 |
| 优先级 | 🔹 扩展包 |
| 实现状态 | 🚧 规划中 — 元数据已登记，本仓库尚未提供实现 |
| 公开函数 | 0 |
| FFI 声明 | 12 |

**关键词:** JWT, JWS, JWE, 令牌, 签名, 验证

## 导入方式

> ⚠️ 这种写法在本仓库跑不通：本仓库未提供实现（缺 stdlib.lightpub.JWT）
>
> 代码块因此标为 `text` 而非光明围栏，不参与文档示例编译扫描——免得它冒充一个能用的示例。

```text
导入 JWT
```

或

> ⚠️ 这种写法在本仓库跑不通：本仓库未提供实现（缺 stdlib.lightpub.JWT）
>
> 代码块因此标为 `text` 而非光明围栏，不参与文档示例编译扫描——免得它冒充一个能用的示例。

```text
导入 标准JWT
```
