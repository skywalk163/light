# 响应式编程

> Observable模式、事件流、操作符

## 包信息

| 属性 | 值 |
|------|-----|
| 版本 | 1.0.0 |
| 分类 | 语言特性 |
| 优先级 | 🔹 扩展包 |
| 实现状态 | 🚧 规划中 — 元数据已登记，本仓库尚未提供实现 |
| 公开函数 | 0 |
| FFI 声明 | 18 |

**关键词:** 响应式, 流, 观察者, 订阅, 事件

## 导入方式

> ⚠️ 这种写法在本仓库跑不通：本仓库未提供实现（缺 stdlib.lightpub.响应式编程）
>
> 代码块因此标为 `text` 而非光明围栏，不参与文档示例编译扫描——免得它冒充一个能用的示例。

```text
导入 响应式编程
```

或

> ⚠️ 这种写法在本仓库跑不通：本仓库未提供实现（缺 stdlib.lightpub.响应式编程）
>
> 代码块因此标为 `text` 而非光明围栏，不参与文档示例编译扫描——免得它冒充一个能用的示例。

```text
导入 标准响应式编程
```
