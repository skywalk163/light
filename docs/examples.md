# 示例代码

> **版本：** v6.0
> **最后更新：** 2026-08-07

## Hello World

```光明
打印 "你好，世界！"
```

## 变量和函数

```光明
段落 加法 接收 甲, 乙：
    返回 甲 + 乙

段落 阶乘 接收 n：
    如果 n <= 1：
        返回 1
    否则：
        返回 n * 阶乘(n - 1)

设 结果 为 加法(10, 20)
打印 结果
```

## 列表操作

```光明
设 列表 为 [1, 2, 3, 4, 5]

# 遍历列表
遍历 列表 为 项：
    打印 项

# 列表追加
列表.追加(6)

# 列表长度
打印 长度(列表)
```

## 条件判断

```光明
设 年龄 为 20

如果 年龄 > 18：
    打印 "成年人"
否则：
    打印 "未成年人"
```

## 循环

```光明
# 当循环
设 计数 为 0
当 计数 < 5：
    打印 计数
    设 计数 为 计数 + 1

# 遍历循环
遍历 i 在 1 到 10：
    如果 i % 2 == 0：
        打印 i
```

## 类和对象

```光明
类 猫：
    属性 名字

    构造 接收 名字：
        己.名字 为 名字

    段落 叫 接收：
        打印 己.名字 + " 说：喵~"

设 我的猫 为 猫("小白")
我的猫.叫()
```

## 异常处理

```光明
尝试：
    设 结果 为 10 / 0
捕获 错误：
    打印 "发生错误：" + 转字符串(错误)
最终：
    打印 "执行完毕"
```

## 模块导入

```光明
# 导入模块
导入 数学

设 半径 为 5
设 面积 为 数学.π * 半径 * 半径
打印 "圆的面积：" + 转字符串(面积)

# 从模块导入特定函数
从 数学 导入 平方根, 正弦
打印 "根号16 = " + 转字符串(平方根(16))
```

## 文件操作

```光明
从 文件系统 导入 读取, 写入, 存在

# 写入文件
写入("test.txt", "你好，光明！")

# 读取文件
如果 存在("test.txt")：
    设 内容 为 读取("test.txt")
    打印 内容
```

## 模式匹配

```光明
匹配 状态码：
    情况 200：
        打印 "成功"
    情况 404：
        打印 "未找到"
    情况 500：
        打印 "服务器错误"
    其他：
        打印 "未知状态"
```

## 完整程序示例

```光明
# 计算斐波那契数列
段落 斐波那契 接收 n：
    如果 n <= 1：
        返回 n
    否则：
        返回 斐波那契(n - 1) + 斐波那契(n - 2)

# 输出前 10 个斐波那契数
遍历 i 于 范围(0, 10)：
    打印 "F(" + 转字符串(i) + ") = " + 转字符串(斐波那契(i))
```