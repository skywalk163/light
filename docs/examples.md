# 示例代码

## Hello World

```光明
打印 "你好，世界！"
```

## 变量和函数

```光明
段落 加法 接收 甲 加 乙：
    返回 甲 加 乙

段落 阶乘 接收 n：
    如果 n 小于等于 1 那么：
        返回 1
    否则：
        返回 n 乘 阶乘(n 减 1)

结果 等于 加法 10 20
打印 结果
```

## 列表操作

```光明
列表 等于 [1, 2, 3, 4, 5]

# 遍历列表
遍历 项 在 列表：
    打印 项

# 列表追加
列表 追加 6

# 列表长度
打印 长度 列表
```

## 条件判断

```光明
年龄 等于 20

如果 年龄 大于 18 那么：
    打印 "成年人"
否则：
    打印 "未成年人"
```

## 循环

```光明
# while 循环
计数 等于 0
当 计数 小于 5：
    打印 计数
    计数 等于 计数 加 1

# for 循环
遍历 i 在 范围 0 10：
    如果 i 模 2 等于 0 那么：
        打印 i
```

## 类和对象

```光明
类 猫：
    段落 初始化 接收 名字：
        己.名字 等于 名字

    段落 叫：
        打印 己.名字 加 " 说：喵~"

我的猫 等于 猫 "小白"
我的猫.叫()
```

## 文件操作

```光明
导入 文件系统

# 写入文件
文件系统.写入 "test.txt" "你好"

# 读取文件
内容 等于 文件系统.读取 "test.txt"
打印 内容
```

## 异常处理

```光明
尝试：
    结果 等于 10 除 0
捕获 错误：
    打印 "发生错误："
    打印 错误
最终：
    打印 "完成"
```
