#     @静态方法
#     def解析代码(代码):
#         return_解析AST(代码)
#
#     @静态方法
#     def解析文件(文件路径):
#         文件=_打开文件(文件路径,'r')
#         代码=文件.读取()
#         文件.关闭()
#         return_解析AST(代码)
#
#     @静态方法
#     def生成代码(树):
#         return_AST转代码(树)
#
#     @静态方法
#     def美化输出(树):
#         return_AST转字符串(树)
#
#     @静态方法
#     def获取函数节点(树,函数名):
#                 return节点
#         return无
#
#     @静态方法
#     def获取类节点(树,类名):
#                 return节点
#         return无
#
#     @静态方法
#     def获取所有函数(树):
#
#     @静态方法
#     def获取所有类(树):
#
#     def初始化():
#
#     def替换函数名(树,旧名称,新名称):
#                 节点.名称=新名称
#                 节点.函数名=新名称
#         return树
#
#     def重命名变量(树,旧名称,新名称):
#                 节点.标识=新名称
#         return树
#
#     @静态方法
#     def生成函数(函数名,参数列表=无,函数体='跳过'):
#         参数=', '.连接(参数列表)如果参数列表否则''
#         函数体缩进='    '.函数体.分割('\n').连接('\n    ')
#
#     @静态方法
#     def生成类(类名,父类列表=无,属性字典=无,方法字典=无):
#         父类=', '.连接(父类列表)如果父类列表否则''
#         类头部='class '+类名+'('+父类+'):'如果父类否则'class '+类名+':'
#
#         类体=[]
#
#                 类体.追加('    '+属性名+' = '+字符串(属性值))
#
#                 缩进方法体='        '.方法体.分割('\n').连接('\n        ')
#                 类体.追加('    def '+方法名+'(self):\n        '+缩进方法体)
#
#             类体.追加('    跳过')
#
#         return类头部+'\n'+'\n'.连接(类体)+'\n'
#
#     @静态方法
#     def生成单例(类名):
#
#     def初始化(代码=无,文件路径=无):
#         否则如果文件路径:
#         否则:
#             抛出值错误('必须提供代码或文件路径')
#
#     def函数数量():
#
#     def类数量():
#
#     def行数量(代码):
#         return长度(代码.去除空白().分割('\n'))
#
#     def生成报告():
#         }
#
#     @静态方法
#     def查找模式(树,模式函数):
#
#     @静态方法
#     def查找空函数(树):
#         def是空函数(节点):
#                 return假
#                 return真
#                 return真
#             return假
#
#         return模式匹配器.查找模式(树,是空函数)
#
# def解析代码(代码):
#
# def生成代码(树):
#
# def分析代码(代码):
#     分析器=代码分析器(代码)
#     return分析器.生成报告()
#
# def生成函数(函数名,参数列表=无,函数体='跳过'):
#     return代码生成器.生成函数(函数名,参数列表,函数体)
#
# def生成类(类名,父类列表=无,属性字典=无,方法字典=无):
#     return代码生成器.生成类(类名,父类列表,属性字典,方法字典)
#
# 用法：
#   从《AST操作》导入《AST节点类型》。

导出 AST节点类型 AST工具 AST访问者 AST转换器 代码生成器 代码分析器。
导出 装饰器生成器 模式匹配器 解析代码 生成代码 美化AST 分析代码。
导出 生成函数 生成类 重写代码。
