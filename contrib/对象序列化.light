光明标准库 - 对象序列化模块

封装 pickle 模块，提供对象的序列化和反序列化功能。

导出：
  序列化(对象, 协议) - 序列化对象为字节
  反序列化(数据) - 反序列化字节为对象
  保存到文件(对象, 文件路径, 协议) - 保存对象到文件
  从文件加载(文件路径) - 从文件加载对象
  序列化为字符串(对象) - 序列化为Base64字符串
  从字符串反序列化(字符串) - 从Base64字符串反序列化
  深复制(对象) - 通过序列化实现深复制
  JSON序列化(对象, 缩进, 确保ASCII) - JSON序列化
  JSON反序列化(JSON字符串) - JSON反序列化
