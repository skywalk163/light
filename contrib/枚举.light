# 光明标准库 - 枚举模块
#
# 封装 enum 模块，提供枚举类型定义功能。
#
# 导出：
#   枚举类 - 枚举基类
#     所有成员() - 获取所有成员列表
#     所有名称() - 获取所有成员名称
#     所有值() - 获取所有成员值
#     从名称获取(名称) - 根据名称获取成员
#     从值获取(值) - 根据值获取成员
#     包含名称(名称) - 检查是否包含名称
#     包含值(值) - 检查是否包含值
#     成员数量() - 获取成员数量
#     到字典() - 转换为字典
#     名称 - 获取成员名称属性
#     值 - 获取成员值属性
#     下一个() - 获取下一个成员
#     上一个() - 获取上一个成员
#   标志枚举类 - 标志枚举（支持按位运算）
#   创建枚举(名称, 成员) - 动态创建枚举类
#   创建整数枚举(名称, 成员列表, 起始值) - 快速创建整数枚举
#
# 用法：
#   从《枚举》导入《枚举》。

导出 枚举 创建枚举 创建整数枚举 标志枚举。
