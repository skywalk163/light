#     名称=''
#     版本='1.0.0'
#     描述=''
#     作者=''
#
#     def初始化():
#
#     def初始化插件():
#
#     def启动():
#
#     def停止():
#
#     def销毁():
#
#     def是否已启用():
#
#     def初始化():
#
#     def添加插件路径(路径):
#
#     def加载插件(插件名):
#
#             抛出插件未找到异常('插件'+插件名+'未找到')
#
#         插件实例=插件类()
#         插件实例.初始化插件()
#         插件实例._已启用=真
#         插件实例.启动()
#
#         return插件实例
#
#     def卸载插件(插件名):
#             抛出插件未找到异常('插件'+插件名+'未找到')
#
#         插件实例.停止()
#         插件实例.销毁()
#         插件实例._已启用=假
#
#         删除self._插件字典[插件名]
#
#     def重新加载插件(插件名):
#
#     def获取插件(插件名):
#
#     def获取所有插件():
#
#     def插件列表():
#
#     def已加载(插件名):
#         return插件名在self._插件字典
#
#     def启用插件(插件名):
#             插件.启动()
#             插件._已启用=真
#
#     def禁用插件(插件名):
#             插件.停止()
#             插件._已启用=假
#
#     def注册扩展点(扩展点名,回调函数):
#
#     def触发扩展点(扩展点名,*参数,**关键字参数):
#         结果列表=[]
#                 结果=回调(*参数,**关键字参数)
#                 结果列表.追加(结果)
#         return结果列表
#
#     def加载目录(目录路径):
#         已加载插件=[]
#             return已加载插件
#
#                 插件名=文件名[:-3]
#                 尝试:
#                     已加载插件.追加(插件名)
#                 除异常:
#                     跳过
#
#         return已加载插件
#
#     def销毁所有():
#             尝试:
#             除异常:
#                 跳过
#
#     def初始化(名称='',版本='1.0.0',描述=''):
#
#     def__调用__(类):
#         return类
#
#     @静态方法
#     def从文件加载(文件路径,模块名=无):
#             抛出插件加载异常('文件不存在:'+文件路径)
#
#         模块名=模块名或_文件名(文件路径)
#
#         return_从文件加载模块(文件路径,模块名)
#
#     @静态方法
#     def从字符串加载(代码,模块名='动态模块'):
#         return_从字符串加载模块(代码,模块名)
#
#     @静态方法
#     def重新加载模块(模块名):
#         return_重新加载模块(模块名)
#
#     def初始化():
#
#     def监控模块(模块名,文件路径):
#
#     def检查更新():
#         更新列表=[]
#
#             尝试:
#                 新时间戳=_获取文件修改时间(文件路径)
#                     模块名=_文件名(文件路径)
#                     更新列表.追加(模块名)
#             除异常:
#                 跳过
#
#         return更新列表
#
#     def自动更新():
#         结果={}
#
#             尝试:
#                 动态模块加载器.重新加载模块(模块名)
#                 结果[模块名]=真
#             除异常:
#                 结果[模块名]=假
#
#         return结果
#
# def创建插件管理器():
#     return插件管理器()
#
# def创建热更新管理器():
#     return热更新管理器()
#
# def动态加载文件(文件路径,模块名=无):
#     return动态模块加载器.从文件加载(文件路径,模块名)
#
# def动态加载代码(代码,模块名='动态模块'):
#     return动态模块加载器.从字符串加载(代码,模块名)
#
# 用法：
#   从《插件系统》导入《插件异常》。

导出 插件异常 插件加载异常 插件未找到异常 插件 插件管理器 扩展点。
导出 插件装饰器 动态模块加载器 热更新管理器 创建插件管理器 创建热更新管理器 动态加载文件。
导出 动态加载代码。
