#     def初始化(名称,描述=''):
#
#     def初始化(名称,描述=''):
#
#     def添加权限(权限名):
#
#     def移除权限(权限名):
#
#     def获取权限():
#
#     def添加父角色(角色名):
#
#     def获取父角色():
#
#     def包含权限(权限名):
#         return权限名在self._权限集合
#
#     def初始化():
#
#     def创建角色(名称,描述=''):
#         新角色=角色(名称,描述)
#         return新角色
#
#     def获取角色(名称):
#
#     def删除角色(名称):
#             删除self._角色字典[名称]
#             return真
#         return假
#
#     def给角色授权(角色名,权限名):
#             角色.添加权限(权限名)
#             return真
#         return假
#
#     def撤销角色权限(角色名,权限名):
#             return角色.移除权限(权限名)
#         return假
#
#     def添加角色继承(子角色名,父角色名):
#             子角色.添加父角色(父角色名)
#             return真
#         return假
#
#     def获取角色所有权限(角色名,已访问=无):
#             已访问=集合()
#
#             return集合()
#         已访问.添加(角色名)
#
#             return集合()
#
#         权限集合=角色.获取权限()
#
#         return权限集合
#
#     def分配用户角色(用户ID,角色名):
#
#     def移除用户角色(用户ID,角色名):
#             return真
#         return假
#
#     def获取用户角色(用户ID):
#
#     def获取用户权限(用户ID):
#         权限集合=集合()
#         return权限集合
#
#     def检查用户权限(用户ID,权限名):
#         return权限名在self.获取用户权限(用户ID)
#
#     def检查用户角色(用户ID,角色名):
#         return角色名在self.获取用户角色(用户ID)
#
#     def初始化(主体,资源,操作,允许=真):
#
#     def匹配(主体,资源,操作):
#
#     def初始化():
#
#     def设置默认策略(允许):
#
#     def添加规则(主体,资源,操作,允许=真):
#
#     def移除规则(主体,资源,操作):
#                 删除self._规则列表[i]
#                 return真
#         return假
#
#     def检查权限(主体,资源,操作):
#                 return规则.允许
#
#     def清空规则():
#
# def创建RBAC管理器():
#
# def创建ACL管理器(默认允许=假):
#     管理器=ACL管理器()
#     管理器.设置默认策略(默认允许)
#     return管理器
#
# 用法：
#   从《访问控制》导入《权限》。

导出 权限 角色 RBAC管理器 ACL规则 ACL管理器 权限检查装饰器。
导出 角色检查装饰器 创建RBAC管理器 创建ACL管理器。
