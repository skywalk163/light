#     def初始化(方法名,参数,关键字参数,返回值=无,异常=无):
#
#     def初始化(目标类型=无):
#
#     def设置返回值(方法名,返回值):
#
#     def设置副作用(方法名,函数):
#
#     def设置属性(名称,值):
#
#     def设置方法存根(方法名,函数):
#
#     def获取调用记录(方法名=无):
#
#     def获取调用次数(方法名=无):
#
#     def重置调用记录():
#
#     def断言被调用(方法名=无,最少次数=1):
#             方法描述=f"'{方法名}' "如果方法名否则''
#             抛出断言错误(f"断言失败:方法{方法描述}调用次数{次数}<{最少次数}")
#
#     def断言未被调用(方法名=无):
#             方法描述=f"'{方法名}' "如果方法名否则''
#             抛出断言错误(f"断言失败:方法{方法描述}被调用了{次数}次")
#
#     def断言被调用次数(方法名=无,期望次数=1):
#             方法描述=f"'{方法名}' "如果方法名否则''
#             抛出断言错误(f"断言失败:方法{方法描述}调用次数{次数}!={期望次数}")
#
#     def断言以参数调用(方法名,*期望参数,**期望关键字参数):
#                 返回
#         抛出断言错误(f"断言失败:方法'{方法名}'未以指定参数调用")
#
#     def__获取属性__(名称):
#
#             def包装(*参数,**关键字参数):
#                 记录=Mock调用记录(名称,参数,关键字参数)
#
#                 尝试:
#                     记录.返回值=返回值
#                 除异常(e):
#                     记录.异常=e
#                     抛出e
#
#             return包装
#
#         def模拟方法(*参数,**关键字参数):
#             记录=Mock调用记录(名称,参数,关键字参数)
#
#                     返回值=返回值(*参数,**关键字参数)
#                 记录.返回值=返回值
#             否则:
#                 返回值=Mock对象()
#                 记录.返回值=返回值
#
#         return模拟方法
#
#     def初始化():
#
#     def设置方法(方法名,函数):
#
#     def设置属性(名称,值):
#
#     def__获取属性__(名称):
#
#     def初始化():
#
#     def打桩对象(模块,对象名,替代对象):
#         _设置属性(模块,对象名,替代对象)
#
#     def打桩方法(对象,方法名,替代方法):
#         _设置属性(对象,方法名,替代方法)
#
#     def打桩属性(对象,属性名,替代值):
#         _设置属性(对象,属性名,替代值)
#
#     def恢复所有():
#             _设置属性(模块,对象名,原始对象)
#
#             _设置属性(对象,方法名,原始方法)
#
# def创建Mock(目标类型=无):
#
# def创建Stub():
#
# def创建打桩器():
#     return打桩器()
#
# defMock(目标类型=无):
#
# defStub():
#
# def打桩(模块,对象名,替代对象):
#     打桩器=创建打桩器()
#     打桩器.打桩对象(模块,对象名,替代对象)
#     return打桩器
#
# def模拟返回(对象,方法名,返回值):
#         对象.设置返回值(方法名,返回值)
#     否则:
#         原始方法=_获取属性(对象,方法名)
#
#         def包装(*参数,**关键字参数):
#
#         _设置属性(对象,方法名,包装)
#         return原始方法
#
# def模拟异常(对象,方法名,异常):
#         def抛出异常(*参数,**关键字参数):
#             抛出异常
#
#         对象.设置返回值(方法名,抛出异常)
#     否则:
#         原始方法=_获取属性(对象,方法名)
#
#         def包装(*参数,**关键字参数):
#             抛出异常
#
#         _设置属性(对象,方法名,包装)
#         return原始方法
#
# def模拟副作用(对象,方法名,函数):
#         对象.设置副作用(方法名,函数)
#     否则:
#         原始方法=_获取属性(对象,方法名)
#
#         def包装(*参数,**关键字参数):
#             return原始方法(*参数,**关键字参数)
#
#         _设置属性(对象,方法名,包装)
#         return原始方法
#
# def验证调用(对象,方法名,期望次数=1):
#         对象.断言被调用次数(方法名,期望次数)
#     否则:
#         抛出值错误('对象必须是Mock对象')
#
# def验证未调用(对象,方法名=无):
#         对象.断言未被调用(方法名)
#     否则:
#         抛出值错误('对象必须是Mock对象')
#
#     def初始化(模块,对象名,替代对象):
#
#     def__进入__():
#
#     def__退出__(exc_type,exc_val,exc_tb):
#
# defMock上下文(模块,对象名,替代对象):
#
# 用法：
#   从《Mock工具》导入《Mock调用记录》。

导出 Mock调用记录 Mock对象 Stub对象 打桩器 创建Mock 创建Stub。
导出 创建打桩器 Mock Stub 打桩 模拟返回 模拟异常。
导出 模拟副作用 验证调用 验证未调用 Mock上下文管理器 Mock上下文 属性Mock。
