# 光明标准库 - 函数工具模块
#
# 封装 functools 模块，提供函数式编程工具。
#
# 导出：
#   偏函数(函数, *参数, **关键字参数) - 创建偏函数
#   偏函数方法(函数, *参数, **关键字参数) - 偏函数方法版本
#   归约(函数, 可迭代对象, 初始值) - 归约reduce
#   累积(函数, 序列, 初始值) - 累积计算
#   包装函数(被包装函数) - wraps装饰器
#   更新包装器(包装器, 被包装函数) - 更新包装器元数据
#   缓存LRU(最大条目数) - LRU缓存装饰器
#   缓存无限() - 无限缓存装饰器
#   单分派(函数) - 单分派泛函数
#   总序(类) - 总序装饰器
#   求和(可迭代对象, 初始值) - 求和
#   求积(可迭代对象, 初始值) - 求积
#   最大值(可迭代对象, 键函数, 默认值) - 最大值
#   最小值(可迭代对象, 键函数, 默认值) - 最小值
#   全部为真(可迭代对象) - 所有元素都为真
#   任一为真(可迭代对象) - 任一元素为真
#   组合(函数列表) - 函数组合
#   管道(数据, 函数列表) - 管道操作
#   柯里化(函数) - 柯里化函数
#
# 用法：
#   从《函数工具》导入《偏函数》。

导出 偏函数 偏函数方法 归约 累积 包装函数 更新包装器。
导出 缓存LRU 缓存无限 单分派 总序 求和 求积。
导出 最大值 最小值 全部为真 任一为真 组合 管道。
导出 柯里化。
