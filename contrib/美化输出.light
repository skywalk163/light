# 光明标准库 - 美化输出模块
#
# 封装 pprint 模块，提供美观的数据结构输出功能。
#
# 导出：
#   美化输出(对象, 缩进, 宽度, 深度, 紧凑) - 返回美化后的字符串
#   打印(对象, 缩进, 宽度, 深度, 紧凑) - 美化打印对象
#   美化JSON(对象, 缩进, 确保ASCII) - 返回JSON格式美化字符串
#   打印JSON(对象, 缩进, 确保ASCII) - 以JSON格式美化打印
#   格式化表格(数据, 列标题) - 将数据格式化为表格字符串
#   打印表格(数据, 列标题) - 打印表格
#   美化XML(xml字符串, 缩进) - 美化XML字符串
#
# 用法：
#   从《美化输出》导入《美化输出》。

导出 美化输出 打印 美化JSON 打印JSON 格式化表格 打印表格。
导出 美化XML。
