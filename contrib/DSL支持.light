#     def初始化():
#
#     def添加规则(类型,模式):
#
#     def分词(代码):
#         token列表=[]
#         位置=0
#         行号=1
#         列号=1
#
#             匹配=无
#
#                 结果=_正则匹配(模式,代码,位置)
#                     值=结果
#                     token列表.追加({
#                         '类型':类型,
#                         '值':值,
#                         '行号':行号,
#                         '列号':列号
#                     })
#
#                     位置+=长度(值)
#                     打破
#
#                 抛出DSL解析异常('无法识别的字符')
#
#     @静态方法
#     def创建默认词法分析器():
#         分析器=词法分析器()
#         分析器.添加规则('数字',r'\d+(\.\d+)?')
#         分析器.添加规则('字符串',r'"[^"]*"')
#         分析器.添加规则('标识符',r'[a-zA-Z_][a-zA-Z0-9_]*')
#         分析器.添加规则('运算符',r'[+\-*/=<>!&|]')
#         分析器.添加规则('括号',r'[()\[\]{}]')
#         分析器.添加规则('逗号',r',')
#         分析器.添加规则('点号',r'\.')
#         分析器.添加规则('空白',r'\s+')
#         return分析器
#
#     def初始化(类型,值=无,子节点=无):
#
#     def__表示__():
#
#     def初始化():
#
#     def分析(token列表):
#
#     def_当前token():
#         return无
#
#     def_消费token(期望类型=无):
#             抛出DSL解析异常('意外的结束')
#             抛出DSL解析异常('期望'+期望类型+',实际'+token['类型'])
#
#     def_解析表达式():
#
#     def_解析加减():
#
#             左节点=语法节点('二元运算',运算符['值'],[左节点,右节点])
#
#         return左节点
#
#     def_解析乘除():
#
#             左节点=语法节点('二元运算',运算符['值'],[左节点,右节点])
#
#         return左节点
#
#     def_解析基础表达式():
#
#             抛出DSL解析异常('意外的结束')
#
#             return语法节点('数字',浮点型(token['值']))
#
#             return语法节点('字符串',token['值'][1:-1])
#
#                 参数列表=[]
#
#                 return语法节点('函数调用',token['值'],参数列表)
#
#             return语法节点('变量',token['值'])
#
#             return表达式
#
#         抛出DSL解析异常('意外的token')
#
#     def初始化():
#
#     def设置变量(名称,值):
#
#     def获取变量(名称):
#             抛出DSL解析异常('未定义的变量:'+名称)
#
#     def注册函数(名称,函数):
#
#     def求值(ast):
#
#             运算符=ast.值
#
#                 return左值+右值
#                 return左值-右值
#                 return左值*右值
#                 return左值/右值
#
#             抛出DSL解析异常('未知的运算符:'+运算符)
#
#                 抛出DSL解析异常('未定义的函数:'+ast.值)
#
#         抛出DSL解析异常('未知的节点类型:'+ast.类型)
#
#     def执行(代码):
#         词法分析器实例=词法分析器.创建默认词法分析器()
#         token列表=词法分析器实例.分词(代码)
#
#         语法分析器实例=语法分析器()
#         ast=语法分析器实例.分析(token列表)
#
#     def初始化():
#
#     def_初始化默认函数():
#
#     def设置变量(名称,值):
#
#     def注册函数(名称,函数):
#
#     def求值(表达式):
#         解释器实例=解释器()
#             解释器实例.设置变量(名称,值)
#             解释器实例.注册函数(名称,函数)
#         return解释器实例.执行(表达式)
#
#     def初始化():
#
#     def设置变量(名称,值):
#
#     def渲染(模板,变量字典=无):
#             上下文.更新(变量字典)
#
#         def替换变量(匹配):
#             表达式=匹配.组(1).去除空白()
#             尝试:
#                 求值器=表达式求值器()
#                     求值器.设置变量(名称,值)
#                 return字符串(求值器.求值(表达式))
#             除异常:
#                 return匹配.组(0)
#
#         结果=_正则替换(r'\{\{(.+?)\}\}',替换变量,模板)
#         return结果
#
#     def初始化():
#
#     def_注册内置函数():
#
#     def设置变量(名称,值):
#
#     def注册函数(名称,函数):
#
#     def执行(代码):
#
#     def求值(表达式):
#
# def求值表达式(表达式,变量字典=无):
#     求值器=表达式求值器()
#             求值器.设置变量(名称,值)
#     return求值器.求值(表达式)
#
# def渲染模板(模板,变量字典=无):
#     引擎=模板引擎DSL()
#     return引擎.渲染(模板,变量字典)
#
# def创建DSL():
#     return简单DSL()
#
# def创建表达式求值器():
#     return表达式求值器()
#
# def创建模板引擎():
#     return模板引擎DSL()
#
# def分词(代码):
#     分析器=词法分析器.创建默认词法分析器()
#     return分析器.分词(代码)
#
# 用法：
#   从《DSL支持》导入《DSL解析异常》。

导出 DSL解析异常 词法分析器 语法节点 语法分析器 解释器 表达式求值器。
导出 模板引擎DSL 简单DSL 自定义语法 求值表达式 渲染模板 创建DSL。
导出 创建表达式求值器 创建模板引擎 分词。
