# def解析URL(URL):
#     解析结果=_解析URL结构(URL)
#         '协议':解析结果['协议'],
#         '域名':解析结果['域名'],
#         '端口':解析结果['端口'],
#         '路径':解析结果['路径'],
#         '查询参数':解析结果['查询参数'],
#         '片段':解析结果['片段']
#     }
#
# def获取域名(URL):
#     解析结果=_解析URL结构(URL)
#     return解析结果['域名']或''
#
# def获取路径(URL):
#     解析结果=_解析URL结构(URL)
#     return解析结果['路径']
#
# def获取查询参数(URL):
#     解析结果=_解析URL结构(URL)
#     参数=_解析参数字符串(解析结果['查询参数'])
#
# def获取单个参数(URL,参数名):
#     参数=获取查询参数(URL)
#     return参数.获取(参数名)
#
# def获取片段(URL):
#     解析结果=_解析URL结构(URL)
#     return解析结果['片段']
#
# def获取协议(URL):
#     解析结果=_解析URL结构(URL)
#     return解析结果['协议']
#
# def构建URL(基础URL,参数=无):
#         查询字符串=_编码参数(参数)
#             return基础URL+'&'+查询字符串
#         否则:
#             return基础URL+'?'+查询字符串
#     return基础URL
#
# def构建查询字符串(参数):
#     return_编码参数(参数)
#
# def添加参数(URL,参数名,参数值):
#     查询字符串=_编码参数({参数名:参数值})
#     否则:
#
# def更新参数(URL,参数名,参数值):
#     解析结果=_解析URL结构(URL)
#     参数=获取查询参数(URL)
#     参数[参数名]=字符串(参数值)
#     新查询=_编码参数(参数)
#
#     return_构建完整URL(
#         解析结果['协议'],
#         解析结果['主机'],
#         解析结果['路径'],
#         新查询,
#         解析结果['片段']
#     )
#
# def删除参数(URL,参数名):
#     解析结果=_解析URL结构(URL)
#     参数=获取查询参数(URL)
#         删除参数[参数名]
#     新查询=_编码参数(参数)
#
#     return_构建完整URL(
#         解析结果['协议'],
#         解析结果['主机'],
#         解析结果['路径'],
#         新查询,
#         解析结果['片段']
#     )
#
# defURL编码(字符串):
#     return_编码URL(字符串)
#
# defURL解码(字符串):
#     return_解码URL(字符串)
#
# defURL全编码(字符串):
#     return_全编码URL(字符串)
#
# defURL全解码(字符串):
#     return_全解码URL(字符串)
#
# def编码参数字典(参数):
#     return_编码参数(参数)
#
# def解码参数字符串(字符串):
#     参数=_解析参数字符串(字符串)
#
# def合并URL(基础URL,相对路径):
#     return_合并URL路径(基础URL,相对路径)
#
# def获取绝对URL(基础URL,相对路径):
#     return_合并URL路径(基础URL,相对路径)
#
# def验证URL(URL):
#     模式=r'^https?://[^\s/$.?#].[^\s]*$'
#     return_匹配正则表达式(模式,URL)
#
# def验证HTTPURL(URL):
#     解析结果=_解析URL结构(URL)
#     return解析结果['协议']在['http','https']
#
# def验证HTTPSURL(URL):
#     解析结果=_解析URL结构(URL)
#     return解析结果['协议']=='https'
#
# def获取端口(URL,默认端口=无):
#     解析结果=_解析URL结构(URL)
#         return解析结果['端口']
#
#         return默认端口
#
#     否则如果解析结果['协议']=='http':
#     否则:
#
# def是否是相对路径(URL):
#     解析结果=_解析URL结构(URL)
#     return不是解析结果['协议']且不是解析结果['主机']
#
# def是否是绝对路径(URL):
#     解析结果=_解析URL结构(URL)
#     return解析结果['协议']或解析结果['主机']
#
# def获取文件名(URL):
#     路径=获取路径(URL)
#         return路径.分割('/')[-1]
#     return路径
#
# def获取扩展名(URL):
#     文件名=获取文件名(URL)
#         return文件名.分割('.')[-1]
#
# def构建完整URL(协议,域名,路径='',参数=无,片段=无,端口=无):
#         主机=域名+':'+字符串(端口)
#     否则:
#         主机=域名
#
#     查询=_编码参数(参数)如果参数否则''
#
#     return_构建完整URL(协议,主机,路径,查询,片段或'')
#
# def标准化URL(URL):
#     解析结果=_解析URL结构(URL)
#
#     主机=解析结果['域名']
#         主机=主机.转小写()
#             主机=主机+':'+字符串(解析结果['端口'])
#
#     路径=解析结果['路径']
#         路径='/'+路径
#
#     return_构建完整URL(
#         解析结果['协议'],
#         主机或'',
#         路径,
#         解析结果['查询参数'],
#         解析结果['片段']
#     )
#
# def拼接URL路径(*路径片段):
#     路径='/'.连接([字符串(片段).去除首尾('/')对于片段在路径片段])
#         路径='/'+路径
#     return路径
#
# def提取子域名(URL):
#     域名=获取域名(URL)
#         部分=域名.分割('.')
#             return部分[0]
#     return无
#
# def提取顶级域名(URL):
#     域名=获取域名(URL)
#         部分=域名.分割('.')
#             return_连接([部分[-2],部分[-1]],'.')
#     return无
#
# def判断同域(URL1,URL2):
#     域名1=获取域名(URL1)
#     域名2=获取域名(URL2)
#     return域名1==域名2
#
# def获取URL哈希(URL):
#     return_MD5哈希(URL.编码('utf-8')).十六进制()
#
# def比较URL(URL1,URL2,忽略片段=真):
#     解析1=_解析URL结构(URL1)
#     解析2=_解析URL结构(URL2)
#
#             解析1['协议']==解析2['协议']且
#             解析1['域名']==解析2['域名']且
#             解析1['路径']==解析2['路径']且
#             解析1['查询参数']==解析2['查询参数']
#         )
#     否则:
#
# def从URL提取路径片段(URL):
#     路径=获取路径(URL)
#     片段=路径.分割('/')
#
# def从URL提取层级(URL):
#     片段=从URL提取路径片段(URL)
#     return长度(片段)
#
# def是否包含参数(URL,参数名):
#     参数=获取查询参数(URL)
#     return参数名在参数
#
# def替换协议(URL,新协议):
#     解析结果=_解析URL结构(URL)
#     return_构建完整URL(
#         新协议,
#         解析结果['主机'],
#         解析结果['路径'],
#         解析结果['查询参数'],
#         解析结果['片段']
#     )
#
# def转换为HTTPS(URL):
#     return替换协议(URL,'https')
#
# def转换为HTTP(URL):
#     return替换协议(URL,'http')
#
# 用法：
#   从《URL工具》导入《解析URL》。

导出 解析URL 获取域名 获取路径 获取查询参数 获取单个参数 获取片段。
导出 获取协议 构建URL 构建查询字符串 添加参数 更新参数 删除参数。
导出 URL编码 URL解码 URL全编码 URL全解码 编码参数字典 解码参数字符串。
导出 合并URL 获取绝对URL 验证URL 验证HTTPURL 验证HTTPSURL 获取端口。
导出 是否是相对路径 是否是绝对路径 获取文件名 获取扩展名 构建完整URL 标准化URL。
导出 拼接URL路径 提取子域名 提取顶级域名 判断同域 获取URL哈希 比较URL。
导出 从URL提取路径片段 从URL提取层级 是否包含参数 替换协议 转换为HTTPS 转换为HTTP。
