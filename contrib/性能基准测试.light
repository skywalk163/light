#     def初始化(名称,耗时,调用次数=1):
#
#     def初始化(名称,峰值内存,当前内存,分配数=0):
#
#     def初始化(名称,计时结果=无,内存结果=无):
#
# def计时(函数,*参数,**关键字参数):
#     开始时间=_获取当前时间()
#     结束时间=_获取当前时间()
#     耗时=结束时间-开始时间
#
# def多次计时(函数,次数=1000,*参数,**关键字参数):
#     开始时间=_获取当前时间()
#     结束时间=_获取当前时间()
#     耗时=结束时间-开始时间
#
# def测量内存(函数,*参数,**关键字参数):
#     _开始内存追踪()
#     当前内存,峰值内存=_获取追踪内存()
#     _停止内存追踪()
#
# def测量内存详细(函数,*参数,**关键字参数):
#     _开始内存追踪()
#     快照1=_拍摄内存快照()
#
#     快照2=_拍摄内存快照()
#     当前内存,峰值内存=_获取追踪内存()
#     _停止内存追踪()
#
#     分配数=长度(快照2)-长度(快照1)
#
# def基准测试(函数,次数=1000,测量内存=假,*参数,**关键字参数):
#     计时结果=多次计时(函数,次数,*参数,**关键字参数)
#
#     内存结果=无
#         内存结果=测量内存详细(函数,*参数,**关键字参数)
#
#     def初始化(名称=''):
#
#     def添加测试(名称,函数,次数=1000,测量内存=假):
#             '名称':名称,
#             '函数':函数,
#             '次数':次数,
#             '测量内存':测量内存
#         })
#
#     def添加对比测试(名称1,函数1,名称2,函数2,次数=1000):
#
#     def运行():
#
#             函数=测试['函数']
#             次数=测试['次数']
#             测量内存=测试['测量内存']
#
#             开始时间=_获取当前时间()
#             耗时=_获取当前时间()-开始时间
#
#             计时结果=计时结果(测试['名称'],耗时,次数)
#
#             内存结果=无
#                 _开始内存追踪()
#                 当前内存,峰值内存=_获取追踪内存()
#                 _停止内存追踪()
#                 内存结果=内存测量结果(测试['名称'],峰值内存,当前内存)
#
#     def打印报告():
#
#         打印('\n'+'='*60)
#         打印('='*60)
#
#             打印('\n测试: '+结果.名称)
#                 计时=结果.计时结果
#                 打印('  总耗时: '+字符串(计时.耗时)+'秒')
#                 打印('  调用次数: '+字符串(计时.调用次数))
#                 打印('  平均耗时: '+字符串(计时.平均耗时*1000000)+'微秒')
#                 打印('  每秒调用: '+字符串(int(1/计时.平均耗时))+'次/秒')
#
#                 内存=结果.内存结果
#                 打印('  峰值内存: '+字符串(内存.峰值内存/1024)+' KB')
#                 打印('  当前内存: '+字符串(内存.当前内存/1024)+' KB')
#
#         打印('\n'+'='*60)
#
#     def初始化():
#
#     def开始(名称):
#
#     def结束(名称):
#
#     def获取平均耗时(名称):
#         return总和(记录)/长度(记录)
#
#     def获取最大耗时(名称):
#         return最大(记录)
#
#     def获取最小耗时(名称):
#         return最小(记录)
#
#     def获取调用次数(名称):
#
#     def打印统计():
#         打印('\n'+'='*60)
#         打印('性能计数器统计')
#         打印('='*60)
#
#             平均=总和(记录)/长度(记录)如果记录否则0
#             最大=最大(记录)如果记录否则0
#             最小=最小(记录)如果记录否则0
#
#             打印('\n'+名称+':')
#             打印('  调用次数: '+字符串(长度(记录)))
#             打印('  平均耗时: '+字符串(平均*1000000)+'微秒')
#             打印('  最大耗时: '+字符串(最大*1000000)+'微秒')
#             打印('  最小耗时: '+字符串(最小*1000000)+'微秒')
#
#     def重置(名称=无):
#                 删除self._计数器[名称]
#                 删除self._开始时间[名称]
#         否则:
#
#     def初始化():
#
#     def开始监控():
#         _开始内存追踪()
#
#     def停止监控():
#         _停止内存追踪()
#
#     def拍摄快照(标签=''):
#             返回
#
#         当前内存,峰值内存=_获取追踪内存()
#             '标签':标签,
#             '时间':_获取当前时间(),
#             '当前内存':当前内存,
#             '峰值内存':峰值内存
#         })
#
#     def获取快照列表():
#
#     def打印快照():
#         打印('\n'+'='*60)
#         打印('内存监控快照')
#         打印('='*60)
#
#             打印('\n标签: '+快照['标签'])
#             打印('  当前内存: '+字符串(快照['当前内存']/1024)+' KB')
#             打印('  峰值内存: '+字符串(快照['峰值内存']/1024)+' KB')
#
#     def生成对比报告(快照1=0,快照2=-1):
#
#             '内存增长':(后快照['当前内存']-前快照['当前内存'])/1024,
#             '峰值增长':(后快照['峰值内存']-前快照['峰值内存'])/1024,
#             '时间差':后快照['时间']-前快照['时间']
#         }
#
# def计算复杂度(函数,输入大小列表,*参数):
#     结果=[]
#
#         计时结果=多次计时(函数,100,大小,*参数)
#         结果.追加({
#             '输入大小':大小,
#             '平均耗时':计时结果.平均耗时,
#             '总耗时':计时结果.耗时
#         })
#
# def对比性能(函数列表,次数=1000):
#     结果=[]
#
#         计时结果=多次计时(函数,次数)
#         结果.追加({
#             '函数名':函数.__名称__,
#             '总耗时':计时结果.耗时,
#             '平均耗时':计时结果.平均耗时,
#             '每秒调用':1/计时结果.平均耗时
#         })
#
#     return排序(结果,键=lambda x:x['平均耗时'])
#
# def性能装饰器(次数=1000):
#     def包装(函数):
#         def内部(*参数,**关键字参数):
#             开始时间=_获取当前时间()
#                 结果=函数(*参数,**关键字参数)
#             耗时=_获取当前时间()-开始时间
#
#             打印('性能测试 - '+函数.__名称__+':')
#             打印('  总耗时: '+字符串(耗时)+'秒')
#             打印('  平均耗时: '+字符串((耗时/次数)*1000000)+'微秒')
#             打印('  每秒调用: '+字符串(int(次数/耗时))+'次/秒')
#
#             return结果
#
#         return内部
#
#     return包装
#
# 用法：
#   从《性能基准测试》导入《计时结果》。

导出 计时结果 内存测量结果 基准测试结果 计时 多次计时 测量内存。
导出 测量内存详细 基准测试 基准测试套件 性能计数器 内存监控器 计算复杂度。
导出 对比性能 性能装饰器 内存使用 GC统计 打印系统性能信息。
