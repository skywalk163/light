#     def初始化(请求对象):
#
#     def获取路径():
#         返回self._路径
#
#     def获取方法():
#         返回self._方法
#
#     def获取查询参数(名称=无):
#             返回值列表[0]如果值列表否则无
#         返回{k:v[0]对于k,v在self._查询参数.项()}
#
#     def获取请求头(名称=无):
#
#     def获取正文():
#
#     def获取JSON():
#             尝试:
#             除异常:
#
#     def获取表单数据():
#         数据=_解析表单(正文)
#
#     def初始化():
#
#     def设置状态码(状态码):
#
#     def设置响应头(名称,值):
#
#     def设置正文(正文):
#
#     def设置JSON(数据):
#
#     def设置HTML(HTML):
#
#     def设置文本(文本):
#
#     def设置二进制(数据,类型='application/octet-stream'):
#
#     def重定向(URL,状态码=302):
#
#     def获取状态码():
#
#     def获取响应头():
#
#     def获取正文():
#
#     def初始化(主机='127.0.0.1',端口=8080):
#
#     def路由(路径,方法='GET'):
#         def包装(函数):
#             键=方法.转大写()+':'+路径
#             返回函数
#         返回包装
#
#     def注册路由(路径,函数,方法='GET'):
#         键=方法.转大写()+':'+路径
#
#     defGET(路径):
#
#     defPOST(路径):
#
#     defPUT(路径):
#
#     defDELETE(路径):
#
#     def添加中间件(函数):
#
#     def设置静态文件目录(目录):
#
#     def启动(阻塞=真):
#
#         否则:
#             import线程
#             t.启动()
#
#     def停止():
#
#     def_创建处理器类():
#         服务端实例=self
#
#         class请求处理器:
#             def处理GET():
#
#             def处理POST():
#
#             def处理PUT():
#
#             def处理DELETE():
#
#             def_处理请求(方法):
#                 请求=HTTP请求(self)
#                 响应=HTTP响应()
#
#                     结果=中间件(请求,响应)
#
#                 键=方法+':'+请求.获取路径()
#                 处理函数=服务端实例._路由.获取(键)
#
#                     尝试:
#                         处理函数(请求,响应)
#                     除异常(e):
#                         儿应.设置状态码(500)
#                         儿应.设置JSON({'错误':字符串(e)})
#                 否则如果服务端实例._静态文件目录:
#                     文件路径=_合并路径(服务端实例._静态文件目录,请求.获取路径())
#                     否则:
#                         儿应.设置状态码(404)
#                         儿应.设置文本('404 Not Found')
#                 否则:
#                     响应.设置状态码(404)
#                     响应.设置文本('404 Not Found')
#
#             def_发送响应(响应):
#
#             def_发送静态文件(文件路径,响应):
#                 mime类型=_获取MimeType(文件路径)
#                 内容=_读取文件(文件路径,'rb')
#                 响应.设置响应头('Content-Type',mime类型)
#                 响应.设置二进制(内容,mime类型)
#
#         return请求处理器
#
# def创建HTTP服务端(主机='127.0.0.1',端口=8080):
#
# def创建请求(请求对象):
#
# def创建响应():
#
# def返回JSON(数据):
#     响应=HTTP响应()
#     响应.设置JSON(数据)
#
# def返回HTML(HTML):
#     响应=HTTP响应()
#     儿应.设置HTML(HTML)
#
# def返回文本(文本,状态码=200):
#     响应=HTTP响应()
#     响应.设置状态码(状态码)
#     响应.设置文本(文本)
#
# def返回404(消息='404 Not Found'):
#     响应=HTTP响应()
#     响应.设置状态码(404)
#     响应.设置文本(消息)
#
# def返回500(消息='500 Internal Server Error'):
#     响应=HTTP响应()
#     响应.设置状态码(500)
#     响应.设置文本(消息)
#
# def返回重定向(URL):
#     响应=HTTP响应()
#     响应.重定向(URL)
#
# defCORS中间件(请求,响应):
#     响应.设置响应头('Access-Control-Allow-Origin','*')
#     响应.设置响应头('Access-Control-Allow-Methods','GET, POST, PUT, DELETE, OPTIONS')
#     响应.设置响应头('Access-Control-Allow-Headers','Content-Type, Authorization')
#
#         响应.设置状态码(200)
#
# defJSON中间件(请求,响应):
#         请求._JSON=请求.获取JSON()
#
# def日志中间件(请求,响应):
#     打印(请求.获取方法()+' '+请求.获取路径())
#
# def静态文件中间件(目录):
#     def中间件(请求,响应):
#         路径=请求.获取路径()
#         文件路径=_合并路径(目录,路径)
#
#             mime类型=_获取MimeType(文件路径)
#             内容=_读取文件(文件路径,'rb')
#             响应.设置响应头('Content-Type',mime类型)
#             响应.设置二进制(内容,mime类型)
#
# def基础认证中间件(用户名,密码):
#     def中间件(请求,响应):
#         认证头=请求.获取请求头('Authorization')
#
#             响应.设置状态码(401)
#             响应.设置响应头('WWW-Authenticate','Basic realm="Login Required"')
#             响应.设置文本('401 Unauthorized')
#
#         尝试:
#             编码=认证头.替换('Basic ','')
#             解码=_解码Base64(编码)
#             用户,密=解码.分割(':')
#
#             否则:
#                 响应.设置状态码(401)
#                 儿应.设置文本('401 Unauthorized')
#         除异常:
#             响应.设置状态码(401)
#             响应.设置文本('401 Unauthorized')
#
# def速率限制中间件(限制=100,时间窗口=60):
#     请求计数={}
#
#     def中间件(请求,响应):
#         import时间
#         客户端=请求.获取请求头('X-Forwarded-For')或'unknown'
#         当前时间=_获取当前时间()
#
#             请求计数[客户端]={}
#             请求计数[客户端]['次数']=0
#             请求计数[客户端]['开始时间']=当前时间
#
#         数据=请求计数[客户端]
#
#             数据['次数']=0
#             数据['开始时间']=当前时间
#
#         数据['次数']+=1
#
#             响应.设置状态码(429)
#             响应.设置文本('429 Too Many Requests')
#
# 用法：
#   从《HTTP服务端》导入《HTTP请求》。

导出 HTTP请求 HTTP响应 HTTP服务端 创建HTTP服务端 创建请求 创建响应。
导出 返回JSON 返回HTML 返回文本 返回404 返回500 返回重定向。
导出 CORS中间件 JSON中间件 日志中间件 静态文件中间件 基础认证中间件 速率限制中间件。
