#     def初始化(URL):
#
#     def连接():
#         主机=解析['主机']
#         端口=解析['端口']或80
#
#         键=_编码Base64(_哈希(''))
#         路径=解析['路径']或'/'
#
#         握手请求=_格式化握手(路径,主机,键)
#
#     def发送文本(文本):
#
#     def发送JSON(数据):
#
#     def发送二进制(数据):
#
#     def接收():
#
#         尝试:
#                 opcode,数据=帧
#                 否则如果opcode==2:
#         除异常:
#
#     def接收JSON():
#             尝试:
#             除异常:
#
#     def设置消息处理器(处理器):
#
#     def开始接收():
#         def接收循环():
#
#     def关闭():
#
#     def是否连接():
#
#     def_发送帧(数据,opcode=1):
#
#         长度=长度(数据)
#
#             头=_打包字节(0x80或opcode,长度)
#         否则如果长度<=65535:
#             头=_打包字节(0x80或opcode,126,长度)
#         否则:
#             头=_打包字节(0x80或opcode,127,长度)
#
#     def_接收帧():
#
#         opcode=头[0]且0x0f
#         长度=头[1]且0x7f
#
#         否则如果长度==127:
#
#     def初始化(主机='127.0.0.1',端口=8765):
#
#     def设置消息处理器(处理器):
#
#     def设置连接处理器(处理器):
#
#     def启动():
#
#             请求=客户端socket.接收(4096)
#
#                 接受键=_生成接受键(键)
#
#                 响应=_格式化接受响应(接受键)
#                 客户端socket.发送(响应)
#
#                 客户端=WebSocket客户端连接(客户端socket,地址)
#
#     def停止():
#             客户端.关闭()
#
#     def广播(消息):
#                 客户端.发送文本(消息)
#
#     def广播JSON(数据):
#                 客户端.发送JSON(数据)
#
#     def发送给客户端(客户端ID,消息):
#                 客户端.发送文本(消息)
#                 中断
#
#     def获取客户端列表():
#
#     def获取客户端数量():
#
#     def_处理客户端(客户端):
#             消息=客户端.接收()
#
#     def_提取WebSocketKey(请求):
#
#     def初始化(socket,地址):
#
#     def获取ID():
#
#     def获取地址():
#
#     def发送文本(文本):
#
#     def发送JSON(数据):
#
#     def发送二进制(数据):
#
#     def接收():
#         尝试:
#                 opcode,数据=帧
#                 否则如果opcode==2:
#         除异常:
#
#     def关闭():
#
#     def是否连接():
#
#     def_发送帧(数据,opcode=1):
#         长度=长度(数据)
#
#             头=_打包字节(0x80或opcode,长度)
#         否则如果长度<=65535:
#             头=_打包字节(0x80或opcode,126,长度)
#         否则:
#             头=_打包字节(0x80或opcode,127,长度)
#
#     def_接收帧():
#
#         opcode=头[0]且0x0f
#         长度=头[1]且0x7f
#
#         否则如果长度==127:
#
# def创建WebSocket客户端(URL):
#
# def创建WebSocket服务端(主机='127.0.0.1',端口=8765):
#
# defWebSocket连接(URL):
#     客户端=WebSocket客户端(URL)
#     客户端.连接()
#
# def发送WebSocket消息(URL,消息):
#     客户端=WebSocket连接(URL)
#         客户端.发送文本(消息)
#         客户端.关闭()
#
# def接收WebSocket消息(URL):
#     客户端=WebSocket连接(URL)
#         消息=客户端.接收()
#         客户端.关闭()
#
# 用法：
#   从《WebSocket支持》导入《WebSocket客户端》。

导出 WebSocket客户端 WebSocket服务端 WebSocket客户端连接 创建WebSocket客户端。
导出 创建WebSocket服务端 WebSocket连接 发送WebSocket消息 接收WebSocket消息。
