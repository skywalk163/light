#     random.seed(种子)
#
#     random.setstate(状态)
#
#     random.shuffle(序列)
#     return序列
#
#     副本=序列.copy()
#     random.shuffle(副本)
#     return副本
#
#     项列表=列表(字典.items())
#     random.shuffle(项列表)
#     return字典(项列表)
#
#     哈希值=int(hashlib.md5(str(输入值).encode()).hexdigest(),16)
#     return哈希值%(最大值+1)
#
# 用法：
#   从《随机》导入《设置随机种子》。

导出 设置随机种子 获取随机种子 设置随机状态 随机整数 随机浮点数 随机0到1。
导出 随机正态分布 随机指数分布 随机对数正态分布 随机伽马分布 随机贝塔分布 随机三角分布。
导出 随机选择 随机选择多个 随机采样 随机打乱 随机打乱副本 随机布尔。
导出 随机字符 随机字符串 随机大写字母 随机小写字母 随机数字 随机字母数字。
导出 随机十六进制 随机UUID 随机颜色十六进制 随机RGB 随机概率 随机权重选择。
导出 随机权重选择多个 随机范围采样 随机打乱字典项 基于哈希的随机。
