# 光明标准库 - 压缩模块
#
# 封装 gzip、zipfile、tarfile 等模块，提供文件压缩解压功能。
#
# 导出：
#   ZIP相关：
#     创建ZIP(源路径, 压缩文件路径, 包含目录名) - 创建ZIP压缩文件
#     解压ZIP(压缩文件路径, 目标目录) - 解压ZIP文件
#     列出ZIP内容(压缩文件路径) - 列出ZIP文件内容
#     ZIP文件存在(压缩文件路径, 文件名) - 检查ZIP中是否存在文件
#     读取ZIP文件(压缩文件路径, 文件名) - 从ZIP读取文件
#     添加到ZIP(压缩文件路径, 文件路径, 归档名) - 向ZIP添加文件
#     是ZIP文件(路径) - 检查是否为ZIP文件
#   GZIP相关：
#     GZIP压缩(源文件路径, 压缩文件路径) - GZIP压缩单个文件
#     GZIP解压(压缩文件路径, 输出路径) - GZIP解压文件
#     GZIP压缩字符串(数据, 编码) - GZIP压缩字符串
#     GZIP解压字符串(数据, 编码) - GZIP解压为字符串
#     是GZIP文件(路径) - 检查是否为GZIP文件
#   TAR相关：
#     创建TAR(源路径, 压缩文件路径, 压缩) - 创建TAR归档
#     解压TAR(压缩文件路径, 目标目录) - 解压TAR归档
#     列出TAR内容(压缩文件路径) - 列出TAR文件内容
#     是TAR文件(路径) - 检查是否为TAR文件
#   内存压缩：
#     内存压缩(数据, 级别) - 内存中压缩数据
#     内存解压(数据) - 内存中解压数据
#     压缩字符串(文本, 编码) - 压缩字符串为字节
#     解压字符串(数据, 编码) - 解压字节为字符串
#     CRC32(数据) - 计算CRC32校验值
#     Adler32(数据) - 计算Adler-32校验值
#
# 用法：
#   从《压缩》导入《创建ZIP》。

导出 创建ZIP 解压ZIP 列出ZIP内容 ZIP文件存在 读取ZIP文件 添加到ZIP。
导出 是ZIP文件 GZIP压缩 GZIP解压 GZIP压缩字符串 GZIP解压字符串 是GZIP文件。
导出 创建TAR 解压TAR 列出TAR内容 是TAR文件 内存压缩 内存解压。
导出 压缩字符串 解压字符串 CRC32 Adler32。
