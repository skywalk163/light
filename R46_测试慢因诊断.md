# light-merge 全量测试「1 小时没结束」诊断（R46 排障）

> 时间：2026-09-17｜诊断人：主 agent
> 现象：用户本地单进程跑 `pytest tests/` 超过 1 小时未结束，怀疑死循环。

## 一、结论（先说重点）

**不是死循环，是「整体太慢」。** 具体证据：

1. 全量收集到 **8101 个用例**（收集阶段约 60s，正常）。
2. 用「实时进度 + 单测超时 120s」重跑，3 分 21 秒才到 **2%（≈160/8101）**，
   全程 **0 个 TIMEOUT**，说明前面没有任何测试卡死，只是推进慢。
3. 按实测速度外推，单进程全量跑完 ≈ **2 小时**；用户那条 1h19m 的命令只跑到约 40%。
4. 该命令还被 `| tail -40` 缓冲，**全程看不到任何进度**，所以像“卡住”，其实在跑。

## 二、慢的三点根因（仓库配置里都写明了）

| # | 根因 | 仓库依据 |
|---|------|---------|
| 1 | **用例体量极大**：8101 个 | `pytest --collect-only` 实测 |
| 2 | **单进程串行**：CI 用 xdist 并行（`-n`），本地没开 | `tests/conftest.py` 注释：「37min→15min 由 xdist 并行 + PR 跳过 slow 命中」 |
| 3 | **没跳过 `slow` 标记用例**：CI 在 PR 时传 `-m "not slow"` 跳过 e2e / 大项目编译等慢测试 | `light-merge/pyproject.toml` 的 `markers.slow` + 注释「PR 触发时跳过，main 推送时全量」 |

`pyproject.toml` 现状：`addopts = "--tb=short --durations=15 --ignore=tests/archive"`，
`testpaths = ["tests"]`，`python_files = ["test_*.py", "_test_*.py"]`。

## 三、有没有「特别耗费时间 / 真挂死」的测试？

- **慢但不是挂死**（最耗时的一类，集中在后面）：`test_self_host*.py`（自举编译）、
  `test_ffi*.py` / `test_ffi_at_c.py`（真编译 C + 跑可执行）、
  `test_llvm_*.py`（LLVM codegen）、各 `test_e2e_*.py`（e2e / 大项目）。
  这类每个几秒到几十秒，叠 8101 个就很久。
- **真挂死风险最高的**（起 server / 异步 agent 循环 / 子进程 / 网络超时）：
  `test_http_server_light` `test_http_client` `test_agent_loop_light` `test_harness_*`
  `test_process_tree_light` `test_pty_light` `test_tool_parallel_light` `test_async*` 等。
- **定向排查进行中**：已对 42 个「易挂死」测试文件重跑，单测超时 60s 直接杀
  （真死循环会标 `TIMEOUT`）。结束若发现 TIMEOUT 会另行补报。

## 四、正确 / 省时的跑法（推荐）

```bash
# 推荐：并行 + 跳过慢用例（贴近 CI PR 口径，最快）
python -m pytest tests/ -n auto -m "not slow" -q

# 只跑某块（最快定位）
python -m pytest tests/ -k "lexer" -q
python -m pytest tests/unit -q            # 单元子集，几分钟

# 想看「哪条最慢」每轮自报（已默认 --durations=15）
python -m pytest tests/ -n auto --durations=20

# 怀疑某测试挂死时，加单测超时兜底（真挂死 60s 被杀并标 TIMEOUT）
python -m pytest tests/test_xxx.py --timeout=60 --timeout-method=thread -q
```

> 注意：xdist 并行下，起固定端口 server 的测试可能端口冲突；若遇此类偶发红，
> 优先用 `-p no:xdist` 单进程跑那一个文件定位，而非全量并行。

## 五、给用户的直接回答

- **死循环？** 前面 2% 进度内 0 个超时，基本排除启动即死循环；更深处的 server/异步类
  测试仍在定向排查，若有真挂死会补报。
- **特别耗时？** 有——自举编译 / FFI-C / LLVM / e2e 这几类单文件就慢，叠加 8101 用例、
  单进程、未跳 slow，全量 ≈ 2 小时，属预期而非异常。
- **建议**：日常用 `-n auto -m "not slow"`；定位单点用 `-k` 或单文件 + `--timeout`。
