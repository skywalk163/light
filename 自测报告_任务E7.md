# 自测报告 · 任务 E7「度量与门禁重建」（M20）

- 工作树：`wt-E7`，分支 `task-E7`，基线提交 `4250f1fc`（= tag `pre-merge-round7`）
- 口径声明（全文有效）：**本报告里所有基线数字都是「合并前的存量」**。开工时 A7/B7/C7/D7 四路的代码还不存在，因此三份新基线（`python_direct_baseline.json`、`spec_coverage_baseline.json`、`bootstrap_rate_baseline.json` 的 v3 字段）都只反映合并前这棵树。**五路全部合入后，必须在合并点重建一次基线**，否则四路新增的 `.light` 会立刻把「只降不升 / 只升不降」的棘轮顶红。

---

## 1. 做了什么（对照任务书六项 + 两项附加）

| 项 | 交付物 | 状态 |
| --- | --- | --- |
| ① Python 直调计数门禁 | `tools/ci/python_direct_calls.py` + `tools/ci/python_direct_baseline.json` | 完成，双向反跑通过 |
| ② 功能对标清单门禁化 | `任务书/功能对标清单_harness.json` + `tools/ci/spec_coverage.py` + `spec_coverage_baseline.json` | 完成，双向反跑通过 |
| ③ `assert_quality.py --root` 默认值修正 | `tools/ci/assert_quality.py` | 完成，修前/修后对比 + 反斜杠归一化反跑 |
| ④ 关键路径自举率子指标 | `tools/ci/critical_path_modules.json` + `bootstrap_rate.py` | 完成，基线 14/18 = 77.78% |
| ⑤ 两条新门禁接入两份 `ci.yml` | `.gitea/workflows/ci.yml`、`.github/workflows/ci.yml` | 完成，硬绿点、无 `\|\| true` |
| ⑥ 文档门禁 docstring 纠正 | `tests/unit/test_doc_examples_gate.py` | 完成，不写新硬编码数字 |
| 附加 A | `.github/workflows/ci.yml` 顶部说明「无 GitHub 远端、这棵树从未执行过、别删」 | 完成 |
| 附加 B | 两处 `GENERATED_MARK` 首行豁免复核 | 完成，见 §7 |

自测用例：`tests/unit/test_ci_gates.py` 新增 `TestPython直调计数`（6 条）、`Test关键路径自举率`（3 条）、`Test功能对标清单`（6 条）。

```
29 passed in 0.70s
0.02s call  tests/unit/test_ci_gates.py::Test关键路径自举率::test_清单删条目判红
0.02s call  tests/unit/test_ci_gates.py::TestPython直调计数::test_双向反跑
0.02s call  tests/unit/test_ci_gates.py::Test关键路径自举率::test_四种形态各归其位
0.02s call  tests/unit/test_ci_gates.py::Test功能对标清单::test_双向反跑
0.01s call  tests/unit/test_ci_gates.py::Test功能对标清单::test_行号漂移不判红
```

`tests/unit/test_doc_examples_gate.py`（只改 docstring）：`exit_code: 0`。

---

## 2. 第一天口径裁决：什么叫「Python 直调」

冻结口径（写在 `python_direct_calls.py` 的 docstring 里，是唯一权威）：

**计入**：非注释、非字符串的一行里出现 `<模块名>.<属性>`，且 `<模块名>` 来自本文件的 `导入` / `从…导入`，且不是光明模块。

**刻意不计入**（每条都写了理由，避免以后有人当 bug 来"修"）：
- 方法级调用如 `.encode("utf-8")` / `.strip()` / `句柄.write(...)` 的接收者不是模块名 —— 判定需要类型推断，静态正则做不到，硬做必然误伤光明自己的对象方法。
- `引 Python：` 块内的行 —— 那是自举率门禁已有的「逃逸」指标的地盘，两条门禁不重复计数。
- 被 `设 X 为` / `接收 X` 重新绑定过的模块名 —— 名字已经不指 Python 模块了。

**误伤排除口径**：模块名若在 `stdlib/` 下有同名 `.light`/`.py`，或与本文件同目录有同名文件，则判为光明模块，不计入。全仓排除 39 处，样本：

```
[Python直调门禁] 误伤排除（不计入指标）：光明模块（stdlib/ 或同目录同名文件） 39 处
       ~ bootstrap/compiler.light：`codegen`     → 光明模块（stdlib/ 或同目录同名文件）
       ~ bootstrap/compiler.light：`lexer`       → 光明模块（stdlib/ 或同目录同名文件）
       ~ bootstrap/parser.light：`light_ast`     → 光明模块（stdlib/ 或同目录同名文件）
       ~ examples/harness/行流.light：`JSON`      → 光明模块（stdlib/ 或同目录同名文件）
       ~ examples/harness/评测驱动.light：`JSON`  → 光明模块（stdlib/ 或同目录同名文件）
       ~ examples/modules/main.light：`math_utils` / `string_utils`
       ~ examples/weather_app/main.light：`config` / `utils`
       ~ stdlib/大模型客户端.light：`SSE`         → 光明模块（stdlib/ 或同目录同名文件）
       ~ antlrparser/self_hosted/bootstrap_test.light：`ast`/`interpreter`/`parser`/`tokenizer`
```
若没有这条排除，上面每一处都会被 `<名>.<属性>` 正则打成"Python 直调"，`JSON.解析(...)` 这种纯光明调用会被记成逃逸——这就是"无误伤"这条要求的具体含义。

**基线只降不升，但不要求 0**：要求 0 会逼出两种假降——把直调塞进 `引 Python：` 块（换成另一条门禁的存量），或把一行拆成多行。所以判据是"总数不升 + 每个文件不升"，不是"必须为 0"。

### 2.1 与总纲 §0/§2.8 的数字差异（必须记账）

总纲说 harness 有 **91 行** Python 直调。按上面冻结的口径，诚实数字是 **39 行**（`examples/harness/主程序.light` 21 + `评测驱动.light` 12 + `M2_流式对话.light` 6）。差额来自被刻意排除的方法级调用（`.encode` / `.strip` / `.write` 一类）。我没有为了对齐总纲而放宽正则——放宽会立刻误伤光明对象的方法调用，制造一批永远洗不掉的假红。**总纲 §0/§2.8 的 91 这个数字建议在合并点改成 39，并注明口径。**

---

## 3. 全仓基线 + top 10 施工地图

`python_direct_baseline.json`：**311 行 / 32 个文件**，键是 POSIX 相对路径（不带行号，行号会漂移造幻影红），`built_from_commit: 4250f1fce011`，含 `total` 与逐文件 `files` 映射。

```
[Python直调门禁] 扫描 .：32 个 .light 文件里共 311 行 Python 直调
[Python直调门禁] top 10 文件排行（后续轮次的施工地图）：
         98 行  stdlib/伪终端.light
         48 行  stdlib/代理工具集.light
         32 行  stdlib/路径护栏.light
         26 行  stdlib/进程树.light
         21 行  examples/harness/主程序.light
         12 行  examples/harness/评测驱动.light
         11 行  stdlib/流式.light
          9 行  stdlib/选择器.light
          7 行  stdlib/大模型客户端.light
          6 行  examples/harness/M2_流式对话.light
[Python直调门禁] 被直调最多的模块：os(116)、ctypes(76)、sys(39)、time(22)、re(11)、json(10)、threading(7)、codecs(5)、importlib(4)、selectors(4)
[Python直调门禁] 通过：总数 311 ≤ 基线 311，无文件上升。
rc=0
```

读法：`伪终端 / 代理工具集 / 路径护栏 / 进程树` 四个文件占 204/311 = 66%，是后续轮次"把 Python 换成光明"最大的四块地；`os(116) + ctypes(76)` 占 62%，说明缺口集中在**文件系统 API 与 FFI**，不是散落各处。

---

## 4. 两条新门禁的双向反跑

### 4.1 Python 直调门禁

**方向一 · 加一处 `os.path.join` → 红**（探针文件 `_taskE7_探针.light`）
```
[Python直调门禁] 扫描 .：33 个 .light 文件里共 312 行 Python 直调
[Python直调门禁] 红：1 个文件的 Python 直调行数高于基线（只许降不许升）：
       ! _taskE7_探针.light：基线 0 → 当前 1
           _taskE7_探针.light:3  [os] 返回 os.path.join("甲", "乙")
       为什么是红：`导入 <Python 模块>` 后直调 Python API，等于这段逻辑并没有用光明写；这条通道以前不被任何门禁计数。
[Python直调门禁] 红：总行数 312 高于基线 311。
rc=1
```

**方向二 · 删掉一条基线（连 total 一起伪造，模拟最会掩盖的改法）→ 红**
```
已从基线删掉 stdlib/JSONL.light 的 3 行，并同步扣减 total
[Python直调门禁] 红：1 个文件的 Python 直调行数高于基线（只许降不许升）：
       ! stdlib/JSONL.light：基线 0 → 当前 3
           stdlib/JSONL.light:24  [json] 设 记录 为 json.loads(内容)
           stdlib/JSONL.light:41  [json] 生成 json.loads(内容)
           stdlib/JSONL.light:51  [json] 句柄.write(json.dumps(记录, ensure_ascii=False) + "\n")
[Python直调门禁] 红：总行数 311 高于基线 308。
rc=1
```
注意这条：即使把 `total` 一起改小（伪造自洽性），逐文件棘轮仍然拦下。另有 `total != sum(files)` 的自洽性检查独立判红（用例 `test_基线自相矛盾即红`）。

**方向三 · 注释与多行字符串里的 `os.path.join` → 不判红**
```
[Python直调门禁] 扫描 .：32 个 .light 文件里共 311 行 Python 直调
[Python直调门禁] 通过：总数 311 ≤ 基线 311，无文件上升。
rc=0
```

### 4.2 功能对标清单门禁

三份变异清单 + 原样清单：
```
=== A: done 退成 none ===
[对标清单门禁] 清单 _taskE7_清单_变异A.json：共 20 条，done 7 / partial 5 / none 8
[对标清单门禁] 红：基线里已是 done 的条目退回了：
       ! #1：done → none
       为什么是红：只卡 done 总数会留一个洞——拆一条补一条总数不变，而被拆掉的那条从此无人记账。
[对标清单门禁] 红：done 条数 7 低于基线 8（完成度只升不降）。
rcA=1

=== B: 删掉证据 ===
[对标清单门禁] 红：清单本身有 1 处不合规：
       ! #1 状态是 done 但没有任何证据——「做完了」必须指得出在哪
rcB=1

=== C: 证据文件不存在 ===
[对标清单门禁] 红：清单本身有 1 处不合规：
       ! #1 证据文件不存在：examples/harness/不存在的文件.light（行号不校验，但文件必须在）
rcC=1

=== D: 原样清单（应绿） ===
[对标清单门禁] 清单 任务书/功能对标清单_harness.json：共 20 条，done 8 / partial 5 / none 7
[对标清单门禁] 完成度持平基线 8。
[对标清单门禁] 通过：清单合规、完成度未降、无条目消失、无 done 退回。
rcD=0
```
另有 `test_行号漂移不判红`：把证据里的行号改成任意值仍绿（行号一定会漂，校验它等于自造幻影红）。

---

## 5. 功能对标清单：schema 与 20 条状态分布

文件：`任务书/功能对标清单_harness.json`（放 `任务书/`，不放 `docs/`——`docs/` 受文档门禁管，一改就撞别的闸门）。

字段（每条必备）：
- `编号` 唯一整数
- `功能` 一句话，来自总纲 §4.1
- `状态` ∈ `done` / `partial` / `none`
- `证据` 数组；`done`/`partial` 至少 1 条，每条的**文件必须存在**（行号不校验）
- `本轮目标` 非空
- `备注`；`状态=none` 时必填（不许出现「没做、也没写为什么没做」）

分布 **done 8 / partial 5 / none 7**：
- done：1、3、4、9、10、11、12、18
- partial：2、6、16、17、20
- none：5、7、8、13、14、15、19

证据指向的都是真实存在的文件：`examples/harness/行流.light`、`编排.light`、`打分.light`、`评测驱动.light`、`LLM通道.light`、`stdlib/代理循环.light`、`路径护栏.light`、`代理工具集.light`、`事件总线.light`、`大模型客户端.light`、`统计.py`、`伪终端.light`、`重试.light`。#19（MCP）与 #20（PTY/landlock）写明「本轮不做，刻意记账」——这正是"能回答离自研 deepseek-harness 还差多远"的那部分：**差 7 条 none + 5 条 partial**。

---

## 6. 关键路径自举率子指标

清单：`tools/ci/critical_path_modules.json`，**18 个模块，可增不可删**。任务书给的 16 个（`JSON核心`/`文件流`/`JSONL`/`度量`/`并发`/`重试`/`大模型客户端`/`流式`/`SSE`/`选择器`/`代理循环`/`代理工具集`/`模式校验`/`事件总线`/`路径护栏`/`进程树`），我**另加了 `JSON` 与 `文件系统` 两条**：这两个 decl 0 的空壳正是"文件维度自举率说假话"的原始动机，把它们排除在子指标外，等于让新指标绕开自己的立项理由。加两条只会让门禁更紧，不会松。

判据：分子 = decl ≥ 1 **且**（无同名 `.py` **或** `.light` 首两行含 `纯光明实现` 魔数）。遮蔽判定与运行时钩子同源（`stdlib/_light_import_hook.py::_is_pure_light` 只读**前两行**），用例 `test_魔数口径与钩子同源` 与"魔数在第 3 行不算"的边界用例钉死。缺失的 `.light` **计入分母**（否则删文件就能涨指标）。

```
[自举率门禁] stdlib 文件维度自举率：22/74 = 29.73%
[自举率门禁] stdlib 同名 .py 影子数：53/74（只许降不许升）
[自举率门禁] 关键路径自举率（子指标，只升不降）：14/18 = 77.78%
       - 未计入：JSON核心 —— 无同名 .light
       - 未计入：文件流 —— 无同名 .light
       - 未计入：JSON —— decl 0（只有 导出 清单，实现在同名 .py 里）
       - 未计入：文件系统 —— decl 0（只有 导出 清单，实现在同名 .py 里）
[自举率门禁] 通过：破零守住、自举率未降、影子数未升、关键路径未降、无转手造假。
```

基线升到 `version 3`，新增 `critical_path_total 18` / `critical_path_has_impl 14` / `critical_path_rate 0.7777777777777778` / 模块清单。新判据：清单丢模块判红、子指标下降判红。`impl_with_shadow` 改成动态判定"取代 / ⚠被遮蔽"，**不含任何硬编码文件名**，D7 新增的成员会被自动认出。主指标（文件维度 29.73%）刻意保持原算法不动，保证历史可比。

---

## 7. `--root` 默认值修正 + 反斜杠归一化反跑

`tools/ci/assert_quality.py:284-285` 的 `--root` 默认是 `tests`，而 CI 传的是 `.`。忘带参数时的实测幻影：

```
（修前，不带 --root）
[假测试门禁] 扫描 tests：命中 424 条违规
[假测试门禁] 新增违规 424 条（视为回归，闸门拦下）：
       ! _test_hm_inference.py:104  [非空断言式] assert subs is not None
       ...（424 条全打成"新增"）
rc=1
```
根因：基线键是相对 `.` 的路径，用 `tests` 当根扫出来的键对不上，全量被判成新增。默认值改成 `.` 后：

```
（修后，不带 --root）
[假测试门禁] 扫描 .：命中 469 条违规
[假测试门禁] 无新增违规。
[假测试门禁] 通过：存量冻结、无新增。
```
与显式 `--root .` 的输出用 `fc /b` 比对：**找不到差异**（字节一致）。

POSIX 归一化复核（`:151-153` / `:229` / `:279`）：把基线键全部改成反斜杠形态后重跑，期望 rc=0：
```
样例键:antlrparser\self_hosted\test_tokenizer.py
[假测试门禁] 扫描 .：命中 469 条违规
[假测试门禁] 无新增违规。
[假测试门禁] 通过：存量冻结、无新增。
rc=0
```
归一化有效（这正是 gitea run 71「本机绿 CI 红」的病根，不能再犯）。

期间踩过一个自己的坑：首次反跑得到"469 条新增"的假红，是 `python -c` 里的 cmd 转义把 `a\b.py` 写成了 `a\\b.py`，`_posix` 把它映射成 `a//b`。改用 `chr(92)` 构造后结果才是上面的 rc=0——**反跑本身也会说假话，探针要先自证。**

`GENERATED_MARK` 首行豁免复核（附加 B）：豁免只对 `.py` 生效且只看**首行**，`test_标记不在首行则照扫` / `test_编译器生成的py整份豁免` 两条用例仍绿。B7 的机读清单是 `.json`、D7 新增的是 `.light`，两者都不在 `.py` 通道上，**不可能误命中豁免**。

---

## 8. CI 接入与耗时

两份 `ci.yml` 各加两步，位置在**自举率门禁之后、时间预算闸门之前**（门禁区，不与测试并发），无 `|| true`，是硬绿点：
```yaml
- name: Python 直调计数门禁（口径见脚本 docstring·只降不升）
  run: python tools/ci/python_direct_calls.py --root .
- name: 功能对标清单门禁（完成度只升不降）
  run: python tools/ci/spec_coverage.py --root .
```
`.github/` 版本两步都带 `if: runner.os == 'Linux'`，并在文件顶部加了说明：本项目没有 GitHub 远端，**这棵树从未执行过一次**，跨平台门禁只存在于此因而从未真跑，但不要删。

耗时（两条都是纯静态扫描，不 import、不编译 `.light`）：
- 直调门禁：首版 **46.61s** → 优化后 **9.35s**（与现存自举率门禁 9.09s 同量级）
- 清单门禁：**0.19s**
- 新增总量约 **+10s**：第六轮 CI 全程 671s → 约 **681s**，预算 1200s，余量充足。

46.61s 违反"必须秒级"，根因是对全仓每个 `.light` 都做字符级剥注释/剥字符串。改成两遍扫描：第一遍廉价正则收 ASCII 模块候选，无候选直接 return；只有真的 `导入` 了 ASCII 模块的文件才进第二遍做字符级剥离，外加按目录缓存兄弟模块名。优化后重跑证明**数字一字未变**（32 文件 / 311 行，模块直方图一致），29 条用例仍全绿。

另修一个不可复现性：模块直方图曾在两次运行间给出 `os 116` 与 `os 111`——归因是遍历 `set` 归属首个匹配模块，受 `PYTHONHASHSEED` 影响。改成 `sorted` 的 `[(名, 正则)]` 列表，并在代码里写明"排序 = 可复现"。

---

## 9. 文档门禁 docstring 纠正

`tests/unit/test_doc_examples_gate.py:19-28` 原来抄了一份陈旧快照（`ROT 238 / ANNOTATED 18 / … 共 305`），而真实基线是 19 条、ROT 0。改法：保留各类**含义**说明，删掉所有条数，写明

> 各类的条数不在这里复述——`tests/unit/doc_examples_baseline.json` 是唯一真值。

不写新数字是有意的：再写一遍就是再造一个会腐烂的第二真值源。表格放在多行字符串里（`_prose_lines` 只豁免注释与多行字符串，**不豁免单行字符串**）。

跨平台基线 `tests/ci_baseline_failures_crossplat.txt` 追加一条 E7 复述：这份基线刻意为空，**任何人（包括门禁自己的维护者）都不许往里加条目**——加一条就等于把"整批没跑起来"冻结成正常；并记下它至今没被任何真实运行读过。

---

## 10. 边界与未验证项（诚实清单）

- 三份新基线是**合并前存量**，合并点必须重建一次（见文首口径声明）。
- 总纲 §0/§2.8 的「91 行」与冻结口径下的 39 行不一致，建议在合并点改总纲，不建议放宽正则。
- 方法级 Python 调用（`.encode` / `.write` 一类）**不在**直调指标内，所以"直调 = 0"不等于"零 Python"。这是刻意的口径边界，不是漏洞。
- 功能对标清单的证据只校验"文件存在"，不校验"证据确实证明了该功能"——后者无法静态判定，靠评审。
- 两条新门禁只在本机（Windows / PowerShell 5.1）与自测用例里跑过；**gitea 上的真实执行要等合并点推送后才能验证**。`.github/` 那份仍然从未执行。
- 所有权边界内未越界：只改了 `tools/ci/**`、两份 `ci.yml`、`tests/unit/test_ci_gates.py`、`tests/unit/test_doc_examples_gate.py`（仅 docstring）、`tests/ci_baseline_failures_crossplat.txt`、`.gitignore`，新增 `任务书/功能对标清单_harness.json`。`src/`、`stdlib/`、`examples/`、`cli/light.py` 与其它测试**一行未动**——门禁只改判据，绝不改被判的代码来换绿。
- 新增的两份非 `*_baseline.json` 清单已加 `.gitignore` 的 `!` 例外并用 `git check-ignore -v` 验证；三份 `*_baseline.json` 已被既有的 `!tools/ci/*_baseline.json` 覆盖。
