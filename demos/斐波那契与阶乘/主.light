# 光明示范项目 — 斐波那契与阶乘
# 展示：段落定义、递归、循环、条件判断

段落 斐波那契 接收 n：
    如果 n 等于 0：
        返回 0
    如果 n 等于 1：
        返回 1
    返回 斐波那契(n减1) 加 斐波那契(n减2)

段落 阶乘 接收 n：
    如果 n 小于等于 1：
        返回 1
    返回 n 乘 阶乘(n减1)

段落 累加 接收 n：
    设 结果 为 0
    设 i 为 1
    当 i 小于等于 n：
        设 结果 为 结果 加 i
        设 i 为 i 加 1
    返回 结果

段落 主 接收：
    打印("=== 光明示范项目 ===")
    打印("")
    打印("--- 斐波那契数列 ---")
    设 k 为 0
    当 k 小于 10：
        打印("F(" 加 转字符串(k) 加 ")=" 加 转字符串(斐波那契(k)))
        设 k 为 k 加 1
    打印("")
    打印("--- 阶乘 ---")
    打印("5!=" 加 转字符串(阶乘(5)))
    打印("10!=" 加 转字符串(阶乘(10)))
    打印("")
    打印("--- 累加 ---")
    打印("1+2+...+100=" 加 转字符串(累加(100)))
    打印("")
    打印("=== 完成 ===")

主()
