# 纯光明实现 —— 中文数字转换模块（取代同名 .py 壳）
#
# 提供：中文转阿拉伯数字 / 阿拉伯数字转中文 / 中文转浮点数 / 数字转大写金额。
# 算法逐行对齐 stdlib/中文数字转换.py（ChineseNumberConverter 类方法），
# 含其进位与零位规则（如「二万零三百」→2000300、「一百」→「一百零」等）。
# 光明无正则、且 O0 下字典查找不可靠，全部解析用「字符串包含 + 逐字符扫描 +
# 码位/下标判断」实现，零字典、零导入、零引 Python。
# 注意：与 .py 一致，本模块不支持「亿」（单位只有 十百千万，遇「亿」抛 值错误）。

导出 中文转阿拉伯数字 阿拉伯数字转中文 中文转浮点数 数字转大写金额。

# 中文数字串（下标即数值：零=0 … 九=9，两=2 单独判断）
设 中文数字串 为 "零一二三四五六七八九两"
# 阿拉伯数字转中文 的输出字序（与 小写串 一一对应，用于转大写金额）
设 小写串 为 "零一二三四五六七八九十百千万"
设 大写串 为 "零壹贰叁肆伍陆柒捌玖拾佰仟万"

# ── 取中文数字的值：数字表含 零一二三四五六七八九两；非数字返回 -1 ──
段落 取数字值 接收 字符:
  设 i 为 0
  当 i < 长(中文数字串):
    如果 中文数字串[i] == 字符:
      如果 i == 10:
        返回 2
      返回 i
    设 i 为 i 加上 1
  返回 -1

# ── 小写字在大写串中的下标（用于金额逐字转大写）；找不到返回 -1 ──
段落 小写索引 接收 字符:
  设 i 为 0
  当 i < 长(小写串):
    如果 小写串[i] == 字符:
      返回 i
    设 i 为 i 加上 1
  返回 -1

# ── 取单位的值：十百千万；非单位返回 0 ──
段落 取单位值 接收 字符:
  如果 字符 == "十":
    返回 10
  如果 字符 == "百":
    返回 100
  如果 字符 == "千":
    返回 1000
  如果 字符 == "万":
    返回 10000
  返回 0

# ── 中文转阿拉伯数字（对齐 .py chinese_to_arabic）──
段落 中文转阿拉伯数字 接收 中文串:
  如果 中文串 == "":
    抛出 值错误("输入不能为空")
  设 文本 为 去除空格(中文串)
  # 单个数字直接返回（如「零」→0、「两」→2）
  如果 长(文本) == 1:
    设 单值 为 取数字值(文本)
    如果 单值 >= 0:
      返回 单值
  # 去空格后为空：与 .py 一致返回 0（循环不执行，result += current(0)）
  如果 文本 == "":
    返回 0
  设 结果 为 0
  设 当前 为 0
  设 上次单位 为 1
  # 处理「零」开头（.py：去掉首字后继续）
  如果 文本[0] == "零":
    设 文本 为 截取(文本, 1, 长(文本) 减 1)
    如果 文本 == "":
      返回 0
  # 处理「十」开头（如「十二」=12）
  如果 文本[0] == "十":
    设 结果 为 10
    设 文本 为 截取(文本, 1, 长(文本) 减 1)
    如果 文本 == "":
      返回 结果
  设 i 为 0
  当 i < 长(文本):
    设 字符 为 文本[i]
    设 位值 为 取数字值(字符)
    如果 位值 >= 0:
      设 当前 为 位值
    否则:
      设 单位 为 取单位值(字符)
      如果 单位 == 0:
        抛出 值错误("无效的中文数字字符")
      如果 当前 == 0:
        设 当前 为 1
      如果 单位 >= 10000:
        设 结果 为 (结果 加上 当前) 乘以 单位
        设 当前 为 0
      否则:
        如果 单位 > 上次单位:
          设 结果 为 (结果 加上 当前) 乘以 单位
          设 当前 为 0
        否则:
          设 结果 为 结果 加上 (当前 乘以 单位)
          设 当前 为 0
        设 上次单位 为 单位
    设 i 为 i 加上 1
  设 结果 为 结果 加上 当前
  返回 结果

# ── 万以下转换（对齐 .py _convert_under_10000，含零位插入规则）──
段落 万以下转换 接收 数字:
  如果 数字 == 0:
    返回 "零"
  如果 数字 < 10:
    返回 中文数字串[数字]
  设 结果 为 ""
  设 i 为 0
  设有零 为 假
  当 数字 > 0:
    设 位 为 取模(数字, 10)
    如果 位 == 0:
      如果有零 == 假:
        如果 i > 0:
          设 结果 为 "零" 加上 结果
          设有零 为 真
    否则:
      设有零 为 假
      设 部分 为 中文数字串[位]
      如果 i == 1:
        设 部分 为 部分 加上 "十"
      否则:
        如果 i == 2:
          设 部分 为 部分 加上 "百"
        否则:
          如果 i == 3:
            设 部分 为 部分 加上 "千"
      设 结果 为 部分 加上 结果
    设 数字 为 整除(数字, 10)
    设 i 为 i 加上 1
  返回 结果

# ── 阿拉伯数字转中文（对齐 .py arabic_to_chinese）──
段落 阿拉伯数字转中文 接收 数字:
  如果 类型(数字) == "int":
    设 是否整数 为 真
  否则:
    设 是否整数 为 假
  如果 是否整数 == 假:
    抛出 值错误("输入必须是整数")
  如果 数字 < 0:
    抛出 值错误("不支持负数转换")
  如果 数字 > 99999999:
    抛出 值错误("数字超出支持范围")
  如果 数字 == 0:
    返回 "零"
  设 结果 为 ""
  设 万位 为 整除(数字, 10000)
  设 剩余 为 数字
  如果 万位 > 0:
    设 结果 为 结果 加上 万以下转换(万位) 加上 "万"
    设 剩余 为 取模(数字, 10000)
    如果 剩余 < 1000:
      如果 剩余 > 0:
        设 结果 为 结果 加上 "零"
  如果 剩余 > 0:
    设 结果 为 结果 加上 万以下转换(剩余)
  返回 结果

# ── 中文转浮点数（对齐 .py chinese_to_arabic_float）──
段落 中文转浮点数 接收 中文串:
  如果 中文串 == "":
    抛出 值错误("输入不能为空")
  设 文本 为 去除空格(中文串)
  设 点数 为 0
  设 j 为 0
  当 j < 长(文本):
    如果 文本[j] == "点":
      设 点数 为 点数 加上 1
    设 j 为 j 加上 1
  # 无小数点：当作整数转 float
  如果 点数 == 0:
    返回 浮点数(中文转阿拉伯数字(文本))
  # 小数点必须恰好一个（.py split 后 len != 2 抛错）
  如果 点数 != 1:
    抛出 值错误("无效的中文数字格式")
  设 点位置 为 0
  设 j 为 0
  当 j < 长(文本):
    如果 文本[j] == "点":
      设 点位置 为 j
      设 j 为 长(文本)
    否则:
      设 j 为 j 加上 1
  设 整数部分串 为 截取(文本, 0, 点位置)
  设 小数部分串 为 截取(文本, 点位置 加上 1, 长(文本) 减 点位置 减 1)
  设 整数部分 为 0
  如果 整数部分串 != "":
    设 整数部分 为 中文转阿拉伯数字(整数部分串)
  设 小数部分 为 0.0
  设 k 为 0
  当 k < 长(小数部分串):
    设 字符 为 小数部分串[k]
    设 位值 为 取数字值(字符)
    如果 位值 >= 0:
      设 小数部分 为 小数部分 加上 (浮点数(位值) 乘以 _light_builtin.幂(10.0, 0 - (k 加上 1)))
    否则:
      抛出 值错误("无效的中文数字字符")
    设 k 为 k 加上 1
  返回 浮点数(整数部分) 加上 小数部分

# ── 四舍五入到分（对齐 Python round(x, 2) 的常规用例）──
段落 四舍五入到分 接收 x:
  设 放大 为 x 乘以 100.0
  设 取整 为 整数(四舍五入(放大))
  返回 浮点数(取整) 除以 100.0

# ── 数字转大写金额（对齐 .py arabic_to_chinese_currency）──
段落 数字转大写金额 接收 金额:
  如果 金额 < 0:
    抛出 值错误("不支持负数金额")
  如果 金额 > 99999999.99:
    抛出 值错误("金额超出支持范围")
  设 取整金额 为 四舍五入到分(金额)
  设 整数部分 为 整数(取整金额)
  设 小数部分 为 四舍五入到分(取整金额 减去 浮点数(整数部分))
  如果 整数部分 == 0:
    如果 小数部分 == 0.0:
      返回 "零圆整"
  设 结果 为 ""
  如果 整数部分 > 0:
    设 中文整数 为 阿拉伯数字转中文(整数部分)
    设 k 为 0
    当 k < 长(中文整数):
      设 字符 为 中文整数[k]
      设 下标 为 小写索引(字符)
      如果 下标 >= 0:
        设 结果 为 结果 加上 大写串[下标]
      否则:
        设 结果 为 结果 加上 字符
      设 k 为 k 加上 1
    设 结果 为 结果 加上 "圆"
  否则:
    设 结果 为 "零圆"
  如果 小数部分 == 0.0:
    设 结果 为 结果 加上 "整"
  否则:
    设 角 为 取模(整数(小数部分 乘以 10.0), 10)
    设 分 为 取模(整数(四舍五入(小数部分 乘以 100.0)), 10)
    如果 角 > 0:
      设 结果 为 结果 加上 大写串[角] 加上 "角"
    否则:
      如果 整数部分 > 0:
        设 结果 为 结果 加上 "零"
    如果 分 > 0:
      设 结果 为 结果 加上 大写串[分] 加上 "分"
  返回 结果
