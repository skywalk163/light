导入 sys。
导入 os。
导入 subprocess。
导入 threading。
导入 signal。
导入 time。
导入 tempfile。

# ============================================================
# 进程树（纯光明实现）
#
# 跨平台安全执行外部命令，并支持：
#  - 分离进程组/进程树（POSIX setsid 建新会话组；Windows CREATE_NEW_PROCESS_GROUP
#    =0x200），超时/杀树时能杀掉整棵树（含孙子进程）而不只杀直接子进程
#  - 信号升级链：先优雅 SIGTERM / taskkill /T /PID → 宽限期后强制 SIGKILL / taskkill /F
#  - 有界输出：内存只保留尾部 N 字节（默认 64KB），超出部分写 spill 文件（O_EXCL 防
#    symlink 攻击），并在截断处插入明确标记
#  - 总超时 + 空闲超时（多久无新输出算卡死），超时走杀树并返回"超时"结果
#  - 安全：命令只走参数数组（shell 默认关），环境变量白名单/黑名单可控
#
# 注意：
#  - Windows 的 taskkill /T 按进程树杀；POSIX 的 os.killpg 按进程组杀。
#  - POSIX 分支（setsid 新建会话组、SIGTERM→SIGKILL、信号语义）仅从原理推导与
#    Windows 侧等价设计，未在任何 POSIX 机器实测。
# ============================================================

类 执行结果:
  属性 退出码 等于 0。
  属性 标准输出 等于 ""。
  属性 标准错误 等于 ""。
  属性 溢出文件 等于 空。
  属性 是否超时 等于 假。
  属性 耗时毫秒 等于 0。

# ---- 有界输出采集器：维持尾部窗口 + spill ----
类 流采集器:
  段落 初始化 接收 界限, 目录:
    己.尾部 为 b""。
    己.溢出路径 为 空。
    己.溢出句柄 为 空。
    己.溢出计数 为 0。
    己.总字节 为 0。
    己.界限 为 界限。
    己.目录 为 目录。

  段落 喂入 接收 块:
    # 若字节块为空直接返回
    如果 长度(块) 等于 0:
      返回。
    结束。
    己.总字节 为 己.总字节 加上 长度(块)。
    设 合 为 己.尾部 + 块。
    设 现界限 为 己.界限。
    如果 长度(合) 大于 现界限:
      设 保留数 为 现界限。
      设 起始 为 长度(合) 减去 保留数。
      设 溢出块 为 合[0:起始]。
      己.尾部 为 合[起始:长度(合)]。
      如果 己.溢出句柄 等于 空:
        己.溢出句柄 为 己.新建溢出(溢出块)。
      否则:
        os.write(己.溢出句柄, 溢出块)。
      结束。
    否则:
      己.尾部 为 合。
    结束。

  段落 新建溢出 接收 块:
    # 用 O_EXCL 创建，防 symlink 攻击；撞名则换名重试（不许自动覆盖）
    己.溢出计数 为 己.溢出计数 加上 1。
    设 标志 为 os.O_WRONLY 加上 os.O_CREAT 加上 os.O_EXCL。
    设 时刻 为 str(int(时间戳() * 1000000))。
    当 真:
      设 名 为 "_taskD_spill_" + str(己.溢出计数) + "_" + 时刻 + ".bin"。
      设 路径 为 os.path.join(己.目录, 名)。
      尝试:
        设 fd 为 os.open(路径, 标志, 384)。
        os.write(fd, 块)。
        己.溢出路径 为 路径。
        返回 fd。
      捕获 错误:
        己.溢出计数 为 己.溢出计数 加上 1。
      结束。
    结束。

  段落 收尾:
    # 关闭 spill 文件句柄（若有）
    如果 己.溢出句柄 不等于 空:
      尝试:
        os.close(己.溢出句柄)。
      捕获 错误:
      结束。
      己.溢出句柄 为 空。
    结束。

  # 返回解码后的尾部文本；溢出时在开头插入省略标记；处理 UTF-8 多字节边界
  段落 文本:
    设 原始 为 己.尾部。
    设 内容 为 ""。
    尝试:
      内容 为 原始.decode("utf-8")。
    捕获 错误:
      # 尾部可能从多字节字符中间截断：从头逐个字节丢弃直到可完整解码
      设 k 为 0。
      当 真:
        尝试:
          内容 为 原始[k:长度(原始)].decode("utf-8")。
          跳出。
        捕获 错误:
          k 为 k 加上 1。
          如果 k 大于 3:
            内容 为 原始.decode("utf-8", "replace")。
            跳出。
          结束。
        结束。
      结束。
    结束。
    如果 己.溢出路径 不等于 空:
      设 已省略 为 己.总字节 减去 长度(己.尾部)。
      设 标记 为 "...[已省略 " + str(已省略) + " 字节，完整输出见 " + 己.溢出路径 + "]..."。
      内容 为 标记 + 内容。
    结束。
    返回 内容。

类 进程树:
  段落 初始化 接收 命令数组, 配置:
    己.命令 为 命令数组。
    己.配置 为 配置。
    己.进程对象 为 空。
    己.读线程 为 []。
    己.out采集 为 空。
    己.err采集 为 空。
    己.开始时刻 为 时间戳() * 1000。
    己.最后活动 为 己.开始时刻。
    己.开动故障 为 空。
    己.结果 为 空。
    己.已杀 为 假。
    己.界限 为 己.配置.get("限尾字节", 65536)。
    己.目录 为 己.配置.get("溢出目录", tempfile.gettempdir())。

  段落 启动:
    设 环境 为 己.构建环境(己.配置)。
    设 工作目录 为 己.配置.get("工作目录", 空)。
    设 out 为 流采集器(己.界限, 己.目录)。
    设 err 为 流采集器(己.界限, 己.目录)。
    己.out采集 为 out。
    己.err采集 为 err。
    尝试:
      如果 sys.platform 等于 "win32":
        # CREATE_NEW_PROCESS_GROUP = 0x200 = 512
        己.进程对象 为 subprocess.Popen(己.命令, -1, 空, 空, subprocess.PIPE, subprocess.PIPE, 空, True, 假, 工作目录, 环境, 假, 空, 512)。
      否则:
        # POSIX：preexec_fn=os.setsid 建立新会话（含独立进程组，组长即 pid）
        己.进程对象 为 subprocess.Popen(己.命令, -1, 空, 空, subprocess.PIPE, subprocess.PIPE, os.setsid, True, 假, 工作目录, 环境)。
      结束。
    捕获 错误:
      己.开动故障 为 错误。
      返回 假。
    结束。
    设 t1 为 threading.Thread(空, 己.读流, 空, [己.进程对象.stdout, out])。
    设 t2 为 threading.Thread(空, 己.读流, 空, [己.进程对象.stderr, err])。
    t1.daemon 为 True。
    t2.daemon 为 True。
    己.读线程 为 [t1, t2]。
    t1.start()。
    t2.start()。
    返回 真。

  # 读取线程体：把流喂给采集器，并更新"最后活动"
  段落 读流 接收 流, 采集器:
    当 真:
      设 块 为 流.read(8192)。
      如果 块 等于 b"":
        跳出。
      结束。
      采集器.喂入(块)。
      己.最后活动 为 时间戳() * 1000。
    结束。
    采集器.收尾()。

  段落 是否存活:
    如果 己.进程对象 等于 空:
      返回 假。
    结束。
    返回 己.进程对象.poll() 等于 空。

  # 等待结束或超时；超时后按需组装结果
  段落 等待 接收 超时毫秒:
    如果 己.开动故障 不等于 空:
      设 结果 为 执行结果()。
      结果.退出码 为 -1。
      结果.标准错误 为 str(己.开动故障)。
      结果.是否超时 为 假。
      己.结果 为 结果。
      返回 结果。
    结束。
    设 截止 为 己.开始时刻 加上 超时毫秒。
    设 空闲限 为 己.配置.get("空闲超时毫秒", 0)。
    设 宽限 为 己.配置.get("宽限期毫秒", 5000)。
    设 超时发生 为 假。
    当 真:
      设 码 为 己.进程对象.poll()。
      如果 码 不等于 空:
        跳出。
      结束。
      设 现在 为 时间戳() * 1000。
      如果 现在 大于 截止:
        超时发生 为 真。
        己.已杀 为 真。
        己.杀树(宽限)。
        跳出。
      结束。
      如果 空闲限 大于 0:
        如果 现在 大于 己.最后活动 加上 空闲限:
          超时发生 为 真。
          己.已杀 为 真。
          己.杀树(宽限)。
          跳出。
        结束。
      结束。
      time.sleep(0.03)。
    结束。
    遍历 t 于 己.读线程:
      t.join()。
    结束。
    设 结果 为 己.汇总(超时发生)。
    己.结果 为 结果。
    返回 结果。

  段落 汇总 接收 超时发生:
    设 结果 为 执行结果()。
    设 码 为 己.进程对象.poll()。
    如果 码 等于 空:
      码 为 -1。
    结束。
    结果.退出码 为 码。
    结果.是否超时 为 超时发生。
    如果 己.out采集 不等于 空:
      结果.标准输出 为 己.out采集.文本()。
      结果.溢出文件 为 己.out采集.溢出路径。
    结束。
    如果 己.err采集 不等于 空:
      结果.标准错误 为 己.err采集.文本()。
      如果 己.err采集.溢出路径 不等于 空:
        如果 结果.溢出文件 等于 空:
          结果.溢出文件 为 己.err采集.溢出路径。
        结束。
      结束。
    结束。
    结果.耗时毫秒 为 int(时间戳() * 1000 减去 己.开始时刻)。
    返回 结果。

  # 杀整棵树：Windows taskkill /T（优雅→宽限→/F）；POSIX os.killpg（SIGTERM→SIGKILL）
  段落 杀树 接收 宽限期毫秒:
    如果 己.进程对象 等于 空:
      返回。
    结束。
    设 pid 为 己.进程对象.pid。
    如果 sys.platform 等于 "win32":
      subprocess.call(["taskkill", "/T", "/PID", str(pid)])。
      己.等待到死(宽限期毫秒)。
      如果 己.是否存活():
        subprocess.call(["taskkill", "/F", "/T", "/PID", str(pid)])。
        己.等待到死(宽限期毫秒)。
      结束。
    否则:
      尝试:
        os.killpg(pid, signal.SIGTERM)。
      捕获 错误:
        尝试:
          os.kill(pid, signal.SIGTERM)。
        捕获 错误:
        结束。
      结束。
      己.等待到死(宽限期毫秒)。
      如果 己.是否存活():
        尝试:
          os.killpg(pid, signal.SIGKILL)。
        捕获 错误:
          尝试:
            os.kill(pid, signal.SIGKILL)。
          捕获 错误:
          结束。
        结束。
        己.等待到死(宽限期毫秒)。
      结束。
    结束。

  # 轮询直到进程死亡或宽限期耗尽；返回是否确认已死
  段落 等待到死 接收 毫秒:
    设 截止 为 时间戳() * 1000 加上 毫秒。
    当 真:
      如果 己.进程对象.poll() 不等于 空:
        返回 真。
      结束。
      如果 时间戳() * 1000 大于 截止:
        返回 假。
      结束。
      time.sleep(0.03)。
    结束。

  # 构建受控环境：白名单（非空则仅保留匹配前缀）+ 黑名单覆盖
  段落 构建环境 接收 配置:
    设 环境 为 {}。
    设 允许 为 配置.get("允许环境", 空)。
    设 拒绝 为 配置.get("拒绝环境", 空)。
    如果 允许 等于 空:
      允许 为 []。
    结束。
    如果 拒绝 等于 空:
      拒绝 为 []。
    结束。
    遍历 名 于 os.environ:
      如果 己.环境许可(名, 允许, 拒绝):
        环境[名] 为 os.environ.get(名)。
      结束。
    结束。
    返回 环境。

  段落 环境许可 接收 名, 允许, 拒绝:
    如果 名 在 拒绝:
      返回 假。
    结束。
    如果 长度(允许) 等于 0:
      返回 真。
    结束。
    遍历 前缀 于 允许:
      如果 名.startswith(前缀):
        返回 真。
      结束。
    结束。
    返回 假。