# 纯光明实现 —— 中文文本处理（取代同名 .py 壳）。
#
# 提供：统计中文字符 / 提取中文 / 去除标点 / 判断全中文 / 判断含中文
#       / 中文分句 / 中文分段 / 中文词频。
# 全部只用光明内置原语，零 导入、零 引 Python。
#
# CJK 判定四段（与 .py 一致）：
#   0x4E00-0x9FFF、0x3400-0x4DBF、0x20000-0x2A6DF、0xF900-0xFAFF

导出 统计中文字符 提取中文 去除标点 判断全中文 判断含中文 中文分句 中文分段 中文词频。

# 中文标点集合（.py _CHINESE_PUNCTUATION 去重后的全部字符）
设 中文标点表 为 "，。、；：？！…—·～〃「」『』【】《》（）〔〕﹁﹂﹃﹄﹝﹞＂＇｀｜＠＃＄％＾＆＊＋＝｛｝［］＼"

# 单字符是否汉字（CJK 四段）
段落 是汉字 接收 字符:
  设 码 为 码位(字符)
  如果 码 >= 19968 且 码 <= 40959:
    返回 真
  如果 码 >= 13312 且 码 <= 19903:
    返回 真
  如果 码 >= 131072 且 码 <= 173791:
    返回 真
  如果 码 >= 63744 且 码 <= 64255:
    返回 真
  返回 假

# 单字符是否空白（空格 32 / 制表 9 / 换行 10 / 回车 13）
段落 是空白 接收 字符:
  设 码 为 码位(字符)
  如果 码 == 32:
    返回 真
  如果 码 == 9:
    返回 真
  如果 码 == 10:
    返回 真
  如果 码 == 13:
    返回 真
  返回 假

# 单字符是否句末符（。！？或换行）
段落 是句末 接收 字符:
  如果 字符 == "。":
    返回 真
  如果 字符 == "！":
    返回 真
  如果 字符 == "？":
    返回 真
  如果 字符 == "\n":
    返回 真
  返回 假

# 两端去空白（仅去 空格/制表/换行/回车）
段落 去空白 接收 文本:
  设 n 为 长(文本)
  设 起 为 0
  当 起 < n 且 是空白(文本[起]):
    设 起 为 起 + 1
  设 止 为 n
  当 止 > 起 且 是空白(文本[止 - 1]):
    设 止 为 止 - 1
  设 结果 为 ""
  设 k 为 起
  当 k < 止:
    设 结果 为 结果 加上 文本[k]
    设 k 为 k + 1
  返回 结果

段落 统计中文字符 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 0
  设 总数 为 0
  设 i 为 0
  当 i < n:
    如果 是汉字(文本[i]):
      设 总数 为 总数 + 1
    设 i 为 i + 1
  返回 总数

段落 提取中文 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 ""
  设 结果 为 ""
  设 i 为 0
  当 i < n:
    如果 是汉字(文本[i]):
      设 结果 为 结果 加上 文本[i]
    设 i 为 i + 1
  返回 结果

段落 去除标点 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 ""
  设 结果 为 ""
  设 i 为 0
  当 i < n:
    如果 字符串包含(中文标点表, 文本[i]):
      设 i 为 i + 1
    否则:
      设 结果 为 结果 加上 文本[i]
      设 i 为 i + 1
  返回 结果

段落 判断全中文 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 假
  设 i 为 0
  当 i < n:
    如果 是汉字(文本[i]):
      设 i 为 i + 1
    否则:
      返回 假
  返回 真

段落 判断含中文 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 假
  设 i 为 0
  当 i < n:
    如果 是汉字(文本[i]):
      返回 真
    设 i 为 i + 1
  返回 假

段落 中文分句 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 []
  设 结果 为 []
  设 当前 为 ""
  设 i 为 0
  当 i < n:
    如果 是句末(文本[i]):
      设 剥离 为 去空白(当前)
      如果 剥离 != "":
        结果.追加(剥离)
      设 当前 为 ""
      当 i < n:
        如果 是句末(文本[i]):
          设 i 为 i + 1
        否则:
          跳出
    否则:
      设 当前 为 当前 加上 文本[i]
      设 i 为 i + 1
  设 剥离 为 去空白(当前)
  如果 剥离 != "":
    结果.追加(剥离)
  返回 结果

段落 中文分段 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 []
  设 结果 为 []
  设 当前 为 ""
  设 i 为 0
  当 i < n:
    如果 是空白(文本[i]):
      设 起 为 i
      当 i < n 且 是空白(文本[i]):
        设 i 为 i + 1
      设 止 为 i
      设 换行数 为 0
      设 k 为 起
      当 k < 止:
        如果 文本[k] == "\n":
          设 换行数 为 换行数 + 1
        设 k 为 k + 1
      如果 换行数 >= 2:
        设 剥离 为 去空白(当前)
        如果 剥离 != "":
          结果.追加(剥离)
        设 当前 为 ""
      否则:
        设 k 为 起
        当 k < 止:
          设 当前 为 当前 加上 文本[k]
          设 k 为 k + 1
    否则:
      设 当前 为 当前 加上 文本[i]
      设 i 为 i + 1
  设 剥离 为 去空白(当前)
  如果 剥离 != "":
    结果.追加(剥离)
  返回 结果

# 对 (字符, 次数) 列表按次数降序做稳定插入排序
段落 排序对表 接收 对表:
  设 结果 为 []
  设 n 为 长(对表)
  设 i 为 0
  当 i < n:
    设 v 为 对表[i]
    设 j 为 长(结果)
    当 j > 0:
      如果 结果[j - 1][1] >= v[1]:
        跳出
      设 j 为 j - 1
    设 新表 为 []
    设 k 为 0
    当 k < j:
      新表.追加(结果[k])
      设 k 为 k + 1
    新表.追加(v)
    设 k 为 j
    当 k < 长(结果):
      新表.追加(结果[k])
      设 k 为 k + 1
    设 结果 为 新表
    设 i 为 i + 1
  返回 结果

段落 中文词频 接收 文本:
  设 n 为 长(文本)
  设 频次 为 新建字典()
  设 i 为 0
  当 i < n:
    设 c 为 文本[i]
    如果 是汉字(c):
      设 频次 为 字典设置(频次, c, 字典获取(频次, c, 0) + 1)
    设 i 为 i + 1
  设 键表 为 字典键列表(频次)
  设 对表 为 []
  设 k 为 0
  当 k < 长(键表):
    对表.追加((键表[k], 字典获取(频次, 键表[k], 0)))
    设 k 为 k + 1
  设 排序后 为 排序对表(对表)
  设 结果 为 新建字典()
  设 j 为 0
  当 j < 长(排序后):
    设 对 为 排序后[j]
    设 结果 为 字典设置(结果, 对[0], 对[1])
    设 j 为 j + 1
  返回 结果
