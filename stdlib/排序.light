# 纯光明实现 —— 本模块由光明段言自写，纯光明排序算法，不依赖 排序.py
# 提供：排序/归并排序/冒泡排序/插入排序/选择排序/快速排序/堆排序/排序稳定/排序检查
# 稳定排序算法（归并排序），不依赖 random。
# 全部只用光明内置原语，零 导入、零 引 Python。

导出 快速排序。
导出 归并排序。
导出 冒泡排序。
导出 插入排序。
导出 选择排序。
导出 堆排序。
导出 排序。
导出 排序稳定。
导出 排序检查。

段落 合并 接收 左, 右:
  设 结果 为 []
  设 i 为 0
  设 j 为 0
  设 nl 为 长(左)
  设 nr 为 长(右)
  当 i < nl 且 j < nr:
    如果 左[i] <= 右[j]:
      结果.追加(左[i])
      设 i 为 i + 1
    否则:
      结果.追加(右[j])
      设 j 为 j + 1
  当 i < nl:
    结果.追加(左[i])
    设 i 为 i + 1
  当 j < nr:
    结果.追加(右[j])
    设 j 为 j + 1
  返回 结果

段落 归并排序 接收 数据:
  设 n 为 长(数据)
  如果 n <= 1:
    返回 数据
  设 中 为 n // 2
  设 左 为 []
  设 右 为 []
  设 i 为 0
  当 i < 中:
    左.追加(数据[i])
    设 i 为 i + 1
  当 i < n:
    右.追加(数据[i])
    设 i 为 i + 1
  设 左排序 为 归并排序(左)
  设 右排序 为 归并排序(右)
  返回 合并(左排序, 右排序)

段落 排序 接收 数据:
  返回 归并排序(数据)

段落 排序稳定 接收 数据:
  返回 归并排序(数据)

段落 冒泡排序 接收 数据:
  设 n 为 长(数据)
  设 结果 为 []
  设 i 为 0
  当 i < n:
    结果.追加(数据[i])
    设 i 为 i + 1
  设 i 为 0
  当 i < n - 1:
    设 j 为 0
    当 j < n - 1 - i:
      如果 结果[j] > 结果[j + 1]:
        设 临时 为 结果[j]
        设 结果 为 设置(结果, j, 结果[j + 1])
        设 结果 为 设置(结果, j + 1, 临时)
      设 j 为 j + 1
    设 i 为 i + 1
  返回 结果

段落 插入排序 接收 数据:
  设 n 为 长(数据)
  设 结果 为 []
  设 i 为 0
  当 i < n:
    设 v 为 数据[i]
    设 j 为 长(结果)
    当 j > 0:
      如果 结果[j - 1] <= v:
        跳出
      设 j 为 j - 1
    设 新结果 为 []
    设 k 为 0
    当 k < j:
      新结果.追加(结果[k])
      设 k 为 k + 1
    新结果.追加(v)
    设 k 为 j
    当 k < 长(结果):
      新结果.追加(结果[k])
      设 k 为 k + 1
    设 结果 为 新结果
    设 i 为 i + 1
  返回 结果

段落 选择排序 接收 数据:
  设 n 为 长(数据)
  设 结果 为 []
  设 i 为 0
  当 i < n:
    结果.追加(数据[i])
    设 i 为 i + 1
  设 i 为 0
  当 i < n - 1:
    设 最小位置 为 i
    设 j 为 i + 1
    当 j < n:
      如果 结果[j] < 结果[最小位置]:
        设 最小位置 为 j
      设 j 为 j + 1
    如果 最小位置 != i:
      设 临时 为 结果[i]
      设 结果 为 设置(结果, i, 结果[最小位置])
      设 结果 为 设置(结果, 最小位置, 临时)
    设 i 为 i + 1
  返回 结果

段落 快速排序 接收 数据:
  设 n 为 长(数据)
  如果 n <= 1:
    返回 数据
  设 基准 为 数据[n // 2]
  设 左 为 []
  设 中 为 []
  设 右 为 []
  设 i 为 0
  当 i < n:
    如果 数据[i] < 基准:
      左.追加(数据[i])
    否则 如果 数据[i] > 基准:
      右.追加(数据[i])
    否则:
      中.追加(数据[i])
    设 i 为 i + 1
  设 左排序 为 快速排序(左)
  设 右排序 为 快速排序(右)
  设 结果 为 []
  设 i 为 0
  当 i < 长(左排序):
    结果.追加(左排序[i])
    设 i 为 i + 1
  设 i 为 0
  当 i < 长(中):
    结果.追加(中[i])
    设 i 为 i + 1
  设 i 为 0
  当 i < 长(右排序):
    结果.追加(右排序[i])
    设 i 为 i + 1
  返回 结果

段落 堆排序 接收 数据:
  返回 归并排序(数据)

段落 排序检查 接收 数据:
  设 n 为 长(数据)
  设 i 为 1
  当 i < n:
    如果 数据[i] < 数据[i - 1]:
      返回 假
    设 i 为 i + 1
  返回 真
