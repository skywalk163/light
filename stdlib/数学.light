# 纯光明实现 —— 本模块由光明自写，纯光明数学函数，不依赖 数学.py
# 提供：绝对值/最大值/最小值/幂/平方根/立方根/正弦/余弦/正切/弧度转角度/角度转弧度
#       /向上取整/向下取整/四舍五入/圆周率/自然常数/黄金比例/阶乘/对数/自然对数/常用对数/对数2/指数
#       /双曲正弦/双曲余弦/双曲正切/反双曲正弦/反双曲余弦/反双曲正切
#       /反正弦/反余弦/反正切/排列/组合/双阶乘
#       /复数实部/复数虚部/复数模/复数共轭（兼容 CPython complex 透传与 [实部,虚部] 列表两种表示）
# CIB 契约对齐：与 数学.py 导出对齐并补英文别名（pi/pow/sqrt/sin/cos/tan/floor/ceil/round/random）。
#   幂 用 ** 中缀；四舍五入 接收 小数位数=0，floor-half-up（不用 _light_builtin.四舍五入：
#   它生成 Python round() 会被英文别名 round 段落遮蔽导致无限递归）；random 委托 随机.light 随机0到1。
# 随机3导出（随机整数/随机浮点/随机选择）归 T5C 随机模块负责，本模块不实现。
# 全部只用光明内置原语与 随机0到1，零 引 Python。
# 反三角/双曲为 0.88 路2 补齐（test_stdlib_complete TestMath 5 红）：反三角复用
# 《复数》反正切2 同款级数+角度减半算法（独立命名避免跨模块段名冲突）；
# 反正弦(±1)/反余弦(±1) 走 反正切2 的 xf==0 精确分支，边界值 0/π/2 无级数误差。

从 随机 导入 随机0到1

导出 绝对值 最大值 最小值 幂 平方根 立方根。
导出 正弦 余弦 正切 弧度转角度 角度转弧度。
导出 向上取整 向下取整 四舍五入 圆周率 自然常数 阶乘。
导出 对数 自然对数 常用对数 对数2 指数。
导出 pi pow sqrt sin cos tan floor ceil round random。
导出 双曲正弦 双曲余弦 双曲正切 反双曲正弦 反双曲余弦 反双曲正切。
导出 反正弦 反余弦 反正切。
导出 排列 组合 双阶乘 黄金比例。
导出 复数实部 复数虚部 复数模 复数共轭。
# 随机3导出归 T5C：随机整数 随机浮点 随机选择

设 pi 为 3.141592653589793

段落 绝对值 接收 x:
  返回 _light_builtin.绝对值(x)

段落 最大值 接收 甲, 乙:
  如果 甲 >= 乙:
    返回 甲
  返回 乙

段落 最小值 接收 甲, 乙:
  如果 甲 <= 乙:
    返回 甲
  返回 乙

段落 幂 接收 底数, 指数:
  返回 底数 ** 指数

段落 平方根 接收 x:
  返回 _light_builtin.平方根(x)

段落 立方根 接收 x:
  如果 x >= 0:
    返回 _light_builtin.幂(x, 1.0 / 3.0)
  返回 0.0 - _light_builtin.幂(0.0 - x, 1.0 / 3.0)

段落 正弦 接收 x:
  返回 _light_builtin.正弦(x)

段落 余弦 接收 x:
  返回 _light_builtin.余弦(x)

段落 正切 接收 x:
  返回 _light_builtin.正弦(x) / _light_builtin.余弦(x)

段落 弧度转角度 接收 弧度:
  返回 弧度 * 180.0 / 3.141592653589793

段落 角度转弧度 接收 角度:
  返回 角度 * 3.141592653589793 / 180.0

段落 向上取整 接收 x:
  返回 _light_builtin.向上取整(x)

段落 向下取整 接收 x:
  返回 _light_builtin.向下取整(x)

段落 四舍五入 接收 x, 小数位数 = 0:
  如果 小数位数 == 0:
    返回 _light_builtin.向下取整(x 加上 0.5)
  设 因子 为 1
  设 i 为 0
  当 i < 小数位数:
    设 因子 为 因子 乘 10.0
    设 i 为 i 加上 1
  返回 _light_builtin.向下取整(x 乘 因子 加上 0.5) / 因子

段落 圆周率:
  返回 3.141592653589793

段落 自然常数:
  返回 2.718281828459045

段落 阶乘 接收 n:
  如果 n < 0:
    抛出 运行时错误("负数没有阶乘")
  设 结果 为 1
  设 i 为 1
  当 i <= n:
    设 结果 为 结果 * i
    设 i 为 i + 1
  返回 结果

段落 自然对数 接收 x:
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求自然对数")
  返回 _light_builtin.自然对数(x)

段落 常用对数 接收 x:
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求常用对数")
  返回 _light_builtin.常用对数(x)

段落 对数2 接收 x:
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求对数2")
  返回 _light_builtin.对数2(x)

段落 对数 接收 x, 底数:
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求对数")
  如果 底数 <= 0:
    抛出 运行时错误("对数底数必须为正数")
  返回 自然对数(x) / 自然对数(底数)

段落 指数 接收 x:
  返回 _light_builtin.指数(x)

段落 pow 接收 底数, 指数:
  返回 幂(底数, 指数)

段落 sqrt 接收 x:
  返回 平方根(x)

段落 sin 接收 x:
  返回 正弦(x)

段落 cos 接收 x:
  返回 余弦(x)

段落 tan 接收 x:
  返回 正切(x)

段落 floor 接收 x:
  返回 _light_builtin.向下取整(x)

段落 ceil 接收 x:
  返回 _light_builtin.向上取整(x)

段落 round 接收 x, 小数位数 = 0:
  返回 四舍五入(x, 小数位数)

段落 random 接收:
  返回 随机0到1()

# ── 双曲函数（0.88 路2 补齐）──
# sinh/cosh 用 e^x ± e^-x；O0 规避：不写负数字面量（0 - x）。

段落 双曲正弦 接收 x:
  设 ex 为 指数(x)
  返回 (ex - 指数(0 - x)) / 2.0

段落 双曲余弦 接收 x:
  设 ex 为 指数(x)
  返回 (ex + 指数(0 - x)) / 2.0

段落 双曲正切 接收 x:
  返回 双曲正弦(x) / 双曲余弦(x)

段落 反双曲正弦 接收 x:
  返回 自然对数(x + 平方根(x * x + 1.0))

段落 反双曲余弦 接收 x:
  如果 x < 1:
    抛出 运行时错误("反双曲余弦要求 x >= 1")
  返回 自然对数(x + 平方根(x * x - 1.0))

段落 反双曲正切 接收 x:
  如果 x >= 1:
    抛出 运行时错误("反双曲正切要求 |x| < 1")
  如果 x <= 0 - 1:
    抛出 运行时错误("反双曲正切要求 |x| < 1")
  返回 0.5 * (自然对数(1.0 + x) - 自然对数(1.0 - x))

# ── 纯光明反正切（与《复数》《向量》模块同算法，独立命名避免跨模块段名冲突）──

段落 反正切级数 接收 t:
  设 t2 为 t 乘 t
  设 t3 为 t2 乘 t
  设 t5 为 t3 乘 t2
  设 t7 为 t5 乘 t2
  设 t9 为 t7 乘 t2
  设 t11 为 t9 乘 t2
  设 t13 为 t11 乘 t2
  设 t15 为 t13 乘 t2
  返回 t - t3 / 3.0 + t5 / 5.0 - t7 / 7.0 + t9 / 9.0 - t11 / 11.0 + t13 / 13.0 - t15 / 15.0

段落 反正切正 接收 t:
  如果 t > 1.0:
    设 r 为 1.0 / t
    返回 1.5707963267948966 - 反正切正(r)
  # 阈值取 0.4 而非 0.5：t=0.5 附近若直接展开级数，首截断项 t^17/17 ≈ 4.5e-7，
  # 会超 1e-7 容差（反正切(-2)、反正弦(0.9) 实测踩中）。降阈值后最坏 |u| = 3/7，
  # u^17/17 ≈ 3.3e-8。
  如果 t > 0.4:
    设 u 为 (t - 1.0) / (t + 1.0)
    返回 0.7853981633974483 + 反正切级数(u)
  返回 反正切级数(t)

段落 反正切2 接收 y, x:
  设 yf 为 y + 0.0
  设 xf 为 x + 0.0
  如果 xf == 0.0:
    如果 yf > 0.0:
      返回 1.5707963267948966
    如果 yf < 0.0:
      返回 0.0 - 1.5707963267948966
    返回 0.0
  设 基础 为 反正切正(_light_builtin.绝对值(yf / xf))
  如果 xf > 0.0:
    如果 yf >= 0.0:
      返回 基础
    返回 0.0 - 基础
  如果 yf >= 0.0:
    返回 3.141592653589793 - 基础
  返回 基础 - 3.141592653589793

段落 反正切 接收 x:
  如果 x >= 0.0:
    返回 反正切正(x)
  返回 0.0 - 反正切正(0.0 - x)

# 反正弦 = 反正切2(x, √(1-x²))；x=±1 时第二参数为 0，走 xf==0 精确分支。
段落 反正弦 接收 x:
  如果 x > 1.0:
    抛出 运行时错误("反正弦要求 |x| <= 1")
  如果 x < 0.0 - 1.0:
    抛出 运行时错误("反正弦要求 |x| <= 1")
  返回 反正切2(x, _light_builtin.平方根(1.0 - x * x))

段落 反余弦 接收 x:
  如果 x > 1.0:
    抛出 运行时错误("反余弦要求 |x| <= 1")
  如果 x < 0.0 - 1.0:
    抛出 运行时错误("反余弦要求 |x| <= 1")
  返回 1.5707963267948966 - 反正弦(x)

# ── 组合数学（0.88 路2 补齐）──

段落 排列 接收 n, k:
  如果 k < 0:
    抛出 运行时错误("排列要求 k 不为负数")
  如果 k > n:
    抛出 运行时错误("排列要求 k 不大于 n")
  设 结果 为 1
  设 i 为 0
  当 i < k:
    设 结果 为 结果 * (n - i)
    设 i 为 i + 1
  返回 结果

段落 组合 接收 n, k:
  如果 k < 0:
    抛出 运行时错误("组合要求 k 不为负数")
  如果 k > n:
    抛出 运行时错误("组合要求 k 不大于 n")
  返回 排列(n, k) / 阶乘(k)

段落 双阶乘 接收 n:
  如果 n < 0:
    抛出 运行时错误("负数没有双阶乘")
  设 结果 为 1
  设 i 为 n
  当 i > 1:
    设 结果 为 结果 * i
    设 i 为 i - 2
  返回 结果

# ── 常数（0.88 路2 补齐）──

段落 黄金比例:
  返回 (1.0 + 平方根(5.0)) / 2.0

# ── 复数工具（0.88 路2 补齐）──
# 兼容两种表示：CPython complex 透传（z.real / z.conjugate() 属性与方法直取，
# 已探针验证 codegen 支持）与《复数》模块的 [实部, 虚部] 列表表示（是列表 判型分派）。

段落 复数实部 接收 z:
  如果 是列表(z):
    返回 z[0]
  返回 z.real

段落 复数虚部 接收 z:
  如果 是列表(z):
    返回 z[1]
  返回 z.imag

段落 复数模 接收 z:
  如果 是列表(z):
    返回 _light_builtin.平方根(z[0] * z[0] + z[1] * z[1])
  返回 _light_builtin.绝对值(z)

段落 复数共轭 接收 z:
  如果 是列表(z):
    返回 [z[0], 0 - z[1]]
  返回 z.conjugate()