# 纯光明实现 —— 本模块由光明段言自写，纯光明数学函数，不依赖 数学.py
# 提供：绝对值/最大值/最小值/幂/平方根/立方根/正弦/余弦/正切/弧度转角度/角度转弧度
#       /向上取整/向下取整/四舍五入/圆周率/自然常数/阶乘/对数/自然对数/常用对数/对数2/指数
# 随机3导出（随机整数/随机浮点/随机选择）归 T5C 随机模块负责，本模块不实现。
# 全部只用光明内置原语，零 导入、零 引 Python。

导出 绝对值 最大值 最小值 幂 平方根 立方根。
导出 正弦 余弦 正切 弧度转角度 角度转弧度。
导出 向上取整 向下取整 四舍五入 圆周率 自然常数 阶乘。
导出 对数 自然对数 常用对数 对数2 指数。
# 随机3导出归 T5C：随机整数 随机浮点 随机选择

段落 绝对值 接收 x:
  返回 _light_builtin.绝对值(x)

段落 最大值 接收 甲, 乙:
  如果 甲 >= 乙:
    返回 甲
  返回 乙

段落 最小值 接收 甲, 乙:
  如果 甲 <= 乙:
    返回 甲
  返回 乙

段落 幂 接收 底数, 指数:
  返回 _light_builtin.幂(底数, 指数)

段落 平方根 接收 x:
  返回 _light_builtin.平方根(x)

段落 立方根 接收 x:
  如果 x >= 0:
    返回 _light_builtin.幂(x, 1.0 / 3.0)
  返回 0.0 - _light_builtin.幂(0.0 - x, 1.0 / 3.0)

段落 正弦 接收 x:
  返回 _light_builtin.正弦(x)

段落 余弦 接收 x:
  返回 _light_builtin.余弦(x)

段落 正切 接收 x:
  返回 _light_builtin.正弦(x) / _light_builtin.余弦(x)

段落 弧度转角度 接收 弧度:
  返回 弧度 * 180.0 / 3.141592653589793

段落 角度转弧度 接收 角度:
  返回 角度 * 3.141592653589793 / 180.0

段落 向上取整 接收 x:
  返回 _light_builtin.向上取整(x)

段落 向下取整 接收 x:
  返回 _light_builtin.向下取整(x)

段落 四舍五入 接收 x:
  返回 _light_builtin.四舍五入(x)

段落 圆周率:
  返回 3.141592653589793

段落 自然常数:
  返回 2.718281828459045

段落 阶乘 接收 n:
  如果 n < 0:
    抛出 运行时错误("负数没有阶乘")
  设 结果 为 1
  设 i 为 1
  当 i <= n:
    设 结果 为 结果 * i
    设 i 为 i + 1
  返回 结果

段落 自然对数 接收 x:
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求自然对数")
  设 结果 为 0.0
  设 值 为 x + 0.0
  # 安全防护：O0 下 float 参数可能被零初始化
  如果 值 <= 0.0:
    返回 0.0
  当 值 > 2.0:
    设 值 为 值 / 2.0
    设 结果 为 结果 + 0.6931471805599453
  当 值 < 0.5:
    设 值 为 值 * 2.0
    设 结果 为 结果 - 0.6931471805599453
  设 t 为 (值 - 1.0) / (值 + 1.0)
  设 t2 为 t * t
  设 项 为 t
  设 和 为 t
  设 k 为 3
  当 k <= 99:
    设 项 为 项 * t2
    设 和 为 和 + 项 / k
    设 k 为 k + 2
  设 结果 为 结果 + 2.0 * 和
  返回 结果

段落 常用对数 接收 x:
  # 内联自然对数计算，避免跨段 float 返回值在算术中被零初始化
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求常用对数")
  设 结果 为 0.0
  设 值 为 x + 0.0
  如果 值 <= 0.0:
    返回 0.0
  当 值 > 2.0:
    设 值 为 值 / 2.0
    设 结果 为 结果 + 0.6931471805599453
  当 值 < 0.5:
    设 值 为 值 * 2.0
    设 结果 为 结果 - 0.6931471805599453
  设 t 为 (值 - 1.0) / (值 + 1.0)
  设 t2 为 t * t
  设 项 为 t
  设 和 为 t
  设 k 为 3
  当 k <= 99:
    设 项 为 项 * t2
    设 和 为 和 + 项 / k
    设 k 为 k + 2
  设 结果 为 结果 + 2.0 * 和
  返回 结果 / 2.302585092994046

段落 对数2 接收 x:
  # 内联自然对数计算，避免跨段 float 返回值在算术中被零初始化
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求对数2")
  设 结果 为 0.0
  设 值 为 x + 0.0
  如果 值 <= 0.0:
    返回 0.0
  当 值 > 2.0:
    设 值 为 值 / 2.0
    设 结果 为 结果 + 0.6931471805599453
  当 值 < 0.5:
    设 值 为 值 * 2.0
    设 结果 为 结果 - 0.6931471805599453
  设 t 为 (值 - 1.0) / (值 + 1.0)
  设 t2 为 t * t
  设 项 为 t
  设 和 为 t
  设 k 为 3
  当 k <= 99:
    设 项 为 项 * t2
    设 和 为 和 + 项 / k
    设 k 为 k + 2
  设 结果 为 结果 + 2.0 * 和
  返回 结果 / 0.6931471805599453

段落 对数 接收 x, 底数:
  # 内联两次自然对数计算，避免跨段 float 返回值
  如果 x <= 0:
    抛出 运行时错误("不能对非正数求对数")
  如果 底数 <= 0:
    抛出 运行时错误("对数底数必须为正数")
  # ln(x)
  设 结果1 为 0.0
  设 值 为 x + 0.0
  如果 值 <= 0.0:
    返回 0.0
  当 值 > 2.0:
    设 值 为 值 / 2.0
    设 结果1 为 结果1 + 0.6931471805599453
  当 值 < 0.5:
    设 值 为 值 * 2.0
    设 结果1 为 结果1 - 0.6931471805599453
  设 t 为 (值 - 1.0) / (值 + 1.0)
  设 t2 为 t * t
  设 项 为 t
  设 和 为 t
  设 k 为 3
  当 k <= 99:
    设 项 为 项 * t2
    设 和 为 和 + 项 / k
    设 k 为 k + 2
  设 结果1 为 结果1 + 2.0 * 和
  # ln(底数)
  设 结果2 为 0.0
  设 值2 为 底数 + 0.0
  如果 值2 <= 0.0:
    返回 0.0
  当 值2 > 2.0:
    设 值2 为 值2 / 2.0
    设 结果2 为 结果2 + 0.6931471805599453
  当 值2 < 0.5:
    设 值2 为 值2 * 2.0
    设 结果2 为 结果2 - 0.6931471805599453
  设 t3 为 (值2 - 1.0) / (值2 + 1.0)
  设 t4 为 t3 * t3
  设 项2 为 t3
  设 和2 为 t3
  设 k 为 3
  当 k <= 99:
    设 项2 为 项2 * t4
    设 和2 为 和2 + 项2 / k
    设 k 为 k + 2
  设 结果2 为 结果2 + 2.0 * 和2
  返回 结果1 / 结果2

段落 指数 接收 x:
  设 n 为 0
  设 值 为 x + 0.0
  当 值 > 1.0:
    设 值 为 值 / 2.0
    设 n 为 n + 1
  当 值 < -1.0:
    设 值 为 值 / 2.0
    设 n 为 n + 1
  设 项 为 1.0
  设 和 为 1.0
  设 k 为 1
  当 k <= 30:
    设 项 为 项 * 值 / k
    设 和 为 和 + 项
    设 k 为 k + 1
  设 i 为 0
  当 i < n:
    设 和 为 和 * 和
    设 i 为 i + 1
  返回 和
