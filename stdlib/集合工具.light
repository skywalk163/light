# 光明标准库 —— 集合工具模块（原生腿真身）
#
# 对齐 stdlib/集合工具.py（15 个导出：创建默认字典 创建有序字典 创建计数器
# 创建双端队列 命名元组 链字典 用户字典 用户列表 用户字符串 统计频率 可迭代对象
# + 四个别名：默认字典 有序字典 计数器 双端队列）。
#
# 原生腿能力边界（详见 docs/known_issues.md T5C 章节）：
# 1. 创建默认字典：默认工厂函数不可作为值传递/调用（原生腿边界），返回普通
#    字典（缺失键返回 空，不做工厂补值）。
# 2. 命名元组：原生腿无类/元组类型，返回字段名列表副本（类型名仅作标注）。
# 3. 用户字典 / 用户列表 / 用户字符串：自定义子类不可表达，返回对应内建容器。
# 4. 创建双端队列：以列表实现，最大长度参数仅存储不强制截断。

导出 创建默认字典 默认字典 创建有序字典 有序字典。
导出 创建计数器 计数器 创建双端队列 双端队列 命名元组。
导出 链字典 用户字典 用户列表 用户字符串 统计频率 可迭代对象。

# 创建默认字典（默认工厂=None）—— 工厂不可调用，返回普通字典
段落 创建默认字典 接收 默认工厂 = 空:
  设 映射 为 {}
  字典设置(映射, "默认工厂", 默认工厂)
  返回 映射

段落 默认字典 接收 默认工厂 = 空:
  返回 创建默认字典(默认工厂)

# 创建有序字典（数据=None）—— 以字典实现（原生腿字典保插入序）
段落 创建有序字典 接收 数据 = 空:
  设 映射 为 {}
  如果 数据 不等于 空:
    设 键们 为 字典键列表(数据)
    设 i 为 0
    当 i < 长(键们):
      字典设置(映射, 键们[i], 字典获取(数据, 键们[i], 空))
      设 i 为 i 加上 1
  返回 映射

段落 有序字典 接收 数据 = 空:
  返回 创建有序字典(数据)

# 创建计数器（可迭代对象=None）—— 元素计数字典
段落 创建计数器 接收 可迭代对象 = 空:
  设 映射 为 {}
  如果 可迭代对象 不等于 空:
    设 i 为 0
    当 i < 长(可迭代对象):
      设 当前 为 可迭代对象[i]
      设 次数 为 字典获取(映射, 当前, 0)
      字典设置(映射, 当前, 次数 加上 1)
      设 i 为 i 加上 1
  返回 映射

段落 计数器 接收 可迭代对象 = 空:
  返回 创建计数器(可迭代对象)

# 创建双端队列（可迭代对象=None, 最大长度=None）—— 以列表实现
段落 创建双端队列 接收 可迭代对象 = 空, 最大长度 = 空:
  设 队列 为 新建列表()
  如果 可迭代对象 不等于 空:
    设 i 为 0
    当 i < 长(可迭代对象):
      追加(队列, 可迭代对象[i])
      设 i 为 i 加上 1
  设 包装 为 {}
  字典设置(包装, "队列", 队列)
  字典设置(包装, "最大长度", 最大长度)
  返回 包装

段落 双端队列 接收 可迭代对象 = 空, 最大长度 = 空:
  返回 创建双端队列(可迭代对象, 最大长度)

# 命名元组（类型名, 字段名列表）—— 原生腿无类/元组，返回字段名列表副本
段落 命名元组 接收 类型名, 字段名列表:
  设 结果 为 新建列表()
  设 i 为 0
  当 i < 长(字段名列表):
    追加(结果, 字段名列表[i])
    设 i 为 i 加上 1
  返回 结果

# 链字典（*映射列表）—— 合并键值（后者覆盖前者）
# 链字典 —— T7B / T5C-05 根因修复后变参可绑定，改回变参（对齐 .py）
段落 链字典 接收 *映射们:
  设 结果 为 {}
  设 k 为 0
  当 k < 长(映射们):
    设 当前 为 映射们[k]
    设 键们 为 字典键列表(当前)
    设 i 为 0
    当 i < 长(键们):
      字典设置(结果, 键们[i], 字典获取(当前, 键们[i], 空))
      设 i 为 i 加上 1
    设 k 为 k 加上 1
  返回 结果

# 用户字典 —— 自定义子类不可表达，返回内建字典
段落 用户字典:
  返回 新建字典()

# 用户列表 —— 返回内建列表
段落 用户列表:
  返回 新建列表()

# 用户字符串 —— 返回空字符串
段落 用户字符串:
  返回 ""

# 统计频率（可迭代对象）—— 元素计数字典
段落 统计频率 接收 可迭代对象:
  设 映射 为 {}
  设 i 为 0
  当 i < 长(可迭代对象):
    设 当前 为 可迭代对象[i]
    设 次数 为 字典获取(映射, 当前, 0)
    字典设置(映射, 当前, 次数 加上 1)
    设 i 为 i 加上 1
  返回 映射

# 可迭代对象（可迭代对象, n=2）—— 按 n 分块
段落 可迭代对象 接收 可迭代对象, n = 2:
  设 结果 为 新建列表()
  设 块 为 新建列表()
  设 i 为 0
  当 i < 长(可迭代对象):
    追加(块, 可迭代对象[i])
    如果 长(块) == n:
      追加(结果, 块)
      设 块 为 新建列表()
    设 i 为 i 加上 1
  如果 长(块) > 0:
    追加(结果, 块)
  返回 结果
