# class栈:
#
#     def压入(self,元素):
#
#     def弹出(self):
#             抛出错误("栈为空")
#
#     def顶部(self):
#             抛出错误("栈为空")
#
#     def空(self):
#
#     def大小(self):
#
#     def清空(self):
#
#     def转为列表(self):
#
# class队列:
#
#     def入队(self,元素):
#
#     def出队(self):
#             抛出错误("队列为空")
#
#     def队首(self):
#             抛出错误("队列为空")
#
#     def队尾(self):
#             抛出错误("队列为空")
#
#     def空(self):
#
#     def大小(self):
#
#     def清空(self):
#
#     def转为列表(self):
#
# class双端队列:
#
#     def左入队(self,元素):
#
#     def右入队(self,元素):
#
#     def左出队(self):
#             抛出错误("队列为空")
#
#     def右出队(self):
#             抛出错误("队列为空")
#
#     def队首(self):
#             抛出错误("队列为空")
#
#     def队尾(self):
#             抛出错误("队列为空")
#
#     def空(self):
#
#     def大小(self):
#
#     def清空(self):
#
#     def转为列表(self):
#
# class优先队列:
#
#     def入队(self,优先级,元素):
#
#     def出队(self):
#             抛出错误("队列为空")
#
#     def队首(self):
#             抛出错误("队列为空")
#
#     def队首优先级(self):
#             抛出错误("队列为空")
#
#     def空(self):
#
#     def大小(self):
#
#     def清空(self):
#
# class链表节点:
#
# class单链表:
#
#     def头部插入(self,值):
#         新节点=链表节点(值)
#
#     def尾部插入(self,值):
#         新节点=链表节点(值)
#             while当前.下一个is not None:
#                 当前=当前.下一个
#             当前.下一个=新节点
#
#     def指定位置插入(self,索引,值):
#             抛出错误("索引越界")
#         if索引==0:
#         新节点=链表节点(值)
#         for _ in范围(索引-1):
#             当前=当前.下一个
#         新节点.下一个=当前.下一个
#         当前.下一个=新节点
#
#     def删除头部(self):
#             抛出错误("链表为空")
#         return值
#
#     def删除尾部(self):
#             抛出错误("链表为空")
#             return值
#         while当前.下一个.下一个is not None:
#             当前=当前.下一个
#         值=当前.下一个.值
#         当前.下一个=None
#         return值
#
#     def删除指定值(self,值):
#         while当前.下一个is not None:
#             if当前.下一个.值==值:
#                 当前.下一个=当前.下一个.下一个
#             当前=当前.下一个
#
#     def查找(self,值):
#         索引=0
#         while当前is not None:
#             if当前.值==值:
#                 return索引
#             当前=当前.下一个
#             索引+=1
#
#     def获取(self,索引):
#             抛出错误("索引越界")
#         for _ in范围(索引):
#             当前=当前.下一个
#         return当前.值
#
#     def修改(self,索引,值):
#             抛出错误("索引越界")
#         for _ in范围(索引):
#             当前=当前.下一个
#         当前.值=值
#
#     def空(self):
#
#     def大小(self):
#
#     def清空(self):
#
#     def转为列表(self):
#         结果=[]
#         while当前is not None:
#             结果.append(当前.值)
#             当前=当前.下一个
#         return结果
#
# class二叉树节点:
#
# class二叉搜索树:
#
#     def插入(self,值):
#
#         if值<节点.值:
#             if节点.左子树is None:
#                 节点.左子树=二叉树节点(值)
#             if节点.右子树is None:
#                 节点.右子树=二叉树节点(值)
#
#     def查找(self,值):
#
#         if节点is None:
#         if值==节点.值:
#         elif值<节点.值:
#
#     def删除(self,值):
#
#         if节点is None:
#         if值<节点.值:
#         elif值>节点.值:
#             if节点.左子树is None:
#                 return节点.右子树
#             if节点.右子树is None:
#                 return节点.左子树
#             节点.值=最小节点.值
#         return节点
#
#         while节点.左子树is not None:
#             节点=节点.左子树
#         return节点
#
#     def中序遍历(self):
#         结果=[]
#         return结果
#
#         if节点is not None:
#             结果.append(节点.值)
#
#     def前序遍历(self):
#         结果=[]
#         return结果
#
#         if节点is not None:
#             结果.append(节点.值)
#
#     def后序遍历(self):
#         结果=[]
#         return结果
#
#         if节点is not None:
#             结果.append(节点.值)
#
#     def层序遍历(self):
#         结果=[]
#         while队列:
#             节点=队列.pop(0)
#             结果.append(节点.值)
#             if节点.左子树:
#                 队列.append(节点.左子树)
#             if节点.右子树:
#                 队列.append(节点.右子树)
#         return结果
#
#     def高度(self):
#
#         if节点is None:
#
#     def大小(self):
#
#         if节点is None:
#
#     def空(self):
#
#     def清空(self):
#
# 用法：
#   从《数据结构》导入《栈》。

导出 栈 队列 双端队列 优先队列 链表节点 单链表。
导出 二叉树节点 二叉搜索树 创建栈 入栈 出栈 查看栈顶。
导出 栈是否为空 栈大小 创建队列 入队 出队 查看队首。
导出 队列是否为空 队列大小 创建双端队列 左入 右入 左出。
导出 右出 查看左端 查看右端 双端队列是否为空 双端队列大小。
