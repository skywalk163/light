# 纯光明实现 —— 字符串常量与字符分类（取代同名 .py 壳）。
#
# 常量部分为纯字面量导出，与 Python string 模块逐字符一致。
# 可委托原生腿 builtin 的（大写/小写/去除空格/右去除/替换/查找/计数/反向查找/
# 以开头/以结尾/字符串包含/分割/重复）直接委托；无 builtin 的（交换大小写、
# 标题大小写、补零、居中/左/右对齐、连接、去除左端空白）用纯光明循环实现。
#
# 能力边界（见 docs/known_issues.md T5B 章节）：
#   字符列表全部是 / 字符列表有一个是 —— .py 版本第二参是「检测函数」，而原生后端
#       不支持高阶函数（把函数作值传参），故本模块改为接收「分类名字符串」
#       （"字母"/"数字"/"字母数字"/"小写"/"大写"/"空白"/"可打印"/"标点"/"十六进制"/"八进制"）。
#   右分割 —— 原生腿无 rsplit runtime，退化为从左到右全分割（不限制次数）。
#   按行分割 —— 退化为按 "\n" 分割，忽略「保留换行符」参数。

导出 小写字母 大写字母 字母 数字 十六进制数字 八进制数字。
导出 标点符号 可打印字符 空白字符 换行符 制表符 回车符。
导出 空字符串 是字母 是数字 是字母数字 是小写 是大写。
导出 是空白 是可打印 是标点 是十六进制 是八进制 字符列表全部是。
导出 字符列表有一个是 首字母大写 标题大小写 全大写 全小写 交换大小写。
导出 去除两端空白 去除左端空白 去除右端空白 居中填充 左对齐 右对齐。
导出 补零 连接 分割 右分割 按行分割 替换。
导出 包含 以开头 以结尾 查找 反向查找 计数。

# ── 常量 ──
设 小写字母 为 "abcdefghijklmnopqrstuvwxyz"
设 大写字母 为 "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
设 字母 为 小写字母 加上 大写字母
设 数字 为 "0123456789"
设 十六进制数字 为 "0123456789abcdefABCDEF"
设 八进制数字 为 "01234567"
设 标点符号 为 "!\"#$%&'()*+,-./:;<=>?@[\\]^_`{|}~"
设 空白字符 为 " \t\n\r" 加上 字符自码位(11) 加上 字符自码位(12)
设 可打印字符 为 数字 加上 字母 加上 标点符号 加上 空白字符
设 换行符 为 "\n"
设 制表符 为 "\t"
设 回车符 为 "\r"
设 空字符串 为 ""

# ── 字符分类（单字符，码位判定）──
段落 是小写 接收 c:
  设 o 为 码位(c)
  如果 o >= 97:
    如果 o <= 122:
      返回 真
  返回 假

段落 是大写 接收 c:
  设 o 为 码位(c)
  如果 o >= 65:
    如果 o <= 90:
      返回 真
  返回 假

段落 是数字 接收 c:
  设 o 为 码位(c)
  如果 o >= 48:
    如果 o <= 57:
      返回 真
  返回 假

段落 是字母 接收 c:
  如果 是小写(c) == 真:
    返回 真
  如果 是大写(c) == 真:
    返回 真
  返回 假

段落 是字母数字 接收 c:
  如果 是字母(c) == 真:
    返回 真
  如果 是数字(c) == 真:
    返回 真
  返回 假

段落 是空白 接收 c:
  设 o 为 码位(c)
  如果 o == 32:
    返回 真
  如果 o >= 9:
    如果 o <= 13:
      返回 真
  返回 假

段落 是标点 接收 c:
  返回 字符串包含(标点符号, c)

段落 是十六进制 接收 c:
  设 o 为 码位(c)
  如果 o >= 48:
    如果 o <= 57:
      返回 真
  如果 o >= 65:
    如果 o <= 70:
      返回 真
  如果 o >= 97:
    如果 o <= 102:
      返回 真
  返回 假

段落 是八进制 接收 c:
  设 o 为 码位(c)
  如果 o >= 48:
    如果 o <= 55:
      返回 真
  返回 假

段落 是可打印 接收 c:
  返回 字符串包含(可打印字符, c)

# ── 批量判定（分类名字符串，非高阶函数）──
段落 _判定 接收 c, 分类名:
  如果 分类名 == "字母":
    返回 是字母(c)
  如果 分类名 == "数字":
    返回 是数字(c)
  如果 分类名 == "字母数字":
    返回 是字母数字(c)
  如果 分类名 == "小写":
    返回 是小写(c)
  如果 分类名 == "大写":
    返回 是大写(c)
  如果 分类名 == "空白":
    返回 是空白(c)
  如果 分类名 == "可打印":
    返回 是可打印(c)
  如果 分类名 == "标点":
    返回 是标点(c)
  如果 分类名 == "十六进制":
    返回 是十六进制(c)
  如果 分类名 == "八进制":
    返回 是八进制(c)
  返回 假

段落 字符列表全部是 接收 文本, 分类名:
  设 i 为 0
  当 i < 长(文本):
    如果 _判定(文本[i], 分类名) == 假:
      返回 假
    设 i 为 i 加上 1
  返回 真

段落 字符列表有一个是 接收 文本, 分类名:
  设 i 为 0
  当 i < 长(文本):
    如果 _判定(文本[i], 分类名) == 真:
      返回 真
    设 i 为 i 加上 1
  返回 假

# ── 大小写变换 ──
段落 全大写 接收 文本:
  返回 大写(文本)

段落 全小写 接收 文本:
  返回 小写(文本)

段落 首字母大写 接收 文本:
  设 n 为 长(文本)
  如果 n == 0:
    返回 文本
  设 首 为 大写(文本[0])
  如果 n == 1:
    返回 首
  设 i 为 1
  设 结果 为 首
  当 i < n:
    设 结果 为 结果 加上 小写(文本[i])
    设 i 为 i 加上 1
  返回 结果

段落 交换大小写 接收 文本:
  设 n 为 长(文本)
  设 结果 为 ""
  设 i 为 0
  当 i < n:
    设 c 为 文本[i]
    如果 是小写(c) == 真:
      设 结果 为 结果 加上 大写(c)
    否则:
      如果 是大写(c) == 真:
        设 结果 为 结果 加上 小写(c)
      否则:
        设 结果 为 结果 加上 c
    设 i 为 i 加上 1
  返回 结果

段落 标题大小写 接收 文本:
  设 n 为 长(文本)
  设 结果 为 ""
  设 新词 为 真
  设 i 为 0
  当 i < n:
    设 c 为 文本[i]
    如果 是字母数字(c) == 真:
      如果 新词 == 真:
        设 结果 为 结果 加上 大写(c)
        设 新词 为 假
      否则:
        设 结果 为 结果 加上 小写(c)
    否则:
      设 结果 为 结果 加上 c
      设 新词 为 真
    设 i 为 i 加上 1
  返回 结果

# ── 去除空白 ──
段落 去除两端空白 接收 文本:
  返回 去除空格(文本)

段落 去除右端空白 接收 文本:
  返回 右去除(文本, 空白字符)

段落 去除左端空白 接收 文本:
  设 n 为 长(文本)
  设 i 为 0
  当 i < n:
    如果 是空白(文本[i]) == 假:
      设 结果 为 ""
      设 j 为 i
      当 j < n:
        设 结果 为 结果 加上 文本[j]
        设 j 为 j 加上 1
      返回 结果
    设 i 为 i 加上 1
  返回 ""

# ── 填充对齐 ──
段落 左对齐 接收 文本, 宽度, 填充字符:
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  返回 文本 加上 重复(填充字符[0], 缺)

段落 右对齐 接收 文本, 宽度, 填充字符:
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  返回 重复(填充字符[0], 缺) 加上 文本

段落 居中填充 接收 文本, 宽度, 填充字符:
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  设 左 为 整除(缺, 2)
  设 右 为 缺 减去 左
  返回 重复(填充字符[0], 左) 加上 文本 加上 重复(填充字符[0], 右)

段落 补零 接收 文本, 宽度:
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  如果 文本[0] == "-":
    设 余 为 ""
    设 i 为 1
    当 i < 长(文本):
      设 余 为 余 加上 文本[i]
      设 i 为 i 加上 1
    返回 "-" 加上 重复("0", 缺) 加上 余
  如果 文本[0] == "+":
    设 余 为 ""
    设 i 为 1
    当 i < 长(文本):
      设 余 为 余 加上 文本[i]
      设 i 为 i 加上 1
    返回 "+" 加上 重复("0", 缺) 加上 余
  返回 重复("0", 缺) 加上 文本

# ── 连接 / 分割 ──
段落 连接 接收 分隔符, 字符串列表:
  设 n 为 长(字符串列表)
  设 结果 为 ""
  设 i 为 0
  当 i < n:
    如果 i > 0:
      设 结果 为 结果 加上 分隔符
    设 结果 为 结果 加上 字符串列表[i]
    设 i 为 i 加上 1
  返回 结果

段落 分割 接收 文本, 分隔符:
  返回 split(文本, 分隔符)

# 能力边界：退化为从左到右全分割（忽略最大分割次数）。
段落 右分割 接收 文本, 分隔符, 最大分割次数:
  返回 split(文本, 分隔符)

# 能力边界：退化为按 "\n" 分割（忽略保留换行符参数）。
段落 按行分割 接收 文本, 保留换行符:
  返回 split(文本, "\n")

# ── 检索 ──
段落 替换 接收 文本, 旧子串, 新子串:
  返回 replace(文本, 旧子串, 新子串)

段落 包含 接收 文本, 子串:
  返回 字符串包含(文本, 子串)

段落 以开头 接收 文本, 前缀:
  返回 startswith(文本, 前缀)

段落 以结尾 接收 文本, 后缀:
  返回 endswith(文本, 后缀)

段落 查找 接收 文本, 子串:
  返回 find(文本, 子串)

段落 反向查找 接收 文本, 子串:
  返回 rfind(文本, 子串)

段落 计数 接收 文本, 子串:
  返回 str_count(文本, 子串)
