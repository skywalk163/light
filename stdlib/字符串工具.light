# 纯光明实现 —— 字符串工具（取代同名 .py 壳）
#
# 对齐 stdlib/字符串工具.py 的全部导出（大小写/修剪/分割/连接/查找替换/判断/
# 分词/正则提取/编码哈希/验证/格式化等）。
#
# 实现策略：
# - 纯字符串运算（大小写/反转/切片/修剪/填充/查找/计数/分词/相似度/格式化）
#   全部用光明原生算子手写，O0 真编译真跑。
# - 正则类（英文分词/去除HTML标签/提取数字邮箱手机号URL/去除标点特殊字符/
#   替换换行/替换多空白/文本清洗/各路验证）委托 正则表达式.light（已原生腿验证）。
# - 编码类（Base64/URL/HTML 实体/十六进制/二进制/GBK/Unicode 转义）委托
#   编码解码.light；哈希类（MD5/SHA1/SHA256/SHA512）委托 哈希.light。
# - 生成UUID 委托 uuid工具.light（原生腿无 os.urandom，降级为确定性伪随机，
#   见 uuid工具.light 能力边界说明）。
#
# 原生腿能力边界（已确认）：
# 1. 字符串 .分割() 无参不按空白切分（返回 []），故 去除所有空白/空白分词手动实现。
# 2. 字符串无 .左填充/.右填充/.居中填充 方法，填充类手动实现。
# 3. 字符码点互转用 码位(字符首字符)/字符自码位(码点)；Unicode转ASCII 仅对
#    非 ASCII 字符做 \uXXXX 转义（码位() 取首字符码点，单字符场景等价 Python ord）。
# 4. 随机用 随机数()（0..1 双精度）派生整数下标。

从 正则表达式 导入 查找所有 替换 完全匹配 提取单词。
从 re 导入 re_花括号。
从 编码解码 导入 Base64编码 Base64解码 URL编码 URL解码 HTML实体编码 HTML实体解码 十六进制编码 十六进制解码 字符串转二进制 二进制转字符串 文本转十六进制 十六进制转文本 UTF8转GBK GBK转UTF8 Unicode转ASCII ASCII转Unicode 字符集转换。
从 哈希 导入 MD5 SHA1 SHA256 SHA512。
从 uuid工具 导入 生成UUID4。

导出 转大写 转小写 首字母大写 每个单词首字母大写 反转字符串 截取字符串。
导出 分割字符串 连接字符串 替换字符串 去除首尾空白 去除左侧空白 去除右侧空白。
导出 去除所有空白 左填充 右填充 居中填充 零填充 重复字符串 字符串长度 字符计数。
导出 子串查找 子串查找最后 包含子串 以子串开头 以子串结尾 字符串对齐。
导出 分词 中文分词 英文分词 按长度分词 字符转码 UTF8转GBK GBK转UTF8。
导出 ASCII转Unicode Unicode转ASCII Base64编码 Base64解码 URL编码 URL解码。
导出 HTML编码 HTML解码 十六进制转字符串 字符串转十六进制 二进制转字符串 字符串转二进制。
导出 MD5哈希 SHA256哈希 生成随机字符串 生成UUID 验证邮箱 验证手机号 验证身份证号。
导出 验证URL 验证IP地址 验证日期 验证数字 验证字母 验证字母数字 验证空白 验证标题 验证小写 验证大写。
导出 去除HTML标签 提取HTML标签 提取文本中的数字 提取文本中的邮箱 提取文本中的手机号 提取文本中的URL。
导出 去除标点符号 去除特殊字符 替换换行符 替换多个空白 文本清洗 生成哈希值 字符串比较 字符串相似度。
导出 查找所有出现位置 安全字符串 格式化数字 格式化金额 格式化百分比 插入千分位 格式化定点。

# ---------------- 大小写 / 反转 / 切片 ----------------

段落 转大写 接收 文本:
  返回 文本.转大写()

段落 转小写 接收 文本:
  返回 文本.转小写()

段落 首字母大写 接收 文本:
  如果 长(文本) == 0:
    返回 文本
  设 首 为 文本[0].转大写()
  设 尾 为 文本[1:长(文本)].转小写()
  返回 首 加上 尾

段落 每个单词首字母大写 接收 文本:
  设 结果 为 ""
  设 i 为 0
  设 长文 为 长(文本)
  设 词首 为 真
  当 i < 长文:
    设 字符 为 文本[i]
    如果 字符 == " ":
      设 词首 为 真
      设 结果 为 结果 加上 字符
    否则:
      如果 词首:
        设 结果 为 结果 加上 字符.转大写()
        设 词首 为 假
      否则:
        设 结果 为 结果 加上 字符
    设 i 为 i 加上 1
  返回 结果

段落 反转字符串 接收 文本:
  设 结果 为 ""
  设 i 为 长(文本) 减去 1
  当 i >= 0:
    设 结果 为 结果 加上 文本[i]
    设 i 为 i 减去 1
  返回 结果

段落 截取字符串 接收 文本, 起始, 结束 = 空:
  如果 结束 == 空:
    返回 文本[起始:长(文本)]
  返回 文本[起始:结束]

# ---------------- 分割 / 连接 / 替换 ----------------

段落 分割字符串 接收 文本, 分隔符 = 空, 最大分割次数 = 空:
  如果 最大分割次数 == 0:
    设 单 为 新建列表()
    追加(单, 文本)
    返回 单
  如果 分隔符 == 空:
    返回 按空白分割(文本, 最大分割次数)
  如果 最大分割次数 == 空:
    返回 按分隔符分割(文本, 分隔符, 空)
  返回 按分隔符分割(文本, 分隔符, 最大分割次数)

# 内部：按空白（空格/制表/换行）切分，支持最大段数
段落 按空白分割 接收 文本, 最大段数:
  设 结果 为 新建列表()
  设 当前 为 ""
  设 i 为 0
  设 长文 为 长(文本)
  当 i < 长文:
    设 字符 为 文本[i]
    如果 字符 == " " 或 字符 == "\t" 或 字符 == "\n" 或 字符 == "\r":
      如果 长(当前) > 0:
        追加(结果, 当前)
        设 当前 为 ""
        如果 最大段数 != 空 且 长(结果) == 最大段数:
          追加(结果, 文本[i 加上 1:长文])
          返回 结果
    否则:
      设 当前 为 当前 加上 字符
    设 i 为 i 加上 1
  如果 长(当前) > 0:
    追加(结果, 当前)
  返回 结果

# 内部：按固定分隔符切分，支持最大段数（<=0 表示不限）
段落 按分隔符分割 接收 文本, 分隔符, 最大段数:
  设 结果 为 新建列表()
  设 长文 为 长(文本)
  设 长子 为 长(分隔符)
  设 起点 为 0
  设 i 为 0
  当 i <= 长文 减去 长子:
    如果 文本[i:i 加上 长子] == 分隔符:
      追加(结果, 文本[起点:i])
      设 起点 为 i 加上 长子
      如果 最大段数 != 空 且 最大段数 > 0 且 长(结果) == 最大段数:
        追加(结果, 文本[起点:长文])
        返回 结果
      设 i 为 起点
    否则:
      设 i 为 i 加上 1
  追加(结果, 文本[起点:长文])
  返回 结果

段落 连接字符串 接收 列表们, 连接符 = "":
  设 结果 为 ""
  设 n 为 长(列表们)
  如果 n == 0:
    返回 ""
  设 结果 为 转文本(列表们[0])
  设 i 为 1
  当 i < n:
    设 结果 为 结果 加上 连接符 加上 转文本(列表们[i])
    设 i 为 i 加上 1
  返回 结果

段落 替换字符串 接收 文本, 旧串, 新串, 次数 = 空:
  如果 次数 == 0:
    返回 文本
  如果 次数 == 空:
    返回 文本.替换(旧串, 新串)
  设 结果 为 ""
  设 已换 为 0
  设 i 为 0
  设 长文 为 长(文本)
  设 长老 为 长(旧串)
  当 i <= 长文 减去 长老:
    如果 已换 < 次数 且 文本[i:i 加上 长老] == 旧串:
      设 结果 为 结果 加上 新串
      设 i 为 i 加上 长老
      设 已换 为 已换 加上 1
    否则:
      设 结果 为 结果 加上 文本[i]
      设 i 为 i 加上 1
  设 结果 为 结果 加上 文本[i:长文]
  返回 结果

# ---------------- 修剪 / 填充 ----------------

段落 去除首尾空白 接收 文本:
  设 长文 为 长(文本)
  设 起 为 0
  当 起 < 长文:
    设 字符 为 文本[起]
    如果 字符 == " " 或 字符 == "\t" 或 字符 == "\n" 或 字符 == "\r":
      设 起 为 起 加上 1
    否则:
      跳出
  设 终 为 长文
  当 终 > 起:
    设 字符 为 文本[终 减去 1]
    如果 字符 == " " 或 字符 == "\t" 或 字符 == "\n" 或 字符 == "\r":
      设 终 为 终 减去 1
    否则:
      跳出
  返回 文本[起:终]

段落 去除左侧空白 接收 文本:
  设 长文 为 长(文本)
  设 起 为 0
  当 起 < 长文:
    设 字符 为 文本[起]
    如果 字符 == " " 或 字符 == "\t" 或 字符 == "\n" 或 字符 == "\r":
      设 起 为 起 加上 1
    否则:
      跳出
  返回 文本[起:长文]

段落 去除右侧空白 接收 文本:
  设 长文 为 长(文本)
  设 终 为 长文
  当 终 > 0:
    设 字符 为 文本[终 减去 1]
    如果 字符 == " " 或 字符 == "\t" 或 字符 == "\n" 或 字符 == "\r":
      设 终 为 终 减去 1
    否则:
      跳出
  返回 文本[0:终]

段落 去除所有空白 接收 文本:
  设 结果 为 ""
  设 i 为 0
  设 长文 为 长(文本)
  当 i < 长文:
    设 字符 为 文本[i]
    如果 字符 != " " 且 字符 != "\t" 且 字符 != "\n" 且 字符 != "\r":
      设 结果 为 结果 加上 字符
    设 i 为 i 加上 1
  返回 结果

段落 左填充 接收 文本, 宽度, 填充字符 = " ":
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  返回 重复字符(填充字符, 缺) 加上 文本

段落 右填充 接收 文本, 宽度, 填充字符 = " ":
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  返回 文本 加上 重复字符(填充字符, 缺)

段落 居中填充 接收 文本, 宽度, 填充字符 = " ":
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  设 左 为 缺 整除 2
  设 右 为 缺 减去 左
  返回 重复字符(填充字符, 左) 加上 文本 加上 重复字符(填充字符, 右)

段落 零填充 接收 文本, 宽度:
  设 缺 为 宽度 减去 长(文本)
  如果 缺 <= 0:
    返回 文本
  返回 重复字符("0", 缺) 加上 文本

段落 重复字符串 接收 文本, 次数:
  返回 文本 乘以 次数

段落 重复字符 接收 字符, 次数:
  设 结果 为 ""
  设 i 为 0
  当 i < 次数:
    设 结果 为 结果 加上 字符
    设 i 为 i 加上 1
  返回 结果

段落 字符串长度 接收 文本:
  返回 长(文本)

段落 字符计数 接收 文本, 字符:
  设 次数 为 0
  设 i 为 0
  设 长文 为 长(文本)
  当 i < 长文:
    如果 文本[i] == 字符:
      设 次数 为 次数 加上 1
    设 i 为 i 加上 1
  返回 次数

# ---------------- 查找 ----------------

段落 子串查找 接收 文本, 子串, 起始 = 0, 结束 = 空:
  设 长文 为 长(文本)
  设 长子 为 长(子串)
  如果 长子 == 0:
    返回 0
  设 终 为 长文
  如果 结束 != 空:
    设 终 为 结束
  设 i 为 起始
  当 i <= 终 减去 长子:
    如果 文本[i:i 加上 长子] == 子串:
      返回 i
    设 i 为 i 加上 1
  返回 -1

段落 子串查找最后 接收 文本, 子串, 起始 = 0, 结束 = 空:
  设 长文 为 长(文本)
  设 长子 为 长(子串)
  如果 长子 == 0:
    返回 0
  设 终 为 长文
  如果 结束 != 空:
    设 终 为 结束
  设 位置 为 -1
  设 i 为 起始
  当 i <= 终 减去 长子:
    如果 文本[i:i 加上 长子] == 子串:
      设 位置 为 i
    设 i 为 i 加上 1
  返回 位置

段落 包含子串 接收 文本, 子串:
  如果 子串查找(文本, 子串) >= 0:
    返回 真
  返回 假

段落 以子串开头 接收 文本, 前缀:
  设 长前 为 长(前缀)
  如果 长(文本) < 长前:
    返回 假
  返回 文本[0:长前] == 前缀

段落 以子串结尾 接收 文本, 后缀:
  设 长后 为 长(后缀)
  设 长文 为 长(文本)
  如果 长文 < 长后:
    返回 假
  返回 文本[长文 减去 长后:长文] == 后缀

段落 字符串对齐 接收 文本, 宽度, 对齐方式 = "left":
  如果 对齐方式 == "left":
    返回 右填充(文本, 宽度)
  如果 对齐方式 == "right":
    返回 左填充(文本, 宽度)
  如果 对齐方式 == "center":
    返回 居中填充(文本, 宽度)
  返回 文本

# ---------------- 分词 ----------------

段落 分词 接收 文本, 分隔符 = 空:
  返回 分割字符串(文本, 分隔符)

段落 中文分词 接收 文本:
  设 结果 为 新建列表()
  设 i 为 0
  设 长文 为 长(文本)
  当 i < 长文:
    追加(结果, 文本[i])
    设 i 为 i 加上 1
  返回 结果

段落 英文分词 接收 文本:
  返回 提取单词(文本)

段落 按长度分词 接收 文本, 长度:
  设 结果 为 新建列表()
  设 起点 为 0
  设 长文 为 长(文本)
  当 起点 < 长文:
    设 终点 为 起点 加上 长度
    如果 终点 > 长文:
      设 终点 为 长文
    追加(结果, 文本[起点:终点])
    设 起点 为 终点
  返回 结果

# ---------------- 编码 / 转码 ----------------
# 注：UTF8转GBK / GBK转UTF8 / Unicode转ASCII 直接复用 编码解码.light 同名导出
# （导入即再导出，避免跨模块同名段 IR 重定义 T5C-06；编码解码版为能力边界占位，
# 原生腿无 codecs，见 编码解码.light T6C 章节）。
# ASCII转Unicode 本地恒等实现：Python 定义为 ''.join(chr(ord(c)) for c in 文本)
# 对任意输入恒等于原串；与 编码解码.light 的解码版语义相反，故本地定义遮盖导入。

段落 字符转码 接收 文本, 源编码 = "utf-8", 目标编码 = "gbk":
  返回 字符集转换(文本, 源编码, 目标编码)

段落 ASCII转Unicode 接收 文本:
  返回 文本

段落 Unicode转ASCII 接收 文本:
  返回 编码解码.Unicode转ASCII(文本)

段落 UTF8转GBK 接收 文本:
  返回 编码解码.UTF8转GBK(文本)

段落 GBK转UTF8 接收 文本:
  返回 编码解码.GBK转UTF8(文本)

段落 字符集转换 接收 文本, 源编码, 目标编码:
  返回 编码解码.字符集转换(文本, 源编码, 目标编码)

段落 Base64编码 接收 文本:
  返回 编码解码.Base64编码(文本)

段落 Base64解码 接收 文本:
  返回 编码解码.Base64解码(文本)

段落 URL编码 接收 文本:
  返回 编码解码.URL编码(文本)

段落 URL解码 接收 文本:
  返回 编码解码.URL解码(文本)

段落 HTML编码 接收 文本:
  返回 HTML实体编码(文本)

段落 HTML解码 接收 文本:
  返回 HTML实体解码(文本)

段落 十六进制转字符串 接收 十六进制:
  返回 十六进制解码(十六进制)

段落 字符串转十六进制 接收 文本:
  返回 十六进制编码(文本)

段落 二进制转字符串 接收 二进制:
  返回 编码解码.二进制转字符串(二进制)

段落 字符串转二进制 接收 文本:
  返回 编码解码.字符串转二进制(文本)

段落 MD5哈希 接收 文本:
  返回 MD5(文本)

段落 SHA256哈希 接收 文本:
  返回 SHA256(文本)

段落 生成随机字符串 接收 长度 = 10, 字符集 = "abcdefghijklmnopqrstuvwxyz0123456789":
  设 结果 为 ""
  设 i 为 0
  当 i < 长度:
    设 下标 为 随机下标(长(字符集))
    设 结果 为 结果 加上 字符集[下标]
    设 i 为 i 加上 1
  返回 结果

段落 随机下标 接收 范围:
  设 浮 为 随机数()
  设 整 为 整数(浮 乘以 范围)
  如果 整 >= 范围:
    返回 范围 减去 1
  返回 整

段落 生成UUID 接收:
  返回 生成UUID4()

# ---------------- 验证 ----------------
# 正则校验类委托 re.light 原语（完全匹配 + re_花括号 拼 {n,m}），与
# 正则表达式.light 内部写法一致；原生 codegen 不支持「导入后原样再导出」，
# 故本地实现（对齐 字符串工具.py 的本地 re.match 写法）。
# 验证数字/字母/字母数字/空白/标题/大小写 为纯逻辑函数本地实现。

段落 验证邮箱 接收 邮箱:
  设 模式 为 "^[a-zA-Z0-9._%+-]+@[a-zA-Z0-9.-]+\\.[a-zA-Z]" 加上 re_花括号(2, -1)
  返回 完全匹配(模式 加上 "$", 邮箱)

段落 验证手机号 接收 手机号:
  返回 完全匹配("^1[3-9]\\d" 加上 re_花括号(9, 9) 加上 "$", 手机号)

段落 验证身份证号 接收 身份证号:
  返回 完全匹配("^\\d" 加上 re_花括号(17, 17) 加上 "[\\dXx]$", 身份证号)

段落 验证URL 接收 URL:
  返回 完全匹配("^https?://[^\\s/$.?#].[^\\s]*$", URL)

段落 验证IP地址 接收 IP地址:
  设 段 为 "(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)"
  设 模式 为 "^(" 加上 段 加上 "\\.)" 加上 re_花括号(3, 3) 加上 段 加上 "$"
  返回 完全匹配(模式, IP地址)

段落 验证日期 接收 日期:
  设 模式 为 "^\\d" 加上 re_花括号(4, 4)
  设 模式 为 模式 加上 "-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12]\\d|3[01])$"
  返回 完全匹配(模式, 日期)

段落 验证数字 接收 文本:
  如果 长(文本) == 0:
    返回 假
  设 i 为 0
  当 i < 长(文本):
    如果 是数字字符(文本[i]) == 假:
      返回 假
    设 i 为 i 加上 1
  返回 真

段落 是数字字符 接收 字符:
  设 码 为 码位(字符)
  返回 码 >= 48 且 码 <= 57

段落 验证字母 接收 文本:
  如果 长(文本) == 0:
    返回 假
  设 i 为 0
  当 i < 长(文本):
    如果 是字母字符(文本[i]) == 假:
      返回 假
    设 i 为 i 加上 1
  返回 真

段落 是字母字符 接收 字符:
  设 码 为 码位(字符)
  如果 码 >= 65 且 码 <= 90:
    返回 真
  如果 码 >= 97 且 码 <= 122:
    返回 真
  如果 码 >= 19968 且 码 <= 40959:
    返回 真
  返回 假

段落 验证字母数字 接收 文本:
  如果 长(文本) == 0:
    返回 假
  设 i 为 0
  当 i < 长(文本):
    设 字符 为 文本[i]
    如果 是数字字符(字符) == 假 且 是字母字符(字符) == 假:
      返回 假
    设 i 为 i 加上 1
  返回 真

段落 验证空白 接收 文本:
  如果 长(文本) == 0:
    返回 假
  设 i 为 0
  当 i < 长(文本):
    设 字符 为 文本[i]
    如果 字符 != " " 且 字符 != "\t" 且 字符 != "\n" 且 字符 != "\r":
      返回 假
    设 i 为 i 加上 1
  返回 真

段落 验证标题 接收 文本:
  设 长文 为 长(文本)
  如果 长文 == 0:
    返回 假
  设 有字母 为 假
  设 i 为 0
  当 i < 长文:
    设 字符 为 文本[i]
    如果 是字母字符(字符):
      设 有字母 为 真
      设 是小写 为 字符 == 字符.转小写() 且 字符 != 字符.转大写()
      如果 是小写:
        如果 i == 0:
          返回 假
        设 前 为 文本[i 减去 1]
        如果 是字母字符(前) == 假:
          返回 假
    设 i 为 i 加上 1
  返回 有字母

段落 验证小写 接收 文本:
  如果 长(文本) == 0:
    返回 假
  返回 文本 == 文本.转小写()

段落 验证大写 接收 文本:
  如果 长(文本) == 0:
    返回 假
  返回 文本 == 文本.转大写()

# ---------------- 正则提取 / 清洗 ----------------
# 委托 re.light 原语（查找所有/替换 + re_花括号 拼 {n,m}），本地实现：
# 原生 codegen 不支持「导入后原样再导出」，故不复用 正则表达式.light 同名导出。

段落 提取HTML标签 接收 文本:
  返回 查找所有("<[^>]+>", 文本)

段落 去除HTML标签 接收 文本:
  返回 替换("<[^>]+>", 文本, "")

段落 提取文本中的数字 接收 文本:
  返回 查找所有("-?\\d+\\.?\\d*", 文本)

段落 提取文本中的邮箱 接收 文本:
  设 模式 为 "[a-zA-Z0-9._%+-]+@[a-zA-Z0-9.-]+\\.[a-zA-Z]" 加上 re_花括号(2, -1)
  返回 查找所有(模式, 文本)

段落 提取文本中的手机号 接收 文本:
  返回 查找所有("1[3-9]\\d" 加上 re_花括号(9, 9), 文本)

段落 提取文本中的URL 接收 文本:
  返回 查找所有("https?://[^\\s/$.?#].[^\\s]*", 文本)

段落 去除标点符号 接收 文本:
  设 结果 为 ""
  设 i 为 0
  设 长文 为 长(文本)
  当 i < 长文:
    设 字符 为 文本[i]
    如果 是数字字符(字符) 或 是字母字符(字符) 或 字符 == " " 或 字符 == "\t" 或 字符 == "\n":
      设 结果 为 结果 加上 字符
    设 i 为 i 加上 1
  返回 结果

段落 去除特殊字符 接收 文本:
  设 结果 为 ""
  设 i 为 0
  设 长文 为 长(文本)
  当 i < 长文:
    设 字符 为 文本[i]
    设 码 为 码位(字符)
    如果 是数字字符(字符) 或 是字母字符(字符) 或 (码 >= 19968 且 码 <= 40959) 或 字符 == " " 或 字符 == "\t" 或 字符 == "\n":
      设 结果 为 结果 加上 字符
    设 i 为 i 加上 1
  返回 结果

段落 替换换行符 接收 文本, 替换为 = " ":
  返回 替换("\\r?\\n", 文本, 替换为)

段落 替换多个空白 接收 文本, 替换为 = " ":
  返回 替换("\\s+", 文本, 替换为)

段落 文本清洗 接收 文本:
  设 结果 为 去除首尾空白(文本)
  设 结果 为 替换多个空白(结果)
  设 结果 为 去除特殊字符(结果)
  返回 结果

段落 生成哈希值 接收 文本, 算法 = "md5":
  如果 算法 == "md5":
    返回 MD5(文本)
  如果 算法 == "sha1":
    返回 SHA1(文本)
  如果 算法 == "sha256":
    返回 SHA256(文本)
  如果 算法 == "sha512":
    返回 SHA512(文本)
  抛出 新建 异常("未知算法: " 加上 算法)

段落 字符串比较 接收 文本1, 文本2, 忽略大小写 = 假:
  如果 忽略大小写:
    如果 文本1.转小写() == 文本2.转小写():
      返回 真
    返回 假
  如果 文本1 == 文本2:
    返回 真
  返回 假

# Levenshtein 相似度。用扁平一维数组 dp，下标采用 1-based 偏移布局
# (r+1)*宽 + (c+1)，规避 native codegen「从列表下标 0 取值赋给变量返回 []」的缺陷。
段落 字符串相似度 接收 文本1, 文本2:
  设 n 为 长(文本1)
  设 m 为 长(文本2)
  如果 n == 0 且 m == 0:
    返回 1.0
  设 宽 为 m 加上 2
  设 尺寸 为 (n 加上 2) 乘以 宽
  设 dp 为 []
  设 k 为 0
  当 k < 尺寸:
    dp.追加(0)
    设 k 为 k 加上 1
  # 第 0 行（逻辑行 r=0 存于物理行 1）
  设 c 为 0
  当 c <= m:
    设 写位 为 1 乘以 宽 加上 (c 加上 1)
    设 dp[写位] 为 c
    设 c 为 c 加上 1
  # 第 0 列（逻辑列 c=0 存于物理列 1）
  设 r 为 0
  当 r <= n:
    设 写位 为 (r 加上 1) 乘以 宽 加上 1
    设 dp[写位] 为 r
    设 r 为 r 加上 1
  # 填充
  设 i 为 1
  当 i <= n:
    设 j 为 1
    当 j <= m:
      设 左上位 为 i 乘以 宽 加上 j
      设 左上 为 dp[左上位]
      设 上位 为 i 乘以 宽 加上 (j 加上 1)
      设 上 为 dp[上位]
      设 左位 为 (i 加上 1) 乘以 宽 加上 j
      设 左 为 dp[左位]
      设 成本 为 1
      如果 文本1[i 减去 1] == 文本2[j 减去 1]:
        设 成本 为 0
      设 删代价 为 上 加上 1
      设 插代价 为 左 加上 1
      设 替代价 为 左上 加上 成本
      设 最优 为 删代价
      如果 插代价 < 最优:
        设 最优 为 插代价
      如果 替代价 < 最优:
        设 最优 为 替代价
      设 写位 为 (i 加上 1) 乘以 宽 加上 (j 加上 1)
      设 dp[写位] 为 最优
      设 j 为 j 加上 1
    设 i 为 i 加上 1
  设 末位 为 (n 加上 1) 乘以 宽 加上 (m 加上 1)
  设 距离 为 dp[末位]
  设 最大 为 n
  如果 m > 最大:
    设 最大 为 m
  返回 1.0 减去 (距离 除以 最大)

段落 查找所有出现位置 接收 文本, 子串:
  设 位置 为 新建列表()
  设 起始 为 0
  设 长文 为 长(文本)
  当 起始 <= 长文:
    设 idx 为 子串查找(文本, 子串, 起始)
    如果 idx == -1:
      返回 位置
    追加(位置, idx)
    设 起始 为 idx 加上 1
  返回 位置

段落 安全字符串 接收 文本, 最大长度 = 1000:
  设 截断 为 文本
  如果 长(文本) > 最大长度:
    设 截断 为 文本[0:最大长度]
  返回 替换("[<>\"]", 截断, "")

段落 格式化数字 接收 数字, 小数位数 = 2:
  返回 格式化定点(数字, 小数位数)

段落 格式化金额 接收 金额:
  设 文本 为 格式化定点(金额, 2)
  返回 插入千分位(文本)

段落 格式化百分比 接收 数值:
  设 百分 为 数值 乘以 100
  返回 格式化定点(百分, 2) 加上 "%"

# 内部：定点格式化 数值 到 小数位数 位小数
段落 格式化定点 接收 数值, 小数位数:
  设 比例 为 1
  设 k 为 0
  当 k < 小数位数:
    设 比例 为 比例 乘以 10
    设 k 为 k 加上 1
  设 缩放 为 整数((数值 乘以 比例) 加上 0.5)
  设 整 为 缩放 整除 比例
  设 小 为 缩放 减去 整 乘以 比例
  设 小数文本 为 转文本(小)
  设 需要 为 小数位数 减去 长(小数文本)
  设 前导 为 ""
  设 p 为 0
  当 p < 需要:
    设 前导 为 前导 加上 "0"
    设 p 为 p 加上 1
  返回 转文本(整) 加上 "." 加上 前导 加上 小数文本

# 内部：给 "1234.56" 这类定点串插入千分位逗号（从左到右，避免反转字符串）
段落 插入千分位 接收 文本:
  设 点 为 子串查找(文本, ".")
  设 整数部分 为 文本
  设 小数部分 为 ""
  如果 点 >= 0:
    设 整数部分 为 文本[0:点]
    设 小数部分 为 文本[点:长(文本)]
  设 结果 为 ""
  设 长整 为 长(整数部分)
  设 i 为 0
  当 i < 长整:
    设 结果 为 结果 加上 整数部分[i]
    设 右数 为 长整 减去 1 减去 i
    设 余 为 右数 减去 (右数 整除 3) 乘以 3
    如果 i < 长整 减去 1 且 余 == 0:
      设 结果 为 结果 加上 ","
    设 i 为 i 加上 1
  返回 结果 加上 小数部分
