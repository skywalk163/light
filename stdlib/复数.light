# 纯光明实现 —— 复数运算（取代同名 .py 壳）。
#
# 对齐 stdlib/复数.py 的导出。原生腿无类可实例化，复数以列表表示：
# [实部, 虚部]。类方法（共轭/模/幅角/实部/虚部）以独立段落等价提供
# （复数实部 / 复数虚部），实现同能力、独立命名。
#
# 幅角使用纯光明反正切2（级数 + 角度减半化简，全象限），|误差| < 4e-7
# （对拍容差 1e-4）。O0 已知规避：不写负数字面量（0 - x）。

导出 复数 创建复数 极坐标转复数 复数加法 复数减法 复数乘法 复数除法。
导出 复数模 复数幅角 复数共轭 复数幂 复数相等 复数实部 复数虚部。

# ── 纯光明反正切2（与《向量》模块同算法，独立命名避免跨模块段名冲突）──

段落 复数反正切级数 接收 t:
  设 t2 为 t 乘 t
  设 t3 为 t2 乘 t
  设 t5 为 t3 乘 t2
  设 t7 为 t5 乘 t2
  设 t9 为 t7 乘 t2
  设 t11 为 t9 乘 t2
  设 t13 为 t11 乘 t2
  设 t15 为 t13 乘 t2
  返回 t - t3 / 3.0 + t5 / 5.0 - t7 / 7.0 + t9 / 9.0 - t11 / 11.0 + t13 / 13.0 - t15 / 15.0

段落 复数反正切正 接收 t:
  如果 t > 1.0:
    设 r 为 1.0 / t
    返回 1.5707963267948966 - 复数反正切正(r)
  如果 t > 0.5:
    设 u 为 (t - 1.0) / (t + 1.0)
    返回 0.7853981633974483 + 复数反正切级数(u)
  返回 复数反正切级数(t)

段落 复数反正切2 接收 y, x:
  设 yf 为 y + 0.0
  设 xf 为 x + 0.0
  如果 xf == 0.0:
    如果 yf > 0.0:
      返回 1.5707963267948966
    如果 yf < 0.0:
      返回 0.0 - 1.5707963267948966
    返回 0.0
  设 基础 为 复数反正切正(_light_builtin.绝对值(yf / xf))
  如果 xf > 0.0:
    如果 yf >= 0.0:
      返回 基础
    返回 0.0 - 基础
  如果 yf >= 0.0:
    返回 3.141592653589793 - 基础
  返回 基础 - 3.141592653589793

# ── 复数（[实部, 虚部] 列表表示）──

段落 复数 接收 实部 = 0, 虚部 = 0:
  返回 [实部, 虚部]

段落 创建复数 接收 实部 = 0, 虚部 = 0:
  返回 [实部, 虚部]

段落 极坐标转复数 接收 模长入, 幅角:
  返回 [模长入 * _light_builtin.余弦(幅角), 模长入 * _light_builtin.正弦(幅角)]

段落 复数加法 接收 z1, z2:
  返回 [z1[0] + z2[0], z1[1] + z2[1]]

段落 复数减法 接收 z1, z2:
  返回 [z1[0] - z2[0], z1[1] - z2[1]]

段落 复数乘法 接收 z1, z2:
  返回 [z1[0] * z2[0] - z1[1] * z2[1], z1[0] * z2[1] + z1[1] * z2[0]]

段落 复数除法 接收 z1, z2:
  设 分母 为 z2[0] * z2[0] + z2[1] * z2[1]
  如果 分母 == 0:
    抛出 运行时错误("除数不能为零")
  设 分母浮 为 分母 + 0.0
  设 实 为 (z1[0] * z2[0] + z1[1] * z2[1]) / 分母浮
  设 虚 为 (z1[1] * z2[0] - z1[0] * z2[1]) / 分母浮
  返回 [实, 虚]

段落 复数模 接收 z:
  返回 _light_builtin.平方根(z[0] * z[0] + z[1] * z[1])

段落 复数幅角 接收 z:
  返回 复数反正切2(z[1], z[0])

段落 复数共轭 接收 z:
  返回 [z[0], 0.0 - z[1]]

段落 复数幂 接收 z, n:
  设 模长 为 复数模(z)
  设 角 为 复数幅角(z)
  设 新模 为 _light_builtin.幂(模长, n)
  设 新角 为 角 * n
  返回 [新模 * _light_builtin.余弦(新角), 新模 * _light_builtin.正弦(新角)]

段落 复数相等 接收 z1, z2:
  如果 z1[0] == z2[0]:
    如果 z1[1] == z2[1]:
      返回 真
  返回 假

段落 复数实部 接收 z:
  返回 z[0]

段落 复数虚部 接收 z:
  返回 z[1]
