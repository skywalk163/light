# 数学工具模块

导出 《求和》。
导出 《求积》。
导出 《平均值》。

《求和》段(数组):
    定义 总和 等于 0。
    定义 i 等于 0。
    当 i 小于 listLen(数组):
        总和 等于 总和 加 数组[i]。
        i 等于 i 加 1。
    结束。
    返回 总和。
结束。

《求积》段(数组):
    定义 积 等于 1。
    定义 i 等于 0。
    当 i 小于 listLen(数组):
        积 等于 积 乘 数组[i]。
        i 等于 i 加 1。
    结束。
    返回 积。
结束。

《平均值》段(数组):
    如果 listLen(数组) 等于 0 那么:
        返回 0。
    结束。
    返回 《求和》(数组) 除 listLen(数组)。
结束。