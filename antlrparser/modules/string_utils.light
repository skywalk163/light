# 字符串工具模块

导出 《反转串》。
导出 《首字母大写》。
导出 《分割串》。

《反转串》段(文本):
    定义 结果 等于 ""。
    定义 i 等于 len(文本) 减 1。
    当 i 大于等于 0:
        结果 等于 结果 加 substring(文本, i, i加1)。
        i 等于 i 减 1。
    结束。
    返回 结果。
结束。

《首字母大写》段(文本):
    如果 len(文本) 等于 0 那么:
        返回 文本。
    结束。
    定义 首字母 等于 upper(substring(文本, 0, 1))。
    定义 其余 等于 substring(文本, 1, len(文本))。
    返回 首字母 加 其余。
结束。

《分割串》段(文本, 分隔符):
    返回 split(文本, 分隔符)。
结束。