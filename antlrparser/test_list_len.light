# 光明测试程序 - 列表长度

《主段》段():
  打印("=== 列表长度测试 ===")。
  
  # 创建列表
  定义arr等于【10, 20, 30, 40, 50】。
  
  # 获取长度
  定义len等于arr之长度。
  打印("列表长度: ")。
  打印(len)。
  
  # 测试索引访问
  定义sum等于0。
  定义i等于0。
  当i小于len：
    sum等于sum加arr[i]。
    i等于i加1。
  结束。
  
  打印("元素总和: ")。
  打印(sum)。
  
  # 测试空列表
  定义empty等于【】。
  定义empty_len等于empty之长度。
  打印("空列表长度: ")。
  打印(empty_len)。
  
  打印("")。
  打印("=== 测试完成 ===")。
结束。