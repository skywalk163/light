# 光明测试程序 - 简化版

《主段》段():
  打印("=== 列表测试 ===")。
  
  # 创建列表
  定义arr等于【10, 20, 30, 40, 50】。
  
  # 获取长度
  定义len等于arr之长度。
  打印("长度: ")。
  打印(len)。
  
  # 访问元素
  定义first等于arr[0]。
  打印("第一个: ")。
  打印(first)。
  
  打印("")。
  打印("=== 测试完成 ===")。
结束。