# 高级模块 - 测试嵌套导入（该模块自身导入 mod_util）
# 被 test_module.py 的测试用例导入

导出《计算表达式》。
导出《拼接字符串》。

从 mod_util 导入《双倍》，《连接》。

《计算表达式》段(数值):
  定义_a等于《双倍》(数值)。
  返回_a加数值。
结束。

《拼接字符串》段(甲, 乙, 丙):
  返回《连接》(《连接》(甲, 乙), 丙)。
结束。