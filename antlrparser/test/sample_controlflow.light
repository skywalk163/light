# 光明语法测试文件 - 控制流语句

# if/else if/else 条件判断
定义分数等于85。

如果分数大于等于90那么：
  打印'优秀'。
否则若分数大于等于80那么：
  打印'良好'。
否则若分数大于等于60那么：
  打印'及格'。
否则：
  打印'不及格'。
结束。

# if 简单形式（无else）
定义甲等于真。
如果甲那么：
  打印甲。
结束。

# 遍历循环
定义列表等于【1, 2, 3】。
遍历 项 列表：
  打印项。
结束。

# 当循环
定义计数等于3。
当计数大于0：
  打印计数。
  定义计数等于计数减1。
结束。

# 跳出/跳过
定义索引等于1。
当索引小于10：
  如果索引等于5那么：
    定义索引等于索引加1。
    跳过。
  结束。
  如果索引等于8那么：
    跳出。
  结束。
  定义索引等于索引加1。
结束。