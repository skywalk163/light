# 光明语法综合测试 - v1.6语法规范

# 1. 基本变量声明
设 甲 为 10。
设 乙 为 20。
设 结果 为 甲 加 乙。
打印 结果。

# 2. 单参数函数
段落 平方 接收 数值：
  返回 数值 乘 数值。
结束。

设 平方值 为 平方(5)。
打印 平方值。

# 3. 多参数函数
段落 求和 接收 甲, 乙：
  返回 甲 加 乙。
结束。

设 和值 为 求和(10, 20)。
打印 和值。

# 4. 条件语句
设 分数 为 85。
如果 分数 大于 等于 60：
  打印 "及格"。
否则：
  打印 "不及格"。
结束。

# 5. 当循环
设 计数 为 0。
当 计数 小于 3：
  打印 计数。
  设 计数 为 计数 加 1。
结束。

# 6. 遍历循环
设 水果 为 ["苹果", "香蕉", "橙子"]。
遍历 水果项 之 水果：
  打印 水果项。
结束。

# 7. 列表操作
设 数字列表 为 [1, 2, 3, 4, 5]。
打印 数字列表[0]。
打印 数字列表[2]。

# 8. 字典操作
设 学生信息 为 ["姓名"： "张三", "年龄"： 18, "分数"： 95]。
打印 学生信息["姓名"]。
打印 学生信息["分数"]。

# 9. 嵌套函数调用
段落 三倍 接收 数值：
  返回 数值 乘 3。
结束。

段落 九倍 接收 数值：
  返回 三倍(三倍(数值))。
结束。

打印 九倍(2)。

# 10. 递归函数
段落 阶乘 接收 数值：
  如果 数值 小于等于 1：
    返回 1。
  否则：
    返回 数值 乘 阶乘(数值 减 1)。
  结束。
结束。

打印 阶乘(5)。

# 11. 异常处理
尝试：
  设 错误值 为 10 除以 0。
  打印 错误值。
捕获 异常信息：
  打印 "捕获到异常："。
  打印 异常信息。
结束。

# 12. 类定义（待完善）
# 类 动物：
#   属性 名称。
#   构造 接收 名称：
#     己名称 为 名称。
#   结束。
#   段落 叫声 接收：
#     打印 "动物叫声"。
#   结束。
# 结束。

# 设 小狗 为 新建 动物 "旺财"。
# 小狗.叫声()。

打印 "所有测试完成！"。
