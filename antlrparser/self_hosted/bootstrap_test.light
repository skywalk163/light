# 光明自举完整测试
# 在光明环境中测试光明版解释器执行光明代码

# 加载所有必要的组件
从 tokenizer 导入《分词器》。
从 ast 导入《创建模块》。
从 parser 导入《解析》。
从 interpreter 导入《_run》。

# 测试用例
《测试算术运算》段():
  打印("=== 测试算术运算 ===")。
  
  _code等于"定义a等于10加20。定义b等于a乘3。定义c等于b减10。定义d等于c除5。打印(a)。打印(b)。打印(c)。打印(d)。"。
  
  # 分词
  _tokens等于《分词器》(_code)。
  打印("分词完成: "加_tokens之长度加" tokens")。
  
  # 解析
  _result等于《解析》(_tokens)。
  如果_result["_type"]等于"Error"那么：
    打印("解析失败: "加_result["message"])。
    返回假。
  结束。
  
  _ast等于_result["result"]。
  打印("解析成功")。
  
  # 执行
  《_run》(_ast)。
  
  打印("测试完成")。
  返回真。
结束。

《测试条件语句》段():
  打印("=== 测试条件语句 ===")。
  
  _code等于"定义x等于100。如果x大于50那么：打印(\"大于50\")。否则：打印(\"不大于50\")。结束。"。
  
  _tokens等于《分词器》(_code)。
  _result等于《解析》(_tokens)。
  如果_result["_type"]等于"Error"那么：
    打印("解析失败: "加_result["message"])。
    返回假。
  结束。
  
  《_run》(_result["result"])。
  打印("测试完成")。
  返回真。
结束。

《测试循环》段():
  打印("=== 测试循环 ===")。
  
  _code等于"定义i等于0。定义sum等于0。当i小于5：sum等于sum加i。i等于i加1。结束。打印(sum)。"。
  
  _tokens等于《分词器》(_code)。
  _result等于《解析》(_tokens)。
  如果_result["_type"]等于"Error"那么：
    打印("解析失败: "加_result["message"])。
    返回假。
  结束。
  
  《_run》(_result["result"])。
  打印("测试完成")。
  返回真。
结束。

《测试段落调用》段():
  打印("=== 测试段落调用 ===")。
  
  _code等于"《平方》段(n): 返回n乘n。结束。打印(《平方》(5))。"。
  
  _tokens等于《分词器》(_code)。
  _result等于《解析》(_tokens)。
  如果_result["_type"]等于"Error"那么：
    打印("解析失败: "加_result["message"])。
    返回假。
  结束。
  
  《_run》(_result["result"])。
  打印("测试完成")。
  返回真。
结束。

# 运行所有测试
打印("开始光明自举测试...")。
打印()。

《测试算术运算》()。
打印()。

《测试条件语句》()。
打印()。

《测试循环》()。
打印()。

《测试段落调用》()。
打印()。

打印("所有测试完成！")。