# 光明自举测试
# 用光明代码测试光明版解释器

# 加载分词器
从 tokenizer 导入《分词器》。

# 加载解析器相关
从 ast 导入《创建模块》。
从 parser 导入《解析》。

# 加载解释器
从 interpreter 导入《_run》。

# 测试代码
《测试解释器》段():
  打印("=== 测试光明版解释器 ===")。
  
  # 测试代码
  _testCode等于"定义x等于10。定义y等于20。定义z等于x加y。打印(z)。"。
  
  # 分词
  _tokens等于《分词器》(_testCode)。
  打印("分词完成: "加_tokens之长度加" 个Token")。
  
  # 解析
  _parsed等于《解析》(_tokens)。
  如果_parsed["_type"]等于"Error"那么：
    打印("解析失败: "加_parsed["message"])。
    返回假。
  结束。
  
  _ast等于_parsed["result"]。
  打印("解析成功")。
  
  # 执行
  《_run》(_ast)。
  
  打印("测试完成！")。
  返回真。
结束。

# 运行测试
《测试解释器》()。