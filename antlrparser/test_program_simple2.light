# 光明测试程序 - 简单列表操作

《主段》段():
  打印("=== 列表测试 ===")。
  
  # 创建列表
  定义arr等于【10, 20, 30, 40, 50】。
  
  # 访问元素
  定义first等于arr[0]。
  定义second等于arr[1]。
  定义last等于arr[4]。
  
  打印("第一个: ")。
  打印(first)。
  打印("第二个: ")。
  打印(second)。
  打印("最后一个: ")。
  打印(last)。
  
  # 计算总和
  定义sum等于first加second加last。
  打印("总和: ")。
  打印(sum)。
  
  打印("")。
  打印("=== 测试完成 ===")。
结束。