# D4-5 查证与留档：捕获流编码护栏

**任务书判据**（D4-5）：① 全仓哪些文件调用了 `sys.stdout/stderr.reconfigure(`、
各自有没有带 `errors='replace'`；② `tests/unit/test_capture_encoding_guard.py`
存在吗；③ `tests/conftest.py` 里到底修了什么。护栏 AST 级扫全仓，缺 `errors` 即红；
反跑：临时加不带 errors 的 reconfigure 探针 → 护栏红 → 删掉恢复绿。

## 一、查证结论（以实测为准）

1. **`tests/unit/test_capture_encoding_guard.py` 存在**（5692 字节，AST 级护栏，
   4 条测试：现状扫描 + 收集面对齐 pyproject + 反例抓取 + 收集面非空）。
   memory 里「reset 丢了」的说法**是错的**，第三轮总纲 §10 的说法正确。
   结论：**无需新建**，护栏已在。
2. **`tests/conftest.py` 第三轮改的两处**（以实测为准）：
   - 统一 `sys.path.insert`：按 `__file__` 推导仓库根，解决 c:/traework 绝对路径
     换机后 ImportError 的问题；
   - `_preserve_stdout` autouse fixture：恢复被关闭/改写的 stdout/stderr，
     防止跨用例污染。
3. **全仓 `reconfigure` 普查**（AST 级，排除 .git/archive/__pycache__ 等）：

   | 类别 | 数量 | 位置 |
   |---|---|---|
   | `sys.stdout/stderr.reconfigure` 带 `errors=` | 4 | `tests/test_class_definition.py:18`、`tests/test_comprehensive.py:13`、`tests/test_summary.py:10`、`tools/gen_lightpub_docs.py:39` |
   | `sys.stdout/stderr.reconfigure` **缺 `errors=`** | 15 | 见下方移交清单 |
   | 非 sys 流的 reconfigure（不相关） | 1 | `tools/ai_copilot/syntax_card.py:379` |
   | ast.parse 失败（扫描盲区） | 2 | `bootstrap/level6_test_output.py:370`、`stdlib/拼音转换.py:25` |

4. **任务书点名的三个 test_*.py 全部已带 `errors='replace'`**（test_summary.py:10、
   test_class_definition.py:18、test_comprehensive.py:13）→ 移交清单**不包含**它们。
5. **护栏扫描面**：`test_*.py` + `_test_*.py` + `conftest.py`（对齐 pyproject
   `python_files`），排除 `archive/`、`__pycache__/`。**15 处缺 errors 的调用
   全在扫描面之外**（cli/、tools/ai_copilot/、积木库/评估/）——所以护栏现状绿
   不是「把问题藏起来了」，而是「这些文件不会被 pytest 收集、顶层副作用不存在」。

## 二、现状绿（不是「我加了个永远绿的东西」）

```
$ python -m pytest tests/unit/test_capture_encoding_guard.py -v
test_被收集的模块里reconfigure必须显式给errors PASSED   [ 25%]
test_收集面排掉的目录确实不被pytest收集 PASSED         [ 50%]
test_护栏能抓到反例 PASSED                             [ 75%]
test_收集面不为空 PASSED                               [100%]
============================== 4 passed in 1.76s ==============================
```

## 三、反跑证明（判据 2：探针必须红）

```
步骤1 现状：           4 passed（rc=0）
步骤2 塞探针 tests/_test_taskD4_probe.py
     （sys.stdout.reconfigure(encoding='utf-8')，缺 errors）：
     E assert not ['tests/_test_taskD4_probe.py:5 sys.stdout.reconfigure']
     1 failed, 3 passed（rc=1）—— 护栏点名「文件:行号 流.reconfigure」
步骤3 移走探针：       4 passed（rc=0）
```

探针证物：`第四轮留档/证物_D4-5_探针_reconfigure缺errors.py.md`。

## 四、移交清单（15 处缺 `errors=`，全在 D4 独占清单之外）

| # | 文件:行 | 位置形态 | 风险 |
|---|---|---|---|
| 1 | `积木库/评估/ci_eval.py:53` | **模块顶层**，try/except 包裹 | **高**：gitea 积木库门禁直接 `python 积木库/评估/ci_eval.py --并发 8`，顶层执行即触发 |
| 2 | `积木库/评估/跑分.py:36` | **模块顶层**，try/except 包裹 | **高**：同上，跑分脚本顶层执行 |
| 3 | `cli/light.py:1080` | 函数 `_ensure_utf8()` 体内 | 中：仅 AI Copilot 子命令路径触发 |
| 4-15 | `tools/ai_copilot/*.py` 共 12 处（build_sft_dataset.py:2203、build_sft_dataset_v10.py:1591、cli.py:119、diagnose_loss.py:169、download_model.py:144、local_infer.py:573、merge_and_convert.py:349、pipeline.py:444、prompt_generator.py:180、snippets.py:231、train_cpu_lora.py:546、train_gpu_lora.py:1038） | 函数体内 | 低：工具链脚本，非 CI 判绿路径 |

**移交建议**：1、2 号先修（模块顶层 + CI 直接调用，`errors='replace'` 一行）。
修复动作统一为 `sys.stdout.reconfigure(encoding='utf-8', errors='replace')`。
修完后护栏覆盖不了这些文件（不在收集面），靠 `git grep "reconfigure(encoding"` 复查。
