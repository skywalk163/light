# D4-6 合并预演：四路合并后的最终数字（预估值，合并点需复核）

**执行方式**：临时 detached worktree 合并 task-A4 + task-B4 + task-C4 + D4 工作区改动，
在合并结果上重建基线、跑自举率。**这是预估值**——真实合并点以 main 上实际合并后的
提交为准复核；若三路后续再改，数字会变。

## 合并结果

- 四路合并**零冲突**（A4→B4→C4→D4 顺序，D4 的 tools/ci + workflows + docs 改动无重叠）。
- 三路分支当前状态：A4 领先 main 2 提交（b860d538）、B4 领先 1（b649efb4）、
  C4 领先 1（470f8d25）。**均未进 main**。

## 假测试基线（D4-1 形态）

| 项 | 本轮单 D4（e1dbfc3a） | 四路合并预演 |
|---|---|---|
| 总条数 | 394 | **358** |
| string-assert | 101 | 101（未变） |
| assertin-string | 113 | 113（未变） |
| upper-bound | 2 | 2（未变） |
| lower-bound | 27 | **26**（C4 修掉 1） |
| not-none | 151 | **152**（B4/C4 各带进 1） |
| trivial-ge0 / returncode-in | 0（预防性） | 0（预防性） |

**358 的来源**：合并后相比单 D4 基线，消失 38 条 = test_async.py 37 条（C4 重写
test_async.py，把 802 行大改，存量违规清掉大半）+ test_agent_tools_light.py 1 条
（B4 修掉）；新增 2 条 = test_agent_tools_light.py 1（B4）+ test_async.py 1（C4）。

**说明**：任务书 §D4-1 的「总数落 390–400」是按「三路未动」预估的；C4 的
test_async.py 重写（802 行变更）清掉了其 37 条存量违规，故合并后总数降到 358。
这**不是** D4 口径问题，是 C4 交付的实际效果——合并点复核时按实际数字写报告，
并注明与 390-400 预估的偏差原因（C4 清存量）。

## 自举率（D4-2 形态）

| 项 | 本轮单 D4 | 四路合并预演 |
|---|---|---|
| 文件维度 | 17/69 = 24.64% | **18/70 = 25.71%** |
| 影子数 | 53 | **53** |
| 防造假命中 | 0 | 0 |

**18/70 的来源**：C4 新增 `stdlib/选择器.light`（有实现、无影子、无自导入），
分子分母同时 +1 → 自举率不降反升。合并点需 `--write-baseline` 刷新
bootstrap_rate_baseline.json（version 2，file_dim_rate=0.2571...，shadow_count=53）。

## 合并点动作清单（前三路进 main 后执行）

1. 重建假测试基线：`python tools/ci/assert_quality.py --root tests --write-baseline tools/ci/assert_quality_baseline.json`（预计 358 条）
2. 刷新自举率基线：`python tools/ci/bootstrap_rate.py --root . --write-baseline tools/ci/bootstrap_rate_baseline.json`（预计 18/70 = 25.71%，shadow 53）
3. 复核 C4 的 37 条 test_async.py 存量清理是否符合预期（预演显示已全部消失）
4. 把最终数字写进 `docs/自举率报告.md`，把 run 号 + status + 耗时写进 `第四轮留档/` 与合并报告
5. `docs/known_issues.md` 补本轮的移交项（见 D4-5 留档的 15 处 reconfigure）
