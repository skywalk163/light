====================================================================
D4-1 反跑证明：三个新增形态各造一次回退，看门禁红不红
口径：探针写进 tests/_taskD4_probe.py -> 门禁必须 rc=1 并点名该行
      -> 删掉探针 -> 门禁必须恢复 rc=0
基线：tools/ci/assert_quality_baseline.json（394 条，built_from e1dbfc3aa1e2）
合并期附注：本文所有数字是**合并前**（task-D4 分支、基线 394 条）的快照，照原样
      重跑不会复现——C4/A4/B4 进 main 后基线降到 358，主线又回补了下界形态的
      `> N` 漏拼法后升到 424。反跑的判据与结论仍成立（新增即红、撤掉即绿），
      变的只是分母。当前真值见 docs/known_issues.md 14.4。
====================================================================

---------- 步骤 0：动手前的干净态（必须 rc=0）----------
[假测试门禁] 扫描 tests：命中 394 条违规
       字符串断言式             101 条  (string-assert)
       unittest字符串断言      113 条  (assertin-string)
       上界断言式                2 条  (upper-bound)
       下界断言式               27 条  (lower-bound)
       非空断言式              151 条  (not-none)
       预防性形态（当前零命中，只防新增，未在拦任何存量）：恒真断言式/trivial-ge0、成败都算通过/returncode-in
[假测试门禁] 无新增违规。
[假测试门禁] 通过：存量冻结、无新增。
rc=0

==================== 形态 4 / assertin-string ====================
探针内容：
    # -*- coding: utf-8 -*-
    """D4-1 反跑探针（形态 4）。收尾必删。"""
    import unittest
    
    
    class T(unittest.TestCase):
        def test_probe(self):
            py_code = "def f(): pass"
            self.assertIn('def f', py_code)

门禁输出（造回退后，期望 rc=1 且点名 _taskD4_probe.py:9）：
[假测试门禁] 扫描 tests：命中 395 条违规
       字符串断言式             101 条  (string-assert)
       unittest字符串断言      114 条  (assertin-string)
       上界断言式                2 条  (upper-bound)
       下界断言式               27 条  (lower-bound)
       非空断言式              151 条  (not-none)
       预防性形态（当前零命中，只防新增，未在拦任何存量）：恒真断言式/trivial-ge0、成败都算通过/returncode-in
[假测试门禁] 新增违规 1 条（视为回归，闸门拦下）：
       ! _taskD4_probe.py:9  [unittest字符串断言] self.assertIn('def f', py_code)
           为什么是假绿：assertIn('x', py_code) 与 assert 'x' in py_code 完全同源，换个 unittest API 就绕过门禁——这类实测比 assert 写法还多。
[假测试门禁] 红：出现了基线条目之外的新增假绿断言。
rc=1

删掉探针后（期望恢复 rc=0）：
[假测试门禁] 扫描 tests：命中 394 条违规
       字符串断言式             101 条  (string-assert)
       unittest字符串断言      113 条  (assertin-string)
       上界断言式                2 条  (upper-bound)
       下界断言式               27 条  (lower-bound)
       非空断言式              151 条  (not-none)
       预防性形态（当前零命中，只防新增，未在拦任何存量）：恒真断言式/trivial-ge0、成败都算通过/returncode-in
[假测试门禁] 无新增违规。
[假测试门禁] 通过：存量冻结、无新增。
rc=0

==================== 形态 5 / lower-bound ========================
探针内容：
    # -*- coding: utf-8 -*-
    """D4-1 反跑探针（形态 5）。收尾必删。"""
    
    
    def test_probe():
        xs = [1, 2, 3]
        assert len(xs) >= 2

门禁输出（造回退后，期望 rc=1 且点名 _taskD4_probe.py:7）：
[假测试门禁] 扫描 tests：命中 395 条违规
       字符串断言式             101 条  (string-assert)
       unittest字符串断言      113 条  (assertin-string)
       上界断言式                2 条  (upper-bound)
       下界断言式               28 条  (lower-bound)
       非空断言式              151 条  (not-none)
       预防性形态（当前零命中，只防新增，未在拦任何存量）：恒真断言式/trivial-ge0、成败都算通过/returncode-in
[假测试门禁] 新增违规 1 条（视为回归，闸门拦下）：
       ! _taskD4_probe.py:7  [下界断言式] assert len(xs) >= 2
           为什么是假绿：len(...) >= N：只断「至少有几个」，多出来的/错位的/重复的一概不管，集合非空即恒真。
[假测试门禁] 红：出现了基线条目之外的新增假绿断言。
rc=1

删掉探针后（期望恢复 rc=0）：
[假测试门禁] 扫描 tests：命中 394 条违规
       字符串断言式             101 条  (string-assert)
       unittest字符串断言      113 条  (assertin-string)
       上界断言式                2 条  (upper-bound)
       下界断言式               27 条  (lower-bound)
       非空断言式              151 条  (not-none)
       预防性形态（当前零命中，只防新增，未在拦任何存量）：恒真断言式/trivial-ge0、成败都算通过/returncode-in
[假测试门禁] 无新增违规。
[假测试门禁] 通过：存量冻结、无新增。
rc=0

==================== 形态 6 / not-none ===========================
探针内容：
    # -*- coding: utf-8 -*-
    """D4-1 反跑探针（形态 6）。收尾必删。"""
    
    
    def test_probe():
        结果 = {"a": 1}
        assert 结果 is not None

门禁输出（造回退后，期望 rc=1 且点名 _taskD4_probe.py:7，中文标识符也要抓到）：
[假测试门禁] 扫描 tests：命中 395 条违规
       字符串断言式             101 条  (string-assert)
       unittest字符串断言      113 条  (assertin-string)
       上界断言式                2 条  (upper-bound)
       下界断言式               27 条  (lower-bound)
       非空断言式              152 条  (not-none)
       预防性形态（当前零命中，只防新增，未在拦任何存量）：恒真断言式/trivial-ge0、成败都算通过/returncode-in
[假测试门禁] 新增违规 1 条（视为回归，闸门拦下）：
       ! _taskD4_probe.py:7  [非空断言式] assert 结果 is not None
           为什么是假绿：x is not None：只断「不是 None」，对值、类型、结构零约束，是零信号断言。
[假测试门禁] 红：出现了基线条目之外的新增假绿断言。
rc=1

删掉探针后（期望恢复 rc=0）：
[假测试门禁] 扫描 tests：命中 394 条违规
       字符串断言式             101 条  (string-assert)
       unittest字符串断言      113 条  (assertin-string)
       上界断言式                2 条  (upper-bound)
       下界断言式               27 条  (lower-bound)
       非空断言式              151 条  (not-none)
       预防性形态（当前零命中，只防新增，未在拦任何存量）：恒真断言式/trivial-ge0、成败都算通过/returncode-in
[假测试门禁] 无新增违规。
[假测试门禁] 通过：存量冻结、无新增。
rc=0
