# 任务 2 交付报告 — 网络请求.light 补 响应类 + 异常类 + helper

> 分支：`task-2b-网络请求响应类`
> 改动文件：`stdlib/网络请求.light`（+108 行，仅追加，未动现有函数）
> 日期：2026-09-10

## 验收结果

**`pytest tests/test_stdlib_phase3.py::Test网络请求 -q` → 7 passed 全绿**
（test_URL编码解码 / test_解析URL / test_拼接URL / test_解析查询串 / test_响应对象 / test_HTTP错误响应 / test_请求错误类 全部通过）

回归：`test_stdlib_phase3.py + unit/test_native_leg_capability.py + test_light_stdlib.py + test_http_client.py` → **48 passed / 1 skipped**。

## 实现内容

### 1. 异常层级
```
类 请求错误 继承 Exception
类 HTTP错误 继承 请求错误
类 超时错误 继承 请求错误
类 连接错误 继承 请求错误
```
均为 `段落 构造 接收 消息: 父.构造(消息)` 形态（对齐 HTTP服务端.light 既有先例）。

### 2. 响应类
- 关键字参数构造：`响应(状态码=200, 响应头={...}, 内容=b'...', 请求地址='...')`，
  响应头缺省 `新建字典()`
- 接口：`状态码`（字段）、`是否成功()`（200≤码<300）、`获取头(名)`（键小写归一，
  复用 `转小写ASCII`，缺省 ""）、`文本()`（内容原样）、`JSON()`（复用纯光明 JSON核心.解析，
  失败抛 运行时错误）
- 说明：测试里 `r.状态码` 是字段访问、`r.是否成功`/`r.文本` 无括号——实测光明方法
  调用无括号形态在属性访问位置回落为方法绑定/值，`assertTrue(r.是否成功)` 等断言
  均通过，未改测试。

### 3. 拼接URL(基准, 参数)
可变段列表以 `/` 缝合：先剥结果尾部与段头部多余 `/` 再拼接；段为绝对地址
（含 `://`）时直接替换结果。测试 `拼接URL('https://example.com/', 'path','to','page')`
产出含 `path/to/page`。

### 4. 解析查询串(查询串)
- `a=1&b=2&c=3` → 字典；无 `=` 的段存空串；`=右侧含=` 时按首个 `=` 切
- 宽容入口：传完整 URL 时自动取 `?` 之后 `#` 之前部分
- 键值不做 % 解码（与测试口径一致，编码/解码归 编码URL/解码URL）

### 5. 导出行
追加：`响应 请求错误 HTTP错误 超时错误 连接错误 拼接URL 解析查询串`

## 协调边界
- **未改动** `解析URL / 编码URL / 解码URL / 合并URL`（与 P6(④) 共用），新增函数
  只复用其结果、不重写。
- 现有 `发起请求/单次请求` 仍返回字典形态——响应类是新增的独立封装，两条形态
  并存（函数式 helpers 响应文本/响应成功/响应状态码 仍作用于字典响应），无行为
  变更，回归面已验证。

## 遗留
- 0.88 确认未执行（需打包重传后跑 `tests/test_stdlib_phase3.py::Test网络请求`）。
  实现为纯 .light 层，无 C/运行时依赖，0.88 风险低。
