# 自测报告_任务T7

**日期**：2026-09-04
**分支 / worktree**：`task-T7` / `G:\dswork\duan-light-merge\wt-T7`
**提交**：`fde6fae8` feat(tls): POSIX 补 mbedTLS 原生 TLS 后端，替代 stub（B9 S2）
**定向测试**：`python -m pytest tests/test_llvm_tls.py -q`（Windows/Schannel 侧）→ **2 passed**；POSIX mbedTLS 分支编译检查通过（详见 §5）

---

## 1. 任务范围

- **修复目标**：`src/llvm/runtime_typed.c` 的 POSIX TLS 从 stub（握手即失败）补齐真实现，API 签名与行为对齐 Windows Schannel 分支（证书校验、握手、读写、错误码），排期 **B9 S2**。
- **三选一决策**：选 **mbedTLS**（非 OpenSSL）。
- **反跑判据**：POSIX 下握手成功路径 + 失败路径行为与 Windows 一致；编译冒烟通过（`tests/test_llvm_tls.c` 相关定向用例）。
- **回写文档**：`docs/known_issues.md` §17.2。
- **文件隔离**：只动 `src/llvm/runtime_typed.c`、`tests/test_llvm_tls.py`（TLS 定向测试驱动）、`docs/known_issues.md`。`src/llvm/size_optimizer.py`（归 T3）与其它生产 Python 模块（归 T9，含 `compiler.py` 的 `get_link_libs`）**未触碰**。

## 2. 原状根因

`runtime_typed.c` 的 TLS 段：公共层（`DV_TLS_*` 错误码、`g_tls_error`、`g_tls_verify_default=1`、`dv_tls_last_error`）两平台共享；Windows 分支（`#ifdef _WIN32`）是 Schannel 真实现；POSIX 分支（`#else`）是 stub：`dv_tls_wrap` → NULL、`handshake/send/recv` → `DV_TLS_ERROR`、`dv_tls_backend()` → `"none"`、报「本平台未实现原生 TLS」。

缺的是**实现**，不是错误可见性——`dv_tls_last_error` 定义在 `#ifdef _WIN32` 之前，POSIX 上本可返回可读错误。

## 3. 修复内容

**为什么选 mbedTLS（三选一）**：
1. 原 stub 错误文本即点名「POSIX 待补 mbedTLS」，方向既定；
2. 与 Schannel「不装库 / 不改分发形态」口径最接近：mbedTLS 体积小、可静态捆绑、无运行时依赖；OpenSSL 在 Linux 上虽普遍但 ABI 版本漂移大（1.1 vs 3.0），许可约束也比 Apache-2.0 的 mbedTLS 紧；
3. mbedTLS 的 `MBEDTLS_ERR_SSL_WANT_READ/WANT_WRITE` 非阻塞语义与 `dv_tls_handshake` 的可重入状态机天然对齐。

**POSIX 分支改造**（`runtime_typed.c:6118-6556`）：
- `#if defined(LIGHT_TLS_MBEDTLS)`：完整 mbedTLS 客户端后端；`#else`：保留原 stub（未装 mbedTLS 的 POSIX 构建不破，满足「勿破坏原生腿其它部分」）。
- `LightTLS` 字段对齐 Windows：fd、host、verify、handshake_done、hs_started、peer_closed、recv_status、`SSL`/conf、out_pending 三缓冲（len/off/cap）。
- **队列式 BIO**（与 Windows out_pending 同构）：mbedTLS 加密产物先入 out_pending 队列，由 `dv_tls_flush`/`dv_tls_want_event` 非阻塞冲刷，半包写不完不阻塞事件循环。
- **证书校验默认开启**（`g_tls_verify_default=1`）：`MBEDTLS_SSL_VERIFY_REQUIRED` + `mbedtls_ssl_set_hostname`（SNI + 主机名校验）；`dv_tls_set_verify(t,0)` 关校验时打【安全告警】（与 Windows 同文案）。
- **独占根信任锚**（对应 Windows hExclusiveRoot、curl --cacert 语义）：`dv_tls_add_trusted_cert_file` 把显式 PEM/DER 证书加入独立 `mbedtls_x509_crt`，一旦设置 `g_tls_roots_explicit=1` → 握手只用这批根，比系统根更严；未设置时懒加载系统 CA 包（`SSL_CERT_FILE` 优先 + 发行版标准路径）。
- 握手可重入：`dv_tls_handshake` 返回 OK/WANT_READ/WANT_WRITE/ERROR，`dv_tls_want_event` 翻译成 `DV_POLL_READ/WRITE`（定义于公共层 4912-4913）喂 `dv_coro_await_io`，不阻塞事件循环。
- 读写：`dv_tls_send_n` 按已排队返回、`dv_tls_recv_status` 返回 OK/WANT_READ/CLOSED/ERROR；`MBEDTLS_ERR_SSL_PEER_CLOSE_NOTIFY`/EOF → CLOSED。
- `dv_tls_backend()` 返回 `"mbedTLS"`；14 个对外函数签名一个不变（`codegen_typed.py` 归 T9 未动）。

## 4. 反跑判据验证

任务书反跑判据为「POSIX 下握手成功路径 + 失败路径行为与 Windows 一致；编译冒烟通过」。本机是 Windows、目标 Ubuntu 机不可达（见 §8），故 POSIX 侧做**代码层对齐 + 编译检查**，行为级验证待上机补跑：

| 判据 | 本机验证 | 结果 |
|---|---|---|
| POSIX mbedTLS 分支可编译（Ubuntu 24 同款 **mbedTLS 2.28.8** 头） | 独立 TU `_taskT7_posix_syntax.c`（从 runtime_typed.c 抠出的 `#if defined(LIGHT_TLS_MBEDTLS)` 分支 + 公共层 stub）`-fsyntax-only -Wall -Wextra` | **通过**（exit 0，零告警） |
| 同分支完整 `-c` 编译（验证代码生成） | `clang -c -O2`（2.28.8 头） | **通过**（产出 .o） |
| mbedTLS 3.5.2 头兼容（其它发行版） | `-fsyntax-only` | **通过**（exit 0） |
| Windows Schannel 分支不被破坏 | `python -m pytest tests/test_llvm_tls.py -q` 正/负例 | **2 passed** |
| POSIX 握手成功/失败路径行为与 Windows 一致 | 需真实 Ubuntu 上机 | **未实测**（.86 不可达，见 §8） |

**改坏缩进/编译追踪立即立红**：语法检查曾抓到 2 个真问题（`MBEDTLS_ERR_NET_RECV_FAILED` 未包含于 2.x 头、`mbedtls_ssl_conf_ca_chain` 参数 const 不匹配），已修正后再跑全绿——证明该 TU 能即时捕获分支内编译错误。

## 5. 定向测试结果

```
# Windows 侧（Schannel 路径，本机）
python -m pytest tests/test_llvm_tls.py -q
2 passed in 5.89s   # test_tls_positive_handshake_echo + test_tls_negative_untrusted_cert
```

- 测试文件改动：正/负例去掉 `仅Windows` 跳过（双平台都跑真后端）、删除 `仅POSIX` 桩反向钉住用例、POSIX 编译显式加 `-DLIGHT_TLS_MBEDTLS -lmbedtls -lmbedx509 -lmbedcrypto`（生产 `get_link_libs()` 的 POSIX 分支保持 `-lm` 不动，属 T9 范围）。
- 本机第一次跑 skip 是因为缺 cryptography（证书生成依赖），装 `cryptography` 后 2 passed——skip 判据本身正常。
- 未跑全量 pytest（任务书禁止全量）。

## 6. 文档回写

`docs/known_issues.md` §17.2 已改为「——已修复（Task T7：mbedTLS 后端）」，说明：原状 → 修复内容（校验默认开、独占根、可重入握手、out_pending 队列）→ 开/关宏与依赖 → 验证结果 → 未在 POSIX 实测标注 → 记账口径更新。

## 7. 文件隔离核对

| 文件 | 状态 |
|---|---|
| `src/llvm/runtime_typed.c` | 修改（仅 POSIX TLS 段 6118-6556；Windows 分支与公共层一字未动，git diff 验证） |
| `tests/test_llvm_tls.py` | 修改（平台 skip 逻辑退役 + POSIX 编译旗标） |
| `docs/known_issues.md` | 修改（§17.2） |
| `tests/test_llvm_tls.c` | **未改动**（超出隔离范围，POSIX 编译命令写入本文档） |
| `src/llvm/compiler.py` / `codegen_typed.py` / `size_optimizer.py` | **未触碰**（归 T9 / T3） |

POSIX 编译 test_llvm_tls.c 的命令（上机补跑时用）：
```
clang -O2 -DLIGHT_TLS_MBEDTLS -o _taskT7_tls_test tests/test_llvm_tls.c -lmbedtls -lmbedx509 -lmbedcrypto
```

## 8. 未实测项标注（重要）

- **未在 POSIX 实测**：任务书指定目标机 `192.168.0.86`（SSH 22，凭据见 `.env`）**不可达**——ICMP/TCP 22 全失败、`ssh` 直连 `Connection timed out`（exit 255）、多次重试无效；同网段 `.88`/`.1.5` 的 22 端口可达，证明路由/网段正常，.86 当前未开机或 SSH 未监听。真实 Ubuntu 上的握手正/负例**未执行**。
- **POSIX 编译冒烟未跑**：本机无 Linux 环境（WSL 无发行版、无 docker），`-lssl/-lmbedtls` 链接冒烟无法在本机执行；已用真实 mbedTLS 头做 `-fsyntax-only` + `-c` 替代（§4）。
- 补跑路径：.86 可用后，`apt install libmbedtls-dev` → `python -m pytest tests/test_llvm_tls.py -q`（该文件已在 POSIX 去掉 skip，会跑正/负两例真后端）。
- 临时文件 `_taskT7_*`（含下载的 mbedtls 源码包与 TU）已清理，未进提交。
