# 自测报告_任务T7

**日期**：2026-09-04（POSIX 实测于同日补跑）
**分支 / worktree**：`task-T7` / `G:\dswork\duan-light-merge\wt-T7`
**提交**：`1457b95f` fix(tls): POSIX 真机实测修复——mbedTLS 补配 RNG（B9 S2）
**定向测试**：`python -m pytest tests/test_llvm_tls.py -q` → **Windows 2 passed**（Schannel）+ **POSIX 2 passed**（mbedTLS 真机实测，Ubuntu 24 / 192.168.0.86）

---

## 1. 任务范围

- **修复目标**：`src/llvm/runtime_typed.c` 的 POSIX TLS 从 stub（握手即失败）补齐真实现，API 签名与行为对齐 Windows Schannel 分支（证书校验、握手、读写、错误码），排期 **B9 S2**。
- **三选一决策**：选 **mbedTLS**（非 OpenSSL）。
- **反跑判据**：POSIX 下握手成功路径 + 失败路径行为与 Windows 一致；编译冒烟通过（`tests/test_llvm_tls.c` 相关定向用例）。
- **回写文档**：`docs/known_issues.md` §17.2。
- **文件隔离**：只动 `src/llvm/runtime_typed.c`、`tests/test_llvm_tls.py`（TLS 定向测试驱动）、`docs/known_issues.md`。`src/llvm/size_optimizer.py`（归 T3）与其它生产 Python 模块（归 T9，含 `compiler.py` 的 `get_link_libs`）**未触碰**。

## 2. 原状根因

`runtime_typed.c` 的 TLS 段：公共层（`DV_TLS_*` 错误码、`g_tls_error`、`g_tls_verify_default=1`、`dv_tls_last_error`）两平台共享；Windows 分支（`#ifdef _WIN32`）是 Schannel 真实现；POSIX 分支（`#else`）是 stub：`dv_tls_wrap` → NULL、`handshake/send/recv` → `DV_TLS_ERROR`、`dv_tls_backend()` → `"none"`、报「本平台未实现原生 TLS」。

缺的是**实现**，不是错误可见性——`dv_tls_last_error` 定义在 `#ifdef _WIN32` 之前，POSIX 上本可返回可读错误。

## 3. 修复内容

**为什么选 mbedTLS（三选一）**：
1. 原 stub 错误文本即点名「POSIX 待补 mbedTLS」，方向既定；
2. 与 Schannel「不装库 / 不改分发形态」口径最接近：mbedTLS 体积小、可静态捆绑、无运行时依赖；OpenSSL 在 Linux 上虽普遍但 ABI 版本漂移大（1.1 vs 3.0），许可约束也比 Apache-2.0 的 mbedTLS 紧；
3. mbedTLS 的 `MBEDTLS_ERR_SSL_WANT_READ/WANT_WRITE` 非阻塞语义与 `dv_tls_handshake` 的可重入状态机天然对齐。

**POSIX 分支改造**（`runtime_typed.c:6118-6570` 左右）：
- `#if defined(LIGHT_TLS_MBEDTLS)`：完整 mbedTLS 客户端后端；`#else`：保留原 stub（未装 mbedTLS 的 POSIX 构建不破，满足「勿破坏原生腿其它部分」）。
- `LightTLS` 字段对齐 Windows：fd、host、verify、handshake_done、hs_started、peer_closed、recv_status、`ssl`/`conf`、`ctr_drbg`/`entropy`（RNG）、out_pending 三缓冲（len/off/cap）。
- **队列式 BIO**（与 Windows out_pending 同构）：mbedTLS 加密产物先入 out_pending 队列，由 `dv_tls_flush`/`dv_tls_want_event` 非阻塞冲刷，半包写不完不阻塞事件循环。
- **证书校验默认开启**（`g_tls_verify_default=1`）：`MBEDTLS_SSL_VERIFY_REQUIRED` + `mbedtls_ssl_set_hostname`（SNI + 主机名校验）；`dv_tls_set_verify(t,0)` 关校验时打【安全告警】（与 Windows 同文案）。
- **独占根信任锚**（对应 Windows hExclusiveRoot、curl --cacert 语义）：`dv_tls_add_trusted_cert_file` 把显式 PEM/DER 证书加入独立 `mbedtls_x509_crt`，一旦设置 `g_tls_roots_explicit=1` → 握手只用这批根，比系统根更严；未设置时懒加载系统 CA 包（`SSL_CERT_FILE` 优先 + 发行版标准路径）。
- **RNG（实测发现并修复）**：mbedTLS 硬性要求配置随机源，漏配 `mbedtls_ssl_conf_rng` 握手直接 `-0x7400`（No RNG provided）。LightTLS 加 `mbedtls_entropy_context` + `mbedtls_ctr_drbg_context`，握手初始化 `mbedtls_ctr_drbg_seed(entropy_func, "light-tls")` 后 `mbedtls_ssl_conf_rng(ctr_drbg_random)`；`dv_tls_free` 同步释放。
- 握手可重入：`dv_tls_handshake` 返回 OK/WANT_READ/WANT_WRITE/ERROR，`dv_tls_want_event` 翻译成 `DV_POLL_READ/WRITE` 喂 `dv_coro_await_io`，不阻塞事件循环。
- 读写：`dv_tls_send_n` 按已排队返回、`dv_tls_recv_status` 返回 OK/WANT_READ/CLOSED/ERROR；`MBEDTLS_ERR_SSL_PEER_CLOSE_NOTIFY`/EOF → CLOSED。
- `dv_tls_backend()` 返回 `"mbedTLS"`；14 个对外函数签名一个不变（`codegen_typed.py` 归 T9 未动）。

## 4. 反跑判据验证（最终结论：全部通过）

任务书反跑判据 = 「POSIX 下握手成功路径 + 失败路径行为与 Windows 一致；编译冒烟通过」。

**真实 POSIX 实测（Ubuntu 24.04 / 192.168.0.86，clang 18.1.3，libmbedtls-dev 2.28.8）**：

```
python3 -m pytest tests/test_llvm_tls.py -q
2 passed in 4.06s   # test_tls_positive_handshake_echo + test_tls_negative_untrusted_cert
```

C 二进制定向用例直接输出证据（`tests/test_llvm_tls.c` positive / negative）：

```
=== TLS Positive Test ===
[PASS] dv_tls_add_trusted_cert_file(.../test_cert.pem)
[PASS] dv_socket_create / dv_socket_connect(127.0.0.1,19166)
[PASS] dv_tls_wrap(fd,localhost)
[PASS] dv_tls_handshake
[PASS] dv_tls_send(hello_tls_c_unit)=16
[PASS] dv_tls_recv(echo=hello_tls_c_unit)
[PASS] dv_tls_backend()=mbedTLS
[PASS] dv_tls_free + dv_socket_close
=== TLS Positive Results: ALL PASSED ===

=== TLS Negative Test ===
[PASS] tls_handshake rejected untrusted cert: TLS 握手失败: X509 - Certificate verification failed, e.g. CRL, CA or signature check failed (-0x2700)
=== TLS Negative Results: ALL PASSED ===
```

| 判据 | 验证方式 | 结果 |
|---|---|---|
| 握手成功路径（加信任锚 → 握手 → 收发回显） | .86 真机 C 测试 positive | **通过**（`dv_tls_backend()=mbedTLS`） |
| 失败路径（不加信任锚 → 必须失败） | .86 真机 C 测试 negative | **通过**（`X509 verify failed -0x2700`，正确的证书校验失败，非 RNG/其它） |
| 编译冒烟（`tests/test_llvm_tls.c` 相关定向用例） | .86 `clang -O2 -DLIGHT_TLS_MBEDTLS ... -lm -lmbedtls -lmbedx509 -lmbedcrypto` | **通过**（rc=0，含 RNG 修复后） |
| Windows Schannel 分支不被破坏 | 本机 `pytest tests/test_llvm_tls.py -q` | **2 passed** |
| mbedTLS 分支代码层（2.28.8 与 3.5.2 头） | 本机 `-fsyntax-only` + `-c` | **通过** |

**实测过程中抓到并修复的真 bug**：首跑 POSIX 正例失败——`TLS 握手失败: SSL - No RNG was provided to the SSL module (-0x7400)`，即漏配 RNG。这是**编译检查无法发现的运行时要求**，只有真机握手才会暴露；负例当时虽过但属假绿（同因 RNG 失败）。修复（§3 RNG 段）后正/负例均以**正确原因**通过。这正说明真 POSIX 实测不可替代。

**改坏即立红**：若把 RNG 配置或握手缩进改坏，.86 上正例立即失败（`-0x7400` 或编译错误）；本机 TU 语法检查也能即时捕获编译级错误（首轮已抓到 `MBEDTLS_ERR_NET_RECV_FAILED` 未包含、`mbedtls_ssl_conf_ca_chain` const 不匹配两个问题）。

## 5. 定向测试结果

```
# Windows（本机，Schannel）
python -m pytest tests/test_llvm_tls.py -q   → 2 passed in 5.15s
# POSIX（.86 真机，mbedTLS）
python3 -m pytest tests/test_llvm_tls.py -q  → 2 passed in 4.06s
```

- 测试文件改动：正/负例去掉 `仅Windows` 跳过（双平台都跑真后端）、删除 `仅POSIX` 桩反向钉住用例、POSIX 编译显式加 `-DLIGHT_TLS_MBEDTLS -lmbedtls -lmbedx509 -lmbedcrypto`（生产 `get_link_libs()` 的 POSIX 分支保持 `-lm` 不动，属 T9 范围）。
- 本机首次跑 skip 是因为缺 cryptography，装后 2 passed——skip 判据本身正常。
- 未跑全量 pytest（任务书禁止全量）。

## 6. 文档回写

`docs/known_issues.md` §17.2 已改为「——已修复（Task T7：mbedTLS 后端）」，并已更新为「POSIX 真机实测通过」（192.168.0.86 / Ubuntu 24 / mbedTLS 2.28.8，`pytest tests/test_llvm_tls.py -q` 2 passed）。

## 7. 文件隔离核对

| 文件 | 状态 |
|---|---|
| `src/llvm/runtime_typed.c` | 修改（仅 POSIX TLS 段；Windows 分支与公共层一字未动，git diff 验证） |
| `tests/test_llvm_tls.py` | 修改（平台 skip 逻辑退役 + POSIX 编译旗标） |
| `docs/known_issues.md` | 修改（§17.2） |
| `tests/test_llvm_tls.c` | **未改动**（超出隔离范围，POSIX 编译命令见下） |
| `src/llvm/compiler.py` / `codegen_typed.py` / `size_optimizer.py` | **未触碰**（归 T9 / T3） |

POSIX 编译 test_llvm_tls.c 的命令（.86 实测用）：
```
clang -O2 -DLIGHT_TLS_MBEDTLS -o _taskT7_tls_test tests/test_llvm_tls.c -lm -lmbedtls -lmbedx509 -lmbedcrypto
```

## 8. 未实测项标注（更新）

- ~~未在 POSIX 实测~~：**已完成**。首次交付时目标机 `192.168.0.86` 不可达（SSH 22 Connection timed out），按任务书标注「未在 POSIX 实测」；用户确认 .86 连通后，于 Ubuntu 24.04（clang 18.1.3 / libmbedtls-dev 2.28.8）完成真机握手正/负例实测，全部通过（§4）。
- 范围外未测：公网 TLS 服务器连接（如 https:// 真实站点）未做；握手/读写/证书校验的**本机环回**正负路径已覆盖，行为与 Windows Schannel 一致。
- 临时文件 `_taskT7_*`（含下载/拉取的 mbedtls 头与 TU）已清理，未进提交。
