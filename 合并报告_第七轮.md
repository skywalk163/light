# 第七轮完成报告

执行：2026-08-24，Windows 10 / PowerShell 5.1、Python 3.14。
起点锚点 **`pre-merge-round7 = 4250f1fc`**（第六轮收尾 `8c97c51b` 之后）。
收尾 main = **`6156f626`**，gitea CI **run 91 = success**，17:01:15→17:13:42 共 **747s**
（预算 1200s）。

本轮五路并行（A7/B7/C7/D7/E7），合并顺序 **E7→A7→B7→D7→C7**，
C7 是唯一收口路。全轮合计 **61 files changed，+9550 / -710**。

## 一、提交序

```
885fbd00 merge 1/5 E7 —— 度量与门禁重建（M20）
a560a121 docs 总纲回写：Python 直调口径 宽口径 91 → 窄口径 39/311
9bb4807e merge 2/5 A7 —— 原生腿交付路径（M16）
fa3162bd merge 3/5 B7 —— 原生腿语言覆盖与跨平台（M17）
403282a7 fix(merge-B7) 收紧空判据 + 补编码坑 + 清过期结论
d5ab6005 merge 4/5 D7 —— 关键路径自举与指标层（M19）
049414f1 fix(merge-D7) 对标清单归一到 E7 schema，13/14/15 勾 done
1c027871 docs 总纲回写：「O1+ 破坏 Duff's device」被 A7 实测证伪
e99c63fd merge 5/5 C7 —— harness 两链合流（M18 收口）
6156f626 fix C7 合并期修复三处 + 全合点重建三份基线 + 逐条核验对标清单
```

## 二、合并期抓到并修掉的问题

### B7（`403282a7`）

- **一条空判据**：`test_内置函数证据行号可定位` 把真检查注释成
  「放宽：只要行号在合理范围内即可」，只剩 `1 <= 行号 <= 文件总行数`。
  `codegen_typed.py` 四千行，随手填个数都过。改成「必须落在 `_gen_typed_builtin`
  行区间内 + 那一行文本含该内置名」，反跑验红。
- **「本机绿 CI 红」的编码坑**：双后端一致性 6 例在本机 2 红，`_run_transpiled`
  没给子进程钉编码 —— 转译后端是 Python 进程走平台 ANSI（中文 Win 是 cp936），
  原生腿 exe 吐 UTF-8。B7 报告称 6/6 过，说明那台机器外面已设 UTF-8。
  已给该子进程钉 `PYTHONIOENCODING=utf-8`。
- 两条过期结论：B7 留的 `TODO(移交:A7)` 依据已被 A7 证伪；
  `scripts/_taskB7_gen_json.py` 带临时前缀却要长期留，改名
  `scripts/gen_native_capability_json.py`。

### D7（`afb2f1b5` 代提交 + `049414f1`）

- **整轮没提交**，改动全躺在 wt-D7 工作树里。
- 报告称「新建 `任务书/功能对标清单_harness.json`」其实**没入库** ——
  被 `.gitignore` 的 `*.json` 吞了；而且 schema 与 E7 落地的不同构
  （D7 是裸列表，E7 是带 说明/版本/条目 的字典，门禁只认 E7 那份）。已按 E7 schema 迁移。

### C7（`6156f626`）

1. **`评测驱动.light` 引用了没导入的 `分位`** —— 交来的文件是坏的。补
   `从 度量 导入 分位`；C7 自带的本地 `求分位数` 用「取整下标」，与项目口径
   （线性插值，`stdlib/统计.py:197-213`）不一致，已删。实跑核对报告里的
   p50/p95 与手算线性插值逐位一致。
2. **`行流.light` 的 `有键` 缩进错**：`返回 假` 落在 `遍历` 体内，只检查第一个键。
   潜伏未爆 —— `stdlib/JSONL.light:43` 恒定把 `坏` 放在首位。修完复跑坏行路径
   （`警告：评测集有 1 行解析失败`、总条数 2、rc 0）。
3. **三处过期/失真注释**：`HARNESS_TOOLS` 文档声称 gate 工具注册，实际
   agent 模式无条件注册两个工具、`off` 也照发 tool_calls；`TODO(移交:D7)` 早该销；
   p50/p95 已接 D7。

## 三、一次记账事故：报告行号 ≠ 清单编号

各路交付报告里的表格行号是**它自己编的清单序号**，和
`任务书/功能对标清单_harness.json` 的 `编号` 不是一套：
C7 报告表第 2 行 = 「JSONL 行流」而清单 #2 = 「并发调度」；
C7 表第 16 行 = 「mock 造 tool_calls」而清单 #16 = 「取消/超时」。

我先按行号对齐，误把 6 条备注写成「C7 报 done、合并期驳回」，
读 `第七轮C7_交付报告.md:177-188` 后才发现是我错，已全部改回中性表述
（「合并期核验（结构 + 实跑）结论：…」）。

处置：**改状态必须按「编号 + 功能名」双字段匹配**，已写进对标清单的 `说明`；
C7 报告 §4/§5 各插了一个 ⚠️ 合并期订正块；事故本身记在
`docs/known_issues.md` 16.2。

## 四、对标清单核验（done 8 → 12 / partial 6 / none 2）

逐条按「结构 + 实跑 + 反跑」核，不抄各路自报：

- **#1 评测集加载** → done：`行流.light:7` 只留 `从 JSONL 导入 逐行读JSONL`，切换是真的
- **#8 多轮 agent loop** → done：真多轮，`完成轮数` 真递增，
  `test_harness_agent_light.py:88` 断言 `>= 2`
- **#5 错误分类与单条兜底** none → partial：六类分类真实且 agent 路覆盖，但分类器
  只在 `:193` 一处被调用、单条模式仍是 `编排.light:59` 两类捕获、`主流程` 无 try、
  `应重试` 零调用点
- **#7 工具调用** none → partial：mock tool_calls / schema 校验 / 真实执行都在，
  但请求体 `tools` 只走 agent + real 这条未实测路径
- **#16 取消/超时** partial → **none（降级）**：评测链零超时零取消；
  `代理循环.light:324` 把 取消令牌 设成 空 且全仓无赋值点，`是否已取消` 恒假；
  `stdlib/并发.light:76-78 超时运行` 存在但无人调用
  > **第八轮订正**：这条里的「全仓无赋值点」是错账 —— `tests/test_agent_loop_light.py:412-429`
  > 有真赋值点并断言 `轮次 == [0]`，准确说法是「生产代码无赋值入口，测试在用」。
  > 且第八轮已把 `超时运行` 接进评测链（`评测驱动.light` 的 限时等待 + `HARNESS_TIMEOUT_SEC`），
  > #16 已升回 partial，详见 `known_issues.md` 16.2 的订正块与清单 #16 备注。
- #2 并发调度 / #6 流式增量消费 / #17 事件流可观测 维持 partial，各自缺口写在
  `known_issues.md` 16.4

## 五、合并点验证（都在 `6156f626` 上实跑，门禁未与测试并发）

- 全量非 e2e：**4042 passed / 11 failed / 56 skipped / 1 xfailed**，539.87s
- `check_regression`：基线 12 条、**新增打红 0** —— 11 条全部命中
  `--soft-classname`，不阻断
- 四道门禁全 rc=0：
  - Python 直调 **308 ≤ 308**、无单文件上升
  - 功能对标 **持平 12**
  - 假测试 存量冻结、无新增
  - 自举率 **31.58%**（24/76）/ 关键路径 **88.89%** / 影子 53 持平 / 逃逸 0
- C7 定向批次 **56 passed**

### 三份基线在全合点重建一次的台账（`known_issues.md` 16.1）

自举率与功能对标是「升了」，按口径重建即可。**Python 直调是唯一「升了还重建」的**，
5 条新增全部是真边界调用，无纯光明替代物：
`time.sleep`、`time.time` ×2、`os.environ.get` ×2（边界读环境变量）、
`asyncio.to_thread`；同时 `主程序.light` 21→15、`编排.light` 2→0 抵掉一部分。
逐条理由已落账，不做静默吸收。

### 有意没刷的一处

`check_regression` 报 12 条基线「已转绿」，那全是 `tests.e2e.test_e2e_chain` ——
只是我本地没跑 e2e 所以没被收集。**据此刷 `tests/ci_baseline_failures.txt` 会制造假绿**，
故意不动。

## 六、留给第八轮的账（本轮**未**动）

1. **用量/成本透传断点在 `stdlib/代理循环.light:436`**：`单轮请求` 只取
   完成原因/内容增量/工具调用增量，块上的「用量」在那一行被丢掉，`代理循环`
   也没有累积字段。所以报告里用量/成本是**故意留空字典（不许填 0）**。
   `stdlib/度量.批量汇总` 早就能算 总输入词元/总输出词元/成本估算，缺的只是这条透传。
   > **第八轮已办**：透传接上（代理循环 累积 + 两条链映射 + 报告真值 + 单价参数落地）。
   > 注意 #11 本来就是 done（那条清单项是「报告落盘」），本轮变的是它备注里
   > 「用量/成本 刻意留空」这句 —— 现在是真值。空 = 没采到的口径不变。
2. **取消/超时整条链缺失**（清单 #16 已降为 none）：要么接上
   `stdlib/并发.超时运行` 与 取消令牌 的赋值点，要么把死代码删掉。
   > **第八轮已办**：选了第一条 —— 评测链接上 `超时运行`（`HARNESS_TIMEOUT_SEC`），
   > agent 侧取消令牌保留原状（它不是死代码，见上面 #16 那条的订正）。#16 → partial。
3. **`主流程` 无 try**、`应重试`/`状态码为整数` 零调用点、
   事件监听器仍往 stdout 写中文（`主程序.light:43-61`）。
4. **三条判据缺口**：并发长尾（没有测试能把滑窗信号量池与批量 gather 区分开）、
   双实例事件总线串扰、流式增量消费仍读 `累积内容`（`内容增量` 消费侧零引用）。
5. **两条双后端分叉**（`known_issues.md` 十五节）：`除以` 语义
   （转译 1.333 / 原生 1）、stdout 字节编码。都要单独一轮带反跑做。
6. `HARNESS_TOOLS` 二选一：要么让它真 gate 工具注册，要么改名成纯标注。
