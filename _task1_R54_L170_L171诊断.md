# 第54轮 任务1：L-170 + L-171 复现与根因诊断

> 日期：2026-09-17 ｜ 编译器侧：light-merge（`src/parser_stmt.py`）
> 范围：仅诊断，不改编译器源码（铁律）
> 复现环境：`light-merge/.venv/Scripts/python.exe -B`（强制走源码，规避 Z620 时钟漂移导致的 `.pyc` 不失效问题）

---

## 1. 复现结论速览

| Bug | 任务书描述现象 | 当前 HEAD 是否复现 | 真因 |
|---|---|---|---|
| **L-170** | `映射[k] 为 f(...)` 静默编译成 `(映射[k] == f(...))`，赋值丢失、运行期 KeyError | **复现（真 bug）** | 解析器把 VERB_ARITY 核心动词在语句首**无条件当动词调用**，且漏覆盖「被声明后重分类为 IDENTIFIER」的形态 |
| **L-171** | `写 "x=" + 变量` 静默丢实参 | **不复现（已修）** | L-055 的 `写 X+Y` BinaryOp 合并分支已覆盖；当前 HEAD 输出 `x=y` 正确 |

---

## 2. L-170 复现与根因

### 2.1 最小复现（逐字命中任务书）

复现用例（`examples/test_L170.light` 来源形态）：

```light
段落 主:
  设 映射 为 {}
  设 k 为 "a"
  映射[k] 为 99          # ← 期望：映射["a"] = 99 ；实际：编译成 (映射["a"] == 99)
  映射之x 为 1          # ← 期望：映射["x"] = 1  ；实际：(映射["x"] == 1)
  映射.项 为 1          # ← 期望：映射["项"] = 1  ；实际：(映射["项"] == 1)
```

`cli.light compile` 产物（修复前）：

```python
# LIGHT_SRC:3  (映射[k] 为 99)
(映射[k] == 99)         # ← 赋值静默变比较！应为 映射[k] = 99
# LIGHT_SRC:4  (映射之x 为 1)
(_light_attr_get(映射, 'x') == 1)
# LIGHT_SRC:5  (映射.项 为 1)
(_light_attr_get(映射, '项') == 1)
```

编译期零报错；运行期 `映射[k]` 仍是 `{}` 默认值（比较表达式结果被丢弃），若后续读该键则 KeyError。

### 2.2 根因定位（解析阶段，非词法/codegen）

**罪魁在 `src/parser_stmt.py` 的 `_parse_statement_inner` 动词调用分支：**

```python
# 修复前
if tok.type == TokenType.KEYWORD and tok.value in VERB_ARITY:
    return self._parse_expr_stmt()     # ← 任何 VERB_ARITY 动词在语句首都当动词调用
```

中文 `为` 在**表达式上下文**里是 `==`，于是 `映射[k] 为 99` 被解析成「`映射[k]` 与 `99` 的比较」，本应走赋值的 `映射[k] 为 99` 形态被吞掉。

**两个叠加缺陷，缺一不可：**

1. **KEYWORD 路径缺赋值守卫**：核心动词在语句首被无条件当动词调用，没有「后跟 `[`/`.`/`之`/`的`/`为`/`等于`/`=` 就应改走赋值」的分流。这是「裸 `映射[k] 为 99`」（未先声明 映射）也翻车的原因。

2. **IDENTIFIER 路径完全漏管**（这是真实代码必定触发的关键）：
   - `light` 的 **deterministic lexer 会把「已被声明过的动词名」重分类为 IDENTIFIER**。实证 token 流：

     ```
     裸 `映射[k] 为 99`              → 映射 = KEYWORD
     `设 映射 为 {}` 后 `映射[k]...` → 映射 = IDENTIFIER   ← 真实代码几乎都先声明
     ```

   - 因此 `设 映射 为 {}` 之后再写 `映射[k] 为 99`，`映射` 是 **IDENTIFIER**，上面的 `tok.type == KEYWORD` 守卫根本不命中；而 `if tok.type == TokenType.IDENTIFIER and tok.value in STDLIB_VERB_ARITY`（L-058 修复只覆盖 **STDLIB** 动词）也不命中（映射是核心 VERB_ARITY，非 STDLIB）。两侧都不命中 → 落到默认表达式语句 → 静默变比较。
   - **结论：任务书「实际代码里才会触发」的根因就是 lexer 重分类**。纯语法层面的「核心动词」与「被声明后的动词名」在 token 类型上已经分裂成 KEYWORD / IDENTIFIER 两套，守卫必须两套都覆盖。

### 2.3 影响面度量（全 VERB_ARITY 成员）

`VERB_ARITY`（40 个核心动词，如 `映射/筛选/严格/构造/保护/输出/步/替换/加/减/乘/...`）作变量名用于下标/成员赋值目标，全部踩同一坑。影响面 = 所有「把核心动词当字典/对象变量名 + 下标或成员赋值」的写法。修正后该写法应一律正确赋值。

---

## 3. L-171 复现与根因（结论：已修复，本轮无需改）

### 3.1 任务书描述现象

> `写 "x=" + 变量` 静默丢实参——`写` 只吃单字面量/变量，返回空 → 报「不能对 空值 与 字符串 做 加 运算」。

### 3.2 实证：当前 HEAD 不复现

`cli.light run` 直接实跑：

| 形态 | 代码 | 实际输出 | 判定 |
|---|---|---|---|
| 任务书原形态 | `写 "x=" + 变量`（变量="y"） | `x=y` | ✅ 正确，未丢参 |
| 变量在左 | `写 变量 + "=end"`（变量=9） | 运行期 `整数 + 字符串` 类型错误 | 非 `写` bug（见下） |
| 多段拼接 | `写 "v=" + a + b` | 运行期 `字符串 + 整数` 类型错误 | 非 `写` bug（见下） |
| 纯常量拼接 | `写 "a" + "b"` | `ab` | ✅ 正确 |

`写 "x=" + 变量` 编译产物（当前 HEAD）：

```python
_light_builtin.写入输出(str(("x=" + 变量)))
```

`str(...)` 包裹正确，实参未被丢弃。

### 3.3 根因说明

- L-055 已在 `src/code_generator.py` 的 `_try_merge_output_concat` 加了形态① BinaryOp 合并分支（约 L977），把 `写 X + Y` 合并为 `写入输出(str(X + Y))`。当前 HEAD 该分支完整有效，故 L-171 已修。
- 3.2 里「变量在左 / 多段拼接」的失败是 **`整数 + 字符串` 类型错误**（`Light` 不自动把整数强转字符串），与 `写` 无关，按语言设计本就该报类型错，不是静默丢参。
- **结论：L-171 在当前 HEAD 已修复，本轮任务2 无需改动编译器即可满足验收（见任务2 §4 复核）。**

---

## 4. 诊断方法（可复用配方）

1. **强制走源码**：Z620 时钟漂移会让 `.pyc` 在源文件改动后仍被当作有效 → 运行务必加 `-B` 或 `PYTHONDONTWRITEBYTECODE=1`，改完编译器先 `find . -name __pycache__ -exec rm -rf {} +`。
2. **dump 编译产物而非只跑运行**：`cli.light compile <f> -o out.py` 看 `主` 函数体，赋值静默 bug 在产物里是 `(x == y)` 而非 `x = y`。
3. **token 流印证 lexer 重分类**：`from lexer import Lexer; Lexer(deterministic=True).tokenize(src)` 打印 token 类型，确认「声明后动词名变 IDENTIFIER」。
4. **运行时断言赋值真生效**：用 `如果 实际 != 期望: 抛出` 断言，而非只看编译产物——静默 bug 的编译产物和运行结果会不一致。

---

## 5. 交付物

- `light-merge/examples/test_L170.light` —— L-170 最小复现 + 赋值生效断言（运行打印 `L170-OK`，rc=0）
- `light-merge/_task2_R54_L170_L171修复.md` —— 修复方案与验证
