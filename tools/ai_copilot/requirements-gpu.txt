# 光明翻译器 GPU LoRA 微调依赖
#
# 安装方式：
#   pip install -r requirements-gpu.txt
#
# 注意：torch 需根据 CUDA 版本单独安装，例如：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# 核心依赖
# Qwen3.5-2B 需要 transformers>=5.0，实测 5.14.1 可用
# Kaggle/Colab 预装版本可能较旧，安装后会自动升级
transformers>=5.14.1,<6.0
peft>=0.13.0
datasets>=2.20.0
accelerate>=0.34.0
packaging>=23.0

# QLoRA 4bit 量化训练（显存不足时使用，所有模型均可用）
bitsandbytes>=0.43.0

# peft 依赖：torchao 必须 >= 0.16.0，否则 LoRA 模块创建会报 ImportError
# 报错信息：Found an incompatible version of torchao. Found version 0.10.0, but only versions above 0.16.0 are supported
torchao>=0.16.0

# Unsloth 优化后端（可选，省 30-50% 显存 + 2x 加速）
# 安装后可用 --unsloth 参数启用
# 注意：Unsloth 可能与 transformers 6.x 不兼容，如遇冲突请降级 transformers
unsloth>=2025.3.19
