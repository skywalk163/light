# 练习 3：条件判断（参考答案）

段落 判断奇偶 接收 数：
  如果 数 模 2 等于 0：
    打印(转字符串(数) 加 " 是 偶数")
  否则：
    打印(转字符串(数) 加 " 是 奇数")

段落 成绩等级 接收 分数：
  如果 分数 大于等于 90：
    返回 "优秀"
  否则若 分数 大于等于 80：
    返回 "良好"
  否则若 分数 大于等于 70：
    返回 "中等"
  否则若 分数 大于等于 60：
    返回 "及格"
  否则：
    返回 "不及格"

判断奇偶(7)
判断奇偶(12)
打印("分数 85 的等级是：" 加 成绩等级(85))
打印("分数 42 的等级是：" 加 成绩等级(42))