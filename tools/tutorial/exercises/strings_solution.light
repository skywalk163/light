# 练习 7：字符串处理（参考答案）

设 原始 为 "Hello, 段言!"。
打印("原始字符串：" 加 原始)

设 替换后 为 替换字符串(原始, "Hello", "你好")
打印("替换后：" 加 替换后)

设 水果 为 "apple,banana,orange"。
设 分割结果 为 分割字符串(水果, ",")
打印("分割后：" 加 转字符串(分割结果))

设 词列表 为 ["段言", "编程", "语言"]。
设 拼接结果 为 连接字符串(词列表, "-")
打印("拼接后：" 加 拼接结果)