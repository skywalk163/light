# 练习 5：函数定义和调用

## 题目描述
编写程序完成以下任务：
1. 定义一个 `求矩形面积` 函数，接收长和宽，返回面积
2. 定义一个 `求最大值` 函数，接收两个数，返回较大的那个
3. 调用这两个函数并打印结果

## 提示
- 使用 `段落 函数名 接收 参数1, 参数2：` 定义函数
- 使用 `返回 值` 返回结果
- 函数定义后直接调用即可

## 预期输出
```
矩形面积（长5，宽3）：15
最大值（7, 12）：12
```

## 参考答案
见 [functions_solution.duan](functions_solution.duan)