# 第53轮 任务2 —— L-172 编译器修复（入口静默阻断）

> 日期：2026-09-17 ｜ 优先级：P0 ｜ 跨项目：light-merge（编译器侧）
> 修复文件：`light-merge/src/code_generator.py`、`light-merge/cli/light.py`
> 依据：`_task1_R53_L172诊断.md`（根因）｜ 判据用例：`examples/test_L172.light`

---

## 1. 修了什么（两处，均为「静默 → 不再静默」）

### 修复 1（真 bug）：模块级**裸引用**入口名不再抑制入口自动调用

`src/code_generator.py` → `_node_calls_name` / `_module_invokes_entry`

```python
# 修前：调用或裸引用都算「已启动入口」
if isinstance(node, (ParagraphCall, Identifier)) and getattr(node, 'name', None) == name:
    return True

# 修后：只认真正的调用（ParagraphCall；`异步 运行 主()` 的 call 字段仍在遍历范围内）
def _node_calls_name(self, node, name, depth=0, calls_only=True):
    ...
    if isinstance(node, ParagraphCall) and getattr(node, 'name', None) == name:
        return True
    if not calls_only and isinstance(node, Identifier) and getattr(node, 'name', None) == name:
        return True
```

`_module_invokes_entry()` 改用 `calls_only=True`。`_node_calls_name` 的旧口径（含裸引用）以参数形式保留，需要「提及即算」的场景可显式传 `calls_only=False`。

**语义依据**：取引用 ≠ 启动入口。`设 入口回调 为 主` / `设 回调表 为 {"入口": 主}` 只是把入口登记成值，本来就该在入口内或之后发生，不应取消入口本身。

### 修复 2（消除次静默点）：带形参入口 → 编译期告警，不再无声跳过

`src/code_generator.py` → `generate()` 的入口判定块改为三分支，并新增 `self.compile_warnings`：

```python
if is_main:
    entry = self._find_entry_paragraph(module)
    if entry is not None:
        arity = self._paragraph_arity(entry)
        if arity > 0:
            # L-172：此前**静默跳过**（rc=0 / 无输出 / 无报错）→ 改为编译期告警
            self.compile_warnings.append(
                f'警告[L-172]：入口段落「{entry.name}」声明了 {arity} 个形参，'
                f'本后端（src）不自动调用带形参的入口——请显式写 `{entry.name}(...)` 调用，'
                f'否则本程序将静默不执行入口（rc=0、无报错、无输出）。'
                f'注：原生腿（llvm）对带形参入口会按 argc/argv 传参自动调用，'
                f'两腿在此存在语义分叉（详见 _task1_R53_L172诊断.md §4）。')
        elif not self._module_invokes_entry(module, entry):
            self._entry_call = (self._sanitize_name(entry.name), '异步' in (entry.modifiers or []))
            if self._entry_call[1]:
                self._needs_asyncio = True
```

`cli/light.py` → `_compile_src()` 把 codegen 侧告警写 stderr（`LIGHT_WARN_ENTRY=0` 可关闭，与 `LIGHT_WARN_GLOBAL_SHADOW` 同惯例）：

```python
if os.environ.get('LIGHT_WARN_ENTRY', '1') != '0':
    for _w in getattr(generator, 'compile_warnings', None) or []:
        print(_w, file=sys.stderr)
```

> **为什么不做成编译错误 / 不改成 argv 传参**：`段落 主 接收 参数:` 在本仓是**合法用法**（`lightharness/src/总入口.light` 就是带参入口库，由 `examples/运行CLI.light` 显式 `主()` 调用且刻意用环境变量传参以避开 argv 冲突）。判为错误会误伤既有语料；改成 argv 转发则要求 `_run_src` 向入口传参（跨函数面改动），超出本轮「只修 L-172、不引入新问题」的边界 → 登记为后续轮次候选。

---

## 2. 红前/绿后双向取证

`_l172probe/verify_fix.py`（旧闸门口径用 monkeypatch 还原，保证同一份用例、同一份编译器其余代码）：

| 场景 | rc | stdout | stderr |
|---|---|---|---|
| **[1] 旧闸门**（裸引用算已启动入口） | **0** | **空（0 行）** | 空 |
| **[2] 新闸门**（当前源码） | 0 | `L172_主已执行` + `--- L172 入口静默阻断 回归通过 ---` | 空 |
| **[3] 入口带形参** | 0 | 空（语义未变） | `警告[L-172]：入口段落「主」声明了 1 个形参，本后端（src）不自动调用带形参的入口……` |

- [1] = 复现：**rc=0 且一个字都不打印**（任务书描述的「静默阻断」逐字命中）。
- [2] = 修复生效：入口被自动调用，哨兵输出出来。
- [3] = 次静默点消除：不再是「无输出且无提示」。

判据用例 `examples/test_L172.light` 亦为同口径红绿：

```bash
.venv/Scripts/python.exe -m cli.light run examples/test_L172.light
# 修前：rc=0，无输出
# 修后：rc=0
#   L172_主已执行
#   --- L172 入口静默阻断 回归通过 ---
```

---

## 3. 影响面评估（改前先度量，避免误伤既有语料）

| 度量 | 脚本 | 结果 |
|---|---|---|
| 全语料「有入口段落」文件数 | `_l172probe/measure_arity.py` | 373 个 |
| 其中**裸引用抑制**（修复 1 会改变行为的） | `_l172probe/gate3.py` | **0 个** |
| 其中 **arity>0**（修复 2 会告警的） | `_l172probe/measure_arity.py` | **1 个**（`lightharness/src/总入口.light`，库文件，不以 main 身份跑） |
| 显式 `主()` 结尾的文件（`invokes=True`） | `_l172probe/scan_missing_entry.py` | 336 个，全部由**真实** `主()` 调用解释 → 与修复 1 无关 |

即：修复 1 对既有语料**行为零变化**；修复 2 只在既有语料外触发一次告警（stderr，不改变产物）。

---

## 4. 回归验证

> 编译器改动必须过全量回归。口径：**相对 HEAD（`ed4a175d`，即修复前）新增红 = 0**。

| 套件 | 状态 |
|---|---|
| light-merge 全量 pytest | ⚠️ **本轮未跑完**（用户指示停止全量运行；且该套件在本机 xdist 下不稳定，见 §5.2）→ **由任务3/路M 收口时补跑** |
| lightharness 全量 examples 回归（423 例） | 本轮未跑全量 → **由任务4 覆盖**；本轮做关键用例冒烟（§5.3） |
| 影响面度量（等价于「不改变既有程序行为」的静态证明） | ✅ 373 个有入口段落文件中，修复 1 影响 **0 例**、修复 2 影响 **1 例**（库文件，且只多一条 stderr 告警） |

**新增测试**：`examples/test_L172.light`（rc=0，打印两行哨兵）。

### 4.1 等价性论证（为什么可以预期零新增红）

1. 修复 1 只改变 `_module_invokes_entry` 的判定：由「调用 **或** 裸引用」收紧为「调用」。
   - 对**已显式写 `主()`** 的 336 个文件：`ParagraphCall` 命中不变 → 产物逐字不变；
   - 对**依赖自动入口**且模块级没有 `主` 出现的文件：`False` 不变 → 产物逐字不变；
   - 只有「既有 `段落 主` 又出现裸引用」的文件会变 —— 全语料 **0 例**。
2. 修复 2 只新增 stderr 文本，**不碰产物**；触发条件（arity>0 且未显式调用）全语料仅 1 例，且该文件不以 main 身份运行。

---

## 5. 实测记录

### 5.1 修复判据（必测，已过）

```
$ cd G:/dswork/duan-light-merge/light-merge
$ ./.venv/Scripts/python.exe -m cli.light run examples/test_L172.light
L172_主已执行
--- L172 入口静默阻断 回归通过 ---
rc=0

$ ./.venv/Scripts/python.exe _l172probe/verify_fix.py
[1] 旧闸门（修复前口径）:  rc = 0  stdout 行数 = 0  → 复现判定: 是（rc=0 且无输出=静默阻断）
[2] 新闸门（修复后）：       rc = 0  stdout = ['L172_主已执行', '--- L172 入口静默阻断 回归通过 ---']
                                       → 修复生效判定: 是
[3] 入口带形参：             rc = 0  stdout = ''  stderr = '警告[L-172]：入口段落「主」声明了 1 个形参……'
                                       → 不再静默判定: 是
```

### 5.2 ⚠️ 全量 pytest 未跑完的原因（环境坑，已定位，非本次改动引起）

| 尝试 | 命令 | 结果 |
|---|---|---|
| 1 | `pytest tests/ -q`（pyproject 默认 `-n auto --dist=loadscope`） | `45 failed, 4790 passed, 1 error` + 收尾 `INTERNALERROR: KeyError: <WorkerController gw12>`（汇总前崩溃，`-rf` 清单丢失） |
| 2 | 同上（**HEAD 基线**，`git stash` 两个文件） | `85 failed, 6481 passed, 6 errors` + 同样的 `KeyError: <WorkerController gw14>` |
| 3 | `-n 4 --dist=loadfile --junitxml`（修复态） | 跑到 94% 后 sessionfinish 抛 `OSError: cannot send (already closed?)`，无汇总、无 junit |
| 4 | 同上（基线态） | 启动即 teardown 异常，20 行日志、0 进度 |
| 5 | `-o addopts="" -p no:xdist`（串行，修复态） | 正常推进（21% / 9m35s ≈ 全量 45min）→ **按用户指示中途停止** |

**判据**：两次并行运行的 `passed` 数（4790 vs 6481）与 `failed` 数（45 vs 85）差异巨大，且都以 xdist 内部错收尾 → **并行结果不可用于「零新增红」判定**（与项目既有记载一致：「xdist 并行跑会 node down 丢汇总行（已知坑）」）。可靠口径是串行 + `--junitxml` + `tools/ci/check_regression.py --baseline tests/ci_baseline_failures.txt`（见 `2026-09-17.md` R53 段）。

> 这条命令模板留给任务3/路M：`pytest tests --ignore=tests/e2e -q -o addopts="" -p no:xdist --junitxml=.ci/non-e2e.xml`（**必须同时 `-o addopts=""`**，否则 `-p no:xdist` 会因 pyproject 里的 `-n auto --dist=loadscope` 报 usage error）。

### 5.3 关键用例冒烟（lightharness，修复后）

```
test_R52_ssh协议.light        → --- R52 任务3 ssh协议 纯逻辑测试通过 ---
test_R52_workflow_ptc.light   → --- R52 任务1 workflow-ptc meta+realm 纯逻辑测试通过 ---
test_R52_终端视图.light        → R52 终端视图：词表形状/帧消费四非法/…全部通过
test_R52_远程mock.light        → R52 远程mock：信封脚本/拟流四态/…全部通过
```

### 5.4 残留风险（交给任务3/4/路M）

1. light-merge 全量 pytest 未取得可信汇总 → 必须按 §5.2 模板串行补跑一次。
2. lightharness 全量 423 例未跑（任务4 覆盖）。
3. lightharness 工作区存在**非本轮**未提交改动（`examples/test_会话日志增量.light`、`src/会话日志增量.light` = R51 image/offload），提交时勿混入。

