# 第五轮合并报告

合并执行：2026-08-23，Windows 10 / PowerShell 5.1、Python 3.14。
合并前锚点 `e1dbfc3a`（标签 **`pre-merge-round5`**，同时也是第四轮的 `pre-merge-round4`）。
合并后 main = **`3053c75f`**。

## 一、合了什么

四路全合，按 B5 → A5 → C5 → D5 推进，每路 `--no-ff` 留独立合并提交，便于单路回滚。

- `5e0fce78` 合并 1/4 B5 —— 语言核心四补：一等函数值 / 推迟 / 异步遍历泛化 / 位运算
  - 落盘 `3f31c68f`
- `e3ef8325` **合并期修复（我写的）**：`函数` 当标识符在调用位置被误判为段落调用
- `88aa73a6` 合并 2/4 A5 —— 异步 I/O 内核，selector 接到语言层 `await`
  - 落盘 `dfb86e1a`、`44d0191d`、`edacf3c7`（TLS 上的异步读腿覆盖）
- `c201bb78` 合并 3/4 C5 —— 并发编排 stdlib（并发/重试/度量/JSONL）+ 语言内测试框架强化
  - 落盘 `2cd90359`
- `fc5bcd6c` 合并 4/4 D5 —— 试金石 harness（用光明写 deepseek-harness MVP）+ `cli harness` 子命令
  - 落盘 `7a154a6e`、`913b9cdf`、`32f47b5e`（真实 DeepSeek 链路实测 3/6=0.5）
- `3053c75f` **合并点修复（我写的）**：测试产物不再落回源码树 + 两条门禁基线按干净树重建

四路零冲突。全轮合计 43 files changed，+4251 / -138。

## 二、合并期我修的两类真缺陷

### 1. B5 的解析器回归：`函数` 当形参名不能用了（`e3ef8325`）

`函数` 是双身份的：既是 C 风格匿名函数关键字 `函数(参数){体}`，也是极常用的形参名。
B5 给 `函数(...)` 加了「后面没有 `{` 就回退」的路径，回退落进了「段落调用
`函数/段落 段名(...)`」分支，于是 `docs/L2_文言体语法规范_v4.0.md` 里的 `映射` 示例编不过。

修在解析器（`src/parser_expr.py` 段落调用分支）：关键字后直接跟 `(` 时，
说明这里的 `函数/段落` 被当普通变量名用，退回标识符走通用调用后缀。
**不改文档** —— 该文档块是合法 L2，且 B5 之前编得过，按既定口径缺陷归代码。

两向反跑：
- 新增 `tests/test_feature_core_light.py::Test函数当标识符` 两例（形参名可调用 + C 风格匿名函数仍可用），22 passed。
- 把修复变异掉，新测试与文档闸门**同时转红**；改回即绿。证明这条修复承重。
- 另确认修复后发的是**真调用**（`结果.append(函数(项))` → `[1, 4, 9]`），
  不是 B5 之前那种静默编成 lambda 的错编。

### 2. 测试产物落回源码树，让门禁在 CI 里必红（`3053c75f`）

CI 的 `.gitea/workflows/ci.yml` 里，「断言质量门禁」（第 217 行起）排在
「全量测试」（第 96 行起）**之后**，而门禁扫全仓 `.py`。
于是任何测试写进源码树的编译产物都会被门禁当成新增假绿断言——产物里带着
`src/code_generator.py:890` 发的 `assert _x is not None, "尝试解包空值"`。

命中两处（第三处在合并前已修）：
- `tests/test_e2e_full_coverage.py`：`test_bootstrap_file_compiles` /
  `test_bootstrap_chain_compilation` 把产物写回 `bootstrap/bootstrap_level{3,4,5}.py`
- `tests/test_feature_core_light.py`：`test_bitnot_block_compiles` 把产物写进 `积木库/blocks_v5/`

都改成 `compile -o` 到 `tempfile.TemporaryDirectory()`。

**这三个产物命中 `.gitignore:91`，不进仓** —— 所以 `git status` 和按 gitignore
过滤的文件搜索都看不见它们，只有 `dir` 能看到。我在本轮因此误判过一次
（D5 把这 3 条从基线里删掉，我先记成「CI 长红引信」，中途又翻成「本机红/CI 绿」，
最后按 workflow 步序 + 用例源码定案：**原判成立，CI 会红**）。

两向反跑：盘上留着产物时门禁报「新增违规 5 条」rc=1；删干净后 471 条 rc=0。

### 门禁基线：按干净树重建

- `tools/ci/assert_quality_baseline.json`：471 条，含两处纯行号漂移修正
  （`src/code_generator.py:874→890`、`tests/test_async.py:656→846`）。
- `tools/ci/bootstrap_rate_baseline.json`：刷到 **22/74 = 29.73%**
  （上轮 18/70 = 25.71%，增益来自 C5 新增的 4 个带真 `decl` 的 stdlib `.light`），
  影子数 53 不变，用新基线把成果锁死。

调用口径要点：门禁必须 `python tools/ci/assert_quality.py --root .`。
不带参数默认 `--root tests`，而基线键是相对扫描根的，会造出「424 新增违规」的幻影。
`--write-baseline` 需显式给路径，且必须与校验用同一个 root。

## 三、合并后验证（都在合并后的 main 上实跑）

逐路定向回归：

- B5：`tests/test_feature_core_light.py` 22 passed
- A5：异步 I/O 定向批次 **52 passed**（12.97s）
- C5：并发/度量/JSONL **36 passed**（2.54s）
- D5：harness e2e + 链路 **160 passed / 4 failed**（4:06，4 条即下文的 matplotlib 残留红）

主线全量：

- 除 `test_e2e_full_coverage.py` 的全量：**4331 passed / 14 failed / 86 skipped / 1 xfailed**（10:44）
- 合并点修复前的一次含 e2e 全量：**3926 passed / 11 failed / 56 skipped**（6:43）
- `-k bootstrap` 4 passed，且跑完 `git status --porcelain bootstrap 积木库` 为空
- 假测试门禁：471 条冻结、无新增，**通过**
- 自举率门禁：29.73% 持平、影子 53 未升、`引 Python` 逃逸 0、转手造假 0，**通过**

### 那 14 条红全部有归因，无一是合并回归

- `tests/test_datetime.py` 8 条：缺 `lunardate`
- `tests/test_http_client.py` 2 条：缺 `aiohttp`
- `tests/e2e/test_e2e_chain.py` 4 条：缺 `matplotlib`（CI 的全量步不含 `tests/e2e`）
- 另一次跑里 `tests/test_lightpub_bridge.py::test_获取JSON` 红过一次：httpbin 抖动。
  探针 `/get` 拿到 200 才没跳过，真调用却拿到非 200 → 复跑即绿。

按「本机装库转绿不算修好」的口径，这些库我没装。

## 四、留给第六轮的账（本轮**未**动）

1. **`等待 目标()` / `设 结果 为 目标()` 在局部绑定名上丢调用括号**。
   根在解析器：裸引用被包成零参 `ParagraphCall`，`目标` 与 `目标()` 无法区分；
   `src/code_generator.py:2768`（`if shadowed and not expr.args: return py_name`）
   于是把括号丢了。**不是本轮回归**，C5 的 `设 容器 为 [目标]` + `容器[0]()` 绕法必须保留。
2. **`stdlib/lightpub/HTTP客户端.py:589-594` 的 `获取JSON` 在非 200 时 `return None`**，
   把错误吞成空值——正是「零静默降级」口径要拦的形态。改它是行为变更，合并点不自裁。
3. **提案：编译器生成的 `.py`（首行 `# 由光明编译器生成`）在假测试门禁里豁免**。
   否则每个「测试编译真文件」的用例都得自己记得写临时目录。这是口径变更，等裁决。
4. 补跑 D5 的真实 DeepSeek 实测：它那次 3/6=0.5 是在 A5 合入**前**跑的，不带并发。
5. 跨平台 sep-guard 探针在干净树上复跑一次（上次跑时被并发的全量污染了）。

## 五、本轮踩到、值得写进规程的三条

1. **生成物是否残留，必须用 `dir` 看**。被 `.gitignore` 命中的产物，`git status`
   和按 gitignore 过滤的搜索都装作它不存在。
2. **门禁不能与测试并发跑**。我并发跑过一次，门禁把测试中途生成的产物扫成
   「新增 5 条」，白排查一轮。
3. **编辑变异要带上函数体行做唯一锚**。我按缩进匹配 `if ... LPAREN:` 时命中了
   `parser_expr.py:2860` 的同形行，回改写错缩进，整包 import 全崩。
