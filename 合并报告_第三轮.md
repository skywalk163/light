# 第三轮合并报告

合并执行：2026-08-22，Windows 10 / PowerShell 5.1。
合并前基线 `5e5943ca`，已打标签 **`pre-merge-round3-actual`**（回滚锚点，
注意与更早的 `pre-merge-round3` = `115e5e24` 不是同一个提交）。

## 一、合了什么

按既定顺序 C3 → A3 → B3 → D3 推进，**实际只合入了 C3 与 A3**，两条都用
`--no-ff` 留下独立合并提交，便于单独回滚某一路。

- `d94b4ac5` merge(round3): 合入 C3 —— 原生腿零静默降级 + 泛型注解 + 前端阻断指路
  - 落盘提交 `69e2c1bb`（12 files, +1062/-22）
- `c5d97df2` merge(round3): 合入 A3 —— harness 主干闭环、工具协议与沙箱 e2e
  - `616723e5` 请求体真带 tools（补协议断点）
  - `257f1cb9` 退避/预算/取消 + 主程序.light + e2e 磁盘判据
  - `bf71ea44` 显式 null 分帧修复（在线实测逼出来的真 bug）

两次合并**零冲突**：C3 只动 `src/**`、`docs/**` 与它自己的新测试；A3 只动
`stdlib/**`、`examples/harness/**` 与它自己的测试。文件所有权表这一轮真的起作用了。

## 二、B3 / D3 为什么没合

**三个分支（task-B3 / task-C3 / task-D3）在我接手时 HEAD 全都等于 main，
提交数为 0。** 成果全部悬在各自工作区未提交——这正是第二轮被整轮回滚的形态。

- **C3**：6 改 5 新 + `自测报告_任务C3.md`（五段齐全）。**唯一齐活的**，已代为落盘并合入。
- **B3**：`stdlib/代理工具集.light`、`tests/test_agent_tools_light.py`、
  改了 `tests/test_path_guard_light.py`。**无自测报告** → 按门禁不合。
- **D3**：只有 `tools/ci/assert_quality.py` 与 baseline json。
  **没有** `docs/known_issues.md` 改动、**没有** workflow 改动、**无自测报告** → 不合。

### wt-D3 的工作区曾处于危险状态（已修复）

`.git/worktrees/wt-D3/` 里**没有 `index` 文件**，却留着一个 0 字节的 `index.lock`。
之前有 git 进程写索引时被打断，锁没释放，此后该工作区任何要写索引的操作都失败
——**D3 大概根本提交不了，不是它偷懒**。

处置：删掉残留锁 + `git reset`（只重建索引，不动工作树）。`git status` 从
38922 条「全仓删除」降到 29883 条。剩下这些是**半检出**导致的幻影删除，
且 **100% 是非 ASCII 路径（纯 ASCII 路径 0 条）**，文件都在盘上，只是中文名
字节与索引对不上。结论：**在 wt-D3 里逐路径白名单提交是安全的**
（幻影删除留在未暂存区，进不了 commit），但**绝不可用 `git add -A` 或 `commit -a`**。

## 三、合并后验证

在合并后的 main 上实跑（不是沿用各路自称的数字）：

- A3 五个测试文件 + C3 四个新测试文件：**92 passed / 0 failed**（36.50s）
  （= A3 的 54 + C3 新增的 22+5+7+4=38）。
  C3 动了代码生成器，A3 的 stdlib `.light` 全走这条链——这条联合跑是本轮最该做的检查，**没有回归**。
- 更宽一层（`test_frontend_blockers_run.py` + `tests/e2e/test_llvm_pipeline.py` +
  全部 `tests/*_light.py`）：**233 passed / 8 skipped / 2 failed**。
- 合并前我还在 wt-C3 独立复跑过 C3 的定向批次：**99 passed / 0 failed**，
  与其报告分文件数字 22+5+7+4+61 逐项吻合。

### 那 2 条 failed 是我自己制造的假红，不是回归

- `test_process_tree_light.py::Test解码口径::test_默认编码按平台探测而非硬编码UTF8`
- `test_pty_light.py::TestWindowsConPTY::test_写输入不崩`

原因：我为了让中文输出可读，给整条命令加了 `PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8`。
而这两条用例的判据恰恰是「**默认编码要按平台探测**」和 ConPTY 的字节往返
——强制 UTF-8 把被测前提改掉了，它们**理应**变红。

**去掉这两个环境变量后单独复跑：20 passed / 4 skipped，全绿。**

由此固化一条操作口径（比这次的结论更重要）：

> **A3 家族的测试需要 `PYTHONUTF8=1` 才能正确读写中文；
> `test_process_tree_light.py` / `test_pty_light.py` 家族必须在不带
> `PYTHONUTF8` 的环境里跑。两者不能放在同一条命令里。**

这与已登记的「Windows 全量门禁 3565 errors 幻影」是同一类陷阱的另一副面孔：
上次是捕获流被 reconfigure 成 strict，这次是被测前提被环境变量改写。

## 四、未做与未测

- **全量门禁未跑**，因此本轮**没有**新的 Windows/Linux/FreeBSD 三平台基线数字。
  第三轮总纲 §7 的旧基线仍是唯一参照。
- **POSIX 未实测**：C3 的原生腿新能力与 A3 的 e2e 都只在 Windows 验证过。
- C3 自称的 1202 条全量定向批次我未复跑，沿用其落盘记录；我复跑的是与 diff
  直接相关的 99 条。
- `事件总线.light` 从「被 `.py` 影子挡着」变成真的被加载，是一次行为变更；
  只验证了 A3 范围内的 13 条用例，其它使用方未回归。

## 五、B3 / D3 的合入清单（缺什么）

### B3
1. 写 `自测报告_任务B3.md`，五段齐全（改了哪些文件 / 定向测试结果 / 移交清单 /
   已知未完成与未实测 / **skip 清单与掩盖分析**）。
2. `stdlib/代理工具集.light` 必须与已冻结契约对齐：
   `注册全部(循环, 根目录, 选项)`、`注册工具(名称, 描述, 参数模式, 实现)`，
   工具名冻结为 ASCII `read_file/write_file/edit_file/list_dir/grep/run_command`。
   **A3 已合入的 `代理循环.校验工具名` 会当场拒掉非 ASCII 名**，中文名过不了。
3. 合入后要把 `examples/harness/主程序.light` 里自带的三件套整体换成
   `代理工具集.注册全部(...)`，不要两套并存。
   `tests/test_harness_e2e.py` 的断言是按行为写的，**不需要改**。
4. 跑 `tests/test_agent_tools_light.py` + `tests/test_path_guard_light.py`
   （后者**不要**带 `PYTHONUTF8`）。

### D3
1. 写 `自测报告_任务D3.md`，五段齐全。
2. 交付面明显缺口：`docs/known_issues.md` 未更新、CI workflow 未改。
   现在只有 `tools/ci/assert_quality.py` + baseline。
3. 在 wt-D3 提交时**只能逐路径白名单 add**，理由见 §二。
   更稳妥的做法是**重建一个干净的 worktree** 再把这两个文件搬过去。
4. 两条已由 A3 核查更正的条目，别再当待办去追：
   - `tests/e2e/test_llvm_pipeline.py` 的 `returncode in [0,1]` **已不存在**，
     现在是 `assertEqual(result.returncode, 0)`（`:72-77`），全仓 grep 零命中。
   - `cli/light.py run` **不透传被测程序退出码**（`sys.exit(2)` 实测拿到 rc=1）。
     门禁若想靠 rc 区分错误类型，得先修这个。

## 六、回滚方式

```
git reset --hard pre-merge-round3-actual   # 回到 5e5943ca，两路都撤
git revert -m 1 c5d97df2                   # 只撤 A3
git revert -m 1 d94b4ac5                   # 只撤 C3
```

`--no-ff` 就是为了让后两条成立。

---

# 第二次合并：B3 与 D3（2026-08-23）

上面那份报告写在只合了 C3+A3 的时候。B3/D3 退回改 P0 之后已合入，本节接着记。

## 七、这次合了什么

```
main = 72cc1960
  72cc1960 fix(ci): 假测试门禁把 docstring 里的反面教材当成违规了   ← 合并期修的
  bfa089f9 merge(D3): 质量度量门禁真接进 CI + 假测试普查 + 文档腐烂勘误
  916994aa merge(B3): 代理工具集六个真工具 + Windows 杀树漏后代修复
  698bb7ff（前一份报告的终点，C3+A3）
```

- **B3** = `080b614d`（进程树杀树修复）+ `3cbc5470`（代理工具集 + 测试 + 报告）
- **D3** = `58b511f9`（门禁接入 CI + 基线入仓）+ `c23c16f9`（普查清单 + 魔法守卫 + 文档勘误）
- 两次 merge 都 `--no-ff`，都零冲突。回滚点：`pre-merge-round3-bd`（= 698bb7ff）

## 八、退回时点出的 P0，逐条落在哪

**B3（四条）**
1. 杀树测试恒真 → 改成真断言（孙子 sleep 2.5s、等到 4.0s、并按 PID 查存活），
   **一改就红**：孙子活着写出了标记。这是本次合并抓到的最实的一个缺陷 ——
   `进程树.light` 的 Windows 分支先 `taskkill /T`（不带 `/F`）求优雅，可能先把根杀掉，
   根一死 `是否存活()` 变假、`/F /T` 升级不执行、`/T` 也再枚举不到后代。
   改成直接 `taskkill /F /T`。变异验证：修前 marker=True，修后 marker=False。
2. 两条 `A not in x or B not in x` 恒真断言 → 各留唯一要守的那一半。
3. 大小写归一断言用了会被错误消息回显的文件名子串 → 改断言文件内容里的独有标记。
4. `list_dir` 递归不逐层过护栏 → 逐层 `解析路径` 重新核准，补
   `test_嵌套junction不被递归展开`（正例断链接名在、反例断外部条目名不在）。

**D3（两条硬阻断 + 两条顺带）**
1. 两个门禁脚本没被任何 workflow 引用 → 已接入 `.gitea`（权威）与 `.github`（镜像，仅 Linux），
   都不带 `|| true`。真探针：喂新增假绿 → rc=1；把自举率基线篡改成 0.99 → rc=1；无回退 → 双 0。
2. `积木库/blocks/文本/过滤阈值下文本.light` 被清零成 0 字节（越界、报告未提）→ 已恢复 410 字节，
   且不在提交范围内。
3. `.gitignore` 的 `*.json` 把两份门禁基线吃掉了（脚本进仓、基线不进仓，而两个脚本找不到基线
   都是 `return 2`，真接上 CI 就是长红）→ 加 `!tools/ci/*_baseline.json` 并把基线入仓。
   **这条是接 CI 时才暴露的，评审阶段两方都没看见。**
4. `.github` 装依赖补 `cryptography`（TLS/SSE 硬判绿点是模块级 import，缺包会 collect 就红）。

## 九、合并后验证（都在本机跑过）

- 定向回归 11 个文件：**184 passed, 7 skipped, 1 xfailed, 0 failed**
  （`test_agent_tools_light` / `test_path_guard_light` / `test_process_tree_light` /
  `test_pty_light` / `test_pure_light_hook` / `test_deepseek_mock` / `test_llm_client_light` /
  `test_agent_loop_light` / `test_event_bus_light` / `test_generics_c3` / `test_schema_light`）
- A3 的 harness 端到端：`tests/test_harness_e2e.py` **4 passed**
- 两道新门禁在合并后的 main 上：`assert_quality` rc=0、`bootstrap_rate` rc=0

**跨任务冲撞抓到一个**：C3 的 `tests/test_generics_c3.py` 模块 docstring 里写着
「不做 `assert 'T' in py_code` 那种字符串断言式假测试」当反面教材，D3 的纯正则把这句
**说明文字**当成新增违规，门禁在 main 上直接红。用 `tokenize` 跳过注释行与多行字符串的行修掉；
tokenize 失败的文件不豁免（宁可多报，不静默放过）。

两份基线按合并后真值刷新：假测试 105 → **103** 条；自举率 23.53% → **24.64%**
（B3 带进来 `stdlib/代理工具集.light`，分母分子同时 +1；不刷的话将来退回 23.53% 会被读成「持平」）。

## 十、仍然没做 / 仍然不成立

- **D3 判据 9（合并点全量对账）还是 ⏸**。本机跑不了 gitea 的全量口径，Windows 上跑全量会撞
  编码污染（捕获流被 reconfigure 成 strict + 子进程 GBK → 几千条幻影 error，不是回归）。
  留给有 Linux/FreeBSD runner 的一侧收。
- **两道新门禁只在本机验证过，没在真 runner 上跑过一次**。第一次 push 到 gitea/github 时要盯
  这两步，尤其 `.github` 那道 TLS/SSE 硬判 + 新装的 cryptography。
- `tools/ci/time_budget.py --dry-run` 打硬编码时间串、永远不会红 —— 移交。
- **`stdlib/路径护栏.light` 本轮没动**，它自述的两个缺口（硬链接逃逸、TOCTOU）在 B3 的六个
  文件工具下依然成立。B3 全部工具都建在这层护栏上，这是实际风险面。
- `grep` 的 `遍历目录` 同样不逐层核准；它不泄露内容（`护栏.读取` 会拒），但会走到沙箱外的
  目录树里遍历 —— 移交。
- `允许shell` 默认关的那条 POSIX 分支（`["sh","-c"] 加上 命令数组`）参数映射不正确，本机无
  POSIX 环境，未实测。
- `_构建敏感拒绝` 漏 `AWS_SECRET_ACCESS_KEY` / `SECRET_KEY` / `*_PASSPHRASE` 这类命名。
- 断言质量扫描仍不覆盖 `self.assertIn(...)`（仓内 122 处）、`assert len(x) >= N`（N>0，27 处）、
  `assert x is not None`（174 处）。任务书点名过最后这一形态，本轮未扩，避免一次把基线撑爆。

## 十一、这次的回滚方式

```
git reset --hard pre-merge-round3-bd   # 回到 698bb7ff，B3/D3 两路都撤（C3+A3 保留）
git revert -m 1 916994aa               # 只撤 B3
git revert -m 1 bfa089f9               # 只撤 D3
git revert 72cc1960                    # 只撤合并期那个门禁误判修复
```

## 十二、给下一轮的三条经验

1. **报告说「已验证」不等于验证过**。B3 的判据 7 与 D3 的判据 2 都标了 ✅，一个是恒真断言、
   一个根本没接上闸门。合并方必须自己跑一次判据本身，不能只读结论。
2. **恒真断言的两种新形态记进口径**：`A not in x or B not in x`（双否定 or 恒真）、
   以及「断言的子串会被错误消息回显的入参喂饱」。后者比上界断言更隐蔽，因为它长得像正常断言。
3. **门禁的判绿依据必须跟着脚本一起进仓**，并且要用「造一次回退看它红不红」来验收接入 ——
   `.gitignore` 那条 `*.json` 就是靠这个才暴露的。

