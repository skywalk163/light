# 第三轮合并报告

合并执行：2026-08-22，Windows 10 / PowerShell 5.1。
合并前基线 `5e5943ca`，已打标签 **`pre-merge-round3-actual`**（回滚锚点，
注意与更早的 `pre-merge-round3` = `115e5e24` 不是同一个提交）。

## 一、合了什么

按既定顺序 C3 → A3 → B3 → D3 推进，**实际只合入了 C3 与 A3**，两条都用
`--no-ff` 留下独立合并提交，便于单独回滚某一路。

- `d94b4ac5` merge(round3): 合入 C3 —— 原生腿零静默降级 + 泛型注解 + 前端阻断指路
  - 落盘提交 `69e2c1bb`（12 files, +1062/-22）
- `c5d97df2` merge(round3): 合入 A3 —— harness 主干闭环、工具协议与沙箱 e2e
  - `616723e5` 请求体真带 tools（补协议断点）
  - `257f1cb9` 退避/预算/取消 + 主程序.light + e2e 磁盘判据
  - `bf71ea44` 显式 null 分帧修复（在线实测逼出来的真 bug）

两次合并**零冲突**：C3 只动 `src/**`、`docs/**` 与它自己的新测试；A3 只动
`stdlib/**`、`examples/harness/**` 与它自己的测试。文件所有权表这一轮真的起作用了。

## 二、B3 / D3 为什么没合

**三个分支（task-B3 / task-C3 / task-D3）在我接手时 HEAD 全都等于 main，
提交数为 0。** 成果全部悬在各自工作区未提交——这正是第二轮被整轮回滚的形态。

- **C3**：6 改 5 新 + `自测报告_任务C3.md`（五段齐全）。**唯一齐活的**，已代为落盘并合入。
- **B3**：`stdlib/代理工具集.light`、`tests/test_agent_tools_light.py`、
  改了 `tests/test_path_guard_light.py`。**无自测报告** → 按门禁不合。
- **D3**：只有 `tools/ci/assert_quality.py` 与 baseline json。
  **没有** `docs/known_issues.md` 改动、**没有** workflow 改动、**无自测报告** → 不合。

### wt-D3 的工作区曾处于危险状态（已修复）

`.git/worktrees/wt-D3/` 里**没有 `index` 文件**，却留着一个 0 字节的 `index.lock`。
之前有 git 进程写索引时被打断，锁没释放，此后该工作区任何要写索引的操作都失败
——**D3 大概根本提交不了，不是它偷懒**。

处置：删掉残留锁 + `git reset`（只重建索引，不动工作树）。`git status` 从
38922 条「全仓删除」降到 29883 条。剩下这些是**半检出**导致的幻影删除，
且 **100% 是非 ASCII 路径（纯 ASCII 路径 0 条）**，文件都在盘上，只是中文名
字节与索引对不上。结论：**在 wt-D3 里逐路径白名单提交是安全的**
（幻影删除留在未暂存区，进不了 commit），但**绝不可用 `git add -A` 或 `commit -a`**。

## 三、合并后验证

在合并后的 main 上实跑（不是沿用各路自称的数字）：

- A3 五个测试文件 + C3 四个新测试文件：**92 passed / 0 failed**（36.50s）
  （= A3 的 54 + C3 新增的 22+5+7+4=38）。
  C3 动了代码生成器，A3 的 stdlib `.light` 全走这条链——这条联合跑是本轮最该做的检查，**没有回归**。
- 更宽一层（`test_frontend_blockers_run.py` + `tests/e2e/test_llvm_pipeline.py` +
  全部 `tests/*_light.py`）：**233 passed / 8 skipped / 2 failed**。
- 合并前我还在 wt-C3 独立复跑过 C3 的定向批次：**99 passed / 0 failed**，
  与其报告分文件数字 22+5+7+4+61 逐项吻合。

### 那 2 条 failed 是我自己制造的假红，不是回归

- `test_process_tree_light.py::Test解码口径::test_默认编码按平台探测而非硬编码UTF8`
- `test_pty_light.py::TestWindowsConPTY::test_写输入不崩`

原因：我为了让中文输出可读，给整条命令加了 `PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8`。
而这两条用例的判据恰恰是「**默认编码要按平台探测**」和 ConPTY 的字节往返
——强制 UTF-8 把被测前提改掉了，它们**理应**变红。

**去掉这两个环境变量后单独复跑：20 passed / 4 skipped，全绿。**

由此固化一条操作口径（比这次的结论更重要）：

> **A3 家族的测试需要 `PYTHONUTF8=1` 才能正确读写中文；
> `test_process_tree_light.py` / `test_pty_light.py` 家族必须在不带
> `PYTHONUTF8` 的环境里跑。两者不能放在同一条命令里。**

这与已登记的「Windows 全量门禁 3565 errors 幻影」是同一类陷阱的另一副面孔：
上次是捕获流被 reconfigure 成 strict，这次是被测前提被环境变量改写。

## 四、未做与未测

- **全量门禁未跑**，因此本轮**没有**新的 Windows/Linux/FreeBSD 三平台基线数字。
  第三轮总纲 §7 的旧基线仍是唯一参照。
- **POSIX 未实测**：C3 的原生腿新能力与 A3 的 e2e 都只在 Windows 验证过。
- C3 自称的 1202 条全量定向批次我未复跑，沿用其落盘记录；我复跑的是与 diff
  直接相关的 99 条。
- `事件总线.light` 从「被 `.py` 影子挡着」变成真的被加载，是一次行为变更；
  只验证了 A3 范围内的 13 条用例，其它使用方未回归。

## 五、B3 / D3 的合入清单（缺什么）

### B3
1. 写 `自测报告_任务B3.md`，五段齐全（改了哪些文件 / 定向测试结果 / 移交清单 /
   已知未完成与未实测 / **skip 清单与掩盖分析**）。
2. `stdlib/代理工具集.light` 必须与已冻结契约对齐：
   `注册全部(循环, 根目录, 选项)`、`注册工具(名称, 描述, 参数模式, 实现)`，
   工具名冻结为 ASCII `read_file/write_file/edit_file/list_dir/grep/run_command`。
   **A3 已合入的 `代理循环.校验工具名` 会当场拒掉非 ASCII 名**，中文名过不了。
3. 合入后要把 `examples/harness/主程序.light` 里自带的三件套整体换成
   `代理工具集.注册全部(...)`，不要两套并存。
   `tests/test_harness_e2e.py` 的断言是按行为写的，**不需要改**。
4. 跑 `tests/test_agent_tools_light.py` + `tests/test_path_guard_light.py`
   （后者**不要**带 `PYTHONUTF8`）。

### D3
1. 写 `自测报告_任务D3.md`，五段齐全。
2. 交付面明显缺口：`docs/known_issues.md` 未更新、CI workflow 未改。
   现在只有 `tools/ci/assert_quality.py` + baseline。
3. 在 wt-D3 提交时**只能逐路径白名单 add**，理由见 §二。
   更稳妥的做法是**重建一个干净的 worktree** 再把这两个文件搬过去。
4. 两条已由 A3 核查更正的条目，别再当待办去追：
   - `tests/e2e/test_llvm_pipeline.py` 的 `returncode in [0,1]` **已不存在**，
     现在是 `assertEqual(result.returncode, 0)`（`:72-77`），全仓 grep 零命中。
   - `cli/light.py run` **不透传被测程序退出码**（`sys.exit(2)` 实测拿到 rc=1）。
     门禁若想靠 rc 区分错误类型，得先修这个。

## 六、回滚方式

```
git reset --hard pre-merge-round3-actual   # 回到 5e5943ca，两路都撤
git revert -m 1 c5d97df2                   # 只撤 A3
git revert -m 1 d94b4ac5                   # 只撤 C3
```

`--no-ff` 就是为了让后两条成立。
