# 光明翻译器测试 - 翻译对照表

**模型**: light-translator-v5
**时间**: 2026-07-25 19:27:34
**测试用例**: 30

## basic_01 - 变量赋值 [PASS]

**Python:**
```python
x = 10
name = "hello"
print(x, name)
```

**光明:**
```
设 x 为 10
定义 name 等于 "hello"
打印(x, name)
```

**通过项:**
- keyword '设 x 为' present
- keyword '定义' present
- keyword '打印' present

**耗时**: 33.6s

---

## basic_02 - 条件判断 [FAIL]

**Python:**
```python
score = 85
if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
else:
    grade = "C"
print(grade)
```

**光明:**
```
设 score 为 85
如果 score 大于等于 90：
    设 grade 为 "A"
否则如果 score 大于等于 80：
    设 grade 为 "B"
否则：
    设 grade 为 "C"
打印(grade)
```

**问题:**
- MISSING keyword: '否则若'
- FOUND 否则如果 instead of 否则若
- ERROR: 否则如果 should be 否则若

**通过项:**
- keyword '如果' present
- keyword '否则' present

**耗时**: 9.6s

---

## basic_03 - for循环 [PASS]

**Python:**
```python
for i in range(5):
    print(i)
```

**光明:**
```
遍历 i 于 0至4：
    打印(i)
```

**通过项:**
- keyword '遍历' present
- keyword '打印' present

**耗时**: 5.4s

---

## basic_04 - while循环 [PASS]

**Python:**
```python
count = 0
while count < 10:
    count += 1
print(count)
```

**光明:**
```
设 count 为 0
当 count 小于 10：
    count 加上 1
打印(count)
```

**通过项:**
- keyword '当' present
- keyword '加上' present

**耗时**: 6.5s

---

## basic_05 - break_continue [PASS]

**Python:**
```python
for i in range(10):
    if i == 3:
        continue
    if i == 7:
        break
    print(i)
```

**光明:**
```
遍历 i 于 0至9：
    如果 i 等于 3：
        跳过
    如果 i 等于 7：
        跳出
    打印(i)
```

**通过项:**
- keyword '遍历' present
- keyword '跳过' present
- keyword '跳出' present
- break correctly translated
- continue correctly translated

**耗时**: 7.3s

---

## ops_01 - 算术运算 [PARTIAL]

**Python:**
```python
a = 10
b = 3
print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a // b)
print(a % b)
print(a ** b)
```

**光明:**
```
设 a 为 10
设 b 为 3
打印(a 加 b)
打印(a 减 b)
打印(a 乘 b)
打印(a 除以 b)
打印(a 取余 b)
打印(a 乘方 b)
```

**问题:**
- MISSING keyword: '幂'
- SUSPECT: Chinese text '乘方' may be translated variable name

**通过项:**
- keyword '加' present
- keyword '减' present
- keyword '乘' present
- keyword '除以' present
- keyword '取余' present
- // correctly translated
- % correctly translated

**耗时**: 7.7s

---

## ops_02 - 复合赋值 [PASS]

**Python:**
```python
x = 10
x += 5
x -= 3
x *= 2
x //= 4
print(x)
```

**光明:**
```
设 x 为 10
x 加上 5
减去 3
乘以 2
除以 4
打印(x)
```

**通过项:**
- keyword '加上' present
- keyword '减去' present
- keyword '乘以' present
- keyword '除以' present

**耗时**: 5.6s

---

## ops_03 - 负数 [FAIL]

**Python:**
```python
def get_index():
    return -1

temp = -100
print(get_index(), temp)
```

**光明:**
```
返回 减 100
```

**问题:**
- MISSING keyword: '返回 -1'
- Negative number incorrectly translated
- ERROR: 返回 减 should be 返回 -

**耗时**: 4.1s

---

## ops_04 - 逻辑运算 [PARTIAL]

**Python:**
```python
a = True
b = False
print(a and b)
print(a or b)
print(not a)
```

**光明:**
```
设 a 为 真
设 b 为 假
打印(a 和 b)
打印(a 或 b)
打印(not a)
```

**问题:**
- MISSING keyword: '且'
- MISSING keyword: '非'

**通过项:**
- keyword '或' present

**耗时**: 5.1s

---

## func_01 - 函数定义 [PARTIAL]

**Python:**
```python
def greet(name):
    return f"Hello, {name}!"

print(greet("World"))
```

**光明:**
```
返回 f"Hello, {name}!"
```

**问题:**
- MISSING keyword: '段落'
- MISSING keyword: '接收'

**通过项:**
- keyword '返回' present

**耗时**: 3.7s

---

## func_02 - 多返回值 [PARTIAL]

**Python:**
```python
def divmod_result(a, b):
    return a // b, a % b

q, r = divmod_result(17, 5)
print(q, r)
```

**光明:**
```
设 a 为 17
设 b 为 5
返回 a 除以 b, a 取余 b
```

**问题:**
- MISSING keyword: '段落'

**通过项:**
- keyword '返回' present
- keyword '除以' present
- keyword '取余' present
- // correctly translated

**耗时**: 4.9s

---

## func_03 - 递归 [PARTIAL]

**Python:**
```python
def factorial(n):
    if n <= 1:
        return 1
    return n * factorial(n - 1)

print(factorial(5))
```

**光明:**
```
如果 n 小于等于 1：
    返回 1
否则：
    返回 n 乘 裁定(n 减 1)
```

**问题:**
- MISSING keyword: '段落'
- MISSING keyword: '乘以'
- SUSPECT: Chinese text '裁定' may be translated variable name

**通过项:**
- keyword '如果' present
- keyword '返回' present

**耗时**: 4.5s

---

## func_04 - lambda [PARTIAL]

**Python:**
```python
square = lambda x: x ** 2
print(square(5))
```

**光明:**
```
设 square 为 接收 x：返回 x 乘 x 乘 x
打印(square(5))
```

**问题:**
- MISSING keyword: '幂'

**通过项:**
- keyword '接收' present
- keyword '返回' present

**耗时**: 3.8s

---

## data_01 - 列表操作 [PASS]

**Python:**
```python
lst = [1, 2, 3]
lst.append(4)
lst[0] = 10
print(lst)
print(len(lst))
```

**光明:**
```
设 lst 为 [1, 2, 3]
lst.append(4)
lst[0] = 10
打印(lst)
打印(len(lst))
```

**通过项:**
- keyword '设' present
- keyword 'append' present
- keyword 'len' present

**耗时**: 4.5s

---

## data_02 - 列表推导 [PARTIAL]

**Python:**
```python
squares = [x ** 2 for x in range(10)]
print(squares)
```

**光明:**
```
设 squares 为 []
遍历 x 于 1至9：
    squares.追加(x 乘 x)
打印(squares)
```

**问题:**
- MISSING keyword: '幂'
- SUSPECT: Chinese text '追加' may be translated variable name

**通过项:**
- keyword '遍历' present

**耗时**: 3.8s

---

## data_03 - 字典操作 [PASS]

**Python:**
```python
d = {"name": "Alice", "age": 30}
print(d["name"])
d["age"] = 31
print(d.get("age"))
```

**光明:**
```
设 d 为 {"name": "Alice", "age": 30}
打印(d["name"])
d["age"] = 31
打印(d.get("age"))
```

**通过项:**
- keyword '设' present
- keyword '打印' present

**耗时**: 4.2s

---

## data_04 - f-string [PASS]

**Python:**
```python
name = "Alice"
age = 30
print(f"{name} is {age} years old")
```

**光明:**
```
设 name 为 "Alice"
设 age 为 30
打印(f"{name} is {age} years old")
```

**通过项:**
- keyword '打印' present

**耗时**: 3.4s

---

## oop_01 - 类定义 [PARTIAL]

**Python:**
```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    
    def distance(self):
        return (self.x ** 2 + self.y ** 2) ** 0.5

p = Point(3, 4)
print(p.distance())
```

**光明:**
```
class Point：
    属性 x、y
    构造 接收 x、y：
        己x 为 x
        己y 为 y
    
    段落 距离：
        返回 (己x 乘 己x 加 己y 乘 己y) ** 0.5

设 p 为 新建 Point(3, 4)
打印(p距离())
```

**问题:**
- MISSING keyword: '类'
- MISSING 己. for self
- SUSPECT: Chinese text '距离' may be translated variable name
- SUSPECT: Chinese text '距离' may be translated variable name

**通过项:**
- keyword '构造' present
- keyword '己' present

**耗时**: 5.7s

---

## oop_02 - 类继承 [PARTIAL]

**Python:**
```python
class Animal:
    def __init__(self, name):
        self.name = name
    
    def speak(self):
        return f"{self.name} makes a sound"

class Dog(Animal):
    def speak(self):
        return f"{self.name} barks"

d = Dog("Rex")
print(d.speak())
```

**光明:**
```
类 动物：
    属性 名字
    构造 接收 名字：
        己名字 为 名字
    段落 声音：
        返回 f"{己名字} makes a sound"

class 狗 继承 动物：
    段落 声音：
        返回 f"{己名字} barks"

设 d 为 新建 狗("Rex")
打印(d.声音())
```

**问题:**
- SUSPECT: Chinese text '动物' may be translated variable name
- SUSPECT: Chinese text '名字' may be translated variable name
- SUSPECT: Chinese text '名字' may be translated variable name
- SUSPECT: Chinese text '己名字' may be translated variable name
- SUSPECT: Chinese text '名字' may be translated variable name
- SUSPECT: Chinese text '声音' may be translated variable name
- SUSPECT: Chinese text '己名字' may be translated variable name
- SUSPECT: Chinese text '动物' may be translated variable name
- SUSPECT: Chinese text '声音' may be translated variable name
- SUSPECT: Chinese text '己名字' may be translated variable name
- SUSPECT: Chinese text '声音' may be translated variable name

**通过项:**
- keyword '类' present
- keyword '继承' present
- keyword '构造' present

**耗时**: 5.6s

---

## exc_01 - try_except [PARTIAL]

**Python:**
```python
try:
    result = 10 / 0
except ZeroDivisionError as e:
    print(f"Error: {e}")
finally:
    print("Done")
```

**光明:**
```
尝试：
    设 result 为 10 除以 0
捕获 捕获 特殊异常 e：
    打印(f"Error: {e}")
最终：
    打印("Done")
```

**问题:**
- SUSPECT: Chinese text '特殊异常' may be translated variable name

**通过项:**
- keyword '尝试' present
- keyword '捕获' present
- keyword '最终' present
- try correctly translated
- except correctly translated
- finally correctly translated

**耗时**: 3.6s

---

## exc_02 - raise [PASS]

**Python:**
```python
def check_age(age):
    if age < 0:
        raise ValueError("Age cannot be negative")
    return age

print(check_age(25))
```

**光明:**
```
如果 age 小于 0：
    抛出 "ValueError"("Age cannot be negative")
否则：
    返回 age
```

**通过项:**
- keyword '如果' present
- keyword '抛出' present
- raise correctly translated

**耗时**: 2.9s

---

## file_01 - 文件读写 [PASS]

**Python:**
```python
with open("test.txt", "w") as f:
    f.write("Hello")

with open("test.txt", "r") as f:
    content = f.read()
print(content)
```

**光明:**
```
使用 打开文件("test.txt", "w") 为 f：
    f.write("Hello")
使用 打开文件("test.txt", "r") 为 f：
    设 content 为 f.read()
打印(content)
```

**通过项:**
- keyword '使用' present
- keyword '打开文件' present
- with correctly translated

**耗时**: 3.3s

---

## algo_01 - 冒泡排序 [PARTIAL]

**Python:**
```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr

print(bubble_sort([64, 34, 25, 12, 22, 11, 90]))
```

**光明:**
```
段落 冒泡排序 接收 arr：
    设 n 为 len(arr)
    遍历 i 于 range(n)：
        遍历 j 于 range(0, n 减 i 乘 2)：
            如果 arr[j] 大于 arr[j 加 1]：
                定义 tmp 等于 arr[j]
                arr[j] = arr[j 加 1]
                arr[j 加 1] = tmp
    返回 arr

打印(冒泡排序([64, 34, 25, 12, 22, 11, 90]))
```

**问题:**
- SUSPECT: Chinese text '冒泡排序' may be translated variable name
- SUSPECT: Chinese text '冒泡排序' may be translated variable name

**通过项:**
- keyword '段落' present
- keyword '遍历' present
- keyword '如果' present

**耗时**: 5.7s

---

## algo_02 - 二分查找 [PARTIAL]

**Python:**
```python
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1

print(binary_search([1, 3, 5, 7, 9], 5))
```

**光明:**
```
段落 二分查找 接收 arr, target：
    设 left 为 0
    设 right 为 len(arr) 减 1
    当 left 小于等于 right：
        设 mid 为 (left 加 right) 除以 2
        如果 arr[mid] 等于 target：
            返回 mid
        否则若 arr[mid] 小于 target：
            设 left 为 mid 加 1
        否则：
            设 right 为 mid 减 1
    返回 倒数

打印(段落 二分查找([1, 3, 5, 7, 9], 5))
```

**问题:**
- MISSING keyword: '返回 -1'
- SUSPECT: Chinese text '二分查找' may be translated variable name
- SUSPECT: Chinese text '倒数' may be translated variable name
- SUSPECT: Chinese text '二分查找' may be translated variable name

**通过项:**
- keyword '段落' present
- keyword '当' present
- keyword '如果' present
- keyword '否则若' present
- // correctly translated
- elif correctly translated

**耗时**: 5.8s

---

## algo_03 - Fibonacci [PARTIAL]

**Python:**
```python
def fib(n):
    if n <= 1:
        return n
    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b
    return b

for i in range(10):
    print(fib(i))
```

**光明:**
```
如果 n 小于等于 1：
    返回 n
否则设 a 为 0
设 b 为 1
遍历 _ 于 2至n加1：
    设 a 为 b
    设 b 为 a 加 b
跳过
```

**问题:**
- MISSING keyword: '段落'

**通过项:**
- keyword '如果' present
- keyword '遍历' present
- keyword '返回' present

**耗时**: 3.1s

---

## algo_04 - 质数判断 [PARTIAL]

**Python:**
```python
def is_prime(n):
    if n < 2:
        return False
    for i in range(2, int(n ** 0.5) + 1):
        if n % i == 0:
            return False
    return True

print(is_prime(17))
```

**光明:**
```
如果 n 小于 2：
    返回 假
否则遍历 i 于 2至int(n 乘 0.5 加 1)：
    如果 n 取余 i 等于 0：
        返回 假
返回 True

打印(is_prime(17))
```

**问题:**
- MISSING keyword: '段落'

**通过项:**
- keyword '如果' present
- keyword '遍历' present
- keyword '取余' present
- keyword '返回 假' present
- % correctly translated

**耗时**: 3.3s

---

## complex_01 - FizzBuzz [PASS]

**Python:**
```python
for i in range(1, 16):
    if i % 15 == 0:
        print("FizzBuzz")
    elif i % 3 == 0:
        print("Fizz")
    elif i % 5 == 0:
        print("Buzz")
    else:
        print(i)
```

**光明:**
```
遍历 i 于 range(1, 16)：
    如果 i 取余 15 等于 0：
        打印("FizzBuzz")
    否则若 i 取余 3 等于 0：
        打印("Fizz")
    否则若 i 取余 5 等于 0：
        打印("Buzz")
    否则：
        打印(i)
```

**通过项:**
- keyword '遍历' present
- keyword '如果' present
- keyword '否则若' present
- keyword '取余' present
- keyword '打印' present
- elif correctly translated
- % correctly translated

**耗时**: 3.9s

---

## complex_02 - 高阶函数 [PARTIAL]

**Python:**
```python
nums = [1, 2, 3, 4, 5]
evens = list(filter(lambda x: x % 2 == 0, nums))
squared = list(map(lambda x: x ** 2, nums))
print(evens, squared)
```

**光明:**
```
设 evens 为 [1, 2, 3, 4, 5]
设 squared 为 list(map(lambda x: x ** 2, nums))
打印(evens、squared)
```

**问题:**
- MISSING keyword: '筛选'
- MISSING keyword: '映射'
- MISSING keyword: '接收'
- MISSING keyword: '取余'
- MISSING keyword: '幂'

**耗时**: 2.7s

---

## complex_03 - 字典推导 [PASS]

**Python:**
```python
words = ["hello", "world", "python"]
word_lens = {w: len(w) for w in words}
print(word_lens)
```

**光明:**
```
设 words 为 ["hello", "world", "python"]
定义 word_lens 等于 {w: len(w) 遍历 w 之 words}
打印(word_lens)
```

**通过项:**
- keyword '遍历' present
- keyword 'len' present

**耗时**: 2.4s

---

## complex_04 - 装饰器 [PARTIAL]

**Python:**
```python
def timer(func):
    def wrapper(*args, **kwargs):
        result = func(*args, **kwargs)
        return result
    return wrapper

@timer
def say_hello():
    print("Hello!")

say_hello()
```

**光明:**
```
段落 间隔 计时 接收 函数：
    段落 包装 接收 *
    设 结果 为 函数 *
    返回 结果
段落 返回 包装

返回 包装 说 hello
```

**问题:**
- MISSING keyword: '标注'
- SUSPECT: Chinese text '间隔' may be translated variable name
- SUSPECT: Chinese text '计时' may be translated variable name
- SUSPECT: Chinese text '函数' may be translated variable name
- SUSPECT: Chinese text '包装' may be translated variable name
- SUSPECT: Chinese text '结果' may be translated variable name
- SUSPECT: Chinese text '函数' may be translated variable name
- SUSPECT: Chinese text '结果' may be translated variable name
- SUSPECT: Chinese text '包装' may be translated variable name
- SUSPECT: Chinese text '包装' may be translated variable name

**通过项:**
- keyword '段落' present

**耗时**: 3.0s

---

