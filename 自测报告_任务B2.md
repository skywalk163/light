# 自测报告 · 任务 B2（原生腿验证路径与 TLS）

分支 `task-B2`，基线 `e27277c2`，工作树 `g:\dswork\duan-light-merge\wt-B2`。

---

## 1. 改了什么

| 文件 | 增量 | 内容 |
|---|---|---|
| `src/llvm/runtime_typed.c` | +989 | poller 后端换 WSAPoll/poll、注册表按需增长、拒绝路径不再静默丢 fd；新增 Schannel TLS 段（`dv_tls_*`） |
| `src/llvm/codegen_typed.py` | +134 | 只改**外部声明段 + 内建注册段**（与 A2 的重叠约束）：14 条 `declare` + `包装tls/握手tls/发送tls/接收tls/释放tls/校验tls/信任证书tls/错误tls` 与 `poller后端/poller计数/poller错误` 内建 |
| `src/llvm/compiler.py` | +6/-3 | 三处链接标志加 `-lsecur32 -lcrypt32`（Schannel + 证书链） |
| `tests/test_llvm_net.py` | +253 | B2-1 逐腿 skip 判据；把 6 条弱断言改成真断言；补 clang 腿/MCJIT 腿各自的用例 |
| `tests/e2e/test_llvm_pipeline.py` | +38 | 拆掉 `assertIn(returncode, [0,1])` 这道假闸，改成 `assertEqual(returncode, 0)` + 缺依赖时 `skipTest` |
| `tests/test_llvm_c_runtime.c` | 新增（+57 本轮追加） | C 层直调 runtime；本轮追加 test 19-22：后端自报 + 超容量记账 |
| `tests/test_llvm_c_unit.py` | 新增（+2 用例、链接标志修正） | 驱动上面那个 C 程序；新增「后端与容量记账」「select 回退是编译期宏」两条 |
| `tests/test_llvm_tls.c` / `tests/test_llvm_tls.py` | 新增（本轮修 2 处） | TLS 正负两例；本轮修「证书生成走 cryptography 而不是硬依赖 openssl」「subprocess 指定 utf-8 编码」 |
| `docs/B2-5_HTTP决策分析.md` | 新增 | B2-5 决策 + 11 条缺失特性 + 7 条移交清单 |

### B2-1 逐腿 skip 判据

判据落在 **mark** 上，不是只写在 reason 文本里：

```python
HAS_CLANG_LEG = CLANG_PATH is not None
HAS_MCJIT_LEG = HAS_LLVMLITE and MSVC_VCVARS is not None
skip_no_clang_leg = pytest.mark.skipif(not HAS_CLANG_LEG, reason=_clang_leg_reason)
skip_no_mcjit_leg = pytest.mark.skipif(not HAS_MCJIT_LEG, reason=_mcjit_leg_reason)
```

**每条 skip 掩盖了什么**（reason 里逐字写着）：

| skip 条件 | 掩盖的未验证事实 |
|---|---|
| 缺 clang | `.light → IR → clang → exe → 真跑` 这条**发布形态**路径未验证；socket 原语 / poller / 事件循环的原生端到端行为全部未验证（B1/B2/B3） |
| 缺 llvmlite 或 MSVC | `IR → llvmlite MCJIT → 进程内执行`未验证；**IR 本身能否被 LLVM 官方 `parse_assembly` + `verify` 接受**没人查过——clang 腿即使 IR 类型写歪，只要后端还能编出东西就发现不了 |
| 缺 C 编译器（C 层测试） | `dv_socket_*` / `dv_poller_*` / 事件循环的 C 层行为未验证；后端选择、超容量拒绝而非静默丢 fd 都没跑 |
| 缺 clang（select 回退用例） | `DV_POLLER_FORCE_SELECT` 宏分支是否还能编译、select 回退是否在 FD_SETSIZE 处显式拒绝，均未验证 |
| 非 Windows / 无 Schannel | `dv_tls_*` 一行没跑：握手让权、收发往返、证书校验负例全部未验证 |
| 缺 cryptography 且无 openssl | 生不出自签证书 → TLS 三条全未验证 |

### B2-3 poller

- 后端由**编译期宏**选：`DV_POLLER_FORCE_SELECT` > `_WIN32`→WSAPoll > POSIX→poll。没有运行时选择。
- `DV_POLLER_MAX 256` 从**硬上限**改成**初始容量**：WSAPoll/poll 后端按需翻倍增长。
- **拒绝而不是静默丢**：`dv_poller_register` 在 NULL poller / fd<0 / select 后端超 FD_SETSIZE / realloc 失败时返回 -1 并写中文 `g_poller_error`。
- `dv_coro_await_io` 原来**忽略注册返回值**（注册失败协程就永久挂着）。现在注册失败会打 stderr 并把协程放回 run_queue。
- `dv_poller_wait_n` 新增容量参数，就绪数超出输出缓冲时**返回 -1 并报错**，不截断上报；事件循环按注册数分配缓冲。

### B2-4 TLS

`dv_tls_wrap/handshake/send/recv/free` + `dv_tls_want_event/is_ready/flush_public/set_verify/add_trusted_cert_file/last_error/backend`，Windows Schannel（无第三方依赖）。

- **与 `dv_coro_await_io` 协作**：握手是可重入状态机，返回 `DV_TLS_WANT_READ/WANT_WRITE`；`dv_tls_want_event()` 返回的正是 `DV_POLL_READ/DV_POLL_WRITE`——与 `dv_coro_await_io` 吃的是同一套掩码。半包写不完进 `out_pending` 队列，返回 WANT_WRITE 而不是阻塞。
- **证书校验默认开**（`g_tls_verify_default = 1`）。关闭开关默认不生效，且 `dv_tls_set_verify(t, 0)` 会往 stderr 打【安全告警】。
- 正例**不是靠关校验过的**：`dv_tls_add_trusted_cert_file()` 把自签证书装进内存 store，用 `CertCreateCertificateChainEngine` + **`hExclusiveRoot`** 建链引擎（curl `--cacert` 语义，比系统根**更严**）。负例是同一个 server 不给信任锚，必须失败。

---

## 2. 怎么验的（B2-2 路线选择）

**选 (b) clang 直接原生编译**，理由与另两条为什么不选：

- (a) 装 llvmlite：本机 clang 已在（`C:\Program Files\LLVM\bin\clang.exe`，22.1.8，target `x86_64-pc-windows-msvc`），不需要为了转绿装库；且 MCJIT 进程内执行离发布形态更远。
- (b) clang 直编：**已选**。`.light → IR → clang → exe → 与真 Python echo server 收发 → 比对 stdout`，就是 M3 里程碑的定义本身。
- (c) 纯 C 层单测：**额外做了，不当替代**。它完全不经过 IR 生成，IR 写歪它照样绿；但它能独立给 socket/poller 层一个绿，还能断言 static 状态（后端名、错误文本、注册计数），这些经 IR 摸不到。

M3 那条真绿的命令（在 `wt-B2` 下）：

```
C:\Python314\python.exe -m pytest tests/test_llvm_net.py -v
```

---

## 3. 定向测试结果

只跑本模块定向文件，**没跑全量**。

| 文件 | 结果 |
|---|---|
| `tests/test_llvm_net.py` | **11 passed, 2 skipped**（skip = MCJIT 腿，缺 llvmlite） |
| `tests/test_llvm_c_unit.py` | **4 passed** |
| `tests/test_llvm_tls.py` | **2 passed**（正例 + 负例） |
| `tests/unit/test_ir_verify.py` | **16 passed, 2 skipped**（skip = `TestVerifyIRWithLlvmlite` 两条，缺 llvmlite） |
| `tests/e2e/test_llvm_pipeline.py` | **5 passed, 1 skipped**（skip = `test_simple_ir_generation`，IR 生成由 `tests/test_llvm_net.py` 覆盖） |

### 3.1 合并一次跑（实测）

```
C:\Python314\python.exe -m pytest tests/test_llvm_net.py tests/test_llvm_c_unit.py ^
  tests/test_llvm_tls.py tests/unit/test_ir_verify.py tests/e2e/test_llvm_pipeline.py ^
  tests/test_llvm_async.py tests/test_llvm_exception.py ^
  tests/test_llvm_optimization.py tests/test_llvm_optimizer.py -v
```

结果：**collected 135 items → 130 passed, 5 skipped, 26 warnings in 153.46s (0:02:33)**。

5 条 skip 全部落在「缺 llvmlite」和「与他处重复覆盖」这两类，逐条掩盖内容见第 1 节 `B2-1 逐腿 skip 判据` 的表：

| skip | 条数 | 掩盖了什么 |
|---|---|---|
| `TestMcjitLegOnly`（`tests/test_llvm_net.py`） | 2 | IR 能否被 LLVM 官方 `parse_assembly` + `verify` 接受、runtime 符号能否从 DLL 解析 |
| `TestVerifyIRWithLlvmlite`（`tests/unit/test_ir_verify.py`） | 2 | 同上（`verify_ir` 的 llvmlite 后端半边） |
| `test_simple_ir_generation`（`tests/e2e/test_llvm_pipeline.py`） | 1 | 无掩盖：同一条链路由 `tests/test_llvm_net.py` 的 clang 腿真跑覆盖 |

**0 failed、0 error**。26 条 warning 全是既有的 `PytestReturnNotNoneWarning`（`test_llvm_exception.py` 等老文件里 `return True` 风格的测试），不是本轮引入的。

几条真断言的样子（不是「stdout 里有没有某个词」）：

- `test_socket_last_error`：以前是 `test_socket_connect_failure` 的逐字副本、从没调过 `socket错误码`。现在断言 `连接socket` 返回 -1、`socket错误码()` 非 0、`socket错误()` 有可读文本。
- `test_poller_basic`：以前只断言 stdout 含子串 `poller`（"poller创建成功" 一行就够绿）。现在断言后端名 ∈ {WSAPoll,poll,select}、Windows 上必须是 WSAPoll、注册前计数 0、注册后 1。
- `test_poller_select_one_ready`：以前完全不看就绪数（poller 返回 0 靠 recv 阻塞也能过）。现在断言 1 ≤ ready ≤ 2 且收到 `ping1`。
- `test_coro_sleep_basic`：以前只断言 `sleep前`（协程 yield 后再没恢复也算过）。现在断言四行顺序 `开始/sleep前/sleep后/结束`。
- `test_concurrent_echo`：以前只断言 `hello1`（段落2 没跑也算过）。现在两条都要到，且顺序符合延迟。
- `test_local_var_preservation`：以前断言 `"42"`（第一行的 42 就够）。现在断言 `["42","test","42"]` 整表。
- `test_socket_send_recv_large`：以前断言 `"A"*100`（256 被截成 100 也算过）。现在要求原样 256 字节回来。
- 超容量：断言 `accepted + rejected == made`（静默丢 fd 会让这条账对不上）、`dv_poller_count == accepted`、有拒绝就必须有错误文本。实测 WSAPoll 后端 300 个 fd 全接受且注册表长过 256；`-DDV_POLLER_FORCE_SELECT` 重编后自报 `select` 且**确实拒了**一批、错误文本非空。
- TLS 正例断言 `VERIFY_DEFAULT_ON=1`、`HANDSHAKE_RC=0`、握手**至少让权过一次**、`RECV` 与发出内容一致；负例断言握手非 0 + 错误文本非空。

---

## 4. 诚实交代（本轮没验到的、以及任务书前提的偏差）

1. **任务书与总纲的前提在本机是过期的**。两份文件都写 `tests/test_llvm_net.py` 10 条红是「缺 llvmlite + `find_clang()` 失败」。本机**clang 在**，clang 腿被走通，改动前实测就是 **10 passed / 62.97s**，不是 10 failed。所以 B2-1 修的不是「让红转绿」，而是「让 skip 判据逐腿成立，并把绿得太容易的断言收紧」。这条差异必须记档，否则下一轮还会照着「10 红」排期。
2. **MCJIT 腿本机仍未验证**：llvmlite 确实没装。它掩盖的是「生成的 IR 能否被 LLVM 官方 `parse_assembly` + `verify` 接受」。CI 语义：装了 llvmlite + 有 MSVC 就该跑，不该 skip；skip 时 reason 会写清这句。**没装库转绿，这条就照实挂着。**
3. **TLS 只有 Windows 半边**。POSIX 侧是诚实 stub（`dv_tls_backend()` 返回 `"none"`，各函数返回 ERROR 并打一次「本平台无 TLS 后端」），不是假装成功。mbedTLS/OpenSSL 路线本轮没做。
4. **TLS 只验了客户端方向**：服务端 TLS（`AcceptSecurityContext`）没写、没测。
5. **`.light` 层的 TLS 内建没有端到端用例**。`包装tls/握手tls/…` 的 `declare` 与内建注册进去了、IR 能生成，但「.light 源码真跑一次 TLS 往返」没有测试钉住——TLS 的真跑证据全在 C 层。这是**顺延不是销账**。
6. **事件循环 + TLS 的合体没验**：C 层测试里握手让权用的是手写 poller 循环（掩码与 `dv_coro_await_io` 同一套），但没有一条用例是「协程里做 TLS 握手 → 挂 IO 等待队列 → 被事件循环唤醒」。
7. **`git status` 不能只看自己那几个文件**：本轮第一次 dump 时 PowerShell 管道把输出截断成 1 行，看着像「只有 codegen_typed.py 改了」。改用 `cmd /c git … > 文件` 才拿到全量。第 7 节是全量核对结果。
8. **首版报告写在 `docs/B2_自测报告.md`，那份的数字已过期**（37 passed/1 skipped、只列 1 条 skip、一份过期的 `git status` 快照、以及「10 条全部假红」这个与实测相反的说法）。本文件是正本；那份已就地加更正说明并指过来，没有删除。两份并存但**不许再出现两套数字**。


---

## 5. 产物与清理

我的测试会生成：

| 产物 | 谁生成 | 是否自清 |
|---|---|---|
| `tests/_taskB2_*.ll` / `tests/_taskB2_*.exe` | `run_net_test`（clang 腿） | 是（`_cleanup`） |
| `tests/_taskB2_runtime.def` / `.dll` | `_ensure_runtime_dll`（MCJIT 腿） | **否**，要手删 |
| `tests/_taskB2_c_test.exe` / `_taskB2_c_test_select.exe`（+`.obj`） | C 层测试 | 是（finally），异常中断会留 |
| `%TEMP%\_taskB2_tls_*\`（证书、key、exe） | TLS 测试 `teardown_class` | 是 |
| 仓库根 `_taskB2_*.txt` | 我的跑测日志（`.gitignore:191 *.txt` 已忽略） | **否**，要手删 |
| 仓库根 `runtime_typed.obj` | MSVC 腿编译 runtime 时留下的目标文件 | **否**，要手删（本轮已删） |


清理命令（PowerShell，在 `wt-B2` 下）：

```
Remove-Item -Force -ErrorAction SilentlyContinue tests\_taskB2_*, _taskB2_*.txt, runtime_typed.obj
Remove-Item -Recurse -Force -ErrorAction SilentlyContinue "$env:TEMP\_taskB2_tls_*"
```


---

## 6. 移交清单（`原生腿写不出什么 → 临时绕法 → 期望能力`）

正本在 `docs/B2-5_HTTP决策分析.md` §5，共 **7 条**：

| 原生腿写不出什么 | 临时绕法 | 期望能力 |
|---|---|---|
| `生成`（yield / 生成器） | 回调式或收完再返回，牺牲流式 | yield 语句 + 生成器遍历（codegen 协程状态机扩展） |
| `b""` bytes 字面量 | 用字符串代替（二进制不安全） | bytes 类型，或二进制安全字符串 |
| `缓冲[0:分隔]` 切片 | 手写循环逐字符拷贝 | `[start:end]` 切片（IndexAccess 加 Slice 分支） |
| `导入 socket` | C 层已有 `dv_socket_*`，缺虚拟模块映射 | `导入 socket` 自动映射到 native socket |
| `.encode("utf-8")` | C 层加 `dv_str_encode` | `.encode()` 方法注册 |
| `codecs.getincrementaldecoder` | 一次性解码 | 增量 UTF-8 解码器 |
| `s.recv()` / `s.sendall()` | `dv_socket_recv` / `dv_socket_send` | socket 对象方法封装（LightValue OBJ + 方法表） |

**B2-5 决策**：HTTP/1.1 + chunked **不在 C 层重写**。C 层只出原语（socket/TLS/poller/事件循环），协议逻辑复用 C2 已写的 `stdlib/流式.light`；补齐上面 7 条后可直接复用，不用改那 267 行 `.light`。理由与代码量/可维护性/两条腿行为一致性的对照见该文档 §2。这条与 A2-1 的「原生后端未支持语句清单」对齐。

---

## 7. 收尾 git status 逐行核对

用 `cmd /c git … > 文件` 拿全量（**不用 PowerShell 管道**，见第 4 节第 7 条）：

```
git -C g:/dswork/duan-light-merge/wt-B2 status --porcelain --untracked-files=all
```

本次提交前的输出，逐行核对：

| 行 | 判断 |
|---|---|
| ` M docs/B2-5_HTTP决策分析.md` | 我改的（一处错字 `C 屽` → `C 层加`） |
| ` M docs/B2_自测报告.md` | 我改的（首版报告加过期更正 + 指向本文件） |
| ` M tests/test_llvm_tls.py` | 我改的（负例 `subprocess.run` 补 `encoding="utf-8"`） |
| `?? 自测报告_任务B2.md` | 本文件，本次新增 |

**四行全部是我的改动，没有第五行。** 其中三行本来是前面 5 个 commit 的收尾余量，本次一并提交。

`git diff --stat e27277c2`（基线 → 现在，含已提交部分）共 **11 个文件、+2677/-76**：
`docs/B2-5_HTTP决策分析.md`、`docs/B2_自测报告.md`、`src/llvm/codegen_typed.py`（仅内建注册 + 外部声明两段，A2 的重叠面没碰）、`src/llvm/compiler.py`、`src/llvm/runtime_typed.c`、`tests/e2e/test_llvm_pipeline.py`、`tests/test_llvm_c_runtime.c`、`tests/test_llvm_c_unit.py`、`tests/test_llvm_net.py`、`tests/test_llvm_tls.c`、`tests/test_llvm_tls.py`。

**`stdlib/`、`积木库`、其他 worktree 零变动**——上面全量输出里没有任何一行落在那些路径下。分支 `task-B2`，**没有 push、没有 merge、没有 tag**。

已落的 5 个 commit：`241031be`（poller + Schannel TLS）、`9733bf75`（TLS 正负例）、`4516b9e0`（B2-5 决策 + 首版报告）、`ffdb92a2`（poller 后端与容量会计）、`c413a5e8`（证书生成改 cryptography 优先）。

