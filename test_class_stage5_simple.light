打印 "=== 阶段5：运算符重载返回对象测试 ==="。

类 数字：
    属性 值。
    
    段落 数字 接收 初始值：
        己值 为 初始值。
    
    段落 __add__ 接收 其他：
        返回 新建 数字(己值 加 其他.值)。
    
    段落 __sub__ 接收 其他：
        返回 新建 数字(己值 减 其他.值)。
    
    段落 __mul__ 接收 倍率：
        返回 新建 数字(己值 乘 倍率)。
    
    段落 __div__ 接收 分：
        返回 新建 数字(己值 除 分)。
    
    段落 显示：
        打印 "值: " 加 转文本(己值)。

打印 "--- 测试1：加法重载返回对象 ---"。
设 n1 为 新建 数字(10)。
设 n2 为 新建 数字(5)。
打印 "n1 = "。
n1.显示()。
打印 "n2 = "。
n2.显示()。
设 n3 为 n1 加 n2。
打印 "n1 加 n2 = "。
n3.显示()。

打印 "--- 测试2：减法重载返回对象 ---"。
设 n4 为 n1 减 n2。
打印 "n1 减 n2 = "。
n4.显示()。

打印 "--- 测试3：乘法重载返回对象 ---"。
设 n5 为 n1 乘 3。
打印 "n1 乘 3 = "。
n5.显示()。

打印 "--- 测试4：除法重载返回对象 ---"。
设 n6 为 n1 除 2。
打印 "n1 除 2 = "。
n6.显示()。

打印 "--- 测试5：基础类型运算不受影响 ---"。
设 a 为 100。
设 b 为 20。
打印 "100 加 20 = " 加 转文本(a 加 b)。
打印 "100 减 20 = " 加 转文本(a 减 b)。
打印 "100 乘 20 = " 加 转文本(a 乘 b)。
打印 "100 除 20 = " 加 转文本(a 除 b)。

打印 "=== 测试完成 ==="。
