# 第十轮总纲（2026-08-27 起草，遗留未修项排期）

> **前置**：`协作规程.md` + 第二～九轮总纲全文仍有效，本文只做**增补与覆盖**，冲突处以本文为准。
> **仓库**：`g:\dswork\duan-light-merge\light-merge`，主分支 `main`，工作树干净。
> **代码基线 = 第九轮收尾点**（开工当天自己跑 `git rev-parse HEAD` 取值并 `git tag pre-merge-round10 <当日 HEAD>`，别在任务书里写死 SHA）。
> **来源**：本文排期全部来自 `docs/known_issues.md` 里「发现但**未修**」的条目 + 第九轮移交清单里
> 明确挂账、未在第九轮 S1–S3 内收掉的项。第九轮已排期的项（POSIX TLS → B9 S2、L5 → D9 S1、
> `light build --target native` → B9 S3、影子数 → C9 M22、原生腿产品清单 → B9/G9）**不在此重复排**，
> 只在本节「已归属第九轮」表里备查，避免双排。

---

## 0. 本轮的排法

第十轮是**清账轮**：九轮下来 `known_issues.md` 攒了一批「能修但没轮到」的真缺陷，本轮按
「谁的问题归谁修」原则分进泳道，**每一条都要带反跑判据**（没有反跑不算判据——沿用第七/九轮口径）。

排期来源逐条列在 §1 表里，出处都指向 `docs/known_issues.md` 的节号，不靠记忆。

### 已归属第九轮的项（备查，勿双排）

| 未修项 | 出处 | 第九轮归属 |
|---|---|---|
| POSIX TLS stub（有壳无实现） | `known_issues.md §17.2` | B9 S2：mbedTLS/OpenSSL 三选一 |
| 溢出文件绝对路径泄进 stdout | `known_issues.md §17.3` | D9 S1：`进程树.light:248` 改回显 basename + `:666` 断言大小写不敏感 |
| `light build --target native` 单文件 exe | 第九轮总纲 B9 S3 | B9 S3 |
| 影子数 53 → ≤35 | 第九轮总纲 C9 M22 | C9 |
| 原生腿产品清单.json | 第九轮总纲 §4.3 | B9 建 / G9 守 |
| Lambda/切片/推导式/元组 原生支持 | 第九轮总纲 A9 S2 | A9 S2（至少两类） |

---

## 1. 遗留未修项清单（本轮要处理的）

| # | 条目 | 出处 | 现状 | 本轮处置计划 |
|---|---|---|---|---|
| **R10-1** | `cli/lightc.py` 对任何输入必崩：`SemanticAnalyzer()` 少传 `module` 参数 → `TypeError`；`tests/conftest.py` 的 `analyzer` fixture 是同一处签名不匹配 | `§12.2` | 未修，门禁看不见（无用例消费） | **修**：补 `module` 参数；给 `cli/lightc.py` 加一条真跑用例（`python cli/lightc.py <文件>` rc=0 且输出正确）；`conftest.py` 的 `analyzer` fixture 一并修好或删除死代码 |
| **R10-2** | `_inline_small_functions_pass` 过度激进：把「指令数 ≤5 且被调用 ≤1 次」的非 main 函数整段删掉，不看其它引用形式 | `§12.4` | 未修，潜伏 | **修**：内联前统计全部引用形式（含地址取用、间接调用、外部引用），被引用即不内联；补反跑判据（构造一个「被地址引用的小函数」，修复前被删、修复后保留且行为正确） |
| **R10-3** | 原生腿拒绝文案没有源码行号：`AstAdapter` 转换不带 `lineno`，后端拿不到行号 | `§9.4` | 未修，需动 `src/compiler.py` | **修**：`AstAdapter.convert` 保留 `lineno`（v3 节点带行号，转换时透传）；`_gen_statement` 拒绝文案带行号；改 `test_llvm_stmt_coverage.py` 里「源码行 未知」的断言为真实行号 |
| **R10-4** | `除以` 算术语义双后端分叉：转译 `/` 真除法 vs 原生腿 `i64 /` 整数截断 | `§15.1` | 未修，需裁决 | **先裁决后修**：两条路——(a) 原生腿补浮点类型，`除以` 双端都是浮点除法；(b) 把 `除以` 定为整数除法、靠 `整除`/浮点字面量区分。文档现口径是 `/` 与 `//`，倾向 (a)。裁决后把 `tests/test_llvm_c3_expr.py` 那 6 例的 `整除` 换回 `除以` 当判据 |
| **R10-5** | 转译后端 stdout 编码不钉死：Windows 上非 ASCII 输出按 ANSI 代码页（cp936），与原生腿 UTF-8 字节不一致，无法映射字符抛 `UnicodeEncodeError` | `§15.2` | 未修，需单独一轮带反跑做 | **修**：CLI 入口 `sys.stdout.reconfigure(encoding='utf-8', errors='replace')`（**必须带 `errors=`**，防第四轮「reconfigure 收紧成 strict 整场级联」的坑，见 `§14.1`）；加反跑：Windows 上输出重定向进文件，断言字节是 UTF-8 而非 cp936 |
| **R10-6** | 自举编译器覆盖范围：极边缘特性（如复杂嵌套泛型）未完全自举 | `§一 P0-1` | ~~未修，计划 v6.1~~ → **已评估关闭** | **评估完成**：第九轮 A9/C9 已消除大部分差距（泛型类/函数/嵌套泛型/类型别名/异步段落/符号运算符/推导式/切片/match/decorator/interface 等 17+ 特性均由 src/ 权威解析器覆盖）。原描述「复杂嵌套泛型未自举」不成立。实际剩余 3 条缺口：G1 SemanticAnalyzer 与 v3 Module 不兼容（v7.0 架构重构）、G2 lambda 表达式（v6.1）、G3 with 语句（v6.1）。自举率门禁 41.57% ≥ 基线 40.48%，未倒退。已回写 `docs/known_issues.md §一 P0-1` 为已关闭。 |
| **R10-7** | LLVM 后端标准库覆盖：部分 stdlib 函数在 LLVM 后端未完全实现，回退 Python 解释执行 | `§一 P0-2` | 未修，计划 v6.1 | **评估后决定**：以第九轮交付的「原生腿产品清单.json」为准，逐模块核对「原生可编译比例 ≥60%」；未达标的模块列出阻断原因，交给对应泳道补实现或显式标不可编译 |

---

## 2. 泳道与归属

| 泳道 | 主题 | 主战场 | 认领项 |
|---|---|---|---|
| **A10** | 语言层收尾（前端 + 原生 codegen 表达式/语句分派） | `src/parser_*.py`、`src/code_generator.py`、`src/llvm/codegen_typed.py`（语句/表达式分派段） | R10-3（行号透传，需动 `src/compiler.py` 的 `AstAdapter`，A10/B10 联合）、R10-4 裁决后的实现、R10-6 语法自举盘点 |
| **B10** | 原生腿产品路径 | `src/llvm/compiler.py`、`cli/light.py`、`src/package_manager.py` | R10-2（内联 pass）、R10-7（原生可编译比例核对） |
| **G10** | 度量 / 门禁 / 文档（主线兼任） | `tools/ci/*`、`docs/known_issues.md`、合并报告 | R10-1（lightc.py 崩溃，工具链脚本归主线）、R10-5（CLI 入口编码，归属 A10/B10 协商）、R10-4 的裁决记录、所有已修项的状态翻篇 |

> **重叠约定**：R10-3 与 R10-5 都跨泳道，动手前先出**一行方案**交给主线裁决归属；不许单边开工。

---

## 3. 判据与红线（沿用第九轮，新增一条）

- 每条处置必须带**反跑判据**：改哪一行会让它立红（R10-2/R10-4/R10-5 已在上表写明反跑方式，其余补上）。
- 红线沿用第九轮五条：不许建同名 `.py`、不许 `引 Python` 掩盖缺陷、不许造壳、不许靠本机装库转绿、不许在合并前刷基线。
- **新增红线**：`known_issues.md` 里标记「已解决」的条目**不许再被当成新问题排查**；处置完的条目必须回写文档状态（勾选 + 证据行），不留「修了但文档没翻篇」的账。

---

## 4. 环境与协作（照抄第九轮，不改）

- 每人一个 worktree：`git worktree add ../wt-X10 -b task-X10 main`，只在自己目录里作业。
- **只跑定向测试，不许跑全量**；定向测试超过 5 分钟先问主线。
- 端口一律 `bind(("127.0.0.1", 0))` + `getsockname()[1]` 取回，不写死端口。
- 临时文件加任务前缀 `_taskX10_*`，收尾删干净。
- 杀进程只许按自己 spawn 的 PID，**绝对禁止按进程名杀**。
- 中文输出必须落盘再读；落盘用 `cmd /c` 字节重定向。
- 临时验证脚本不写盘，用 `python -c` 或落到临时目录，收尾删。

## 5. 交付格式（每条认领项一份）

1. 改了哪些文件（逐个 + 一句意图）
2. 定向测试结果（跑了哪些，输出落盘）
3. 反跑判据：改哪一行立红 + 复位后绿
4. 已知未完成 / 未实测项（只在 Windows 实测过的 POSIX 分支必须标「未实测」）
5. 处置完同步回写 `docs/known_issues.md`（勾选 + 证据行 + commit 号）

---

# 增补 A：复刻暴露的语言缺陷排期（lightharness → 语言泳道）

> **来源**：`g:\dswork\duan-light-merge\lightharness\docs\功能对标\语言缺陷账.md`（L-001~L-011，全部带最小复现与绕法）。
> 这些是用光明复刻 DeepSeek Harness 实测压出来的**语言本体缺陷**，归语言团队（A10 主修 / G10 流程）处置；
> lightharness 侧已用命名规范等绕法先行，修复后应逐步松绑绕法（见「处置完回填」要求）。

| # | 缺陷（摘要） | lightharness 现状/绕法 | 本轮处置计划 | 泳道 |
|---|---|---|---|---|
| **L-004/L-007**（家族，最高优先） | 词法器按关键字边界拆分中文标识符，且有状态（新增局部变量会改变其他成员名拆分结果） | 已用 `光明命名规范v1.md`（危险词根禁入成员名 + 铁律）绕行；大量成员被迫改名 | **修**：标识符整体最长匹配优先，拆分决策与符号表状态无关（确定性、可复现）；修复后 lightharness 回填命名规范状态并逐步松绑 | A10 |
| L-001 | 跨目录点号模块导入不可用 | 扁平目录 + 命名前缀绕行 | **修**：支持 `包/子模块` 点号导入 | A10 |
| L-002 | 不支持多行表达式/字面量续行 | 复杂字面量压单行绕行（可读性差） | **修**：括号内换行续行（Python 风格） | A10 |
| L-003 | 类内成员空列表赋值被误编译成比较/拆分 | 拆类 / 改名 / 同名 temp 自赋值绕行 | **修**：`设 己.X 为 []` 稳定编译为赋值 | A10 |
| L-005 | 非 self 的成员下标访问被编译成调用（`总线.监听器表[事件名]` → `总线.监听器表([事件名])`） | 临时变量承接绕行 | **修**：成员访问 + 下标组合稳定编译为下标 | A10 |
| L-006 | 闭包内不能修改外层局部标量（无 nonlocal 等价物） | 改用可变容器（list/dict）绕行 | **修**：内层段落对外层标量赋值生成 nonlocal 或报清晰错误 | A10 |
| L-007b/007c | 调用方局部变量名污染跨模块成员访问；类内局部变量与属性名前缀重叠触发属性拆分 | 安全改名绕行 | **修**：成员名解析只由对象自身类定义决定，与调用方/类内局部变量无关 | A10 |
| L-008 | JSON 序列化入口不统一（`序列化` vs `从 JSON 导入 序列化JSON`） | 统一 `从 JSON 导入 序列化JSON` | **修/文档**：全局 `序列化` 或统一文档化入口 | A10 |
| **L-010** | 模块名/字段名首字符为关键字被拆（`主`/`令`/`返回` 等），**阻塞 12/13 退出码精确值** | 安全命名绕行；`命令结果.返回码` 改走 `标准输出/标准错误/是否成功` | **修**：`从 X 导入` 模块名 X 与字段名整体视为标识符；修复后 lightharness 的 12/13 退出码精确化任务（见增补 B T6）可解除阻塞 | A10 |
| L-011 | 关键字根 `等待`(await) 拆分成员访问（`外部命令.等待进程(...)` → `外部命令.等待(进程(...))`，校验编译未拦截） | `收尾进程` 别名绕行 | **修**：async 关键字族成员名可安全访问；`校验编译.py` 补拦截（成员名含关键字根 + 函数参数紧随） | A10 |
| L-009 | 注释内嵌 `导入 X` 字样被 `-replace` 类文本替换误伤（注释尾巴变代码行） | 工程规避（替换前检查注释） | **修**：词法忽略注释；批量替换工具避免命中注释/字符串内目标串 | G10 |

> **红线补充（增补 A 生效）**：处置 L-* 的提交必须带反跑判据（改哪一行使复现用例立红→复位绿），
> 复现用例以 `lightharness/examples/test_L0XX.light`（增补 B T7 固化）为准，禁止另造壳用例。

---

# 增补 B：复刻深对齐任务池（lightharness 独立仓库，T1~T7 并行分发）

> **仓库**：`g:\dswork\duan-light-merge\lightharness`（独立 git 仓库，主分支干净）。
> **运行**：`python 运行.py examples/xxx.light`（自包含 stdlib + 光明编译器）。
> **口径**：延续第一版「P0 全 + P1 部分」；本轮把 11/12/14 三个 partial 收口、新增异步代理循环与终端模块、
> 固化 11 条语言缺陷复现套件。**每个任务必须带反跑判据**（改断言一行即红）。
> **文件所有权**（防并行冲突）：每个任务只写自己「写」列的文件；只读依赖用现有公开接口，不得改动。
> 涉及 `对标清单.json` 时，各任务只更新自己的条目（按「编号 + 功能名」双字段匹配），并回写 `语言缺陷账.md`。

| 任务 | 主题 | 写（新增/修改） | 只读依赖 | 原版对照 | 依赖 |
|---|---|---|---|---|---|
| **T1** | 异步代理循环深对齐 | `src/异步代理.light`(新)、`src/中止.light`(新)、`src/重试.light`(新)、`src/客户端.light`(HTTP 重试/中止) | `代理.light`、`会话.light`、`流.light`、`工具.light` | `packages/core/agent-loop/src/agent.ts`、`core/agent/abort.ts`、`llm/llm-retry` | 无 |
| **T2** | CLI 实时 API 联调与交互收口（#11） | `src/总入口.light`、`examples/运行CLI.light`、`examples/test_联调CLI.light`(新) | `客户端.light`(只读) | `apps/cli`、`bundle/headless` | DEEPSEEK_API_KEY |
| **T3** | 文件与存储加深（#14 收口） | `src/文件.light`、`examples/test_文件深.light`(新) | 无 | `packages/fs`、`packages/storage` | 无 |
| **T4** | 终端(terminal/PTY) 模块新建（P1 补齐） | `src/终端.light`(新)、`examples/test_终端.light`(新) | `子进程.light`(只读) | `packages/terminal` | T6 公开接口保持稳定 |
| **T5** | 工具执行流水线深对齐（prepare/dispatch/finalize + JSON Schema 校验） | `src/工具.light`、`examples/test_工具深.light`(新) | `代理.light`(只读) | `packages/core/tools/json-schema.ts`、`agent-loop/tool-calls.ts` | 无 |
| **T6** | 子进程/沙箱退出码精确化（#12/#13 收口） | `src/子进程.light`、`src/沙箱.light`、`stdlib/外部命令.py` | `工具.light`(只读) | `packages/subprocess`、`packages/sandbox` | **L-010 修复后解除阻塞**（或先探索绕法） |
| **T7** | 语言缺陷复现套件固化（为增补 A 提供判据） | `examples/test_L001~L011.light`(新)、`docs/功能对标/语言缺陷账.md` | 全部(只读) | — | 无 |

> **T1/T2 边界**：`src/客户端.light` 修改权归 T1（HTTP 重试/中止）；T2 只调用现有对话接口，需要改客户端时
> 先出「一行方案」交由主线裁决，不许单边改。
> **T4/T6 边界**：`src/子进程.light` 修改权归 T6；T4 只读现有公开接口（运行后台/等待后台/运行进程），
> T6 必须保持这些公开签名不变。

每任务完整任务书（可直接复制分发）：`任务书/任务T10_01_异步代理循环.md` … `任务书/任务T10_07_缺陷复现套件.md`。
