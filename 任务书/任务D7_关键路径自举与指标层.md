# 任务 D7：关键路径自举 + 指标层

> **前置必读**：`第七轮总纲.md` 全文，特别是 §2.6/§2.7 真值表、§3 文件归属、§4.3 usage 与指标契约、§4.5 首日裁决、§5 硬约束。
> **里程碑**：M19（硬）、M21（可选，只升不降）。**人日预算 40**（只有 90 人日时砍到 20，只做 §3.1 阶段一 + §3.3 + §3.4）。
> **一句话**：自举率 29.73% 是**文件维度**的数字，而 harness 关键路径上的 JSON 与文件系统是两个 decl=0 的空壳——评测集读取和报告写盘的底层实现仍然是 Python。本轮把关键路径真正自举，并把评测指标从「只有通过率」补到「token/成本/分位」。

---

## 0. 你要解决的两个「指标说谎」问题

**问题一：自举率涨了，关键路径自举率还是 0。**
- `stdlib/JSON.light` **145 行、decl 0**：`:1-134` 是整段注释化的 Python 残骸（连 `段落` 头都没了），
  `:136-145` 是 10 条 `导出` 共 **56** 个名字。实现全在 `stdlib/JSON.py`（**1147 行 58 个 def**，
  `__all__` 在 `:1116-1147`）。
- `stdlib/文件系统.light` **18 行、decl 0**，`:4` 明写「本模块实现见同名 .py 文件，本 .duan 仅作导出清单」，
  `:9-18` 导出 115 个名字。实现全在 `stdlib/文件系统.py`（**1522 行 116 个 def，无 `__all__`**）。
- harness 关键路径实际只用 **5 个符号**：`解析JSON`（`JSON.py:13`）、`序列化JSON`（`:32`）、
  `写入JSON文件`（`:87`）、`文件行列表`（`文件系统.py:325`）、`写入文件`（`:20`）。
  引用点：`examples/harness/行流.light:16-17`、`评测驱动.light:53-54`。

**问题二：评测报告只有通过率。**
- 全仓**没有任何地方解析 LLM 响应的 `usage`**：`.light` 侧 grep `usage/prompt_tokens/completion_tokens`
  唯一命中是 `shutil.disk_usage`；`stdlib/大模型客户端.light:252-256 取消息` 只读 `choices[0]`，
  `数据["usage"]` 从不读。
- 增量块六字段固定（角色/内容增量/工具调用增量/累积内容/完成原因/结束），**无 usage**
  （`大模型客户端.light:216`、`:173`、`:353`；mock 侧 `examples/harness/LLM通道.light:63-65`）。
- 请求体**无 `stream_options.include_usage`**（`大模型客户端.light:116-125`）→ 即使想读流式 usage
  也不会被下发。
- 唯一「token 感」的代码是**粗估**（字符数 ÷ 2.5 + 4）：`stdlib/代理循环.light:167-178 估算条`。
- **无任何分位数计算**：`stdlib/度量.light:47-61 批量汇总` 只有总数/通过数/通过率/总耗时/平均耗时。
  分位数实现**已存在但从未被调用**：`stdlib/统计.py:197-213 百分位数`（线性插值）、`:176-195 四分位数`。

---

## 1. 开工首日必须先定的一件事

**`JSON` 自举做到哪一层？**

遮蔽机制（已核实，注意行号纠正）：`.light` 首**两行**含字符串 `纯光明实现` 即由 `.light`
**完全取代**同名 `.py`（`stdlib/_light_import_hook.py:85-97` 判定、`:157-159` 让路、
`:160-161` 取代、`:186` `meta_path.insert(1, …)`）。
⚠️ 流传的「`_light_import_hook.py:140-142` 同名 .py 存在则 .light 直接跳过」**是过期结论**。
⚠️ `PURE_LIGHT_ONLY` 这个开关**在代码里不存在**（全仓 3 处命中全在旧任务书 Markdown 里）。

- **(a) 新建 `stdlib/JSON核心.light` 纯光明真实现（解析/序列化/写文件），harness 切过去，
  不遮蔽 `JSON.py`。【默认，必做】**
- (b) 把 `JSON.light` 全量实现到 56 个导出并挂魔数**取代** `JSON.py`。【stretch，有前置条件】

**默认取 (a)** 的理由：魔数一挂就是**整模块取代**，全仓 12 个 `examples/` 与积木库都会被影响
（`积木库/格式/JSON美化.light:3` 用 `美化JSON`；`examples/` 下 12 个文件 `导入 JSON。`），
风险/收益不成比例。而 (a) 已经足够兑现「关键路径不再落到 Python 实现上」这个目标。

**做 (b) 的前置条件（缺一条就不许挂魔数）**：56 个导出全部有真实现且全绿 +
12 个 `examples/` 定向回归全绿 + 积木库门禁绿。做不到就保持 (a)，并在交付报告写明卡在哪。

⚠️ **唯一「纯光明取代同名 .py」的先例是 `stdlib/列表工具.light`**（130 行 10 decl，魔数 `:1-2`，
同名 `列表工具.py` 确实存在并被取代；门禁侧 `tools/ci/bootstrap_rate.py:276-277 impl_with_shadow`
的唯一成员）。**照它的首两行写法抄魔数格式。**
另外 16 个带魔数的 `.light` 都是「无同名 `.py`」，只是优先加载，**不构成取代先例**。

---

## 2. 你的独占文件

**独占**：
- 新建 `stdlib/JSON核心.light`、新建 `stdlib/文件流.light`
- `stdlib/JSON.light`(145)、`stdlib/文件系统.light`(18)
- `stdlib/列表工具.light`（**仅订正首行错注释**，见 §3.5）
- `stdlib/度量.light`(61)、`stdlib/统计.light`(25)、`stdlib/JSONL.light`(53)
- `stdlib/大模型客户端.light`(364)
- `examples/kids/story_generator.light`（**仅修 `:113` 悬空导入**，见 §3.5）
- 新建 `tests/test_json_core_light.py`、新建 `tests/test_file_stream_light.py`
- `tests/test_metrics_light.py`(15)、`tests/test_llm_client_light.py`(12)

**只读、不许改**：
- `stdlib/JSON.py`、`stdlib/文件系统.py`、`stdlib/统计.py` —— **只读参照**。
  ⚠️ 特别注意：**不许为了让 `.light` 通过而去改 `.py`**。
- `stdlib/流式.light`、`stdlib/选择器.light`、`stdlib/SSE.light` —— 第五轮 A5 冻结面
  （你要改 `大模型客户端.light` 是被授权的，但**不许连带改这三个**）
- `examples/harness/**`、`stdlib/代理循环.light`、`stdlib/事件总线.light` —— 归 **C7**
- `stdlib/并发.light`、`stdlib/重试.light` —— 本轮无人授权修改
- `src/`、`src/llvm/`、`cli/light.py`、`tools/ci/**` —— 全不许碰

---

## 3. 交付项与判据

### 3.1 【必做】关键路径自举：`JSON核心.light` + `文件流.light`（M19 主判据）

**`stdlib/JSON核心.light`（纯光明，首两行挂魔数）**：至少三个能力 ——
- `解析(文本)`：完整 JSON 解析（对象/数组/字符串含转义与 `\uXXXX`/数字含指数/true/false/null）
- `序列化(值, 缩进)`：往回写，含转义与中文（**不许 `ensure_ascii`，要保中文原样**）
- `写入文件(路径, 值)`：序列化 + 落盘

**`stdlib/文件流.light`（纯光明，首两行挂魔数）**：
- `行列表(路径)`：按行读，等价 `文件系统.py:325 文件行列表`
- `逐行(路径)`：**惰性生成器**（大评测集不该整份进内存）
- `写文本(路径, 文本)`、`追加文本(路径, 文本)`

**判据（缺一即红）**：
1. **round-trip 一致**：对 `examples/harness/评测集.jsonl` 的 6 条 + 一组构造样本
   （深嵌套、中文、转义、大整数、浮点指数、空对象/空数组），
   `解析 → 序列化 → 解析` 与 `JSON.py` 的 `解析JSON` 结果**逐字段相等**。
   **用 `JSON.py` 做对照实现**（这是允许的：它是参照答案，不是兜底）。
2. **差异清单**：凡与 `JSON.py` 行为不一致的地方（比如重复键、NaN/Infinity、超大整数精度），
   **必须逐条列出并说明取哪个语义、为什么**。**不许静默不一致。**
3. **零 `引 Python`**：这两个文件里 `引 Python` 计数 = 0，且**不许 `导入 json`**
   （那等于绕过整件事）。`导入 os`/`导入 sys` 也要压到最低——**E7 本轮新增的「Python 直调行数」
   门禁会数你这两个文件**，你是这条新指标的第一个被测对象。
4. **harness 关键路径真的切过去**：`examples/harness/行流.light:16-17` 与
   `评测驱动.light:53-54` 改成引 `JSON核心` / `文件流`。
   ⚠️ 这两个文件归 **C7**！**你不许改**——把切换写进移交清单交 C7，
   并在你的测试里自己构造等价用例证明能力可用。
5. **反跑**：把解析器的某个分支变异掉（比如转义处理），round-trip 用例必须变红。

**（b）stretch：`JSON.light` 全量接管**。做的话必须：56 个导出全绿 + 12 个 examples 定向回归 +
积木库门禁绿 + 魔数在首两行。做不到就把 `JSON.light` 保持现状，**但要把 `:1-134` 那堆
注释化的 Python 残骸清理掉**（它是纯噪声，让人误以为有实现）——清理后 decl 仍是 0，
自举率分子不动，但可读性回来了。

### 3.2 【必做】usage 采集（M19，契约见总纲 §4.3）

**增量块新增第七字段 `用量`**：
- 正常增量块里为空字典 `[]`
- 收尾块里为 `["输入词元": N, "输出词元": M, "总词元": K]`
- **取不到时留空字典，不许填 0**（填 0 会让「没拿到」和「真的是 0」混在一起）

改动点（已核实）：
1. `stdlib/大模型客户端.light:116-125 组装载荷`：**仅当 `流式` 为真时**加
   `设 载荷["stream_options"] 为 ["include_usage": 真]`，位置放在 `:120-122` 的 tools 块之后、
   `:123-124` 的 `额外参数` 循环**之前**（否则会被外部注入覆盖）。
   ⚠️ `:50` 的既有口径是「不塞默认值」，你要在注释里写明这条是为什么例外。
2. `:195-216 组装增量`：增读 `json体["usage"]`。
   ⚠️ **usage-only 帧的 `choices` 是空数组**，现在会走 `:197-198` 的兜底被整帧丢弃 ——
   这是最容易踩的一个坑，必须先处理 usage 再判 choices。
3. `:173`（同步收尾块）与 `:353`（异步收尾块）加字段。
4. 非流式路径 `:252-256 取消息` 也读 `数据["usage"]`。

**同步版 `流式对话`（`:139`）与 `流式对话2`（`:186`）的既有六字段语义冻结**，只许加字段不许改。
判据：`tests/test_llm_client_light.py` 一行不许动仍全绿（沿用第四/五轮的机读证明口径）。

**判据**：
- 本地 mock server 造一个带 usage 的流式响应（含一个 `choices` 为空的 usage-only 帧），
  断收尾块的 `用量` 三个数字正确
- 造一个**不带** usage 的响应，断 `用量` 是空字典而**不是** `["输入词元": 0, …]`
- 真实通道跑一次，贴真实的 usage 数字（这是 `stream_options` 真被下发的证明）
- 反跑：摘掉 `stream_options`，真实通道的 `用量` 必须变空

### 3.3 【必做】指标层：分位数 + 成本（M19）

- **`stdlib/度量.light` 新增 `分位(耗时列表, 百分位)`**：口径与 `stdlib/统计.py:197-213`
  的线性插值**一致**，但**必须用光明重写，不许 `引 Python`，不许 `导入 统计`**
  （那是转手造假，`tools/ci/bootstrap_rate.py` 会查）。
  在文件头写明「两处口径同源、为什么不复用」。
- **`批量汇总`（`:47-61`）扩字段**：加 `p50耗时`、`p95耗时`、`总输入词元`、`总输出词元`、
  `成本估算`、`失败分类计数`。
  ⚠️ **既有五字段（总数/通过数/通过率/总耗时/平均耗时）语义冻结**，只加不改。
- **成本估算**：`成本 = 输入词元/1000*单价输入 + 输出词元/1000*单价输出`。
  单价由参数传入，**默认为空 → 成本字段为空并标注「未配置单价」，不许默认填 0**。
  单价单位（每千词元）要写进文件头，别让人猜。
- **判据**：分位数与 `stdlib/统计.py:197-213` 在同一批数据上**逐个百分位相等**
  （3/5/7/100 个样本各测一次，含只有 1 个样本的边界）；成本与手算一致；
  反跑：把插值公式改成取整，用例变红。

### 3.4 【必做】「精确匹配」语义收敛（总纲 §4.3 裁决）

现状语义冲突：`examples/harness/打分.light:34-35` 的「精确」是**子串包含**，
`stdlib/度量.light` 的「精确」是**逐字相等**。

- **裁决：以逐字相等为「精确」，子串语义改名为 `包含`。**
- 你改 stdlib 侧命名并在文件头指明这次收敛；**harness 侧调用由 C7 改**（写进移交清单）。
- **判据**：`tests/test_metrics_light.py` 覆盖两个语义各自的正反例；
  ⚠️ **这是行为变更**，会改动 harness 现有分数（C7 那边要贴 6 条得分对比），
  你这边要贴「哪些既有用例的断言因此改了、为什么」。

### 3.5 顺手修的三条真缺陷

1. **`examples/kids/story_generator.light:113`**：`从 文件系统 导入 写入, 文件存在` ——
   `文件系统.py` **无 `def 写入`**，`.light` 导出清单也无该名 → **这一行必然 ImportError**。
   改成 `写入文件`（`文件系统.py:20`）。**判据**：该示例能真跑起来（或至少 import 阶段不炸）。
2. **`stdlib/列表工具.light:1` 与 `:7-8` 的注释是错的**：自称「无同名 .py 兜底」，
   而 `stdlib/列表工具.py` **确实存在**并被取代。订正注释（**只改注释，不动实现**）。
3. **`stdlib/文件系统.py` 的 `文件列表` 重复定义**（`:88` 被 `:235` 覆盖）——
   `.py` 归只读，**不许改**，但要在移交清单里登记这条，交后续轮次。

### 3.6 定时炸弹登记（登记即可，本轮不必全修）

4 个 `.light` 有真实现但**魔数不在首两行**，一旦有人建同名 `.py` 就被静默遮蔽：
`stdlib/插件.light:5`、`stdlib/路径护栏.light:5`、`stdlib/进程树.light:12`、`stdlib/伪终端.light`；
另 `stdlib/SSE.light` **全文无魔数**（靠「无同名 .py」侥幸生效）。
豁免名单在 `tests/test_pure_light_hook.py:84-86`（其中 `事件总线` 已修好，是历史残留）。

这些文件**都不在你的授权面内**（路径护栏/进程树/SSE 是冻结面，插件未授权）。
**你只登记进移交清单**，写清「谁的文件、改什么、为什么现在不改」。

### 3.7 自举率账（M21）

- 基线 **22/74 = 29.73%**，影子 **53**（只许降）。
- 你新建 2 个 `.light`（`JSON核心`、`文件流`）→ 分母 74 → 76；两个都有真实现 → 分子 22 → 24
  → **24/76 = 31.58%**（升）。
- 若做了 stretch (b)（`JSON.light` 全量化）→ 分子再 +1、影子 -1。
- **交付报告必须分子分母各贴**，不许只算一半（第五轮 §2.3 的记账要求）。
- ⚠️ **新建的每个 `.light` 必须 `decl ≥ 1`**，纯导出壳会让率下降 → 直接判红。

---

## 4. 硬约束（本路专属）

1. **不许 `引 Python`**，也不许「`导入 json` 然后转手调」——那是转手造假，
   `tools/ci/bootstrap_rate.py` 会查，且违背这条线的全部意义。
2. **不许为了让 `.light` 过而改 `.py`**。`.py` 是只读参照答案。
3. **不许改 `examples/harness/**` 与 `stdlib/代理循环.light`**（C7 的），
   也不许改 `stdlib/流式.light`/`选择器.light`/`SSE.light`（A5 冻结面）。
   需要它们配合 → 移交清单。
4. **同步签名冻结的机读证明**：`tests/test_llm_client_light.py` 你可以加用例，
   但**既有 12 例的断言一行不许改**还要全绿（第四/五轮口径）。
5. **魔数必须在首两行**（`stdlib/_light_import_hook.py:93-95` 只读两行），
   照 `stdlib/列表工具.light:1-2` 抄格式。
6. **判据要真**：round-trip、逐字段相等、真实 usage 数字。
   假测试门禁扫全仓，基线 469 条只许减；反面样例写**多行**字符串。
7. **端口段 19700-19799**，占用即报错退出。临时文件前缀 `_taskD7_`。
8. **不许在源码树落测试产物**，一律 `tempfile.TemporaryDirectory()`；跑完用 `dir` 确认。
9. **只跑定向测试**：`tests/test_json_core_light.py`、`tests/test_file_stream_light.py`、
   `tests/test_metrics_light.py`、`tests/test_llm_client_light.py`、`tests/test_pure_light_hook.py`、
   `tests/test_jsonl_light.py`、`tests/test_bootstrap_light.py`。

---

## 5. 交付格式

照总纲 §9 六项 + 第 7 项（功能对标清单：你负责把第 1/13/14/15 条推到 done）。
**本路额外必贴**：

- **round-trip 用例的样本清单** + **与 `JSON.py` 的行为差异逐条清单**（§3.1 判据 2）
- **真实通道拿到的真 usage 数字** + 摘掉 `stream_options` 后变空的反跑输出（§3.2）
- **分位数与 `统计.py` 逐百分位相等的对照表**（§3.3）
- **自举率分子/分母/率的三个数字变化**（§3.7）
- **打分语义收敛导致哪些既有断言改了**（§3.4）
- **移交清单**必须包含：给 C7 的切换项（`行流.light:16-17`、`评测驱动.light:53-54`、
  打分语义调用改名）、`文件系统.py` 重复定义、§3.6 的 5 个定时炸弹
