{
  "说明": [
    "harness 功能对标清单（第七轮总纲 §4.1 落地，E7 建，M18/M20 的判据锚点）。",
    "口径变更：项目完成度不再按「特性点齐了没」算，而是对着原版 deepseek-harness 的",
    "功能清单勾状态。状态三值：done / partial / none。",
    "门禁在 tools/ci/spec_coverage.py：done 条数只升不降、done 与 partial 必须带至少一条",
    "存在的证据文件、none 必须写明本轮做不做。行号只作人读线索，门禁**不校验行号内容**",
    "——行号会漂移，校验它只会造长红。",
    "本文件放 任务书/ 而不是 docs/：docs/ 会被文档示例门禁扫（tests/unit/doc_block_scan.py），",
    "而任务书目录本来就是这批文档的家（第五轮 2595a6d0 的既有裁决）。",
    "各路改状态时必须同时改证据，并在交付报告第 7 项写明「从什么变成什么」。",
    "改状态只认本文件的 编号 + 功能 两个字段一起对上：第七轮 C7 交付报告里另有一套自编行号",
    "（它的第 2 行是 JSONL 行流、第 16 行是 mock 造 tool_calls），与本文件的 编号 撞车，",
    "照报告的数字改清单会改错条目（第七轮全合点已踩过，详见 docs/known_issues.md 16.2）。",
    "第九轮续维（G9）：非 done 的 9 条加了 第九轮归属 字段（E9/A9/D9/F9），本轮目标 已改写成",
    "第九轮的目标并在括号里保留原目标。字段契约见 任务书/度量契约_第九轮.md §5。",
    "门禁不读 第九轮归属，它是给各路认领用的；改状态仍按「编号 + 功能名」双字段匹配。"

  ],
  "版本": 1,
  "对标对象": "deepseek-harness（原版）",
  "建立于轮次": "第七轮",
  "条目": [
    {
      "编号": 1,
      "功能": "评测集加载（JSONL，坏行记账）",
      "状态": "done",
      "证据": ["examples/harness/行流.light:7", "examples/harness/行流.light:21-32", "stdlib/JSONL.light:34-45"],
      "本轮目标": "保持 done；底层实现换成纯光明（D7 的 JSON核心/文件流）",
      "备注": "坏行进 坏行表 并跳过，不静默成功也不中断整批。C7 已把切换点做完：行流.light:7 只 `从 JSONL 导入 逐行读JSONL`，底层读取全委托纯光明的 stdlib/JSONL.light（逐行读JSONL 是懒惰生成器，坏行以 [\"坏\": 真, …] 记录给出），行流 自己只做「坏记录 → 坏行表（行号/内容）」的翻译。合并期复核连带修两处：删掉已成死码的 导入 os/sys/json；有键 的 返回 假 原先误缩进在 遍历 体内（只看第一个键），已提到循环外。"


    },
    {
      "编号": 2,
      "功能": "并发调度",
      "状态": "partial",
      "证据": ["examples/harness/编排.light:20", "examples/harness/编排.light:39-41", "stdlib/并发.light:53-73"],
      "第九轮归属": "E9",
      "本轮目标": "【第九轮 E9】done：补一条长尾判据（1 条慢任务 + N 条快任务，断言总耗时接近最长单条而非批次之和），能区分滑动窗口与分批 gather（第七轮原目标：改滑动窗口，切到 stdlib/并发.light 的信号量任务池 —— 实现侧已兑现，卡的是判据）",
      "备注": "合并期核验（结构 + 实跑）结论：实现侧确已兑现：编排.light:20 `从 并发 导入 任务池…`，:39-41 的 任务池运行 只是两行薄适配，harness 侧无第二套实现；stdlib/并发.light:53-60 一次性建出全部任务再 并发等待、并发上限由 :65-73 带限流 的信号量钳制，是真滑动窗口。**卡在判据，故不给 done**：没有一条测试能区分滑动窗口与分批 gather —— test_concurrency_light.py:145 只断并发峰值 ≤2，test_harness_e2e_light.py:110 与 test_harness_agent_light.py:137 都是均匀延迟下断「并发 < 串行」，批大小 2 的分批实现同样全绿；且 test_harness_e2e_light.py:128 的注释仍写着「应接近 2 批 × 0.4」这种分批口径。补齐条件：一条长尾判据（1 条慢任务 + N 条快任务，断言总耗时接近最长单条而非批次之和）。"

    },
    {
      "编号": 3,
      "功能": "速率限制",
      "状态": "done",
      "证据": ["examples/harness/编排.light:46"],
      "本轮目标": "保持 done；实现切到 stdlib，harness 侧只留薄适配",
      "备注": "不许继续双实现（总纲 §5 第 4 条）。"
    },
    {
      "编号": 4,
      "功能": "重试 + 指数退避",
      "状态": "done",
      "证据": ["examples/harness/编排.light:65-92", "stdlib/重试.light"],
      "本轮目标": "保持 done；切到 stdlib/重试.light",
      "备注": "口径同源但不共码，是双写。"
    },
    {
      "编号": 5,
      "功能": "错误分类与单条兜底",
      "状态": "partial",
      "证据": ["examples/harness/错误分类.light:36-64", "examples/harness/评测驱动.light:188-195", "tests/test_harness_agent_light.py:99"],
      "第九轮归属": "E9 + A9",
      "本轮目标": "【第九轮 E9+A9】done：分类器接进 single 链（并发等待 要有 return_exceptions 语义，归 E9）+ 主流程 :344-418 补真 最终（依赖 A9 的 finally，归 A9）+ 应重试/状态码为整数 接上调用点（第七轮原目标：六类错误分类 + 单条失败不打死全场，报告必须落盘）",
      "备注": "合并期核验（结构 + 实跑）结论，从 none 升到 partial（不到 done）。已兑现：错误分类.light:36-64 真分出六类（超时/HTTP状态/连接/解析/工具/未分类），流式.light 的 连接错误/读取错误/HTTP错误 三类都能命中；agent 路径 评测驱动.light:188-195 协程内全捕获，单条失败不打死全场；test_harness_agent_light.py:99 注入四类故障断言整场跑完 + 报告落盘 + 逐条分类 + 失败分类计数，是真判据。**未兑现两处**：(1) 分类器只有 评测驱动.light:193 一个调用点，即只有 agent 模式吃到；single 模式走 编排.light:59 的 `捕获 TimeoutError, ConnectionError`，真实通道三类异常落不进去会直接冒泡，而 stdlib/并发.light 的 并发等待 编译成不带 return_exceptions 的 gather，single+real 下任一条 401/连接错误就整场崩。(2) 主流程 评测驱动.light:344-418 仍无 try，写盘/打分/汇总自身异常时报告整份不落盘 —— 清单第 11 条备注里那句「主流程无 try」至今未修。另：错误分类.light:77-90 的 应重试 与 :101-107 的 状态码为整数 全仓零调用点，是文件头声称接了重试决策、实际没接。"

    },
    {
      "编号": 6,
      "功能": "流式增量消费",
      "状态": "partial",
      "证据": ["examples/harness/编排.light:54-58", "stdlib/大模型客户端.light:235"],
      "第九轮归属": "E9",
      "本轮目标": "【第九轮 E9】done：编排.light:54-58 改用 块[\"内容增量\"] 逐块消费，并加一条断言「收到 >1 块且逐块内容拼起来等于最终文本」（第七轮原目标：增量可被消费方逐块处理，而不是只取最后累积块）",
      "备注": "合并期核验（结构 + 实跑）结论：本轮未推进。生产侧合格：大模型客户端.light:235 与 LLM通道.light:66-68 都吐真增量（内容增量 与 累积内容 分开给）。但评测链唯一的消费点 编排.light:54-58 仍是 `设 累积 为 块[\"累积内容\"]` 逐轮覆盖，等价于只取最后一块；内容增量 在整个 examples/harness/ 的消费侧零引用。原备注「拿到的是累积内容，等于把流式退化成一次性」在当前代码里依然成立。harness 层无测试覆盖（无任何测试调 带重试对话 或断言收到 >1 块）；test_llm_client_light.py:193 只证明 stdlib 客户端层是增量。"

    },
    {
      "编号": 7,
      "功能": "工具调用（tools 下发 + 校验 + 执行）",
      "状态": "partial",
      "证据": ["stdlib/大模型客户端.light:118-134", "examples/harness/LLM通道.light:170-172", "stdlib/代理循环.light:691-710", "tests/test_harness_agent_light.py:66"],
      "第九轮归属": "E9 + A9",
      "本轮目标": "【第九轮 E9+A9】done：single 模式的 LLM通道/真实通道 也调 配置工具，请求体恒带 tools；补一条评测链侧断言请求体含 tools 的测试（第七轮原目标：评测链请求体带 tools，mock 能造出 finish_reason == tool_calls —— 后半已兑现）",
      "备注": "合并期核验（结构 + 实跑）结论，从 none 升到 partial。已兑现：mock 能造 finish_reason == tool_calls（LLM通道.light:170-172，:198-208 命中工具题表时发出）；参数真校验（代理循环.light:691-700，json.loads + 模式校验）；工具真执行（:705-710 真调实现，评测驱动.light:102-109 注册的 工具查首都/工具查故障 是真查真抛）；test_harness_agent_light.py:66 断言两条查首都条目 得分==1 且 工具调用次数>=1、完成轮数>=2，是结构性必须调工具才能答对的真判据。**未兑现**：「评测链请求体带 tools」只在 agent+real 这条路径成立（大模型客户端.light:122-124 有条件组装），而 real 路径无测试；默认被测的 agent+mock 路径根本没有请求体（LLM通道.light:181-184 的 配置工具 只存声明）；single 模式的 LLM通道/真实通道 从不调 配置工具，请求体恒无 tools。断言请求体含 tools 的测试只有 agent 链的 test_deepseek_mock.py:275 一组，评测链侧无。"

    },
    {
      "编号": 8,
      "功能": "多轮 agent loop",
      "状态": "done",
      "证据": ["examples/harness/评测驱动.light:235-236", "examples/harness/评测驱动.light:139-146", "examples/harness/评测驱动.light:189", "stdlib/代理循环.light:492-542", "tests/test_harness_agent_light.py:88"],
      "本轮目标": "done（M18 判据 1）：评测集驱动多轮 agent",
      "备注": "合并期核验（结构 + 实跑）**确认成立**，从 none 升到 done。入口 评测驱动.light:235-236（HARNESS_MODE=agent 走 评测一条_agent）；代理循环.运行 仍是同步方法（代理循环.light:492），跨界走的是既定口径「丢线程再 await」——评测驱动.light:139-146 包零参同步闭包、:189 `等待 asyncio.to_thread(运行任务)`；每条目一个独立客户端（:126-133 客户端工厂 → 代理循环.light:311-315），守住「一个客户端一条 socket」。完成轮数 是 代理循环.light:500 在循环体内真加出来的，test_harness_agent_light.py:88 断言 完成轮数 >= 2，锁死多轮真发生。**同表内两处常量字段另账**：评测驱动.light:205 成功路径 尝试次数 恒 1、:218 失败路径 尝试次数 填的是重试上限（不是实测次数），:218/:246 的 完成轮数/工具调用次数 在失败路径与 single 模式恒 0 —— 这几个字段像真值实为常量，归第 11 条。"

    },
    {
      "编号": 9,
      "功能": "工具并行",
      "状态": "done",
      "证据": ["stdlib/代理循环.light:544", "stdlib/代理循环.light:612"],
      "本轮目标": "保持 done；线程侧不动，跨界只走「丢线程再 await」",
      "备注": "threading 实现，异步化时注意不要把阻塞 join 留在 loop 里。"
    },
    {
      "编号": 10,
      "功能": "沙箱执行",
      "状态": "done",
      "证据": ["examples/harness/主程序.light:38", "examples/harness/主程序.light:91", "stdlib/代理工具集.light:150", "stdlib/路径护栏.light:135-143", "tests/test_harness_e2e.py:172"],
      "本轮目标": "保持 done；harness 改用 代理工具集.注册全部，不再用自带三件套",
      "备注": "C7 已做完并核验通过：主程序.light:38 `从 代理工具集 导入 注册全部`、:91 `注册全部(代理器, 沙箱根, [\"允许写\": 真])`，自带三件套及其 schema 已物理删除（全文无 代理器.注册工具( 调用）；「代理工具集尚未合入」那句过期注释已订正（:27-32 改写成「工具集来自 stdlib，不是本文件自带」）。护栏真接上：代理工具集.light:150 新建 路径护栏，路径护栏.light:135-143 越界抛错，:119 用 根目录 + os.sep 做分量级前缀比、:107-112 先 realpath 再 normcase。判据是 subprocess 真跑主程序的 test_harness_e2e.py:172（越界被拦且沙箱外无文件）与 :193（未注册工具名不静默成功）。"

    },
    {
      "编号": 11,
      "功能": "报告落盘（JSON + Markdown）",
      "状态": "done",
      "证据": ["examples/harness/评测驱动.light:408-410", "examples/harness/评测驱动.light:280-292", "examples/harness/评测驱动.light:212", "tests/test_harness_agent_light.py:99"],
      "本轮目标": "保持 done；加总纲 §4.4 的新字段（轮数/工具调用次数/错误分类/用量/分位）",
      "备注": "落盘本体保持 done（:408 JSON、:410 Markdown）。五组新字段的落点与真值判定（合并期逐个核过）：错误分类 真值（:212 逐条 + :267-273 计失败分类 + :280 汇总，test_harness_agent_light.py:99 断言四类分类与失败分类计数）；p50/p95 真值（:284-285 调 stdlib/度量.分位，合并期实测报告值与手算线性插值逐位相等）；完成轮数 / 工具调用次数 只在 agent 成功路径是真值（:212），失败路径 :218 与 single 模式 :246 恒 0；尝试次数 :205 恒 1、:218 填重试上限，不是实测；**用量 / 成本 第八轮已接真值**：块上的 \"用量\" 由 stdlib/代理循环.累计用量（agent 链，逐轮相加、只取每块最后一个非空 usage）或 编排.带重试对话（单模链）翻成 输入词元/输出词元 → 打分.批量汇总 透传给 stdlib/度量.批量汇总(评分表, 单价) → 评测驱动.补扩展汇总 装成 汇总[\"用量\"]/汇总[\"成本\"]；一条都没采到时仍是空字典（不许填 0），单价未配时 成本估算 空串 + 成本说明「未配置单价」。CLI 的 --price-in/--price-out 经 HARNESS_PRICE_IN/OUT 落到 造单价（两个都给才生效），此前是悬空参数。报告层已有测试：tests/test_harness_e2e_light.py::Test用量与成本进报告（字符数对账 / 配价后与手算相等 / 只配一半按未配置）、tests/test_harness_agent_light.py::TestM18功能::test_多轮的用量按轮累加且失败条目留空、tests/test_agent_loop_light.py::Test用量透传（含反跑：改成逐块相加即红）。主流程 :344-418 仍无 try（写盘/汇总自身异常时报告不落盘），与第 5 条同一笔账。p50/p95 在报告层仍无测试覆盖。"

    },
    {
      "编号": 12,
      "功能": "通过率与子集统计",
      "状态": "done",
      "证据": ["examples/harness/打分.light:90-121"],
      "本轮目标": "保持 done",
      "备注": "「精确」语义本轮收敛：逐字相等为精确，子串语义改名 包含（总纲 §4.3）。"
    },
    {
      "编号": 13,
      "功能": "token 用量",
      "状态": "done",
      "证据": ["stdlib/大模型客户端.light:235", "stdlib/大模型客户端.light:302-307", "tests/test_llm_client_light.py:TestUsageCollection"],
      "本轮目标": "done（M19）：增量块第七字段 用量，取不到留空字典而不是填 0",
      "备注": "D7 落地：流式请求下发 stream_options.include_usage；组装增量 先读 usage 再判 choices（usage-only 帧此前被兜底行整帧丢弃）；取用量 取不到返回 {} 而非填 0。真实 api.deepseek.com 两轮实测拿到 9/54/63 与 9/50/59。"
    },
    {
      "编号": 14,
      "功能": "成本估算",
      "状态": "done",
      "证据": ["stdlib/度量.light:批量汇总", "tests/test_metrics_light.py:test_未配置单价_成本留空"],
      "本轮目标": "done（M19）：单价可配，默认 0 且报告标注「未配置单价」",
      "备注": "D7 落地：单价空 → 成本估算 留空串 + 成本说明「未配置单价」，不填 0（「没配」和「真是 0」必须可区分）。依赖第 13 条的 usage 采集。"
    },
    {
      "编号": 15,
      "功能": "延迟分位数 p50/p95",
      "状态": "done",
      "证据": ["stdlib/度量.light:分位", "tests/test_metrics_light.py:Test分位"],
      "本轮目标": "done（M19）：stdlib/度量.light 新增 分位()，口径与 统计.py 的线性插值同源",
      "备注": "D7 落地：纯光明 分位（k=(n-1)*p/100 线性插值），与 stdlib/统计.py 逐百分位对照 Δ=0（5 组样本 × 8 个百分位）；反跑 test_反跑_线性插值非取整 锁死不许退化成取整下标。"
    },

    {
      "编号": 16,
      "功能": "取消 / 超时",
      "状态": "done",
      "证据": ["examples/harness/评测驱动.light:限时等待", "examples/harness/评测驱动.light:造超时秒", "stdlib/并发.light:76-78", "examples/harness/编排.light:57-63", "stdlib/进程树.light:416-453", "tests/test_harness_e2e_light.py:Test单条超时", "tests/test_harness_agent_light.py:TestM18超时", "tests/test_path_a_process_isolation_light.py"],
      "第九轮归属": "E9",
      "本轮目标": "【第九轮 E9】done（E9-S2 收口 Path A 已交付并验证）：超时真能掐掉已起跑的工作（不是只取消等待）—— 由 编排.限时运行进程 + 进程树.杀树 的 SIGKILL/taskkill /F /T 进程隔离兜底实现；取消令牌 生产赋值入口 作为独立跟踪项保留（Path A 硬杀对危险阻塞更强，已覆盖该场景）。",
      "备注": "合并期核验（结构 + 实跑）结论，**从 partial 降到 none**（降级理由须留档，不许靠状态词糊过去）。评测链侧一行都没有：examples/harness/ 全目录 grep 超时运行/限时/wait_for/取消令牌/cancel 零命中；编排.light:44-65 带重试对话 是「无限等 + 捕获两类异常重试」，评测驱动.light:189 的 `等待 asyncio.to_thread(运行任务)` 是裸 await，一条慢任务能无限期挂住协程。报告里的「超时」只是对 mock 主动抛的 TimeoutError 做事后分类（错误分类.light:36-38，异常源 LLM通道.light:77/:149），不是超时触发。agent 侧的取消令牌也是死码：代理循环.light:324 在构造里把 己.取消令牌 置空，全仓无赋值点，评测驱动.light:142 新建时也不传，故 是否已取消（:471-482）恒返回假 —— 原备注「取消令牌只在 agent 侧」比实情还乐观。现成能力没吃：stdlib/并发.light:76-78 已有 超时运行（等价 wait_for）且有测试（test_concurrency_light.py:170/:190），但 编排.light:20 的导入清单里没有它。无测试覆盖：test_harness_e2e_light.py:135 注入的是「立刻抛 TimeoutError」，断的是 尝试次数==3 / 得分==0，不涉及慢任务与时限。 || 第八轮订正与升级（none → partial）：(1) 上面「agent 侧取消令牌是死码、全仓无赋值点」是错账 —— tests/test_agent_loop_light.py:412-429 有真赋值点并断言 轮次 == [0]（令牌先置位则一轮都不跑）；准确说法是「生产代码里没有赋值入口，只有测试在用」，构造置空是默认不可取消。(2) 评测链已接上 stdlib/并发.超时运行：评测驱动.light 新增 限时等待（超时秒 空 → 直接 await，数字 → 交 超时运行）与 造超时秒（空串/非正数 → 空），环境变量 HARNESS_TIMEOUT_SEC 驱动；单模链在 评测一条 新增 尝试/捕获 单条兜底（组装单模成功/组装单模失败 两个同步段落规避 捕获 折叠 bug），agent 链在 评测一条_agent 里把 asyncio.to_thread(...) 包进 限时等待。超时抛的 TimeoutError 经 错误分类.分类 落「超时」类（asyncio.TimeoutError 自 3.11 起即内置 TimeoutError 别名，本机 3.14 实测为真），整场跑完、报告落盘、rc=0。判据：tests/test_harness_e2e_light.py::Test单条超时（3 条：限时<延迟六条全超时且词元留空 / 限时>延迟与不限时逐项等价 / 0 秒当没配）、tests/test_harness_agent_light.py::TestM18超时（2 条），反跑把限时条件取反后两条「全条超时」立红、其余不动。(3) 仍是 partial 不是 done：agent 模式被限时的对象是 asyncio.to_thread(...)，wait_for 取消的是等待，掐不掉已起跑的 worker 线程；生产代码也仍没有给 取消令牌 赋值的入口。**E9-S2 收口（Path A）补上「超时真杀」硬兜底**：新增 examples/harness/编排.light:57-63 限时运行进程，把危险操作放进独立子进程、超时后由 stdlib/进程树.light:416-453 杀树 按平台强杀整棵树（POSIX os.killpg(pid, SIGKILL) / Windows taskkill /F /T），跨平台一致、协作式 wait_for 失效时仍真杀；进程树 是中文模块→本层零新增 Python 直调（python_direct_calls 双棘轮不破）。判据 tests/test_path_a_process_isolation_light.py 三条全绿：超时硬杀挂起命令（是否超时 真 + DONE 永不被写）、未超时正常返回、不过杀阳性对照（同命令给足超时则正常写完 DONE，证明只真杀真超时、不误杀）；本机 Windows 实测 taskkill rc=0 真杀，POSIX 走同一份 进程树 代码（os.killpg SIGKILL，CI 实证）。**剩余集成步**（独立任务，不挡 done）：评测链两处 限时等待 调用（评测一条_agent:246 / 评测一条:299）尚未传 命令数组，故 Path A 默认未启用、eval 超时仍走协作式 wait_for；接通 命令数组 即激活进程隔离兜底。"

    },
    {
      "编号": 17,
      "功能": "事件流可观测",
      "状态": "partial",
      "证据": ["stdlib/事件总线.light:85-89", "stdlib/代理循环.light:319", "stdlib/代理循环.light:678-688", "tests/test_tool_parallel_light.py:348"],
      "第九轮归属": "E9",
      "本轮目标": "【第九轮 E9】done：补一条「两实例并发、各自只收到自己的事件」的判据；监听器不再直写 stdout（第七轮原目标：并发下不互串 —— 前半已兑现，靠约束而非汇聚）",
      "备注": "合并期核验（结构 + 实跑）结论：维持 partial。已兑现前半：事件总线.light:85-89 把 监听者表/句柄集/下一个句柄 全放实例属性（不是全局单例），代理循环.light:319 在构造里逐实例 `新建 事件总线()`，评测驱动.light:142 每条目一个代理 → 各自一条总线。**后半没兑现**：代码里没有任何队列 / 汇聚器 / call_soon_threadsafe，所谓「汇聚到主协程再发」不存在；真实口径是「工作线程干脆不发事件」（代理循环.light:678-688 并行槽体 只写自己的槽位），8 个发布点（:432/:497/:501/:517/:528/:583/:627/:643）都在主线程栈上，靠约束而非汇聚达成不互串。监听器仍往 stdout 写中文：主程序.light:43-61 四个监听器（:46 sys.stdout.write 等）原样保留，原备注这半个问题未处置。判据缺口：test_event_bus_light.py 13 例全是单实例同步语义，最接近的 test_tool_parallel_light.py:348 只断「单实例内事件都在主线程发」，没有一条「两实例并发、各自只收到自己的事件」。"

    },
    {
      "编号": 18,
      "功能": "会话 / 预算截断",
      "状态": "done",
      "证据": ["stdlib/代理循环.light:77-269"],
      "本轮目标": "保持 done",
      "备注": "token 估算是字符数粗估，不是真 tokenizer；真 usage 由第 13 条补。"
    },
    {
      "编号": 19,
      "功能": "MCP 客户端",
      "状态": "none",
      "证据": [],
      "第九轮归属": "F9（评估）",
      "本轮目标": "【第九轮 F9】只评估不承诺：F9 做分布式调用层时顺带判断 MCP 客户端能不能建在同一套线协议上；结论写进 S3 交付报告，做不做仍按 none 记账",
      "备注": "本轮不做，刻意记账（总纲 §4.1）：与 M16–M20 无依赖，塞进来会挤掉 M18 的韧性判据工时。第八轮起草必须带着它进任务书。|| 第九轮：归 F9 评估（总纲 §4.1）。"
    },
    {
      "编号": 20,
      "功能": "PTY / landlock / seccomp",
      "状态": "partial",
      "证据": ["stdlib/伪终端.light"],
      "第九轮归属": "D9",
      "本轮目标": "【第九轮 D9】partial → 争取 done：伪终端 去 ctypes（76 处）并给出系统调用级隔离的落地或明确记账（第七/八轮原目标：本轮不做）",
      "备注": "本轮不做，刻意记账（总纲 §4.1）：PTY 有，沙箱的系统调用级隔离为零。第八轮起草必须带着它进任务书。|| 第九轮：归 D9（总纲 §4.1）；D9 的主战场是 伪终端(ctypes 76) 与 进程树(26)，隔离能力若仍做不到必须写清「差多少、下一轮怎么补」，不许靠状态词糊过去。"
    }
  ]
}
