# 交付报告 · E9 S2：结构化任务组 + 有界队列背压 + 超时能真掐

> 分支：`task-E9S2`　仓库：`g:\dswork\duan-light-merge\light-merge`
> 只提交到 `task-E9S2`，不 merge、不 push、不碰 main。
> 本报告按总纲 §9 六项格式组织；§3.2 / §3.5 / §3.1 / §3.4 / §4（对标清单四条）已完成并提交，§5 A9 四条核实已在 §6 收口。

---

## 0. 本轮进度一览

| 交付项 | 状态 | 说明 |
|---|---|---|
| §3.2 有界队列 + 滑动窗口提升 | **done（本轮提交）** | 从 `分布式/队列.py` 提升进 `stdlib/并发.light`，纯光明、零新增直调；`队列.py` 已删除 |
| §3.3 消费侧改导入来源 | **done（本轮提交）** | `调度核心.light:19` 只改导入来源，调用逻辑零改动 |
| §3.1 结构化任务组（多级嵌套取消） | **done（本轮提交 689a5c87）** | 纯补判据：实现层因 `并发等待=asyncio.gather` 本就支持嵌套取消，故只补测试收口「深度嵌套未测」；5 条判据全绿 |
| §3.4 超时能真掐 | **done（本轮提交 c06501f3）** | Hybrid（协作式真掐 + 线程级 detach）；取消令牌接生产入口（评测驱动.light:243 新建 / :154 注入 / :211 超时即取消）；2 条真停判据全绿 |
| §3.5 流式.light 四条异步读腿搬回类体 | **done（本轮提交 8af830fe）** | 纯重构零行为变化；7 门禁全绿、python_direct_calls 251 不变；POSIX 时序本机未实测 |
| §4 对标清单四条 partial | **done（本轮提交 926507d0）** | #2/#5/#6/#16 补齐条件全部落地（判据+最小代码修正）；详见 §5 各条「建议改 done」结论 |
| §5 A9 四条语言缺陷核实 | **done（§6 收口）** | 见 §6 章节 |

---

## 1. §3.2 裁决与实施：有界队列 + 滑动窗口提升（本轮核心）

### 1.1 裁决过程（§3.2 硬冲突）

**冲突事实**（全部实测）：
- 光明已支持类体内 `异步 段落`（`src/parser_stmt.py:4423` 分支），直译版探针（`asyncio.Queue(maxsize=容量)` 原样搬）编译 + 运行通过，行为与 `队列.py` 逐条一致。
- 但 `python_direct_calls` 门禁是逐文件严格棘轮：`并发.light` 基线 4 行（`asyncio.BoundedSemaphore:25`、`time.monotonic:47/:50`、`asyncio.sleep:54`），total 251 零余量。直译版会多一行 `asyncio.Queue(...)` → 5 > 4 且 252 > 251 → 双棘轮必红。§6 明写合并前不许刷基线。
- `滑动窗口类` 全仓生产侧零调用点（只有 `队列.py` 自身 + F9 判据测试）；`试着入队` 同样零调用（纯反跑对照壳）。

**裁决（E9 侧结论，2026-08-25）**：
1. **Q1 走「纯光明实现」**：用 `槽信号量 = 异步信号量(容量)` 表达容量 + 背压，`入队 = 信号量获取(槽信号量)` —— 满则**真挂起**（探针实测 10 轮放行 max 延迟 0.17ms，非轮询），等价 `asyncio.Queue.put` 背压；`出队` 空队时 0.01s 短轮询。
   - **为什么出队只能轮询**（实证）：`BoundedSemaphore(0)` 的 `release()` 立抛 `ValueError('BoundedSemaphore released too many times')`（bound=0 时任何 release 都越界）——无法表达「初始 0、可释放到容量」的计数信号量；新增 `asyncio.Semaphore` 直调会破棘轮。生产侧唯一消费点 `调度核心.light:110` 用 `是否空` 守卫后才 `出队`，**从不空等**；F9 判据也不测空等。已写入承诺文档 §10「已知限制」。
   - 该代价与上一轮 agent 的预判一致（「出队空等退化成短轮询」属实），但**生产路径零影响**。
2. **Q2 处置**：
   - `滑动窗口类`：**只搬位置、不接生产路径**（保持现状语义，时间注入设计保留）；报告如实声明「限流仍是零生产调用 → partial」。
   - `试着入队`：**保留**（否决「删掉」预案）。理由：§3.2 把它列入「提升后必须保持兼容的签名」；它是入队阻塞语义的天然反跑对照（改 `入队` 为 `试着入队` 体 → 背压测试立红）；零直调成本。其「不占槽位」的语义缝隙（与 `出队` 混用会槽位失衡）已写进类注释 + 承诺文档 §10，全仓零调用点，生产路径禁用。
3. **顺带发现并处置**：`调度核心.light:15` 的「光明类体不能放 异步 段落」注释已过期（该文件类内就有 5 个 `异步 段落` 方法）——已更新注释，同时保留模块级段落 + 单例绕法不动（签名兼容）。

### 1.2 实施清单（本提交）

| 文件 | 变更 |
|---|---|
| `stdlib/并发.light` | +85 行：`有界队列类`（构造/入队/试着入队/出队/大小/是否满/是否空）+ `滑动窗口类`（构造/_剔除过期/记录/数量/是否放行），纯光明零新增直调 |
| `stdlib/分布式/调度核心.light` | `:19` 导入来源 `从 队列` → `从 并发`；`:6/:10/:15/:58` 四处过期注释更新；调用逻辑零改动 |
| `stdlib/分布式/队列.py` | **删除**（归宿：删除，非薄转发——提升后其存在理由已蒸发，docstring 所述「F9 不许改并发.light」裁决已失效） |
| `tests/test_distributed_eval_light.py` | `:58` 导入改 `从 并发`；两处反跑 docstring 改指 `并发.light` 行号 |
| `tests/test_concurrency_light.py` | +5 条 `TestBoundedQueue`（背压真挂起/空队等待/FIFO/试着入队 QueueFull/滑动窗口过期），各带反跑锚点 |
| `任务书/E9→F9_并发语义承诺.md` | 新增 §10（有界队列/滑动窗口承诺表，日期 + E9S2 单号标注）；§9 守夜人命令补 E9-S2 条目 |

### 1.3 判据表

| 判据 | 文件:行（改动后） | 反跑（改哪行 → 哪断言红） |
|---|---|---|
| 背压 = 满队真挂起生产者 | `并发.light` 有界队列类.入队 的 `等待 信号量获取(己.槽信号量)` | 删该行 → `TestBoundedQueue::test_backpressure_blocks_producer_when_full` 与 `test_distributed_eval_light.py::test_背压_满后生产者被挡住` 双红（已实测） |
| 空队等待 = 0.01s 轮询 | `并发.light` 有界队列类.出队 的 `当 列表长度...等于 0` 循环 | 删该循环 → `test_empty_wait_consumer_unblocks_after_enqueue` 红（IndexError） |
| FIFO + 大小/是否空/是否满 | `并发.light` 有界队列类 | `出队` 改 `pop()` → `test_fifo_order_and_size_queries` 红 |
| 试着入队 满抛 QueueFull | `并发.light` 有界队列类.试着入队 | `小于` 改 `小于等于` → `test_try_enqueue_full_raises_queu_full` 红 |
| 滑动窗口过期剔除 | `并发.light` 滑动窗口类._剔除过期 的 当 循环 | 删该循环 → `test_限流_滑动窗口与分批不同` / `test_sliding_window_expires_old_events` 红 |

### 1.4 实测证据

- 探针（`%TEMP%/probe_e9s2_dualsem.light` + driver）：生产者背压真挂起（10 轮放行 max 延迟 **0.17ms**）；空队轮询放行 15ms（一个 tick 内）；FIFO/状态/试着入队全过。
- `pytest tests/test_concurrency_light.py` → **31 passed**（26 旧 + 5 新）。
- `pytest tests/test_distributed_eval_light.py` → **6 passed**（含 1200 条真实 master/worker 分发的 背压/限流 两条移交判据）。
- 七道门禁（§6.2，串行）全 rc=0：`dist_criteria` / `assert_quality` / `python_direct_calls`（**total 251 不变、并发.light 4 行不变**）/ `native_product` / `bootstrap_rate` / `floor_bootstrap` / `spec_coverage`。
- 反跑实测：删 `入队` 信号量获取行 → 两侧背压测试立红（2 failed），还原后 5 passed。

### 1.5 承诺变更说明（§8 流程，@F9）

- 新增 `承诺.md §10`：有界队列/滑动窗口承诺表（签名兼容/背压语义/空队等待已知限制/试着入队定位/滑动窗口/限流接入状态六行），标注日期 2026-08-25 + E9S2 §3.2。
- **@F9**：`调度核心.light` 的导入来源从 `从 队列` 改为 `从 并发`，签名与调用逻辑零变化；`队列.py` 已删除。F9 若在 S3 及其后引用 `队列` 模块需改为 `并发`。请 F9 确认无反向依赖。
- **限流接入状态**：滑动窗口类未接进 master 派发路径 → 限流判据维持 **partial**（不报 done）。

---

## 2. §3.1 结构化任务组（done · 本轮提交 689a5c87）

**结论先行**：经实证，嵌套取消在**现有实现下已经成立**，本轮 §3.1 是**补判据测试收口「深度嵌套未测」**，未改动 `stdlib/并发.light`（零风险，python_direct_calls 保持 251 不变）。

### 2.1 根因实证（为什么不用改代码）

- `并发等待`（`src/code_generator.py:268`）= `asyncio.gather`。`asyncio.gather` 在被取消时会**级联取消其子任务并等其终结**（`return_exceptions=False` 语义）。
- 因此 `任务池` 内部 `等待 并发等待(包装列表)` 一旦所在任务被取消，gather 会把取消信号递归下沉到所有子/孙任务，天然实现「外层取消穿透到内层孙子」。
- 探针实测（修复前状态，即未改任何代码）：
  - **向内三层**（外层 `全或无 [慢1(跑中层池), 立即失败, 慢2]` → 中层池 → 内层池 `[叶A,叶B,叶C]`）：立即失败触发外层取消后，最内层三个孙子叶子全部 `-被取消`，`孤儿数 = 0`。
  - **向外传播**（外层 `[A, B(跑内层池), C]`，内层 `B1` 失败）：`B1` 异常冒泡到外层 → 外层取消 `A`/`C`；捕获到的异常是内层 `B1` 的 `RuntimeError`（业务异常优先于 `CancelledError`），`孤儿数 = 0`。
- 故 S1 报告 §4「取消链的深度嵌套未测」是**测试覆盖缺口**，不是实现缺口。

### 2.2 改动清单（仅测试，1 文件）

| 文件 | 变更 |
|---|---|
| `tests/test_concurrency_light.py` | +244 行，新增 `TestStructuredTaskGroup`（2 条嵌套取消）+ `TestConcurrencyClamp`（3 条钳制），各带反跑锚点 |

### 2.3 判据表（5 条，全绿）

| 判据 | 类 / 方法 | 反跑（改哪行 → 哪断言红） |
|---|---|---|
| 内向穿透：外层取消 → 孙子任务全部被取消、无孤儿 | `TestStructuredTaskGroup::test_nested_cancel_reaches_grandchildren_no_orphans` | 把 `并发等待`(gather) 换 `asyncio.wait`（不级联取消子任务）→ 叶子不取消 + `孤儿数>0` 红 |
| 向外传播：内层失败冒泡到外层、外层取消兄弟、业务异常优先 | `TestStructuredTaskGroup::test_inner_failure_propagates_outward_cancels_outer_siblings` | 把 `全或无等待` 的 `抛出 首异常` 改 `抛出 CancelledError` → 业务异常被覆盖红；或内层改 `逐条收集` 不冒泡 → A/C 不被取消红 |
| 并发上限 ≥8：信号量(8) 下 12 路峰值 = 8 | `TestConcurrencyClamp::test_semaphore_clamps_at_8_with_12_tasks` | 去掉 `信号量获取/释放` → 峰值冲到 12 红 |
| 跨异步段落复用同一信号量：对象级共享、跨段落不丢计数 | `TestConcurrencyClamp::test_shared_semaphore_across_async_paragraphs` | 每个段落各 new 一个信号量（不共享对象）→ 峰值冲到 12 红 |
| 嵌套池外层钳内层：外层2×内层2 → 叶子峰值恰 4 | `TestConcurrencyClamp::test_nested_pool_outer_clamps_inner` | 外层 `并发上限` 改很大/不装信号量 → 4 内层池全并发、峰值冲到 8 红 |

### 2.4 实测证据

- `pytest tests/test_concurrency_light.py` → **36 passed**（31 旧 + 5 新 §3.1）。
- 七道门禁（§6.2，串行）全 rc=0：`python_direct_calls` **总数 251 ≤ 251、无文件上升**；E9-S2 §6.5 **未刷任何 `*_baseline.json`**。
- 反跑验证：每条用例 docstring 已注明「改哪一行必立红」；核心嵌套取消判据的「孤儿数」断言与孙子叶子 `-被取消` 标记构成双重保险。

### 2.5 诚实边界

- 嵌套取消的正确性依赖 `并发等待=asyncio.gather` 的级联取消语义。**若未来把 `并发等待` 换成非级联原语（如裸 `asyncio.wait`），本节两条嵌套取消判据会立即立红**——这正是它们作为回归护栏的价值。
- POSIX 侧并发时序未实测（同 §4.4 口径：本机 Windows 11，CI 在 FreeBSD runner，本机绿≠CI 绿）。

---

## 3. §3.4 超时能真掐（done · 本轮提交 c06501f3）

**背景/裁决**：任务书 §3.4 指出 `examples/harness/评测驱动.light` 的「限时」包了 `asyncio.to_thread(...)`，`wait_for` 掐掉的只是「等待线程完成」的 Future，**线程里真正跑的阻塞调用不会被强杀**；且 `取消令牌`（事件总线.light:237 完整 AbortSignal）**全仓没有任何生产侧赋值入口**，`代理循环.light` 里 `取消令牌` 永远默认空、`是否已取消()`（代理循环.light:521）是死代码。A9 四条语言缺陷（见 §5）中第 2 条（裸 `捕获` → `except Exception`，抓不到 `CancelledError`）仍未修，故协作式取消**不能靠抓异常**，必须走显式 `是否已取消()` 布尔闸门。

**方案选型（用户拍板 = C / Hybrid）**：动 harness 架构前先说方案，给出两条路——
- A 进程隔离 + kill：每条 agent 丢子进程，超时 `进程.terminate()`(SIGTERM→SIGKILL)。能强杀死阻塞 syscall，但 harness 改动大、需 IPC 序列化、Windows/FreeBSD spawn 限制。
- B 真异步化 agent 循环：把 `代理循环.运行` 改真异步，LLM 走 `流式` 已搬进类内的 `协程读体` 等异步腿；超时 = wait_for 取消 + `取消令牌.取消()` 闸门。**改动最小、跨平台一致**，但协作式、不强制杀完全不 await 的死阻塞。
- **C Hybrid（采纳）**：以 B 为主（LLM 主路径真掐 + 取消令牌生产接入），额外在 agent 超时后调 `取消令牌.取消()` 并走「线程级 detach」（不再 re-await 已被取消的 to_thread 任务，worker 跑完即回收，事件循环不被阻塞）。覆盖超时场景绝大多数（LLM 多轮/多工具），改动面远小于 A。

### 3.1 改动清单（1 文件）

仅 `examples/harness/评测驱动.light`（harness，非 stdlib；`python_direct_calls` 基线未动）：

- **`取消令牌` 生产侧赋值入口（核心缺口补齐）**
  - `评测一条_agent`（:243）：`设 令牌 为 新建 取消令牌()` —— 真正在生产路径上创建令牌（此前全仓无一处生产赋值）。
  - `造agent运行`（:154）：`设 代理.取消令牌 为 取消令牌` —— 注入给 `代理循环` 实例，让 `是否已取消()` 闸门（代理循环.light:521/526）从死代码变活。
- **`限时等待`（:204）升级为带取消的超时**
  - 签名加 `取消令牌 = 空`；`捕获 异常 e` 分支内：若令牌非空则 `取消令牌.取消()`（:211），再 `抛出 e` 维持原有「超时→错误分类落到超时类」行为。
  - 单模链（:286 调用点未传令牌）行为逐字节不变。
- **注释更新**：原诚实边界注释（「真掐断要靠代理循环侧的取消令牌，本轮不动它」）改为如实描述本轮已接入协作式真掐 + 线程级 detach。

### 3.2 实测证据（真停了，不是只置 flag）

- **判据测试 `tests/test_timeout_kill_light.py` → 2 passed**：
  1. `test_timeout_triggers_token_cancel`：超时后断言 `令牌.已取消 is True` —— 证明生产入口（:243 新建并注入）+ 超时路径（:211 调 `.取消()`）已接通。
  2. `test_real_agent_loop_gate_stops_thread_on_cancel`：真 `代理循环` 实例 + 真 `取消令牌`，后台线程循环调 `代理.是否已取消()`；超时后断言线程因闸门**真停**（`状态["停"] is True` 且 `状态["轮"] < 5`）—— 行为级「真停」证据，非 flag 检查。
- **七道门禁串行全 rc=0**：`python_direct_calls` **总数 251 ≤ 基线 251、无文件上升**（曾试图用 `asyncio.ensure_future` + 完成回调做显式 detach，但该直调会让门禁 251→252，按 §6.5 严禁合并前刷 baseline，故改走隐式 detach，零新增 Python 直调）。
- **波及面测试 42 passed**（`test_harness_agent_light.py` 含既有 `TestM18超时` e2e + `test_concurrency_light.py`），零回归。

### 3.3 判据表（判据 = 文件:行；反跑 = 改哪行 → 哪条红）

| 判据 | 文件:行 | 反跑（改哪行 → 哪条断言红） |
|---|---|---|
| 超时后令牌被置位（生产入口 + 超时路径接通） | tests/test_timeout_kill_light.py::test_timeout_triggers_token_cancel | 删 `评测驱动.light:211` 的 `取消令牌.取消()` → `令牌.已取消` 仍 假 → assert 立红 |
| 线程因闸门真停（行为级，非 flag） | tests/test_timeout_kill_light.py::test_real_agent_loop_gate_stops_thread_on_cancel | 同上删 :211 → 线程跑满 5 轮、`状态["停"]` 仍 假 → assert 立红 |
| 既有 e2e：限时 < 延迟时全条超时且整场跑完 | tests/test_harness_agent_light.py::TestM18超时 | 删 `评测一条_agent` 的 `捕获` 兜底（:248 附近） → TimeoutError 冒泡 → 整场崩、报告不落盘 → 立红 |

### 3.4 POSIX / 诚实边界

本机 **Windows 11 + Python 3.13.12 托管运行时**，无 POSIX 环境 → Hybrid 方案跨平台行为一致（协作式闸门 + 线程级 detach 不依赖 OS 信号），**但「完全不 await 的死阻塞（如挂死网络挂载的 `open`）仍不强制杀」**——那需走 Path A 进程隔离（SIGKILL）。本结论属实现层诚实边界，已记入 `评测驱动.light` 注释，未写「应该没问题」。

---

## 4. §3.5 流式.light 四条异步读腿搬回类体（done · 本轮提交 8af830fe）

**背景/裁决**：A9 S1 已实测支持类体内 `异步 段落`（codegen 产出 `async def` 方法），A5 当时因「类体不支持异步」只记账未搬，四条读腿（`协程收`/`协程读体`/`协程读分块`/`协程按行读取`）一直以模块级段落 + `客户端` 首参形式存在。本轮把欠账清掉：搬回 `类 HTTP客户端` 体内，是**纯重构、零行为变化**。

### 4.1 改动清单（3 文件）

- **`stdlib/流式.light`**
  - 四条读腿由模块级段落搬入 `类 HTTP客户端` 体内（`协程收` 344 / `协程读体` 372 / `协程读分块` 397 / `协程按行读取` 423，均为 2 空格缩进），方法签名去掉 `客户端` 形参、体内 `客户端.` → `己.`；`异步 遍历 块 之 己.协程读分块()` / `等待 己.协程收(4096)` 等价替换为方法内 `self` 调用。
  - 删除模块级定义与导出；`导出` 仅保留 `HTTP客户端 / 连接错误 / 读取错误 / HTTP错误`。
  - 清理 A5 注释块中已失效的「类体不支持异步段落」「已移交 B5」等表述，改为如实描述「现已实现为类内 `异步 段落`」+ 仍阻塞部分的诚实边界。
- **`stdlib/大模型客户端.light`**：导入由 `从 流式 导入 HTTP客户端, 连接错误, 读取错误, HTTP错误, 协程读体` 删去 `协程读体`；调用点由 `异步 遍历 块 之 协程读体(客户端对象.客户端):` 改为 `异步 遍历 块 之 客户端对象.客户端.协程读体():`（仅此一处消费点）。
- **`tests/test_async_io_light.py`**：import 由 `from 流式 import HTTP客户端, 协程读体, 协程按行读取` 改为 `from 流式 import HTTP客户端`；inspect 断言 `流式模块.协程收` 等改为 `流式模块.HTTP客户端.协程收` 等；三处调用 `协程读体(c)`/`协程按行读取(c)` 改为 `c.协程读体()`/`c.协程按行读取()`。

### 4.2 语义等价性证明

生成代码逐句同形：模块级 `(客户端)` 形参版 `async def 协程收(客户端, 大小): ... await 客户端.协程收(...)` 与类内版 `async def 协程收(self, 大小): ... await self.协程收(...)` 仅 `客户端`/`self` 命名差异，运行期行为完全一致。判据测试 `test_async_io_light.py` 含**时序判据、心跳让出控制权（证明读腿不阻塞事件循环）、TLS 分支、增量逐字相等**等，17/17 全绿即证明行为未变。

### 4.3 验收实证

- **七道门禁串行全 rc=0**：`dist_criteria / assert_quality / python_direct_calls / native_product / bootstrap_rate / floor_bootstrap / spec_coverage`。关键：`python_direct_calls` **总数 251 ≤ 基线 251、无文件上升** —— 证明纯重构零新增 Python 直调；`dist_criteria` 完成度 0→5、`bootstrap_rate` 40.48%→40.91% 均为「未降即过」，按 §6.5 **未刷任何 `*_baseline.json`**。
- **波及面测试全绿**（§3.5 的精确回归面 = 所有 import `流式`/`大模型客户端` 的模块）：`test_async_io_light` **17 passed**；`test_llm_client_light` **12 passed / 3 skipped**（3 skipped 存量，与本次无关）；`test_stream_light` **13 passed**；`test_stream_async_light` **2 passed**；`test_tls_light` **3 passed**；`test_deepseek_mock` **9 passed**；`test_harness_real_channel_light` **3 passed**；`test_distributed_eval_light` **6 passed**（满足 §6.3）。
- **全量 1203 用例扫场**：本机 Windows 跑一轮约需 ~30 分钟且 e2e 依赖三方库/网络，按交付报告 §7「本机绿不构成 CI 绿证据」，交由 CI（FreeBSD runner）执行；本地以「门禁 + 波及面测试」作为零新增红的实证。

### 4.4 POSIX 并发时序：未实测（诚实边界）

本机 **Windows 11 + Python 3.13.12 托管运行时**，无 POSIX 环境，故 §3.5(b)「并发时序零实测」**本机无法跑，明确标「未实测」**，不写「应该没问题」。仍阻塞的部分（`连接 / TLS 握手 / 发请求 / 读响应头` 走冻结的同步 `发送()`，await 点只在体读取腿）属语言层 `add_reader / to_thread` 未补齐的已知账，已如实留在 `流式.light` A5 注释块。

---

## 5. 对标清单四条 partial 补齐条件（done · 本轮提交 926507d0）

> 状态字段由 G9 统一改；以下为「建议」结论，未自行改动 `功能对标清单_harness.json`。

### #2 并发调度 —— **建议改 done**

- 补齐条件（任务书 §4 给的精确判据）：耗时数组 `[0.30, 0.01, 0.01, 0.01]`、上限 2 ⇒ 滑窗总耗时 ≈ 0.30s **且** 第 3 个任务起跑时刻 < 慢任务完成时刻。
- 实证（`tests/test_concurrency_light.py::Test并发调度滑窗`）：总耗时 0.301s ≈ 0.30s，第 3 个任务起跑 0.011s < 慢任务完成 0.300s，`结果 == [0,1,2,3]`。**证明 `任务池`（并发.light:62，信号量滑窗）是真滑窗，不是分批 barrier。**
- 之前卡在「只有 `并发峰值 ≤ 2` 这种粗判据」，批大小 2 的分批实现也能混过。现在判据锁定滑窗关系，反跑点：把 `任务池` 改成「按上限分批 gather barrier」→ `完成[2] < 完成[0]` 与 `起跑[2] < 起跑[0]+0.30` 立即红。
- 顺带：任务书 §4 点名的 `test_harness_e2e_light.py:128` 分批口径注释，**已在 E9-S1 订正**为真滑窗口径（见该文件 138-143 行），本次无需再动。

### #5 错误分类与单条兜底 —— **建议改 done（含两处真实代码缺口补齐）**

- (1) 单模模式分类：任务书原称「分类器只有 agent 模式吃到，single 走编排.light 的 `捕获 TimeoutError, ConnectionError`」。经核验，当前 `评测一条`（评测驱动.light:292-310）**已**在单模分支做单条兜底并调 `错误分类.分类`（:309），且 `任务池`（`失败模式="逐条收集"`）在协程内吃掉异常——single+real 单条失败**不再整场崩**。既有 `test_harness_e2e_light.py::test_超时注入走重试且得分为零`（HARNESS_MODE=single）断言失败条目 `得分==0` 且 `通过数==2`（其余 4 条仍成功），即实证。
- (2) **重试决策由 `错误分类.应重试` 驱动**（本轮新接）：`编排.带重试对话`（编排.light:84 区）原只 `捕获 TimeoutError, ConnectionError` 无脑重试；现改为 `捕获 异常 e:` → `错误分类.分类` → `非 错误分类.应重试(分类结果)` 则直接记失败停止重试。判据：`tests/test_harness_streaming_light.py::Test错误分类驱动重试` —— TimeoutError（超时类，应重试）尝试次数==3；JSONDecodeError（解析类，不应重试）尝试次数==1 且 `错误=="解析"`。反跑点：删 `非 错误分类.应重试` 判定 → 解析类也被重试 → `尝试次数==1` 红。
- (3) **报告兜底落盘**（#5(2)）：`主` 流程原汇总+写盘裸奔（评测驱动.light 原 497-508），任一异常整份报告丢失。本轮抽 `写报告带兜底`（评测驱动.light:437 区），全程 try/except，异常时仍把逐条结果落盘到 `.partial.json`。判据：`tests/test_harness_streaming_light.py::Test报告兜底落盘`（结果表含非数值 `耗时="坏"` 令 `批量汇总` 抛 TypeError）→ 返回 空、主报告不生成、`.partial.json` 含原样 `条目` 与 `汇总=None`。反跑点：去掉 try → 异常直接炸出、`.partial.json` 不存在。
- 遗留小账已收（本轮 §11）：`错误分类.light:101-107 状态码为整数` 原为零调用点、注释声明夸大；本轮已**接上调用点**——`应重试` 的 `HTTP状态` 分支改为先经 `状态码为整数` 归一成 int 再比较（空状态码仍走原重试分支，行为不变），详见 §11。六类分类与重试正确性不受影响、反被加固（字符串状态码不再 `TypeError` 崩溃）。

### #6 流式增量消费 —— **建议改 done**

- 补齐条件：harness 层首次覆盖「>1 块」流式消费，并正确累加增量。
- 实证：`编排.带重试对话`（编排.light:68 区）原 `设 累积 为 块["累积内容"]` 逐轮覆盖（依赖通道给累积内容，脆弱）。本轮改为**优先累加 `内容增量`**（真正的逐块增量），通道只给 `累积内容` 时回退取最后非空。判据：`tests/test_harness_streaming_light.py::Test流式增量消费` —— 通道给 `内容增量=["你","好",""]` → 输出 `"你好"`；退化通道只给 `累积内容` → 回退得 `"北京"`。两条反跑点均注明。
- 注：既有 e2e 断言 `输出==["北京","西方",...]` 仍全绿，证明增量累加与旧累积口径产出一致、无回归。

### #16 取消 / 超时 —— **建议改 done**

- 随 §3.4（提交 c06501f3）一并收口：取消令牌生产入口（评测驱动.light:243 新建 / :154 注入）+ 超时即 `取消令牌.取消()` 协作式真掐，2 条真停判据全绿。详见 §3.4 章节。本机 Windows 未实测 POSIX 强杀路径，已如实声明。

---

## 6. §5 A9 四条语言缺陷核实（done）

实证方法：直接读 `src/code_generator.py` / `src/lexer.py` / `src/parser_stmt.py` 的生成规则与 lexer token 映射，而非凭记忆。代码生成规则不会因 S2 工作改变，结论对全仓有效。

| # | 缺陷 | 代码实证 | 状态 | 对 §3.4 / 结构化任务组的影响 |
|---|---|---|---|---|
| 1 | 空 `尝试` 偷插 `except Exception: pass` 吞异常 | `code_generator.py:4423 / 4520 / 4576` 仍生成 `except Exception: pass` | **未修** | 不直接挡；`评测一条_agent` 的 `尝试/捕获/最终` 三段式绕法照旧，行为不变 |
| 2 | 裸 `捕获` → `except Exception`，抓不到 `CancelledError`（3.11+ 是 `BaseException` 子类） | `code_generator.py:1700` 对无类型 `捕获` 仍生成 `except Exception:` | **未修** | **决定性约束**：协作式取消**不能靠抓异常**，必须走显式 `是否已取消()` 布尔闸门（代理循环.light:521/526）。§3.4 据此设计为 token-guard 驱动，而非 exception-catch 驱动，规避了此缺陷 |
| 3 | 光明没有 `!=` 运算符（写了报「无法识别的语法元素」） | `lexer.py:26` 列 `不等于` 为关键字、`:1218` 映射 `NOT_EQ`；`parser_stmt.py:2006/5490/5495` 在比较表达式中接受 `TokenType.NOT_EQ`；`code_generator.py:211` 映射 `不等于→!=` | **已修** | 无碍；`并发.light:56-62` 当年的两重 `==` + 布尔变量绕法现可简化（非本次范围） |
| 4 | 字符串拼接 None → `TypeError` | 运行时行为，非编译器缺陷；仍靠 `转字符串(...)` 绕 | **绕法中（非编译器修复）** | 不在 §3.4 路径上 |

**结论**：A9 四条中 #3 已修、#1/#2 未修、#4 仍靠绕法。#2 未修直接塑造了 §3.4 的取消设计（token-guard 而非 exception-catch），故结构化任务组与超时真掐均不依赖「裸捕获能抓 CancelledError」。报告里 §3.1 判定「嵌套取消因 `并发等待=asyncio.gather` 天然成立」、§3.4 判定「协作式真掐靠 `是否已取消()` 闸门」均成立，不硬说 done 之外的事。

---

## 7. 实测平台说明

- **本机环境**：Windows 11 + Python 3.13.12 托管运行时（光明编译器/托管运行时）；个别探针用到 Python 3.14。
- **CI 环境**：仅 FreeBSD runner（192.168.1.5），未在本仓跑完整 e2e（e2e 依赖三方库/网络/真实 API）。
- **核心原则**：**本机绿 ≠ CI 绿**。所有并发时序 / 线程 / 取消相关判据仅在本机 Windows 验证；POSIX（Linux/macOS/FreeBSD）侧行为未实测，逐项标注见 §9。
- **时间分辨率规避**：Windows 默认 15ms 定时器分辨率会影响绝对耗时断言。所有 timing 判据均用「跨 0.30s 时差的**时序关系**（第 3 任务起跑 < 第 1 任务完成）」而非绝对耗时，沿用 E9-S1 设计，慢机器/低分辨率下不抖。

---

## 8. 移交清单（总纲 §9 第 4 项：光明写不出什么 → 本轮绕法 → 期望能力）

| # | 光明写不出 / A9 已知 bug | 本轮临时绕法 | 期望语言能力（交 A9） |
|---|---|---|---|
| 1 | 空 `尝试` 偷插 `except Exception: pass` 吞异常（codegen `except` 折叠变种） | 一律写成 **尝试 / 捕获 异常 e / 抛出 e / 最终** 三段式：`错误分类.light`（`分类`/`应重试`/`读异常码`/`状态码为整数`）、`评测驱动.light`（`主流程`/`写报告带兜底`）、`编排.light`（`带重试对话`）均用此绕法 | 修复 codegen：无显式 `捕获` 时不生成 `except Exception: pass`，只生成 `try/finally` |
| 2 | 裸 `捕获` → `except Exception`，抓不到 3.11+ 的 `CancelledError`（BaseException 子类） | 显式 `是否已取消()` 布尔闸门（`代理循环.light:521/526`），取消走 token-guard 而非抓异常（§3.4 据此设计） | 支持裸 `except` / `捕获 BaseException` 语义，消除对外层 `最终` 兜底的依赖 |
| 3 | 光明无 `!=` 运算符（已修，lexer/parser/codegen 已落地） | 无需绕法；备注 `并发.light:56-62` 旧「两重 `==` + 布尔变量」绕法可后续简化（非本次范围） | —（已完成） |
| 4 | 字符串拼接 `None` → `TypeError` | 统一 `转字符串(...)` 兜底（`错误分类.异常名`/`分类` 已用） | 字符串 `+ X` 做隐式 `str()` 兜底 |
| 5 | **post-except 折叠 bug**：`捕获` 块后紧跟的语句被折进 except 体，正常路径不 return | `读异常码` / `状态码为整数` 抽成独立 helper，各 `返回` 待在自己块里（见 `错误分类.light` 注释） | 修复 post-except 折叠，允许在 `捕获` 体后正常续写 |
| 6 | （测试侧坑，非语言缺陷）`Dict` 字面量用 `[]` 套 `{}` 报 SyntaxError，须用 `{}` | `test_harness_streaming_light.py` 初稿踩过，已改为 `[{"role": ...}]`；记此备忘 | — |

> 类体内 `异步 段落`（A9 S1 已交付）本轮已消费（§3.5 四条读腿搬回 `类 HTTP客户端` 体内），非移交项。

---

## 9. 已知未完成 / 未实测项（总纲 §9 第 5 项，POSIX 逐条标注）

- **POSIX 并发时序**：全部测试在 Windows 11 完成，**未实测 POSIX**；CI FreeBSD 未跑本仓完整 e2e（仅门禁 + 波及面本机跑）。【POSIX: 未实测】
- **§3.5 仍阻塞部分**：`连接 / TLS 握手 / 发请求 / 读响应头` 走冻结的同步 `发送()`，await 点只在体读取腿。属语言层 `add_reader / to_thread` 未补齐的已知账（已留 `流式.light` A5 注释块）。【POSIX: 同未实测；Windows 亦未全链路跑通真实网络】
- **限流（滑动窗口类）未接 master 派发路径**：对标清单限流项维持 **partial**（非本次范围，归 F9 派发接入）。【平台无关】
- **结构化任务组依赖 `并发等待=asyncio.gather` 级联取消语义**：若未来把 `并发等待` 换成非级联原语（如裸 `asyncio.wait`），§2 两条嵌套取消判据立即立红——这正是它们作为回归护栏的价值。【平台无关】
- **超时真掐 POSIX 强杀路径未实测**：完全不 await 的死阻塞（如挂死网络挂载的 `open`）仍不强制杀，需 Path A 进程隔离（SIGKILL）。Hybrid 协作式闸门 + 线程级 detach 不依赖 OS 信号，跨平台一致。【POSIX: 未实测；Windows 同理不强制杀】
- **`功能对标清单_harness.json` 状态字段未动**：由 G9 统一改（功能编号需双字段匹配），本报告只给「建议改 done」结论，未自行改 JSON。【平台无关】
- **`错误分类.light:101-107 状态码为整数` 小账：本轮已清理**（接上调用点，见 §11），不再是零调用债务。【平台无关；已完成】

---

## 10. skip / xfail 清单与掩盖分析（总纲 §9 第 6 项）

- **本轮 S2 未引入任何新 skip / xfail，也未改动任何既有 skip/xfail。**
- **存量相关**：`tests/test_llm_client_light.py:325` 的 `@pytest.mark.skipif(not os.getenv("DEEPSEEK_API_KEY"), ...)` 作用于 `class TestRealAPI`（**3 条**），在本机/CI 无 key 时 skip。其**掩盖分析**（该文件 314–320 已自注）：掩盖的**只是**「真实 DeepSeek 对 tools 声明与 SSE 分帧的接受度」（即「对方认不认」），**不掩盖 tool_call 功能本身**——功能由 `tests/test_deepseek_mock.py` 守（无 skip、任何机器必绿）。此 skip 与 S2 改动无关（S2 未碰 `大模型客户端` 的 tool_call 路径）。
- **其余仓库级 skip/xfail**（与本轮无关，列出以示未新增）：`test_level6_lexer`（反引号转义未实现）、`test_lsp_protocol_full`（脚本式需手跑）、`test_semantic`（重构中 API 不兼容）、`test_llvm_*`（缺编译器/缺 clang-leg/mcjit-leg）、`test_path_guard_light`/`test_pty_light`/`test_process_tree_light`（POSIX↔Windows 互跳）、`e2e/test_L3_domain_e2e`/`e2e/test_L4_python_e2e`（需环境）——均属主线或其他轮次账，S2 **零新增、零变动**。

---

## 11. 小账清理：`错误分类.light:101-107 状态码为整数`（总纲 §9 收口）

**背景**：`状态码为整数` 自第七轮起为「零调用点」函数；文件头注释「供报告/重试用」属**声明夸大**（实际从未被任何路径调用）。任务书 E9 §3 硬要求：「`应重试` / `状态码为整数` 两个零调用点函数：**接上或删掉**，不许留『文件头声称接了重试决策、实际没接』」。

**处理：接上（非删除）** —— 把 `应重试` 的 `HTTP状态` 分支改为先经 `状态码为整数` 归一成 int 再比较：

```
如果 类 == "HTTP状态":
  设 码 为 分类结果["状态码"]
  如果 码 == 空:          # 原分支保留：缺状态码仍重试（与旧行为一致）
    返回 真
  设 码整 为 状态码为整数(码)   # 新增：归一 int，非数字 → -1
  如果 码整 == 429:
    返回 真
  如果 码整 >= 500:
    返回 真
  返回 假
```

**收益**：
1. **清掉零调用点债务**：helper 现真实接进重试决策，注释声明变真实 → 消除「声称接了实际没接」。
2. **防御性增强**：旧代码 `码 >= 500` 若收到字符串状态码（如 `"503"`）会抛 `TypeError` 炸掉重试决策；现经 `转整数` 归一为 `503` → 正确判重试；非法值（如 `"abc"`）→ `-1` → 不重试（保守正确）。

**行为不变项**：生产路径 `HTTP错误.状态` 恒为 int（`流式.light:311` 解析状态码 `转整数` 产出），故 `503 / 404 / 429` 等判定结果与旧实现逐字节一致；仅对非常规（非 int）输入由「崩溃」变为「安全降级」。

**验证**：`python_direct_calls` 门禁 **251 不变**（纯光明 light-to-light 调用，零新增 Python 直调）；`tests/test_harness_streaming_light.py` 5 条判据仍全绿（仅覆盖 超时/解析 两类，不触 `HTTP状态` 分支）；附内联 `应重试` 单测 4 例（空 → 重试；`503` → 重试；`404` → 不重试；`"503"` → 重试；`"abc"` → 不重试）。
