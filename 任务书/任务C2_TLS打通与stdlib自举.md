# 任务 C2｜转译腿破 M2：TLS 打通 DeepSeek + stdlib 自举率破 10% + agent 循环骨架

> **任务编号**：C2　**优先级**：P0（M2 唯一硬阻断在你手里）　**依赖**：弱依赖 A2 的异步并发原语（可先写同步版）
> **仓库**：`g:\dswork\duan-light-merge\light-merge`，主分支 `main`（HEAD `e27277c2`）
> **工作树**：`../wt-C2`，分支 `task-C2`　**端口段**：19200–19299
> **⚠️ 开工前必读**：`任务书/协作规程.md` + `任务书/第二轮总纲与规程增补.md`

---

## 0. 上一轮你交的东西是这一轮的地基（先确认状态）

我逐行核实并实跑过，**你上一轮交的三个模块是纯 `.light` 真实现，不是壳**——你是第一个真正激活 `.light` 加载路径的人：

- `stdlib/SSE.light`（154 行）：增量喂入 `:35-43`、三种行尾 + 跨 chunk 断行 `:55-80`、`codecs` 增量解码器兜 UTF-8 跨块 `:29`、前导单空格剥离 `:91-111`、多 data 行 `\n` 拼接 `:114-138`。20 条真跑用例全绿
- `stdlib/流式.light`（268 行）：手写 HTTP/1.1 请求 `:131-149`、响应头解析 `:152-175`、**chunked 解码** `:178-201`、三种体读取 `:72-94`、通用 `按行读取` `:98-128`、连接/读超时分开 `:33-34`
- `stdlib/大模型客户端.light`（197 行）：`/chat/completions` `:51`、非流式 `:56-64`、流式 `:67-101`、`[DONE]` 哨兵 `:87`、tool_calls 按 index 分组累积 `:127-148`、key 只从 `DEEPSEEK_API_KEY` 环境变量读且不打印 `:37`。6 passed + 2 skipped

**但有一个洞被 skip 盖住了**，见 C2-1。

---

## C2-1 【P0，本轮最高优先】TLS：现在明文往 443 端口发

**证据**：`stdlib/流式.light` 用裸 socket，**全文无 ssl/TLS**。而 `stdlib/大模型客户端.light:43-44` 对 `https` scheme 的处理只是**把端口设成 443**，然后明文发出去 → 真打 `api.deepseek.com` 必挂。

**为什么没被发现**：两条真 API 用例 `tests/test_llm_client_light.py:198-209` 在无 `DEEPSEEK_API_KEY` 时 skip，正好是你报告里那 2 skipped。**这是第二轮总纲 §5 点名的"被 skip 掩盖的洞"的标准案例。**

**要做的**
1. `stdlib/流式.light` 的传输层加 TLS。**允许 `导入 ssl`**（普通 import，不算 `引 Python` 逃逸），用 `ssl.创建默认上下文` / `包装socket` 把已连接的 socket 升级。你上一轮已经把传输层抽成了接口，正好从这里换实现。
2. **证书校验默认开启**，SNI 必须带（`server_hostname`）。提供 `校验证书=假` 开关也必须默认真，且关闭时 stderr 打醒目告警。**不许为了让测试过而默认跳过校验。**
3. `大模型客户端.light:43-44` 的 scheme 处理改成真按 scheme 选传输层，不是只改端口。
4. **代理支持**（`HTTPS_PROXY` 环境变量 + CONNECT 隧道）：如果本机需要代理才能出网，这条就是 P0；否则记为 P2 移交。开工先测一下本机能不能直连 `api.deepseek.com:443`。

**测试（关键：不许再让 skip 盖住）**
- **离线 TLS 用例（必须有，不许 skip）**：测试里用 Python `ssl` 起一个**自签证书**的 HTTPS + SSE server（端口段 19200–19299），让 `.light` 客户端连它。正例：把自签 CA 传给客户端 → 握手成功、SSE 帧正确解析。**负例：不传 CA → 必须握手失败并抛明确的光明异常**（这条负例是防"默认跳过校验"回归的护栏）。
- **真 API 用例**：仍然 `skipif(无 API key)`，但报告里必须写明"这条 skip 掩盖了什么"。有 key 时你**必须亲自跑一次并把结果贴进报告**（不许只交一个"理论上能通"）。

**M2 的验收定义**：一个 `.light` 脚本，`异步 运行` 启动，真连 `https://api.deepseek.com`，流式逐 token 打印一轮回答，再跑一次 tool_call 往返。**`引 Python` 计数为 0。** 这个脚本放 `examples/harness/M2_流式对话.light`。

---

## C2-2 【P0】修 `.py` 遮蔽机制：现在有一个自我矛盾的文件

**证据**：`stdlib/列表工具.light` 文件头自称「本模块没有 Python 实现兜底，全部由段言自身写成」，有 10 个真实 `段落` 实现（`求和`/`最大值`/`反转列表` 等）。但 `stdlib/列表工具.py`（77 行真 Python 实现）存在 → 钩子 `stdlib/_light_import_hook.py:141` 直接 `return None`，**光明版本被完全遮蔽、永不执行**。注释与实际加载行为矛盾。

**根本问题**：钩子的策略是「同名 `.py` 存在则 `.light` 绝对跳过」，没有任何"本模块由光明实现"的表达方式。这个策略是"自举率永远上不去"的结构性原因。

**要做的**——加一个**显式的纯光明声明机制**，三种设计选一，理由写进报告：
- a) `.light` 文件首行魔法注释（如 `# 纯光明实现`）→ 钩子读到就优先加载 `.light`，无视同名 `.py`；
- b) `stdlib/纯光明清单.txt` 白名单文件 → 钩子查表；
- c) 环境变量/全局开关 `PURE_LIGHT_ONLY`（**注意：这个名字已存在但全仓值恒为 0，钩子的 `.light` 分支在 stdlib 范围是死代码**）→ 激活它。

**倾向 a)**：自描述、不需要维护第二份清单、和"文件自称纯光明"的现状自然对齐。但你评估。

落地后：
- `stdlib/列表工具.light` 必须真正被加载（写一条断言"加载到的是 `.light` 版本"的测试，例如比对某个只有 `.light` 版才有的行为或属性）；
- **`stdlib/列表工具.py` 不要删**（可能有现存调用方），由主线裁定是否废弃。你只负责让 `.light` 优先。

**这是本轮唯一允许你改的 `stdlib/` Python 基建文件**（`_light_import_hook.py`）。改动要小、要可回退、要有测试。

---

## C2-3 【P1】自举率：从 8/63 提到 ≥13/63，并清掉腐烂源

**现状实测**：`stdlib/` 顶层 63 个 `.light` / 78 个 `.py`；`stdlib/lightpub/` 递归 **0 个 `.light`** / 56 个 `.py`。

**有真实现的 `.light` 只有 8 个**：`插件`(23 个段落/类)、`流式`(22)、`事件总线`(20)、`进程树`(18)、`大模型客户端`(15)、`模式校验`(12)、`SSE`(11)、`列表工具`(10)。

**其余 55 个是腐烂源**：抽查 `字符串工具.light`(272 行)、`日期时间.light`(295 行)、`数据结构.light`(245 行)、`CSV读写器.light`(230 行)、`断言工具.light`(250 行)、`JSON.light`(146 行) —— **每一行实现都以 `# ` 开头**（形如 `# def转大写(文本):`），是自动转写留下的残迹，无任何可执行光明代码。这些文件比不存在更糟：它们让人以为 stdlib 已经用光明写好了。

**要做的**
1. **真写 5 个**（优先选 harness 用得上的）：建议 `字符串工具`、`数据结构`、`JSON`（编解码，harness 刚需）、`断言工具`、`日期时间`。配合 C2-2 的机制让它们真正被加载。
   - **注意 JSON 的坑**：`stdlib/JSON.py` 存在且有现存调用方与测试（`tests/test_stdlib_complete.py:248-253` 测的是里面那个旧 `JSONSchema验证`）。C2-2 的机制落地后 `.light` 会优先——**所以你的 `JSON.light` 必须行为兼容**，或者换个模块名（如 `JSON编解码.light`）避免影响现存调用方。**换名更安全，倾向换名。**
2. **剩下 50 个整文件注释的伪代码怎么处置**：本轮**只出方案不动手**——是删掉（让 `导入 X` 明确失败并指向 `.py`）、还是缩成三行导出清单 + 一句"实现见同名 .py"（像 `stdlib/文件系统.light:1-7` 那样诚实）？给出建议交主线裁定。**不要一次性批量改 50 个文件**，那会把这轮的 diff 淹掉。
3. 报告里给出**自举率前后对比数字**（分子/分母定义写清楚：有真实现的 `.light` / `.light` 总数，以及实际被加载的数量）。

---

## C2-4 【P1】agent 循环骨架 → `examples/harness/` + `stdlib/代理循环.light`

这是把 A2/B2/C2/D2 四路成果第一次串起来的东西。范围克制，**只做单 agent 单轮工具调用的最小闭环**：

- 会话状态：消息列表（system/user/assistant/tool 四种角色）、追加、截断策略（超 token 上限时丢最早的 user/assistant 对，保留 system）
- 工具注册：`注册工具(名称, 描述, 参数模式, 实现)`，参数模式用你上一轮写的 `stdlib/模式校验.light` 校验，**校验失败要把可读错误喂回模型让它重试**（这正是你上一轮做结构化错误路径的用途，现在把它接上）
- 主循环：发请求 → 流式打印 → 若 `finish_reason == tool_calls` 则分发工具 → 结果作为 `tool` 角色消息追加 → 再发一轮 → 直到 `stop` 或达到最大轮数
- 工具执行走 **D2 的 `stdlib/进程树.light`**（执行 shell 命令类工具）——**跨任务依赖，接口先约定好，D2 在修那个模块的解码 bug，别自己去改它**
- 重试与超时：网络错误指数退避。**这里需要 A2 的 `异步睡眠`**；A2 未就绪前用同步 `时间.睡眠` 并留 `# 待 A2 异步原语就绪后改` 标记
- 事件钩子走 **C2 之外的 `stdlib/事件总线.light`**（你上一轮的产出，13 条用例全绿，直接用）：`请求开始`/`增量到达`/`工具调用`/`轮次结束`

**测试**：`tests/test_agent_loop_light.py`（新建），全程离线——fake server 回放录制好的 SSE（含一次 tool_calls 往返）。断言：工具被正确调用、参数校验失败时有重试、最大轮数生效、事件按序发出。

---

## 交付要求

1. **可改/新建**：
   - 改：`stdlib/流式.light`、`stdlib/大模型客户端.light`、`stdlib/_light_import_hook.py`（**仅 C2-2 那处策略，改动要小**）
   - 新建：`stdlib/代理循环.light`、`stdlib/字符串工具.light` 等 5 个真实现（覆盖现有空壳内容）、`examples/harness/M2_流式对话.light`
   - 新建测试：`tests/test_tls_light.py`、`tests/test_agent_loop_light.py`、`tests/test_pure_light_hook.py`，并扩充 `tests/test_llm_client_light.py`
   - **绝对不许**建 `stdlib/代理循环.py`、`stdlib/字符串工具.py` 之类同名 `.py`
   - **不许碰**：`src/` 全部（A2/B2 的地盘）、`stdlib/JSON.py`、`stdlib/进程树.light` 与 `stdlib/事件总线.light`（D2 的地盘，可以 `导入` 使用，不许改）
2. **`引 Python` 计数必须为 0**。`导入 ssl` / `导入 socket` / `导入 codecs` 都可以。
3. **定向测试**：你新建/改的那些 + `tests/test_sse_light.py`、`tests/test_llm_client_light.py`、`tests/test_schema_light.py`、`tests/test_stdlib_complete.py`。**不许跑全量**，不许 `git stash`。
4. **skip 清单与掩盖分析**（第二轮新增交付项）：每条 skip 写明掩盖了什么。C2-1 的真 API 用例是重点。
5. **端口写死 19200–19299**，绑定前探测，占用则报错退出，**不许自动 +1 重试**（会漂到 B2 的段）。
6. 临时文件前缀 `_taskC2_`，收尾删干净。
7. **安全**：API key 只从环境变量读；日志/报告里不许出现 key 或完整 `Authorization` 头；请求体不许明文落盘；自签证书与私钥用测试临时目录，**不许提交进仓库**。
8. **PowerShell 5.1**：中文输出落盘再读；不支持 `&&` 与 heredoc。
9. 提交逐路径 `git add` 白名单，**绝不用 `-A`**（第一轮你的 worktree 出现过积木库缺 18681 个文件的半检出）。

## 移交清单

格式 `光明写不出什么 → 临时绕法 → 期望的语言能力`。上一轮你的清单直接决定了 A2 修什么，继续保持。已知会命中的：
- `外层` 在段落名冲突时静默失效（A2-2 在修）→ 用类属性兜
- 异步并发原语缺失（A2-3 在修）→ 同步版 + 标记
- 泛型被 codegen 丢弃（A2-4 在修）→ 不写泛型
- `stdlib/lightpub/__init__.py:45` 路由到**外部仓库** `C:\dumatework\lightpub`，其 `__index__.py` 声明了本仓库不存在的模块 → 别依赖 lightpub，登记即可
