# 原生腿覆盖 · 第一批攻坚交付报告（R10-8）

- **日期**：2026-09-04
- **范围**：light-merge「代码生成不支持」13 个模块的首批编译器修复
- **文件隔离**：只动 `src/llvm/codegen_typed.py`、`src/llvm/runtime_typed.c`、`docs/原生腿能力清单.json`、`docs/known_issues.md`；未触碰 `src/llvm/size_optimizer.py`（T3）、`src/llvm/runtime_typed.c` TLS 段（T7）、其它 codegen 模块。
- **基线**：R10-7（T9 核对）原生可编译 **5/89（5.62%）**。

## 一、成果：5 → 12（13.48%）

新增 6 个可编译模块（`compile_light_typed` 真出 IR）：

| 模块 | 此前阻断 | 本轮修复 |
|---|---|---|
| 内置核心判型 | 未定义的段落：类型 | 内建名 `类型` 补到取类型分支 |
| 内置核心转换 | 未定义的段落：str | 内建名 `str` 补到转字符串分支 |
| 内置核心字典 | 未定义的段落：映射 | 变量/内置判型名重名歧义兜底（`是浮点` 同源，字典内部变量名冲突） |
| 列表工具 | 未定义的段落：是浮点 | 变量歧义兜底 |
| 路径运算 | 未定义的段落：开头/字符串重复/分割字符串 | 补 `开头`→`dv_str_starts_with`、`字符串重复`→`dv_str_repeat`、`分割字符串`→split 别名 |
| 操作系统 | 未定义的段落：连接路径/字符串包含/列表排序/目录存在 | 新增 `dv_path_join`（runtime）+ `连接路径` builtin；补 `字符串包含`、`列表排序`、`目录存在` |

## 二、落地修复明细

### 1. `src/llvm/codegen_typed.py`
- **内建别名补齐**（`_gen_typed_builtin`，全部为既有运行时函数接线）：
  - `类型` → 取类型分支（L1710 区域）
  - `str` → 转字符串分支
  - `替换字符串` → 替换分支
  - `转大写` / `转小写` → 大写/小写分支
  - `开头`/`以开头`/`startswith`/`starts_with`/`前缀是` → `dv_str_starts_with`（原 declare 未接线）
  - `字符串重复`/`重复`/`str_repeat`/`repeat` → `dv_str_repeat`
  - `字符串包含`/`str_contains`/`包含字符串` → `dv_str_contains`
  - `连接路径`/`join_path`/`path_join` → `dv_path_join`
  - `分割字符串` → split 分支别名
  - `列表排序` → sort 分支别名
  - `目录存在`/`dir_exists`/`is_dir` → `dv_is_dir`（原 declare 未接线）
- **异常类名直呼构造**（`_gen_typed_builtin` 开头）：stdlib 写 `抛出 运行时错误("…")` 是裸类名调用（非 `新建`），此前纯注册异常别名不够，补直呼构造分支（走 `dv_class_new_named`，与 `新建 异常` 同语义）。覆盖中文 6 别名 + 英文 8 名。
- **变量名与内置判型名重名歧义兜底**（`_gen_typed_function_call` reject 前）：v3 解析器把 `设 是浮点 为 假` 后 `如果 是浮点 == 真` 的变量引用解析成**无参 ParagraphCall**（因 `是浮点` 是内置判型名），适配层转成 `FunctionCall`，原生腿当作函数调用报「未定义段落」。兜底：**无参调用且名字已在 `_local_vars` 时降级为变量引用**，与转译腿语义一致。这是 `是浮点`（阻塞 JSON/JSON核心/列表工具 3 模块中的前身）、`映射`（内置核心字典）的公共根因。

### 2. `src/llvm/runtime_typed.c`
- **新增 `dv_path_join`**：POSIX 风格路径连接（对齐 `stdlib/内置核心路径.light` 的 `连接路径` 口径：b 绝对则丢弃前面全部、a 尾斜杠不重复补）。clang -fsyntax-only 无 error（仅既有 strcpy deprecated 警告）。

### 3. `docs/原生腿能力清单.json`
- 全量同步：builtin 243→290、runtime 215→216（新增 `dv_path_join`）；因 codegen/runtime 插入代码导致既有行号漂移，**全量重算** evidence 行号（能力单点测试通过）。

## 三、验证证据

- `python _taskT9b_check.py`（13 模块逐个 `compile_light_typed`）：12 OK / 7 FAIL（剩余 7 个根因见下）。
- 定向回归（codegen 相关，全绿）：
  - `tests/unit/test_llvm_stmt_coverage.py` — 13 passed
  - `tests/unit/test_native_leg_capability.py` — 11 passed（清单×2 双向咬合 + 行号可定位 + runtime 定义定位）
  - `tests/test_llvm_c3_expr.py` — 56 passed
- `runtime_typed.c`：clang -fsyntax-only 通过（无 error）。

**口径说明**：`compile_light_typed` 成功 = 解析+适配+IR 生成通过。**运行期语义正确性未在本批验证**（需对应模块的 `.light` 真跑测试，属下一批）；「可编译」与「可运行」之间的差距按 T9 既定口径分层标注。

## 四、剩余 7 个「代码生成不支持」模块：根因 + 后续路线

| 模块 | 阻断 | 所需能力 | 规模 |
|---|---|---|---|
| JSON / JSON核心 / 内置核心列表 | KeywordArg | 关键字参数（`打开文件(路径,"w",encoding="utf-8")`、`表.sort(reverse=反向)`）+ **文件句柄对象 + 方法分派**（`f.write(文本)`/`f.close()`） | 大（新运行时类型） |
| 内置核心路径 | TupleLiteral | 元组字面量 + 解包/索引（`返回 (头, 尾)`） | 大（新运行时类型） |
| 数据结构轻量 | 列表弹出 | **变异函数属性写回**（`列表弹出(己.数据)` 原地改属性）+ runtime `dv_list_pop` | 中 |
| 文件流 | YieldStmt | 生成器（`生成`/`yield`） | 大 |
| 文件系统 | 目录名 | runtime `dv_path_dirname` + 文件句柄（同 KeywordArg 依赖） | 中（但依赖文件句柄） |

**建议下一批顺序**：① 变异函数写回 + `dv_list_pop`（解锁 数据结构轻量，中等、可先做）；② KeywordArg + 文件句柄（解锁 3 个模块，收益最大但需新类型）；③ 元组；④ 生成器。

## 五、未实测项标注

- 本批为 Windows 本机编译核对 + 定向 pytest 回归；**未做模块级运行语义真跑**（`compile_light_typed` 成功≠运行正确）。运行验证与对应 `.light` 测试纳入下一批。
- `dv_path_join` 行为未做 C 层单元测试，仅语法检查 + IR 生成通过。

## 六、文件清单

- 修改：`src/llvm/codegen_typed.py`（+90）、`src/llvm/runtime_typed.c`（+29）、`docs/原生腿能力清单.json`（同步+重算）、`docs/known_issues.md`（R10-8 回写）
- 交付：本报告（`任务书/原生腿覆盖_R10_交付报告.md`）
- 临时核对脚本 `_taskT9b_*` 已清理。

---

# 追加：第二批（R10-9，2026-09-05）—— 变异函数属性写回 + dv_list_pop

## 成果：12 → 13（14.61%），解锁 数据结构轻量（编译 + 运行语义）

**落地修复**：
1. **runtime `dv_list_pop`**：index<0 弹末尾，复用 `dv_list_remove`；对应 declare。
2. **builtin `列表弹出`/`list_pop`**：返回移除元素后的新列表（数据结构轻量用法是「先取值、后弹、弃返回值」）。**刻意不注册 `弹出`/`pop`**——会劫持 `栈.弹出()` 类方法调用（已踩坑修正）。
3. **函数式变异调用写回**（`_gen_typed_function_call`）：`列表弹出(己.数据)` / `列表插入(己.数据, 0, x)` 等 mutating builtin 返回新列表后，写回第一参数（`_persist_to_receiver`，字段/变量均覆盖）。名单：追加/清空/设置/插入/删除/列表弹出 及别名。
4. **`_recv_is_field` 扩展**：放行无点局部变量接收者——此前方法调用 `栈.追加(元素)` 对局部变量接收者不写回（`_persist_to_receiver` 情况一明明支持变量但被门槛挡掉），跨调用累积丢失。

## 运行语义验证（真跑 exe，非仅编译）

| 用例 | 输出 | 结果 |
|---|---|---|
| 栈 `s.压入(10)/s.压入(20)/s.弹出()/s.大小()` | `20 1 10 0` | PASS |
| 队列 `q.入队/q.出队`（`列表弹出(己.数据, 0)` 带索引） | `1 1 2 0` | PASS |
| 双端队列 `d.左入队/d.右入队/d.左出队/d.右出队` | `7 8` | PASS |
| 优先队列（`列表弹出(己.数据, 最小下标)` 变量索引 + 嵌套索引） | `甲 乙 丙` | PASS |

## 架构缺口（重要发现，需单独排期）

**原生腿段参数传值**，列表作为参数跨段变异传递不生效：
- 转译腿 = Python 引用（`入栈(s, 10)` 后 s 更新）；原生腿 = LightValue 值拷贝 + 共享底层数组，`dv_list_append`/`remove` 的 realloc 移动后调用方副本悬垂。
- 受影响：数据结构轻量的**辅助函数版本**（`创建栈/入栈/出栈/栈大小` 以列表为参数）——编译通过但运行受限（真跑抛「栈为空」）。
- **不受影响**：类版本（`己.数据` 字段 + self 对象共享，写回有效）——故 4 个类全部正确。
- 跨段引用语义是原生腿架构级缺口，影响所有「列表参数跨段变异」代码；需专项评估（REF 句柄 / 引用参数调用约定），非本批范围。

## 验证

- 能力清单同步：builtin 293 / runtime 217（`列表弹出`/`list_pop`/`列表插入` + `dv_list_pop`；`弹出`/`pop` 因劫持类方法刻意未注册，已从 JSON 清除）。
- codegen 定向回归 143 全绿：stmt_coverage 13 + capability 24 + c3_expr 56 + stdlib_phase2/13。
- runtime C 语法检查无 error。

---

# 追加：第三批（R10-10，2026-09-05）—— KeywordArg 展开 + 文件句柄链

## 成果：13 → 16（17.98%），解锁 JSON / JSON核心 / 内置核心列表（编译 + 运行语义）

**落地修复（codegen/compiler/runtime 三侧）**：

1. **KeywordArg 展开**：`ast_nodes` 新增 `KeywordArg`（ID=102）；`compiler._convert_keyword_arg`；codegen 函数/方法调用参数展开分离位置参数与关键字参数，`_merge_kwargs` 按 `_BUILTIN_KWARGS` 签名表（打开文件/排序 的 encoding/reverse）或光明段 `parameters` 参数名映射回位置——stdlib `打开文件(路径, "w", encoding=...)`、`表.sort(reverse=...)` 用。
2. **文件句柄链**：runtime 新增 `LV_TYPE_FILE`（type=22）：`dv_open_file`/`dv_file_write`/`dv_file_close`（fclose 后置 NULL 防双关）；codegen builtin `打开文件`/`write`/`close` 分支接线。
3. **try/catch IR 非法修复**：`catch` 体与 `finally` 体若已以 terminator 结束不再多发 `br`。
4. **dict 遍历 0 次根因**：`遍历 项 之 对象` 改用 `dv_len`（dict/list/str 统一取长）+ 新 runtime `dv_foreach_get`（开头 `v = dv_deref(v)` 处理 dict 内数组 REF）。
5. **段优先于 builtin**：`_gen_typed_function_call` 分派顺序调整为「本地段 > builtin > 导入段」，对齐转译腿 Python 语义（修复 JSON核心 `段落 连接` 被 `连接`(dv_concat) 劫持）。
6. **type 名对齐**：`dv_get_type_name` 返回英文名（NoneType/int/float/str/list/bool/dict），REF（type=8）解引用一层返回底层类型名。
7. **段默认参数丢失**：`compiler._convert_paragraph/_convert_parameter` 补 `default_value`（新增 `_convert_default_value` helper）；codegen 缺参分支应用默认值（stdlib `缩进 = 0`/`美化 = 假` 等）。
8. **dict 索引赋值**：`_gen_typed_assignment` 新增 IndexAccess 分支（`dv_dict_set` 原地设置 + 写回目标变量防 realloc 悬垂）；**并修复 `_convert_indexed_assignment`**——v3 的 `设 映射[键] 为 值` 在段落内 target 是已解析的 IndexAccess 节点，原实现 `Identifier(name=node.target)` 把节点塞进 name 导致 `'IndexAccess' object has no attribute 'encode'`（内置核心字典回归根因），现对非字符串 target 直接 `self.convert`。
9. **方法内调方法崩溃**：runtime 所有 `dv_call_method`/`dv_call_class_method`/`dv_call_static_method`/接口调用在 `method(result,...)` 前加 `dv_null(result)` 初始化结果槽。

## JSON 数组内 null：三处 REF 根因（本批关键调试成果）

| # | 根因 | 现象 | 修复 |
|---|---|---|---|
| ① | `dv_foreach_get` 不 deref REF | dict 内数组经 `dv_dict_get` 取值是 REF，`遍历 项 之 对象` 元素全 null（`{"b":[1,2,3]}` → `[null,null,null]`） | `dv_foreach_get` 开头 `v = dv_deref(v)` |
| ② | 索引 REF 字符串 type 判断误走 list | dict 内字符串经索引取值是 REF，codegen load type 后 `icmp eq 3`(str) 失败误走 list 分支取到垃圾 `\u0000`（`{"c":"x"}` → `\u0000`） | runtime 新增 `dv_deref_value`（解引用并复制实际值），codegen 索引访问 obj_slot 后先 deref |
| ③ | `dv_eq` 不 deref REF | dict 内 bool/null 与 `空` 比较失败（`序列化({"t":真,...})` 崩） | `dv_eq` 开头加 `dv_deref` 解引用 |

## 运行语义验证（真跑 exe，非仅编译）

JSON round-trip **12/12 全过**：

| 用例 | 输出 | 结果 |
|---|---|---|
| 顶层数组 `[1,2,3]` | `[1, 2, 3]` | PASS |
| 顶层对象 `{"a":1}` | `{"a": 1}` | PASS |
| 对象内数组 `{"b":[1,2,3]}` | `{"b": [1, 2, 3]}` | PASS |
| 嵌套 `{"a":1,"b":[1,2,3],"c":"x"}` | 一致 | PASS |
| 深层嵌套 `{"x":{"y":[{"z":42}]}}` | 一致 | PASS |
| 空对象/空数组 | `{}` / `[]` | PASS |
| 字符串转义 | `"a\nb"` | PASS |
| 中文 | `{"名": "光明"}` | PASS |
| **布尔/null** `{"t":true,"f":false,"n":null}` | 一致 | PASS |
| 浮点 `{"p":3.14}` | 一致 | PASS |
| 多数组 | 一致 | PASS |

## 本批修复的回归

- **内置核心字典**（R10-8 解锁模块）：本批新增 dict 索引赋值分支后暴露适配层 bug——`_convert_indexed_assignment` 对 v3 段落内的 IndexAccess target 处理错误，已修并回归通过。

## 验证

- 13 模块编译核对：**OK 10**（JSON / JSON核心 / 内置核心列表 本批新解锁 + 内置核心判型/转换/字典、列表工具、操作系统、数据结构轻量、路径运算）；FAIL 3 = 内置核心路径（TupleLiteral）、文件流（YieldStmt）、文件系统（目录名）——下一批。
- 能力清单全量重建：builtin 299 / runtime 222（evidence 行号全量重算）；`tests/unit/test_native_leg_capability.py` 11 passed。
- 定向回归：`tests/test_native_import.py` 5 passed + `tests/test_native_cli.py` 35 passed。
- runtime C 语法检查无 error。

## 文件清单

- 修改：`src/ast_nodes.py`（+KeywordArg ID 102）、`src/compiler.py`（+_convert_keyword_arg、+_convert_default_value、params default_value、修复 _convert_indexed_assignment）、`src/llvm/codegen_typed.py`（KeywordArg 展开+_merge_kwargs+文件 builtin+try terminator+段优先+foreach+dict 索引赋值+缺参默认值+dv_deref_value declare+索引 deref）、`src/llvm/runtime_typed.c`（+dv_open_file/dv_file_write/dv_file_close、+dv_foreach_get 带 deref、+dv_deref_value、dv_get_type_name 英文名、dv_call_* 入口 dv_null、dv_eq deref）、`docs/原生腿能力清单.json`（全量重建）、`docs/known_issues.md`（R10-10 回写）
- 交付：本报告追加 R10-10 章节
- 临时调试脚本 `_taskT9d_*` 已清理。

---


## R10-11a：内置核心路径元组（2026-09-05）

### 任务

基线 main @ be28a5a7（16/89）。`stdlib/内置核心路径.light` 的 6 个导出段中，`目录名`/`文件名`/`连接路径`/`扩展名` 已可编译，唯一阻断是 `分割路径`/`分割扩展名` 的 `返回 (头, 尾)` 元组字面量（TupleLiteral）与 `扩展名` 的 `二元[1]` 元组索引。

### 落地修复

| 文件 | 改动 |
|---|---|
| `src/ast_nodes.py` | +`AST_TYPE_ID_TUPLE_LITERAL = 103`；+`TupleLiteral` dataclass（镜像 `ListLiteral`） |
| `src/compiler.py` | `_node_converters` 注册 `'TupleLiteral'`；+_convert_tuple_literal |
| `src/llvm/codegen_typed.py` | +4 declare（dv_tuple_new/append/get/len）；_gen_expression +TupleLiteral 分派 → _gen_typed_tuple_literal；_gen_typed_index_access +type==23 分支 → dv_tuple_get |
| `src/llvm/runtime_typed.c` | +#define LV_TYPE_TUPLE 23；dv_len +type==23 分支；文件末尾 +dv_tuple_new/dv_tuple_append/dv_tuple_get/dv_tuple_len（含 REF deref、容量扩容、元素克隆） |
| `docs/原生腿能力清单.json` | expression_nodes +TupleLiteral；runtime_symbols +dv_tuple_new/append/get/len；全量 evidence 行号重算（codegen/runtime 行偏移修正） |
| `docs/原生腿能力边界.md` | 表达式节点表 +TupleLiteral 行 |
| `tests/unit/test_native_leg_capability.py` | _DOC_EXPR_TYPES +TupleLiteral |
| `docs/known_issues.md` | R10-11a 条目回写 |

### 运行语义验证

- `内置核心路径.light` 编译通过（IR length 17）。
- `分割路径`/`分割扩展名` 与 `posixpath.split`/`posixpath.splitext` 逐字符等价（对拍样本见 `tests/unit/test_地板搬迁_路径_S2.py`）。

### 反跑验证

- 破坏 `_gen_expression` 的 TupleLiteral 分派（`if False and isinstance(...)`）→ 编译立即报 `暂不支持表达式「TupleLiteral」`。
- 恢复后编译通过。

### 验证

- 17/89 模块编译通过（本批新增 内置核心路径）。
- 能力清单同步：expression_nodes 21 / runtime_symbols 226。
- 定向回归 `test_地板搬迁_路径_S2.py` + `test_native_leg_capability.py` 共 478 passed。

### 文件隔离遵从

- 只动 ast_nodes.py、compiler.py、codegen_typed.py（元组表达式分支）、runtime_typed.c（元组段，末尾非 TLS）、docs/known_issues.md、docs/原生腿能力清单.json、docs/原生腿能力边界.md（连带）、tests/unit/test_native_leg_capability.py（连带）、本报告。
- 未动 size_optimizer.py（T3）、runtime_typed.c TLS 段（T7）、生成器/文件相关区域（T4b/T4c）。
- 临时文件 `_taskT4a_*` 提交后清理。

### R10-11a 增量修复：rfind / rstrip / ends_with / str_repeat / REF deref / 字符偏移（主控 agent 评审追加）

**触发**：主控 agent 评审指出 codegen 缺 `rfind` 方法分支、runtime 缺 `dv_str_rfind`，要求补齐后用原生腿对拍 posixpath.split/splitext 重验。实做中发现连带缺失 4 项，一并修复。

| 文件 | 改动 |
|---|---|
| `src/llvm/codegen_typed.py` | +`declare i64 @dv_str_rfind`、+`declare void @dv_str_rstrip`；`_gen_typed_builtin` +rfind/rstrip/ends_with 分支；`_gen_typed_binary_op` `*` 分支 +STRING×INT→`dv_str_repeat` 分派 |
| `src/llvm/runtime_typed.c` | +`dv_str_rfind`（含 REF deref + 字节→字符偏移转换）；+`dv_str_rstrip`（右侧剥字符集/空白） |
| `docs/原生腿能力清单.json` | builtin 299→310、runtime 226→228（+dv_str_rfind/dv_str_rstrip +11 builtin 别名）；全量 evidence 行号重算 |
| `docs/known_issues.md` | R10-11a 条目追加增量修复明细 |

**修复明细**：

1. **rfind builtin 分支**：`_gen_typed_builtin` 无 `rfind`/`右查找`/`str_rfind` 分支 → 走 `dv_call_method` 静默返回 null。新增分支生成 `call i64 @dv_str_rfind`。
2. **rstrip builtin 分支**：`头.rstrip("/")` 同样走 `dv_call_method`。新增 `rstrip`/`右去除`/`str_rstrip` 分支 → `call void @dv_str_rstrip`（无 chars 参数时传 null 默认剥空白）。
3. **ends_with builtin 分派**：`结果.结尾("/")` 走 `dv_call_method`；codegen 已有 `dv_str_ends_with` declare 和 runtime 实现，但 `_gen_typed_builtin` 无 `结尾` 分支。新增 `结尾`/`endswith`/`ends_with`/`后缀是`/`以结尾` 分支。
4. **字符串乘法 `"/" * n`**：`*` 运算符对 STRING×INT 走 `dv_mul`（数值乘法），不处理字符串重复。在 `_gen_typed_binary_op` 的 arith_ops 数值路径之后、cmp_ops 之前新增 STRING×INT / INT×STRING 分派 → `dv_str_repeat`。
5. **dv_str_rfind REF deref**：`dv_str_rfind` 缺 `str = dv_deref(str); sub = dv_deref(sub);`——REF 传入时 `type != 3` 直接返回 -1（对齐 R10-10 的 dict/字符串 REF 教训）。
6. **dv_str_rfind 字节→字符偏移**：rfind 用 C `strncmp` 按字节搜索返回字节偏移，但 `dv_substr` 按字符位置切片（`dv_utf8_char_count`/`dv_utf8_char_offset`）——中文路径（如 `中文/目录/文件.txt`）字节偏移 ≠ 字符偏移导致索引错位。用 `dv_utf8_seq_len` 逐字符累计转换为字符偏移。空子串返回 `dv_utf8_char_count(s)`（对齐 Python 字符语义）。

**对拍验证**：

32 样本（含空串、纯斜杠、前导点、`.a/b.c/d`、`中文/目录/文件.txt`、`目录/中文名.文本` 等中文路径）与 `posixpath.split`/`posixpath.splitext` 逐字符等价，`diff EXIT=0`。

**验证**：

- 478 定向测试全绿（`test_地板搬迁_路径_S2.py` + `test_native_leg_capability.py`）。
- 能力清单同步：builtin 310 / runtime 228，全量 evidence 行号重算，`test_native_leg_capability.py` 11 passed。

---

# 追加：第四批B（R10-11b，2026-09-05）—— 生成器（YieldStmt），文件流 编译阻断解除

## 成果

原生腿补齐**生成器**：`stdlib/文件流.light` 里 `逐行` 的 `生成 ...` 不再被
`_gen_statement` 链尾以「暂不支持语句类型 YieldStmt」拒绝；整模块 `compile_source_typed`
真出 IR（46884 字符）。R10-8 起剩余 3 个 FAIL 模块中的「文件流（YieldStmt 生成器）」
编译阻断解除（内置核心路径的 TupleLiteral、文件系统的 目录名 不在本批范围）。

## 落地修复明细

### 1. `src/ast_nodes.py`
- 新增一等语句节点 `YieldStatement`（`AST_TYPE_ID_YIELD_STATEMENT = 103`）：
  `value: Optional[ASTNode]`（产出表达式）、`is_from: bool`（`生成 全部 X` 标记）。

### 2. `src/compiler.py`（AstAdapter）
- 注册 `_convert_yield_stmt`：v3 `YieldStmt` → v2 `YieldStatement`；`生成 全部 X`
  保留 `is_from=True`——codegen 对它显式报 NotImplementedError，**不静默降级成
  `生成 X`**（丢值比报错糟）。

### 3. `src/llvm/codegen_typed.py`
- **生成器段整体改造**（`_gen_generator_segment`）：含 `生成` 的段落编成两个函数——
  - 状态机函数 `_genf_<名>(ptr %gen)`：entry 块先取 state、按**预扫到的生成点数**
    一次造齐 `switch` 恢复落点（`i32 k → label %gen_resume_k`）；所有局部变量/临时
    槽改放堆上槽数组（`dv_gen_slot`，跨 `ret void` 存活）；`生成 表达式` →
    `dv_gen_yield` + `ret void` + 紧跟恢复标签（编号与预扫一一对应，乱序/漏点直接
    编译期自相矛盾）。
  - 包装段 `_seg_<名>`：`dv_gen_create(func, 槽位数)` + 实参/默认值按序抄进前 n 个
    槽 + `dv_gen_make` 把生成器句柄包成 LightValue（type=24）写进 `%result`。
- **`遍历` 消费双路径**：迭代对象先 `dv_is_generator` 判别——生成器走
  `dv_gen_resume` + `dv_gen_value` 逐条拉取，列表/字符串/字典走既有
  `dv_len` + `dv_foreach_get`；两条取值路径写**同一个**循环变量槽、br 到**同一份**
  循环体，`跳出`/`跳过` 落点唯一，列表路径的取值方式一字未改（回归零风险）。
- 生成器内 `返回` 语义 = 结束生成（`dv_gen_finish` + `ret void`），不写 `%result`。
- `_reject_unsupported_stmt`/`_gen_statement` 的 YieldStmt 文案路径保留给
  「生成器外裸用/`生成 全部`」两种残余场景，删改链尾兜底。

### 4. `src/llvm/runtime_typed.c`
- 新增生成器轻量状态机（10 符号，插在 TLS 段之前的非 TLS 区域）：
  `dv_gen_create` / `dv_gen_free` / `dv_gen_slot` / `dv_gen_state` /
  `dv_gen_yield` / `dv_gen_finish` / `dv_gen_make` / `dv_gen_resume` /
  `dv_gen_value` / `dv_is_generator`。
- 设计取舍：不沿用 `dv_coro_*`（推送式调度器，消费者拿不到中间值）——生成器是
  拉取式，两套状态机语义正交，故独立实现。本批不回收生成器结构体，登记为已知债务。

## 两处 IR 正确性修复（本批关键调试成果）

1. **entry 回插指令必须用符号名寄存器**（`%gs.N` / `%ga.N`，`_gen_entry_symbolic`）：
   槽位指针与循环计数器 alloca 在正文发射**过程中**按需生成、再回插到 entry 块
   switch 之前。数字寄存器按「生成时刻」编号 → 文本序与编号序错位，LLVM 文本解析
   报 `instruction expected to be numbered '%N' or greater`。符号名不参与自动编号，
   可被任意块引用，正文数字寄存器仍单调递增。
2. **`遍历` 的列表路径 `len` 改走内存槽**（entry 预分配 `len_slot`，`list_init`
   store、`list_loop` load）：`list_loop` 有回边来自共用的循环步进块
   `foreach_cont`（生成器路径在静态上也能绕到那里），`list_init` 不支配
   `list_loop`，SSA 值 `len_val` 过不了支配校验；load/store 不是 SSA 值，不适用
   支配规则，合法。

## 运行语义验证（真跑 exe，zig cc 工具链；本机无 MSVC 头文件，clang 链不齐）

临时编排脚本 `_taskT4b_run.py`：`.light → IR → zig cc 编 runtime+IR → 链 exe → 跑`。

- **状态机用例**（while 内 yield / if-else 分支挂起恢复 / `跳过` 后恢复 /
  `返回` 提前结束 / 末尾 `返回 999`）输出全对：
  - `数到(4)` → `0 1 2 3`（计数 4）
  - `奇偶(6)`（i==3 跳过 + 末尾返回 999）→ `0 1 2 4 5`（计数 5）
  - `提前结束(9)`（i==2 返回）→ `0 1`（计数 2）
  - 列表 `列(10,20,30)` 遍历回归 `10 20 30` ✓（消费路径改动未伤列表）
- **`逐行` 同构真跑逐字节 MATCH**：与 `stdlib/文件流.light::逐行` 逐字同构的程序
  读三个数据文件（含行尾换行保留 / 中间空行 / 末行无换行不补 / 空文件），stdout
  与 Python `readlines` 逐字节相等，遍历次数与行数一致（4 / 3 / 0）。
- `stdlib/文件流.light` 整模块原生腿编译 OK（IR 46884 字符）。

## 验证

- 定向回归 **31 全绿**：`tests/test_file_stream_light.py` 7 + 
  `tests/unit/test_llvm_stmt_coverage.py` 13（`生成` 由正跑 6 挪入反跑 6）+
  `tests/unit/test_native_leg_capability.py` 11。
- 能力清单全量重建：statement_nodes **16→17**（+`YieldStatement`）、runtime_symbols
  **222→232**（+10 生成器符号）、builtin 299（本批未增）。evidence 行号全量重算，
  能力单点测试 11 passed。
- 能力边界文档 / §9 未支持语句清单同步（上游无转换器 10→9、下游分支 16→17、
  实测分桶 17/5/2）。
- **重生成脚本顺带修一个静默丢数据的 bug**：`if name in (...)` 名单跨多行时逐行
  正则整条配不上，重生成会静默丢掉那一整批内置名（实测丢 22 个异常类内置名）；
  改为整段扫描 + 按名字定位真实行号（能力测试要求 evidence 行文本含该名字）。

## 文件清单

- 修改：`src/ast_nodes.py`（+`YieldStatement` ID 103）、`src/compiler.py`
  （+`_convert_yield_stmt`）、`src/llvm/codegen_typed.py`（生成器状态机段 +
  foreach 双路径 + 符号名回插 + len 内存槽 + `_gen_typed_yield`）、
  `src/llvm/runtime_typed.c`（+10 生成器符号，TLS 段前非 TLS 区域）、
  `docs/原生腿能力清单.json`（全量重建）、`docs/原生腿能力边界.md`（§1.1 加
  YieldStatement、§1.3 移除 YieldStmt 报错行）、`docs/known_issues.md`（§9 同步 +
  R10-11b 回写）、`scripts/gen_native_capability_json.py`（跨行名单扫描修复 +
  YieldStatement 描述）、`tests/unit/test_llvm_stmt_coverage.py`（`生成` 挪正跑）、
  `tests/unit/test_native_leg_capability.py`（_DOC_STMT_TYPES +YieldStatement）、
  `tests/test_llvm_c3_expr.py`（拒绝清单移除 `生成语句`、+生成器真跑用例）
- 交付：本报告追加 R10-11b 章节
- 临时调试文件 `_taskT4b_*`（含 zig 工具链 383MB、IR、exe、数据）已清理。

## 合流前修订（2026-09-05 补）：类型号 23→24 避让撞车

合流核对发现 B 分支生成器 `LV_TYPE_GENERATOR 23` 与 A 分支元组
`LV_TYPE_TUPLE 23` **同号**。按合流决议，A 元组保持 23（已过其交付核验），
本批生成器改 **24**：

- `src/llvm/runtime_typed.c`：`#define LV_TYPE_GENERATOR 24`（23 号在注释标注
  保留给元组）；`dv_gen_make` 赋值与 `dv_is_generator` 比较均走宏，无字面量残留。
- `src/llvm/codegen_typed.py` / `docs/known_issues.md` / 本报告：`type=23` 文案
  同步为 `type=24`（仅注释，无行数变化，能力清单 JSON evidence 行号零漂移，
  重生成 diff 为空）。
- 复验：定向 31 全绿；zig cc 真跑 exe —— 生成器状态机冒烟（`数到(4)`→0 1 2 3、
  列表回归、`返回` 提前结束→0 1）全过，`逐行` 同构程序读三份数据文件仍与
  Python `readlines` **逐字节相等**（4/3/0）；`stdlib/文件流.light` 编译 OK。

---


# R10-11c：原生腿覆盖第四批C —— 文件系统句柄（2026-09-05）

## 目标与基线

- 基线：main @ be28a5a7（16/89）。
- 修复 stdlib/文件系统.light 三个编译/运行阻断点：① `目录名(路径)` 未定义段落；② 文件句柄缺 `f.read()` 方法（R10-10 已支持 打开文件/write/close）；③ `_light_builtin.XXX` 点访问未按名转发（此前把模块名当接收者，参数错位）。
- 结果：**16/89 → 18/89（20.22%）**。文件系统 编译 + 写入→读取→追加→删除 全链真跑正确；文件流 因 YieldStmt 编译放行可出 exe（其 `逐行` 生成器语义为占位，真实生成器归 T4a/T4b）。

## 落地修复

### 1. 目录名（dirname）——选 runtime 内置方案

- runtime 新增 `dv_path_dirname`：POSIX dirname 语义（`os.path.dirname`，posixpath.split 的 head），覆盖 `"a/b/c"→"a/b"`、`"/"→"/"`、`"a"→""`、`"a/b/"→"a/b"`、`""→""`。
- codegen builtin 注册 `目录名`/`dirname`/`path_dirname`。
- **选择理由（vs 定义 段落 目录名）**：
  1. 转译腿本就映射 `目录名` → `_light_builtin.目录名` = `os.path.dirname`，原生腿补 runtime 内置与转译语义完全对齐；
  2. `内置核心路径` 的 `段落 目录名` 逐字实现依赖 `.rfind`/字符串切片/字符串乘法 `"/" * 长(头)`/`.rstrip` 等原生腿尚未支持的方法，逐字同构会级联新阻断点；
  3. 与 R10-8 `连接路径`→`dv_path_join` 的路径函数模式一致（runtime C 函数，非 Light 段落）。

### 2. 文件句柄 read

- runtime 新增 `dv_file_read`：对齐 `dv_file_write` 的 LightValue* 输出模式——从当前文件位置读到 EOF，`str` 字段持 malloc 内容（`dv_free` type=3 释放），失败（无句柄/非文件/已关闭）置 null；句柄生命周期沿用 `dv_file_close` 关闭后置 NULL 防双关。
- codegen 文件句柄方法 `read`/`读取` 接线（`_call_dv_func('dv_file_read', args[0])`），与 write/close 同链。

### 3. `_light_builtin.XXX` 模块点访问转发

- `_gen_typed_method_call`：当 `prop.obj` 为 `Identifier('_light_builtin')` 时，不再把模块名当接收者求值入参，改为按名转发 `_gen_typed_builtin(method_name, args_dv)`（含 kwargs 合并），对齐转译腿 `_light_builtin.删除文件`→`删除文件` 语义。
- 覆盖：删除文件/文件大小/创建目录/目录存在/删除目录/列出目录（遍历目录/文件列表）/绝对路径/时间戳。

### 4. 路径/目录系统调用边界内置补齐

- runtime 新增：`dv_path_basename`（`文件名`/`basename`/`path_basename`）、`dv_abspath`（`绝对路径`/`abspath`/`abs_path`，相对路径基于 cwd 拼接 + normpath 规范化，含 Windows 盘符前缀处理）、`dv_makedirs`（`创建目录`/`mkdir`/`make_dir`/`makedirs` = `os.makedirs(path, exist_ok=True)` 逐级建父目录）。
- `删除目录`/`rmdir`/`remove_dir` 接线既有 `dv_rmdir`。
- `文件系统.写入文件` 的「自动建父目录」由此链实现：`目录名(路径)` → `创建目录(目录)` → `打开文件` → `f.write` → `f.close`。

### 5. YieldStmt 编译放行（必要使能，非生成器实现）

- 文件系统 依赖 文件流（`从 文件流 导入 行列表`），多模块编译会连同 文件流 的 `逐行`（`生成`/YieldStmt）编译。原生腿尚无生成器实现（归 T4a/T4b），codegen 对 `<unknown:YieldStmt>/<unknown:YieldFromStmt>` 按 no-op 跳过。
- 效果：文件系统 全链可编译可运行；文件流 可出 exe，但 `逐行` 生成器语义为占位（返回空）。真实生成器语义由 T4a/T4b 实现后补齐。

### 6. 打开文件 失败抛异常（对齐转译腿 Python open，主控意见）

- 主控 agent 意见：`dv_open_file` fopen 失败时应抛异常（对齐转译腿 Python `open` 的 FileNotFoundError），据此重验 `文件存在`。
- 落地：`dv_open_file` 在 `fopen(path, mode)` 返回 NULL 时，经 `dv_create_exception_with_cause` + `dv_throw_exception` 创建并抛出 `IOException`（消息 `无法打开文件: <path>`）；两个函数的**前向声明**置于 文件句柄 段（`#define LV_TYPE_FILE` 之后），实际定义在文件后部异常机制区（新增代码全部位于非 TLS 区域，未动 TLS/元组/生成器区域）。
- 效果：
  - `文件系统.文件存在` 的 `尝试/捕获 异常` 能接住打开失败并返回 **假**（此前对不存在文件返回 真）；
  - `读取文件` 等无 try 的调用打开不存在文件时，按「未捕获的异常: IOException: 无法打开文件: ...」打印并退出（对齐 Python 未捕获 FileNotFoundError，真跑验证 exit=1 且后续语句不执行）；
  - `写入文件`/`追加文件`/`JSON` dump 等所有 `打开文件` 消费方语义对齐转译腿。
- 影响面核对：stdlib 中 `打开文件` 调用点（JSON.light / JSON核心.light / 文件系统.light 的 写入文件/读取文件/追加文件/文件存在/创建临时文件）均对齐 Python `open` 失败即抛，无依赖 null 返回的调用点。

## 运行语义验证（真跑 exe）

文件系统 全链冒烟（`_taskT4c_smoke.light`，临时文件已清理）：

| 步骤 | 输出 | 结果 |
|---|---|---|
| 写入文件(自动建父目录) 后 f.read() | `READ1=第一行\n第二行` | PASS |
| 追加文件 后 f.read() | `READ2=第一行\n第二行第三行` | PASS |
| 文件大小（_light_builtin 转发） | `SIZE=29`（Windows 文本模式 `\n→\r\n`，与 Python 语义一致） | PASS |
| 目录存在（_light_builtin 转发） | `DIR_EXIST=真` | PASS |
| 文件列表（_light_builtin.列出目录） | `LIST=1` `LIST0=文件.txt` | PASS |
| 绝对路径（_light_builtin 转发） | `ABS=G:/dswork/.../文件.txt`（盘符后分隔符正确） | PASS |
| 获取文件名（文件名 builtin） | `BASE=文件.txt` | PASS |
| 删除文件（_light_builtin 转发）后 文件存在 | `删后存在=假`（打开失败抛异常 → 尝试/捕获 接住，本次修复核心） | PASS |
| 删除文件 后文件大小 | `SIZE_AFTER_DEL=0`（双指标确认删除真实生效） | PASS |
| 删除目录（_light_builtin 转发） | `DIR_EXIST_AFTER=假` | PASS |

`目录名`/`获取文件名` 边界直测（`_taskT4c_dirname.light`，临时文件已清理）：`D1=a/b` `D2=/` `D3=`(空) `D4=a/b` `D5=`(空) `B1=c` `B2=`(空) `B3=`(空)——与 `os.path.dirname/basename` 一致。

## 反跑判据验证

- 移除 `目录名` 接线 → 文件系统 编译立即立红（`未定义的段落：目录名`）。
- 移除 `read`/`读取` 接线 → 文件系统 编译通过但运行链路破坏（`f.read()` 走 `dv_call_method` 兜底，读取为空 `READ1=空`），即「编译/运行」二选一立红满足。

## 已解决（原记录限制）

- 此前记录「`文件系统.文件存在` 对不存在文件仍返回 真」已通过第 6 节「打开文件 失败抛异常」修复：`文件存在` 现对已删除文件返回 **假**，删除验证可用 `文件存在`=假 或 `文件大小`=0 双指标确认。

## 验证

- 定向回归：`tests/test_native_import.py` 5 passed + `tests/unit/test_native_leg_capability.py` 11 passed（16 passed）。
- 能力清单同步：builtin 299→317 / runtime 222→227（evidence 行号重算，符号集合增量 = 本次新增 18 内置名 + 5 运行时符号，无删减）。
- runtime C 语法检查（clang -fsyntax-only）无 error。
- 13 模块编译核对（定向，非全量）：文件系统 OK、文件流 OK（生成器占位）；剩余 FAIL = 内置核心路径（TupleLiteral）。
- 文件存在 重验（主控意见）：删除文件后 `文件存在`=假；读取不存在文件 → 未捕获 `IOException` 退出（exit=1）；全链回归 16 passed。

## 文件清单

- 修改：`src/llvm/codegen_typed.py`（+`dv_path_dirname`/`dv_path_basename`/`dv_abspath`/`dv_file_read`/`dv_makedirs` declare；builtin `read`/`读取`、`目录名`/`dirname`/`path_dirname`、`文件名`/`basename`/`path_basename`、`绝对路径`/`abspath`/`abs_path`、`创建目录`/`mkdir`/`make_dir`/`makedirs`、`删除目录`/`rmdir`/`remove_dir`；`_light_builtin` 模块点访问转发；YieldStmt 编译放行）、`src/llvm/runtime_typed.c`（+`dv_file_read` 于文件句柄段；+`dv_path_dirname`/`dv_path_basename`/`dv_abspath`/`dv_normpath`/`dv_mkdir_one`/`dv_makedirs` 于非 TLS 区域末尾；修复 `dv_normpath` Windows 盘符绝对路径分隔符；`dv_open_file` 失败抛 `IOException` + 异常机制前向声明于文件句柄段）、`docs/原生腿能力清单.json`（增量 + evidence 行号重算，本次抛异常改动后 runtime evidence 再重算 128 条）、`docs/known_issues.md`（R10-11c 回写，含打开失败抛异常记录）
- 交付：本报告追加 R10-11c 章节
- 临时调试脚本 `_taskT4c_*` 已清理。

---

## R10-11a 打回 A2：O0 多段组合段错误修复

**日期**：2026-09-05  
**分支**：`task-T4aR2`（worktree `wt-T4aR2`，基于 main `2c5e7ed7`）  
**前置**：R10-11a 打回修复 f598a7b2（分支 `task-T4aR`）已查收，模块级导入分派生效但 O0 仍段错误。

### 问题

O0（`optimize_level=0`）下选择导入多段组合段错误（rc=3221225477 = 0xC0000005 ACCESS_VIOLATION），O2 掩盖不崩。

精确触发条件（主控二分定位）：主段做 tuple 索引（`t[0]`/`t[1]`）后跟 ≥2 次函数调用时崩溃。单段/2 段/6 段全量不崩，3 段含「扩展名+目录名+第三段」崩。

### 根因

`_gen_typed_segment_call`（L3118）和 `_gen_imported_segment_call`（L1362）分配 `result_slot = self._new_dv_slot()` 后**未 `dv_null` 初始化**。

- `_new_dv_slot`（L589）从复用型临时槽位池分配 `alloca` 数组，**不零初始化**（L594 注释明确）。
- 段函数返回路径 `_emit_return_with_self_writeback`（L4211）调 `dv_obj_release_slot(result_ptr)` 释放旧值。
- O0 下槽位残留垃圾被误判为 REF 指针 → `memcpy` 非法地址 → 段错误。
- O1+ 优化消除未初始化读取 → 不崩。
- 与 `_call_dv_func`（L690-695）已有的同模式修复（P0-2 内存安全修复）一致。

### 修复

`src/llvm/codegen_typed.py`（+19 行，3 处）：

1. **`_gen_typed_segment_call`**（L3118 后）：`result_slot` 分配后补 `call void @dv_null(ptr {result_slot})`。
2. **`_gen_imported_segment_call`**（L1362 后）：同理补 `dv_null`。
3. **`_gen_typed_method_call`**（L2710 后）：补回模块级导入分派（`obj_name in self._imported_modules and method_name in self._segments` → `_gen_typed_segment_call`），来自 f598a7b2 未合入 main 的修复，一并补回防止模块级导入回归。

### 反跑验证

| 用例 | O0 修复前 | O0 修复后 | O2 |
|------|----------|----------|-----|
| 4 段（分割路径+分割扩展名+扩展名+目录名） | rc=3221225477 | rc=0 输出正确 | rc=0 |
| 3 段 A（分割路径+扩展名+目录名） | rc=3221225477 | rc=0 | — |
| 3 段 B（分割扩展名+扩展名+目录名） | rc=3221225477 | rc=0 | — |
| 单段/2 段/6 段全量 | rc=0 | rc=0 不回归 | — |
| 模块级导入（导入→模块名.段名） | rc=0 | rc=0 不回归 | rc=0 |

### 定向测试

`python -m pytest tests/unit/test_地板搬迁_路径_S2.py tests/unit/test_native_leg_capability.py -q`  
**482 passed**（原 480 + 新增 4 O0 反跑用例）。

新增 O0 反跑用例（`test_地板搬迁_路径_S2.py` §7）：
- `test_O0_四段选择导入不崩且输出正确`：4 段全调用，锁定 `optimize_level=0`，验证 rc=0 且输出 `a|b.c`/`x.tar|.gz`/`.gz`/`/a//b`。
- `test_O0_三段A_分割路径_扩展名_目录名不崩`：3 段组合 A，验证 rc=0。
- `test_O0_三段B_分割扩展名_扩展名_目录名不崩`：3 段组合 B，验证 rc=0。
- `test_O0_模块级导入不回归`：模块级导入 O0，验证 rc=0 且输出正确。

### 能力清单

`docs/原生腿能力清单.json`：328 条 builtin evidence 行号全量重算（+3/+14/+19 偏移，按 3 处插入点分段）。

### 文件清单

- 修改：`src/llvm/codegen_typed.py`（+19 行：3 处 dv_null + 模块级导入分派）、`tests/unit/test_地板搬迁_路径_S2.py`（+4 O0 反跑用例）、`docs/原生腿能力清单.json`（evidence 行号重算）、`docs/known_issues.md`（R10-11a A2 回写）
- 交付：本报告追加 R10-11a A2 章节
- 临时调试脚本 `_taskT4aR2_*` 已清理。

---

## T5A：数学/统计/排序真 .light 化

### 任务概述

将 `stdlib/数学.light`、`stdlib/统计.light`、`stdlib/排序.light` 三个「decl 0
空壳」补为纯光明真实现，解锁原生腿（LLVM/C 后端）编译。实现参照
`stdlib/文件流.light` 模式：纯光明、零 import、只用光明内置原语。

### 解锁模块

| 模块 | .light 行数 | 导出数 | 状态 |
|------|------------|--------|------|
| 数学.light | 225 | 21（+1 指数，-3 随机归 T5C） | ✅ 真实现 |
| 统计.light | 382 | 20 | ✅ 真实现 |
| 排序.light | 171 | 9 | ✅ 真实现 |

### 数学.light 导出

绝对值 / 最大值 / 最小值 / 幂 / 平方根 / 立方根 / 正弦 / 余弦 / 正切 /
弧度转角度 / 角度转弧度 / 向上取整 / 向下取整 / 四舍五入 / 圆周率 /
自然常数 / 阶乘 / 对数 / 自然对数 / 常用对数 / 对数2 / 指数

> 随机 3 导出（随机整数 / 随机浮点 / 随机选择）归 T5C 随机模块负责，
> 本模块仅标注能力边界，不实现。

### 统计.light 导出（20 全译）

均值 / 中位数 / 众数 / 多众数 / 标准差 / 样本标准差 / 方差 / 样本方差 /
数据最小值 / 数据最大值 / 极差 / 求和 / 四分位数 / 百分位数 / 协方差 /
相关系数 / 线性回归 / 累积和 / 频率分布 / 标准化

> `最小值`/`最大值` 重命名为 `数据最小值`/`数据最大值`，原因见下文「能力边界」。

### 排序.light 导出

快速排序 / 归并排序 / 冒泡排序 / 插入排序 / 选择排序 / 堆排序 /
排序（统一入口，默认快速排序）/ 排序稳定（归并排序）/ 排序检查

稳定排序算法（归并排序），不依赖 random。

### 新增注册

`src/llvm/codegen_typed.py` L1815-1819：注册既有 runtime `dv_round` 为
「四舍五入」/「round」builtin（仅注册，不新增 C 函数）。

```python
if name in ('四舍五入', 'round'):
    if args:
        return self._call_dv_func('dv_round', args[0]), 'dv'
    return self._create_int_dv('0'), 'dv'
```

### 能力边界与 workaround

O0 下发现 6 个 codegen 缺陷（详见 `docs/known_issues.md` R10-12a 章节），
均以纯光明 workaround 绕过，不修改 codegen 核心：

1. **跨段 float 返回值零初始化**：所有统计函数自包含内联计算，不依赖其他段
   float 返回值。
2. **int/int 除法零初始化**：统一 `(总计 + 0.0) / n` 模式。
3. **builtin sqrt 返回值零初始化**：相关系数/标准化内联牛顿法 sqrt（20 次迭代）。
4. **float 参数零初始化**：百分位数接收 int 参数，内部转 float。
5. **跨模块同名段冲突**：统计.light 的 `最小值`/`最大值` → `数据最小值`/
   `数据最大值`（`_safe_func_name` 全局 counter 冲突，归 T3 修复）。
6. **自然对数 float 参数死循环**：添加 `值 <= 0.0` 安全防护。

### 对拍结果

测试文件：`tests/unit/test_T5a_数学统计排序_原生腿.py`（468 行，16 个测试）

| 分组 | 测试数 | 对拍基准 | 结果 |
|------|--------|----------|------|
| 数学 | 5 | Python `math` | ✅ |
| 统计 | 5 | Python `statistics` | ✅ |
| 排序 | 2 | Python `sorted()` | ✅ |
| 三模块联合 | 1 | Python `math`+`statistics` | ✅ |
| 反跑空壳→红 | 3 | `NativeImportError` | ✅ |
| **总计** | **16** | | **16 passed** |

反跑判据验证：
- 真实现 → 16 passed（绿）
- 恢复空壳 .light（去掉实现）→ 3 个反跑测试 passed（验证 `decl 0 空壳` 错误）
- O0 下不崩、输出与 Python 对拍一致（数值容差 1e-4）

### 验证方式

```bash
cd wt-T5a
python -m pytest tests/unit/test_T5a_数学统计排序_原生腿.py -v
# 16 passed in ~35s
```

### 文件清单

- 修改：`stdlib/数学.light`（225 行，空壳→真实现）
- 修改：`stdlib/统计.light`（382 行，空壳→真实现）
- 修改：`stdlib/排序.light`（171 行，空壳→真实现）
- 修改：`src/llvm/codegen_typed.py`（+5 行：dv_round 注册）
- 新增：`tests/unit/test_T5a_数学统计排序_原生腿.py`（468 行，16 测试）
- 回写：`docs/known_issues.md`（R10-12a T5A 章节，6 个 O0 codegen 缺陷）
- 临时调试产物 `_taskT5a_*` 已清理

### 能力清单

能力清单由最后合并者重建并跑 `tests/unit/test_native_leg_capability.py`。

---

## R10-11a 打回 A2：O0 多段组合段错误修复

**日期**：2026-09-05  
**分支**：`task-T4aR2`（worktree `wt-T4aR2`，基于 main `2c5e7ed7`）  
**前置**：R10-11a 打回修复 f598a7b2（分支 `task-T4aR`）已查收，模块级导入分派生效但 O0 仍段错误。

### 问题

O0（`optimize_level=0`）下选择导入多段组合段错误（rc=3221225477 = 0xC0000005 ACCESS_VIOLATION），O2 掩盖不崩。

精确触发条件（主控二分定位）：主段做 tuple 索引（`t[0]`/`t[1]`）后跟 ≥2 次函数调用时崩溃。单段/2 段/6 段全量不崩，3 段含「扩展名+目录名+第三段」崩。

### 根因

`_gen_typed_segment_call`（L3118）和 `_gen_imported_segment_call`（L1362）分配 `result_slot = self._new_dv_slot()` 后**未 `dv_null` 初始化**。

- `_new_dv_slot`（L589）从复用型临时槽位池分配 `alloca` 数组，**不零初始化**（L594 注释明确）。
- 段函数返回路径 `_emit_return_with_self_writeback`（L4211）调 `dv_obj_release_slot(result_ptr)` 释放旧值。
- O0 下槽位残留垃圾被误判为 REF 指针 → `memcpy` 非法地址 → 段错误。
- O1+ 优化消除未初始化读取 → 不崩。
- 与 `_call_dv_func`（L690-695）已有的同模式修复（P0-2 内存安全修复）一致。

### 修复

`src/llvm/codegen_typed.py`（+19 行，3 处）：

1. **`_gen_typed_segment_call`**（L3118 后）：`result_slot` 分配后补 `call void @dv_null(ptr {result_slot})`。
2. **`_gen_imported_segment_call`**（L1362 后）：同理补 `dv_null`。
3. **`_gen_typed_method_call`**（L2710 后）：补回模块级导入分派（`obj_name in self._imported_modules and method_name in self._segments` → `_gen_typed_segment_call`），来自 f598a7b2 未合入 main 的修复，一并补回防止模块级导入回归。

### 反跑验证

| 用例 | O0 修复前 | O0 修复后 | O2 |
|------|----------|----------|-----|
| 4 段（分割路径+分割扩展名+扩展名+目录名） | rc=3221225477 | rc=0 输出正确 | rc=0 |
| 3 段 A（分割路径+扩展名+目录名） | rc=3221225477 | rc=0 | — |
| 3 段 B（分割扩展名+扩展名+目录名） | rc=3221225477 | rc=0 | — |
| 单段/2 段/6 段全量 | rc=0 | rc=0 不回归 | — |
| 模块级导入（导入→模块名.段名） | rc=0 | rc=0 不回归 | rc=0 |

### 定向测试

`python -m pytest tests/unit/test_地板搬迁_路径_S2.py tests/unit/test_native_leg_capability.py -q`  
**482 passed**（原 480 + 新增 4 O0 反跑用例）。

新增 O0 反跑用例（`test_地板搬迁_路径_S2.py` §7）：
- `test_O0_四段选择导入不崩且输出正确`：4 段全调用，锁定 `optimize_level=0`，验证 rc=0 且输出 `a|b.c`/`x.tar|.gz`/`.gz`/`/a//b`。
- `test_O0_三段A_分割路径_扩展名_目录名不崩`：3 段组合 A，验证 rc=0。
- `test_O0_三段B_分割扩展名_扩展名_目录名不崩`：3 段组合 B，验证 rc=0。
- `test_O0_模块级导入不回归`：模块级导入 O0，验证 rc=0 且输出正确。

### 能力清单

`docs/原生腿能力清单.json`：328 条 builtin evidence 行号全量重算（+3/+14/+19 偏移，按 3 处插入点分段）。

### 文件清单

- 修改：`src/llvm/codegen_typed.py`（+19 行：3 处 dv_null + 模块级导入分派）、`tests/unit/test_地板搬迁_路径_S2.py`（+4 O0 反跑用例）、`docs/原生腿能力清单.json`（evidence 行号重算）、`docs/known_issues.md`（R10-11a A2 回写）
- 交付：本报告追加 R10-11a A2 章节
- 临时调试脚本 `_taskT4aR2_*` 已清理。


---

## T5B：Base64/编码/哈希/字符串常量真 .light 化（R10-12b）

### 任务概述

将 `stdlib/Base64.light`、`stdlib/编码.light`、`stdlib/哈希.light`、
`stdlib/字符串常量.light` 四个「decl 0 空壳」补为纯光明真实现，解锁原生腿
（LLVM/C 后端）编译与运行。实现参照 T5A 模式：纯光明、零 import；Base64 /
MD5 / SHA1 / SHA256 复用 runtime 既有函数，仅补 codegen 注册，**runtime_typed.c
一行未改**。

### 解锁模块

| 模块 | .light 行数 | 段定义 | 状态 |
|------|------------|--------|------|
| Base64.light | 320 | 18 | ✅ 真实现 |
| 编码.light | 138 | 16 | ✅ 真实现 |
| 哈希.light | 63 | 9 | ✅ 真实现 |
| 字符串常量.light | 312 | 36 | ✅ 真实现 |

### codegen 变更（src/llvm/codegen_typed.py，仅注册既有 runtime，未新增 C 函数）

- 新增注册：`Base64编码/解码`（dv_base64_encode/decode）、`MD5`（dv_md5）、
  `SHA1`（dv_sha1）、`SHA256`（dv_sha256）、`四舍五入/round`（dv_round）、
  `整除/div`（dv_div）。
- 修复 7 处既有接线缺陷：`dv_str_len/contains/starts_with/ends_with/rfind/count`
  入参误传 `char*`（应先 `_store_dv` 取 `LightValue*` 指针），导致返回空串/恒假/
  恒 0；`dv_round` 第二参误传 struct 值导致 IR 校验失败。

### 能力边界（详见 known_issues 十八节）

- `SHA512` / `HMAC_SHA256`：返回空串占位（无 dv_sha512 runtime；光明缺 64 位
  逻辑右移与按位运算符，无法可靠模拟）。
- 高阶函数：`字符串常量` 的「字符列表全部是/有一个是」第二参改为分类名字符串
  （"字母"/"数字"/…），与 .py 签名不兼容，已在模块头登记。
- 跨模块同名段（`编码.Base64编码` vs `Base64.Base64编码`）：每模块独立编译单元
  规避（`core.py:_safe_func_name` 全局 counter，归 T3 修复）。
- 字节口径：原生后端字符串索引按字符、Python base64/hashlib 按 UTF-8 字节，
  纯光明统一以「十六进制串」作字节中间表示，已验证中文逐字节一致。

### 对拍结果

测试文件：`tests/unit/test_T5B_编码哈希.py`（4 用例，全部 `optimize_level=0`，
各模块独立编译单元）

| 用例 | 对拍基准 | 结果 |
|------|----------|------|
| Base64 编解码 | Python `base64` | ✅ |
| 哈希（MD5/SHA1/SHA256） | Python `hashlib` | ✅ |
| 编码/哈希联合 | Python | ✅ |
| 字符串常量 | Python `string` | ✅ |

反跑判据验证：真实现 → 4 绿；恢复空壳 .light → 4 红（`decl 0 空壳`）。

### 文件清单

- 修改：`stdlib/Base64.light`（320 行）、`stdlib/编码.light`（138 行）、
  `stdlib/哈希.light`（63 行）、`stdlib/字符串常量.light`（312 行）
- 修改：`src/llvm/codegen_typed.py`（+127 行：注册 + 7 处接线修复）
- 新增：`tests/unit/test_T5B_编码哈希.py`（330 行，4 用例）
- 回写：`docs/known_issues.md`（十八、T5B 章节）
- 临时调试产物 `_taskT5b_*` 已清理

### 能力清单

能力清单由最后合并者重建并跑 `tests/unit/test_native_leg_capability.py`
（合并后 builtin 368 / runtime 246，11 passed）。

## T5C：集合/随机/对象池缓存真 .light 化（R10-12c）

### 任务概述

将 `stdlib/集合.light`、`stdlib/集合工具.light`、`stdlib/集合操作.light`、
`stdlib/随机.light`、`stdlib/对象池缓存.light` 五个「decl 0 空壳」补为纯光明
真实现，解锁原生腿（LLVM/C 后端）编译与运行。实现参照 T5A 模式：纯光明、
零 import、只用光明内置原语 + 本批新增的确定性随机数运行时。

### 解锁模块

| 模块 | .light 行数 | 导出数 | 状态 |
|------|------------|--------|------|
| 随机.light | 323 | 22 | ✅ 真实现 |
| 集合.light | 450 | 36 | ✅ 真实现 |
| 集合工具.light | 134 | 15 | ✅ 真实现 |
| 集合操作.light | 261 | 13 | ✅ 真实现 |
| 对象池缓存.light | 352 | 31 | ✅ 真实现 |

### 运行时与 codegen 变更

- **`src/llvm/runtime_typed.c`**（+154 行，**文件末尾独立函数区，未触碰 TLS 段**）：
  新增确定性随机数运行时 —— MT19937 全套（init_genrand / init_by_array /
  genrand_res53）+ 三个对外函数：
  - `dv_random_seed(i64)`：按 CPython 3.14 口径播种（`|seed|` 小端切 32 位字、
    `init_by_array`；`seed=0` 补单字 `[0]`；未播种时用 `dv_timestamp()` 自动播种）
  - `dv_random()`：`genrand_res53`（与 `random.Random.random()` 位级一致）
  - `dv_random_bits(i64)`：对齐 `getrandbits`（右移保留高 k 位，k ≤ 63）
  - 任务书要求 LCG，但对拍判据要求与 CPython 逐位一致 → 算法偏离为 MT19937
    （见 known_issues T5C 章节）。
- **`src/llvm/codegen_typed.py`**（+51/-11）：
  - 注册 4 组 builtin：`随机数`/`random`/`dv_random`、`播种随机种子`/
    `设置随机种子`/`random_seed`/`dv_random_seed`、`随机位`/`random_bits`/
    `getrandbits`/`dv_random_bits`、`对数`/`log`/`自然对数`（→ dv_log）；
  - 3 处浮点快路径类型守卫修复（T5C-01，见 known_issues）：`_infer_expr_type`
    算术分支、算术 FLOAT 快路径、比较 FLOAT 快路径 —— 任一侧静态类型未知时
    回退通用 `dv_*` 路径，不再误取浮点 DV 的 i64 槽（恒 0）。

### 各模块要点

- **随机.light（22 导出）**：随机整数 / 随机浮点 / 随机选择 / 随机选择权重 /
  随机洗牌 / 随机抽样 / 随机布尔 / 随机字节 / 随机范围 / 随机种子 / 随机0到1 /
  随机选择多个 / 随机打乱 / 随机字符串 / 随机字母数字 / 随机UUID / 正态分布 /
  均匀分布 / 随机密码 / 安全随机整数 / 安全随机令牌 / 安全随机URL令牌。
  确定性核心：`随机下限(n)` = `_randbelow_with_getrandbits` 同构；洗牌/打乱 =
  CPython `shuffle` 同构（`for i in reversed(range(1,n)): j = 随机下限(i+1)`）；
  正态分布 = Box-Muller 同构 + 两值缓存；随机选择权重 = `fl(random()*total)`
  区间累加（要求整数权重、总和 < 1024，见能力边界）。安全随机* 用
  `dv_random` 种子实现，**不具密码学安全性**，仅格式对齐。
- **集合.light（36 导出）**：原生腿无 set 类型 → 列表模拟（插入序）：创建 /
  添加 / 移除 / 丢弃 / 弹出 / 清空 / 大小 / 包含 / 相等 / 子集 / 真子集 /
  超集 / 真超集 / 不相交 / 并集 / 交集 / 差集 / 对称差 / 多个并集 / 多个交集 /
  补集 / 幂集 / 笛卡尔积 / 杰卡德相似度（×2^53 整数指纹）/ 唯一元素 / 重复元素等。
- **集合工具.light（15 导出）**：默认字典 / 有序字典 / 计数器 / 双端队列 /
  命名元组 / 链字典 / 用户字典 / 用户列表 / 用户字符串 / 统计频率等 —— 字典/
  列表模拟，`链字典` 固定双参（变参限制）。
- **集合操作.light（13 导出）**：去重 / 合并 / 交集 / 差集 / 补集 / 分组 /
  排序 / 反转顺序 / 打乱 / 扁平 / 取部分 / 条件过滤 / 条件映射。
  `交集`/`差集` 与 集合.light 同名 → 同一程序内不得同时导入两模块
  （known_issues T5C-06）。
- **对象池缓存.light（31 导出）**：全部缓存基于「键/值/截止 并行列表」架构
  （字典新增键经 REF 写回丢失 → 走「字典读出句柄 + 追加」），LRU / 简单缓存 /
  定时缓存 / 对象池 / 装饰器类。过期清理用 `时间戳()` 时间戳；缓存值契约
  不可为 `空`。

### 验证证据

测试文件：`tests/unit/test_原生腿_T5C_集合随机.py`（9 用例，全部
`optimize_level=0` 强 O0）

| 分组 | 测试数 | 对拍基准 | 结果 |
|------|--------|----------|------|
| 随机固定种子 42 逐位对拍 | 1 | CPython `random.Random(42)`（约 35 行流） | ✅ |
| 随机负种子/种子 0 | 1 | CPython 同种子 | ✅ |
| 随机安全类格式与边界 | 1 | 格式断言 + 同流 `getrandbits(63)` | ✅ |
| 随机空壳判红 | 1 | `NativeImportError` | ✅ |
| 集合运算对拍 | 1 | CPython `set` 运算 | ✅ |
| 集合操作对拍 | 1 | CPython 语义 | ✅ |
| 集合工具冒烟 | 1 | 字典/计数器语义 | ✅ |
| 对象池缓存冒烟 | 1 | 缓存语义（过期/清除） | ✅ |
| 集合空壳判红 | 1 | `NativeImportError` | ✅ |
| **总计** | **9** | | **9 passed** |

- 对拍口径：固定种子 42（`设 空值 为 播种随机种子(42)`）→ 随机整数 /
  随机选择 / 随机选择权重 / 随机选择多个 / 随机布尔 / 随机洗牌 / 随机打乱 /
  随机抽样 / 随机字符串 / 随机字母数字 / 随机字节 / 正态分布 / 均匀分布 /
  随机范围 全部与 CPython 同流逐位一致；令牌 / URL令牌 / UUID 为格式断言
  （.py 走 `os.urandom`，不承诺对拍）。
- 反跑判据验证：
  - 真实现 → 9 passed（绿）；
  - 恢复空壳 .light（`decl 0` 只有声明）→ 2 个空壳判红用例 passed（编译报
    「未定义的段落」/ undefined symbol）；
  - O0 下不崩（全部用例 `optimize_level=0`）；
  - 固定种子随机输出与 CPython 逐位一致。
- 能力清单：`docs/原生腿能力清单.json` builtin 328→342（+13 别名 + 计数）、
  runtime 243→246（+dv_random/dv_random_seed/dv_random_bits）；因 codegen
  插入导致的既有 evidence 行号漂移全量重算 ——
  `tests/unit/test_native_leg_capability.py` 11 passed。
- C 编译冒烟：`clang -c -O0 -Wno-deprecated-declarations runtime_typed.c`
  rc=0（仅既有 strtok const 警告 1 条，与本次新增无关）。
- 遗留标注：`dv_random` 系列**未在 POSIX 实测**（Windows 本机 CPython 3.14.7
  对拍；POSIX 服务器 192.168.0.86 凭据在 `.env`，未连）。

### 文件清单

- 修改：`stdlib/随机.light`（323 行，空壳→真实现）
- 修改：`stdlib/集合.light`（450 行，空壳→真实现）
- 修改：`stdlib/集合工具.light`（134 行，空壳→真实现）
- 修改：`stdlib/集合操作.light`（261 行，空壳→真实现）
- 修改：`stdlib/对象池缓存.light`（352 行，空壳→真实现）
- 修改：`src/llvm/runtime_typed.c`（+154 行：MT19937 + dv_random/dv_random_seed/
  dv_random_bits，文件末尾独立函数区，TLS 段未触碰）
- 修改：`src/llvm/codegen_typed.py`（+51/-11：4 组 builtin 注册 + 3 处浮点快路径
  类型守卫修复）
- 新增：`tests/unit/test_原生腿_T5C_集合随机.py`（9 用例，强 O0）
- 修改：`docs/原生腿能力清单.json`（builtin +14、runtime +3、evidence 行号重算）
- 回写：`docs/known_issues.md`（R10-12c T5C 章节）
- 临时调试产物 `_taskT5c_*` 已清理
