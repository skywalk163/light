# 自测报告 · 任务 A2（语言层：静默错编与异步并发原语）第二轮

- 工作树：`g:\dswork\duan-light-merge\wt-A2`，分支 `task-A2`，基线 `e27277c2`
- 解释器：`C:\Python314\python.exe`（Python 3.14.7 / pytest 9.1.1）
- 本报告所有数字都是**本机实跑**得到的，命令原样抄在第二节

---

## 一、改了哪些文件

`git diff --stat`（11 个文件，+993 / −86）：

| 文件 | 增删 | 对应任务项 | 干了什么 |
|------|------|-----------|---------|
| `src/llvm/codegen_typed.py` | +35 | A2-1 | 语句分派链尾补 `else` 兜底；在 `ExpressionStatement` 分支里拆掉适配层的 `<unknown:XXX>` 伪装。**只动语句分派段，未碰内建注册段**（与 B2 的唯一重叠点） |
| `src/parser_stmt.py` | +140 | A2-2 / A2-5 | 作用域声明（`外层`/`全局`）改按 token 跨度识别，容忍词法切分；类体内允许 `类`（嵌套类），并修正 DEDENT 归属，使嵌套类之后的外层成员不被吞 |
| `src/code_generator.py` | +165 | A2-3 / A2-4 | 五个异步原语映射到 `asyncio`（含 `并发等待` 单参摊平、`首个完成` 的 `FIRST_COMPLETED`）；类 / 段落 / 类型别名产出 `TypeVar` + `Generic[T]`；按需插 `from typing import ...`；类型别名从「整文件编不出」变成能编；嵌套类递归产出 |
| `src/lexer.py` | +44 | A2-3 / A2-7 | 复合词表清理：删 9 个死名字（有词法保护但零实现、零使用），保留本轮已映射的 5 个 + 三个异步文件名；`?` 后缀单独报错并指向 `可空 整数` |
| `src/parser_expr.py` | +30 | A2-3（P0） | `_parse_primary` 拦下修饰符关键字 `异步`：以 `异步` 开头又不在复合词表里的名字会被切成 `异步` + 余下部分，此前一路 PARSE-OK 编成 `await 异步`（最高优先级的静默错编） |
| `src/module_resolver.py` | +54 | A2-6 | 缩进行的导入不再计入模块级依赖（段落体内延迟导入可打破环）；顶格导入照旧算模块级依赖、照旧拒绝成环；环报错的第 4 条建议改成可直接抄的缩进导入示例 |
| `tests/test_frontend_blockers_run.py` | +471 | A2-2..A2-7 + P0 | 新增 22 个真跑用例（两条腿：`run` + `compile` 后执行产物），共 51 个 `def test_`（含 1 个参数化 6 例、1 个 `strict=True` 的 xfail） |
| `tests/unit/test_llvm_stmt_coverage.py` | 新建 173 行 | A2-1 | 正跑 6 条 + 反跑 5 条 + 静态断言 2 条 = 13 个用例 |
| `tests/test_async.py` | −48 | A2-3 | 删掉 4 个字符串断言测试（`assert 'async def' in py_code` 一类，断言的是产物长相不是行为） |
| `docs/known_issues.md` | +78 | A2-1..A2-7 | 新增第九节（原生后端未支持语句清单）、第十节（本轮口径变更表） |
| `docs/release_notes_v6.1.0.md` / `docs/tutorials/进阶教程.md` | +8 / +6 | P0 连带 | 两处文档示例教的是**不存在的名字**（`异步获取` / `异步休眠`），P0 拦下后暴露；改成能编过的写法，见第五节说明 |

**没有碰**：`stdlib/` 全部、`src/llvm/runtime_typed.c`、`c_backend.py`、`codegen_typed.py`
的内建注册段。`引 Python：` 计数仍为 0（未新增任何一处）。

本轮**主动回退**了两处越界改动（上一段会话留下的）：
`src/ast_nodes_v3.py`（给 `ClassDefinition` 加 `nested_classes` slot）与
`src/compiler.py`（`AstAdapter` 里拒绝嵌套类）——两者都不在 A2 可改清单里。
回退后立刻暴露一个真 bug：`_parse_class_definition` 无条件给基类构造器传
`nested_classes=`，导致**所有类**在原生后端炸 `TypeError`。改成「只有真有嵌套类时
才用本文件里的子类节点」后修复（见 `src/parser_stmt.py` 尾部）。

回退还带来一个比原方案更好的结果：嵌套类节点是本文件定义的子类，适配层没有它的
转换器 → 被包成 `<unknown:ClassDefinitionWithNested>` → 被 A2-1 的拆伪装逻辑抓住并
报错。**不改 `compiler.py` 也能让原生后端明确拒绝嵌套类**，见第二节实测。

---

## 二、定向测试结果

### 2.1 定向测试集（一次跑齐）

```powershell
Remove-Item Env:PYTHONIOENCODING -ErrorAction SilentlyContinue
$env:PYTHONUTF8="1"
C:\Python314\python.exe -m pytest tests/test_frontend_blockers_run.py tests/test_async.py `
  tests/test_iterator_protocol.py tests/unit/test_llvm_stmt_coverage.py `
  tests/unit/test_spec_docs_sync.py tests/unit/test_doc_examples_gate.py -q -p no:warnings
```

结果：**collected 120 → 119 passed, 1 xfailed, 100.99s**

| 文件 | 通过 | 备注 |
|------|------|------|
| `tests/test_frontend_blockers_run.py` | 55 passed, 1 xfailed | xfail 是 `test_a23_创建任务`（`strict=True`，见第四节） |
| `tests/test_async.py` | 27 passed | 删掉 4 个字符串断言后剩下的全是真跑 |
| `tests/test_iterator_protocol.py` | 5 passed | 未改动，回归护栏 |
| `tests/unit/test_llvm_stmt_coverage.py` | 13 passed | 本轮新建 |
| `tests/unit/test_spec_docs_sync.py` | 13 passed | 冻结表 ↔ 实现门禁 |
| `tests/unit/test_doc_examples_gate.py` | 6 passed | 文档示例编译门禁 |

保留的输出文件：`wt-A2\自测输出_任务A2_定向测试.txt`（上述命令的完整 stdout）。

### 2.2 A2-1 原生后端未支持语句类型清单（实测）

`compile_source_typed` 逐片段跑 24 条，分三桶：

- **能编 16 条**：赋值/变量声明、`如果`、`遍历`、`当`、段落、`返回`、`跳出`、`继续`、
  `尝试`/`捕获`、`抛出`、`打印`、`引`（**编成空操作**）、类（非嵌套）、接口、`导出`、`延迟`
- **明确拒绝 6 条**（全部 `NotImplementedError`，文案含语句类型名 + 指向转译后端）：

  | 写法 | 文案里的类型名 |
  |------|--------------|
  | `全局 计数。` | `ScopeDeclStmt` |
  | `外层 计数。` | `SegmentDefinition`（嵌套段落先被拦下，如实报拦下它的那一层） |
  | `生成 1。` | `YieldStmt` |
  | `断言 1 等于 1。` | `AssertStmt` |
  | `类型 甲 = 整数。` | `TypeAlias` |
  | 类体内嵌套 `类` | `ClassDefinitionWithNested` |

- **更早的层拦下 2 条**：`匹配`/`情形`、`异步域`（v3 解析器语法就没通，与后端无关）

静态两侧：上游 v3 语句类节点 27 个，其中 **10 个**没有适配层转换器
（`AssertStmt`/`DecoratorDefinition`/`EmbedBlock`/`FFIFunctionDecl`/`FFIVarArgsDecl`/
`PassStmt`/`RunAsyncStmt`/`ScopeDeclStmt`/`TypeCheckToggleStmt`/`YieldStmt`）；
下游 `_gen_statement` 只有 **16** 条 isinstance 分支，适配层可产出的 legacy 节点里
**28 种**没有分支、落链尾兜底。全清单写进 `docs/known_issues.md` 第九节。

改动前后对照（同一份 `全局` 源码）：

- 改动前：编译成功、IR 正常生成、`全局` 那句被**静默丢弃**
- 改动后：`NotImplementedError: 原生后端暂不支持语句类型「ScopeDeclStmt」（源码行 未知（适配层未保留行号））：原生后端暂不支持，请用转译后端（python -m cli.light_unified run）`

反跑护栏（已支持语句仍编得出，且 IR 里不许残留 `<unknown`）：
`设`/`如果`/`遍历`/`返回` 单独与同篇各一例，IR 7800–9161 字符。

### 2.3 积木库 CI 闸（粗筛）

```powershell
C:\Python314\python.exe 积木库\评估\ci_eval.py
```

结果：**全部通过，24 项判据全绿，耗时 203.6s**（块总数 182；冒烟可运行率
1.0 (179/179)；兜底闭环触发率 1.0、沉淀成功率 0.95）。
它产出的 `积木库/评估/报告/ci_eval.json` 是运行副产物、不属 A2 范围，**已 `git checkout` 还原**。

### 2.4 过程中真实红过一次，必须记下来

单独跑 `tests/unit/test_doc_examples_gate.py` 是 6 passed；但**和任何别的测试一起跑**
就红一条 `test_不许新增ROT`，点名两个块：

```
docs/release_notes_v6.1.0.md hash=3ba6716e172f
docs/tutorials/进阶教程.md   hash=041ddb29555c
```

- 顺序依赖的原因：该门禁 `setUpClass` 里 `scan_all()` 在**进程内**编译全部文档代码块，
  单独跑时 `src` 尚未被别的测试导入过；混跑时导入状态不同，两个块才真正走到我改动的
  那条路径上。**「单独跑绿」在这条门禁上不算绿**，报数必须用混跑。
- 用 `git stash` 回到基线跑同一对：**11 passed**。所以是本轮改动引起的，不是既有 flaky。
- 根因不是门禁而是文档：两个块教的是 `等待 异步获取(...)` / `等待 异步休眠(1)`，
  **这两个名字都不存在**。P0 之前它们被切成 `异步` + 余下部分，编成 `果 = await 异步`
  且实参调用结果被丢弃——一路 PARSE-OK。也就是说这两个块此前之所以「能编过」，
  正是因为编译器在静默错编。
- 修法：`异步休眠` → `异步睡眠`（真映射到 `asyncio.sleep`）；`异步获取` 那段改成
  「`异步 段落` 包住 + `异步 运行` 启动 + 被等待的名字不以 `异步` 开头」，并在注释里
  写清原因。两块改完实测能编过，混跑恢复 **24 passed**（含另两个文件）。

---

## 三、移交清单（9 条）

1. **`AstAdapter` 丢行号** —— `src/compiler.py::AstAdapter` 转换时不带 `lineno`，
   原生后端的拒绝文案给不出源码行，只能如实写「源码行 未知（适配层未保留行号）」。
   修它要动 `compiler.py`，不在 A2 范围。
2. **嵌套类在原生后端只是「拒绝」，不是「支持」** —— 转译后端可用；原生后端报
   `ClassDefinitionWithNested`。类型名偏内部，可读性一般，但比静默丢内层类好。
   真要支持得同时改适配层与类型布局。
3. **`引` 在原生后端是空操作** —— `_gen_statement` 里 `ImportStatement: pass`。
   既不报错也不加载模块，属于「静默无效」这一类，本轮没动。它不在「会报错」清单里，
   别把清单读成「原生后端只有这 6 类不支持」。
4. **`异步读取文件`/`异步写入文件`/`异步追加文件`：有词法保护、没有实现** ——
   三个名字仍整体成一个词（删掉保护会让 `NameError` 退化成静默切分），调用时报
   `NameError` 并带完整名字。零依赖实现需要动 `stdlib/`，A2 禁区。
5. **`等待 <局部变量>` 会被粘成一个标识符** —— `test_a23_创建任务` 是
   `@pytest.mark.xfail(strict=True)`，钉住这个缺陷；修好那天它会因「意外通过」而变红，
   提醒你销账。
6. **9 个死名字已从复合词表删除** —— 判据是：37261 份 `.light` 里 0 次使用、0 处定义、
   A/B token 流零差异。若将来要实现它们，得先补词法保护再补实现，顺序不能反。
7. **`?` 可空后缀决策为「永不支持」** —— 报错文案指向 `可空 整数`。这是决策不是缺口，
   但需要一个人拍板确认，否则会有人当 bug 又提一次。
8. **`匹配`/`情形` 与 `异步域` 连 v3 解析器都过不了** —— 本轮实测到，属前端语法缺口，
   不是后端问题。清单里单列一桶就是为了别把它算进「后端不支持」。
9. **`src/parser_expr.py` 的 P0 拦截是一刀切** —— 目前只拦 `异步` 一个词
   （`_MODIFIER_ONLY_KEYWORDS`）。理由是 `异步` 在表达式位置从无合法含义；但同类修饰符
   （若将来新增）不会自动进这个集合，需要逐词加。

---

## 四、已知未完成与未验证

1. **原生后端拒绝文案没有行号** —— 见移交 1。文案里明写了「未知」，没有假装有。
2. **段落级泛型只登记 `TypeVar`，不产出 `def f[T]`** —— `def f[T]` 是 Python 3.12+ 语法，
   为了不抬高最低版本要求，段落只登记 TypeVar、不产出形参列表泛型。
   所以「段落级泛型」是**部分实现**，不是完整实现。
3. **`外层` 在原生后端拿不到 `ScopeDeclStmt` 这条断言** —— 因为嵌套段落本身不支持，
   先撞 `SegmentDefinition`。测试里断言的是实测到的类型名，并在注释里写明原因；
   等嵌套段落支持了，这条断言需要改。
4. **只跑了定向测试集，没跑全量** —— 按规程第 7 条。因此「与本轮无关的地方是否被
   碰坏」这件事**没有证据**。`tests/unit/test_doc_examples_gate.py` 那次红恰好说明
   定向集之外还有能咬人的东西，全量由整合方负责。
5. **P0 拦截的误伤面只在语句层反跑过 4 种形状** —— `异步 段落` / `异步 运行` /
   `异步 遍历` / 已支持的四个异步名字。没做全仓 `.light` 扫描确认零误伤。
6. **两处文档改动只验证了「能编过」，没验证语义正确** —— 门禁只编不跑；
   `网络获取` 这个名字是示意性的，运行期并不存在（该文档整块都是特性说明体裁）。
7. **`积木库/评估/报告/ci_eval.json` 被跑测试改过又还原** —— 还原后与 HEAD 一致，
   但这说明 `ci_eval.py` 有写盘副作用，混在别的验证里跑会污染工作树。

---

## 五、跳过清单（每条跳过掩盖了什么）

| 我跳过了什么 | 为什么跳 | 这个跳过掩盖了什么 |
|-------------|---------|------------------|
| 全量回归 | 规程第 7 条明令只跑自己的定向集 | 掩盖了「本轮 11 个文件的改动是否碰坏定向集之外的东西」。`parser_expr.py` 的 P0 拦截是通则式改动（拦一个关键字在**所有**表达式位置），最有可能在别处咬人，而定向集里没有任何跨模块用例。文档门禁那次红就是这一类的样本，只是它恰好在我的定向集里 |
| 修 `AstAdapter` 的行号丢失 | `src/compiler.py` 不在可改清单 | 掩盖了「用户拿到原生后端的拒绝文案后能不能快速定位」。现在文案只说类型名，源码大一点就得自己找 |
| 在 `compiler.py` 里拒绝嵌套类 | 同上，且回退后发现不需要 | 原本担心「适配层静默丢内层类」；实测子类节点会走 `<unknown:>` 路径被抓住，所以这个跳过**没有留下缺口**——这是本轮唯一一个「跳过之后反而更好」的 |
| 实现三个异步文件原语 | 需要新增 `stdlib/*.light`，A2 禁区 | 掩盖了「异步文件 IO 到底可不可用」。现在的状态是「词法认得、运行期没有」，用户会在运行期才撞到 `NameError` |
| 支持 `?` 可空后缀 | 决策为永不支持 | 掩盖了「从别的语言迁移过来的人第一反应会写 `?`」这个真实摩擦。现在只有报错文案在缓解，没有语法糖 |
| 让原生后端真正支持 `全局`/`生成`/`断言`/类型别名/嵌套类 | 本轮目标是「不许静默」，不是「补齐能力」 | 掩盖了「这些语句在原生后端何时能用」。现在只是从静默错编变成明确拒绝——能力面**一寸没涨** |
| 对 `匹配`/`异步域` 做任何事 | 它们死在 v3 解析器，不属 A2-1 的语句分派问题 | 掩盖了前端语法层还有多少这类缺口。我只测了这两个，没有系统扫过 |
| 全仓 `.light` 扫描确认 P0 零误伤 | 时间与规程范围 | 掩盖了可能存在的、以 `异步` 开头的用户自定义名字。它们现在会从「静默错编」变成「编译报错」——方向对，但对既有代码是破坏性变更 |
| `docs/` 之外的文档站点（`docs-site/`、`README.md`） | 不在本轮任务的文档范围 | 掩盖了「同一批过时写法是否也印在别处」。`异步获取`/`异步休眠` 这两个不存在的名字，我只在 `docs/` 下清掉了 |

---

## 结论（≤300 字）

定向测试集 **120 收集 / 119 通过 / 1 xfailed（strict）/ 0 失败**，耗时 101s；
积木库 CI 闸 24 项判据全绿（203.6s）。**不阻塞**整合。

七个任务项全部落地：A2-1 补上语句分派兜底并拆掉适配层 `<unknown:>` 伪装，实测清单
「能编 16 / 明确拒绝 6 / 更早的层拦下 2」；A2-2 作用域声明改按 token 跨度识别；
A2-3 五个异步原语真映射 `asyncio`、删 9 个死名字、把 4 个字符串断言测试换成真跑，
并额外拦下 `异步` 当值用这个 P0 静默错编；A2-4 泛型产出 `TypeVar`/`Generic[T]`，
类型别名从编不出变成能编；A2-5 嵌套类可用（转译后端），DEDENT 归属修好使外层成员
不被吞；A2-6 实测证明官方建议的破环写法原先无效，改口径后有效；A2-7 决策记档。

过程中回退了两处越界改动（`ast_nodes_v3.py`、`compiler.py`），并因此暴露并修掉一个
让**所有类**在原生后端炸 `TypeError` 的真 bug。另有一次真实红：文档门禁混跑时点名两个
代码块——它们此前「能编过」正是因为编译器在静默错编，已改文档并恢复全绿。

移交 **9 条**，其中要人拍板的是 `?` 永不支持（第 7 条）与 P0 拦截对既有代码的破坏性
（第 9 条 + 跳过清单末两行）。最大的未验证面是**没跑全量**。
