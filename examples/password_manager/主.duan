# 段言密码管理器
# 一个简单的密码生成和存储管理工具

段落 生成随机密码 接收 长度 :
    如果 长度 小于 4:
        设 长度 为 8
    设 大写字母 为 "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    设 小写字母 为 "abcdefghijklmnopqrstuvwxyz"
    设 数字 为 "0123456789"
    设 特殊符号 为 "!@#$%^&*()_+-=[]{}|;:,.<>?"
    设 所有字符 为 大写字母 加 小写字母 加 数字 加 特殊符号

    设 密码 为 ""
    设 随机 为 导入《随机》

    # 确保至少包含一个大写字母、小写字母、数字和特殊符号
    设 密码 为 密码 加 随机.选择(大写字母)
    设 密码 为 密码 加 随机.选择(小写字母)
    设 密码 为 密码 加 随机.选择(数字)
    设 密码 为 密码 加 随机.选择(特殊符号)

    # 填充剩余长度
    遍历 索引 于 范围(0, 长度 减 4):
        设 密码 为 密码 加 随机.选择(所有字符)

    # 打乱密码顺序
    设 密码列表 为 列表(密码)
    随机.打乱(密码列表)
    返回 "" 加 密码列表.连接("")

段落 生成易记密码 接收 :
    设 形容词 为 ["快乐", "勇敢", "聪明", "温暖", "闪亮", "安静", "活泼", "强大"]
    设 名词 为 ["星辰", "大海", "阳光", "微风", "彩虹", "森林", "山峦", "河流"]
    设 随机 为 导入《随机》
    设 数字 为 随机.整数(10, 99)
    设 符号 为 随机.选择(["!", "@", "#", "$"])
    返回 随机.选择(形容词) 加 随机.选择(名词) 加 数字 加 符号

段落 保存密码 接收 服务, 用户名, 密码 :
    设 文件名 为 "passwords.txt"
    尝试:
        使用 打开(文件名, "a", 编码 为 "utf-8") 为 文件:
            文件.写入(服务 加 "|" 加 用户名 加 "|" 加 密码 加 "\n")
        打印("密码已保存到 " 加 文件名)
        返回 真
    捕获 异常 为 错误:
        打印("保存失败: " 加 错误)
        返回 假

段落 读取密码 接收 搜索服务 :
    设 文件名 为 "passwords.txt"
    设 找到 为 假
    尝试:
        使用 打开(文件名, "r", 编码 为 "utf-8") 为 文件:
            遍历 行 于 文件:
                设 部分 为 行.去空白().分割("|")
                如果 长度(部分) 等于 3:
                    设 服务 为 部分[0]
                    设 用户名 为 部分[1]
                    设 密码 为 部分[2]
                    如果 搜索服务 等于 "" 或 服务 包含 搜索服务:
                        打印("服务: " 加 服务)
                        打印("  用户名: " 加 用户名)
                        打印("  密码: " 加 密码)
                        打印("  ---")
                        设 找到 为 真
        如果 非 找到:
            打印("未找到匹配的服务: " 加 搜索服务)
    捕获 文件未找到:
        打印("密码文件不存在，请先保存一些密码")
    捕获 异常 为 错误:
        打印("读取失败: " 加 错误)

段落 计算密码强度 接收 密码 :
    设 分数 为 0
    设 信息 为 []

    # 长度检查
    如果 长度(密码) 大于等于 12:
        分数 为 分数 加 40
        信息.追加("✓ 长度足够 (>=12)")
    否则如果 长度(密码) 大于等于 8:
        分数 为 分数 加 20
        信息.追加("▪ 长度中等 (>=8)")
    否则:
        信息.追加("✗ 长度过短 (<8)")

    # 字符类型检查
    设 有大写 为 假
    设 有小写 为 假
    设 有数字 为 假
    设 有符号 为 假
    遍历 字符 于 密码:
        如果 字符 大于等于 "A" 且 字符 小于等于 "Z":
            设 有大写 为 真
        否则如果 字符 大于等于 "a" 且 字符 小于等于 "z":
            设 有小写 为 真
        否则如果 字符 大于等于 "0" 且 字符 小于等于 "9":
            设 有数字 为 真
        否则:
            设 有符号 为 真

    如果 有大写:
        分数 为 分数 加 15
        信息.追加("✓ 包含大写字母")
    否则:
        信息.追加("✗ 缺少大写字母")

    如果 有小写:
        分数 为 分数 加 15
        信息.追加("✓ 包含小写字母")
    否则:
        信息.追加("✗ 缺少小写字母")

    如果 有数字:
        分数 为 分数 加 15
        信息.追加("✓ 包含数字")
    否则:
        信息.追加("✗ 缺少数字")

    如果 有符号:
        分数 为 分数 加 15
        信息.追加("✓ 包含特殊符号")
    否则:
        信息.追加("✗ 缺少特殊符号")

    # 强度评级
    如果 分数 大于等于 80:
        设 评级 为 "★ 强密码"
    否则如果 分数 大于等于 50:
        设 评级 为 "★★ 中等密码"
    否则:
        设 评级 为 "★★★ 弱密码"

    返回 {"分数": 分数, "评级": 评级, "信息": 信息}

段落 主菜单 接收 :
    打印("=" * 50)
    打印("🔐 段言密码管理器")
    打印("=" * 50)
    打印("1. 生成随机密码")
    打印("2. 生成易记密码")
    打印("3. 保存密码")
    打印("4. 查看密码")
    打印("5. 检测密码强度")
    打印("6. 退出")
    打印("=" * 50)
    返回 输入("请选择 (1-6): ")

段落 主循环 接收 :
    当 真:
        设 选择 为 主菜单()

        如果 选择 等于 "1":
            设 长度 为 整数(输入("请输入密码长度 (默认16): ") 或 "16")
            设 密码 为 生成随机密码(长度)
            打印("生成密码: " 加 密码)

        否则如果 选择 等于 "2":
            设 密码 为 生成易记密码()
            打印("生成密码: " 加 密码)

        否则如果 选择 等于 "3":
            设 服务 为 输入("请输入服务名称: ")
            设 用户名 为 输入("请输入用户名: ")
            设 密码 为 输入("请输入密码: ")
            保存密码(服务, 用户名, 密码)

        否则如果 选择 等于 "4":
            设 搜索 为 输入("请输入搜索关键词 (回车查看全部): ")
            读取密码(搜索)

        否则如果 选择 等于 "5":
            设 密码 为 输入("请输入要检测的密码: ")
            设 结果 为 计算密码强度(密码)
            打印("密码强度评分: " 加 结果["分数"] 加 "/100")
            打印("评级: " 加 结果["评级"])
            打印("详情:")
            遍历 项 于 结果["信息"]:
                打印("  " 加 项)

        否则如果 选择 等于 "6" 或 选择 小写 等于 "q" 或 选择 小写 等于 "quit" 或 选择 小写 等于 "exit":
            打印("感谢使用段言密码管理器，再见！")
            跳出

        否则:
            打印("无效选择，请重新输入")

        打印("")  # 空行

# 启动
主循环()