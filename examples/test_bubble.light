# 光明 - 冒泡排序测试
段落 冒泡排序 接收 甲：
  设 乙 为 列表长度(甲)。
  设 丙 为 零。
  当 丙 小于 乙 减 一：
    设 丁 为 零。
    当 丁 小于 乙 减 丙 减 一：
      如果 甲[丁] 大于 甲[丁 加 一] 那么：
        设 临时 为 甲[丁]。
        甲[丁] 为 甲[丁 加 一]。
        甲[丁 加 一] 为 临时。
      结束
      设 丁 为 丁 加 一。
    结束
    设 丙 为 丙 加 一。
  结束
  #打印甲
  返回 甲。



设 数列 为 [五, 三, 八, 一, 二]。
打印("排序前：")。
打印(数列)。
设 已排序 为 冒泡排序(数列)。
打印("排序后：")。
打印(已排序)。