# 光明 - 图灵机原型机
# 模拟图灵机：二进制加一器

段落 读写纸带 接收 纸带, 指针, 值：
  纸带[指针] = 值
  返回 纸带

段落 执行图灵机 接收 纸带, 状态表：
  设 当前状态 为 "S0"
  设 指针 为 0
  设 步数 为 0
  打印("初始纸带：")
  打印(纸带)

  当 当前状态 不等于 "停机"：
    设 读符 为 纸带[指针]
    设 键 为 当前状态 加 " " 加 读符
    如果 _light_builtin.字典包含键(状态表, 键)：
      设 转移 为 _light_builtin.字典获取(状态表, 键)
      设 当前状态 为 转移[0]
      纸带[指针] = 转移[1]
      设 移动 为 转移[2]
      如果 移动 等于 "0"：
        设 当前状态 为 "停机"
      如果 移动 等于 "-1"：
        设 指针 为 指针 减 1
      如果 移动 等于 "1"：
        设 指针 为 指针 加 1
    否则：
      设 当前状态 为 "停机"
    设 步数 为 步数 加 1
    如果 步数 大于 20：
      打印("超过最大步数，停止")
      设 当前状态 为 "停机"

  打印("最终纸带：")
  打印(纸带)
  打印("执行步数：")
  打印(步数)

# 纸带：用列表表示
设 纸带 为 列("1", "0", "1", "1", " ")

# 状态转移表
设 状态表 为 字典创建()
设 _ 为 _light_builtin.字典设置(状态表, "S0 0", 列("S1", "1", "0"))
设 _ 为 _light_builtin.字典设置(状态表, "S0 1", 列("S0", "0", "-1"))
设 _ 为 _light_builtin.字典设置(状态表, "S1 0", 列("S1", "0", "0"))
设 _ 为 _light_builtin.字典设置(状态表, "S1 1", 列("S1", "1", "0"))
设 _ 为 _light_builtin.字典设置(状态表, "S1  ", 列("S1", " ", "0"))

# 执行
执行图灵机(纸带, 状态表)