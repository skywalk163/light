# L-074 测试：类型判断函数内置化
# 验证：是字典/是列表/是字符串/是布尔/是函数/是数字 免 import 直接可用

段落 辅助：
  返回 1

段落 主：
  # 是字典
  打印(是字典({}))
  打印(是字典([]))
  # 是列表
  打印(是列表([]))
  打印(是列表({}))
  # 是字符串
  打印(是字符串("hello"))
  打印(是字符串(42))
  # 是布尔
  打印(是布尔(真))
  打印(是布尔(1))
  # 是函数
  打印(是函数(辅助))
  打印(是函数(42))
  # 是数字（int/float，排除 bool）
  打印(是数字(42))
  打印(是数字(3.14))
  打印(是数字(真))
  打印(是数字("hello"))

  打印("通过：类型判断函数全部正常")
