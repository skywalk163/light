# 段言 - 终端数据可视化工具
# 功能: 使用 ASCII 字符在终端绘制柱状图、折线图
# 用法: duan run examples/data_visualization/主.duan

段落 绘制柱状图 接收 数据, 标题:
  打印("")。
  打印("=" * 40)。
  打印("  " + 标题)。
  打印("=" * 40)。

  设 最大值 为 0。
  遍历 值 于 数据:
    如果 值 大于 最大值:
      最大值 为 值。

  设 缩放 为 20 / 最大值。
  设 索引 为 0。
  遍历 值 于 数据:
    设 柱长 为 值 * 缩放。
    设 柱状条 为 ""。
    设 计 为 0。
    当 计 小于 柱长:
      柱状条 为 柱状条 + "█"。
      计 为 计 + 1。
    打印(转字符串(索引) + ": " + 柱状条 + " (" + 转字符串(值) + ")")。
    索引 为 索引 + 1。
  打印("")。

段落 绘制折线图 接收 数据, 标题:
  打印("")。
  打印("=" * 40)。
  打印("  " + 标题)。
  打印("=" * 40)。

  设 最大值 为 0。
  遍历 值 于 数据:
    如果 值 大于 最大值:
      最大值 为 值。

  设 高度 为 10。
  设 行 为 高度。
  当 行 大于 0:
    设 行文本 为 ""。
    设 列 为 0。
    遍历 值 于 数据:
      设 归一化值 为 值 / 最大值 * 高度。
      如果 归一化值 大于等于 行:
        行文本 为 行文本 + " * "。
      否则若 列 大于 0 且 列 小于 列表长度(数据) - 1:
        行文本 为 行文本 + "   "。
      否则:
        行文本 为 行文本 + "   "。
      列 为 列 + 1。
    打印(行文本)。
    行 为 行 - 1。
  打印("")。

段落 主程序:
  设 数据1 为 [3, 7, 2, 9, 5, 8, 4]。
  绘制柱状图(数据1, "月度销售额（万元）")。

  设 数据2 为 [2, 5, 3, 8, 6, 9, 7, 4, 6, 8, 5, 10]。
  绘制折线图(数据2, "年度温度变化（℃）")。

主程序()。