# 段言少儿编程 - 猜数字游戏
# 电脑想一个数字，你来猜！
# 运行: duan run examples/kids/number_game.duan

# =============================================
# 游戏设置
# =============================================
设 最高分 为 0        # 历史最高分
设 总游戏次数 为 0      # 总共玩了多少局
设 总猜测次数 为 0      # 总共猜了多少次

# =============================================
# 生成随机数字
# =============================================
段落 生成随机数 接收 最小值, 最大值:
    # 简单随机数生成（基于时间戳）
    设 种子 为 时间戳()
    设 范围 为 最大值 - 最小值 + 1
    设 随机数 为 种子 % 范围 + 最小值
    返回 随机数

# =============================================
# 显示游戏标题
# =============================================
段落 显示标题 接收:
    打印 "================================================"
    打印 "=     🔢 段言猜数字游戏 🔢             ="
    打印 "=     电脑想了一个 1-100 的数字        ="
    打印 "=     看看你能猜几次猜中！              ="
    打印 "================================================"
    打印 ""

# =============================================
# 显示游戏说明
# =============================================
段落 显示规则 接收:
    打印 "📋 游戏规则："
    打印 "  1. 电脑会随机想一个 1 到 100 之间的数字"
    打印 "  2. 你每次输入一个数字来猜"
    打印 "  3. 电脑会告诉你「大了」或「小了」"
    打印 "  4. 猜中的次数越少，分数越高！"
    打印 "  5. 输入 0 可以随时退出游戏"
    打印 ""

# =============================================
# 计算分数
# =============================================
段落 计算分数 接收 尝试次数:
    如果 尝试次数 <= 3:
        返回 100
    否则若 尝试次数 <= 5:
        返回 80
    否则若 尝试次数 <= 7:
        返回 60
    否则若 尝试次数 <= 10:
        返回 40
    否则:
        返回 20

# =============================================
# 获取分数评价
# =============================================
段落 获取评价 接收 分数:
    如果 分数 >= 90:
        返回 "🌟 太厉害了！你是猜数字大师！"
    否则若 分数 >= 70:
        返回 "👍 很棒！你很擅长这个游戏！"
    否则若 分数 >= 50:
        返回 "😊 不错哦！继续加油！"
    否则:
        返回 "💪 多练习几次，你会越来越厉害！"

# =============================================
# 单局游戏
# =============================================
段落 玩一局 接收:
    显示标题()
    
    # 生成随机数字
    设 目标 为 生成随机数(1, 100)
    设 尝试次数 为 0
    设 猜中 为 假
    设 已猜过的 为 []
    
    打印 "🤔 我已经想好了一个 1 到 100 之间的数字..."
    打印 "来猜猜看吧！"
    打印 ""
    
    当 猜中 等于 假:
        设 尝试次数 为 尝试次数 + 1
        打印 "--- 第 " + 转字符串(尝试次数) + " 次猜测 ---"
        打印 "之前猜过的数字：" + 转字符串(已猜过的)
        
        设 输入文本 为 输入("请输入你猜的数字（1-100，输入0退出）：")
        设 猜测 为 转整数(输入文本)
        
        # 检查退出
        如果 猜测 == 0:
            打印 ""
            打印 "😅 你放弃了！答案是：" + 转字符串(目标)
            返回 0
        
        # 检查范围
        如果 猜测 < 1 或 猜测 > 100:
            打印 "⚠️ 请输入 1 到 100 之间的数字！"
            设 尝试次数 为 尝试次数 - 1  # 不计入次数
            继续
        
        # 检查是否重复
        设 重复 为 假
        遍历 已猜 于 已猜过的:
            如果 已猜 == 猜测:
                设 重复 为 真
                跳出
        
        如果 重复:
            打印 "⏳ 这个数字你已经猜过了！再想想别的..."
            设 尝试次数 为 尝试次数 - 1
            继续
        
        listAppend(已猜过的, 猜测)
        
        # 判断大小
        如果 猜测 == 目标:
            设 猜中 为 真
            打印 ""
            打印 "🎉🎉🎉 恭喜你猜中啦！🎉🎉🎉"
            打印 "你用了 " + 转字符串(尝试次数) + " 次猜中了数字 " + 转字符串(目标)
            
            设 分数 为 计算分数(尝试次数)
            打印 "本局得分：" + 转字符串(分数) + " 分"
            打印 获取评价(分数)
            
            # 更新全局统计
            设 总游戏次数 为 总游戏次数 + 1
            设 总猜测次数 为 总猜测次数 + 尝试次数
            如果 分数 > 最高分:
                设 最高分 为 分数
                打印 "🏆 新纪录！你打破了最高分！"
            
            返回 分数
        
        否则若 猜测 < 目标:
            如果 目标 - 猜测 <= 5:
                打印 "🔥 快到了！再大一点点！"
            否则若 目标 - 猜测 <= 15:
                打印 "📈 再大一些！"
            否则:
                打印 "⬆️ 太小了，再大一点！"
        否则:
            如果 猜测 - 目标 <= 5:
                打印 "🔥 快到了！再小一点点！"
            否则若 猜测 - 目标 <= 15:
                打印 "📉 再小一些！"
            否则:
                打印 "⬇️ 太大了，再小一点！"
        
        打印 ""

# =============================================
# 显示统计信息
# =============================================
段落 显示统计 接收:
    打印 "================================================"
    打印 "=     📊 游戏统计 📊                    ="
    打印 "================================================"
    打印 "  总游戏次数：" + 转字符串(总游戏次数)
    如果 总游戏次数 > 0:
        设 平均次数 为 总猜测次数 / 总游戏次数
        打印 "  总猜测次数：" + 转字符串(总猜测次数)
        打印 "  平均每局猜测：" + 转字符串(平均次数) + " 次"
        打印 "  历史最高分：" + 转字符串(最高分) + " 分"
    打印 ""

# =============================================
# 主程序
# =============================================
段落 主程序 接收:
    显示标题()
    显示规则()
    
    设 继续玩 为 真
    当 继续玩:
        设 本局分数 为 玩一局()
        打印 ""
        
        如果 本局分数 > 0:
            显示统计()
        
        设 再来一次 为 输入("再玩一局吗？（y/n）：")
        如果 再来一次 == "n" 或 再来一次 == "N" 或 再来一次 == "no":
            设 继续玩 为 假
        打印 ""
    
    打印 "================================================"
    打印 "=     👋 感谢游玩！再见！ 👋            ="
    打印 "================================================"
    显示统计()
    打印 "💡 提示：你可以修改代码中的数字范围，"
    打印 "   让游戏变得更简单或更难！"
    打印 "   比如改成 生成随机数(1, 50) 就更容易了"

# 开始游戏
主程序()