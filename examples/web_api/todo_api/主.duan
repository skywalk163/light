# 段言 - 待办事项 RESTful API
# 功能: 基于 Web 框架的待办事项管理 API，支持 CRUD 操作
# 用法: duan run examples/web_api/todo_api/主.duan
# API 端点:
#   GET    /api/todos    - 获取所有待办事项
#   GET    /api/todos/:id - 获取单个待办事项
#   POST   /api/todos    - 创建新待办事项
#   PUT    /api/todos/:id - 更新待办事项
#   DELETE /api/todos/:id - 删除待办事项

导入 文件系统。
导入 JSON。

# ===== 配置 =====
设 数据文件 为 "todos_api.json"。
设 服务器地址 为 "127.0.0.1"。
设 服务器端口 为 3000。

# ===== 数据存储层 =====

# 从 JSON 文件加载数据
段落 加载数据:
  如果 文件存在(数据文件):
    设 内容 为 读取文件(数据文件)。
    返回 解析JSON(内容)。
  否则:
    返回 []。

# 保存数据到 JSON 文件
段落 保存数据 接收 数据:
  设 JSON文本 为 序列化JSON(数据, 2)。
  写入文件(数据文件, JSON文本)。

# 生成唯一 ID
段落 生成ID 接收 数据:
  设 最大ID 为 0。
  遍历 项 于 数据:
    如果 项["id"] 大于 最大ID:
      最大ID 为 项["id"]。
  返回 最大ID + 1。

# 获取当前时间戳字符串
段落 获取时间:
  返回 时间戳()。

# ===== API 路由处理函数 =====

# 处理函数：获取所有待办事项
段落 获取所有待办事项 接收 请求, 响应:
  设 数据 为 加载数据()。
  响应.设置JSON(数据)。

# 处理函数：获取单个待办事项
段落 获取单个待办事项 接收 请求, 响应, 编号:
  设 数据 为 加载数据()。
  遍历 项 于 数据:
    如果 项["id"] 等于 编号:
      响应.设置JSON(项)。
      返回。
  响应.设置状态码(404)。
  响应.设置JSON({"error": "未找到该待办事项", "id": 编号})。

# 处理函数：创建新待办事项
段落 创建待办事项 接收 请求, 响应:
  设 数据 为 加载数据()。
  设 请求体 为 请求.获取JSON()。

  如果 请求体 等于 空:
    响应.设置状态码(400)。
    响应.设置JSON({"error": "请求体不能为空"})。
    返回。

  设 标题 为 字典获取(请求体, "title", "")。
  如果 标题 等于 "":
    响应.设置状态码(400)。
    响应.设置JSON({"error": "标题不能为空"})。
    返回。

  设 完成状态 为 字典获取(请求体, "completed", 假)。
  设 新事项 为 {
    "id": 生成ID(数据),
    "title": 标题,
    "completed": 完成状态,
    "created_at": 获取时间(),
    "updated_at": 获取时间()
  }。

  列表追加(数据, 新事项)。
  保存数据(数据)。

  响应.设置状态码(201)。
  响应.设置JSON(新事项)。

# 处理函数：更新待办事项
段落 更新待办事项 接收 请求, 响应, 编号:
  设 数据 为 加载数据()。
  设 请求体 为 请求.获取JSON()。

  如果 请求体 等于 空:
    响应.设置状态码(400)。
    响应.设置JSON({"error": "请求体不能为空"})。
    返回。

  设 找到 为 假。
  设 索引 为 0。
  当 索引 小于 列表长度(数据):
    如果 数据[索引]["id"] 等于 编号:
      找到 为 真。
      设 旧项 为 数据[索引]。
      设 标题 为 字典获取(请求体, "title", 旧项["title"])。
      设 完成状态 为 字典获取(请求体, "completed", 旧项["completed"])。

      数据[索引]["title"] 为 标题。
      数据[索引]["completed"] 为 完成状态。
      数据[索引]["updated_at"] 为 获取时间()。

      保存数据(数据)。
      响应.设置JSON(数据[索引])。
      跳出。
    索引 为 索引 + 1。

  如果 非 找到:
    响应.设置状态码(404)。
    响应.设置JSON({"error": "未找到该待办事项", "id": 编号})。

# 处理函数：删除待办事项
段落 删除待办事项 接收 请求, 响应, 编号:
  设 数据 为 加载数据()。
  设 新数据 为 []。
  设 找到 为 假。
  设 被删项 为 空。

  遍历 项 于 数据:
    如果 项["id"] 等于 编号:
      找到 为 真。
      被删项 为 项。
    否则:
      列表追加(新数据, 项)。

  如果 找到:
    保存数据(新数据)。
    响应.设置JSON({"message": "已删除", "deleted": 被删项})。
  否则:
    响应.设置状态码(404)。
    响应.设置JSON({"error": "未找到该待办事项", "id": 编号})。

# 处理函数：健康检查
段落 健康检查 接收 请求, 响应:
  响应.设置JSON({
    "status": "ok",
    "service": "段言待办事项 API",
    "version": "1.0.0"
  })。
  # 模拟 OPTIONS 请求的 CORS 头
  响应.设置响应头("Access-Control-Allow-Origin", "*")。
  响应.设置响应头("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS")。
  响应.设置响应头("Access-Control-Allow-Headers", "Content-Type, Authorization")。

# 处理 CORS 预检请求
段落 处理CORS 接收 请求, 响应:
  响应.设置状态码(204)。
  响应.设置响应头("Access-Control-Allow-Origin", "*")。
  响应.设置响应头("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS")。
  响应.设置响应头("Access-Control-Allow-Headers", "Content-Type, Authorization")。

# ===== 路由分发 =====

# 解析路径中的 ID
段落 解析路径ID 接收 路径:
  设 部分 为 分割字符串(路径, "/")。
  如果 列表长度(部分) 大于等于 4:
    返回 转整数(部分[3])。
  返回 0。

# 主请求路由
段落 处理请求 接收 请求, 响应:
  # 添加 CORS 头
  响应.设置响应头("Access-Control-Allow-Origin", "*")。

  设 路径 为 请求.获取路径()。
  设 方法 为 请求.获取方法()。

  # OPTIONS 预检请求
  如果 方法 等于 "OPTIONS":
    响应.设置状态码(204)。
    响应.设置响应头("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS")。
    响应.设置响应头("Access-Control-Allow-Headers", "Content-Type, Authorization")。
    返回。

  # 路由匹配
  如果 路径 等于 "/api/todos" 且 方法 等于 "GET":
    获取所有待办事项(请求, 响应)。
  否则若 路径 等于 "/api/todos" 且 方法 等于 "POST":
    创建待办事项(请求, 响应)。
  否则若 开头(路径, "/api/todos/") 且 方法 等于 "GET":
    设 编号 为 解析路径ID(路径)。
    获取单个待办事项(请求, 响应, 编号)。
  否则若 开头(路径, "/api/todos/") 且 方法 等于 "PUT":
    设 编号 为 解析路径ID(路径)。
    更新待办事项(请求, 响应, 编号)。
  否则若 开头(路径, "/api/todos/") 且 方法 等于 "DELETE":
    设 编号 为 解析路径ID(路径)。
    删除待办事项(请求, 响应, 编号)。
  否则若 路径 等于 "/api/health" 或 路径 等于 "/":
    健康检查(请求, 响应)。
  否则:
    响应.设置状态码(404)。
    响应.设置JSON({"error": "接口不存在", "path": 路径})。

# ===== 启动服务器 =====
段落 主程序:
  打印("")。
  打印("=" * 50)。
  打印("  段言待办事项 API v1.0")。
  打印("=" * 50)。
  打印("  服务器地址: http://" + 服务器地址 + ":" + 转字符串(服务器端口))。
  打印("  数据存储: " + 数据文件)。
  打印("=" * 50)。
  打印("")。
  打印("API 端点:")。
  打印("  GET    /api/todos      - 获取所有待办事项")。
  打印("  GET    /api/todos/:id  - 获取单个待办事项")。
  打印("  POST   /api/todos      - 创建新待办事项")。
  打印("  PUT    /api/todos/:id  - 更新待办事项")。
  打印("  DELETE /api/todos/:id  - 删除待办事项")。
  打印("  GET    /api/health     - 健康检查")。
  打印("")。

  设 服务器 为 创建HTTP服务端(服务器地址, 服务器端口)。

  # 注册所有路由
  服务器.注册路由("/api/todos", 处理请求, "GET")。
  服务器.注册路由("/api/todos", 处理请求, "POST")。
  服务器.注册路由("/api/todos", 处理请求, "OPTIONS")。
  服务器.注册路由("/api/todos/{id}", 处理请求, "GET")。
  服务器.注册路由("/api/todos/{id}", 处理请求, "PUT")。
  服务器.注册路由("/api/todos/{id}", 处理请求, "DELETE")。
  服务器.注册路由("/api/todos/{id}", 处理请求, "OPTIONS")。
  服务器.注册路由("/api/health", 处理请求, "GET")。
  服务器.注册路由("/", 处理请求, "GET")。

  打印("API 服务已启动: http://" + 服务器地址 + ":" + 转字符串(服务器端口))。
  打印("按 Ctrl+C 停止服务器")。
  服务器.启动(真)。

主程序()。