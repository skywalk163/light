# 外发任务 D1 — L-073 最小复现 / 修复验证
#
# 运行期 Python 原生异常应翻译为光明层可读提示，并附光明源码行号。
# 本文件复现任务文档的最小复现用例：运行到第一处未捕获异常（AttributeError）
# 即停止，stderr 应输出「类 测试类 缺少方法/属性 不存在的方法（...:行号）」。
# KeyError / IndexError / TypeError 的译文由 tests/unit/test_L068_L073.py 覆盖。

类 测试类:
  段落 方法A 接收:
    返回 1

设 实例 为 新建 测试类()
打印(实例.不存在的方法())   # ← AttributeError：类 测试类 缺少方法 不存在的方法（附源码行号）

设 字典 为 {"a": 1}
打印(字典["b"])             # ← KeyError：字典缺少键 'b'（附源码行号，运行到此处前已因上一行报错停止）
