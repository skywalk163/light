# 段言 - CLI 待办事项管理器
# 完整 CRUD 操作，JSON 文件持久化存储
# 用法: duan run examples/todo_cli/主.duan [命令] [参数]
# 命令: add, list, complete, delete, edit, clear, search, help

导入 文件系统。
导入 JSON。
导入 sys。

# 数据文件路径
设 数据文件 为 "todos.json"。

# 加载待办事项
段落 加载待办事项：
  如果 文件存在(数据文件)：
    设 内容 为 读取文件(数据文件)。
    设 数据 为 解析JSON(内容)。
    返回 数据。
  否则：
    返回 []。

# 保存待办事项
段落 保存待办事项 接收 列表：
  设 数据 为 序列化JSON(列表, 2)。
  写入文件(数据文件, 数据)。

# 生成新 ID
段落 生成新ID 接收 列表：
  设 最大ID 为 0。
  遍历 事项 于 列表：
    如果 事项["id"] 大于 最大ID：
      最大ID 为 事项["id"]。
  返回 最大ID + 1。

# 添加待办事项
段落 添加待办事项 接收 标题：
  设 列表 为 加载待办事项()。
  设 新事项 为 {
    "id": 生成新ID(列表),
    "title": 标题,
    "completed": 假,
    "created_at": 时间戳()
  }。
  列表追加(列表, 新事项)。
  保存待办事项(列表)。
  打印("✓ 已添加: " + 标题)。

# 列出待办事项
段落 列出待办事项：
  设 列表 为 加载待办事项()。
  如果 列表长度(列表) 等于 0：
    打印("📋 暂无待办事项。")。
    返回。

  打印("\n===== 待办事项列表 (" + 转字符串(列表长度(列表)) + " 项) =====")。
  遍历 事项 于 列表：
    设 状态 为 "✓" 如果 事项["completed"] 否则 "✗"。
    设 已完成 为 " (已完成)" 如果 事项["completed"] 否则 ""。
    打印("  " + 转字符串(事项["id"]) + ". [" + 状态 + "] " + 事项["title"] + 已完成)。
  打印("========================")。

# 列出待办事项（含完成状态统计）
段落 列出待办事项详细：
  设 列表 为 加载待办事项()。
  如果 列表长度(列表) 等于 0：
    打印("📋 暂无待办事项。")。
    返回。

  设 已完成数 为 0。
  设 未完成数 为 0。

  打印("\n===== 待办事项列表 =====")。
  遍历 事项 于 列表：
    设 状态 为 "✓" 如果 事项["completed"] 否则 "✗"。
    如果 事项["completed"]：
      已完成数 为 已完成数 + 1。
    否则：
      未完成数 为 未完成数 + 1。
    打印("  " + 转字符串(事项["id"]) + ". [" + 状态 + "] " + 事项["title"])。

  打印("----------------------")。
  打印("总计: " + 转字符串(列表长度(列表)) + " 项 | 已完成: " + 转字符串(已完成数) + " | 待完成: " + 转字符串(未完成数))。
  打印("========================")。

# 完成待办事项
段落 完成待办事项 接收 编号：
  设 列表 为 加载待办事项()。
  设 找到 为 假。
  遍历 事项 于 列表：
    如果 事项["id"] 等于 编号：
      事项["completed"] 为 真。
      找到 为 真。
      打印("✓ 已完成: " + 事项["title"])。
      跳出。

  如果 非 找到：
    打印("✗ 未找到编号为 " + 转字符串(编号) + " 的待办事项。")。
    返回。

  保存待办事项(列表)。

# 删除待办事项
段落 删除待办事项 接收 编号：
  设 列表 为 加载待办事项()。
  设 新列表 为 []。
  设 找到 为 假。

  遍历 事项 于 列表：
    如果 事项["id"] 等于 编号：
      找到 为 真。
      打印("✗ 已删除: " + 事项["title"])。
    否则：
      列表追加(新列表, 事项)。

  如果 非 找到：
    打印("✗ 未找到编号为 " + 转字符串(编号) + " 的待办事项。")。
    返回。

  保存待办事项(新列表)。

# 编辑待办事项
段落 编辑待办事项 接收 编号, 新标题：
  设 列表 为 加载待办事项()。
  设 找到 为 假。
  遍历 事项 于 列表：
    如果 事项["id"] 等于 编号：
      设 旧标题 为 事项["title"]。
      事项["title"] 为 新标题。
      找到 为 真。
      打印("✏ 已更新: \"" + 旧标题 + "\" -> \"" + 新标题 + "\"")。
      跳出。

  如果 非 找到：
    打印("✗ 未找到编号为 " + 转字符串(编号) + " 的待办事项。")。
    返回。

  保存待办事项(列表)。

# 清空所有待办事项
段落 清空待办事项：
  保存待办事项([])。
  打印("🗑 已清空所有待办事项。")。

# 搜索待办事项
段落 搜索待办事项 接收 关键词：
  设 列表 为 加载待办事项()。
  设 结果 为 []。

  遍历 事项 于 列表：
    如果 字符串包含(事项["title"], 关键词)：
      列表追加(结果, 事项)。

  如果 列表长度(结果) 等于 0：
    打印("未找到包含 '" + 关键词 + "' 的待办事项。")。
    返回。

  打印("\n===== 搜索结果 (" + 转字符串(列表长度(结果)) + " 项) =====")。
  遍历 事项 于 结果：
    设 状态 为 "✓" 如果 事项["completed"] 否则 "✗"。
    打印(转字符串(事项["id"]) + ". [" + 状态 + "] " + 事项["title"])。

# 显示帮助信息
段落 显示帮助：
  打印("")。
  打印("📋 待办事项管理器 - 使用方法：")。
  打印("")。
  打印("  add <标题>          添加待办事项")。
  打印("  list                列出所有待办事项")。
  打印("  complete <id>       标记为已完成")。
  打印("  delete <id>         删除待办事项")。
  打印("  edit <id> <新标题>  编辑待办事项标题")。
  打印("  search <关键词>     搜索待办事项")。
  打印("  clear               清空所有待办事项")。
  打印("  help                显示此帮助信息")。
  打印("")。

# 主程序
段落 主程序：
  设 参数 为 sys.argv。
  设 参数数量 为 列表长度(参数)。

  如果 参数数量 小于 2：
    显示帮助()。
    返回。

  设 命令 为 参数[1]。

  如果 命令 等于 "add"：
    如果 参数数量 小于 3：
      打印("请指定待办事项标题。")。
      返回。
    设 标题 为 参数[2]。
    添加待办事项(标题)。

  否则若 命令 等于 "list"：
    列出待办事项详细()。

  否则若 命令 等于 "complete"：
    如果 参数数量 小于 3：
      打印("请指定待办事项编号。")。
      返回。
    设 编号 为 转整数(参数[2])。
    完成待办事项(编号)。

  否则若 命令 等于 "delete"：
    如果 参数数量 小于 3：
      打印("请指定待办事项编号。")。
      返回。
    设 编号 为 转整数(参数[2])。
    删除待办事项(编号)。

  否则若 命令 等于 "edit"：
    如果 参数数量 小于 4：
      打印("请指定待办事项编号和新标题。")。
      返回。
    设 编号 为 转整数(参数[2])。
    设 新标题 为 参数[3]。
    编辑待办事项(编号, 新标题)。

  否则若 命令 等于 "search"：
    如果 参数数量 小于 3：
      打印("请指定搜索关键词。")。
      返回。
    设 关键词 为 参数[2]。
    搜索待办事项(关键词)。

  否则若 命令 等于 "clear"：
    清空待办事项()。

  否则若 命令 等于 "help"：
    显示帮助()。

  否则：
    打印("未知命令: " + 命令)。
    显示帮助()。

主程序()。