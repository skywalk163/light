# 光明 C FFI 示例 3：综合FFI使用
# 演示结构体、回调、类型映射等高级特性

加载库 "libc.so.6" 为 系统库。

外部 段落 获取时间 为 "time" 接收 甲 为 空 返回 整数 在 系统库。

段落 主程序：
    打印("=== 光明 C FFI 综合示例 ===")。
    打印("")。
    
    打印("1. 类型映射：光明类型 ↔ C类型")。
    打印("   整数 ↔ int")。
    打印("   小数 ↔ double")。
    打印("   文本 ↔ char*")。
    打印("   布尔 ↔ bool")。
    打印("   空 ↔ void*")。
    打印("")。
    
    打印("2. 调用系统时间函数：")。
    设 当前时间 为 获取时间()。
    打印("   当前 Unix 时间戳：" + 字符串(当前时间))。
    。

主程序()。