# 光明自举示例 - 表达式求值器
# 演示如何用光明编写编译器的表达式求值组件

打印 "=== 光明自举示例：表达式求值器 ==="

# 简单的表达式求值器（支持四则运算）
段落 求值表达式 接收 表达式：
    """将中缀表达式字符串转换为值"""
    
    # 去除空格
    设 表达式 为 表达式.replace(' ', '')
    
    # 解析数字和运算符
    设 数字 为 []
    设 运算符 为 []
    设 当前数字 为 ""
    
    遍历 字符 于 表达式：
        如果 字符.isdigit() 或 字符 == '.'：
            当前数字 等于 当前数字 加 字符
        否则：
            如果 当前数字 != ""：
                如果 '.' 在 当前数字：
                    添加(数字, 转换(当前数字, 浮数))
                否则：
                    添加(数字, 转换(当前数字, 整数))
                当前数字 等于 ""
            添加(运算符, 字符)
    
    # 添加最后一个数字
    如果 当前数字 != ""：
        如果 '.' 在 当前数字：
            添加(数字, 转换(当前数字, 浮数))
        否则：
            添加(数字, 转换(当前数字, 整数))
    
    # 先处理乘除
    设 i 为 0
    当 i < 长度(运算符)：
        如果 运算符[i] 在 ['*', '/']：
            设 a 为 数字[i]
            设 b 为 数字[i + 1]
            如果 运算符[i] == '*'：
                设 结果 为 a 乘 b
            否则：
                设 结果 为 a 除 b
            
            # 更新列表
            删除(数字, i)
            删除(数字, i)
            插入(数字, i, 结果)
            删除(运算符, i)
        否则：
            i 等于 i 加 1
    
    # 处理加减
    设 结果 为 数字[0]
    设 i 为 0
    遍历 操作 于 运算符：
        如果 操作 == '+'：
            结果 等于 结果 加 数字[i + 1]
        否则：
            结果 等于 结果 减 数字[i + 1]
        i 等于 i 加 1
    
    返回 结果

# 测试表达式求值器
设 测试用例 为 [
    {"表达式": "1+2", "预期": 3},
    {"表达式": "10-5*2", "预期": 0},
    {"表达式": "(10+5)*2", "预期": 30},
    {"表达式": "100/25+7", "预期": 11},
    {"表达式": "3.14*2", "预期": 6.28},
]

打印 "测试表达式求值："
遍历 用例 于 测试用例：
    设 表达式 为 用例["表达式"]
    设 预期 为 用例["预期"]
    设 实际 为 求值表达式(表达式)
    设 状态 为 "✓" 如果 实际 == 预期 否则 "✗"
    打印(f"  {状态} {表达式} = {实际} (预期: {预期})")

打印 ""
打印 "=== 表达式求值完成 ==="
