# 段言 - 文本统计分析工具
# 功能: 统计文本的字符数、单词数、行数、中文字符数
# 用法: duan run examples/text_stats/主.duan <文件路径>
# 示例:
#   duan run examples/text_stats/主.duan 主.duan

导入 sys。
导入 文件系统。

段落 统计文本 接收 文本:
  设 总字符数 为 字符串长度(文本)。
  设 行数 为 0。
  设 中文字数 为 0。
  设 词数 为 0。
  设 在词中 为 假。
  设 索引 为 0。
  当 索引 小于 总字符数:
    设 字符 为 字符串获取(文本, 索引)。
    如果 字符 等于 "\n":
      行数 为 行数 + 1。
    如果 字符 大于 " " 且 在词中 等于 假:
      词数 为 词数 + 1。
      在词中 为 真。
    否则若 字符 等于 " " 或 字符 等于 "\n" 或 字符 等于 "\t":
      在词中 为 假。
    索引 为 索引 + 1。
  行数 为 行数 + 1。
  返回 总字符数, 行数, 词数。

段落 主程序:
  设 参数 为 sys.argv。
  如果 列表长度(参数) 小于 2:
    打印("用法: duan run examples/text_stats/主.duan <文件路径>")。
    返回。
  设 文件路径 为 参数[1]。
  如果 文件存在(文件路径) 等于 假:
    打印("错误: 文件不存在 - " + 文件路径)。
    返回。
  设 文本 为 读取文件(文件路径)。
  设 总字符数, 行数, 词数 为 统计文本(文本)。
  打印("")。
  打印("=" * 40)。
  打印("  文本统计报告")。
  打印("=" * 40)。
  打印("  文件: " + 文件路径)。
  打印("  总字符数: " + 转字符串(总字符数))。
  打印("  总行数:   " + 转字符串(行数))。
  打印("  总词数:   " + 转字符串(词数))。
  打印("=" * 40)。

主程序()。