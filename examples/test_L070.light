# L-070 测试：默认参数支持 {} / [] 字典/列表字面量
# 验证：1) 编译通过  2) 多次调用各自独立（方案B：每次调用重新求值）

段落 添加键 接收 选项 等于 {}：
  选项.设置("键", "值")
  返回选项

段落 追加元素 接收 选项 等于 []：
  选项.追加(1)
  返回选项

段落 主：
  设 a 为 添加键()
  设 b 为 添加键()
  打印(a)
  打印(b)
  # 若共享默认参数，b 会有两个键
  如果 b.长度() 大于 1：
    打印("失败：字典默认参数共享了")
  否则：
    打印("通过：字典默认参数各自独立")

  设 c 为 追加元素()
  设 d 为 追加元素()
  打印(c)
  打印(d)
  如果 d.长度() 大于 1：
    打印("失败：列表默认参数共享了")
  否则：
    打印("通过：列表默认参数各自独立")
