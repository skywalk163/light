# 光明 C FFI 示例 1：调用数学库
# 使用 FFI 调用 C 标准数学库中的函数

加载库 "libm.so.6" 为 数学库。

外部 段落 正弦 为 "sin" 接收 甲 为 小数 返回 小数 在 数学库。
外部 段落 余弦 为 "cos" 接收 甲 为 小数 返回 小数 在 数学库。
外部 段落 平方根 为 "sqrt" 接收 甲 为 小数 返回 小数 在 数学库。
外部 段落 绝对值 为 "fabs" 接收 甲 为 小数 返回 小数 在 数学库。
外部 段落 幂 为 "pow" 接收 底 为 小数, 指数 为 小数 返回 小数 在 数学库。

段落 三角计算 接收 角度：小数：
    设 弧度 为 角度 * 3.14159 / 180.0。
    设 正弦值 为 正弦(弧度)。
    设 余弦值 为 余弦(弧度)。
    打印("角度：" + 字符串(角度) + "°")。
    打印("  正弦：" + 字符串(正弦值))。
    打印("  余弦：" + 字符串(余弦值))。
    。

段落 主程序：
    打印("=== 光明 FFI 数学库示例 ===")。
    打印("")。
    
    打印("平方根计算：")。
    设 平方根结果 为 平方根(16.0)。
    打印("  sqrt(16) = " + 字符串(平方根结果))。
    打印("")。
    
    打印("幂运算：")。
    设 幂结果 为 幂(2.0, 10.0)。
    打印("  2^10 = " + 字符串(幂结果))。
    打印("")。
    
    打印("绝对值：")。
    设 绝对值结果 为 绝对值(-3.14)。
    打印("  | -3.14 | = " + 字符串(绝对值结果))。
    打印("")。
    
    打印("三角函数计算：")。
    三角计算(30.0)。
    三角计算(45.0)。
    三角计算(60.0)。
    。

主程序()。