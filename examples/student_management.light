# 光明编程语言 - 示例项目
# 示例3: 学生管理系统

类 学生：
    属性 姓名。
    属性 年龄。
    属性 成绩。

    构造 接收 姓名参数, 年龄参数：
        己姓名 为 姓名参数。
        己年龄 为 年龄参数。
        己成绩 为 []。

    段落 添加成绩 接收 分数：
        列表追加(己成绩, 分数)。

    段落 平均成绩：
        如果 列表长度(己成绩) 等于 0：
            返回 0。

        设 总分 为 0。
        设 i 为 0。
        当 i 小于 列表长度(己成绩)：
            设 总分 为 总分 加 己成绩[i]。
            设 i 为 i 加 1。

        返回 总分 除 转浮点(列表长度(己成绩))。

    段落 打印信息：
        打印("姓名: " + 己姓名)。
        打印("年龄: " + 转字符串(己年龄))。
        打印("平均成绩: " + 转字符串(己平均成绩()))。

# 创建学生并添加成绩
学生1 等于 新建 学生("张三", 18)。
学生1.添加成绩(85)。
学生1.添加成绩(90)。
学生1.添加成绩(78)。

学生2 等于 新建 学生("李四", 19)。
学生2.添加成绩(92)。
学生2.添加成绩(88)。
学生2.添加成绩(95)。

# 打印学生信息
打印("=== 学生信息 ===")。
学生1.打印信息()。
打印("")。
学生2.打印信息()。
