# 光明编程语言 - 示例项目
# 示例3: 学生管理系统

《学生》类:
    定义 姓名 等于 ""。
    定义 年龄 等于 0。
    定义 成绩 等于 []。
    
    《初始化》方法(姓名参数, 年龄参数):
        姓名 等于 姓名参数。
        年龄 等于 年龄参数。
    结束。
    
    《添加成绩》方法(分数):
        列表追加(成绩, 分数)。
    结束。
    
    《平均成绩》方法():
        如果 列表长度(成绩) 等于 0 那么:
            返回 0。
        结束。
        
        定义 总分 等于 0。
        定义 i 等于 0。
        当 i 小于 列表长度(成绩):
            总分 等于 总分 加 成绩[i]。
            i 等于 i 加 1。
        结束。
        
        返回 总分 除 列表长度(成绩)。
    结束。
    
    《打印信息》方法():
        打印("姓名: " + 姓名)。
        打印("年龄: " + 转字符串(年龄))。
        打印("平均成绩: " + 转字符串(平均成绩()))。
    结束。
结束。

# 创建学生并添加成绩
定义 学生1 等于 新建 学生("张三", 18)。
学生1之添加成绩(85)。
学生1之添加成绩(90)。
学生1之添加成绩(78)。

定义 学生2 等于 新建 学生("李四", 19)。
学生2之添加成绩(92)。
学生2之添加成绩(88)。
学生2之添加成绩(95)。

# 打印学生信息
打印("=== 学生信息 ===")。
学生1之打印信息()。
打印("")。
学生2之打印信息()。