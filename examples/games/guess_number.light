# 段言 - 猜数字游戏
# 电脑随机生成一个数字，用户猜测
# 用法: duan run examples/games/guess_number.duan

# 主游戏函数
段落 猜数字游戏：
  打印("===== 猜数字游戏 =====")。
  打印("我已经想好了一个 1 到 100 之间的数字。")。
  打印("你能猜出来吗？")。

  # 随机生成 1 到 100 之间的数字
  设 目标数字 为 随机整数(1, 100)。
  设 尝试次数 为 0。
  设 游戏结束 为 假。

  当 游戏结束 等于 假：
    打印("\n请输入你的猜测 (1-100): ")。
    设 输入文本 为 input()。
    设 猜测 为 转整数(输入文本)。
    尝试次数 为 尝试次数 + 1。

    如果 猜测 等于 目标数字：
      打印("恭喜你！猜对了！")。
      打印("你一共猜了 " + 转字符串(尝试次数) + " 次。")。
      游戏结束 为 真。
    否则若 猜测 小于 目标数字：
      打印("太小了！再大一点！")。
    否则：
      打印("太大了！再小一点！")。

  # 根据尝试次数给出评价
  打印("\n===== 游戏总结 =====")。
  如果 尝试次数 小于等于 3：
    打印("太厉害了！你是个天才！")。
  否则若 尝试次数 小于等于 7：
    打印("不错！你很擅长这个游戏！")。
  否则若 尝试次数 小于等于 15：
    打印("还可以，继续加油！")。
  否则：
    打印("看来运气不太好，再试一次吧！")。

  # 询问是否再来一局
  打印("\n再来一局？(y/n): ")。
  设 再来 为 input()。
  如果 再来 等于 "y" 或 再来 等于 "Y" 或 再来 等于 "yes"：
    猜数字游戏()。

# 运行游戏
猜数字游戏()。