# 段言 - 贪吃蛇游戏（文本终端版）
# 使用键盘 WASD 控制方向
# 用法: duan run examples/games/snake.duan

# 游戏常量
设 宽度 为 20。
设 高度 为 15。
设 初始长度 为 3。

# 方向常量
设 方向上 为 0。
设 方向下 为 1。
设 方向左 为 2。
设 方向右 为 3。

# 游戏状态
设 蛇身 为 []。
设 食物 为 {"x": 0, "y": 0}。
设 方向 为 方向右。
设 分数 为 0。
设 游戏结束 为 假。

# 初始化游戏
段落 初始化游戏：
  蛇身 为 []。
  设 i 为 0。
  当 i 小于 初始长度：
    设 段 为 {"x": 初始长度 - 1 - i, "y": 高度 / 2}。
    列表追加(蛇身, 段)。
    i 为 i + 1。

  方向 为 方向右。
  分数 为 0。
  游戏结束 为 假。
  生成食物()。

# 生成随机食物
段落 生成食物：
  设 食物位置 为 假。
  当 食物位置 等于 假：
    设 fx 为 随机整数(0, 宽度 - 1)。
    设 fy 为 随机整数(0, 高度 - 1)。
    设 冲突 为 假。

    遍历 段 于 蛇身：
      如果 段["x"] 等于 fx 且 段["y"] 等于 fy：
        冲突 为 真。
        跳出。

    如果 冲突 等于 假：
      食物["x"] 为 fx。
      食物["y"] 为 fy。
      食物位置 为 真。

# 移动蛇
段落 移动蛇：
  设 蛇头 为 蛇身[0]。
  设 新头 为 {"x": 蛇头["x"], "y": 蛇头["y"]}。

  如果 方向 等于 方向上：
    新头["y"] 为 新头["y"] - 1。
  否则若 方向 等于 方向下：
    新头["y"] 为 新头["y"] + 1。
  否则若 方向 等于 方向左：
    新头["x"] 为 新头["x"] - 1。
  否则若 方向 等于 方向右：
    新头["x"] 为 新头["x"] + 1。

  # 检查墙壁碰撞
  如果 新头["x"] 小于 0 或 新头["x"] 大于等于 宽度 或 新头["y"] 小于 0 或 新头["y"] 大于等于 高度：
    游戏结束 为 真。
    返回。

  # 检查自身碰撞
  遍历 段 于 蛇身：
    如果 段["x"] 等于 新头["x"] 且 段["y"] 等于 新头["y"]：
      游戏结束 为 真。
      返回。

  # 插入新头
  列表插入(蛇身, 0, 新头)。

  # 检查是否吃到食物
  如果 新头["x"] 等于 食物["x"] 且 新头["y"] 等于 食物["y"]：
    分数 为 分数 + 10。
    生成食物()。
  否则：
    列表弹出(蛇身)。

# 绘制游戏画面
段落 绘制画面：
  # 清屏
  打印("\n")。

  # 绘制顶部边框
  设 顶部边框 为 "+"。
  设 i 为 0。
  当 i 小于 宽度：
    顶部边框 为 顶部边框 + "-"。
    i 为 i + 1。
  顶部边框 为 顶部边框 + "+"。
  打印(顶部边框)。

  # 绘制游戏区域
  设 y 为 0。
  当 y 小于 高度：
    设 行 为 "|"。
    设 x 为 0。
    当 x 小于 宽度：
      设 是蛇身 为 假。
      设 是蛇头 为 假。

      遍历 段 于 蛇身：
        如果 段["x"] 等于 x 且 段["y"] 等于 y：
          如果 段 等于 蛇身[0]：
            是蛇头 为 真。
          否则：
            是蛇身 为 真。
          跳出。

      如果 是蛇头：
        行 为 行 + "@"。
      否则若 是蛇身：
        行 为 行 + "O"。
      否则若 食物["x"] 等于 x 且 食物["y"] 等于 y：
        行 为 行 + "*"。
      否则：
        行 为 行 + " "。
      x 为 x + 1。
    行 为 行 + "|"。
    打印(行)。
    y 为 y + 1。

  # 绘制底部边框
  设 底部边框 为 "+"。
  设 i 为 0。
  当 i 小于 宽度：
    底部边框 为 底部边框 + "-"。
    i 为 i + 1。
  底部边框 为 底部边框 + "+"。
  打印(底部边框)。

  # 显示分数
  打印("分数: " + 转字符串(分数))。
  打印("操作: W=上 S=下 A=左 D=右 Q=退出")。

# 处理输入
段落 处理输入 接收 键：
  如果 键 等于 "w" 或 键 等于 "W"：
    如果 方向 不等于 方向下：
      方向 为 方向上。
  否则若 键 等于 "s" 或 键 等于 "S"：
    如果 方向 不等于 方向上：
      方向 为 方向下。
  否则若 键 等于 "a" 或 键 等于 "A"：
    如果 方向 不等于 方向右：
      方向 为 方向左。
  否则若 键 等于 "d" 或 键 等于 "D"：
    如果 方向 不等于 方向左：
      方向 为 方向右。
  否则若 键 等于 "q" 或 键 等于 "Q"：
    游戏结束 为 真。

# 主游戏循环
段落 运行游戏：
  初始化游戏()。

  打印("===== 贪吃蛇游戏 =====")。
  打印("使用 WASD 控制蛇的移动方向。")。
  打印("吃到食物(*)可以增加长度和分数。")。
  打印("撞到墙壁或自己则游戏结束。")。
  打印("按 Enter 键开始游戏...")。
  input()。

  当 游戏结束 等于 假：
    绘制画面()。
    移动蛇()。

    如果 游戏结束：
      跳出。

    打印("输入方向 (W/A/S/D) 或 Q 退出: ")。
    设 输入键 为 input()。
    处理输入(输入键)。

  # 游戏结束
  打印("\n===== 游戏结束 =====")。
  打印("最终分数: " + 转字符串(分数))。

  # 根据分数给出评价
  如果 分数 大于等于 100：
    打印("太厉害了！你是贪吃蛇大师！")。
  否则若 分数 大于等于 50：
    打印("不错的表现！继续加油！")。
  否则：
    打印("再试一次吧！")。

  # 询问是否再来一局
  打印("\n再来一局？(y/n): ")。
  设 再来 为 input()。
  如果 再来 等于 "y" 或 再来 等于 "Y"：
    运行游戏()。

运行游戏()。