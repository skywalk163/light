# 段言 - 猜数字游戏
# 功能: 电脑随机生成一个 1-100 的数字，玩家通过提示猜出正确答案
# 用法: duan run examples/games/guess_number/主.duan

# ===== 游戏常量 =====
设 最小值 为 1。
设 最大值 为 100。
设 最大尝试次数 为 10。

# ===== 核心游戏逻辑 =====

# 生成随机目标数字
段落 生成目标数字:
  返回 随机整数(最小值, 最大值)。

# 处理玩家猜测，返回提示信息
段落 处理猜测 接收 目标, 猜测:
  如果 猜测 等于 目标:
    返回 "正确"。
  否则若 猜测 小于 目标:
    返回 "太小"。
  否则:
    返回 "太大"。

# 根据尝试次数给出评价
段落 给出评价 接收 尝试次数:
  如果 尝试次数 小于等于 3:
    返回 "太厉害了！你是天才！"。
  否则若 尝试次数 小于等于 5:
    返回 "很棒！直觉很准！"。
  否则若 尝试次数 小于等于 8:
    返回 "不错哦，耐心就是胜利！"。
  否则:
    返回 "总算猜对了，再接再厉！"。

# 显示游戏标题
段落 显示标题:
  打印("")。
  打印("╔══════════════════════════════╗")。
  打印("║       🎯 猜数字游戏         ║")。
  打印("╚══════════════════════════════╝")。
  打印("")。
  打印("我已经想好了一个 " + 转字符串(最小值) + " 到 " + 转字符串(最大值) + " 之间的数字。")。
  打印("你有 " + 转字符串(最大尝试次数) + " 次机会猜中它！")。
  打印("")。

# 运行一局游戏
段落 运行一局:
  设 目标数字 为 生成目标数字()。
  设 尝试次数 为 0。
  设 游戏进行中 为 真。

  显示标题()。

  当 游戏进行中:
    尝试次数 为 尝试次数 + 1。
    打印("第 " + 转字符串(尝试次数) + " 次猜测（剩余 " + 转字符串(最大尝试次数 - 尝试次数 + 1) + " 次）: ")。
    设 输入文本 为 input()。
    设 猜测 为 转整数(输入文本)。

    设 结果 为 处理猜测(目标数字, 猜测)。
    如果 结果 等于 "正确":
      打印("🎉 恭喜你猜对了！数字就是 " + 转字符串(目标数字))。
      打印("你用了 " + 转字符串(尝试次数) + " 次猜中。")。
      打印("评价: " + 给出评价(尝试次数))。
      游戏进行中 为 假。
    否则若 结果 等于 "太小":
      打印("📈 太小了！再大一点！")。
    否则:
      打印("📉 太大了！再小一点！")。

    # 超过最大尝试次数
    如果 游戏进行中 且 尝试次数 大于等于 最大尝试次数:
      打印("")。
      打印("😅 很遗憾，次数用完了！")。
      打印("正确答案是: " + 转字符串(目标数字))。
      游戏进行中 为 假。

# 显示游戏统计
段落 显示统计 接收 总局数, 总尝试次数, 最高分:
  打印("")。
  打印("╔══════════════════════════════╗")。
  打印("║        📊 游戏统计           ║")。
  打印("╚══════════════════════════════╝")。
  打印("总局数: " + 转字符串(总局数))。
  打印("总尝试次数: " + 转字符串(总尝试次数))。
  如果 总局数 大于 0:
    设 平均次数 为 总尝试次数 / 总局数。
    打印("平均每局: " + 转字符串(平均次数) + " 次")。
  打印("最佳成绩: " + 转字符串(最高分) + " 次猜中")。
  打印("")。

# ===== 主程序 =====
段落 主程序:
  设 总局数 为 0。
  设 总尝试次数 为 0。
  设 最高分 为 999。
  设 继续游戏 为 真。

  当 继续游戏:
    设 本次尝试 为 0。

    # 先定义段言函数捕获尝试次数
    段落 内部运行:
      设 目标数字 为 生成目标数字()。
      设 尝试次数 为 0。
      设 游戏进行中 为 真。

      显示标题()。

      当 游戏进行中:
        尝试次数 为 尝试次数 + 1。
        打印("第 " + 转字符串(尝试次数) + " 次猜测（剩余 " + 转字符串(最大尝试次数 - 尝试次数 + 1) + " 次）: ")。
        设 输入文本 为 input()。
        设 猜测 为 转整数(输入文本)。

        设 结果 为 处理猜测(目标数字, 猜测)。
        如果 结果 等于 "正确":
          打印("🎉 恭喜你猜对了！数字就是 " + 转字符串(目标数字))。
          打印("你用了 " + 转字符串(尝试次数) + " 次猜中。")。
          打印("评价: " + 给出评价(尝试次数))。
          游戏进行中 为 假。
        否则若 结果 等于 "太小":
          打印("📈 太小了！再大一点！")。
        否则:
          打印("📉 太大了！再小一点！")。

        如果 游戏进行中 且 尝试次数 大于等于 最大尝试次数:
          打印("")。
          打印("😅 很遗憾，次数用完了！")。
          打印("正确答案是: " + 转字符串(目标数字))。
          游戏进行中 为 假。

      返回 尝试次数。

    本次尝试 为 内部运行()。

    总局数 为 总局数 + 1。
    总尝试次数 为 总尝试次数 + 本次尝试。
    如果 本次尝试 小于 最高分:
      最高分 为 本次尝试。

    显示统计(总局数, 总尝试次数, 最高分)。

    打印("再来一局？(y/n): ")。
    设 输入 为 input()。
    如果 输入 不等于 "y" 且 输入 不等于 "Y" 且 输入 不等于 "yes":
      继续游戏 为 假。
      打印("")。
      打印("感谢游玩！再见！👋")。
      打印("")。

主程序()。