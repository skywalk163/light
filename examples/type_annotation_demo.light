# 光明类型注解示例
# 演示光明的类型注解语法和用法

打印 "=== 光明类型注解示例 ==="
打印 ""

# 变量类型注解
打印 "1. 变量类型注解："
设 x 为 整数 = 10
设 y 为 小数 = 3.14
设 z 为 文本 = "hello"
设 标志 为 布尔 = 真
设 列表 为 列 = [1, 2, 3]
设 映射 为 典 = {'名字': '张三', '年龄': 25}

打印(f"  x: {x} (整数)")
打印(f"  y: {y} (小数)")
打印(f"  z: {z} (文本)")
打印(f"  标志: {标志} (布尔)")
打印(f"  列表: {列表} (列表)")
打印(f"  映射: {映射} (字典)")

打印 ""
打印 "2. 函数类型注解："

# 函数参数和返回类型注解
段落 加法 接收 a 整数, b 整数 返回 整数：
    返回 a 加 b

段落 计算面积 接收 半径 小数 返回 小数：
    设 π 为 3.14159
    返回 π 乘 半径 乘 半径

段落 格式化名字 接收 姓 文本, 名 文本 返回 文本：
    返回 姓 加 " " 加 名

设 和 为 加法(5, 3)
设 面积 为 计算面积(2.5)
设 全名 为 格式化名字("张", "三")

打印(f"  加法(5, 3) = {和}")
打印(f"  计算面积(2.5) = {面积}")
打印(f"  格式化名字('张', '三') = '{全名}'")

打印 ""
打印 "3. 混合类型注解："

段落 处理数据 接收 数据 任意 返回 文本：
    """处理任意类型的数据"""
    如果 类型(数据) == 'int'：
        返回 f"整数: {数据}"
    如果 类型(数据) == 'float'：
        返回 f"小数: {数据}"
    如果 类型(数据) == 'str'：
        返回 f"文本: '{数据}'"
    返回 f"其他类型: {类型(数据)}"

打印(f"  处理数据(42) = {处理数据(42)}")
打印(f"  处理数据(3.14) = {处理数据(3.14)}")
打印(f"  处理数据('hello') = {处理数据('hello')}")

打印 ""
打印 "=== 类型注解示例完成 ==="
