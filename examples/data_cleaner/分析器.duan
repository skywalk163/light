# 段言 - 数据分析模块
# 提供数据统计分析和报告生成功能

从 数学 导入 平方根。

# 导出模块接口
导出 计算基本统计, 计算频次统计, 计算数值分布, 计算相关系数, 输出分析报告, 输出频次报告, 输出相关性报告, 输出直方图。

# ===== 1. 基本统计 =====
段落 计算基本统计(数据, 表头):
  设 统计结果 为 {}。
  设 总行数 为 列表长度(数据)。
  遍历 列名 于 表头:
    设 非空计数 为 0。
    设 数值总和 为 0。
    设 数值列表 为 []。
    设 是数值列 为 真。
    设 文本值 为 {}。
    遍历 记录 于 数据:
      如果 字典包含键(记录, 列名) 那么:
        设 值 为 记录[列名]。
        如果 值 不等于 空 且 值 不等于 "" 那么:
          非空计数 为 非空计数 + 1。
          尝试:
            设 数值 为 转浮点(值)。
            如果 数值 不等于 空 那么:
              数值总和 为 数值总和 + 数值。
              列表追加(数值列表, 数值)。
            结束。
          捕获:
            是数值列 为 假。
            如果 字典包含键(文本值, 值) 等于 假 那么:
              字典设置(文本值, 值, 0)。
            结束。
            字典设置(文本值, 值, 文本值[值] + 1)。
          结束。
        结束。
      结束。
    结束。
    设 列统计 为 {}。
    字典设置(列统计, "非空数", 非空计数)。
    字典设置(列统计, "空值数", 总行数 - 非空计数)。
    如果 是数值列 且 列表长度(数值列表) 大于 0 那么:
      设 数值个数 为 列表长度(数值列表)。
      字典设置(列统计, "类型", "数值")。
      设 均值 为 数值总和 / 数值个数。
      字典设置(列统计, "均值", 均值)。
      设 排序后 为 排序(数值列表)。
      如果 数值个数 % 2 等于 1 那么:
        字典设置(列统计, "中位数", 排序后[数值个数 // 2])。
      否则:
        设 中位 为 (排序后[数值个数 // 2 - 1] + 排序后[数值个数 // 2]) / 2。
        字典设置(列统计, "中位数", 中位)。
      结束。
      设 最大值 为 排序后[数值个数 - 1]。
      设 最小值 为 排序后[0]。
      字典设置(列统计, "最大值", 最大值)。
      字典设置(列统计, "最小值", 最小值)。
      如果 数值个数 大于 1 那么:
        设 方差和 为 0。
        设 i 为 0。
        当 i 小于 数值个数:
          设 差 为 数值列表[i] - 均值。
          方差和 为 方差和 + 差 * 差。
          i 为 i + 1。
        结束。
        设 方差值 为 方差和 / 数值个数。
        字典设置(列统计, "方差", 方差值)。
        字典设置(列统计, "标准差", 平方根(方差值))。
      结束。
    否则:
      字典设置(列统计, "类型", "文本")。
      设 键列表 为 字典键列表(文本值)。
      设 唯一值数 为 列表长度(键列表)。
      字典设置(列统计, "唯一值数", 唯一值数)。
      设 最频繁值 为 ""。
      设 最频繁计数 为 0。
      遍历 键 于 键列表:
        如果 文本值[键] 大于 最频繁计数 那么:
          最频繁值 为 键。
          最频繁计数 为 文本值[键]。
        结束。
      结束。
      字典设置(列统计, "最频繁值", 最频繁值)。
      字典设置(列统计, "最频繁计数", 最频繁计数)。
    结束。
    字典设置(统计结果, 列名, 列统计)。
  结束。
  返回 统计结果。
结束。

# ===== 2. 频次统计 =====
段落 计算频次统计(数据, 列名):
  设 频次 为 {}。
  设 总计数 为 0。
  遍历 记录 于 数据:
    如果 字典包含键(记录, 列名) 那么:
      设 值 为 记录[列名]。
      如果 值 不等于 空 且 值 不等于 "" 那么:
        如果 字典包含键(频次, 值) 等于 假 那么:
          字典设置(频次, 值, 0)。
        结束。
        字典设置(频次, 值, 频次[值] + 1)。
        总计数 为 总计数 + 1。
      结束。
    结束。
  结束。
  返回 频次, 总计数。
结束。

# ===== 3. 数值分布（直方图） =====
段落 计算数值分布(数据, 列名, 分组量):
  设 数值列表 为 []。
  设 最小值 为 空。
  设 最大值 为 空。
  遍历 记录 于 数据:
    如果 字典包含键(记录, 列名) 那么:
      尝试:
        设 值 为 转浮点(记录[列名])。
        如果 值 不等于 空 那么:
          列表追加(数值列表, 值)。
          如果 最小值 等于 空 或 值 小于 最小值 那么:
            最小值 为 值。
          结束。
          如果 最大值 等于 空 或 值 大于 最大值 那么:
            最大值 为 值。
          结束。
        结束。
      捕获:
        跳过。
      结束。
    结束。
  结束。
  如果 列表长度(数值列表) 等于 0 那么:
    返回 {}, 0, 0。
  结束。
  如果 分组量 等于 空 或 分组量 小于 1 那么:
    分组量 为 10。
  结束。
  设 区间宽度 为 (最大值 - 最小值) / 分组量。
  如果 区间宽度 等于 0 那么:
    区间宽度 为 1。
  结束。
  设 区间 为 {}。
  设 i 为 0。
  当 i 小于 分组量:
    设 下限 为 最小值 + i * 区间宽度。
    设 上限 为 下限 + 区间宽度。
    设 区间名 为 转字符串(下限) + "-" + 转字符串(上限)。
    字典设置(区间, 区间名, 0)。
    i 为 i + 1。
  结束。
  遍历 值 于 数值列表:
    设 索引 为 转整数((值 - 最小值) / 区间宽度)。
    如果 索引 大于等于 分组量 那么:
      索引 为 分组量 - 1。
    结束。
    设 区间键列表 为 字典键列表(区间)。
    设 区间名 为 区间键列表[索引]。
    字典设置(区间, 区间名, 区间[区间名] + 1)。
  结束。
  返回 区间, 最小值, 最大值。
结束。

# ===== 4. 相关性分析 =====
段落 计算相关系数(数据, 字段X, 字段Y):
  设 配对值 为 []。
  设 有效对 为 0。
  遍历 记录 于 数据:
    如果 字典包含键(记录, 字段X) 且 字典包含键(记录, 字段Y) 那么:
      尝试:
        设 值X 为 转浮点(记录[字段X])。
        设 值Y 为 转浮点(记录[字段Y])。
        如果 值X 不等于 空 且 值Y 不等于 空 那么:
          列表追加(配对值, [值X, 值Y])。
          有效对 为 有效对 + 1。
        结束。
      捕获:
        跳过。
      结束。
    结束。
  结束。
  如果 有效对 小于 3 那么:
    返回 0, 有效对。
  结束。
  设 总和X 为 0。
  设 总和Y 为 0。
  遍历 当前对 于 配对值:
    总和X 为 总和X + 当前对[0]。
    总和Y 为 总和Y + 当前对[1]。
  结束。
  设 均值X 为 总和X / 有效对。
  设 均值Y 为 总和Y / 有效对。
  设 协方差 为 0。
  设 方差X 为 0。
  设 方差Y 为 0。
  遍历 当前对 于 配对值:
    设 差X 为 当前对[0] - 均值X。
    设 差Y 为 当前对[1] - 均值Y。
    协方差 为 协方差 + 差X * 差Y。
    方差X 为 方差X + 差X * 差X。
    方差Y 为 方差Y + 差Y * 差Y。
  结束。
  如果 方差X 等于 0 或 方差Y 等于 0 那么:
    返回 0, 有效对。
  结束。
  设 相关系数 为 协方差 / (平方根(方差X) * 平方根(方差Y))。
  返回 相关系数, 有效对。
结束。

# ===== 5. 输出分析报告 =====
段落 输出分析报告(统计结果, 数据, 表头):
  打印("\n" + "=" * 60)。
  打印("  数据统计分析报告")。
  打印("=" * 60)。
  打印("  总记录数: " + 转字符串(列表长度(数据)))。
  打印("  总字段数: " + 转字符串(列表长度(表头)))。
  打印("  字段列表: " + 连接字符串(表头, ", "))。
  打印("=" * 60)。
  遍历 列名 于 表头:
    如果 字典包含键(统计结果, 列名) 那么:
      设 统计 为 统计结果[列名]。
      打印("\n  [字段: " + 列名 + "]")。
      打印("    类型: " + 统计["类型"])。
      打印("    非空值: " + 转字符串(统计["非空数"]) + " / " + 转字符串(列表长度(数据)))。
      打印("    空值数: " + 转字符串(统计["空值数"]))。
      如果 统计["类型"] 等于 "数值" 那么:
        打印("    均值: " + 转字符串(统计["均值"]))。
        打印("    中位数: " + 转字符串(统计["中位数"]))。
        打印("    最大值: " + 转字符串(统计["最大值"]))。
        打印("    最小值: " + 转字符串(统计["最小值"]))。
        如果 字典包含键(统计, "标准差") 那么:
          打印("    标准差: " + 转字符串(统计["标准差"]))。
        结束。
        如果 字典包含键(统计, "方差") 那么:
          打印("    方差: " + 转字符串(统计["方差"]))。
        结束。
      否则:
        打印("    唯一值数: " + 转字符串(统计["唯一值数"]))。
        打印("    最频繁值: " + 统计["最频繁值"] + " (" + 转字符串(统计["最频繁计数"]) + " 次)")。
      结束。
    结束。
  结束。
  打印("\n" + "=" * 60)。
  打印("  分析完成")。
  打印("=" * 60)。
结束。

段落 输出频次报告(频次, 总计数, 列名):
  打印("\n  [频次统计: " + 列名 + "]")。
  打印("    总计: " + 转字符串(总计数) + " 个值")。
  打印("    " + "-" * 40)。
  设 键列表 为 字典键列表(频次)。
  设 排序键 为 排序(键列表)。
  遍历 键 于 排序键:
    设 百分比 为 频次[键] / 总计数 * 100。
    打印("    " + 键 + ": " + 转字符串(频次[键]) + " 次 (" + 转字符串(百分比) + "%)")。
  结束。
结束。

段落 输出相关性报告(相关系数, 有效对, 字段X, 字段Y):
  打印("  [相关性: " + 字段X + " vs " + 字段Y + "]")。
  打印("    有效样本数: " + 转字符串(有效对))。
  打印("    相关系数: " + 转字符串(相关系数))。
  如果 相关系数 大于 0.7 那么:
    打印("    相关性强度: 强正相关")。
  否则若 相关系数 大于 0.3 那么:
    打印("    相关性强度: 弱正相关")。
  否则若 相关系数 大于 -0.3 那么:
    打印("    相关性强度: 无明显相关性")。
  否则若 相关系数 大于 -0.7 那么:
    打印("    相关性强度: 弱负相关")。
  否则:
    打印("    相关性强度: 强负相关")。
  结束。
结束。

段落 输出直方图(区间, 最小值, 最大值, 列名):
  打印("\n  [数值分布: " + 列名 + "]")。
  打印("    范围: " + 转字符串(最小值) + " 到 " + 转字符串(最大值))。
  打印("    " + "-" * 40)。
  设 最大计数 为 0。
  设 区间键 为 字典键列表(区间)。
  遍历 键 于 区间键:
    如果 区间[键] 大于 最大计数 那么:
      最大计数 为 区间[键]。
    结束。
  结束。
  设 最大条长 为 40。
  遍历 键 于 区间键:
    设 条长 为 转整数(区间[键] / 最大计数 * 最大条长)。
    设 条形 为 ""。
    设 i 为 0。
    当 i 小于 条长:
      条形 为 条形 + "#"。
      i 为 i + 1。
    结束。
    打印("    " + 键 + ": " + 条形 + " (" + 转字符串(区间[键]) + ")")。
  结束。
结束。