# 静态属性和方法示例
# 展示类级别的变量和函数

类 学生管理：
    静态 属性 学生总数 等于 零。
    静态 属性 学校名称 等于 "光明学院"。

    属性 姓名。
    属性 学号。

    构造 接收 姓名, 学号：
        己姓名 为 姓名。
        己学号 为 学号。
        学生管理.学生总数 等于 学生管理.学生总数 加 一。

    段落 显示信息：
        打印 "姓名：" 加 己姓名 加 "，学号：" 加 己学号。

    静态 段落 获取总数：
        返回 学生管理.学生总数。

    静态 段落 获取学校名称：
        返回 学生管理.学校名称。

# 创建学生
学生1 等于 新建 学生管理("张三", "2024001")。
学生2 等于 新建 学生管理("李四", "2024002")。
学生3 等于 新建 学生管理("王五", "2024003")。

# 显示每个学生的信息
学生1.显示信息()。
学生2.显示信息()。
学生3.显示信息()。

# 通过静态方法获取总数
打印 "学生总数：" 加 转字符串(学生管理.获取总数())。
打印 "学校名称：" 加 学生管理.获取学校名称()。
