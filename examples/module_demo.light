# 光明编程语言 - 示例项目
# 示例4: 模块使用演示

# 定义工具函数（代替模块导入，兼容 src 后端）
段落 求和 接收 数字列表:
    设 总和 为 0。
    设 i 为 0。
    当 i 小于 列表长度(数字列表):
        设 总和 为 总和 加 数字列表[i]。
        设 i 为 i 加 1。
    结束。
    返回 总和。
结束。

段落 平均值 接收 数字列表:
    返回 求和(数字列表) 除 转浮点(列表长度(数字列表))。
结束。

# 使用数学工具
设 数字列表 为 [1, 2, 3, 4, 5]。
设 总和 为 求和(数字列表)。
设 平均 为 平均值(数字列表)。

打印("=== 数学工具演示 ===")。
打印("列表: [1, 2, 3, 4, 5]")。
打印("总和: " + 转字符串(总和))。
打印("平均值: " + 转字符串(平均))。

打印("")。
打印("模块导入功能在 ANTLR 后端中可用（使用 --backend antlr）。")。