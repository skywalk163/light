# 光明编程语言 - 示例项目
# 示例4: 模块使用演示

# 定义工具函数（代替模块导入，兼容 src 后端）
《求和》段(数字列表):
    定义 总和 等于 0。
    定义 i 等于 0。
    当 i 小于 列表长度(数字列表):
        总和 等于 总和 加 数字列表[i]。
        i 等于 i 加 1。
    结束。
    返回 总和。
结束。

《平均值》段(数字列表):
    返回 《求和》(数字列表) 除 列表长度(数字列表)。
结束。

# 使用数学工具
定义 数字列表 等于 [1, 2, 3, 4, 5]。
定义 总和 等于 《求和》(数字列表)。
定义 平均 等于 《平均值》(数字列表)。

打印("=== 数学工具演示 ===")。
打印("列表: [1, 2, 3, 4, 5]")。
打印("总和: " + 转字符串(总和))。
打印("平均值: " + 转字符串(平均))。

打印("")。
打印("模块导入功能在 ANTLR 后端中可用（使用 --backend antlr）。")。