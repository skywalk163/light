# 类型检查级别: 表达式

# =============================================================================
# Z7：类与接口类型标注
# 展示光明类型系统在面向对象编程中的应用
# 包括：类属性类型标注、方法类型标注、接口实现检查
# =============================================================================

# 基类：带类型标注的属性
类 学生：
    属性 姓名。
    属性 年龄。
    属性 成绩。

    构造 接收 姓名, 年龄：
        己姓名 为 姓名。
        己年龄 为 年龄。
        己成绩 为 【】。

    段落 添加成绩 接收 分数：
        列表追加(己成绩, 分数)

    段落 平均分 返回 浮点：
        若 长(己成绩) == 0：
            返回 0.0
        设 总和: 整数 为 0
        遍 分 于 己成绩：
            总和 = 总和 + 分
        返回 总和 / 长(己成绩)

    段落 信息 返回 字符串：
        返回 己姓名 + "，年龄" + 转字符串(己年龄) + "，平均分" + 转字符串(己平均分())

# 派生类：继承学生类
类 研究生 继承 学生：
    属性 导师。
    属性 论文题目。

    构造 接收 姓名, 年龄, 导师：
        父.构造(姓名, 年龄)。
        己导师 为 导师。
        己论文题目 为 空。

    段落 设置论文 接收 题目：
        己论文题目 为 题目。

段落 主():
    # 创建学生对象
    设 s1: 学生 为 新建 学生("张三", 20)
    打印(s1.信息())

    s1.添加成绩(95)
    s1.添加成绩(87)
    s1.添加成绩(92)
    打印("添加成绩后: " + s1.信息())

    # 创建研究生对象
    设 g1: 研究生 为 新建 研究生("李四", 25, "王教授")
    g1.添加成绩(88)
    g1.添加成绩(91)
    g1.设置论文("光明语言设计")
    打印("研究生: " + g1.信息())
    打印("导师: " + g1.导师)

    若 g1.论文题目 != 空：
        打印("论文: " + g1.论文题目)

段落 主()