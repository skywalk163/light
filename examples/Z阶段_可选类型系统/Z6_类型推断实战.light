# 类型检查级别: 表达式

# =============================================================================
# Z6：类型推断实战
# 展示光明类型推断器（TypeInferencer）的 HM 风格类型推断能力
# 包括：返回值推断、条件类型推断、空安全类型推断
# =============================================================================

# 类型推断：根据运算结果推断返回类型
段落 计算均值(数据: 列表<浮点>) 返回 浮点:
    设 总和: 浮点 为 0.0
    遍 项 于 数据:
        总和 = 总和 + 项
    返回 总和 / 长(数据)

# 多态函数：返回类型取决于参数类型
段落 加倍(x: 整数|浮点) 返回 整数|浮点:
    返回 x + x

# 条件类型推断：根据条件分支推断返回类型
段落 安全获取(数据: 列表<整数>, 位置: 整数) 返回 整数|字符串:
    若 位置 < 0:
        返回 "位置不能为负数"
    若 位置 >= 长(数据):
        返回 "位置超出范围"
    返回 数据[位置]

# 空安全类型推断
段落 查找用户(名称: 字符串) 返回 可空字符串:
    若 名称 == "张三":
        返回 "找到用户：张三"
    若 名称 == "李四":
        返回 "找到用户：李四"
    返回 空

# 类型推断：列表元素类型推导
段落 筛选正整数(数据: 列表<整数>) 返回 列表<整数>:
    设 结果: 列表<整数> 为 【】
    遍 项 于 数据:
        若 项 > 0:
            列表追加(结果, 项)
    返回 结果

# 类型推断：列表求和
段落 列表求和(数据: 列表<整数>) 返回 整数:
    设 总和: 整数 为 0
    遍 项 于 数据:
        总和 = 总和 + 项
    返回 总和

# 类型推断：字典值提取
段落 提取值(数据: 字典<字符串, 整数>, 键: 字符串) 返回 整数|字符串:
    若 键 == "不存在":
        返回 "键不存在"
    返回 数据[键]

段落 主():
    # 浮点均值
    设 浮点列表: 列表<浮点> 为 【1.5, 2.5, 3.0, 4.0】
    设 均值: 浮点 为 计算均值(浮点列表)
    打印("均值 = " + 转字符串(均值))

    # 多态调用
    设 d1: 整数|浮点 为 加倍(10)
    设 d2: 整数|浮点 为 加倍(3.5)
    打印("加倍(10) = " + 转字符串(d1))
    打印("加倍(3.5) = " + 转字符串(d2))

    # 安全获取
    设 数字: 列表<整数> 为 【10, 20, 30, 40, 50】
    设 r1: 整数|字符串 为 安全获取(数字, 2)
    设 r2: 整数|字符串 为 安全获取(数字, 10)
    打印("安全获取(位置2) = " + 转字符串(r1))
    打印("安全获取(位置10) = " + 转字符串(r2))

    # 空安全查找
    设 u1: 可空字符串 为 查找用户("张三")
    设 u2: 可空字符串 为 查找用户("王五")
    打印("查找张三: " + 转字符串(u1))
    打印("查找王五: " + 转字符串(u2))

    # 筛选正整数
    设 混合: 列表<整数> 为 【-3, 5, -1, 8, 0, 12, -7】
    设 正数: 列表<整数> 为 筛选正整数(混合)
    打印("正数列表: ")
    遍 项 于 正数:
        打印(项)

    # 列表求和
    设 总和: 整数 为 列表求和(数字)
    打印("数字总和 = " + 转字符串(总和))

    # 字典值提取
    设 字典: 字典<字符串, 整数> 为 {"得分": 95, "排名": 3}
    设 v1: 整数|字符串 为 提取值(字典, "得分")
    设 v2: 整数|字符串 为 提取值(字典, "不存在")
    打印("提取得分: " + 转字符串(v1))
    打印("提取不存在: " + 转字符串(v2))

段落 主()