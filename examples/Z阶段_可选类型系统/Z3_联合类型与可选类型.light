# 类型检查级别: 表达式

# =============================================================================
# Z3：联合类型与可选类型
# 展示 类型1|类型2 联合类型和 可空类型 可选类型
# =============================================================================

# 联合类型：变量可以是多种类型之一
设 数字或字符串: 整数|字符串 为 42
设 可能结果: 整数|浮点 为 3.14

# 多联合类型
设 通用值: 整数|浮点|字符串 为 "灵活的值"

# 可选类型：值可以为空
设 可空名称: 可空字符串 为 "张三"
设 可空年龄: 可空整数 为 空

# 联合类型在函数参数中的应用
段落 处理输入(值: 整数|字符串):
    打印(值)

段落 查找用户(名称: 字符串) 返回 可空字符串:
    若 名称 == "张三":
        返回 "找到用户：张三"
    返回 空

段落 主():
    处理输入(42)
    处理输入("你好")

    设 结果: 可空字符串 为 查找用户("张三")
    打印(结果)

    设 无结果: 可空字符串 为 查找用户("不存在")
    打印(无结果)

段落 主()