# 类型检查级别: 签名

# =============================================================================
# Z5：分级类型检查
# 展示不同检查级别的行为差异
#
# 文件级指令：
#   类型检查级别: 签名  → 仅检查段落签名
#   类型检查级别: 变量  → 签名 + 变量声明
#   类型检查级别: 表达式 → 全部检查
#   类型模式: 严格       → 强制类型标注
#   类型模式: 松散       → 可选类型标注
# =============================================================================

# 松散模式段落：无类型标注也不会报错
段落 松散段(a, b):
    返回 a + b

# 严格模式段落：必须标注类型
段落 严格段(a: 整数, b: 整数) 返回 整数:
    返回 a + b

# 完全标注的段落
段落 计算面积(宽: 浮点, 高: 浮点) 返回 浮点:
    返回 宽 * 高

段落 主():
    设 x: 整数 为 松散段(1, 2)
    设 y: 整数 为 严格段(3, 4)
    设 z: 浮点 为 计算面积(5.0, 3.0)

    打印(x)
    打印(y)
    打印(z)

段落 主()