# 段言 - 算法库（20+ 算法实现）
# 包含排序、搜索、图算法、动态规划、数据结构
# 用法: duan run examples/algorithms_lib/主.duan

# ============================================
# 第一部分：排序算法（6种）
# ============================================

# 1. 冒泡排序
段落 冒泡排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 i 为 0。
  当 i 小于 n - 1：
    设 j 为 0。
    当 j 小于 n - i - 1：
      如果 数组[j] 大于 数组[j + 1]：
        设 临时 为 数组[j]。
        数组[j] 为 数组[j + 1]。
        数组[j + 1] 为 临时。
      j 为 j + 1。
    i 为 i + 1。
  返回 数组。

# 2. 选择排序
段落 选择排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 i 为 0。
  当 i 小于 n - 1：
    设 最小索引 为 i。
    设 j 为 i + 1。
    当 j 小于 n：
      如果 数组[j] 小于 数组[最小索引]：
        最小索引 为 j。
      j 为 j + 1。
    如果 最小索引 不等于 i：
      设 临时 为 数组[i]。
      数组[i] 为 数组[最小索引]。
      数组[最小索引] 为 临时。
    i 为 i + 1。
  返回 数组。

# 3. 插入排序
段落 插入排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 i 为 1。
  当 i 小于 n：
    设 键 为 数组[i]。
    设 j 为 i - 1。
    当 j 大于等于 0 且 数组[j] 大于 键：
      数组[j + 1] 为 数组[j]。
      j 为 j - 1。
    数组[j + 1] 为 键。
    i 为 i + 1。
  返回 数组。

# 4. 归并排序
段落 合并 接收 数组, 左, 中, 右：
  设 左大小 为 中 - 左 + 1。
  设 右大小 为 右 - 中。
  设 左数组 为 []。
  设 右数组 为 []。

  设 i 为 0。
  当 i 小于 左大小：
    列表追加(左数组, 数组[左 + i])。
    i 为 i + 1。

  设 i 为 0。
  当 i 小于 右大小：
    列表追加(右数组, 数组[中 + 1 + i])。
    i 为 i + 1。

  设 i 为 0。
  设 j 为 0。
  设 k 为 左。
  当 i 小于 左大小 且 j 小于 右大小：
    如果 左数组[i] 小于等于 右数组[j]：
      数组[k] 为 左数组[i]。
      i 为 i + 1。
    否则：
      数组[k] 为 右数组[j]。
      j 为 j + 1。
    k 为 k + 1。

  当 i 小于 左大小：
    数组[k] 为 左数组[i]。
    i 为 i + 1。
    k 为 k + 1。

  当 j 小于 右大小：
    数组[k] 为 右数组[j]。
    j 为 j + 1。
    k 为 k + 1。

段落 归并排序递归 接收 数组, 左, 右：
  如果 左 小于 右：
    设 中 为 (左 + 右) // 2。
    归并排序递归(数组, 左, 中)。
    归并排序递归(数组, 中 + 1, 右)。
    合并(数组, 左, 中, 右)。

段落 归并排序 接收 数组：
  如果 列表长度(数组) 小于等于 1：
    返回 数组。
  归并排序递归(数组, 0, 列表长度(数组) - 1)。
  返回 数组。

# 5. 快速排序
段落 快速排序分区 接收 数组, 低, 高：
  设 基准 为 数组[高]。
  设 i 为 低 - 1。
  设 j 为 低。
  当 j 小于 高：
    如果 数组[j] 小于等于 基准：
      i 为 i + 1。
      设 临时 为 数组[i]。
      数组[i] 为 数组[j]。
      数组[j] 为 临时。
    j 为 j + 1。
  设 临时 为 数组[i + 1]。
  数组[i + 1] 为 数组[高]。
  数组[高] 为 临时。
  返回 i + 1。

段落 快速排序递归 接收 数组, 低, 高：
  如果 低 小于 高：
    设 分区索引 为 快速排序分区(数组, 低, 高)。
    快速排序递归(数组, 低, 分区索引 - 1)。
    快速排序递归(数组, 分区索引 + 1, 高)。

段落 快速排序 接收 数组：
  如果 列表长度(数组) 小于等于 1：
    返回 数组。
  快速排序递归(数组, 0, 列表长度(数组) - 1)。
  返回 数组。

# 6. 堆排序
段落 堆下沉 接收 数组, n, i：
  设 最大 为 i。
  设 左 为 2 * i + 1。
  设 右 为 2 * i + 2。

  如果 左 小于 n 且 数组[左] 大于 数组[最大]：
    最大 为 左。
  如果 右 小于 n 且 数组[右] 大于 数组[最大]：
    最大 为 右。

  如果 最大 不等于 i：
    设 临时 为 数组[i]。
    数组[i] 为 数组[最大]。
    数组[最大] 为 临时。
    堆下沉(数组, n, 最大)。

段落 堆排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 i 为 n // 2 - 1。
  当 i 大于等于 0：
    堆下沉(数组, n, i)。
    i 为 i - 1。

  设 i 为 n - 1。
  当 i 大于 0：
    设 临时 为 数组[0]。
    数组[0] 为 数组[i]。
    数组[i] 为 临时。
    堆下沉(数组, i, 0)。
    i 为 i - 1。
  返回 数组。

# ============================================
# 第二部分：搜索算法（3种）
# ============================================

# 7. 二分查找
段落 二分查找 接收 数组, 目标：
  设 左 为 0。
  设 右 为 列表长度(数组) - 1。
  当 左 小于等于 右：
    设 中 为 (左 + 右) // 2。
    如果 数组[中] 等于 目标：
      返回 中。
    否则如果 数组[中] 小于 目标：
      左 为 中 + 1。
    否则：
      右 为 中 - 1。
  返回 -1。

# 8. BFS（广度优先搜索）
段落 BFS 接收 图, 起点, 目标：
  设 队列 为 [起点]。
  设 已访问 为 {}。
  字典设置(已访问, 起点, 真)。

  当 列表长度(队列) 大于 0：
    设 节点 为 队列[0]。
    设 新队列 为 []。
    设 i 为 1。
    当 i 小于 列表长度(队列)：
      列表追加(新队列, 队列[i])。
      i 为 i + 1。
    队列 为 新队列。

    如果 节点 等于 目标：
      返回 真。

    如果 字典包含键(图, 节点)：
      设 邻接 为 图[节点]。
      遍历 邻居节点 于 邻接：
        如果 字典包含键(已访问, 邻居节点) 等于 假：
          字典设置(已访问, 邻居节点, 真)。
          列表追加(队列, 邻居节点)。

  返回 假。

# 9. DFS（深度优先搜索）
段落 DFS递归 接收 图, 节点, 目标, 已访问：
  如果 节点 等于 目标：
    返回 真。

  字典设置(已访问, 节点, 真)。

  如果 字典包含键(图, 节点)：
    设 邻接 为 图[节点]。
    遍历 邻居节点 于 邻接：
      如果 字典包含键(已访问, 邻居节点) 等于 假：
        如果 DFS递归(图, 邻居节点, 目标, 已访问)：
          返回 真。

  返回 假。

段落 DFS 接收 图, 起点, 目标：
  设 已访问 为 {}。
  返回 DFS递归(图, 起点, 目标, 已访问)。

# ============================================
# 第三部分：图算法（3种）
# ============================================

# 10. Dijkstra 最短路径
段落 Dijkstra 接收 图, 起点, 终点：
  设 距离 为 {}。
  设 未访问 为 []。
  设 前驱 为 {}。

  # 初始化距离
  设 所有节点 为 字典键列表(图)。
  遍历 节点 于 所有节点：
    如果 节点 等于 起点：
      字典设置(距离, 节点, 0)。
    否则：
      字典设置(距离, 节点, 999999)。
    列表追加(未访问, 节点)。

  当 列表长度(未访问) 大于 0：
    # 找距离最小的未访问节点
    设 最小距离 为 999999。
    设 当前节点 为 未访问[0]。
    设 当前索引 为 0。
    设 i 为 0。
    当 i 小于 列表长度(未访问)：
      设 节点 为 未访问[i]。
      如果 距离[节点] 小于 最小距离：
        最小距离 为 距离[节点]。
        当前节点 为 节点。
        当前索引 为 i。
      i 为 i + 1。

    # 从未访问列表中移除
    设 新未访问 为 []。
    设 i 为 0。
    当 i 小于 列表长度(未访问)：
      如果 i 不等于 当前索引：
        列表追加(新未访问, 未访问[i])。
      i 为 i + 1。
    未访问 为 新未访问。

    如果 当前节点 等于 终点：
      跳出。

    # 更新邻居距离
    如果 字典包含键(图, 当前节点)：
      设 邻接 为 图[当前节点]。
      遍历 邻居项 于 邻接：
        设 邻居名 为 邻居项[0]。
        设 边权重 为 邻居项[1]。
        设 新距离 为 距离[当前节点] + 边权重。
        如果 新距离 小于 距离[邻居名]：
          字典设置(距离, 邻居名, 新距离)。
          字典设置(前驱, 邻居名, 当前节点)。

  返回 距离[终点]。

# 11. Floyd-Warshall 算法
段落 Floyd 接收 矩阵：
  设 n 为 列表长度(矩阵)。
  设 距离 为 []。
  设 i 为 0。
  当 i 小于 n：
    设 行 为 []。
    设 j 为 0。
    当 j 小于 n：
      列表追加(行, 矩阵[i][j])。
      j 为 j + 1。
    列表追加(距离, 行)。
    i 为 i + 1。

  设 k 为 0。
  当 k 小于 n：
    设 i 为 0。
    当 i 小于 n：
      设 j 为 0。
      当 j 小于 n：
        如果 距离[i][k] + 距离[k][j] 小于 距离[i][j]：
          距离[i][j] 为 距离[i][k] + 距离[k][j]。
        j 为 j + 1。
      i 为 i + 1。
    k 为 k + 1。

  返回 距离。

# 12. 拓扑排序
段落 拓扑排序 接收 图, 顶点列表：
  设 已访问 为 {}。
  设 栈 为 []。

  段落 拓扑DFS 接收 节点：
    字典设置(已访问, 节点, 真)。
    如果 字典包含键(图, 节点)：
      设 邻接 为 图[节点]。
      遍历 邻居节点 于 邻接：
        如果 字典包含键(已访问, 邻居节点) 等于 假：
          拓扑DFS(邻居节点)。
    列表追加(栈, 节点)。

  遍历 节点 于 顶点列表：
    如果 字典包含键(已访问, 节点) 等于 假：
      拓扑DFS(节点)。

  设 结果 为 []。
  设 i 为 列表长度(栈) - 1。
  当 i 大于等于 0：
    列表追加(结果, 栈[i])。
    i 为 i - 1。
  返回 结果。

# ============================================
# 第四部分：动态规划（3种）
# ============================================

# 13. 斐波那契数列（DP）
段落 斐波那契DP 接收 n：
  如果 n 小于等于 1：
    返回 n。
  设 dp 为 [0, 1]。
  设 i 为 2。
  当 i 小于等于 n：
    列表追加(dp, dp[i - 1] + dp[i - 2])。
    i 为 i + 1。
  返回 dp[n]。

# 14. 0/1 背包问题
段落 背包问题 接收 重量, 价值, 容量：
  设 n 为 列表长度(重量)。
  设 dp 为 []。
  设 i 为 0。
  当 i 小于等于 n：
    设 行 为 []。
    设 j 为 0。
    当 j 小于等于 容量：
      列表追加(行, 0)。
      j 为 j + 1。
    列表追加(dp, 行)。
    i 为 i + 1。

  设 i 为 1。
  当 i 小于等于 n：
    设 j 为 1。
    当 j 小于等于 容量：
      如果 重量[i - 1] 小于等于 j：
        设 包含 为 价值[i - 1] + dp[i - 1][j - 重量[i - 1]]。
        设 不包含 为 dp[i - 1][j]。
        如果 包含 大于 不包含：
          dp[i][j] 为 包含。
        否则：
          dp[i][j] 为 不包含。
      否则：
        dp[i][j] 为 dp[i - 1][j]。
      j 为 j + 1。
    i 为 i + 1。

  返回 dp[n][容量]。

# 15. 最长公共子序列（LCS）
段落 最长公共子序列 接收 文本1, 文本2：
  设 m 为 字符串长度(文本1)。
  设 n 为 字符串长度(文本2)。
  设 dp 为 []。
  设 i 为 0。
  当 i 小于等于 m：
    设 行 为 []。
    设 j 为 0。
    当 j 小于等于 n：
      列表追加(行, 0)。
      j 为 j + 1。
    列表追加(dp, 行)。
    i 为 i + 1。

  设 i 为 1。
  当 i 小于等于 m：
    设 j 为 1。
    当 j 小于等于 n：
      如果 文本1[i - 1] 等于 文本2[j - 1]：
        dp[i][j] 为 dp[i - 1][j - 1] + 1。
      否则：
        如果 dp[i - 1][j] 大于 dp[i][j - 1]：
          dp[i][j] 为 dp[i - 1][j]。
        否则：
          dp[i][j] 为 dp[i][j - 1]。
      j 为 j + 1。
    i 为 i + 1。

  返回 dp[m][n]。

# ============================================
# 第五部分：数据结构（5种）
# ============================================

# 16. 栈
类 栈：
  属性 元素 等于 空。

  段落 初始化：
    己.元素 为 []。

  段落 压入 接收 值：
    列表追加(己.元素, 值)。

  段落 弹出：
    如果 列表长度(己.元素) 等于 0：
      返回 空。
    返回 列表弹出(己.元素)。

  段落 查看栈顶：
    如果 列表长度(己.元素) 等于 0：
      返回 空。
    返回 己.元素[列表长度(己.元素) - 1]。

  段落 是否为空：
    返回 列表长度(己.元素) 等于 0。

  段落 大小：
    返回 列表长度(己.元素)。

# 17. 队列
类 队列：
  属性 元素 等于 空。

  段落 初始化：
    己.元素 为 []。

  段落 入队 接收 值：
    列表追加(己.元素, 值)。

  段落 出队：
    如果 列表长度(己.元素) 等于 0：
      返回 空。
    设 值 为 己.元素[0]。
    设 新列表 为 []。
    设 i 为 1。
    当 i 小于 列表长度(己.元素)：
      列表追加(新列表, 己.元素[i])。
      i 为 i + 1。
    己.元素 为 新列表。
    返回 值。

  段落 查看队首：
    如果 列表长度(己.元素) 等于 0：
      返回 空。
    返回 己.元素[0]。

  段落 是否为空：
    返回 列表长度(己.元素) 等于 0。

  段落 大小：
    返回 列表长度(己.元素)。

# 18. 链表
类 链表节点：
  属性 值 等于 空。
  属性 下一个 等于 空。

  段落 初始化 接收 值参数：
    己.值 为 值参数。
    己.下一个 为 空。

类 链表：
  属性 头 等于 空。
  属性 长度 等于 空。

  段落 初始化：
    己.头 为 空。
    己.长度 为 0。

  段落 追加 接收 值：
    设 新节点 为 新建 链表节点(值)。
    如果 己.头 等于 空：
      己.头 为 新节点。
    否则：
      设 当前 为 己.头。
      当 当前.下一个 不等于 空：
        当前 为 当前.下一个。
      当前.下一个 为 新节点。
    己.长度 为 己.长度 + 1。

  段落 删除 接收 值：
    如果 己.头 等于 空：
      返回 假。
    如果 己.头.值 等于 值：
      己.头 为 己.头.下一个。
      己.长度 为 己.长度 - 1。
      返回 真。
    设 当前 为 己.头。
    当 当前.下一个 不等于 空：
      如果 当前.下一个.值 等于 值：
        当前.下一个 为 当前.下一个.下一个。
        己.长度 为 己.长度 - 1。
        返回 真。
      当前 为 当前.下一个。
    返回 假。

  段落 查找 接收 值：
    设 当前 为 己.头。
    设 索引 为 0。
    当 当前 不等于 空：
      如果 当前.值 等于 值：
        返回 索引。
      当前 为 当前.下一个。
      索引 为 索引 + 1。
    返回 -1。

  段落 转列表：
    设 结果 为 []。
    设 当前 为 己.头。
    当 当前 不等于 空：
      列表追加(结果, 当前.值)。
      当前 为 当前.下一个。
    返回 结果。

# 19. 二叉树
类 分支节点：
  属性 值 等于 空。
  属性 孩子左 等于 空。
  属性 孩子右 等于 空。

  段落 初始化 接收 值参数：
    己.值 为 值参数。
    己.孩子左 为 空。
    己.孩子右 为 空。

类 搜索树：
  属性 根 等于 空。

  段落 初始化：
    己.根 为 空。

  段落 插入 接收 值：
    设 新节点 为 新建 分支节点(值)。
    如果 己.根 等于 空：
      己.根 为 新节点。
      返回。
    设 当前 为 己.根。
    当 真：
      如果 值 小于 当前.值：
        如果 当前.孩子左 等于 空：
          当前.孩子左 为 新节点。
          返回。
        否则：
          当前 为 当前.孩子左。
      否则：
        如果 当前.孩子右 等于 空：
          当前.孩子右 为 新节点。
          返回。
        否则：
          当前 为 当前.孩子右。

  段落 查找 接收 值：
    设 当前 为 己.根。
    当 当前 不等于 空：
      如果 当前.值 等于 值：
        返回 真。
      否则如果 值 小于 当前.值：
        当前 为 当前.孩子左。
      否则：
        当前 为 当前.孩子右。
    返回 假。

  段落 中序遍历 接收 节点, 结果：
    如果 节点 等于 空：
      返回。
    己.中序遍历(节点.孩子左, 结果)。
    列表追加(结果, 节点.值)。
    己.中序遍历(节点.孩子右, 结果)。

  段落 获取最小值：
    如果 己.根 等于 空：
      返回 空。
    设 当前 为 己.根。
    当 当前.孩子左 不等于 空：
      当前 为 当前.孩子左。
    返回 当前.值。

  段落 获取最大值：
    如果 己.根 等于 空：
      返回 空。
    设 当前 为 己.根。
    当 当前.孩子右 不等于 空：
      当前 为 当前.孩子右。
    返回 当前.值。

# 20. 最小堆
类 最小堆：
  属性 元素 等于 空。

  段落 初始化：
    己.元素 为 []。

  段落 插入 接收 值：
    列表追加(己.元素, 值)。
    设 i 为 列表长度(己.元素) - 1。
    当 i 大于 0：
      设 双亲 为 (i - 1) // 2。
      如果 己.元素[i] 大于等于 己.元素[双亲]：
        跳出。
      设 临时 为 己.元素[i]。
      己.元素[i] 为 己.元素[双亲]。
      己.元素[双亲] 为 临时。
      i 为 双亲。

  段落 取出最小值：
    如果 列表长度(己.元素) 等于 0：
      返回 空。
    设 最小值 为 己.元素[0]。
    己.元素[0] 为 己.元素[列表长度(己.元素) - 1]。
    列表弹出(己.元素)。
    己.堆化(0)。
    返回 最小值。

  段落 堆化 接收 i：
    设 最小 为 i。
    设 左 为 2 * i + 1。
    设 右 为 2 * i + 2。
    设 n 为 列表长度(己.元素)。

    如果 左 小于 n 且 己.元素[左] 小于 己.元素[最小]：
      最小 为 左。
    如果 右 小于 n 且 己.元素[右] 小于 己.元素[最小]：
      最小 为 右。
    如果 最小 不等于 i：
      设 临时 为 己.元素[i]。
      己.元素[i] 为 己.元素[最小]。
      己.元素[最小] 为 临时。
      己.堆化(最小)。

  段落 大小：
    返回 列表长度(己.元素)。

  段落 是否为空：
    返回 列表长度(己.元素) 等于 0。

# ============================================
# 第六部分：测试与演示
# ============================================

段落 打印数组 接收 数组：
  设 结果 为 "["。
  设 i 为 0。
  当 i 小于 列表长度(数组)：
    如果 i 大于 0：
      结果 为 结果 + ", "。
    结果 为 结果 + 转字符串(数组[i])。
    i 为 i + 1。
  结果 为 结果 + "]"。
  打印(结果)。

段落 打印分隔线：
  打印("------------------------------------------------")。

段落 测试排序算法：
  打印("\n========== 排序算法测试 ==========")。
  设 原始数组 为 [64, 34, 25, 12, 22, 11, 90, 5, 77, 30]。
  设 名称列表 为 ["冒泡排序", "选择排序", "插入排序", "归并排序", "快速排序", "堆排序"]。
  设 函数列表 为 [冒泡排序, 选择排序, 插入排序, 归并排序, 快速排序, 堆排序]。

  设 算法数 为 列表长度(名称列表)。
  设 idx 为 0。
  当 idx 小于 算法数：
    设 测试数组 为 []。
    遍历 值 于 原始数组：
      列表追加(测试数组, 值)。
    打印("  " + 名称列表[idx] + ": ")。
    如果 idx 等于 0：
      冒泡排序(测试数组)。
    否则如果 idx 等于 1：
      选择排序(测试数组)。
    否则如果 idx 等于 2：
      插入排序(测试数组)。
    否则如果 idx 等于 3：
      归并排序(测试数组)。
    否则如果 idx 等于 4：
      快速排序(测试数组)。
    否则：
      堆排序(测试数组)。
    打印("    结果: ")。
    打印数组(测试数组)。
    idx 为 idx + 1。

段落 测试搜索算法：
  打印("\n========== 搜索算法测试 ==========")。
  设 有序数组 为 [5, 11, 12, 22, 25, 30, 34, 64, 77, 90]。
  打印("有序数组: ")。
  打印数组(有序数组)。
  打印分隔线()。

  设 搜索目标 为 25。
  设 索引 为 二分查找(有序数组, 搜索目标)。
  打印("  二分查找 " + 转字符串(搜索目标) + ": 索引 " + 转字符串(索引))。

  搜索目标 为 100。
  索引 为 二分查找(有序数组, 搜索目标)。
  打印("  二分查找 " + 转字符串(搜索目标) + ": 索引 " + 转字符串(索引))。

  # BFS 测试
  打印("\n  BFS 测试:")。
  设 图 为 {
    "A": ["B", "C"],
    "B": ["A", "D", "E"],
    "C": ["A", "F"],
    "D": ["B"],
    "E": ["B", "F"],
    "F": ["C", "E"]
  }。
  设 bfs结果 为 BFS(图, "A", "F")。
  打印("    BFS 从 A 到 F: " + 转字符串(bfs结果))。
  bfs结果 为 BFS(图, "A", "G")。
  打印("    BFS 从 A 到 G: " + 转字符串(bfs结果))。

  # DFS 测试
  打印("\n  DFS 测试:")。
  设 dfs结果 为 DFS(图, "A", "F")。
  打印("    DFS 从 A 到 F: " + 转字符串(dfs结果))。
  dfs结果 为 DFS(图, "A", "G")。
  打印("    DFS 从 A 到 G: " + 转字符串(dfs结果))。

段落 测试图算法：
  打印("\n========== 图算法测试 ==========")。

  # Dijkstra 测试
  打印("  Dijkstra 最短路径:")。
  设 加权图 为 {
    "A": [["B", 4], ["C", 2]],
    "B": [["A", 4], ["C", 1], ["D", 5]],
    "C": [["A", 2], ["B", 1], ["D", 8], ["E", 10]],
    "D": [["B", 5], ["C", 8], ["E", 2], ["F", 6]],
    "E": [["C", 10], ["D", 2], ["F", 3]],
    "F": [["D", 6], ["E", 3]]
  }。
  设 最短距离 为 Dijkstra(加权图, "A", "F")。
  打印("    A 到 F 的最短距离: " + 转字符串(最短距离))。

  # Floyd 测试
  打印("  Floyd-Warshall 算法:")。
  设 INF 为 999999。
  设 矩阵 为 [[0, 3, INF, 5], [2, 0, INF, 4], [INF, 1, 0, INF], [INF, INF, 2, 0]]。
  设 floyd结果 为 Floyd(矩阵)。
  打印("    最短距离矩阵:")。
  设 i 为 0。
  当 i 小于 列表长度(floyd结果)：
    打印("      " + 转字符串(floyd结果[i]))。
    i 为 i + 1。

  # 拓扑排序测试
  打印("  拓扑排序:")。
  设 拓扑图 为 {
    "A": ["C"],
    "B": ["C", "D"],
    "C": ["E"],
    "D": ["F"],
    "E": ["F"],
    "F": []
  }。
  设 节点顺序 为 ["A", "B", "C", "D", "E", "F"]。
  设 拓扑结果 为 拓扑排序(拓扑图, 节点顺序)。
  打印("    拓扑顺序: ")。
  打印数组(拓扑结果)。

段落 测试DP算法：
  打印("\n========== 动态规划算法测试 ==========")。

  打印("  斐波那契数列 (DP):")。
  设 i 为 0。
  当 i 小于等于 10：
    打印("    F(" + 转字符串(i) + ") = " + 转字符串(斐波那契DP(i)))。
    i 为 i + 1。

  打印("  0/1 背包问题:")。
  设 重量 为 [2, 3, 4, 5]。
  设 价值 为 [3, 4, 5, 6]。
  设 容量 为 8。
  设 背包结果 为 背包问题(重量, 价值, 容量)。
  打印("    物品重量: ")。
  打印数组(重量)。
  打印("    物品价值: ")。
  打印数组(价值)。
  打印("    背包容量: " + 转字符串(容量))。
  打印("    最大价值: " + 转字符串(背包结果))。

  打印("  最长公共子序列 (LCS):")。
  设 字符串1 为 "ABCBDAB"。
  设 字符串2 为 "BDCABA"。
  设 lcs长度 为 最长公共子序列(字符串1, 字符串2)。
  打印("    S1: " + 字符串1)。
  打印("    S2: " + 字符串2)。
  打印("    LCS 长度: " + 转字符串(lcs长度))。

段落 测试数据结构：
  打印("\n========== 数据结构测试 ==========")。

  # 栈测试
  打印("  1. 栈 (Stack):")。
  设 栈实例 为 新建 栈()。
  栈实例.压入(10)。
  栈实例.压入(20)。
  栈实例.压入(30)。
  打印("    弹出: " + 转字符串(栈实例.弹出()))。
  打印("    栈顶: " + 转字符串(栈实例.查看栈顶()))。
  打印("    大小: " + 转字符串(栈实例.大小()))。

  # 队列测试
  打印("  2. 队列 (Queue):")。
  设 队列实例 为 新建 队列()。
  队列实例.入队("A")。
  队列实例.入队("B")。
  队列实例.入队("C")。
  打印("    出队: " + 队列实例.出队())。
  打印("    队首: " + 队列实例.查看队首())。
  打印("    大小: " + 转字符串(队列实例.大小()))。

  # 链表测试
  打印("  3. 链表 (Linked List):")。
  设 列表 为 新建 链表()。
  列表.追加(100)。
  列表.追加(200)。
  列表.追加(300)。
  打印("    链表内容: ")。
  打印数组(列表.转列表())。
  打印("    查找 200: 索引 " + 转字符串(列表.查找(200)))。
  列表.删除(200)。
  打印("    删除 200 后: ")。
  打印数组(列表.转列表())。
  打印("    长度: " + 转字符串(列表.长度))。

  # 二叉搜索树测试
  打印("  4. 二叉搜索树 (BST):")。
  设 bst 为 新建 搜索树()。
  bst.插入(50)。
  bst.插入(30)。
  bst.插入(70)。
  bst.插入(20)。
  bst.插入(40)。
  bst.插入(60)。
  bst.插入(80)。
  设 中序结果 为 []。
  bst.中序遍历(bst.根, 中序结果)。
  打印("    中序遍历: ")。
  打印数组(中序结果)。
  打印("    查找 40: " + 转字符串(bst.查找(40)))。
  打印("    查找 100: " + 转字符串(bst.查找(100)))。
  打印("    最小值: " + 转字符串(bst.获取最小值()))。
  打印("    最大值: " + 转字符串(bst.获取最大值()))。

  # 最小堆测试
  打印("  5. 最小堆 (Min Heap):")。
  设 堆 为 新建 最小堆()。
  堆.插入(5)。
  堆.插入(3)。
  堆.插入(8)。
  堆.插入(1)。
  堆.插入(2)。
  打印("    取出最小值: " + 转字符串(堆.取出最小值()))。
  打印("    取出最小值: " + 转字符串(堆.取出最小值()))。
  打印("    取出最小值: " + 转字符串(堆.取出最小值()))。
  打印("    是否为空: " + 转字符串(堆.是否为空()))。

段落 主程序：
  打印("╔" + "═" * 48 + "╗")。
  打印("║         段言算法库 — 20+ 算法实现         ║")。
  打印("╚" + "═" * 48 + "╝")。
  打印("")。
  打印("包含: 6种排序 | 3种搜索 | 3种图算法 | 3种DP | 5种数据结构")。
  打印("")。

  测试排序算法()。
  测试搜索算法()。
  测试图算法()。
  测试DP算法()。
  测试数据结构()。

  打印("\n" + "=" * 50)。
  打印("  全部算法测试完成！")。
  打印("  共测试 20 种算法/数据结构。")。
  打印("=" * 50)。

主程序()。