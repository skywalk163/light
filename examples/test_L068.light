# 外发任务 D1 — L-068 最小复现 / 修复验证
#
# 验证点：
#   1) 类结束标记（类结束 / 结束类）可用；标记后的段落正确归为模块级函数。
#   2) 模块级函数与某类成员同名 → 编译警告（很可能漏写缩进被当成模块级函数）。
#
# 编译期警告输出到 stderr，运行本身不受影响（warning 非 fatal）。

# ── 场景 1：类结束标记 ──
# 用「类结束」显式收尾类定义区域；其后的「段落 主」是模块级函数，
# 不是 工具 的方法（验证「类结束标记后的段落正确归为模块级函数」）。
类 工具：
  属性 名称。
  构造 接收 名称：
    己名称 为 名称。
  段落 运行 接收：
    返回 己名称
类结束

段落 主 接收:
  设 实例 为 新建 工具("光明")
  打印(实例.运行())
  打印("主函数是模块级函数，不是 工具 的方法")

主()

# ── 场景 2：模块级函数与类成员同名 → 应有 L-068 编译警告 ──
# 模块顶层「段落 计算」与类 计算器 的方法「计算」同名：编译期预警很可能漏写缩进。
类 计算器:
  段落 计算 接收:
    返回 42
段落 计算 接收:
  打印("这是模块级的『计算』，与类 计算器 的方法『计算』同名（应触发 L-068 警告）")
计算()
