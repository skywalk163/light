# 光明 C FFI 示例 2：调用系统库获取系统信息
# 使用 FFI 调用 C 标准库函数

# Windows 系统使用 msvcrt
# Linux 系统使用 libc.so.6
# macOS 系统使用 libSystem.dylib

加载库 "libc.so.6" 为 系统库。

外部 段落 获取时间 为 "time" 接收 甲 为 空 返回 整数 在 系统库。

段落 主程序：
    打印("=== 光明 FFI 系统库示例 ===")。
    打印("")。
    
    打印("系统时间戳：")。
    设 时间戳 为 获取时间()。
    打印("  Unix 时间戳：" + 字符串(时间戳))。
    。

主程序()。