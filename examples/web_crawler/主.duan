# 段言 - 中文网页爬虫
# 功能: 使用 HTTP 请求获取网页内容，提取中文文本，生成站点地图
# 用法: duan run examples/web_crawler/主.duan <URL> [深度]
# 示例: duan run examples/web_crawler/主.duan "https://example.com" 2

导入 文件系统。
导入 JSON。
导入 正则表达式。

# ===== 配置 =====
设 默认深度 为 1。
设 请求超时 为 10。
设 用户代理 为 "DuanLang-Crawler/1.0"。

# ===== 全局状态 =====
设 已访问链接 为 []。
设 站点地图 为 {}。
设 抓取文本 为 {}。

# ===== URL 处理函数 =====

# 从 URL 中提取域名
段落 提取域名 接收 url:
  设 模式 为 编译正则表达式(r"https?://([^/]+)")。
  设 匹配项 为 正则匹配(模式, url)。
  如果 匹配项 不等于 空:
    返回 匹配项.分组(1)。
  返回 ""。

# 判断是否为绝对 URL
段落 是绝对URL 接收 url:
  返回 开头(url, "http://") 或 开头(url, "https://")。

# 将相对 URL 转为绝对 URL
段落 转为绝对URL 接收 相对URL, 基础URL:
  如果 是绝对URL(相对URL):
    返回 相对URL。
  如果 开头(相对URL, "//"):
    返回 提取协议(基础URL) + ":" + 相对URL。
  如果 开头(相对URL, "/"):
    设 域名 为 提取域名(基础URL)。
    返回 提取协议(基础URL) + "://" + 域名 + 相对URL。
  如果 开头(相对URL, "#") 或 相对URL 等于 "":
    返回 空。
  设 基础路径 为 基础URL。
  如果 包含字符串(基础路径, "?"):
    基础路径 为 分割字符串(基础路径, "?")[0]。
  如果 非 结尾(基础路径, "/"):
    设 最后斜杠 为 最后索引(基础路径, "/")。
    如果 最后斜杠 大于 7:
      基础路径 为 字符串获取(基础路径, 0, 最后斜杠 + 1)。
  返回 基础路径 + 相对URL。

# 提取 URL 协议
段落 提取协议 接收 url:
  如果 开头(url, "https://"):
    返回 "https"。
  如果 开头(url, "http://"):
    返回 "http"。
  返回 ""。

# 检查 URL 是否已访问
段落 是否已访问 接收 url:
  遍历 链接 于 已访问链接:
    如果 链接 等于 url:
      返回 真。
  返回 假。

# 检查是否应在同一域名下抓取
段落 应抓取 接收 url, 目标域名:
  设 域名 为 提取域名(url)。
  如果 域名 等于 "":
    返回 假。
  返回 域名 等于 目标域名。

# 过滤非 HTML 资源
段落 是HTML链接 接收 url:
  设 小写URL 为 转小写(url)。
  设 排除列表 为 [".jpg", ".jpeg", ".png", ".gif", ".svg", ".webp",
    ".pdf", ".doc", ".docx", ".zip", ".rar", ".mp3", ".mp4",
    ".avi", ".mov", ".exe", ".css", ".js", ".ico", ".woff", ".woff2"]。
  遍历 扩展名 于 排除列表:
    如果 结尾(小写URL, 扩展名):
      返回 假。
  返回 真。

# ===== 核心功能 =====

# 抓取网页内容
段落 抓取页面 接收 url:
  打印("正在抓取: " + url)。
  尝试:
    设 响应 为 HTTP请求(url, {
      "method": "GET",
      "headers": {
        "User-Agent": 用户代理,
        "Accept": "text/html,application/xhtml+xml",
        "Accept-Language": "zh-CN,zh;q=0.9"
      },
      "timeout": 请求超时
    })。
    设 状态码 为 响应["status"]。
    如果 状态码 不等于 200:
      打印("  警告: 状态码 " + 转字符串(状态码))。
      返回 空。
    设 内容 为 响应["body"]。
    如果 内容 等于 空 或 内容 等于 "":
      返回 空。
    打印("  成功获取 " + 转字符串(字符串长度(内容)) + " 字节")。
    返回 内容。
  捕获 错误 为 e:
    打印("  错误: " + 转字符串(e))。
    返回 空。

# 从 HTML 中提取中文文本
段落 提取中文文本 接收 html:
  如果 html 等于 空:
    返回 ""。
  # 移除 HTML 标签，保留文本内容
  设 纯文本 为 正则替换(html, r"<[^>]+>", "")。
  # 移除多余空白
  纯文本 为 正则替换(纯文本, r"\s+", " ")。
  # 提取中文字符（连续的中文段落）
  设 中文模式 为 编译正则表达式(r"[\u4e00-\u9fff\u3000-\u303f\uff00-\uffef]{2,}")。
  设 匹配结果 为 正则查找所有(中文模式, 纯文本)。
  设 中文段落 为 []。
  遍历 匹配项 于 匹配结果:
    设 文本段 为 匹配项[0]。
    如果 字符串长度(文本段) 大于等于 4:
      列表追加(中文段落, 文本段)。
  返回 连接字符串(中文段落, " ")。

# 从 HTML 中提取链接
段落 提取链接 接收 html, 页面URL:
  如果 html 等于 空:
    返回 []。
  设 链接列表 为 []。
  设 模式 为 编译正则表达式(r'href="([^"]*)"')。
  设 所有匹配项 为 正则查找所有(模式, html)。
  遍历 匹配项 于 所有匹配项:
    设 原始链接 为 匹配项[1]。
    设 绝对链接 为 转为绝对URL(原始链接, 页面URL)。
    如果 绝对链接 不等于 空 且 是HTML链接(绝对链接):
      设 已存在 为 假。
      遍历 已有链接 于 链接列表:
        如果 已有链接 等于 绝对链接:
          已存在 为 真。
          跳出。
      如果 非 已存在:
        列表追加(链接列表, 绝对链接)。
  返回 链接列表。

# 递归抓取网页
段落 递归抓取 接收 url, 目标域名, 最大深度, 本层深度:
  如果 本层深度 大于 最大深度:
    返回。
  如果 是否已访问(url):
    返回。
  如果 非 应抓取(url, 目标域名):
    返回。
  列表追加(已访问链接, url)。
  设 缩进 为 ""。
  设 i 为 0。
  当 i 小于 本层深度:
    缩进 为 缩进 + "  "。
    i 为 i + 1。
  打印(缩进 + "[" + 转字符串(本层深度) + "/" + 转字符串(最大深度) + "] " + url)。
  设 html 为 抓取页面(url)。
  如果 html 等于 空:
    返回。
  # 提取中文文本
  设 中文内容 为 提取中文文本(html)。
  字典设置(抓取文本, url, 中文内容)。
  # 提取链接
  设 链接列表 为 提取链接(html, url)。
  设 有效链接 为 []。
  遍历 链接 于 链接列表:
    如果 应抓取(链接, 目标域名):
      列表追加(有效链接, 链接)。
  设 子链接 为 []。
  遍历 链接 于 有效链接:
    列表追加(子链接, {"url": 链接, "depth": 本层深度 + 1})。
  字典设置(站点地图, url, {
    "depth": 本层深度,
    "links": 子链接,
    "link_count": 列表长度(有效链接)
  })。
  打印(缩进 + "  发现 " + 转字符串(列表长度(有效链接)) + " 个链接")。
  遍历 链接 于 有效链接:
    递归抓取(链接, 目标域名, 最大深度, 本层深度 + 1)。

# 输出抓取报告
段落 输出报告 接收 起始URL, 最大深度:
  打印("\n" + "=" * 60)。
  打印("  爬虫执行报告")。
  打印("=" * 60)。
  打印("起始 URL: " + 起始URL)。
  打印("抓取深度: " + 转字符串(最大深度))。
  打印("抓取页面: " + 转字符串(列表长度(已访问链接)) + " 个")。
  打印("-" * 60)。
  设 页面编号 为 0。
  遍历 页面URL 于 已访问链接:
    设 信息 为 字典获取(站点地图, 页面URL, {})。
    设 页面深度 为 字典获取(信息, "depth", 0)。
    设 链接数 为 字典获取(信息, "link_count", 0)。
    设 中文文本 为 字典获取(抓取文本, 页面URL, "")。
    页面编号 为 页面编号 + 1。
    打印(转字符串(页面编号) + ". " + 页面URL)。
    打印("   深度: " + 转字符串(页面深度) + " | 链接: " + 转字符串(链接数))。
    如果 字符串长度(中文文本) 大于 0:
      设 摘要 为 字符串获取(中文文本, 0, 取最小值(80, 字符串长度(中文文本)))。
      打印("   中文摘要: " + 摘要 + "...")。
  # 导出 JSON 报告
  设 报告 为 {
    "start_url": 起始URL,
    "max_depth": 最大深度,
    "pages_crawled": 列表长度(已访问链接),
    "sitemap": 站点地图,
    "chinese_text": 抓取文本
  }。
  设 JSON报告 为 序列化JSON(报告, 2)。
  写入文件("crawler_report.json", JSON报告)。
  打印("-" * 60)。
  打印("报告已导出到 crawler_report.json")。
  打印("=" * 60)。

# 显示帮助
段落 显示帮助:
  打印("")。
  打印("段言中文网页爬虫 - 使用方法:")。
  打印("")。
  打印("  duan run examples/web_crawler/主.duan <URL> [深度]")。
  打印("")。
  打印("参数:")。
  打印("  URL       起始抓取地址（必填）")。
  打印("  深度      递归抓取深度（可选，默认 " + 转字符串(默认深度) + "）")。
  打印("")。
  打印("示例:")。
  打印("  duan run examples/web_crawler/主.duan https://example.com")。
  打印("  duan run examples/web_crawler/主.duan https://example.com 2")。
  打印("")。

# ===== 主程序 =====
段落 主程序:
  导入 sys。
  设 参数 为 sys.argv。
  设 参数数量 为 列表长度(参数)。
  如果 参数数量 小于 2:
    显示帮助()。
    返回。
  设 起始URL 为 参数[1]。
  设 最大深度 为 默认深度。
  如果 参数数量 大于等于 3:
    最大深度 为 转整数(参数[2])。
  设 目标域名 为 提取域名(起始URL)。
  如果 目标域名 等于 "":
    打印("错误: 无效的 URL - " + 起始URL)。
    返回。
  打印("=" * 60)。
  打印("  段言中文网页爬虫 v1.0")。
  打印("=" * 60)。
  打印("起始 URL: " + 起始URL)。
  打印("目标域名: " + 目标域名)。
  打印("最大深度: " + 转字符串(最大深度))。
  打印("=" * 60)。
  递归抓取(起始URL, 目标域名, 最大深度, 0)。
  输出报告(起始URL, 最大深度)。
  打印("\n爬虫执行完成！")。

主程序()。