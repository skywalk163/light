# 段言试用程序：打印问候 + 判断 + 循环
# 演示：变量声明、函数（段落）、条件、循环

段落 问候 接收 名字：
  返回 "你好，" + 名字 + "！"

打印(问候("段言"))

设 分数 为 85
如果 分数 大于等于 90：
  打印("优秀")
否则如果 分数 大于等于 60：
  打印("及格")
否则：
  打印("不及格")

设 计数 为 1
设 总和 为 0
当 计数 小于等于 10：
  设 总和 为 总和 加 计数
  设 计数 为 计数 加 1

打印("1到10的总和：" + 转字符串(总和))
