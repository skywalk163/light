# 文体: 白话
# 第 9 课：尝试和捕获——程序出错了不要崩
# 目标：「试」/「捕」/「终」三部曲，让程序即使出错也能温柔提示

打印 "======== 让用户输入一个数字：输入字母会错，捕获后友好提示 ========"
段 安全转数字(文字):
    试:
        设 结果 = 转成整数(文字)   # 如果不是数字就会报错
        返 结果, "成功"
    捕 错:
        返 空, "「" + 文字 + "」不是一个合法的整数哦"

设 测试 = ["123", "abc", "-45", "3.14", "2026"]
遍 测试 为 t:
    设 对 = 安全转数字(t)
    若 对[1] == "成功":
        打印 t, " → 转数字成功，值=", 对[0]
    否:
        打印 t, " → ", 对[1]

打印 ""
打印 "======== 「终」（finally）：不管有没有错，最后都会做 ========"
段 读文件安全(路径):
    试:
        打印 "尝试打开文件：", 路径
        # 故意不打开，演示 finally
        抛 "文件不存在"   # 「抛」= 制造一个错误
    捕 e:
        打印 "出错啦：", e
    终:
        打印 "不管成功失败，最后都会做收尾工作 ✓"

读文件安全("不存在的文件.txt")
