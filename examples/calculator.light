# 光明编程语言 - 示例项目
# 示例2: 计算器

类计算器：
  设结果为0

  方法加接收x：
    设结果为结果加x

  方法减接收x：
    设结果为结果减x

  方法乘接收x：
    设结果为结果乘x

  方法除接收x：
    如果x不等于0：
      设结果为结果除x
    否则：
      打印("错误: 除数不能为零")

  方法清零接收：
    设结果为0

  方法获取结果接收：
    返回结果

# 使用计算器
设计算器1为计算器()
计算器1.加(10)
计算器1.乘(2)
计算器1.减(5)
打印("计算结果: " + 转字符串(计算器1.获取结果()))