# L0 核心关键字：段（函数定义）
# v4.0 单字关键字「段」「返回」

# 定义无参数段
段 问候：
  印("你好！")
  返回 0

# 定义带参数段
段 平方(x)：
  返回 x * x

# 定义多参数段
段 求和(a, b)：
  返回 a + b

# 定义递归段
段 阶乘(n)：
  若 n <= 1 则：
    返回 1
  返回 n * 阶乘(n - 1)

# 调用段
问候()
印("5 的平方：")
印(平方(5))
印("10 + 20 = ")
印(求和(10, 20))
印("6 的阶乘：")
印(阶乘(6))