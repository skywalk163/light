# L0 核心关键字：若（条件判断）
# v4.0 单字关键字「若」「则」「否」「或若」

设 分数 为 85

# 基本条件判断
若 分数 >= 90 则：
  印("优秀")
或若 分数 >= 80 则：
  印("良好")
或若 分数 >= 70 则：
  印("中等")
或若 分数 >= 60 则：
  印("及格")
否：
  印("不及格")

# 简洁的条件判断
设 年龄 为 18
若 年龄 >= 18 则：
  印("成年")
否：
  印("未成年")

# 使用逻辑运算符
设 a 为 5
设 b 为 10
设 c 为 7
若 a < b 且 b > c 则：
  印("b 最大")