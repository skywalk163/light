# L0 核心关键字：试（异常处理）
# v4.0 单字关键字「试」「捕」「掷」

# 基本异常捕获
试：
  设 结果 为 10 / 0
  印(结果)
捕：
  印("捕获到除零错误！")

# 带类型检查的异常捕获
试：
  设 列表 为 [1, 2, 3]
  设 越界 为 列表[10]
  印(越界)
捕：
  印("捕获到索引越界错误！")

# 使用「掷」抛出异常
段 检查正数(n)：
  若 n <= 0 则：
    掷("参数必须为正数")
  返回 n

# 测试异常抛出
试：
  检查正数(-5)
捕：
  印("捕获到异常：")
  印(错误信息)