# L0 核心关键字：设（变量定义）
# v4.0 单字关键字「设」定义变量

# 基本变量定义
设 名称 为 "光明"
设 版本 为 4.0
设 活跃 为 真

印("名称：")
印(名称)
印("版本：")
印(版本)
印("活跃：")
印(活跃)

# 变量重新赋值
设 计数 为 0
印("初始：")
印(计数)

计数 为 10
印("更新后：")
印(计数)

# 同时定义多个变量
设 x 为 5
设 y 为 3
设 和 为 x + y
印("x + y = ")
印(和)