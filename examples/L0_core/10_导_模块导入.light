# L0 核心关键字：导（模块导入）
# v4.0 单字关键字「导」「自」「引」「出」

# 导入标准库模块
导 数学 自 数学
导 字符串处理 自 字符串处理

# 使用导入的模块
印("圆周率：")
印(数学.pi)

印("正弦 90°：")
印(数学.sin(90))

# 字符串操作
设 文本 为 "Hello, 光明!"
印("大写：")
印(字符串处理.大写(文本))
印("长度：")
印(字符串处理.长度(文本))

# L4 层：引用 Python 代码
引 Python:
import math
result = math.sqrt(16)
出 result

印("16 的平方根：")
印(result)