# L0 核心关键字：列表操作
# v4.0 列表的创建、访问和操作

# 创建列表
设 空列 为 []
设 数列 为 [1, 2, 3, 4, 5]
设 混合 为 [1, "文本", 真, 3.14]

# 访问元素
印("第一个元素：")
印(数列[0])
印("第三个元素：")
印(数列[2])
印("最后一个元素：")
印(数列[4])

# 列表长度
印("长度：")
印(数列之长度)

# 列表追加
数列 为 数列 + [6]
数列 为 数列 + [7]
印("追加后：")
印(数列)

# 列表切片
设 切片 为 [10, 20, 30, 40, 50]
印("前三个元素：")
印(切片[0:3])
印("后两个元素：")
印(切片[3:5])