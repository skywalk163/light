# L0 核心关键字：匹（模式匹配）
# v4.0 单字关键字「匹」「例」

# 基本模式匹配
段 判断类型(值)：
  匹 值：
    例 数：
      印("这是一个数字：")
      印(值)
    例 串：
      印("这是一个字符串：")
      印(值)
    例 列：
      印("这是一个列表，长度：")
      印(值之长度)
    例：
      印("未知类型")

# 测试模式匹配
判断类型(42)
判断类型("你好")
判断类型([1, 2, 3])
判断类型(真)