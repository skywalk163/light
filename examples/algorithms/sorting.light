# 段言 - 排序与搜索算法库
# 包含 12+ 种经典算法实现
# 用法: duan run examples/algorithms/sorting.duan

# ===== 工具函数 =====

段落 打印数组 接收 数组：
  设 结果 为 "["。
  设 i 为 0。
  当 i 小于 列表长度(数组)：
    如果 i 大于 0：
      结果 为 结果 + ", "。
    结果 为 结果 + 转字符串(数组[i])。
    i 为 i + 1。
  结果 为 结果 + "]"。
  打印(结果)。

段落 打印分隔线：
  打印("----------------------------------------")。

# ===== 1. 冒泡排序 =====
段落 冒泡排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 i 为 0。
  当 i 小于 n - 1：
    设 j 为 0。
    当 j 小于 n - i - 1：
      如果 数组[j] 大于 数组[j + 1]：
        设 临时 为 数组[j]。
        数组[j] 为 数组[j + 1]。
        数组[j + 1] 为 临时。
      j 为 j + 1。
    i 为 i + 1。
  返回 数组。

# ===== 2. 选择排序 =====
段落 选择排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 i 为 0。
  当 i 小于 n - 1：
    设 最小索引 为 i。
    设 j 为 i + 1。
    当 j 小于 n：
      如果 数组[j] 小于 数组[最小索引]：
        最小索引 为 j。
      j 为 j + 1。
    如果 最小索引 不等于 i：
      设 临时 为 数组[i]。
      数组[i] 为 数组[最小索引]。
      数组[最小索引] 为 临时。
    i 为 i + 1。
  返回 数组。

# ===== 3. 插入排序 =====
段落 插入排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 i 为 1。
  当 i 小于 n：
    设 键 为 数组[i]。
    设 j 为 i - 1。
    当 j 大于等于 0 且 数组[j] 大于 键：
      数组[j + 1] 为 数组[j]。
      j 为 j - 1。
    数组[j + 1] 为 键。
    i 为 i + 1。
  返回 数组。

# ===== 4. 快速排序 =====
段落 快速排序分区 接收 数组, 低, 高：
  设 基准 为 数组[高]。
  设 i 为 低 - 1。
  设 j 为 低。
  当 j 小于 高：
    如果 数组[j] 小于等于 基准：
      i 为 i + 1。
      设 临时 为 数组[i]。
      数组[i] 为 数组[j]。
      数组[j] 为 临时。
    j 为 j + 1。
  设 临时 为 数组[i + 1]。
  数组[i + 1] 为 数组[高]。
  数组[高] 为 临时。
  返回 i + 1。

段落 快速排序递归 接收 数组, 低, 高：
  如果 低 小于 高：
    设 分区索引 为 快速排序分区(数组, 低, 高)。
    快速排序递归(数组, 低, 分区索引 - 1)。
    快速排序递归(数组, 分区索引 + 1, 高)。

段落 快速排序 接收 数组：
  如果 列表长度(数组) 小于等于 1：
    返回 数组。
  快速排序递归(数组, 0, 列表长度(数组) - 1)。
  返回 数组。

# ===== 5. 归并排序 =====
段落 合并 接收 数组, 左, 中, 右：
  设 左大小 为 中 - 左 + 1。
  设 右大小 为 右 - 中。
  设 左数组 为 []。
  设 右数组 为 []。

  设 i 为 0。
  当 i 小于 左大小：
    列表追加(左数组, 数组[左 + i])。
    i 为 i + 1。

  设 i 为 0。
  当 i 小于 右大小：
    列表追加(右数组, 数组[中 + 1 + i])。
    i 为 i + 1。

  设 i 为 0。
  设 j 为 0。
  设 k 为 左。
  当 i 小于 左大小 且 j 小于 右大小：
    如果 左数组[i] 小于等于 右数组[j]：
      数组[k] 为 左数组[i]。
      i 为 i + 1。
    否则：
      数组[k] 为 右数组[j]。
      j 为 j + 1。
    k 为 k + 1。

  当 i 小于 左大小：
    数组[k] 为 左数组[i]。
    i 为 i + 1。
    k 为 k + 1。

  当 j 小于 右大小：
    数组[k] 为 右数组[j]。
    j 为 j + 1。
    k 为 k + 1。

段落 归并排序递归 接收 数组, 左, 右：
  如果 左 小于 右：
    设 中 为 (左 + 右) / 2。
    归并排序递归(数组, 左, 中)。
    归并排序递归(数组, 中 + 1, 右)。
    合并(数组, 左, 中, 右)。

段落 归并排序 接收 数组：
  如果 列表长度(数组) 小于等于 1：
    返回 数组。
  归并排序递归(数组, 0, 列表长度(数组) - 1)。
  返回 数组。

# ===== 6. 希尔排序 =====
段落 希尔排序 接收 数组：
  设 n 为 列表长度(数组)。
  设 间隔 为 n / 2。
  当 间隔 大于 0：
    设 i 为 间隔。
    当 i 小于 n：
      设 临时 为 数组[i]。
      设 j 为 i。
      当 j 大于等于 间隔 且 数组[j - 间隔] 大于 临时：
        数组[j] 为 数组[j - 间隔]。
        j 为 j - 间隔。
      数组[j] 为 临时。
      i 为 i + 1。
    间隔 为 间隔 / 2。
  返回 数组。

# ===== 7. 计数排序 =====
段落 计数排序 接收 数组：
  如果 列表长度(数组) 等于 0：
    返回 数组。

  设 最大值 为 数组[0]。
  遍历 值 于 数组：
    如果 值 大于 最大值：
      最大值 为 值。

  设 计数 为 []。
  设 i 为 0。
  当 i 小于等于 最大值：
    列表追加(计数, 0)。
    i 为 i + 1。

  遍历 值 于 数组：
    计数[值] 为 计数[值] + 1。

  设 结果 为 []。
  设 i 为 0。
  当 i 小于等于 最大值：
    当 计数[i] 大于 0：
      列表追加(结果, i)。
      计数[i] 为 计数[i] - 1。
    i 为 i + 1。
  返回 结果。

# ===== 8. 桶排序 =====
段落 桶排序 接收 数组, 桶数：
  如果 列表长度(数组) 等于 0：
    返回 数组。

  设 最小值 为 数组[0]。
  设 最大值 为 数组[0]。
  遍历 值 于 数组：
    如果 值 小于 最小值：
      最小值 为 值。
    如果 值 大于 最大值：
      最大值 为 值。

  设 桶范围 为 (最大值 - 最小值) / 桶数 + 1。
  设 桶 为 []。
  设 i 为 0。
  当 i 小于 桶数：
    列表追加(桶, [])。
    i 为 i + 1。

  遍历 值 于 数组：
    设 桶索引 为 (值 - 最小值) / 桶范围。
    列表追加(桶[桶索引], 值)。

  设 结果 为 []。
  遍历 桶 于 桶：
    插入排序(桶)。
    遍历 值 于 桶：
      列表追加(结果, 值)。
  返回 结果。

# ===== 9. 二分查找 =====
段落 二分查找 接收 数组, 目标：
  设 左 为 0。
  设 右 为 列表长度(数组) - 1。
  当 左 小于等于 右：
    设 中 为 (左 + 右) / 2。
    如果 数组[中] 等于 目标：
      返回 中。
    否则若 数组[中] 小于 目标：
      左 为 中 + 1。
    否则：
      右 为 中 - 1。
  返回 -1。

# ===== 10. 线性查找 =====
段落 线性查找 接收 数组, 目标：
  设 i 为 0。
  当 i 小于 列表长度(数组)：
    如果 数组[i] 等于 目标：
      返回 i。
    i 为 i + 1。
  返回 -1。

# ===== 11. 斐波那契查找 =====
段落 斐波那契查找 接收 数组, 目标：
  设 n 为 列表长度(数组)。
  设 fib2 为 0。
  设 fib1 为 1。
  设 fib 为 fib2 + fib1。
  当 fib 小于 n：
    fib2 为 fib1。
    fib1 为 fib。
    fib 为 fib2 + fib1。

  设 偏移 为 -1。
  当 fib 大于 1：
    设 i 为 最小值(偏移 + fib2, n - 1)。
    如果 数组[i] 小于 目标：
      fib 为 fib1。
      fib1 为 fib2。
      fib2 为 fib - fib1。
      偏移 为 i。
    否则若 数组[i] 大于 目标：
      fib 为 fib2。
      fib1 为 fib1 - fib2。
      fib2 为 fib - fib1。
    否则：
      返回 i。

  如果 fib1 且 数组[偏移 + 1] 等于 目标：
    返回 偏移 + 1。
  返回 -1。

# ===== 12. 跳转查找 =====
段落 跳转查找 接收 数组, 目标：
  设 n 为 列表长度(数组)。
  设 步长 为 平方根(n)。
  设 上一步 为 0。

  当 数组[最小值(步长, n) - 1] 小于 目标：
    上一步 为 步长。
    步长 为 步长 + 步长。
    如果 上一步 大于等于 n：
      返回 -1。

  设 i 为 上一步。
  当 i 小于 最小值(步长, n)：
    如果 数组[i] 等于 目标：
      返回 i。
    i 为 i + 1。
  返回 -1。

# ===== 测试函数 =====
段落 测试排序算法 接收 名称, 排序函数, 数组：
  设 测试数组 为 []。
  遍历 值 于 数组：
    列表追加(测试数组, 值)。
  打印("  " + 名称 + ": ")。
  排序函数(测试数组)。
  打印("    结果: ")。
  打印数组(测试数组)。

段落 测试查找算法 接收 名称, 查找函数, 数组, 目标：
  设 索引 为 查找函数(数组, 目标)。
  打印("  " + 名称 + ": 查找 " + 转字符串(目标) + " -> 索引 " + 转字符串(索引))。

段落 测试所有算法：
  设 无序数组 为 [64, 34, 25, 12, 22, 11, 90, 5, 77, 30]。
  设 有序数组 为 [5, 11, 12, 22, 25, 30, 34, 64, 77, 90]。

  打印("===== 排序算法测试 =====")。
  打印("原始数组: ")。
  打印数组(无序数组)。
  打印分隔线()。

  测试排序算法("冒泡排序", 冒泡排序, 无序数组)。
  测试排序算法("选择排序", 选择排序, 无序数组)。
  测试排序算法("插入排序", 插入排序, 无序数组)。
  测试排序算法("快速排序", 快速排序, 无序数组)。
  测试排序算法("归并排序", 归并排序, 无序数组)。
  测试排序算法("希尔排序", 希尔排序, 无序数组)。
  测试排序算法("计数排序", 计数排序, [4, 2, 2, 8, 3, 3, 1])。
  测试排序算法("桶排序", 桶排序, 无序数组, 5)。

  打印分隔线()。
  打印("\n===== 查找算法测试 =====")。
  打印("有序数组: ")。
  打印数组(有序数组)。
  打印分隔线()。

  测试查找算法("二分查找", 二分查找, 有序数组, 25)。
  测试查找算法("线性查找", 线性查找, 有序数组, 77)。
  测试查找算法("二分查找", 二分查找, 有序数组, 100)。
  测试查找算法("线性查找", 线性查找, 有序数组, 100)。

  打印("\n===== 全部算法测试完成 =====")。
  打印("共测试 8 种排序算法 + 4 种查找算法 = 12 种算法。")。

测试所有算法()。