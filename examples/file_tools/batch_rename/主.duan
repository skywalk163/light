# 段言 - 文件批量重命名工具
# 功能: 支持添加前缀、后缀的批量文件重命名工具
# 用法: duan run examples/file_tools/batch_rename/主.duan <目录> [选项]
# 示例:
#   duan run examples/file_tools/batch_rename/主.duan ./我的文件 --prefix "备份_"
#   duan run examples/file_tools/batch_rename/主.duan ./照片 --suffix "_v2"

导入 sys。

# ===== 配置 =====
设 当前目录 为 ""。
设 要添加前缀 为 ""。
设 要添加后缀 为 ""。
设 预览模式 为 假。
设 计数器 为 0。

# 获取文件扩展名
段落 获取扩展名 接收 文件名:
  设 点位置 为 0。
  设 长度 为 字符串长度(文件名)。
  设 索引 为 长度 - 1。
  当 索引 大于 0:
    如果 字符串获取(文件名, 索引) 等于 ".":
      点位置 为 索引。
      跳出。
    索引 为 索引 - 1。
  如果 点位置 大于 0:
    返回 截取(文件名, 点位置, 长度)。
  返回 ""。

# 获取文件名（不含扩展名）
段落 获取基础名 接收 文件名:
  设 点位置 为 0。
  设 长度 为 字符串长度(文件名)。
  设 索引 为 长度 - 1。
  当 索引 大于 0:
    如果 字符串获取(文件名, 索引) 等于 ".":
      点位置 为 索引。
      跳出。
    索引 为 索引 - 1。
  如果 点位置 大于 0:
    返回 截取(文件名, 0, 点位置)。
  返回 文件名。

# 生成新文件名
段落 生成新文件名 接收 原文件名:
  设 扩展名 为 获取扩展名(原文件名)。
  设 基础名 为 获取基础名(原文件名)。
  设 新文件名 为 要添加前缀 + 基础名 + 要添加后缀 + 扩展名。
  返回 新文件名。

# 执行批量重命名（预览模式）
段落 执行批量重命名 接收 目录路径:
  设 成功数 为 0。
  设 文件列表 为 列出目录(目录路径)。
  如果 列表长度(文件列表) 等于 0:
    打印("目录中没有找到文件。")。
    返回。

  打印("找到 " + 转字符串(列表长度(文件列表)) + " 个文件")。
  打印("")。

  遍历 文件名 于 文件列表:
    设 完整路径 为 目录路径 + "/" + 文件名。
    如果 文件存在(完整路径) 等于 假:
      跳过。
    设 新文件名 为 生成新文件名(文件名)。
    如果 新文件名 等于 文件名:
      跳过。
    计数器 为 计数器 + 1。
    设 预览消息 为 "  [" + 转字符串(计数器) + "] " + 文件名 + " -> " + 新文件名。
    打印(预览消息)。
    成功数 为 成功数 + 1。

  打印("")。
  打印("预览完成: 共 " + 转字符串(成功数) + " 个文件将被重命名")。
  如果 预览模式 等于 假:
    打印("提示: 使用 --dry-run 参数可预览效果，实际重命名功能需要额外权限。")。

# 显示帮助信息
段落 显示帮助:
  打印("段言文件批量重命名工具")。
  打印("")。
  打印("  duan run examples/file_tools/batch_rename/主.duan <目录> [选项]")。
  打印("")。
  打印("选项:")。
  打印("  --prefix <文本>    添加前缀")。
  打印("  --suffix <文本>    添加后缀")。
  打印("  --dry-run          预览模式")。
  打印("  --help             显示帮助")。
  打印("")。

# ===== 参数解析 =====
段落 解析参数 接收 参数列表:
  设 索引 为 0。
  设 参数数量 为 列表长度(参数列表)。
  如果 参数数量 小于 2:
    显示帮助()。
    返回 假。
  当前目录 为 参数列表[1]。
  如果 目录存在(当前目录) 等于 假:
    打印("错误: 目录不存在 - " + 当前目录)。
    返回 假。
  索引 为 2。
  当 索引 小于 参数数量:
    设 参数 为 参数列表[索引]。
    如果 参数 等于 "--prefix":
      索引 为 索引 + 1。
      如果 索引 小于 参数数量:
        要添加前缀 为 参数列表[索引]。
    否则若 参数 等于 "--suffix":
      索引 为 索引 + 1。
      如果 索引 小于 参数数量:
        要添加后缀 为 参数列表[索引]。
    否则若 参数 等于 "--dry-run":
      预览模式 为 真。
    否则若 参数 等于 "--help":
      显示帮助()。
      返回 假。
    索引 为 索引 + 1。
  返回 真。

# ===== 主程序 =====
段落 主程序:
  如果 非 解析参数(sys.argv):
    返回。
  打印("")。
  打印("段言文件批量重命名工具")。
  打印("目标目录: " + 当前目录)。
  如果 要添加前缀 不等于 "":
    打印("添加前缀: " + 要添加前缀)。
  如果 要添加后缀 不等于 "":
    打印("添加后缀: " + 要添加后缀)。
  如果 预览模式:
    打印("模式: 预览")。
  否则:
    打印("模式: 执行")。
  打印("")。
  执行批量重命名(当前目录)。

主程序()。