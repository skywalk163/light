# 段言 - 终端计算器
# 功能: 支持加减乘除四则运算，带历史记录
# 用法: duan run examples/calculator_app/主.duan

导入 sys。

设 历史记录 为 []。
设 当前值 为 0。

段落 计算 接收 运算符, 操作数:
  如果 运算符 等于 "+":
    当前值 为 当前值 + 操作数。
  否则若 运算符 等于 "-":
    当前值 为 当前值 - 操作数。
  否则若 运算符 等于 "*":
    当前值 为 当前值 * 操作数。
  否则若 运算符 等于 "/":
    如果 操作数 不等于 0:
      当前值 为 当前值 / 操作数。
    否则:
      打印("错误: 除数不能为零。")。
      返回 假。
  否则:
    打印("错误: 未知运算符。")。
    返回 假。

  设 记录 为 运算符 + " " + 转字符串(操作数)。
  列表追加(历史记录, 记录)。
  返回 真。

段落 显示历史:
  打印("")。
  打印("计算历史:")。
  打印("-" * 30)。
  遍历 记录 于 历史记录:
    打印("  " + 记录)。
  打印("-" * 30)。

段落 主程序:
  打印("")。
  打印("段言计算器")。
  打印("支持: +, -, *, /")。
  打印("输入: 运算符 数值（如: + 5）")。
  打印("输入: '历史' 查看历史记录")。
  打印("输入: '清空' 重置")。
  打印("输入: '退出' 退出")。
  打印("")。
  当前值 为 0。
  打印("当前值: " + 转字符串(当前值))。
  打印("请使用命令行参数输入: duan run 主.duan + 5")。

  设 参数 为 sys.argv。
  如果 列表长度(参数) 大于 2:
    设 运算符 为 参数[1]。
    设 操作数 为 转整数(参数[2])。
    计算(运算符, 操作数)。
    打印("结果: " + 转字符串(当前值))。

主程序()。