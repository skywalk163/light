# L-170 复现与回归用例
# 验证：核心动词（VERB_ARITY 成员，如 映射/筛选）作变量名用于「下标/成员赋值目标」时，
# 必须真执行赋值，而非被静默编译成比较表达式（比较表达式不报错、赋值丢失）。
#
# 关键坑：deterministic lexer 会把「已被声明过的动词名」（如 `设 映射 为 {}` 之后的 映射）
# 重分类为 IDENTIFIER，因此真实用法 `设 映射 为 {}` 之后 `映射[k] 为 X` 里 映射 是 IDENTIFIER，
# 守卫必须同时覆盖 KEYWORD 与 IDENTIFIER 两种 token 类型，否则静默变比较。
#
# 铁律：修后必须断言赋值真的生效（用 如果/抛出 显式断言，非仅看编译产物）。

段落 主:
  # —— 场景1：下标赋值（最典型的静默 bug 形态）——
  #   此处起 映射 被影射为 dict 变量（lexer 把其后出现的 映射 重分类为 IDENTIFIER）。
  设 映射 为 {}
  设 k 为 "a"
  映射[k] 为 99
  如果 映射[k] != 99: 抛出 "L170_下标赋值_未生效"

  # —— 场景2：之 成员赋值 ——
  映射之x 为 7
  如果 映射["x"] != 7: 抛出 "L170_之成员赋值_未生效"

  # —— 场景3：. 成员赋值 ——
  映射.项 为 8
  如果 映射["项"] != 8: 抛出 "L170_dot成员赋值_未生效"

  # —— 场景4：另一 VERB_ARITY 动词 筛选 同样形态 ——
  设 筛选 为 {}
  设 k2 为 "b"
  筛选[k2] 为 2
  如果 筛选[k2] != 2: 抛出 "L170_筛选下标赋值_未生效"

  打印("L170-OK")
