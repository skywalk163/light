# 段言 - 文件搜索工具
# 功能: 按文件名关键词搜索文件，支持扩展名筛选
# 用法: duan run examples/file_search/主.duan <关键词> [扩展名]
# 示例:
#   duan run examples/file_search/主.duan .duan
#   duan run examples/file_search/主.duan .py src

导入 sys。
导入 文件系统。

段落 搜索文件 接收 目录, 关键词, 扩展名:
  设 结果 为 []。
  设 全部文件 为 列出目录(目录)。
  遍历 文件名 于 全部文件:
    设 完整路径 为 目录 + "/" + 文件名。
    如果 文件存在(完整路径) 等于 假:
      跳过。
    如果 扩展名 不等于 "" 且 文件名.包含(扩展名) 等于 假:
      跳过。
    如果 关键词 等于 "" 或 文件名 包含 关键词:
      设 文件大小 为 文件系统.获取文件名(完整路径)。
      设 信息 为 文件名 + " (" + 转字符串(文件大小) + " 字节)"。
      列表追加(结果, 信息)。
  返回 结果。

段落 主程序:
  设 参数 为 sys.argv。
  设 关键词 为 ""。
  设 扩展名 为 ""。
  如果 列表长度(参数) 大于 1:
    关键词 为 参数[1]。
  如果 列表长度(参数) 大于 2:
    扩展名 为 参数[2]。
  打印("搜索关键词: " + 关键词)。
  如果 扩展名 不等于 "":
    打印("扩展名筛选: " + 扩展名)。
  打印("")。
  设 结果 为 搜索文件(".", 关键词, 扩展名)。
  打印("找到 " + 转字符串(列表长度(结果)) + " 个文件")。
  打印("")。
  遍历 文件信息 于 结果:
    打印("  " + 文件信息)。

主程序()。