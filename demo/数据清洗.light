# 光明语言 - 数据清洗演示
# C5.2: 数据清洗（去重/过滤/格式化/排序）

段 去重(列表):
  设 结果 为 []
  遍 列表 于 项:
    若 结果为 [] 或 不包含(结果, 项) 则:
      结果 为 结果 + [项]
  返 结果

段 不包含(列表, 项):
  遍 列表 于 x:
    若 x == 项 则:
      返 假
  返 真

段 过滤(列表, 阈值):
  设 结果 为 []
  遍 列表 于 项:
    若 项 > 阈值 则:
      结果 为 结果 + [项]
  返 结果

段 排序(列表):
  设 长度 为 列表之长度
  设 新列 为 列表
  设 i 为 0
  当 i < 长度:
    设 j 为 i + 1
    当 j < 长度:
      若 新列[i] > 新列[j] 则:
        设 临时 为 新列[i]
        新列[i] 为 新列[j]
        新列[j] 为 临时
      j 为 j + 1
    i 为 i + 1
  返 新列

段 格式化(列表):
  设 结果 为 []
  遍 列表 于 项:
    设 格式化后 为 项
    若 格式化后 < 10 则:
      结果 为 结果 + ["0" + 转字符串(格式化后)]
    否:
      结果 为 结果 + [转字符串(格式化后)]
  返 结果

# 主程序
印("=== 数据清洗演示 ===")
印("")

# 原始数据（含重复、异常值）
设 原始数据 为 [3, 7, 1, 3, 9, 2, 7, 5, 1, 8, 0, 4, 6, 9, 3]
印("原始数据：")
印(原始数据)
印("")

# 去重
设 去重后 为 去重(原始数据)
印("去重后：")
印(去重后)
印("")

# 过滤（保留大于 3 的值）
设 过滤后 为 过滤(去重后, 3)
印("过滤（>3）：")
印(过滤后)
印("")

# 排序
设 排序后 为 排序(过滤后)
印("排序后：")
印(排序后)
印("")

# 格式化输出
设 格式化后 为 格式化(排序后)
印("格式化输出：")
印(格式化后)
印("")

印("=== 数据清洗完成 ===")