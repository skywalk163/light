# Reasonix 推理引擎 - 主入口
# 段言 (Duan) 编程语言能力演示

从 引擎 导入 推理引擎 创建引擎。
从 工具 导入 显示横幅 显示帮助 显示历史 分隔线 清屏。

段落 主程序:
  设 引擎 为 创建引擎()。

  清屏()。
  显示横幅()。
  打印("")。
  打印("  请输入您的问题（输入 '帮助' 查看命令）:")。
  打印("  " + 分隔线("─", 40))。
  打印("")。

  当 真:
    打印("  > ")。
    设 输入文本 为 输入()。
    设 文本 为 去除空白(输入文本)。

    如果 文本 等于 "":
      继续。

    如果 文本 等于 "退出":
      打印("")。
      打印("  感谢使用 Reasonix 推理引擎！")。
      打印("  再见！")。
      打印("")。
      跳出。

    否则若 文本 等于 "帮助":
      显示帮助()。

    否则若 文本 等于 "历史":
      设 历史记录 为 引擎.获取历史()。
      显示历史(历史记录)。

    否则若 文本 等于 "清屏":
      清屏()。
      显示横幅()。
      打印("")。
      打印("  请输入您的问题（输入 '帮助' 查看命令）:")。
      打印("  " + 分隔线("─", 40))。
      打印("")。

    否则:
      引擎.处理问题(文本)。
      打印("")。

  返回 0。

主程序()。