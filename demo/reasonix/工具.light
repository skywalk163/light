# Reasonix 推理引擎 - 工具函数模块
# 提供格式化输出、界面显示等工具函数

导出 显示横幅 格式化阶段 格式化答案 显示帮助 显示历史。
导出 分隔线 清屏 显示徽标。

段落 清屏:
  打印("\n" * 50)。

段落 分隔线 接收 字符, 长度:
  设 结果 为 ""。
  设 i 为 0。
  当 i 小于 长度:
    结果 为 结果 + 字符。
    i 为 i + 1。
  返回 结果。

段落 显示徽标:
  打印("  ╔══════════════════════════════════════════╗")。
  打印("  ║      段言 Reasonix 推理引擎 v1.0         ║")。
  打印("  ║    段言 (Duan) 编程语言 - 能力演示       ║")。
  打印("  ╚══════════════════════════════════════════╝")。

段落 显示横幅:
  显示徽标()。
  打印("")。
  打印("  " + 分隔线("─", 40))。
  打印("  一个基于 Chain-of-Thought 的多步推理引擎")。
  打印("  " + 分隔线("─", 40))。
  打印("")。

段落 格式化阶段 接收 阶段号, 总阶段数, 标题, 内容:
  打印("")。
  设 前缀 为 "┌─ 第" + 转字符串(阶段号) + "阶段/共" + 转字符串(总阶段数) + "阶段: " + 标题。
  # 用显示宽度（中文计 2）计算填充，使上边框与下边框右侧对齐
  设 填充 为 57 - 显示宽度(前缀)。
  如果 填充 小于 0:
    填充 为 0。
  设 上边框 为 前缀 + 分隔线("─", 填充) + "┐"。
  打印("  " + 上边框)。
  设 行列表 为 分割字符串(内容, "\n")。
  遍历 行 于 行列表:
    打印("  │ " + 行)。
  设 下边框 为 "└" + 分隔线("─", 56) + "┘"。
  打印("  " + 下边框)。
  打印("")。

段落 格式化答案 接收 答案:
  打印("")。
  打印("  " + 分隔线("═", 56))。
  # 标题行内部宽度固定为 54（两个 ║ 之间），用显示宽度计算右补空格
  设 标签 为 "  最终答案"。
  设 补 为 54 - 显示宽度(标签)。
  如果 补 小于 0:
    补 为 0。
  打印("  ║" + 标签 + 分隔线(" ", 补) + "║")。
  打印("  " + 分隔线("═", 56))。
  设 行列表 为 分割字符串(答案, "\n")。
  遍历 行 于 行列表:
    打印("  " + 行)。
  打印("  " + 分隔线("═", 56))。
  打印("")。

段落 显示帮助:
  打印("")。
  打印("  " + 分隔线("─", 40))。
  打印("  可用命令:")。
  打印("")。
  打印("    <任意问题>    - 开始推理")。
  打印("    退出          - 退出程序")。
  打印("    帮助          - 显示此帮助信息")。
  打印("    历史          - 显示推理历史")。
  打印("    清屏          - 清空屏幕")。
  打印("  " + 分隔线("─", 40))。
  打印("")。

段落 显示历史 接收 记录列表:
  打印("")。
  打印("  " + 分隔线("═", 56))。
  打印("  推理历史 (" + 转字符串(列表长度(记录列表)) + " 条)")。
  打印("  " + 分隔线("═", 56))。
  如果 列表长度(记录列表) 等于 0:
    打印("  (暂无推理记录)")。
  否则:
    设 索引 为 0。
    遍历 记录 于 记录列表:
      索引 为 索引 + 1。
      打印("  " + 转字符串(索引) + ". " + 记录["问题"])。
  打印("  " + 分隔线("═", 56))。
  打印("")。