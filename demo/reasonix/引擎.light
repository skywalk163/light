# Reasonix 推理引擎 - 核心引擎模块
# 管理推理流程，支持 AI API 调用和模拟模式

导出 推理引擎 创建引擎。

从 思考链 导入 思考链 创建思考链。
从 提示词 导入 系统提示词 生成分析阶段 生成AI阶段提示。
从 工具 导入 格式化阶段 格式化答案 显示横幅 显示历史 显示帮助 分隔线 清屏。
导入 Python: ai_api_helper。

类 推理引擎:
  属性 名称 等于 "Reasonix 推理引擎"。
  属性 版本 等于 "1.0.0"。
  属性 历史记录 等于 []。
  属性 AI配置 等于 空。
  属性 AI可用 等于 假。

  段落 初始化:
    己历史记录 为 []。
    己AI配置 为 ai_api_helper.load_config()。
    己AI可用 为 ai_api_helper.check_api_key()。
    打印("")。
    如果 己AI可用:
      打印("  [AI 模式] 使用 " + 己AI配置["model"] + " 模型")。
    否则:
      打印("  [模拟模式] 未配置 API 密钥，使用模拟推理")。
      打印("  [提示] 在 .env 中设置 REASONIX_API_KEY 以启用 AI 模式")。
    打印("")。

  段落 调用AI 接收 提示词, 系统提示:
    如果 己AI可用:
      设 结果 为 ai_api_helper.call_ai(系统提示, 提示词, 己AI配置)。
      如果 结果 不等于 空:
        返回 结果。
    返回 空。

  段落 推理 接收 问题:
    设 链 为 创建思考链(问题)。
    设 阶段数据 为 生成分析阶段(问题)。
    设 总阶段数 为 列表长度(阶段数据)。

    设 i 为 0。
    当 i 小于 总阶段数:
      设 当前阶段 为 阶段数据[i]。
      设 标题 为 当前阶段["标题"]。
      设 模拟内容 为 当前阶段["内容"]。

      如果 己AI可用:
        打印("  [AI 分析中...] " + 标题)。
        设 AI提示 为 生成AI阶段提示(问题, 标题, i + 1, 总阶段数)。
        设 AI内容 为 己.调用AI(AI提示, 系统提示词())。
        如果 AI内容 不等于 空:
          链.推演阶段(标题, AI内容)。
          格式化阶段(i + 1, 总阶段数, 标题, AI内容)。
        否则:
          打印("  [回退到模拟内容]")。
          链.推演阶段(标题, 模拟内容)。
          格式化阶段(i + 1, 总阶段数, 标题, 模拟内容)。
      否则:
        链.推演阶段(标题, 模拟内容)。
        格式化阶段(i + 1, 总阶段数, 标题, 模拟内容)。

      如果 i 小于 总阶段数 - 1:
        打印("  [推理中...]")。
      否则:
        打印("  [推理完成]")。

      i 为 i + 1。

    设 最终答案 为 链.综合答案()。
    返回 最终答案。

  段落 处理问题 接收 问题:
    显示横幅()。
    打印("")。
    打印("  问题: " + 问题)。
    打印("  " + 分隔线("─", 56))。
    打印("  开始推理...")。
    设 答案 为 己.推理(问题)。
    格式化答案(答案)。

    设 记录 为 {"问题": 问题, "答案": 答案}。
    列表追加(己历史记录, 记录)。
    返回 答案。

  段落 获取历史:
    返回 己历史记录。

段落 创建引擎:
  返回 新建 推理引擎()。