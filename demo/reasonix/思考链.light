# Reasonix 推理引擎 - 思考链模块
# 管理推理阶段的添加、展示和综合

导出 思考链 创建思考链。

从 提示词 导入 生成分析阶段 综合答案。

类 思考链:
  属性 问题 等于 ""。
  属性 阶段列表 等于 []。
  属性 当前阶段数 等于 0。

  段落 初始化 接收 问题文本:
    己问题 为 问题文本。
    己阶段列表 为 []。
    己当前阶段数 为 0。

  段落 推演阶段 接收 标题, 内容:
    设 序号值 为 己当前阶段数 + 1。
    设 新阶段 为 {"标题": 标题, "内容": 内容, "序号": 序号值}。
    列表追加(己阶段列表, 新阶段)。
    己当前阶段数 为 序号值。

  段落 获取阶段数:
    返回 己当前阶段数。

  段落 获取所有阶段:
    返回 己阶段列表。

  段落 获取阶段 接收 索引:
    如果 索引 大于等于 0 且 索引 小于 己当前阶段数:
      返回 己阶段列表[索引]。
    返回 空。

  段落 综合答案:
    设 答案 为 "基于 " + 转字符串(己当前阶段数) + " 个推理阶段，结论如下：\n\n"。
    设 i 为 0。
    当 i 小于 己当前阶段数:
      设 当前阶段 为 己阶段列表[i]。
      答案 为 答案 + "阶段 " + 转字符串(当前阶段["序号"]) + "：" + 当前阶段["标题"] + "\n"。
      答案 为 答案 + "  " + 当前阶段["内容"] + "\n\n"。
      i 为 i + 1。
    答案 为 答案 + "══════════════════════════════\n"。
    答案 为 答案 + "综合以上分析，得出最终结论。"。
    返回 答案。

段落 创建思考链 接收 问题:
  返回 新建 思考链(问题)。