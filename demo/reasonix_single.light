# Reasonix 推理引擎 - 单文件演示版
# 段言 (Duan) 编程语言能力演示
# 模拟 Claude 风格的多步推理过程

# ===== 工具函数 =====

段落 清屏:
  打印("\n" * 50)。

段落 分隔线 接收 字符, 长度:
  设 结果 为 ""。
  设 i 为 0。
  当 i 小于 长度:
    结果 为 结果 + 字符。
    i 为 i + 1。
  返回 结果。

段落 显示徽标:
  打印("  ╔══════════════════════════════════════════╗")。
  打印("  ║      段言 Reasonix 推理引擎 v1.0         ║")。
  打印("  ║    段言 (Duan) 编程语言 - 能力演示       ║")。
  打印("  ╚══════════════════════════════════════════╝")。

段落 显示横幅:
  显示徽标()。
  打印("")。
  打印("  " + 分隔线("─", 40))。
  打印("  一个基于 Chain-of-Thought 的多步推理引擎")。
  打印("  " + 分隔线("─", 40))。
  打印("")。

段落 格式化环节 接收 环节序, 总环节数, 标题, 内容:
  打印("")。
  设 前缀 为 "┌─ 第" + 转字符串(环节序) + "环节/共" + 转字符串(总环节数) + "环节: " + 标题。
  设 填充 为 56 - 字符串长度(前缀)。
  如果 填充 小于 0:
    填充 为 0。
  设 上边框 为 前缀 + 分隔线("─", 填充) + "┐"。
  打印("  " + 上边框)。
  设 行列表 为 分割字符串(内容, "\n")。
  遍历 行 于 行列表:
    打印("  │ " + 行)。
  设 下边框 为 "  └" + 分隔线("─", 56) + "┘"。
  打印("  " + 下边框)。
  打印("")。

段落 格式化答案 接收 答案:
  打印("")。
  打印("  " + 分隔线("═", 56))。
  打印("  ║  最终答案" + 分隔线(" ", 46) + "║")。
  打印("  " + 分隔线("═", 56))。
  设 行列表 为 分割字符串(答案, "\n")。
  遍历 行 于 行列表:
    打印("  " + 行)。
  打印("  " + 分隔线("═", 56))。
  打印("")。

段落 显示帮助:
  打印("")。
  打印("  " + 分隔线("─", 40))。
  打印("  可用命令:")。
  打印("")。
  打印("    <任意问题>    - 开始推理")。
  打印("    退出          - 退出程序")。
  打印("    帮助          - 显示此帮助信息")。
  打印("    历史          - 显示推理历史")。
  打印("    清屏          - 清空屏幕")。
  打印("  " + 分隔线("─", 40))。
  打印("")。

段落 显示历史 接收 记录列表:
  打印("")。
  打印("  " + 分隔线("═", 56))。
  打印("  推理历史 (" + 转字符串(列表长度(记录列表)) + " 条)")。
  打印("  " + 分隔线("═", 56))。
  如果 列表长度(记录列表) 等于 0:
    打印("  (暂无推理记录)")。
  否则:
    设 索引 为 0。
    遍历 记录 于 记录列表:
      索引 为 索引 + 1。
      打印("  " + 转字符串(索引) + ". " + 记录["问题"])。
  打印("  " + 分隔线("═", 56))。
  打印("")。

# ===== 提示词模板 =====

段落 生成分析环节 接收 问题:
  设 环节列表 为 []。
  设 环节1 为 {"title": "理解问题", "content": "正在分析问题：「" + 问题 + "」\n识别问题类型和关键信息。"}。
  列表追加(环节列表, 环节1)。
  设 环节2 为 {"title": "信息提取", "content": "从问题中提取关键实体和关系。\n识别已知条件和待求解目标。"}。
  列表追加(环节列表, 环节2)。
  设 环节3 为 {"title": "逻辑推理", "content": "应用逻辑规则和领域知识进行推理。\n检查推理链条的完整性和一致性。"}。
  列表追加(环节列表, 环节3)。
  设 环节4 为 {"title": "验证答案", "content": "验证推理结果的正确性。\n确认答案完整地回答了原始问题。"}。
  列表追加(环节列表, 环节4)。
  返回 环节列表。

# ===== 思考链模块 =====

类 思考链:
  属性 问题 等于 ""。
  属性 环节列表 等于 []。
  属性 当前环节数 等于 0。

  段落 初始化 接收 问题文本:
    己问题 为 问题文本。
    己环节列表 为 []。
    己当前环节数 为 0。

  段落 添加环节 接收 标题, 内容:
    设 次序值 为 己当前环节数 + 1。
    设 新环节 为 {"title": 标题, "content": 内容, "index": 次序值}。
    列表追加(己环节列表, 新环节)。
    己当前环节数 为 次序值。

  段落 获取所有环节:
    返回 己环节列表。

  段落 综合答案:
    设 答案 为 "基于 " + 转字符串(己当前环节数) + " 个推理环节，结论如下：\n\n"。
    设 i 为 0。
    当 i 小于 己当前环节数:
      设 当前环节 为 己环节列表[i]。
      答案 为 答案 + "环节 " + 转字符串(当前环节["index"]) + "：" + 当前环节["title"] + "\n"。
      答案 为 答案 + "  " + 当前环节["content"] + "\n\n"。
      i 为 i + 1。
    答案 为 答案 + "══════════════════════════════\n"。
    答案 为 答案 + "综合以上分析，得出最终结论。"。
    返回 答案。

# ===== 核心引擎模块 =====

类 推理引擎:
  属性 名称 等于 "Reasonix 推理引擎"。
  属性 版本 等于 "1.0.0"。
  属性 历史记录 等于 []。

  段落 初始化:
    己历史记录 为 []。

  段落 推理 接收 问题:
    设 链 为 新建 思考链(问题)。
    设 环节数据 为 生成分析环节(问题)。
    设 总环节数 为 列表长度(环节数据)。

    设 i 为 0。
    当 i 小于 总环节数:
      设 当前环节 为 环节数据[i]。
      设 标题 为 当前环节["title"]。
      设 内容 为 当前环节["content"]。

      链.添加环节(标题, 内容)。
      格式化环节(i + 1, 总环节数, 标题, 内容)。

      如果 i 小于 总环节数 - 1:
        打印("  [推理中...]")。
      否则:
        打印("  [推理完成]")。

      i 为 i + 1。

    设 最终答案 为 链.综合答案()。
    返回 最终答案。

  段落 处理问题 接收 问题:
    显示横幅()。
    打印("")。
    打印("  问题: " + 问题)。
    打印("  " + 分隔线("─", 56))。
    打印("  开始推理...")。
    设 答案 为 己.推理(问题)。
    格式化答案(答案)。

    设 记录 为 {"问题": 问题, "答案": 答案}。
    列表追加(己历史记录, 记录)。
    返回 答案。

  段落 获取历史:
    返回 己历史记录。

# ===== 主程序 =====

段落 主程序:
  设 引擎 为 新建 推理引擎()。

  清屏()。
  显示横幅()。
  打印("")。
  打印("  请输入您的问题（输入 '帮助' 查看命令）:")。
  打印("  " + 分隔线("─", 40))。
  打印("")。

  当 真:
    打印("  > ")。
    设 输入文本 为 输入()。
    设 文本 为 去除空白(输入文本)。

    如果 文本 等于 "":
      跳过。

    如果 文本 等于 "退出":
      打印("")。
      打印("  感谢使用 Reasonix 推理引擎！")。
      打印("  再见！")。
      打印("")。
      跳出。

    否则若 文本 等于 "帮助":
      显示帮助()。

    否则若 文本 等于 "历史":
      设 历史记录 为 引擎.获取历史()。
      显示历史(历史记录)。

    否则若 文本 等于 "清屏":
      清屏()。
      显示横幅()。
      打印("")。
      打印("  请输入您的问题（输入 '帮助' 查看命令）:")。
      打印("  " + 分隔线("─", 40))。
      打印("")。

    否则:
      引擎.处理问题(文本)。
      打印("")。

  返回 0。

主程序()。