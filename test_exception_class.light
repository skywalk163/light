打印 "=== 阶段7：异常处理增强 测试 ==="。

类 我的异常 继承 异常：
    段落 我的异常 接收 消息：
        己消息 为 消息。
    结束。
结束。

类 另一个异常 继承 异常：
    段落 另一个异常 接收 消息：
        己消息 为 消息。
    结束。
结束。

打印 "--- 测试1：自定义异常类定义和抛出 ---"。
尝试：
    打印 "开始执行"。
    设 e 为 新建 我的异常("出错了")。
    抛出 e。
    打印 "不会执行到这里"。
捕获 我的异常 e：
    打印 "捕获到我的异常: " 加 e.消息。
捕获 异常 e：
    打印 "捕获到一般异常: " 加 e.消息。
最终：
    打印 "最终块总是执行"。
结束。

打印 "--- 测试2：按类型匹配多个捕获块（子类在前）---"。
尝试：
    抛出 新建 我的异常("测试子类异常")。
捕获 我的异常 e：
    打印 "第一个捕获块匹配（我的异常）: " 加 e.消息。
捕获 异常 e：
    打印 "第二个捕获块匹配（异常）: " 加 e.消息。
结束。

打印 "--- 测试3：基类捕获子类异常 ---"。
尝试：
    抛出 新建 我的异常("基类应该捕获到")。
捕获 异常 e：
    打印 "基类异常捕获成功: " 加 e.消息。
结束。

打印 "--- 测试4：多个不同异常类型 ---"。
尝试：
    抛出 新建 另一个异常("另一个异常")。
捕获 我的异常 e：
    打印 "捕获到我的异常: " 加 e.消息。
捕获 另一个异常 e：
    打印 "捕获到另一个异常: " 加 e.消息。
捕获 异常 e：
    打印 "捕获到一般异常: " 加 e.消息。
结束。

打印 "--- 测试5：内置异常类 ---"。
尝试：
    抛出 新建 值异常("值不对")。
捕获 值异常 e：
    打印 "捕获到值异常: " 加 e.消息。
捕获 异常 e：
    打印 "捕获到一般异常: " 加 e.消息。
结束。

打印 "--- 测试6：字符串异常向后兼容 ---"。
尝试：
    抛出 "传统字符串异常"。
捕获 异常 e：
    打印 "捕获到异常（字符串形式仍兼容）"。
结束。

打印 "=== 阶段7测试完成 ==="。
