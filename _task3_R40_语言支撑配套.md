# 任务3 交付报告：语言支撑配套（全角逗号判定 + NameError 合并嫌疑提示 + _EMBED 复验）

> 轮次：第40轮「互举闭环端到端集成」｜任务3（P0，跨项目 → light-merge）｜完成日期：2026-09-16
> 交付物：`light-merge/tests/test_R40_语言支撑配套.py`、`light-merge/src/enhanced_errors.py`（仅报错文本路径）、
> `lightharness/docs/功能对标/语言缺陷账.md` L-155 登记、本报告
> 铁律核对：**未触碰 `_EMBED_MAX_MATCH_KEYWORDS` 块与 `_tokenize_chinese_sequence`**；**未改切词/解析逻辑**；
> 未改 `src/lexer.py`（工作树 `git diff` 仅 `src/enhanced_errors.py`）

---

## 0. 状态一览

| 子任务 | 结论 | 状态 |
|---|---|---|
| 3.1 全角逗号缺陷 | **判定：路径A（语言容错）已由语言实现 + 文档明文承诺 ⇒ 逗号无罪。** basic.light 的报错真因是 **R19 `为` 整串合并 / R22 `接收` 合并**，与逗号宽度无关（半角逗号同样报错，归一化后**逐字一致**） | ✅ 判定完成；**未改语言、未改示例逗号**（避免伪修复），已补测试钉桩 + 缺陷账 L-155 |
| 3.2 NameError 合并嫌疑提示 | 已落地；误报率经**三轮收紧**从 4.8% → **0.25%**（核心语料）/ **0.10%**（全树）；对既有测试**零影响（可证）** | ✅ 完成 |
| 3.3 `_EMBED` 修复复验 | `d8cf337d` 只修了 `返回` 分支；**`为` 分支残口仍在** ⇒ basic.light L10/L40 仍红 | ✅ 复验完成（**不等待、不修改**，已登记） |

---

## 1. 子任务 3.1：全角逗号缺陷 —— 判定 + 修复

### 1.1 现场复现（先把事实钉死）

任务书现场描述是「`examples/basic.light` L20 / L24 使用全角逗号 → 解析错误」。
实跑 `python cli/light.py run examples/basic.light`，得到 **4 条**语法错误：

| # | 位置 | 报错 |
|---|---|---|
| 1 | **行 10, 列 10** | `期望'为'或'等于'，但得到 「换行」` |
| 2 | **行 20, 列 8** | `期望 冒号「:」，但得到 逗号「,」（附近: '，'）` |
| 3 | 行 21, 列 1 | 缩进不正确（L20 报错导致的连带） |
| 4 | **行 40, 列 11** | `期望'为'或'等于'，但得到 「换行」` |

**关键观察**：任务书点名的 **L24 `设和为加法(3，5)` 根本不在错误列表里** ——
也就是说**实参位置的全角逗号本来就是好的**。

### 1.2 判定依据（三条独立证据链）

#### 依据① 语言实现：全角标点已被归一化，且发同型 token

`src/keywords.py` 的 `SYMBOL_MAP` 把全角标点映射为半角等价物；词法器经该表发出**同一类型**的 token。
直接 dump L20 的 token 流，全角/半角两版**形状一模一样**：

```
L20 全角  段落加法接收甲，乙： → [段落][IDENTIFIER 加法][IDENTIFIER 接收甲][COMMA '，'][IDENTIFIER 乙][COLON '：']
L20 半角  段落加法接收甲,乙：  → [段落][IDENTIFIER 加法][IDENTIFIER 接收甲][COMMA ','][IDENTIFIER 乙][COLON '：']
```

注意报错文本自己就写着「得到 **逗号**」——**词法器确实把 `，` 识别成了 COMMA**，
报错是因为**解析器在那里期望 `：`**，不是因为「没认出来这是逗号」。

#### 依据② 文档承诺：语言**明文**承诺全角等价

`docs/光明-完整规范文档.md`：

| 位置 | 内容 |
|---|---|
| L657 | `#### 3. 逗号：， 与 , 等价`（附「中文逗号 / 英文逗号（等价）」示例） |
| L665 | `#### 4. 分号：； 与 ; 等价` |
| L1907 | `\| , \| 英文逗号 \| 与，等价 \|` |
| L2748 | 标点对照表：`逗号 \| ， \| , \| 甲，乙，丙 / 甲, 乙, 丙` |

⇒ **「路径B（语言不承诺全角标点容错）」的前提被文档直接否证**。

#### 依据③ 全语料扫描（影响面）

扫描根：`light-merge/{src,stdlib,examples,tests,bootstrap,antlrparser,tools,demo,docs}`
+ `lightharness/{src,stdlib,examples,tests,docs}`；口径为**代码位置**
（已剔除字符串与 `#`、`//`、`--` 注释）。

`.light` 文件总数 **1037**。全角标点在 `.light` **代码位置**的使用情况：

| 字符 | 半角 | `.light` 次数 | `.light` 文件 | `SYMBOL_MAP` |
|---|---|---|---|---|
| `，` | `,` | 13 | **12** | `','` ✅ |
| `；` | `;` | 0 | 0 | `';'` ✅ |
| `：` | `:` | 5435 | 116 | `':'` ✅ |
| `（` `）` | `(` `)` | 1 / 1 | 1 / 1 | `'('` `')'` ✅ |
| `、` | `\` | 3 | 1 | `'\\'` ✅ |
| `。` | `.` | 11438 | 328 | `'.'` ✅（**语义标点**，非成员访问等价） |
| `【` `】` | `[` `]` | 84 / 84 | 17 / 17 | `'['` `']'` ✅ |
| `《` `》` | `<` `>` | 901 / 901 | 39 / 39 | `'<'` `'>'` ✅ |
| `！` | `!` | 2 | 2 | **无**（但词法仍发 `BANG`，与半角 `!` 行为一致） |
| `？` | `?` | **0** | 0 | **无**（语料零命中，无影响） |

结论：**全角逗号的全部 12 个使用文件均可正常词法分析**（`examples/test_L087.light` 等 rc=0 实证）。
另外顺带核过 `！`/`？`：`！` 虽不在 `SYMBOL_MAP`，词法层仍发出 `BANG` token，
`！甲` 与半角 `!甲` **同样报解析错误**（逻辑非的正规写法是关键字 `非`）⇒ **行为等价、非缺口**；
`？` 语料零命中。⇒ **全角标点这一侧没有可修的真实缺陷。**

### 1.3 决定性反证：换半角逗号**修不好**，且归一化后报错**逐字一致**

同一份源码，只把 `，` 换成 `,`，其余**一字不动**：

| 用例 | rc | 结果 |
|---|---|---|
| `段落加法接收甲，乙：`（原样，全角） | 1 | 解析错误 |
| `段落加法接收甲,乙：`（**只换逗号**） | **1** | **同样解析错误** |

把报错文本里的 `，`→`,` 归一化后，两者**完全逐字相同**（同一进程内序列化指纹一致）。

再加一组**同型对照矩阵**（只比较「同一形态 全角 vs 半角」）：

| 形态 | 全角 rc | 半角 rc | 结论 |
|---|---|---|---|
| 形参（**连写无空格**） | 1 | 1 | **同样失败** |
| 形参（**带空格**） | **0** | **0** | **全角完全可用** |
| 实参 `加法(3，5)` | 1（名称错误，因 `加法` 未定义） | 1（同上） | 等价 |
| 列表 `[1，2，3]` | 0 | 0 | 等价 |

**决定性一击**：

```
段落 加法 接收 甲，乙：   ← 关键字两侧补空格，逗号【仍是全角】  →  rc = 0，正常解析
段落加法接收甲,乙：       ← 关键字连写，逗号【换成半角】        →  rc = 1，仍报错
```

⇒ **变量是「空格」，不是「逗号宽度」。**

### 1.4 判定结论

> **判定：路径 A 成立且已由语言实现 —— 语言本就把全角标点作为分隔符容错（`SYMBOL_MAP` 归一化），
> 且规范文档明文承诺「`，` 与 `,` 等价」。全角逗号不是缺陷，无需改语言。**
>
> **`examples/basic.light` 的解析错误与逗号宽度无关，真因是两个既有的「关键字连写合并」词法缺陷：**
>
> | 现场 | token 流 | 家族 |
> |---|---|---|
> | L10 `设结果为甲加乙乘2` | `[设][IDENTIFIER 结果为甲加乙乘2]` | **R19 `为` 整串合并** |
> | L40 `设计数为计数加1` | `[设][IDENTIFIER 计数为计数加1]` | **R19 `为` 整串合并** |
> | L20 `段落加法接收甲，乙：` | `[段落][加法][IDENTIFIER 接收甲][COMMA][乙][COLON]` | **R22 清表副作用** |
>
> 佐证：同文件同风格的 L37 `设计数为1` **正常**（`[设][计数][为][1]`），L40 却失败
> ⇒ **行为自身不一致，确系词法缺陷，而非「写法不规范」。**

### 1.5 处置：为什么不改语言、也不改示例的逗号

| 选项 | 做法 | 为什么不做 |
|---|---|---|
| 路径A「改语言」 | 让词法/解析支持全角逗号 | **语言已经支持了**（见 1.2 依据①）；再改是重复劳动、且会与保护表精简方向冲突 |
| 路径B「改示例逗号」 | 把 basic.light 的 `，` 换 `,` | **改完全无效**（见 1.3 实测：仍报错）。这是典型的**伪修复**——改完错误照旧，却会让人误以为「逗号问题已解决」，把真因（R19/R22）继续埋在下面 |
| 本任务实际做法 | ①测试钉桩现状 ②报错路径加提示 ③缺陷账登记 | 让真因**可见、可追、可感知**，而不是把现场涂掉 |

### 1.6 最小修正建议（**精确到字符，本任务未落地**）

若要让 `examples/basic.light` 这个展示用例跑起来，**最小且与逗号无关**的改法是给关键字补空格
（实证 rc=0，且**保留全角逗号**）：

| 行 | 现状 | 最小修正 | 实证 |
|---|---|---|---|
| L20 | `段落加法接收甲，乙：` | `段落加法接收 甲，乙：` ← **只在 `接收` 与 `甲` 之间加 1 个空格** | rc=0，输出 `8` |
| L10 | `设结果为甲加乙乘2` | `设 结果 为 甲 加 乙 乘 2` | 对应 R19 修复面 |
| L40 | `设计数为计数加1` | `设 计数 为 计数 加 1` | 对应 R19 修复面 |

**为何本任务不落地**：这三处的真因是**语言缺陷（L-155）**，而 `_EMBED` 块**正在被另一 agent 修改**
（任务3 铁律：禁止触碰、只登记不修改）。此刻改示例：
① 会把语言缺陷的**现场证物**涂掉，让后续 `_EMBED` 修复失去回归对照；
② 与并行 agent 的写入产生交叉干扰风险。
⇒ **建议由 `_EMBED` 修复方在修完后一并决定**：语言修好则示例无需改；若只修 `返回` 不修 `为`，
则由该方按上表落地示例修正（本文已给出可直接照抄的行级补丁）。

---

## 2. 子任务 3.2：NameError「词法整串合并嫌疑」提示

### 2.1 现场与目标

`examples/advanced.light` 曾报：

```
错误: 名称错误
  name '如果数小于等于二那么返回一' is not defined
```

用户拿到这句话**完全无法定位**——既不像拼写错误，也看不到「关键字连写缺空格」。
目标：在**只改报错文本生成路径**的前提下，给这类错误追加一句可操作的提示。

### 2.2 判据设计（为什么是这个形态）

提示必须同时满足「**该报的报**」与「**不该报的绝不报**」。用全语料真实标识符名做了**三轮**误报测算：

| 版本 | 判据 | 候选名 | 误报 | 率 |
|---|---|---|---|---|
| 第 1 版 | 「名长≥5 且含 **≥2 个**关键字就提示」 | 14613 | 705 | **4.8%** ❌ |
| 第 2 版 | 「命中**从句关键字**」**或**「含 `为` **且** 含运算符」 | 6793 | ~45 | ~0.7% ⚠️ |
| **第 3 版（落地）** | 「命中 **≥2 个不同**从句关键字」**或**「含 `为` **且** 含运算符」 | 6793 | **17** | **0.25%** ✅ |

* **第 1 版为何误报高**：`作用域过滤组装`、`保存索引替换函数`、`从列表删除`、`优先队列是否为空`
  这类**正常的复合名词**确实含多个关键字，全被误报。
* **第 2 版残留**：单个从句关键字出现在正常复合名里非常常见 —— 全语料实测
  `返回JSON`、`统计段落数`、`解析段落定义`、`跳过字符串字面量`、`非空数组断言`、`返回值类型检查`
  都会被单关键字判据误报。
* **第 3 版（形态一要求 ≥2 个不同从句关键字）**：上述单关键字误报**全部消除**，
  而真实被合并的串（`如果数小于等于二那么返回一` 含 如果/那么/返回 三个）**依然命中**。

最终判据与 `_EMBED_MAX_MATCH_KEYWORDS = {为, 返回, 尝试}` 的**实际合并形态一一对应**：

* **形态一（从句型，≥2 个不同从句关键字）**：`如果…那么…返回…` 这一路
  → 例：`如果数小于等于二那么返回一`
* **形态二（赋值/运算型）**：同时含 `为` 与运算符（`加/减/乘/除/大于/小于/等于/与/或/…`）
  → 例：`结果为甲加乙乘2`、`设计数为计数加1`、`设和为加法`、`和为和加i`（均取自真实语料）

代码里刻意**不收** `作用域/类型/函数/模块/接收/打印/生成/定义/实现` 这些「可作名词前缀」的关键字，
也刻意**不收**单字 `设/当/若`（`设置`/`当前`/`若干` 之类正常名词太多）——否则误报率立刻回到 4%。

### 2.3 实现位置（只动报错文本，零耦合）

文件：`light-merge/src/enhanced_errors.py`（**唯一改动的源码文件**）

| 位置 | 内容 |
|---|---|
| 模块级常量 | `_MERGE_HINT_STMT_HEADS`（从句关键字元组）、`_MERGE_HINT_OPERATORS`（运算符关键字元组）、`_MERGE_HINT_EMBED = ('为','返回','尝试')`、`_MERGE_HINT_MARKERS`（懒加载缓存） |
| `ErrorFormatter._merge_hint_markers()` | 懒加载返回 `(从句, 运算符, 全部标记)`，按长度降序缓存；**只读本模块常量，不 import 也不修改任何词法表** |
| `ErrorFormatter._greedy_marker_hits(name, markers)` | 最长匹配**贪心切分**（用贪心而非朴素子串计数，避免 `静态方法名` 同时命中 `静态方法`+`静态` 而记 2 次） |
| `ErrorFormatter._lexical_merge_hint(err_type, err_msg)` | 判据入口：`err_type=='NameError'` ∧ 提取名长度≥5 ∧ 含汉字 ∧（**≥2 个不同从句关键字** ∨（`为`+运算符））→ 返回提示文本 |
| `format_error()` 中「拼写建议」块之后 | 追加一段：`\n  提示: <merge_hint>` |

> **零耦合保证**：新增代码**不参与**切词、解析、求值；只在**已经决定报警之后**生成一段文本。
> 未新增异常类型、未改写原始消息、未改动 `err_type`/`err_msg`。

### 2.4 效果

**触发（HIT）样例**（长未定义名）：

```
name '如果数小于等于二那么返回一' is not defined
  → 提示: 该名称长度异常（14 字符）且含多个关键字（返回/如果/那么），可能被词法整串合并
          （关键字连写缺空格）。请检查这些关键字两侧是否缺少空格，例如写成
          「如果 甲 大于 乙 那么 返回 一。」
```

**不触发（MISS）样例**（正常名字/非 NameError）：

* 短名：`甲`、`余额`、`己余额`
* 含 1 个「像关键字但其实是名词前缀」：`因为天气原因`、`当前工作目录`、`作用域过滤组装`
* 含 **1 个**从句关键字的正常复合名（第 3 版新增消除）：`返回JSON`、`统计段落数`、`解析段落定义`、
  `跳过字符串字面量`、`非空数组断言`、`返回值类型检查`
* 非 NameError 类型：任意

**误报率实测（第 3 版落地后）**：

| 口径 | `.light` 文件 | 候选名 | 触发 | 误报率 |
|---|---|---|---|---|
| **核心语料**（`src/stdlib/examples/tests/bootstrap/antlrparser/tools/demo/docs/edu/contrib/benchmarks` + lightharness 同构目录 + `lightharness/docs`） | 1101 | 6793 | **17** | **0.25%** |
| **全树**（含 `light-merge/积木库` 3.6 万个生成积木） | 38018 | 18122 | **18** | **0.10%** |

核心语料剩余 17 条的形态（可见**都已带多个关键字/运算符**，人看也会觉得可疑，属「宁可少报不可漏报」的保守区）：
`如果忽略大小写否则前缀`、`如果忽略大小写否则后缀`、`断言抛出异常`、`如果数据否则`、
`结果为甲加乙乘2`、`计数为计数加1`、`设和为加法`、`设阶乘结果为阶乘`、`和为和加i`（后者取自 `examples/hello.light`）等 ——
其中 `结果为甲加乙乘2`/`计数为计数加1`/`设和为加法`/`和为和加i` **都是本轮回溯出来的真实被合并串**，属**真阳性**。

### 2.5 零影响证明（三条）

1. **对既有测试零影响（结构性证明）**：本改动是**纯追加**，且只在判据命中时追加。
   而既有失败用例涉及的名字（`异步读取二进制`/`异步写入二进制`/`异步任务等待`/`异步任务取消`/`异步取数`/
   `己项`/`己左`/`己号`/`异步读取文件`/`并发等待`）经逐一实测**全部为 MISS（不触发）**
   ⇒ 输出文本与改动前**逐字相同** ⇒ 不可能改变任何断言的通过性。
   （其中 `test_frontend_blockers_run.py` 那批的报错走的是 `cli/light_unified.py` 的裸 `print`，
   根本不经过 `ErrorFormatter`，属另一路径。）
2. **报错类单测全绿**：`tests/unit/test_error_formatter.py`、`tests/unit/test_errors.py`、
   `tests/test_chinese_errors.py`、`tests/test_error_messages.py` 合计 **143 passed**。
3. **不引入词法变化**：未改 `src/lexer.py`（token 流不可能变化）；
   工作树 `git diff --stat` 中 light-merge 侧源码仅 `src/enhanced_errors.py` 一个文件。

### 2.6 端到端验证

`tests/test_R40_语言支撑配套.py::Test端到端提示` 用 `subprocess` 真跑 `cli/light.py run`：

* 长未定义名（`结果为甲加乙乘2`）→ stderr **出现**提示 ✅
* 正常未定义名（`因为天气原因`）→ stderr **不出现**提示 ✅

---

## 3. 子任务 3.3：`_EMBED` 修复复验（不等待、不修改）

另一 agent 的修复**已提交**：`d8cf337d`「第36轮：修复 R19 嵌入块连写误吞「返回」关键字」。
本轮做了**全语料 token A/B**（A = `d8cf337d^`=`0ebf0db7`，B = `d8cf337d`）：

| 指标 | 结果 |
|---|---|
| 语料 `.light` 文件 | **1035** |
| A 词法异常文件 | 3 |
| B 词法异常文件 | 3（**与 A 相同 ⇒ 非回归**） |
| token 流差异文件 | **39** |
| 差异方向 | **全部是「B 比 A 多出关键字」（`返回`/`为`/`乘`/`加`/`之`/`列表`…）**，即把原来被吞的整串**正确切开** |

**`advanced.light` L10 现场复验**：

```
源码：  如果数小于等于二那么返回一。
修复前：  [IDENTIFIER '如果数小于等于二那么返回一']            ← 整串被吞
修复后：  [如果][数][小于等于][2][那么][返回][1][。]           ← 正确切分 ✅
e2e：    tests/e2e/test_e2e_chain.py -k advanced  →  2 passed ✅
```

**复验结论**：

> ① `返回` 分支修复**有效且无回归**（39 个差异文件全为预期方向的修复，词法异常文件数 A/B 相同）；
> ② 但**只覆盖了 `返回`** —— `_EMBED_MAX_MATCH_KEYWORDS = {为, 返回, 尝试}` 中的
> **`为` 分支残口仍在**：`设结果为甲加乙乘2` 至今仍是 `[设][IDENTIFIER 结果为甲加乙乘2]`，
> 导致 `examples/basic.light` L10/L40 仍红（详见 §1.4）。
> ③ 本任务**未做任何修改**（铁律），仅登记为缺陷账 **L-155**。

---

## 4. 测试交付

**新增**：`light-merge/tests/test_R40_语言支撑配套.py` —— **47 passed**（7.7 s）

| 类 | 用例要点 |
|---|---|
| `Test全角标点等价契约` | 钉住 `SYMBOL_MAP` 全角→半角映射；全角/半角发**同型 token**；同时钉住「`。`→PERIOD / `《》`→RBOOK 是**有意非等价**」的设计，防止误当缺陷修 |
| `Test合并嫌疑提示` | **HIT 5 例**（`如果数小于等于二那么返回一`/`如果甲大于乙那么返回甲`/`结果为甲加乙乘2`/`设和为加法`/`设计数为计数加1`）+ **MISS 15 例**（短名 3、名词前缀 3、**单从句关键字正常名 7**、非 NameError 1） |
| `Test现状钉桩_既有词法回归` | 钉住 `设结果为甲加乙乘2`→`设`+`IDENTIFIER(结果为甲加乙乘2)`（R19）、`接收甲` 合并（R22）、`己余额` 类体内合并（R22）——**修好会反红，逼着同步更新结论与缺陷账**；并含「全角/半角 token 类型序列一致、差异仅逗号字符」的反证断言 |
| `Test端到端提示` | `subprocess` 真跑 `cli/light.py run`，验证提示**出现/不出现** |

> 设计意图：这四类是**契约 + 反证 + 现状钉桩 + 端到端**的组合——第 3 类是「变化探测器」，
> 一旦 `_EMBED` 或保护表被改动而结论未同步更新，它会立刻反红。

**既有测试**：

* 报错相关 4 个测试文件（`tests/unit/test_error_formatter.py`、`tests/unit/test_errors.py`、
  `tests/test_chinese_errors.py`、`tests/test_error_messages.py`）合计 **143 passed**。
* `tests/unit` + 4 个前端/报错测试文件的全量跑（4623 用例、35 min）结果
  `collected=4623 failures=53 errors=0 skipped=9`——**53 条失败全部落在
  lexer / parser / 原生腿 / 文档噪声门 / v34-v35 语法糖 等与本次改动无关的模块**（既有存量欠账，
  典型如 `test_lexer_perf.py::test_lexer_correctness_smoke` 断言 `'为' not found in ['设','甲为三',...]`
  ——**正是 L-155**）。
* 新增测试文件 `tests/test_R40_语言支撑配套.py` **单独跑 47 passed**（它不在 `tests/unit/` 下，
  未参与上述全量跑的收集；因此用独立跑给出结论）。

---

## 5. 铁律核对

| 铁律 | 核对结果 |
|---|---|
| 禁止修改 `_EMBED_MAX_MATCH_KEYWORDS` 块及 `_tokenize_chinese_sequence` 内相关逻辑 | ✅ **未触碰**；light-merge 侧源码改动仅 `src/enhanced_errors.py`（`git diff` 可证） |
| 只改报错生成路径，不改切词/解析逻辑 | ✅ 新增代码只生成**展示文本**；不参与切词/解析/求值，不新增异常类型、不改写原始消息 |
| 全角逗号判定必须给出依据（全语料扫描数据），不能拍脑袋 | ✅ §1.2 三条证据链（SYMBOL_MAP 实现 + 文档 4 处承诺 + 全语料 12 文件扫描）+ §1.3 决定性反证 |
| light-merge 改动必须全语料 token A/B 零意外变化 | ✅ 未改任何词法/解析代码 ⇒ token 流**零变化**；`_EMBED` 的 39 文件差异是**另一 agent 的已提交修复**，本轮 A/B 属复验取证（§3） |
| 新增测试必须通过；既有 pytest 基线外不得新增红 | ✅ 新增测试 **47 passed**；§2.5 给出「对既有测试零影响」的结构性证明 + 143 passed 实证 |
| 任务3 与 _EMBED 修复无文件冲突（隔离遵守） | ✅ 本任务只写 `src/enhanced_errors.py` 与 `tests/test_R40_语言支撑配套.py`；`_EMBED` 修复在 `src/lexer.py` —— **零重叠** |

---

## 6. 缺陷账登记

已在 `lightharness/docs/功能对标/语言缺陷账.md` 追加 **L-155**（第40轮任务3 复验发现），内容包括：

* R19 嵌入块 `为` 分支残口（`d8cf337d` 只修了 `返回`）与 3 个 basic.light 现场（L10/L20/L40）的 token 流对照表；
* 同文件 L37 正常 / L40 失败的**行为不一致**佐证；
* 「半角逗号修不好、归一化后报错逐字一致」的关键反证；
* 影响面（`tests/e2e/test_e2e_chain.py` 的 basic.light 两条腿，且**不在 CI 基线内**）；
* 本轮处置（钉桩 + 提示 + 仅登记不修改）；
* 附注：全角标点等价为**既有能力**（`SYMBOL_MAP` + 文档 4 处明文），非缺陷。

文件编辑校验：BOM 保留 ✅｜行尾 LF 未变（CRLF=0）✅｜条目总数 37 ✅。

---

## 7. 附：本轮证据文件与命令

**只读取证脚本**（仓库根，`_r40_t3_*` 前缀；仅判定用，非交付件）

| 脚本 | 用途 |
|---|---|
| `_r40_t3_全角标点扫描.py` | 全语料全角标点使用情况与影响面 |
| `_r40_t3_逗号等价矩阵.py` | 全角/半角同型对照矩阵（形参/实参/列表/字典） |
| `_r40_t3_分号探针.py` | 全角分号专项探针 |
| `_r40_t3_缺空格扫描.py` | 「关键字连写」扫描（`接收` 32 行 / `X为Y` 995 行） |
| `_r40_t3_linchpin.py` | **§1.3 决定性对照**（同形态全角/半角 + 空格变量） |
| `_r40_t3_EMBED复验AB.py` | §3 `_EMBED` 全语料 token A/B |
| `_r40_t3_hint_scan.py` / `_r40_t3_hint_scan2.py` | §2.4 全语料 HIT 清单与误报率复核（两版口径：全树 / 精简） |

```bash
# 判定复现（关键一条）
cd G:/dswork/duan-light-merge
./light-merge/.venv/Scripts/python.exe _r40_t3_linchpin.py

# 新增测试
cd G:/dswork/duan-light-merge/light-merge
./.venv/Scripts/python.exe -m pytest tests/test_R40_语言支撑配套.py -q

# 报错相关既有测试（应全绿）
./.venv/Scripts/python.exe -m pytest tests/unit/test_error_formatter.py tests/unit/test_errors.py \
    tests/test_chinese_errors.py tests/test_error_messages.py -q

# basic.light 现场（当前仍红，真因见 L-155）
./.venv/Scripts/python.exe cli/light.py run examples/basic.light
```

> ⚠️ 本机 Git-Bash 下，传给**原生 Windows Python** 的路径要写 `G:/...`；
> 写 `/g/...` 会被 MSYS 转成 `G:\g\...`（实测踩坑）。
> 控制台的 `shell-runtime-bash-env.sh: line 3: dirname: command not found` 是**环境噪音**，命令正常执行。
