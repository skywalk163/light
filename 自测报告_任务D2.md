# 自测报告 · 任务 D2（工具执行层：解码与沙箱）

- 工作树：`g:\dswork\duan-light-merge\wt-D2`，分支 `task-D2`，基线 `e27277c2`
- 环境：Windows 10 / PowerShell 5.1 / 控制台代码页 936 / `locale.getpreferredencoding(False) = cp936`
- 测试解释器：`C:\Python314\python.exe`（pytest 9.1.1；托管 python 没装 pytest）
- 落盘证据：`自测报告_任务D2_测试输出.txt`（本目录，UTF-8，逐例状态 + skip 原因原文）。
  **注意它被 `.gitignore:191` 的 `*.txt` 挡着，没有进 commit，且任何 `git clean -fdx`
  都会把它扫掉** —— 所以下面所有数字都**直接写进本报告正文**，不依赖那个文件还在。


> **读日志的方法论（本任务踩过三次，写在最前面）**：PowerShell 5.1 会把本机命令的
> stdout **按 cp936 重新解码**，遇到 cp936 编不出的字符（最常见是 `U+FFFD`）就
> **静默截断该行剩余部分**。上一轮我因此读到三条**根本不存在的失败**。所以本任务
> 所有数字一律取自 pytest 的 **junit-xml**（永远 UTF-8），跑法脚本
> `_taskD2_runner.py` **不设置任何环境变量**——`PYTHONUTF8` 会改变
> `locale.getpreferredencoding()`，等于台架把被测对象改掉（v1 就这么自造了一条假红）。

---

## 一、改了哪些文件

| 路径 | 一句话原因 |
| --- | --- |
| `stdlib/进程树.light` | **D2-1 根因修复**：`流采集器.文本()` 原来硬编码 `decode("utf-8")`，Windows 子进程管道来的是 cp936 字节 → 中文成片变 `U+FFFD`。改为完整解码契约：平台探测编码 + `"编码"` 显式覆盖 + `"解码策略"`（默认 `replace`）+ `字节()`/`文本()` 分离 + `执行结果` 加 `采用编码/标准输出失真/标准错误失真/标准输出字节/标准错误字节` + 窗口切点用一次性增量解码器对齐。 |
| `tests/test_process_tree_light.py` | 原 9 例保留，新增 `Test解码口径` 7 例把新契约逐条钉死（平台探测、显式编码优先、字节接口、坏字节 replace + 失真位、ignore 策略仍报失真、窗口边界切在多字节字符中间、UTF-8 与 GBK 混流不崩）。 |
| `stdlib/伪终端.light` | **D2-3**：ConPTY（Windows）/ `openpty`+`forkpty`（POSIX）纯光明实现。平台不支持时抛 `伪控制台错误` 硬失败，**绝不降级成管道**（降级会让「我在真 PTY 里」这个前提悄悄失真）。 |
| `tests/test_pty_light.py` | D2-3 定向测试。Windows 5 例真跑真断言（读到子进程真发的 `\x1b[31m`/`\x1b[0m` 字节）；POSIX 3 例 skip 且每条写清掩盖了什么。 |
| `tools/taskD2_posix_repro.sh` | **D2-2**：POSIX `setsid`+`killpg` 杀树语义的可复现脚本（4 项断言、逐项 PASS/FAIL、任一 FAIL 非零退出）。本机无 WSL 发行版、无 Docker，**该脚本从未在本机跑过**，是交给有 Linux 的人跑的凭据，不是「已验证」。 |
| `stdlib/路径护栏.light` | **D2-4**：路径白名单护栏。本轮修掉三个真 bug（见第四节「本轮实际修掉的」）：白名单静默丢弃、读失败被吞成空 bytes、Windows 文本模式截断二进制。新增机读口径 `安全声明()` 与 `清单快照()`，空根目录拒绝构造。 |
| `tests/test_path_guard_light.py` | D2-4 定向测试，12 例 → **25 例**。补齐：白名单放在根外（原来放在根内，bug 被「根本来就放行」掩盖）、空根拒绝、二进制字节保真、大文件按块读完整、读失败不吞、兄弟目录裸前缀、尚不存在的路径、`核准` 返回归一化路径、以及**两条反向钉住已知缺口**的用例（硬链接、TOCTOU）。 |
| `stdlib/事件总线.light` | **D2-5**：A2 未合入 main，故**只写设计不写实现**——头部加「异步监听者语义设计」5 条决策（并行度 / 中止传播 / 错误隔离 / 发布接口 / 运行环境）。产品代码一行未改。 |

未碰：`src/`（A2）、`src/llvm/`（B2）、C2 的 `stdlib/SSE.light`/`流式.light`/`大模型客户端.light`/`模式校验.light`。

---

## 二、定向测试结果（真命令、真数字）

一条命令跑完 D2 名下 6 个文件：

```
C:\Python314\python.exe _taskD2_runner.py 自测报告_任务D2_测试输出.txt ^
  tests/test_process_tree_light.py tests/test_pty_light.py tests/test_path_guard_light.py ^
  tests/test_event_bus_light.py tests/test_plugin_light.py tests/test_cross_platform.py
```

**合计 = 103，失败 = 0，错误 = 0，跳过 = 5，用时 13.990s，pytest 退出码 0。**

分文件（逐例状态见落盘文件）：

| 文件 | 例数 | 通过 | 失败 | 跳过 |
| --- | --- | --- | --- | --- |
| `tests/test_process_tree_light.py` | 16 | 15 | 0 | 1 |
| `tests/test_pty_light.py` | 8 | 5 | 0 | 3 |
| `tests/test_path_guard_light.py` | 25 | 25 | 0 | 0 |
| `tests/test_event_bus_light.py` | 13 | 13 | 0 | 0 |
| `tests/test_plugin_light.py` | 12 | 12 | 0 | 0 |
| `tests/test_cross_platform.py` | 29 | 28 | 0 | 1 |

D2-4 单跑一次的独立记录：`合计=25 失败=0 错误=0 跳过=0 用时=1.231s`（junction 与硬链接在本机都真建成了，两条缺口用例是**真跑**不是 skip）。

### 2.1 D2-1 那 5 条红 —— 单独报

**结论：5 条全绿。**

被点名的 5 条是 `tests/test_process_tree_light.py` 里**断言「子进程输出的中文」**的
那 5 条（其余 4 条断言的是退出码、PID 数字串、空串，不经过中文解码，所以当时不红）：

| # | 用例 | 当时红在哪 | 现状 |
| --- | --- | --- | --- |
| 1 | `Test基本退出::test_正常退出取输出` | `"目录甲"/"结束行"` 断言失败 | PASSED（并额外断 `标准输出失真 is False`） |
| 2 | `Test基本退出::test_非零退出码` | `"看这里"` 断言失败 | PASSED |
| 3 | `Test基本退出::test_stderr与stdout分离` | `"仅标准输出"/"告诫进错误流"` 断言失败 | PASSED（两条流的失真位都断了） |
| 4 | `Test有界输出与spill::test_超界触发spill与省略标记` | `"已省略"`、`endswith("中国结束")` 断言失败 | PASSED（并断 `"\ufffd" not in 标准输出`） |
| 5 | `Test有界输出与spill::test_未超界不建spill` | `"小小输出"` 断言失败 | PASSED |

**根因（一句话）**：`流采集器.文本()` 硬编码 `原始.decode("utf-8")`，而 Windows 上
子进程管道里是 **cp936** 字节，UTF-8 解码把每个汉字变成替换字符 —— 不抛异常、不报警，
**是静默数据损坏**，本项目最高优先级的缺陷类。

**修法不是把 `"utf-8"` 换成 `"gbk"`**，那只是把硬编码换个方向。落的是一整套口径：

1. `平台编码()`：配置 `"编码"` 最高优先 → 非 win32 一律 utf-8 → win32 取
   `locale.getpreferredencoding(假)`，空值兜 utf-8。
2. `执行结果` 自报 `采用编码`，调用方能看见到底按什么解的。
3. `字节()` 与 `文本()` 分开：要精确处理的自己拿字节解。
4. `解码策略` 默认 `replace`，但**只要走了兜底就置 `失真` 标志位** ——
   `errors="replace"` 必须伴随一个可见的失真信号，否则又是静默损坏。
   `ignore` 策略同样报失真（`test_解码策略可配为ignore且仍报失真` 钉住）。
5. 窗口切点对齐：`对齐切点()` 用一次性增量解码器按 `getstate()[0]` 的挂起字节把切点
   往回拉，保证 `尾部` 永远从字符边界开始 → `test_窗口边界切在多字节字符中间也不产生半个字符`
   用 3000 个「中」+「尾巴标记」在 `限尾字节=4097` 下真跑验证。
6. **丢头字节的启发式只对自同步编码开放**（utf-8 系）。GBK 不自同步 —— 坏字节会
   「干净地」解成另一个汉字，丢头会让坏输入伪装成好输入、把失真位压下去，那是
   诚实性倒退，所以 GBK 走 `跳出` + 兜底 + 置失真。

---

## 三、移交清单（别人地盘的问题，我没动手）

### 给 A2（语言前端 / codegen）

1. **`设 X 为 …` 里 X 与类属性同名时被编译成 `self.X = …`，不是局部变量。**
   实证：`stdlib/路径护栏.light` 首版 `段落 初始化 接收 根目录, 配置:` 内写
   `设 白名单 为 配置.get("白名单", 空)。`，生成的 Python 是
   ```python
   self.白名单 = 配置.get("白名单", None)   # 本该是局部变量
   ...
   self.白名单 = []                          # 紧接着被清空
   for 项 in self.白名单:                    # 遍历空表
   ```
   **后果是静默的**：传进来的白名单被丢掉、护栏只认根目录，不报任何错。
   形参不受影响（同名形参 `根目录` 编译正确），只有 `设` 语句会被吃掉。
2. **词法器 `user_definitions` 前缀拆分**：登记一个短标识符会把以它开头的长标识符切开。
   本轮的实例是 `设 尾 为 0` 让另一处方法里的 `己.尾部 为 b""` 变成
   `self.尾部 == b''`（比较，不是赋值），**症状出现在完全另一个方法里**。
   现有规避手段是「局部名刻意避开属性/方法名的严格前缀」——这是纪律，不是防护。
   建议：编译期就报「局部名与属性名同名/成前缀」的诊断，这两条都能在第一现场炸掉。
3. **语言没有 `最终`/`finally`**（已 grep `src/light_parser_v3.py` 确认无 `最终`/`finally`）。
   所有「打开句柄 → 中途失败」都得手写「先 `os.close` 再 `抛`」，漏一处就是句柄泄漏。
   `路径护栏.light` 的读/写/续写三处都是手写补的。
4. **codegen 两处怪癖**（已在 `stdlib/伪终端.light` 头部记档，此处只汇总）：
   模块级变量上写 `设 乙 为 甲()` 会把 `()` 吞掉；`继承 ctypes.Structure` 这种带点的
   基类无法解析，只能 `type("X", tuple([ctypes.Structure]), {...})` 绕。

### 给主线（架构决策，不是 bug）

5. **`stdlib/路径护栏.light` 不是安全边界，harness 若要跑不可信工具，必须上容器/VM 级隔离。**
   诚实的风险陈述（也是 `安全声明()` 的机读原文）：
   > 路径护栏只做路径名判定，不是安全边界；存在 TOCTOU 窗口；未做
   > landlock/seccomp/AppContainer 等内核级隔离；硬链接封不住；只拦失误，不拦攻击。
   两条缺口本轮**用测试反向钉住了**（`test_硬链接是已知封不住的缺口`、
   `test_TOCTOU窗口真实存在`），哪天有人以为补上了，那两条会红。
   landlock/seccomp 是 Linux 内核接口，本机 Windows 上做不了，**不是「没做」而是「做不了」**，
   要不要容器化是主线的取舍，不在 D2 范围内。

### 给 C2 / 上层

6. **ANSI 转义序列解析明确不在 `stdlib/伪终端.light` 范围内**：它只把 PTY 里的原始
   字节原样交出（测试断言的就是原始 `\x1b[31m`）。谁要渲染/剥离颜色，自己在上层做。

---

## 四、已知未完成 与 未实测项

### 4.1 未实测（不是「不做」，是「本机测不了」）

| 项 | 为什么没测 | 缺的是什么 |
| --- | --- | --- |
| POSIX `setsid` + `killpg` 杀树 | 本机无 WSL 发行版（`wsl -l -q` rc=1）、`docker` 不可执行（2026-08-22 探测） | `stdlib/进程树.light` 的 POSIX 分支**从未运行过**，只有原理推导 + `tools/taskD2_posix_repro.sh` 待跑 |
| POSIX `openpty`/`forkpty` PTY | 同上 | `stdlib/伪终端.light` POSIX 半边从未运行；`ioctl(TIOCSWINSZ)`、关 master → 宽限 → SIGTERM → SIGKILL 全未实测 |
| landlock / seccomp / Capsicum / AppContainer | Linux 内核接口，Windows 上不存在 | 内核级隔离一条没做，见移交第 5 项 |
| POSIX 上的符号链接逃逸 / 硬链接缺口 / TOCTOU | Windows 上用 `mklink /J`、`/H` 复现，POSIX 要改 `os.symlink`/`os.link` | 这三条结论在 POSIX 上**仍是推导** |
| `路径护栏.读取` 里「`os.read` 失败 → 先关句柄再抛」这条分支 | Windows 对目录 `os.open` 就直接 `PermissionError`，走不到 `os.read` | 手写的 finally 替代路径没被真正走过一次 |

### 4.2 本轮实际修掉的（写在这里防止被读成「一直是对的」）

`stdlib/路径护栏.light` 交到我手上时**编译通过、原有 12 条测试全绿**，但藏着三个静默错：

1. **白名单被静默丢弃**（根因见移交第 1 项）。原测试把白名单目录建在 `tmp_path` 里面，
   而 `tmp_path` 本身就是根，所以「根本来就放行」把 bug 完全掩盖了。
   现在白名单目录建在根**之外**，这条才真的在测白名单。
2. **读失败被吞成空 bytes**：`捕获 错误: 内容 为 b""` —— 权限不足 / 是目录 / 被占用
   统统伪装成「文件是空的」。改为「先 `os.close` 再抛 `路径护栏错误`」，
   `test_读失败不被吞成空bytes` 钉住。
3. **Windows 文本模式截断二进制**：`os.open` 默认文本模式，读到 `0x1A`（Ctrl-Z）当 EOF
   停下、写出时 `0x0A` 被翻成 `0x0D 0x0A`。实测写 230400 字节、**读回来只有 26 字节**
   （第一个 `0x1A` 的下标），全程不报错。修法是所有 `os.open` 或上 `O_BINARY`
   （`段落 二进制位`，POSIX 返 0），`test_二进制字节不被文本模式改写或截断` 钉住。

第 3 条是本轮唯一一条**由新写的测试当场逮到、而非事前推断**的 bug —— 也说明
`test_写入读取往返` 那种「写中文再读回来」的往返测试对二进制损坏是盲的（中文里既没有
`0x1A` 也没有单独的 `0x0A`）。

### 4.3 其它

- **D2-5 只有设计，没有实现**，因为 A2（`异步 段落`/`并发等待`/`限时`）在基线
  `e27277c2` 上未合入。设计写在 `stdlib/事件总线.light` 头部：默认**按注册顺序逐个
  await、不并发**（保住同步版已测定的瀑布语义）；并行组内任一监听者返回 `中止符号`
  则该组判中止，但**已启动的并行监听者不主动取消**；同步 `发布()` 遇到异步监听者
  **抛类型错误而不是静默跳过**。A2 合入后按这份设计实现。
- 临时文件 `_taskD2_*` 已清。**注意**：本会话期间工作树根目录的 `_taskD2_*` 文件被
  某个外部动作整批清掉过一次（跑法脚本连同落盘日志一起消失，**源码改动无损**）。
  从现象看是别处对本工作树跑了 `git clean -fd(x)` 之类：未跟踪的 `.py` 探针和被
  `*.txt` 规则忽略的日志一起没了。因此本报告不把任何结论押在日志文件上，数字全写在正文里。

- 端口：本任务一个都没用（19300–19399 全程闲置）。杀进程只按自己 spawn 的 PID / 进程组，
  从未按进程名。

---

## 五、skip 清单与掩盖分析

本次 103 例里 5 条 skip，逐条列出「为什么跳」+「跳过掩盖了哪个未验证的事实」。

| # | 用例 | 为什么跳 | **掩盖了什么** |
| --- | --- | --- | --- |
| 1 | `test_process_tree_light.py::Test超时杀树::test_posix信号语义跳过占位` | `skipif(win32)`，本机是 Windows | `进程树.light` 的 POSIX 分支（`os.setsid` 建进程组、`killpg` 发 SIGTERM、宽限后 SIGKILL）**一行都没执行过**。Windows 上绿的是 `CREATE_NEW_PROCESS_GROUP` + `taskkill /T` 那条完全不同的路径。POSIX 上到底能不能连带杀掉孙子进程，目前只有原理推导 + 未跑过的 `tools/taskD2_posix_repro.sh`。 |
| 2 | `test_pty_light.py::TestPOSIXOpenPTY::test_启动命令读到带ANSI的输出` | 本机无 WSL/Docker（系统级工具被安全策略禁用） | `伪终端.light` 的 `启动POSIX`（`importlib` 拿 `pty`/`termios`、`openpty`、`forkpty`、`setsid`）**从未运行**。「POSIX 上能起 PTY 并读到 ANSI」是推导。 |
| 3 | `test_pty_light.py::TestPOSIXOpenPTY::test_设窗口大小不炸` | 同上 | `ioctl(TIOCSWINSZ)` 这条设窗口路径未实测；Windows 上绿的是 `ResizePseudoConsole`，两者代码不共享。 |
| 4 | `test_pty_light.py::TestPOSIXOpenPTY::test_优雅关闭长驻进程` | 同上 | POSIX 优雅关闭链（关 master → 宽限 → SIGTERM → SIGKILL）未实测，包括「关掉 master 后子进程是否真收到 SIGHUP」这个关键假设。 |
| 5 | `test_cross_platform.py::TestCrossPlatformSymlink::test_symlink_support` | Windows 建符号链接要管理员/开发者模式（**非我新增，原有用例**） | 真符号链接（区别于 junction）的行为未验证。**注意 `test_path_guard_light.py` 没有依赖这条**：它的链接逃逸用例自己做了两级回退（`os.symlink` → `mklink /J`），本机实测走通了 junction 那条，所以「链接解析后再比前缀」这件事**是真测过的**，不在掩盖范围内。 |

另有两条不是 skip、但必须点明的覆盖缺口：

- `test_path_guard_light.py::test_读失败不被吞成空bytes`：Windows 上异常来自 `os.open`
  而非 `os.read`，所以模块里手写的「`os.read` 失败 → 先关句柄再抛」分支**没被走到**。
  它只证明了「不再静默返回空 bytes」，没证明那段手写 finally 的正确性。
- `test_path_guard_light.py` 的硬链接与 TOCTOU 两条在 Windows 上是**真跑**（`mklink /H`、
  `/J` 均成功，非管理员可用），但带 `skipif(win32 以外)`：在 POSIX 上它们会跳过，
  那时掩盖的是「这两个缺口在 POSIX 上同样存在」这一条仍未实证。
