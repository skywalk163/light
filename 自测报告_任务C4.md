# 自测报告_任务C4 — 非阻塞传输层与 selector

- 工作树：`g:\dswork\duan-light-merge\wt-C4`，分支 `task-C4`，基线 main = `e1dbfc3a`
- 环境：Windows / PowerShell 5.1 / Python 3.14.7
- 一句话：把 `stdlib/流式.light` 的裸阻塞 recv 改成非阻塞 + selector，让「异步」有真正的地基；解析层（read_body / 读分块 / 按行读取）未动。

---

## 1. 改了哪些文件（逐个列 + 改动意图）

| 文件 | 状态 | 改动意图 |
|---|---|---|
| `stdlib/选择器.light` | 新建 | fd 级 I/O 多路复用：`新建选择器/注册/注销/等待/等待就绪/关闭选择器` + TLS `是否愿等读/是否愿等写`。导入 `selectors/errno` 属「导入」，未增加「引 Python」计数。ERRNO 用 `errno.EAGAIN/EWOULDBLOCK` 常量，无硬编码数字。 |
| `stdlib/流式.light` | 修改 | 连接、TLS 握手、发送、读头保持原阻塞语义；发送完成后把套接字 `setblocking(False)`，`收` 改为非阻塞 + selector：命中 `SSLWantReadError`→等读、`SSLWantWriteError`→等写（分开回事件，不吞、不当 EAGAIN 一律等读），`EAGAIN/EWOULDBLOCK`→按事件等对应 fd。解析层与对外签名全部冻结。 |
| `tests/test_stream_light.py` | 新建 | selector 基础行为 + TLS want 分离 + 流式 round-trip（含 `发送后套接字转非阻塞`、裸 recv 应快速抛 EAGAIN）。 |
| `tests/test_stream_async_light.py` | 新建 | C4-3 M7 三重判据（结构 / 时序 / 反跑）。 |
| `tests/test_async.py` | 修改 | C4-4：清算假绿，全部改为「编译+真执行+断运行时行为」。 |
| `stdlib/SSE.light`、`tests/test_sse_light.py` | 未动 | 解析层正确，逐项覆盖，未触碰（约束：别动）。 |

> 签名冻结（总纲 §4.2）：`创建/连接/请求/read_body/按行读取/收/关闭` 签名与语义不变。机读证明：`tests/test_llm_client_light.py` 与 `tests/test_tls_light.py` 一行未动仍全绿（见 §2）。
> 越界检查：未触碰 `src/`、`大模型客户端.light`、`代理循环.light`、`代理工具集.light`、`路径护栏.light`。

## 2. 定向测试结果（汇总行直抄，不人工拼）

跑：`tests/test_stream_light.py tests/test_sse_light.py tests/test_llm_client_light.py tests/test_tls_light.py tests/test_stream_async_light.py tests/test_async.py`

```
71 passed, 3 skipped, 11 warnings in 11.28s
```

分文件：`test_stream_light`（selector 基础 3 条 + round-trip 全部）、`test_stream_async_light`（M7 2 条）、`test_async`（26 条全绿）、`test_sse_light`、`test_tls_light`（3 条）、`test_llm_client_light`（本体 9 条 + 3 skip，均未改）。

M7 时序实测（原始输出，`test_serial_vs_concurrent_timing`）：

```
串行 1.02s / 并发 0.50s
```

三判据核对：
- **结构判据**：`test_read_path_is_nonblocking` 断 `套接字.getblocking() is False`（行为可观察，不 grep 源码）。发送后读路径确为非阻塞。
- **时序判据**：串行 `1.02s > 1.0s`、并发 `0.50s < 0.8s`，且 `0.50 < 1.02`（断关系，不断绝对值）。两 mock server 各 `sleep(0.5)`，串行两条累计 ≥1.0s、并发两条重叠 ≈0.5s。
- **反跑判据**：见 §3 C4-3（改回阻塞 → `TestM7Triples` 变红）。

## 3. 反跑证明（硬要求，每条「改坏 → 变红」实测）

### C4-1 反跑 — 把选择器改坏
改法：`选择器.light` 里 `等待` 直接抛异常 / 不返回就绪项 → `tests/test_stream_light.py::TestSelector` 3 条全红：

```
tests\test_stream_light.py FFF
FAILED TestSelector::test_read_empty_then_flags_ready - AssertionError: assert 580 in []
FAILED TestSelector::test_unregister_no_longer_flagged - assert []
FAILED TestSelector::test_write_event_flags_ready - assert []
======================= 3 failed, 11 warnings in 1.58s ========================
```

### C4-2 反跑 — 把流式改回阻塞
改法：把 `s.setblocking(False)` 改回 `s.setblocking(True)`（阻塞收）→ `tests/test_stream_light.py::TestStreamRoundTrip` 变红：

```
tests\test_stream_light.py ...FF.
FAILED TestStreamRoundTrip::test_after_send_socket_is_nonblocking - AssertionError: assert True is False
FAILED TestStreamRoundTrip::test_naked_recv_raises_eagain_quickly - Failed: DID NOT RAISE OSError
================== 2 failed, 4 passed, 11 warnings in 2.16s ===================
```

### C4-3 反跑 — 把非阻塞那一步改回阻塞
改法：同上，`流式.light` 改回阻塞 → `tests/test_stream_async_light.py::TestM7Triples` 变红：

```
tests\test_stream_async_light.py F.
FAILED TestM7Triples::test_read_path_is_nonblocking - AssertionError: 发送后套接字应已转非阻塞
assert True is False
================== 1 failed, 1 passed, 11 warnings in 3.05s ===================
```

**说明（不掩盖）**：改回阻塞后，时序用例本身仍可能放行（并发重叠主要靠测试级 Python selector 同时等两个已就绪 fd + `收` 对已就绪 fd 立即返回）；真正钉住「读路径必须非阻塞」的是 `test_read_path_is_nonblocking` 这条结构断言，改回阻塞即红。结构断言即 §4.3 要求的「把非阻塞改回阻塞 → 用例变红」触发器，故 C4-3 反跑判据成立。时序用例断的是「两个 0.5s-sleep server：串行 >1.0s、并发 <0.8s 且并发<串行」这一关系本身。

## 4. 移交清单（光明写不出什么 → 临时绕法 → 期望的语言能力）

| 光明写不出什么 | 我的临时绕法 | 期望的语言能力 |
|---|---|---|
| `位与 / 位或` 二元运算符（该方言里未被当作二元运算符） | 用 `EVENT_READ(1) + EVENT_WRITE(2)` 相加等价位或；判中就绪用 `掩码==目标掩码` 等值比较 | 支持位与/位或二元运算符 |
| `await` 类型的 `FutureType→NumberType` 解包（类型推断返回 ANY/未知） | `test_async.py::test_await_unwraps_future` 如实断言「不崩、且不把 FutureType 泄漏进推断结果」，缺陷记入此清单 | 类型推断器实现 `await` 解包（`AwaitExpression` 推断其内部 Future 承载的真实类型） |
| `异步 遍历` 直接遍历普通 list | 遍历源改用异步生成器（`异步 函数 … 生成 …`），再 `异步 遍历 于` 该异步迭代器 | 让 `异步 遍历` 兼容普通可迭代对象（`aiter`/`__aiter__` 回退） |
| `defer` 语义：统一 `UnifiedCodeGenerator` 目前把 defer 体**内联**执行，未延迟到作用域退出 | 测试如实断言内联顺序（`开始→推迟执行→结束`）；若语义应为「延迟到作用域退出」，属待澄清项 | 明确的 defer 延迟语义（try/finally 兜底）说明与实现 |
| 顶层 `创建事件循环()` 运行时不可用（产物把该名字当裸标识符 → `NameError`，未排进 builtin） | 测试按其真实行为断言 `ERR:NameError`，并列此清单 | 把 `创建事件循环` 挂上真实事件循环 builtin（或官方文档化） |
| `等待` 不允许在模块顶层写（要求 `异步 运行 主()` 启动） | 所有异步都放进 `异步 函数` + `异步 运行 主()`；文档化 | 模块级 `await`（顶层异步启动能力） |

## 5. 已知未完成 / 未实测项

- **POSIX 分支未实测**：TLS 非阻塞的 `SSLWantRead/SSLWantWrite` 分离、`EAGAIN` 分流逻辑只在 Windows 下实测；POSIX（如 Linux/macOS）信号语义与 `selectors` 具体实现未跑，与本轮硬约束「POSIX 分支仅 Windows 测试」一致。
- `同步生成器 / 异步生成器 / 事件循环` 的转换只覆盖了 `test_async.py` 中可真跑的路径；`创建事件循环` 属语言缺陷（见 §4），未强行伪造。
- 时序断言的绝对值门限（串行>1.0 / 并发<0.8）按任务书给定；并发重叠的实际触发依赖测试级 selector 对待就绪 fd 的非阻塞读，已在 §3 说明，不掩盖。

## 6. skip / xfail 清单与掩盖分析

| skip/xfail | 跳过原因 | 掩盖了哪个未验证事实 | 是否属本轮已裁决要修 |
|---|---|---|---|
| `test_llm_client_light.py` 3 条 skip | `DEEPSEEK_API_KEY` 未设置 | 未验证真实大模型远端链路（仅验证了产物层解析/分帧逻辑，非阻塞 I/O 走本地 mock server 已覆盖） | 否。文件一行未动，是既有 skip，与 C4 无关 |

- 本文件未新增任何 xfail 额度用于掩盖；`test_async.py` 里的 `test_event_loop_create` 与 `test_await_unwraps_future` 属「如实断言当前语言缺陷行为」（`ERR:NameError` / 不泄漏 Future），不是 xfail 掩盖，缺陷均已列入 §4 移交清单。

---

## C4-4 假绿清算摘要

`tests/test_async.py` 已按第四轮 §5 全量清算：原本约 58 处 `assert '<字符串>' in py_code / in code` 形态全部移除，改为「LightParser 编译 → `exec` 真跑 → 断运行时输出/时序/副作用/返回值」。覆盖 async/await、defer、异步作用域（真并发，用两条各 `异步睡眠(0.5)` 的任务做相对时序判据）、同步/异步生成器、异步遍历+等待、嵌套 await、异步 I/O、类型推断。

现文件已核对：grep 无任何 `_compile_async` / `_run_async_duan` 残留，无 `assert '<字符串>' in code` 式判据（仅 docstring 提及口径）。全文 26 条用例全部真跑断言。