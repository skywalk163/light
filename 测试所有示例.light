# 光明示例测试工具（真正执行版）
# 使用方式：light run 测试所有示例.light --backend src

段落 运行光明文件 接收 文件路径：
  尝试：
    设 源代码 为 读取文件(文件路径)

    设 解析器模块 为 __import__("light_parser_v3")
    设 解析器 为 解析器模块.LightParser()
    设 语法树 为 解析器.parse(源代码)

    设 生成器模块 为 __import__("code_generator")
    设 生成器 为 生成器模块.PythonCodeGenerator()
    设 代码 为 生成器.generate(语法树)

    设 命名空间 为 字典创建()
    设 _ 为 _light_builtin.字典设置(命名空间, "__name__", "__main__")
    exec(代码, 命名空间)

    返回 列(真, "")
  捕获 异常：
    设 错误信息 为 str(异常)
    设 短错误 为 截取(错误信息, 0, 80)
    返回 列(假, 短错误)

段落 主 接收：
  打印("=== 光明示例测试工具 ===")
  打印("")
  设 示例目录 为 "examples"
  设 文件列表 为 列出目录(示例目录)
  设 通过数 为 0
  设 失败数 为 0

  遍历 名 在 文件列表：
    设 名长度 为 长(名)
    如果 名长度 大于等于 5：
      设 后缀 为 截取(名, 名长度减5, 名长度)
      如果 后缀 等于 ".light"：
        设 文件路径 为 示例目录 加 "/" 加 名
        打印("测试: " 加 名)

        设 结果 为 运行光明文件(文件路径)
        如果 结果[0]：
          打印("  ✅ 通过")
          设 通过数 为 通过数 加 1
        否则：
          打印("  ❌ 失败: " 加 结果[1])
          设 失败数 为 失败数 加 1
        打印("")

  打印("=== 测试结果 ===")
  打印("通过: " 加 转字符串(通过数))
  打印("失败: " 加 转字符串(失败数))

  如果 失败数 大于 0：
    返回 1
  否则：
    打印("")
    打印("🎉 所有测试通过！")
    返回 0

主()