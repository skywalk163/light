# 光明记账小程序
# 功能：记录收支、查看统计、保存数据

导入 JSON。

# 全局变量
定义 记录列表 等于 []。
定义 数据文件 等于 "记账数据.json"。

# 添加收入
《添加收入》段(金额, 备注)：
  定义 记录 等于 ["收入", 金额, 备注, 时间戳()]。
  记录列表.追加(记录)。
  打印("收入记录成功！")。

# 添加支出
《添加支出》段(金额, 备注)：
  定义 记录 等于 ["支出", 金额, 备注, 时间戳()]。
  记录列表.追加(记录)。
  打印("支出记录成功！")。

# 查看所有记录
《查看记录》段()：
  如果 len(记录列表) 等于 0 那么：
    打印("暂无记录")。
    返回。
  
  打印("\n========== 记账记录 ==========")。
  定义 序号 等于 1。
  遍历 记录 之 记录列表：
    定义 收支标记 等于 记录[0]。
    定义 金额 等于 记录[1]。
    定义 备注 等于 记录[2]。
    定义 时间 等于 记录[3]。
    定义 时间字符串 等于 格式化时间(时间, "%Y-%m-%d %H:%M")。
    
    打印(str(序号) + ". [" + 收支标记 + "] " + str(金额) + "元 - " + 备注 + " (" + 时间字符串 + ")")。
    定义 序号 等于 序号 加 1。
  
  打印("==============================")。

# 统计收支
《统计收支》段()：
  定义 总收入 等于 0。
  定义 总支出 等于 0。
  
  遍历 记录 之 记录列表：
    如果 记录[0] 等于 "收入" 那么：
      定义 总收入 等于 总收入 加 记录[1]。
    否则：
      定义 总支出 等于 总支出 加 记录[1]。
  
  定义 结余 等于 总收入 减 总支出。
  
  打印("\n========== 收支统计 ==========")。
  打印("总收入: " + str(总收入) + "元")。
  打印("总支出: " + str(总支出) + "元")。
  打印("结  余: " + str(结余) + "元")。
  打印("==============================")。

# 保存数据
《保存数据》段()：
  定义 数据 等于 JSON.序列化(记录列表, 2)。
  写入文件(数据文件, 数据)。
  打印("数据保存成功！")。

# 加载数据
《加载数据》段()：
  如果 文件存在(数据文件) 那么：
    定义 内容 等于 读取文件(数据文件)。
    定义 已加载记录 等于 JSON.解析(内容)。
    记录列表.清空()。
    遍历 记录 之 已加载记录：
      记录列表.追加(记录)。
    
    打印("已加载 " + str(len(记录列表)) + " 条记录")。
  否则：
    打印("无历史数据，开始新记录")。

# 显示菜单
《显示菜单》段()：
  打印("\n========== 记账程序 ==========")。
  打印("1. 添加收入")。
  打印("2. 添加支出")。
  打印("3. 查看记录")。
  打印("4. 收支统计")。
  打印("5. 保存数据")。
  打印("6. 退出程序")。
  打印("==============================")。

# 主程序
《主程序》段()：
  打印("欢迎使用光明记账程序！")。
  《加载数据》()。
  
  定义 运行中 等于 真。
  当 运行中：
    《显示菜单》()。
    打印("请输入选项(1-6): ")。
    定义 选项 等于 input()。
    
    如果 选项 等于 "1" 那么：
      打印("请输入收入金额: ")。
      定义 金额 等于 float(input())。
      打印("请输入备注: ")。
      定义 备注 等于 input()。
      《添加收入》(金额, 备注)。
    否则若 选项 等于 "2" 那么：
      打印("请输入支出金额: ")。
      定义 金额 等于 float(input())。
      打印("请输入备注: ")。
      定义 备注 等于 input()。
      《添加支出》(金额, 备注)。
    否则若 选项 等于 "3" 那么：
      《查看记录》()。
    否则若 选项 等于 "4" 那么：
      《统计收支》()。
    否则若 选项 等于 "5" 那么：
      《保存数据》()。
    否则若 选项 等于 "6" 那么：
      《保存数据》()。
      打印("感谢使用，再见！")。
      定义 运行中 等于 假。
    否则：
      打印("无效选项，请重新输入！")。

# 运行主程序
《主程序》()。