# 光明记账小程序
# 功能：记录收支、查看统计、保存数据
#
# 说明：D07 旧语法清理迁移（2026-08-06）
#   - 定义 X 等于 Y → 设 X 为 Y
#   - 《段名》段(...) → 段落 段名 接收 ...
#   - 如果 X 那么： → 如果 X：
#   - str() → 转字符串()、len() → 列表长度()、.追加() → 列表追加()
#   - JSON.解析/JSON.序列化 → builtins 解析JSON/序列化JSON
#   - 避免函数内修改全局变量：数据通过参数传递

# 添加收入
段落 添加收入 接收 记录列表, 金额, 备注：
  列表追加(记录列表, ["收入", 金额, 备注, 时间戳()])。
  打印("收入记录成功！")。
  返回 记录列表。

# 添加支出
段落 添加支出 接收 记录列表, 金额, 备注：
  列表追加(记录列表, ["支出", 金额, 备注, 时间戳()])。
  打印("支出记录成功！")。
  返回 记录列表。

# 查看所有记录
段落 查看记录 接收 记录列表：
  如果 列表长度(记录列表) 等于 0：
    打印("暂无记录")。
    返回。

  打印("\n========== 记账记录 ==========")。
  设 序号 为 1。
  遍历 记录 于 记录列表：
    设 收支标记 为 记录[0]。
    设 金额 为 记录[1]。
    设 备注 为 记录[2]。
    设 时间 为 记录[3]。
    设 时间字符串 为 格式化时间(时间, "%Y-%m-%d %H:%M")。

    打印(转字符串(序号) + ". [" + 收支标记 + "] " + 转字符串(金额) + "元 - " + 备注 + " (" + 时间字符串 + ")")。
    设 序号 为 序号 加 1。

  打印("==============================")。

# 统计收支
段落 统计收支 接收 记录列表：
  设 总收入 为 0。
  设 总支出 为 0。

  遍历 记录 于 记录列表：
    如果 记录[0] 等于 "收入"：
      设 总收入 为 总收入 加 记录[1]。
    否则：
      设 总支出 为 总支出 加 记录[1]。

  设 结余 为 总收入 减 总支出。

  打印("\n========== 收支统计 ==========")。
  打印("总收入: " + 转字符串(总收入) + "元")。
  打印("总支出: " + 转字符串(总支出) + "元")。
  打印("结  余: " + 转字符串(结余) + "元")。
  打印("==============================")。

# 保存数据
段落 保存数据 接收 记录列表, 数据文件：
  设 数据 为 序列化JSON(记录列表, 2)。
  写入文件(数据文件, 数据)。
  打印("数据保存成功！")。

# 加载数据
段落 加载数据 接收 数据文件：
  如果 文件存在(数据文件)：
    设 内容 为 读取文件(数据文件)。
    设 已加载记录 为 解析JSON(内容)。
    打印("已加载 " + 转字符串(列表长度(已加载记录)) + " 条记录")。
    返回 已加载记录。
  否则：
    打印("无历史数据，开始新记录")。
    返回 []。

# 显示菜单
段落 显示菜单：
  打印("\n========== 记账程序 ==========")。
  打印("1. 添加收入")。
  打印("2. 添加支出")。
  打印("3. 查看记录")。
  打印("4. 收支统计")。
  打印("5. 保存数据")。
  打印("6. 退出程序")。
  打印("==============================")。

# 主程序
段落 主程序：
  打印("欢迎使用光明记账程序！")。
  设 数据文件 为 "记账数据.json"。
  设 记录列表 为 加载数据(数据文件)。
  设 运行中 为 真。

  当 运行中：
    显示菜单()。
    打印("请输入选项(1-6): ")。
    设 选项 为 input()。

    如果 选项 等于 "1"：
      打印("请输入收入金额: ")。
      设 金额 为 float(input())。
      打印("请输入备注: ")。
      设 备注 为 input()。
      记录列表 为 添加收入(记录列表, 金额, 备注)。
    否则若 选项 等于 "2"：
      打印("请输入支出金额: ")。
      设 金额 为 float(input())。
      打印("请输入备注: ")。
      设 备注 为 input()。
      记录列表 为 添加支出(记录列表, 金额, 备注)。
    否则若 选项 等于 "3"：
      查看记录(记录列表)。
    否则若 选项 等于 "4"：
      统计收支(记录列表)。
    否则若 选项 等于 "5"：
      保存数据(记录列表, 数据文件)。
    否则若 选项 等于 "6"：
      保存数据(记录列表, 数据文件)。
      打印("感谢使用，再见！")。
      设 运行中 为 假。
    否则：
      打印("无效选项，请重新输入！")。

# 运行主程序
主程序()。
