# 自测报告_任务T3

**日期**：2026-09-04
**分支 / worktree**：`task-T3` / `G:\dswork\duan-light-merge\wt-T3`
**提交**：`5dd3a0a2` fix(opt): _inline_small_functions 删除前统计全部引用形式，堵地址引用误删
**定向测试**：`python -m pytest tests/test_llvm_optimizer.py -q` → **64 passed**（基线 58 + 新增 6）

---

## 1. 任务范围

- **修复目标**：`_inline_small_functions` 内联/删除前必须统计**全部**引用形式（直接调用、地址取用、间接调用、外部/全局引用）；只要存在任一引用即不内联。
- **反跑用例**：构造「被地址引用的小函数」，修复前被删（行为错误）、修复后保留且行为正确。
- **回写文档**：`docs/known_issues.md` §12.4。
- **文件隔离**：只动 `src/llvm/size_optimizer.py`、`tests/test_llvm_optimizer.py`、`docs/known_issues.md`。`src/llvm/optimizer_pipeline.py` 经核实本就正确，**无需改动**；`src/llvm/runtime_typed.c` 与其它 codegen 模块未触碰。

## 2. 缺陷根因

`src/llvm/size_optimizer.py::SizeOptimizer._inline_small_functions`（旧实现）：

```python
call_count = len(re.findall(rf'@\b{re.escape(func_name)}\b', ir))  # 把定义行也算进去
if call_count <= 1:                                                # main 无调用 → count=1 → 删
    ir = re.sub(rf'define\s+.*?@{func_name}\s*\(.*?\)\s*\{{[^}}]*\}}', '', ir, flags=re.DOTALL)
```

两层错误叠加：
1. **计数口径错**：`call_count` 把**定义行本身**当作一次“调用”。于是无调用点的 `main`（count=1）命中 `<= 1` 被整段删掉；而对 `@helper` 这类「被地址引用、零调用」的小函数，count=2（定义行+全局初始值），单看它自己不会被删。
2. **删除正则扫尾**：删除 `main` 用的是 `define\s+.*?@main...\}`（DOTALL 懒惰匹配），从第一个 `define`（`@helper`）一路吞到 `main` 的收尾 `}`，把夹在中间的 `@helper` 定义**连带删掉**，只留下 `@fp = global i32 (i32)* @helper` 这种悬空引用——链接期 undefined symbol。

实测复现（修复前）：IR 只剩 `@fp = global i32 (i32)* @helper`，`@helper` 与 `@main` 的定义全部消失。

## 3. 修复内容

重写 `SizeOptimizer._inline_small_functions`（含新增 `_function_spans` 按行扫定位与 `_LABEL_CHARS`）：

- **删除前统计全部引用形式**：对每个候选小函数，扫描**定义行以外**的所有行，只要出现 `@name`（直接调用、全局初始值/ptrtoint/bitcast 地址取用、函数指针间接调用、跨函数与外部可见引用）即视为被引用、**保留**。
- **定义行自身不算引用**；**`main` / `__light_init` 一律不删**。
- **函数体定位改用按行扫**（收尾为单独一行 `}`），不再用会被行内结构体类型截断、或 DOTALL 从头吞到尾的 `define ... \{[^}]*\}` 正则。
- 与 `optimizer_pipeline._drop_unreferenced_functions` 的引用语义对齐（该侧本就按文本全量引用计数，已验证覆盖地址引用，无需改动）。

## 4. 反跑判据验证

新增反跑用例类 `Test小函数清理_统计全部引用形式`（tests/test_llvm_optimizer.py）：

| 用例 | 旧实现（只看调用次数） | 修复后 |
|---|---|---|
| `test_被地址引用的小函数必须保留`（`@fp = global ... @helper`） | **FAIL**（@helper 被删、@fp 悬空） | **PASS** |
| `test_地址取用ptrtoint的小函数必须保留` | **FAIL** | **PASS** |
| `test_经函数指针间接调用的小函数必须保留` | **FAIL** | **PASS** |
| `test_有直接调用的小函数必须保留` | **FAIL**（main 误删连带） | **PASS** |
| `test_零引用的小函数仍会被清掉`（反跑护栏） | **FAIL**（main 被误删） | **PASS** |
| `test_间接调用真跑出42`（clang 编译+链接+运行） | **FAIL**（函数被删无法编译） | **PASS** |

- **改回原判断立即立红**：修复前 6 条全部 FAIL（见上表），报错精确复现「`define i32 @helper` not found / `@fp = global i32 (i32)* @helper` 悬空」。
- **修复后绿**：6 条全部 PASS；其中 `test_间接调用真跑出42` 由 clang 实际编译链接、经函数指针 `call %f(i32 41)` 真跑出 **42**，是「行为正确」的最终证据（非 grep IR 文本）。

## 5. 定向测试结果

```
python -m pytest tests/test_llvm_optimizer.py -q
64 passed, 1 warning in ~7s   （基线 58 + 新增 6）
```

- 既有 `TestO3管线产出合法IR`（有调用点的函数不许被删 / 无调用点仍清理 / 结构体函数不留悬空片段）、`Test优化标志不带legacyPass名` 等全部保持通过，零回归。
- 提交后工作树 `git status` 干净。

## 6. 文档回写

`docs/known_issues.md` §12.4 追加「已解决（T3 / 2026-09-04）」块，说明修复内容、反跑用例与验证结果。

## 7. 文件隔离核对

| 文件 | 状态 |
|---|---|
| `src/llvm/size_optimizer.py` | 修改（仅 `_inline_small_functions` 相关） |
| `tests/test_llvm_optimizer.py` | 修改（新增 `Test小函数清理_统计全部引用形式`） |
| `docs/known_issues.md` | 修改（§12.4） |
| `src/llvm/optimizer_pipeline.py` | **未改动**（核实 `_drop_unreferenced_functions` 已按全量引用计数，覆盖地址引用） |
| `src/llvm/runtime_typed.c` / 其它 codegen 模块 | 未触碰（归 T7/T9） |

## 8. 备注（范围外，非本次引入）

`SizeOptimizer.optimize()` 全链对真实 codegen IR 冒烟时观察到：`@main` 在 `_deduplicate_functions`（函数体相同的函数去重合并，`stats.dedup=1`）处被合并掉。这是**既有**行为（仓库已知不在 IR 层跑 `SizeOptimizer`，见 `src/llvm/compiler.py:516` 注释），与本次 `_inline_small_functions` 改动无关（本次该 pass 全程 `inlined=0`，未删任何东西），不在 T3 任务范围，未处理。
