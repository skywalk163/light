# 自测报告 · 任务 G9 · S1（度量先行）

> 泳道 G9（度量 / 门禁 / 验收）。分支 `task-G9`，起点 `5478ebfe`（tag `pre-merge-round9`）。
> 本阶段只碰 `tools/ci/*`、`任务书/*`、`tests/unit/*`、`.gitea/workflows/ci.yml`。
> **产品代码零改动**（`src/` / `stdlib/` / `cli/` / `examples/` 一行未动）。

---

## 1. 改了哪些文件（逐个 + 一句意图）

**新建门禁脚本（3）**

- `tools/ci/floor_bootstrap.py` —— 地板自举率门禁。把 `stdlib/builtins.py` 的 114 个
  内置函数拉进分母，这是六轮来最大的度量盲区。
- `tools/ci/native_product.py` —— 原生腿产品门禁。模块级 / CLI 路径级 / 平台级三张表，
  补住既有 `docs/原生腿能力清单.json`（节点与符号级）之上的产品级空白。
- `tools/ci/dist_criteria.py` —— 分布式判据门禁。核心判据是「有没有能立红的反跑」。

**新建基线（3）**

- `tools/ci/floor_bootstrap_baseline.json`、`tools/ci/native_product_baseline.json`、
  `tools/ci/dist_criteria_baseline.json` —— 首次接入的起点快照。
  **这三份必须在 S1 合并点由主线重建**（总纲 §5 红线 5：基线只在合并点重建）；
  这里写的是「新门禁没有基线就跑不起来」的接入值，不是合并态。

**新建清单（3）**

- `任务书/自举地板清单.json` —— 114 条，`ast` 枚举真值 + G9 的 32 条边界裁决。
- `任务书/原生腿产品清单.json` —— 模块表 76 条 / CLI 路径表 8 条 / 平台矩阵 12 格。
- `任务书/分布式判据清单.json` —— 九条能力，全 `none`（这是真实状态）。

**新建文档（1）**

- `任务书/度量契约_第九轮.md` —— 三张清单的字段契约 + 谁填哪个字段 + 七道门禁复跑命令。
  总纲 §6 要求「先写成文档 commit 再动手」，这是度量侧那一份。

**新建测试（1）**

- `tests/unit/test_ci_gates_round9.py` —— 31 例，三道新门禁的双向反跑。

**改动（3）**

- `.gitea/workflows/ci.yml` —— 门禁段末尾接入三道新门禁（硬判绿，无 `|| true`），
  紧挨既有门禁，不与测试并发。
- `.gitignore` —— 加 `!任务书/*.json`。**这条是实测撞出来的**：`.gitignore:46` 的
  `*.json` 会把三张新清单全吃掉，而 `git status` 看不见被忽略的文件 ——
  差一步就变成「门禁脚本进了仓、清单没进，CI 上读不到清单直接 rc=2 长红」，
  与之前基线被吃掉那次是同一类事故。既有的 `功能对标清单_harness.json` 之所以没事，
  是因为它早就在 index 里，gitignore 对已跟踪文件不生效。
- `任务书/功能对标清单_harness.json` —— 续维：9 条非 done 条目加 `第九轮归属` 字段，
  `本轮目标` 改写成第九轮目标并在括号内保留原目标。**状态一条未改**（done 仍 12）。

---

## 2. 定向测试结果（输出已落盘）

```
python -m pytest tests/unit/test_ci_gates_round9.py -q          → 31 passed（0.94s）
python -m pytest tests/unit/test_ci_gates.py \
    tests/unit/test_ci_gates_round9.py \
    tests/unit/test_native_leg_capability.py -q                 → 71 passed（1.77s）
```

平台矩阵的 Windows 四格是实跑出来的，不是抄文档：

```
python -m pytest tests/test_llvm_net.py tests/test_llvm_tls.py \
    tests/test_native_cli.py -q                                 → 37 passed, 3 skipped（106.7s）
```

（`TestB1SocketPrimitives` = socket，`TestB2IOMultiplexing` = poller，
`TestB3EventLoop` = 事件循环，`test_llvm_tls.py::TestTLSCLayer` = Schannel TLS。）

**七道门禁逐条 rc=0**（独占跑，不与测试并发）：

```
[对标清单门禁]   20 条，done 12 / partial 7 / none 1          通过
[地板门禁]       114 条，0/82 = 0.00%（扣 32 条真边界）        通过
[原生产品门禁]   模块 0/76、坏路径 1、矩阵通过 4 / 未实测 8    通过
[分布式判据门禁] 9 条能力，done 0 / partial 0 / none 9         通过
[自举率门禁]     24/76 = 31.58%，影子 53，关键路径 16/18       通过
[Python直调门禁] 306 行 / 32 文件（基线 308）                  通过
[假测试门禁]     存量 469 条冻结，无新增                        通过
```

---

## 3. 移交清单（光明写不出什么 → 绕法 → 期望能力）

本阶段无 —— G9 只写 Python 侧门禁与 JSON 清单，没碰光明源码，产生不了语言层欠账。

---

## 4. 已知未完成 / 未实测项

1. **三份新基线是「接入值」，不是合并态。** S1 合并点必须重建（第七轮 E7 就是在这
   一点上踩过：三份新基线是合并前存量）。
2. **`原生腿产品清单.json` 的模块表 76 条全是 `未实测`。** 这是唯一诚实的初值 ——
   G9 没有逐个编译过 76 个模块；填 `不可编译` 是替 B9 下结论，填 `可编译` 是造假。
   B9 按契约 §3.1 的判定命令实测后改状态。
3. **平台矩阵 8 格 `未实测`（Linux / macOS 各 4 格）**，没有该平台的 runner。
   另已知 POSIX TLS 是 `dv_tls_unsupported` 桩，S2 才落地。
4. **`spec_coverage_baseline.json` 的 `distribution` 字段是第七轮存量**（记的是
   partial 6 / none 2，而当前真值是 partial 7 / none 1 —— 第八轮 #16 从 none 升 partial
   之后没刷）。门禁只判 `done_count` / `done_ids` / `ids`，所以这不影响判定，
   但**合并点重建基线时要连它一起刷**。
5. **地板清单的 32 条边界裁决是 G9 的初判，可争议。** C9 若认为某条不该豁免
   （或该豁免），走契约 §2 的裁决路径，不许自己改 `native_required`。
6. **`--sync-list` 的删除侧只报告不代删。** 删条目是记账动作，得由人决定并写进
   交付报告。

---

## 5. 四张清单里改了哪几条状态（从什么变成什么）

**状态一条没改。** 本阶段只加字段与建表：

- `功能对标清单_harness.json`：done 12 / partial 7 / none 1 **不变**；
  #2 / #5 / #6 / #7 / #16 / #17 / #19 / #20 加了 `第九轮归属`（E9 / E9+A9 / E9 /
  E9+A9 / E9 / E9 / F9评估 / D9），`本轮目标` 改写成第九轮目标。
- `自举地板清单.json`：新建，114 条全 `python`，地板自举率 **0/82 = 0.00%**（起点）。
- `原生腿产品清单.json`：新建，可编译比例 **0/76 = 0.00%**，坏路径 **1**
  （`light run --backend llvm`），平台矩阵 **已实测通过 4 / 未实测 8**。
- `分布式判据清单.json`：新建，九条能力全 **none**（分布式开工时是零，不是弱）。

---

## 6. 本阶段新增的每条判据 + 反跑方式

判据都在 `tests/unit/test_ci_gates_round9.py`，每条都写成了「反例几乎一样但不该命中」。

**地板门禁**

- 名单双向咬合 → 反跑：清单删掉 `乙` 判红（防腐烂）；加一条 `丙` 判红（防吹牛）。
  `test_名单双向咬合`
- 证据必须落在定义行 ±2 窗口 → 反跑：把行号从 4 改成 20（**仍在文件范围内**），
  判红。这条专门证伪「上界断言」。`test_行号在文件内但不在窗口内即红`
- 落点不许被同名 `.py` 遮蔽 → 反跑：删掉 `.light` 首两行的魔数「纯光明实现」，判红。
  `test_被同名py遮蔽的落点不算搬迁`
- 豁免不许扩张 / 分母不许缩 → 反跑：把 `乙` 的 `native_required` 改成 `true`
  （比例从 50% 变 100%，看着是进步），判红。`test_豁免新增即红且分母不许缩`
- 自举率只升不降 → 反跑：把 `甲` 的语言从 `light` 改回 `python`，判红。
- 豁免必须写理由 / `null` 未分类判红 → 反跑：备注改成空白、`native_required` 改成
  `null`，各判红。
- `--sync-list` 只增不改 → 反跑：sync 后断言 `甲` 的证据与语言字段**未被覆盖**。

**原生产品门禁**

- 模块表与 `stdlib` 双向咬合 → 反跑：删掉 `乙` 判红；加不存在的 `丙` 判红。
- 源码新增后端取值必须登记 → 反跑：给 `cli/light.py` 的 `choices` 加 `native`
  而不登记，判红。`test_源码新增后端取值必须登记`
- 摘除死腿只告警不判红 → 反跑：表里留 `llvm` 而源码没有它，**仍绿**（摘死腿是许可
  处置，不该被拦）。`test_源码摘除后端取值只告警不判红`
- 坏路径数只降不升 → 反跑：新增一条 `坏`，判红。
- 平台矩阵未实测只降不升 → 反跑：新增一格 `未实测`，判红。
- 可编译比例只升不降 → 反跑：把 `可编译` 改回 `未实测`，判红。
- `可编译` 必须带可复跑命令 → 反跑：证据改成「我试过了」（无 `light` 命令），判红。
- 状态值域 → 反跑：状态写「基本可编译」，判红。

**分布式判据门禁**

- `done` 必须有可定位判据 → 反跑：判据行号改成 9999（越界），判红。
- `done` 必须有反跑 → 反跑：`反跑` 清空，判红。
- 反跑必须是动作 → 反跑：改成「见 … 里的说明」（无动作词），判红。
  **已知粗判据边界**：`注释` 既可能是动作也可能是名词，后者会漏判；宁可漏判不误伤。
- 反跑必须可复跑 → 反跑：指向 `tests/不存在.py`，判红。
- 能力可增不可删 + `done` 不许退回 → 反跑：删掉「故障隔离」判红；把 `done` 改成
  `partial` 判红。`test_双向反跑_删能力与done退回都判红`
- `none` 必须记账 → 反跑：备注清空，判红。

**活体判据（不吃临时目录，直接钉真仓库）**：`Test真清单在册` 四例 ——
三张清单必须在 `任务书/` 下且**不在 `docs/`**（放 `docs/` 会被
`tests/unit/doc_block_scan.py` 扫）；地板清单与 `builtins.py` 咬合；
模块表与 `stdlib` 咬合；分布式九条能力齐。

---

## 7. 给其他泳道的话（S1 开工即可用）

- **C9**：`任务书/自举地板清单.json` 的 `当前实现语言` / `计划落点` / `证据` 是你的字段。
  搬完一条改一条，跑 `python tools/ci/floor_bootstrap.py --root .` 自查。
  证据必须指到真的 `段落` 定义行（±2 窗口），且落点不能被同名 `.py` 遮蔽。
- **B9**：`原生腿产品清单.json` 三张表是你的。**先把「原生可编译判定命令」写进
  模块表的 `证据` 字段**（总纲 §6 的第二份契约就落在这），其他路才有自查手段。
  给 `--backend` 加 `native` 取值时记得同步登记，否则门禁判红。
- **F9**：`分布式判据清单.json` 九条全 `none` 等你填。`done` 的门槛是判据 + 反跑
  两个字段都过，写不出反跑的判据不算判据。
- **E9**：对标清单 7 条 partial 里有 6 条归你（#2/#5/#6/#7/#16/#17），
  改状态用「编号 + 功能名」双字段匹配，别照自己报告里的行号改。

到此停下等主线跑全量，不自行合并到 `main`。
