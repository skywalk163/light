# 第53轮 任务1 —— L-172 复现 + 根因诊断

> 日期：2026-09-17 ｜ 优先级：P0 ｜ 跨项目：light-merge（编译器侧）
> 编译器基线：light-merge `HEAD = ed4a175d (R49)`（与 R52 观察期同一份编译器，未变更）
> 复现用例：`light-merge/examples/test_L172.light`
> 诊断脚本（取证用）：`light-merge/_l172probe/*.py`

---

## 0. 结论先行（TL;DR）

| | 结论 |
|---|---|
| 任务书描述的形态 | 「非 `主` 段落，其函数体**按名调用被导入函数**」→ **在当前编译器上不复现**（1620 组形态 + 5 轮定向探针 + 真实 R52 用例机械还原，全部正常执行 `主()`）。该描述是对 R52 现象的**误归因**。 |
| 真复现（同一「入口静默阻断」家族） | **模块级对入口名取裸引用（非调用）会静默抑制入口自动调用**：`设 入口回调 为 主` / `设 回调表 为 {"入口": 主}` → 产物 rc=0、无报错、**一个字都不打印**（`主()` 从未执行）。已复现（§2），已修（任务2）。 |
| 次静默点（同族） | **入口段落带形参 → 静默不调用**（`段落 主 接收 参数:` → rc=0、无输出、无报错）。已改为**编译期告警**（任务2）。 |

复现判据（任务书「rc=0 但主()没跑」）在两种形态下都成立，且都属最危险的**静默失败**类。

---

## 1. 任务书描述的形态：为什么它不复现

### 1.1 描述形态的最小用例（已入库为 test_L172.light §1）

```light
从 JSON 导入 序列化JSON          # 导入一个函数

段落 辅助 接收 值:               # 非 主 段落
  设 文本 为 序列化JSON(值)      # 函数体**按名调用被导入函数**
  返回 文本

段落 主:
  断言相等(辅助({"a": 1}), 序列化JSON({"a": 1}), "1a")
  打印("L172_主已执行")
```

实测：`rc=0`，`L172_主已执行` **正常打印**，`主()` 正常执行。→ 描述形态本身不构成阻断。

### 1.2 复现努力清单（全部未复现）

| # | 探针 | 覆盖 | 结果 |
|---|---|---|---|
| 1 | `_l172probe/matrix.py` | 7 种形态：辅助段落有/无参、调用放在表达式内/语句内/`尝试`内/`返回`里、导入来自 stdlib(`JSON`)/本地 `.light` 模块、辅助定义在导入前/后、两个辅助段落、返回值被主使用 | 7/7 正常执行 `主()` |
| 2 | `_l172probe/fuzz.py` + `fuzz2.py` | **1620 组**形态组合（导入来源 3 × 调用写法 3–5 × 包裹结构 5 × 形参 3 × 入口风格 2 × 定义顺序 2 × 段落名 3），判定「有 `主` 但无 `MAIN-OK` 输出」 | **静默 0 组** |
| 3 | `_l172probe/pre_fix.py` | 把 **R52 真实用例** `test_R52_ssh协议.light` 机械还原为报告 §4.3 描述的 pre-fix 形态：12 个包裹段落（`抛校验远程路径`…`抛校验已准备`），每个段体内**按名**调用被导入校验器 | rc=0，输出「--- R52 任务3 ssh协议 纯逻辑测试通过 ---」，结构完好（`主` body=76） |
| 4 | `_l172probe/w_lightharness.py` | lightharness 布局（主文件在 `examples/`、真模块在 `src/`、走 `_light_import_hook` 运行时导入）+ 包裹段落 + 自动入口 | 入口块正常追加、`主()` 正常执行 |
| 5 | `_l172probe/var_sweep.py` | 空格/连写变体（`为校验远程路径`、`设丢弃`、全角 `：`、标识符改写） | 要么正常，要么**报错 rc=1**；无静默 |
| 6 | 全语料扫描 `_l172probe/scan_missing_entry.py` | 730 个 `.light`：源含 `段落 主` 但产物缺入口调用块 | **0 例伪抑制**（336 例 `invokes=True` 全部由**真实** `主()` 调用解释） |

### 1.3 为什么容易被误判成「按名调用被导入函数」

R52 报告 §4.3 记录的规避手段是「把校验器以**参数引用**传入 `校验抛远程(校验器, 值, ...)`」，同时另一条修复记录（§4.4）是「`函数: <expr>` 内联 lambda 不支持 → 改传具名函数引用」。两次改动**同时**碰了「函数引用」这一条线索，观察者因此把「包裹段落 + 按名调用」当成根因——但真正的阻断条件是**模块级出现对入口名的裸引用**，与段落体内怎么调用被导入函数无关。

---

## 2. 真复现：模块级**裸引用**入口名 → 入口自动调用被静默抑制

### 2.1 最小复现（已入库为 test_L172.light §2）

```light
段落 主:
  打印("MAIN-OK")

设 入口回调 为 主          # ① 或者  设 回调表 为 {"入口": 主}
```

实测（修复前口径）：

```
rc = 0      stdout 行数 = 0      stderr = 空
```

**`MAIN-OK` 不打印、无任何报错、进程 rc=0** —— 与任务书描述的现象逐字一致。

### 2.2 根因（定位到函数级）

`src/code_generator.py`：

```
1354  if is_main:
1355      entry = self._find_entry_paragraph(module)
1356      if (entry is not None
1357              and self._paragraph_arity(entry) == 0
1358              and not self._module_invokes_entry(module, entry)):   # ← 闸门 3
1359          self._entry_call = (...)                                    # ← 只有这里才补入口
...
1434  if self._entry_call:
1438      self._add_line("if __name__ == '__main__':")
1439      self._add_line(f"{entry_name}()")
```

`_module_invokes_entry()`（旧）：

```python
for stmt in module.statements:
    if stmt is entry or isinstance(stmt, (Paragraph, ClassDefinition)): continue
    if self._node_calls_name(stmt, name):        # ← 调用**或裸引用**都算
        return True
```

`_node_calls_name()`（旧）把 `ParagraphCall`（调用）与 `Identifier`（引用）**同等**判定为「已启动入口」：

```python
if isinstance(node, (ParagraphCall, Identifier)) and getattr(node, 'name', None) == name:
    return True
```

于是 `设 入口回调 为 主` 里那个**标识符** `主` 被当成「模块级已经调用过入口」，编译器据此**不再补发** `if __name__ == '__main__': 主()`。

失败链（三段都不报错，故极难诊断）：

1. 隐藏闸门：裸引用 → 判为「已启动入口」→ 不补入口调用（**无告警**）；
2. 产物照常生成：模块级语句（两个段落定义 + `设 入口回调` + 断言）全部执行 → 「模块级代码照常跑」；
3. 进程正常退出：`rc=0`、stdout 空 → 使用者只看到「程序没输出」，无任何线索。

语义上 **取引用 ≠ 启动入口**：把入口登记成回调、塞进调度表，本来就该在入口内或之后发生，而不是取消入口本身。这正是 bug 的判定依据。

---

## 3. 次静默点：入口带形参 → 静默不调用（同族，已改为告警）

```light
段落 主 接收 参数:
  打印("MAIN-OK")
```

实测（修复前）：`rc=0`、stdout 空、stderr 空。`_paragraph_arity(entry) > 0` 时 L-070 直接跳过自动调用，**同样无任何提示**。

全语料影响面（`_l172probe/measure_arity.py`）：373 个有入口段落的文件中，仅 **1 个** arity>0 ——
`lightharness/src/总入口.light`（库文件，由 `examples/运行CLI.light` 显式 `主()` 调用，不以 main 身份直接运行）。故「告警」方案对既有语料零噪声。

---

## 4. 附加发现：两腿入口语义分叉（本轮只诊断、只告警，不改语义）

原生腿 `src/llvm/codegen_typed.py:6423-6450` 的实现与 src 腿**不同**：

| | src 腿（`code_generator.py`） | 原生腿（`codegen_typed.py`） |
|---|---|---|
| 判「已启动入口」 | 旧口径：调用 **或裸引用** | 只认真正的调用（`ast.FunctionCall`）→ **无裸引用误判** |
| 带形参入口 | `arity>0` → **静默不调用** | 按 `argc/argv` **传参自动调用** |

两点结论：
1. 裸引用误判是 **src 腿独有**的缺陷（原生腿无此问题）——与本轮修复范围一致；
2. 「带形参入口」两腿**语义分叉**：原生腿把它当作「命令行参数入口」自动调用，src 腿静默跳过。本轮按「最小、不改语义」处理：src 腿**发编译期告警**说明分叉并要求显式调用，不擅自改成 argv 转发（`_run_src` 当前不向入口传参，改语义风险大于收益，登记为后续轮次候选）。

---

## 5. 本轮对任务书判据的回应

| 任务书要求 | 本轮结果 |
|---|---|
| 写 `examples/test_L172.light`（最小复现） | 已入库。§1 = 任务书描述形态（现即绿）；§2 = 真复现形态（**修复前 rc=0 且无输出**，修复后两行哨兵输出） |
| 诊断哪个阶段出问题 | **codegen 阶段**（`code_generator.generate` 的入口自动调用判定），非词法/非解析；定位到 `_module_invokes_entry` + `_node_calls_name` 的判定口径 |
| 为什么「按名调用被导入函数」会阻断 `主()` | **不会**。该描述不复现；真阻断条件是「模块级对入口名取裸引用」，机制见 §2.2 |
| 不改编译器源码（诊断阶段） | 遵守，诊断期只新增探针脚本，未改 `src/` |

---

## 6. 复现命令

```bash
cd G:/dswork/duan-light-merge/light-merge

# 修后（当前态）：rc=0，打印两行哨兵
.venv/Scripts/python.exe -m cli.light run examples/test_L172.light
# L172_主已执行
# --- L172 入口静默阻断 回归通过 ---

# 红前/绿后双向取证（含 arity 告警）
.venv/Scripts/python.exe _l172probe/verify_fix.py
# [1] 旧闸门 rc=0 / 无输出（复现） [2] 新闸门 rc=0 / 两行输出 [3] arity 用例 stderr 出告警
```

> 探针脚本清单与副作用说明见 `_l172probe/README.md`。
> ⚠️ 其它诊断脚本（`var_sweep.py` / `scan_missing_entry.py` / `fuzz2.py` 等）写的是本次复现中的**临时文件**（已全部清理），脚本可重跑复现；`mut_r52.py` / `pre_fix.py` 会写到 `lightharness/examples/`，跑完请删生成文件。

---

## 7. 本轮未覆盖（交任务3/4/路M）

1. **light-merge 全量 pytest 未取得可信汇总**：本机 xdist 并行（`-n auto --dist=loadscope`）收尾 `INTERNALERROR: KeyError: <WorkerController gwN>`，`--dist=loadfile --junitxml` 在 sessionfinish 抛 `OSError: cannot send`；两次运行的 passed 数 4790 / 6481 不可比 → 并行结果不能用于「零新增红」判定。可靠口径 = 串行 `-o addopts="" -p no:xdist` + `--junitxml` + `tools/ci/check_regression.py --baseline tests/ci_baseline_failures.txt`（约 45 min，本轮按用户指示中途停止）。
2. **lightharness 全量 423 例**未跑（属任务4）；本轮只做了 R52 四个用例冒烟（全绿）。
3. **两腿入口语义分叉**（带形参入口）只告警未对齐，登记待办。

