输出 "你好，光明！"。
设 甲 为 10。
设 乙 为 20。
输出 "甲 加 乙 = " + 转串(甲 加 乙)。
输出 "甲 乘 乙 = " + 转串(甲 乘 乙)。

如果 甲 小于 乙：
    输出 "甲 小于 乙"。
否则：
    输出 "甲 大于等于 乙"。

设 总和 为 0。
设 计数 为 1。
当 计数 小于等于 5：
    总和 等于 总和 加 计数。
    计数 等于 计数 加 1。
输出 "1到5的和 = " + 转串(总和)。

段落 加法 接收 a, b：
    返回 a 加 b。

输出 "加法(3, 4) = " + 转串(加法(3, 4))。