从 数学工具 导入 平方, 阶乘, 最大值。
从 字符串工具 导入 反转, 重复。

输出 "=== 多模块编译测试 ==="。

输出 "平方(5) = " + 转串(平方(5))。
输出 "阶乘(6) = " + 转串(阶乘(6))。
输出 "最大值(3, 8) = " + 转串(最大值(3, 8))。

输出 "反转(\"hello\") = " + 反转("hello")。
输出 "重复(\"ab\", 3) = " + 重复("ab", 3)。

输出 "=== 测试完成 ==="。
