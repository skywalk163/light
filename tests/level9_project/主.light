# Level 9 包管理测试 - 主入口
# 演示使用 package.toml 管理的项目

从 数学工具 导入 阶乘, 是素数, 累加。
从 字符串工具 导入 反转, 重复, 包含。

输出 "=== 光明包管理测试 ==="
输出 ""

# 数学工具测试
输出 "阶乘(8) = " + 转串(阶乘(8))
输出 "是素数(17) = " + 转串(是素数(17))
输出 "累加(100) = " + 转串(累加(100))
输出 ""

# 字符串工具测试
输出 "反转(\"光明\") = " + 反转("光明")
输出 "重复(\"-\", 20) = " + 重复("-", 20)
输出 "包含(\"hello world\", \"world\") = " + 转串(包含("hello world", "world"))
输出 ""

输出 "=== 测试完成 ==="
