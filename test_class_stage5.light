打印 "=== 阶段5：运算符重载测试 ==="。

类 数字：
    属性 值。
    
    段落 数字 接收 初始值：
        己值 为 初始值。
    
    段落 __add__ 接收 其他：
        返回 新建 数字(己值 加 其他.值)。
    
    段落 __sub__ 接收 其他：
        返回 新建 数字(己值 减 其他.值)。
    
    段落 __mul__ 接收 倍率：
        返回 新建 数字(己值 乘 倍率)。
    
    段落 __div__ 接收 分：
        返回 新建 数字(己值 除 分)。
    
    段落 显示：
        打印 "值: " 加 转文本(己值)。

打印 "--- 测试1：加法重载 ---"。
设 n1 为 新建 数字(10)。
设 n2 为 新建 数字(5)。
打印 "n1 = "。
n1.显示()。
打印 "n2 = "。
n2.显示()。
设 n3 为 n1 加 n2。
打印 "n1 加 n2 = "。
n3.显示()。

打印 "--- 测试2：减法重载 ---"。
设 n4 为 n1 减 n2。
打印 "n1 减 n2 = "。
n4.显示()。

打印 "--- 测试3：乘法重载 ---"。
设 n5 为 n1 乘 3。
打印 "n1 乘 3 = "。
n5.显示()。

打印 "--- 测试4：除法重载 ---"。
设 n6 为 n1 除 2。
打印 "n1 除 2 = "。
n6.显示()。

打印 "--- 测试5：基础类型算术运算不受影响 ---"。
设 a 为 100。
设 b 为 20。
打印 "100 加 20 = " 加 转文本(a 加 b)。
打印 "100 减 20 = " 加 转文本(a 减 b)。
打印 "100 乘 20 = " 加 转文本(a 乘 b)。
打印 "100 除 20 = " 加 转文本(a 除 b)。

打印 "--- 测试6：字符串拼接不受影响 ---"。
设 s1 为 "Hello, "。
设 s2 为 "World!"。
打印 s1 加 s2。

打印 "--- 测试7：浮点数运算不受影响 ---"。
设 f1 为 3.5。
设 f2 为 2.0。
打印 "3.5 加 2.0 = " 加 转文本(f1 加 f2)。
打印 "3.5 减 2.0 = " 加 转文本(f1 减 f2)。
打印 "3.5 乘 2.0 = " 加 转文本(f1 乘 f2)。
打印 "3.5 除 2.0 = " 加 转文本(f1 除 f2)。

打印 "--- 测试8：连续运算 ---"。
设 结果 为 n1 加 n2 乘 2。
打印 "n1 加 n2 乘 2 = "。
结果.显示()。

打印 "=== 阶段5测试完成 ==="。
