# 自测报告 — 任务 A5（异步 I/O 内核：把 selector 接到语言层 await）

- 分支 `task-A5`，基线 `1ff86237952fae03dba53dd9939386caf23eccc8`（未改基线）
- 环境：Windows 10 / Python 3.14 / PowerShell 5.1，落盘一律 `cmd /c ... > 文件 2>&1`
- 日期：2026-08-23

---

## 1. 裁决：selector 就绪 → `await` 怎么桥（首日必须先定的一件事）

### 1.1 路线：取 **(a) 轮询让步**（任务书默认路线），(b) 作为 B5 合入后的替换目标

`stdlib/流式.light` 的模块级 `协程收` 每一轮做三件事：

1. 直接 `s.recv(大小)`（socket 早在 `发送()` 里被置为非阻塞）；
2. 抛 want-read / want-write 时，用**新增的**`选择器.探测就绪(sel, s, 事件)` 做一次
   `sel.select(0)` 非阻塞探测；
3. **无论探测结果都让出一次**控制权：就绪 → `等待 异步睡眠(0)`（纯让步不睡）；
   未就绪 → `等待 异步睡眠(0.0005)`。

选它的理由：codegen 只映射 5 个 asyncio 名（`code_generator.py:249-253`），**没有
`add_reader` / `get_running_loop` / `Future`**，而 A5 不许碰 `src/`；纯 stdlib 内能完成，
且能过结构 + 时序判据。代价是粗（轮询开销 + 睡眠粒度抖动，Windows 定时器精度约 15ms），
所以时序判据只断「并发 < 0.8s / 串行 > 1.0s」这个**关系**。文件头已写明「这是过渡实现，
等 B5 一等函数值合入后换 (b) `loop.add_reader` 回调桥接」——(b) 要把回调**当函数值传**，
这正是 B5 在做的能力，按 §8 合并顺序 B5 先合。

**为什么不能直接复用 `等待就绪(…, 0)`**：那个段落循环第一句是 `剩余 为 截止 减去 now`，
`超时秒=0` 时 `剩余 <= 0` 当场 `返回 假`，**一次 `select` 都不会发生**（永远说「没就绪」）；
传一个小正数又是真阻塞，会把事件循环卡住那段时间。所以另立 `探测就绪`：
`select(0)` 是「查当前状态」，不是「等」。

**为什么必须每轮都让出一次**：若写成「探测说就绪就立刻重试、不让步」，遇到
「探测说可写、`recv` 仍抛 want-write」的自旋，就会把事件循环独占到 `超时读取` 为止。
`异步睡眠(0)` 只让步不睡，代价可忽略。

### 1.2 被语言层逼出来的两条定名裁决（已回写总纲 §4.1）

- **类体不接受 `异步 段落`**。实测（探针 CASE A）：
  ```
  类体内不支持的成员声明：'异步'（类体内可用：属性/私属性/性/设/构造/构/函数/段落/段/…）
  ```
  分发链 `src/parser_stmt.py:4320-4431` 没有 `异步` 分支，落到 `:4439` 硬报错；
  方法侧 codegen 也是无条件 `def`（`src/code_generator.py:2397`）。
  → 异步腿一律**模块级段落**，客户端作第一个参数传进来。
  → 类上暴露的入口是**同步方法** `流式对话2(消息列表)`：它不做 I/O，只返回 async 生成器。
  名字带 2 是因为同步版 `流式对话` 签名冻结、语言层又没有同名重载（任务书授权的
  「`流式对话2` 之类」）。**`异步 段落 流式对话` 这个带空格两段式本身一字未改**，
  只是落在模块级。
- **名字不许以 `异步` 开头**。实测（探针 CASE E）：`等待 异步收(客, 10)` 报
  「`异步` 是修饰符，不能当值使用……名字以 `异步` 开头但编译器不认识它，于是被切成
  `异步` + 余下部分」（`src/parser_expr.py:441-450`；白名单只有 5 个有真映射的名）。
  → 读腿定名 `协程收 / 协程读体 / 协程读分块 / 协程按行读取`，不是任务书草稿里的
  `异步收 / 异步 段落 read_body`。

---

## 2. 改了哪些文件（逐个 + 一句意图）

| 文件 | 改动意图 |
|---|---|
| `stdlib/选择器.light` | 新增 `探测就绪(sel, 对象, 事件文本)`：一次 `select(0)` 非阻塞探测，是事件循环里唯一可用的问法；连同头部能力清单与 `导出` 一起更新。**未动** `等待/等待就绪` 的阻塞语义 |
| `stdlib/流式.light` | 新增模块级异步读腿 `协程收 / 协程读体 / 协程读分块 / 协程按行读取`（全部 `导出`），与同步 `收 / read_body / 读分块 / 按行读取` **逐句同形**、只把读腿换成一个 `等待`；`从 选择器 导入` 增加 `探测就绪`；文件头补能力行与「过渡实现 + 诚实边界」说明。同步那一套一行未动 |
| `stdlib/大模型客户端.light` | 新增模块级 `异步 段落 流式对话 接收 客户端对象, 消息列表`（async 生成器，增量块字段与同步版一致）；类上新增同步入口 `流式对话2`；新增模块级同步桥 `取异步流式`（见下）；`从 流式 导入` 增加 `协程读体`；订正 `:174` 那句已失效的「光明无闭包」注释 |
| `tests/test_async_io_light.py`（新建） | A5 的全部异步用例：结构 / 让出控制权（心跳）/ 时序 / 异步与同步增量逐字相等 / 4xx 平价 / 异步 chunked + 按行读取 |
| `任务书/第五轮总纲.md` | 按 §4.1 的要求回写**实际定名**（并给原冻结示例加一句「以实际定名为准」）|

**没碰**：`src/`（零改动）、`stdlib/代理循环.light`、`stdlib/事件总线.light`、
`tests/test_async.py`（归 B5）、`tests/test_llm_client_light.py`、`tests/test_tls_light.py`
（一行未动，见 §4 的机读证明）、`stdlib/SSE.light`（同步解析器可直接复用，无需异步化）。

`git diff --stat` 实测：
```
 stdlib/大模型客户端.light |  87 ++++++++++++++++++++++++++-
 stdlib/流式.light         | 146 +++++++++++++++++++++++++++++++++++++++++++++-
 stdlib/选择器.light       |  20 +++++++
 3 files changed, 250 insertions(+), 3 deletions(-)
```

### 2.1 一处不得不加的绕法：模块级同步桥 `取异步流式`

类里直接写 `返回 流式对话(己, 消息列表)` **调不到模块级那个同名段落**——类体里的裸调用
被隐式改写成 `己.流式对话(...)`，实测报：

```
E   TypeError: 大模型客户端.流式对话() takes 2 positional arguments but 3 were given
```

即它调到了本类的同步生成器方法。类内没有「显式指名模块级」的写法，所以在模块级放一个
**不与任何方法同名**的桥 `取异步流式(客户端对象, 消息列表)`，类方法调它。已进移交清单。

---

## 3. 定向测试结果（汇总行直抄）

新用例（`PYTHONUTF8=1`，未设 `PYTHONIOENCODING`）：

```
....
[心跳] 0.5s 体延迟期间事件循环被唤醒 17 次
.
串行 1.03s / 并发 0.51s
.......
[LLM] 串行 1.08s / 并发 0.51s
..
14 passed, 18 warnings in 5.81s
```

定向回归（A5 家族 + 冻结用例 + 相邻的代理/工具/事件总线/异步语言层）：

```
python -m pytest tests/test_async_io_light.py tests/test_stream_light.py \
  tests/test_stream_async_light.py tests/test_sse_light.py \
  tests/test_llm_client_light.py tests/test_tls_light.py tests/test_deepseek_mock.py \
  tests/test_agent_loop_light.py tests/test_agent_tools_light.py \
  tests/test_tool_parallel_light.py tests/test_event_bus_light.py tests/test_async.py -q
→ 224 passed, 4 skipped, 47 warnings in 45.49s
```

门禁（本机，不跑全量）：

```
[假测试门禁] 扫描 .：命中 471 条违规
[假测试门禁] 无新增违规。
[假测试门禁] 相比基线已减少 3 条。…（bootstrap/bootstrap_level{3,4,5}.py:209:not-none）
[假测试门禁] 通过：存量冻结、无新增。
[自举率门禁] stdlib 文件维度自举率：18/70 = 25.71%
[自举率门禁] stdlib 同名 .py 影子数：53/70
[自举率门禁] stdlib 「引 Python」逃逸：0 处
[自举率门禁] 通过：破零守住、自举率未降、影子数未升、无转手造假。
```

- 基线 474 → 命中 471：**减少的 3 条不是 A5 造成的**，是基线里指向
  `bootstrap/bootstrap_level{3,4,5}.py:209` 的失效条目（那三个文件不在工作树里）。
  按「只许减」口径这不判红；**A5 不重建基线**（重建会动全仓键，属合并点的活）。
- 自举率分子分母都没动：本轮**没有新建 stdlib `.light`**，新增能力全落在既有三个文件里，
  所以 18/70 = 25.71% 持平（M15「只升不降」满足）。

---

## 4. 反跑证明（M11 三连 + 同步签名冻结的机读证明）

### 4.1 反跑-1：把异步读那一步改回同步阻塞 recv（打时序判据 + 让步判据）

改动（`stdlib/流式.light` 的 `协程收` 换成三行阻塞版）：

```
异步 段落 协程收 接收 客户端, 大小:
  设 s 为 客户端.套接字
  s.setblocking(真)
  返回 s.recv(大小)
```

**改坏后实测（红）**：

```
[心跳] 0.5s 体延迟期间事件循环被唤醒 0 次
F
串行 1.03s / 并发 1.00s
F......
[LLM] 串行 1.03s / 并发 1.01s
F.
E   assert 0 >= 5
E   AssertionError: 并发 1.00s 应 < 0.8s
E   assert 1.00391739999759 < 0.8
E   assert 1.0057741999989958 < 0.8
FAILED tests/test_async_io_light.py::Test让出控制权::test_异步读体不占用事件循环
FAILED tests/test_async_io_light.py::Test时序判据::test_串行与并发的关系
FAILED tests/test_async_io_light.py::TestLLM异步流式::test_两路LLM流式并发比串行快
3 failed, 11 passed, 19 warnings in 7.17s
```

**恢复后实测（绿）**：`14 passed`，心跳 17 次，串行 1.03s / 并发 0.51s（§3 那一份）。

判据没有虚位：并发耗时从 0.51s 退化到 1.00s（= 串行），心跳从 17 次退化到 0 次
（读腿把事件循环整段占住了）。

### 4.2 反跑-2：让类上的异步入口返回同步生成器（打结构判据）

改动：`流式对话2` 的 `返回 取异步流式(己, 消息列表)` → `返回 己.流式对话(消息列表)`。

**改坏后实测（红）**：

```
E   assert False is True
E    +  where False = <function isasyncgen at ...>(<generator object 大模型客户端.流式对话 at ...>)
E   AttributeError: 'generator' object has no attribute 'aclose'. Did you mean: 'close'?
E   TypeError: 'async for' requires an object with __aiter__ method, got generator
E   AssertionError: assert 'TypeError' == 'HTTP错误'
8 failed, 6 passed, 18 warnings in 5.34s
```

即「结构判据」不是拿字符串糊的：把 async 生成器换成同步生成器，`inspect.isasyncgen`、
`async for`、`aclose`、乃至 4xx 的异常类型判据一起红。恢复后 14 passed。

### 4.3 同步签名冻结的机读证明（两条，一条静态一条动态）

1. **静态**：`tests/test_llm_client_light.py` 与 `tests/test_tls_light.py`
   **一行都没动**（`git status` 只有 3 个 stdlib 文件 M + 1 个新测试文件 ??），仍全绿
   （回归里 26 passed / 3 skipped 的那一组）。
2. **动态**：`test_同步版流式对话仍是同步生成器_签名冻结的机读证明` 直接问对象：
   `inspect.isgeneratorfunction(大模型客户端.流式对话) is True`，且
   `isasyncgenfunction` / `iscoroutinefunction` 都必须是 `False`。
3. **行为等价**：`test_异步增量与同步增量逐字相等` 对 3 组 SSE 分帧（文本 / 工具 /
   真实 DeepSeek 的显式 null）断言 `异步块列表 == 同步块列表`——字段名、顺序、
   `[DONE]` 早停、tool_calls 按 index 累积、`id` 兜底，全部逐字相等。

---

## 5. 移交清单（光明写不出什么 → 绕法 → 期望能力）

| # | 光明写不出什么 | 现在的绕法 | 期望的语言能力 | 交给谁 |
|---|---|---|---|---|
| 1 | **类体不能有 `异步 段落`**（`parser_stmt.py:4439` 报「类体内不支持的成员声明」；方法 codegen `code_generator.py:2397` 无条件 `def`） | 异步腿全部下沉成模块级段落、客户端当第一参；类上留同步入口 `流式对话2` 返回 async 生成器 | 类方法支持 `异步` 修饰符 → 产出 `async def` 方法。这是本轮最贵的一条：它把「异步 = 一等公民」压成了「异步 = 模块级函数」 | **B5** |
| 2 | **类内无法指名模块级同名段落**：裸调用被隐式改写成 `己.同名(...)`（实测 `takes 2 positional arguments but 3 were given`） | 模块级加一个不同名的桥 `取异步流式` | 一个显式限定写法（如 `模块.名` 或 `全局 名`），或至少在改写时对「模块级也有同名」给出编译期告警 | **B5** |
| 3 | **同步方法里写 `等待` 编译期不拦**：`_require_async_context` 只看 `_in_function`（`code_generator.py:2183`），而方法体里它也是 True → 产出 `def` 里的裸 `await`，错误延后到 Python 语法层 | 不在方法里写 `等待` | 方法体的异步上下文判定要看「所在段落是否 async」 | **B5** |
| 4 | **`等待` 后面吃得太贪**：`等待 f() + 1` 编成 `await (f() + 1)`（`parser_expr.py:506`） | 需要限定就自己加括号 | `等待` 只吃一个 primary/postfix 表达式 | **B5** |
| 5 | **名字不许以 `异步` 开头**（不在 lexer 白名单就被切词，`parser_expr.py:441-450`） | 读腿改名 `协程*` | 要么允许自定义 `异步*` 标识符，要么在报错里指出「这是切词问题」（现在的报错文案已经不错，但仍是硬限制） | **B5** |
| 6 | **没有 `to_thread` 等价物**：`发送()`（connect / TLS 握手 / sendall / 读响应头）是阻塞的，在 loop 里会阻塞那一段。对 mock 是毫秒级，对真实 https 是一次完整握手 | await 点只放在**流式体读取**上；连接段照冻结的同步 `发送()` 走 | 「丢线程再 await」的语言原语（总纲 §4.3 末尾那条契约点名要它）。有了它，异步 connect / 异步握手才可能 | **B5**（原语）→ **C5**（编排消费） |
| 7 | **没有 `创建未来` / `注册读回调` 之类 codegen 映射** → (b) 路线（`add_reader` 回调桥）现在写不出来 | 走 (a) 轮询让步 | 只要 B5 的**一等函数值**落地，`导入 asyncio` + `get_running_loop().add_reader(fd, 回调)` 就能写，**不需要**为此新造关键字（M11 已删 `创建事件循环`，本轮也没把它加回来） | **B5** 合入后 A5/C5 任一路替换 |
| 8 | **异步文件原语 `异步读取文件/异步写入文件/异步追加文件` 有名无实**：`code_generator.py:255-263` 白名单里有名字，调用点 `:2602-2612` 直接抛 CodeGenError「暂无实现」 | 本轮不做（任务书列为「若时间允许」的可选项），也**没有**为凑功能改 `src/` 或别的模块 | 要么真映射到线程池版实现，要么把这三个名字删掉——留着「有名字、一调就报错」是第二轮三禁里的「只有壳的东西」 | **B5** 裁决 |
| 9 | **`选择器.light:21-22` 的注释「不用 位与/位或（该方言里不被当作二元运算符）」在 B5 位运算合入后会变假** | 本轮 B5 未合入，注释此刻**仍为真**，故不改 | B5 位运算合入后由 **A5 文件的所有者**（即本路，或合并点代持）把这两行订正为「可用位运算，但这里保留相加写法因为 EVENT_READ/EVENT_WRITE 位不重叠」。B5 不许越界改它 | **合并点代持**（总纲 §2.2 注释订正第三条） |

---

## 6. 已知未完成 / 未实测

- **POSIX 分支未实测**：全部结论来自 Windows 10 + Python 3.14。跨平台上有两处与平台强相关：
  ① `探测就绪` 走 `selectors.DefaultSelector`（Windows 是 `SelectSelector`，Linux/BSD 是
  `EpollSelector`/`KqueueSelector`），`select(0)` 的非阻塞语义三者一致，但**没在 POSIX 上跑过**；
  ② `异步睡眠(0.0005)` 的实际粒度在 Windows 上约 15ms、在 POSIX 上会细得多 →
  **POSIX 上轮询次数会显著变多（CPU 开销更大），时序判据只会更容易过、不会更难过**。
  CI（FreeBSD）跑完才算实测。
- **TLS 路径上的异步读腿未实测**：`协程收` 的 want-read / want-write 分流逻辑与同步 `收`
  同形（都走 `是否愿等读/是否愿等写`），但新用例里的 mock server 都是明文 HTTP；
  TLS 侧的覆盖仍只有同步版的 `tests/test_tls_light.py`。**这是本轮最大的一块未实测面**，
  补法是给 `test_async_io_light.py` 加一个 TLS mock server 走 `协程读体`——没做进来的原因是
  它需要现造证书链、与 `test_tls_light.py` 的 fixture 重叠，权衡后优先保住三连判据。
- **`协程按行读取` 只被一条用例覆盖**（跨 chunk 的 UTF-8），没有 SSE-over-lines 的端到端用例；
  `大模型客户端` 的异步腿直接用 `协程读体` + 同步 `SSE解析器`，没走它。
- **异步 connect / 异步 sendall 没做**（移交 #6），所以「N 路并发」的连接建立段仍是串行阻塞的
  毫秒级开销；真实 https 下这一段会变成 N 次串行握手。
- **未跑全量**（按总纲 §7「各路不许跑全量」）：CI（FreeBSD）与跨平台闸门留给合并点。

---

## 7. skip / xfail 清单与掩盖分析

新建的 `tests/test_async_io_light.py`：**0 skip、0 xfail**（14 条全跑）。

定向回归里的 4 条 skip：

- `tests/test_llm_client_light.py::TestRealAPI` 3 条（`未设置 DEEPSEEK_API_KEY`）——
  **既有 skip，不是 A5 新增**。掩盖分析沿用该文件 `:265-271` 的结论：只掩盖「真实
  DeepSeek 认不认我们发的 tools / 分帧」，功能本身由 `tests/test_deepseek_mock.py` 守。
  ⚠️ **A5 新增的异步腿在真 key 下同样零覆盖**：`test_llm_client_light.py` 归 A5 但
  本轮一行未动（那是冻结判据），所以在线轨里没有异步版对照。要补就得在
  `test_async_io_light.py` 里另立一个带 `skipif` 的在线用例——本轮没加，因为总纲
  §4.4 已把「跑 mock 即算达成」定为判据，加一条无 key 必 skip 的用例只会多一条掩盖。
  **记账在此，D5 做在线轨时应一并覆盖异步腿。**
- 第 4 条：`tests/test_agent_tools_light.py:704`（`$()/反引号是 POSIX sh 语义，Windows cmd /c 无此分支`）——
  **既有的平台条件 skip，不是 A5 新增**，与本轮改动无关；在 CI（FreeBSD）上它会真跑。
  掩盖的是 Windows 上「命令替换注入」那一支的行为，归 B4 的沙箱账，A5 不动。

---

## 8. 与 A4 线程版数字的对照（说明这不是「把线程换了个名字」）

- 本轮（**0.5s 延迟**档，语言层 await + selector）：串行 1.03s / 并发 0.51s，比值 **2.02**；
  LLM 腿串行 1.08s / 并发 0.51s，比值 **2.12**。
- A4（**0.30s sleep** 档，threading + 手写分批 join）：并行 0.303s / 串行 0.904s，比值 2.98
  （`自测报告_任务A4.md:62-66`）。

**两组不是同一档实验**（延迟档位不同、并发路数不同），只能当「量级参照」，
不许把 0.303/0.904 当本轮的期望值。真正区分二者的不是这两个比值，而是**心跳判据**：
本轮的并发是在**一个线程、一个事件循环**里靠 `等待` 让步做到的（心跳 17 次证明
loop 一直活着），A4 的并发是靠**另开线程**做到的。反跑-1 把让步拆掉后，
比值立刻退化到 1.0（1.03s / 1.00s）。
