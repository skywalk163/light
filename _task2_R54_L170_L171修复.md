# 第54轮 任务2：L-170 + L-171 编译器修复

> 日期：2026-09-17 ｜ 修改文件：`light-merge/src/parser_stmt.py`
> 配套：任务1 诊断（`_task1_R54_L170_L171诊断.md`）+ 复现用例 `examples/test_L170.light`

---

## 1. 修复结论

| Bug | 是否需改编译器 | 改动 |
|---|---|---|
| **L-170** | 是（真 bug） | `parser_stmt.py` 两处：动词调用分支守卫 + 赋值目标名接收动词 |
| **L-171** | 否（当前 HEAD 已修） | 无需改动；复核见 §4 |

---

## 2. L-170 修复（2 处改动，同一文件）

### 2.1 改动 A：动词调用分支——同时覆盖 KEYWORD 与 IDENTIFIER

`_parse_statement_inner` 原代码把 VERB_ARITY 动词在语句首无条件当动词调用：

```python
# 修复前
if tok.type == TokenType.KEYWORD and tok.value in VERB_ARITY:
    return self._parse_expr_stmt()
```

改为：当动词名后紧跟「下标 `[` / 成员 `.` / 之 / 的 / 为 / 等于 / `=`」时改走赋值；正常动词调用（后跟 `(`）仍走表达式语句。**关键：token 类型同时接受 KEYWORD 与 IDENTIFIER**——因为 deterministic lexer 会把「已被声明过的动词名」重分类为 IDENTIFIER，真实代码里 `设 映射 为 {}` 之后的 `映射` 是 IDENTIFIER。

```python
# 修复后
if (tok.type in (TokenType.KEYWORD, TokenType.IDENTIFIER)
        and tok.value in VERB_ARITY):
    _nxt = self._peek(1)
    if _nxt is not None and (
            _nxt.type == TokenType.LBRACKET
            or _nxt.type == TokenType.DOT
            or (_nxt.type == TokenType.KEYWORD
                 and _nxt.value in ('之', '的', '为', '等于'))
            or _nxt.type == TokenType.EQUALS):
        return self._parse_assignment_stmt()
    return self._parse_expr_stmt()
```

### 2.2 改动 B：赋值目标名接收 VERB_ARITY 动词

`_parse_assignment_stmt` 原本只 `consume(IDENTIFIER)` 作目标名，遇到 KEYWORD 动词名会报错。改为 KEYWORD+VERB_ARITY 也能吃掉：

```python
# 修复后
_tok0 = self._current()
if _tok0.type == TokenType.IDENTIFIER:
    name_tok = self._consume(TokenType.IDENTIFIER)
elif _tok0.type == TokenType.KEYWORD and _tok0.value in VERB_ARITY:
    name_tok = self._consume(TokenType.KEYWORD)
else:
    self._error(
        f"赋值目标必须是标识符或可作变量名的动词名，但得到「{_tok0.value}」。",
        _tok0.line, _tok0.col, _tok0.value
    )
name = name_tok.value
```

之后既有的下标 / `之`成员 / `.`成员 / `的`成员解析逻辑完全复用，无需改动。

---

## 3. 修复验证（红前→绿后，运行时断言）

> 全程 `python -B` 强制走源码，规避 Z620 `.pyc` 陈旧。

### 3.1 编译产物对比（修复后，`cli.light compile`）

| 形态 | 修复前产物 | 修复后产物 |
|---|---|---|
| `映射[k] 为 99` | `(映射[k] == 99)` ❌ | `映射[k] = 99` ✅ |
| `映射之x 为 1` | `(_light_attr_get(映射,'x') == 1)` ❌ | `_light_attr_set(映射, 'x', 1)` ✅ |
| `映射.项 为 1` | `(_light_attr_get(映射,'项') == 1)` ❌ | `_light_attr_set(映射, '项', 1)` ✅ |
| `筛选[k2] 为 2` | `(筛选[k2] == 2)` ❌ | `筛选[k2] = 2` ✅ |
| `映射(数据, 函数)`（动词调用） | `map(data, 函数)` ✅ | `map(data, 函数)` ✅（未误伤） |

### 3.2 运行时断言（赋值真生效）

`examples/test_L170.light` 用 `如果 实际 != 期望: 抛出` 断言，运行结果：

```
L170-OK        (rc=0)
```

覆盖场景：下标赋值 / 之成员赋值 / .成员赋值 / 另一动词 筛选 同形态，全部断言通过。

### 3.3 全 VERB_ARITY 形态扫描

对「裸动词名」与「声明后重分类（真实用法）」两种 token 类型，逐一验证 `映射/筛选/严格/构造/保护/输出/步/替换` 等核心动词作变量名的下标/成员赋值，均从「静默变比较」转为「正确赋值」，无残留。

### 3.4 回归冒烟（不改源）

- `lightharness/examples/test_字典.light`：`设 映射["c"] 为 3`（走 `设` 分支，动词名作 dict 变量）rc=0 通过。
- `lightharness/examples/test_R48_subagent深化.light`：含 `映射(...)` 动词调用 rc=0 通过。
- 扫描 light-merge / lightharness 全部 examples，无其它文件在语句首用「动词名[下标]」形态，除本用例外无受影响点。

---

## 4. L-171 复核（无需改）

- 任务书描述 `写 "x=" + 变量` 静默丢实参，当前 HEAD 实测输出 `x=y` 正确（编译产物 `_light_builtin.写入输出(str(("x=" + 变量)))`）。
- 根因已随 L-055 的 `code_generator.py::_try_merge_output_concat` 形态① BinaryOp 合并分支（约 L977）修复，该分支在当前 HEAD 完整有效。
- 失败的 `写 变量 + "=end"`（变量=9）等是 `整数+字符串` 类型错误，非 `写` 丢参，按语言设计应报错。
- **结论：L-171 当前 HEAD 已修复，本轮不引入改动。**

---

## 5. 收口与风险

- 改动仅 1 个文件 `src/parser_stmt.py`，纯解析层分流，未触 codegen / lexer / 标准库。
- 风险点已度量：仅影响「VERB_ARITY 动词名在语句首 + 后跟下标/成员/赋值符」的形态，修正方向唯一（改走赋值），对正常动词调用（后跟 `(`）零影响。
- **提交注意**：`examples/test_L170.light` 被 `.gitignore:311 test_*.light` 忽略，提交须 `git add -f examples/test_L170.light`。
- **待办（任务3/4/路M，本轮未做）**：light-merge 全量 pytest 与 lightharness 全量 examples 回归（用户指示不跑全量，留给后续）；跨平台 0.82 复测。
- **环境坑提醒**：本机改编译器后务必 `find . -name __pycache__ -exec rm -rf {} +`，否则 Z620 时钟漂移下 `.pyc` 不失效会表现成诡异旧行为。

---

## 6. 交付物

- `light-merge/src/parser_stmt.py`（修改：动词调用分支守卫 + 赋值目标名）
- `light-merge/examples/test_L170.light`（复现 + 赋值生效断言）
- `light-merge/_task1_R54_L170_L171诊断.md`（诊断）
- `light-merge/_task2_R54_L170_L171修复.md`（本报告）
