# 自测报告 · 任务 A3（harness 主干闭环与工具协议）

分支 `task-A3`，基线 `5e5943ca`（含 pty/EAGAIN 修复；tag `pre-merge-round3` = `115e5e24`）。
本机：Windows 10 / PowerShell 5.1 / NTFS G: 卷。**未跑全量门禁**，只跑本任务定向用例。

---

## 一、改了哪些文件

已提交（`616723e5`）：

- `stdlib/大模型客户端.light` —— 新增 `工具声明`/`工具选择`/`额外参数` 属性与 `配置工具`/`配置参数`，
  请求体收敛到唯一入口 `组装载荷(消息列表, 流式)`。**这是本轮的协议断点修复**：改动前请求体里
  根本没有 `tools` 字段，工具注册表只活在本地，真实模型永远不会返回 `finish_reason=="tool_calls"`。
- `stdlib/事件总线.light` —— 「纯光明实现」魔法移进首两行（此前它在第 3 行，`_light_import_hook`
  只看前两行，所以 `.light` 一直被 `.py` 影子挡着、从未被加载）；新增 `校验处理器签名`
  （订阅时就用 `inspect.signature(...).bind(事件名, 空)` 卡两参约定）；监听者异常隔离后
  额外写 stderr（此前静默进 `发布结果.错误表`，没人看）。
- `tests/test_deepseek_mock.py`（新）—— 会**解析并断言请求体**的 mock DeepSeek。

本次待提交：

- `stdlib/大模型客户端.light` —— 新增 `取文本(字典, 键)` 单点：**键缺席或值为 `null` 一律当空串**。
  `组装增量` 的 content/role、`累积工具` 的 name/arguments 全部改走它。
  这是**在线实测逼出来的真 bug**（详见第二节）：真实 DeepSeek 发的是 `"content": null`
  ——键在、值是 null，而原实现只判「键在不在」，拿 `None` 去拼字符串直接
  `TypeError: can only concatenate str (not "NoneType") to str`，流式对话**一帧都撑不过**。
- `stdlib/代理循环.light` ——
  - `注册工具` 即时把声明推给客户端（结构上消灭「注册了但没告诉模型」这个状态）；
  - `校验工具名`：非 ASCII / 超长 / 空名当场抛 `工具注册错误`，报错里给出可照抄的正确写法；
  - `退避秒`：线性 → **真指数 + 封顶**（文件头与旧自测报告都自称指数，按「文档腐烂改写优先」口径改代码而非改注释）；睡眠收敛到 `己.睡眠` 单点；
  - `词元预算` + `会话.按预算截断`：按 token 预算截断，**以「assistant(tool_calls) + 其后所有 tool 消息」为原子组**，绝不拆对；
  - `取消令牌` 接进主循环（轮次开始前、工具分发前两个检查点，发 `已取消` 事件）；
  - `压缩历史` 从「追加 assistant 之后」移到「工具结果也进历史之后」——否则 `tool_calls` 刚追加就可能被截掉，配对被拆开；
  - `设置系统提示` → `配置系统提示`（原因见移交清单缺陷 B）。
- `tests/test_agent_loop_light.py`、`tests/test_llm_client_light.py` —— 见第二节。
- `tests/test_harness_e2e.py`（新）—— M5 判绿点。
- `examples/harness/主程序.light`（新）—— 带 `write_file`/`read_file`/`list_dir` 三件套的最小 agent，全部经 `stdlib/路径护栏.light` 核准。
- `examples/harness/M2_流式对话.light` —— 注册名 `执行命令` → `run_shell`，schema 键 `命令` → `command`。
  它原本注册中文工具名；在 A3-1 之前 `tools` 根本没发出去，所以这个错永远暴露不了。现在 `校验工具名` 会当场拦。

---

## 二、定向测试结果

命令（`PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8`，否则 Windows 上中文输出会伪装成失败）：

- `tests/test_agent_loop_light.py` → **16 passed**
- `tests/test_llm_client_light.py` → **12 passed**（含 3 条在线；无 key 时为 9 passed / 3 skipped）
- `tests/test_event_bus_light.py` → **13 passed**
- `tests/test_deepseek_mock.py` → **9 passed**
- `tests/test_harness_e2e.py` → **4 passed**
- 五个文件一起跑（**带 key**）：**54 passed, 0 skipped, 24.26s**
- 五个文件一起跑（**无 key**，即 CI 上的形态）：**51 passed, 3 skipped**

在线一条腿已经真跑过：端点 `https://api.deepseek.com`、模型 `deepseek-v4-flash`（key 取自
`积木库/.env` 的 `OPENAI_API_KEY`，OpenAI 兼容命名）。实录落档
`第三轮留档/M4_在线往返实录.txt`，key 只留后 4 位。真实模型确实按我们发的 tools
返回了 `finish_reason=tool_calls` 与 `get_weather({"city": "北京"})`。

另外 `python cli/light.py run examples/harness/M2_流式对话.light` 仍能编译并友好退出
（打印「请先设置环境变量 DEEPSEEK_API_KEY」），证明改名没把例子改坏。

### 判据有效性（反向控制，不是自我表扬）

1. **杀掉一个假绿**：`TestMaxRounds` 原本断言 `len(events) <= 3` —— 事件一条都没发时同样成立。
   已换成 `events == [0, 1, 2]`。同类「上界断言」在本轮新写的用例里一律改为等值断言。
2. **mock 会因为「少了 tools」而红**：`Test判据本身有效::test_需要工具但未注册时服务端回400且说明原因`
   断言 `HTTP错误.状态 == 400` 且正文含「没有 tools 字段」。判据自己有判据。
3. **e2e 变异探针（实测，非推导）**：把 `主程序.light` 的 `己.护栏.写入(...)` 那一行删掉、
   保留返回的「已写入 N 个字符」文案，重跑 `tests/test_harness_e2e.py` →
   **2 failed, 2 passed**，红在 `产出.exists()` 上。这条实测同时证明了两件事：
   磁盘断言真的在判；而**只断言 stdout/tool 消息的写法会全绿放过**这个变异
   （因为 `代理循环.执行工具` 会把工具异常吞成 tool 消息，「跑完了」并不等于「干成了」）。
   探针已还原，当前工作区是干净的实现。
4. **护栏真的接上了**：`test_越界路径被拦下且沙箱外没有留下文件` 让模型写 `../逃逸.txt`，
   断言沙箱**兄弟目录**里没有文件、且 tool 消息里含「越界」。护栏没接进工具的话这条必红。
5. **退避是真指数**：`[退避秒(1..7)] == [0.2,0.4,0.8,1.6,3.2,6.4,8.0]`，另有一条真连
   `127.0.0.1:1` 的重试用例，用 `agent.睡眠 = lambda 秒: 睡了.append(秒)` 断言 `睡了 == [0.2,0.4,0.8]`。
6. **预算截断不拆对**：`for 预算 in range(40, 600, 20)` 扫一遍，断言任何预算下都不出现
   孤儿 `tool` 消息、也不出现被截断的 `tool_calls`/`tool` 配对（两个方向都查）。
7. **在线一跑就抓到真 bug（本轮最有价值的一条）**：拿到 key 后第一次真连，
   `test_stream_chat` 与 `test_真实模型接受tools声明并返回tool_calls` 双双崩在
   `TypeError: can only concatenate str (not "NoneType") to str`。
   根因：真实 DeepSeek 发的是 **显式 `null`**——
   `{"role":"assistant","content":null}`、工具帧里 `{"content":null,"tool_calls":[...]}`、
   续帧 `function.name` 有时带且为 `null`。而 `组装增量` 只判「键在不在」，
   键在值为 null 时把 `None` 拿去拼字符串。
   **为什么离线全绿**：我写的 mock 刻意「缺席的键就不出现（不许填 null/空串）」——
   那条自以为是的保真假设恰好绕开了真实形状。这就是「在线腿不可省」的实证：
   它不是锦上添花，它是唯一能发现「我对协议的想象跟现实不一样」的判据。
   处置按 TDD：先加 `Test显式null的分帧`（新 mock 模式 `stream_null` 复刻真实形状）
   → **实测 3 failed，报同一个 TypeError** → 再加 `取文本` 单点修 → 全绿。
   报错形状与在线现场逐字一致，不是「猜一个 null 试试」。

---

## 三、移交清单

### 交 C3（语言层，两个**静默错编**，都有现场）

**缺陷 A：字符串字面量里的成对花括号被无条件当 f-string 编译。**
`"^[a-zA-Z0-9_-]{1,64}$"` 生成出 `f"^[a-zA-Z0-9_-]{1(64)}$"`，即 `{1,64}` 被当成表达式
`1(64)`，运行时报 `TypeError: 'int' object is not callable` ——**报错点在模块导入，
与真实原因（一个正则字面量）差得非常远**。现场：`stdlib/代理循环.light` 的工具名正则。
规避方式：正则去掉量词、长度另判；并且把所有报错文案里的花括号都改掉。
建议修法：只有源码里真出现插值语法时才走 f-string；或提供显式的插值标记。

**缺陷 B：语句位置的属性链调用，若方法名以单字关键字 `设` 开头，会被词法器切开。**
`己.客户端.设置工具(...)` 生成出 `self.客户端.设(置工具(...))`。已收敛的边界（两轮探针实测）：
- `己.设置值(1)`、`对象.设置值(2)`、`返回 己.内.设置值(2)` —— **都正常**；
- 只有**语句位置的属性链**会坏；其中一种写法还会报到**下一行**的缩进错误上，归因极难。
规避方式：把 `设置工具/设置参数/设置模型参数/设置系统提示` 全部改名为 `配置*`。
**没有留旧名别名**（全仓无旧名调用点）。

### 交 B3

`examples/harness/主程序.light` 现在自带 `write_file`/`read_file`/`list_dir`。
`stdlib/代理工具集.light` 合入后，主程序里 `沙箱工具集` 那个类连同三行 `注册工具`
应**整体替换**为 `代理工具集.注册全部(代理, 沙箱根, 选项)`，不要两套并存。
本轮**刻意没有写 try-import 回退**——那会造出「B3 没合入时悄悄降级」的分叉。
`tests/test_harness_e2e.py` 的断言是按行为写的（磁盘副作用 + 越界被拦），换成真工具集后**不需要改**。
工具名与 schema 键请沿用已冻结的 ASCII 契约（`path`/`content`）。

### 交 D3

- `docs/known_issues.md` §12.2（`:320`）关于「`cli/lightc.py <文件>` 对任何输入必崩
  （`SemanticAnalyzer()` 少传 module 参数）」的条目仍然有效——代码没修，只是文档/示例里
  的用法都改成了 `cli/light.py run`。`examples/harness/M2_流式对话.light` 的使用说明已是新写法。
- **一条旧债经核查已不存在，特此更正**：`tests/e2e/test_llvm_pipeline.py` 的
  `assert returncode in [0, 1]` 已被改成 `assertEqual(result.returncode, 0)`
  （`:72-77`，注释写明「不再接受 1」）。全仓 grep `returncode in` 零命中。
  第三轮总纲若还把它列为待办，应删掉。
- `cli/light.py` 的 `run` 子命令**不透传被测程序的退出码**：`主程序.light` 里
  `sys.exit(2)` 实测得到 rc=1。所以 e2e 只把 rc==0 当「跑到尽头」的弱信号，
  真判据全部落在磁盘与请求体上。若门禁依赖 rc 区分错误类型，这里得先修。

### 交下一轮（工程量独立成块）

**异步 agent loop 需要先把 `stdlib/流式.light` 换成非阻塞 socket + selector。**
A3-4 **刻意没有**把退避改成 `等待 异步睡眠`：`流式.light` 用阻塞 socket，单轮请求会在
网络 IO 上占住线程十几秒；只把 0.2 秒的退避异步化，而 IO 那几十秒照样堵死事件循环，
是自欺——「看着异步化了，并发度一点没变」。同理 `examples/harness/主程序.light`
**没有**写成 `异步 运行 主()`（语言已支持，M2 例子里就有；这里用不上）。
真要异步得从 `流式.light` 动手，那是独立一块工程。
**主线裁决（2026-08-22）：接受这个判断，真异步作为下一轮独立任务。**

---

## 四、已知未完成与未实测项

- **A3-5 的并行工具执行没做**（只做了取消令牌）。理由：单 agent harness 的瓶颈在模型
  往返，不在工具执行；而并行 + 有序回写会引入「tool 消息顺序必须与 tool_calls 顺序一致」
  这条新约束，收益最小、风险最高。**如实记为未做，而不是搭一个只有壳的并行版本。**
  **主线裁决（2026-08-22）：接受未做，排进下一轮任务书。**
- **在线一条腿已经补上并通过**（本节原先写的「本轮没有实测过」已作废）：
  端点 `api.deepseek.com`、模型 `deepseek-v4-flash`，实录见 `第三轮留档/M4_在线往返实录.txt`。
  仍未覆盖的是：**其它 OpenAI 兼容端点**（如 AMD 那套 `rc-` 开头的 key）的分帧差异，
  以及 DeepSeek 官方 `deepseek-chat` 模型（本次用的是 `deepseek-v4-flash`）。
- 未跑全量门禁（任务书禁止），因此本轮**没有**新的三平台基线数字。
- `事件总线.light` 从「被 `.py` 影子挡着」变成「真的被加载」是一次**行为变更**：
  本轮只验证了 A3 范围内的 13 条事件总线用例，其它使用方（如果有）未回归。合入时值得关注。
- Linux/FreeBSD 上未跑本轮新增用例。`test_harness_e2e.py` 用 `sys.executable` + `os.path.join`，
  没有写死路径分隔符；`路径护栏` 的 normcase 分支只在 win32 生效，理论上可移植，但**未实测**。

---

## 五、skip 清单与掩盖分析

**本机带 key 跑时 skip 数为 0（54 passed）**；CI 上无 key 时是 3 条 skip，
全部集中在 `tests/test_llm_client_light.py::TestRealAPI`，条件
`@pytest.mark.skipif(not os.getenv("DEEPSEEK_API_KEY"))`。三条各自掩盖什么、兜底是谁：

- `test_stream_chat` —— 掩盖：真实 SSE 分帧与我们的解析是否一致。
  兜底：`TestStreaming` 4 条 + **`Test显式null的分帧` 3 条**用录制字节流覆盖
  （含 arguments 切在 JSON 中间、`[DONE]` 哨兵、以及显式 `null` 的真实形状）。
  ⚠️ 这条 skip 的掩盖**曾经真的兑现过**：它掩盖的 null 分帧 bug 让流式对话在真实
  端点上一帧都撑不过，而离线 6 条全绿。现在那个形状已经被钉进离线用例，
  但这不改变结论——**离线 mock 的保真度只等于写它的人对协议的想象**。
- `test_nonstream_chat` —— 掩盖：非流式响应形状。兜底：`TestNonStreaming` 1 条。
  仍未覆盖：非流式且带 tool_calls 时 `message.content` 为 `null`（`对话` 原样返回
  message 字典，调用方拿去拼字符串会踩同一个坑）。**已知，本轮未修**。
- `test_真实模型接受tools声明并返回tool_calls` —— 掩盖：**只掩盖「真实模型是否接受我们的
  tools 声明」这一件事**，不掩盖工具调用功能本身。功能侧由 `tests/test_deepseek_mock.py`
  的 9 条守着，**那个文件里没有任何 skip**，任何机器上都会跑。
  本轮已用真 key 跑通，实录见 `第三轮留档/M4_在线往返实录.txt`。

这条区分是本轮的教训所在：第二轮把「无 key 则 skip」当成了功能验证的替代品，
「请求体里没有 tools」这个协议断点因此藏了整整一轮，而离线测试一路全绿。
现在的分工是：**离线 mock 判功能（不许 skip），在线用例判「真实服务端认不认我们、
以及真实响应长什么样」——后者一旦跑出新形状，必须反哺成离线用例。**
