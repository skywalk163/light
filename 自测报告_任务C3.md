# 自测报告 — 任务 C3｜语言层静默降级堵洞与泛型发射

> 分支：`task-C3`　工作树：`wt-C3`　基线：`main`(5e5943ca)　运行环境：Windows 10 / Python 3.14.7

---

## 一、改了哪些文件

### 新建
- **`docs/原生腿能力边界.md`**（C3-3）：原生腿支持 / 不支持语句与表达式的查表，逐项标注证据行号；明确「不实现就报错，零静默降级」。给 A3/B3 与后来者的地图。
- **`tests/unit/test_native_leg_capability.py`**（C3-3）：**源码级守单点**——读 `codegen_typed.py` 的 `_gen_statement`/`_gen_expression` 分派链与文档清单比对，加新节点忘了更文档就打红。
- **`tests/test_llvm_c3_expr.py`**（C3-1/C3-2/C3-4，22 条）：DictLiteral / StringInterpolation / RangeExpr / `整除` 真跑断言 stdout；未支持表达式参数化报错（文案含真类型名）；`pass` 编空操作；未知段落调用列出候选；切片指路；二元运算符链尾静态守位。
- **`tests/test_generics_c3.py`**（C3-5，5 条）：泛型类/段落形参与返回注解发射 + `exec` 真跑 + `typing.get_type_hints` 解析出 `T`（**非字符串断言**）。
- **`tests/test_c3_parser_codegen.py`**（C3-6/C3-7，7 条）：`对于 … 在 …` 指路文案（两种形态参数化）、`遍历` 仍可用、三个 `异步*文件` 编译期报错指路、同步原语不受影响。

### 修改
- **`src/llvm/codegen_typed.py`**（C3-1/C3-2，本轮核心）：
  - `_gen_expression` 链尾 `return 0` → `_reject_unsupported_expr(...)`，带真实源码行号（复用语句层行号逻辑，取不到明说）。
  - 未知 `ParagraphCall`/`FunctionCall` → `_reject_unknown_call(...)` 列出本模块已定义候选（>10 个截断 + 总数）。
  - `Identifier` 分支先拆 `_ADAPTER_UNKNOWN_PREFIX`，报原 v3 类型名（表达式位揭穿适配层伪装）。
  - `ExpressionStatement` 先剥外壳再递归（表达式位也接得住适配层伪装）。
  - **补实现**：`DictLiteral`（dv_dict_new + 逐条 dv_dict_set）、`StringInterpolation`（拆段 + dv_concat 拼接降级）、`列(...)` 带元素构造列表、`整除`(`//`) 走 sdiv/dv_div、`IndexAccess` 区分列表/字典/字符串三分支。
  - 二元运算符链尾 `dv_add` → `NotImplementedError`（含已支持清单）；`slice` 专用报错指路 `截取/子串`。
- **`src/compiler.py`**（C3-4）：`PassStmt` 转换器返回 None（`_to_list_stmts` 跳过 → 空操作）；`StringInterpolation` 无损保留 parts（改动前转成恒空串的 StringLiteral，插值被静默吃掉）。
- **`src/code_generator.py`**（C3-5/C3-7）：`_generate_method` 真发射形参注解（`名: 类型 = 默认值` 按 PEP 8 加空格）与返回注解（`-> 类型`）；三个 `异步*文件` **编译期** `CodeGenError` 指路同步替代。
- **`src/parser_stmt.py`**（C3-6）：识别 `对`+`于`（`对于` 的忠实切法）给指路文案「遍历 X 于 表：」，不碰词法层单字。
- **`docs/syntax.md`**（C3-6）：明确 `对于 每个 X 在 [...]` 不支持；并列写清遍历语句介词 `于` vs 推导式介词 `之/在` 两套口径。
- **`tests/test_frontend_blockers_run.py`**（C3-7 改判据）：`test_a23_异步文件三名字仍是一个词且失败得响` 的 product 腿改用新增的 `_run_product_allow_compile_fail`（C3-7 把失败从运行期提前到编译期，判据「失败得响 + 完整名字」不变）。

---

## 二、定向测试结果（输出落盘：`_taskC3_*.txt`）

按任务书 §3 命令 + 新增用例，只跑定向、不跑全量：

| 批次 | 结果 | 落盘 |
|---|---|---|
| §3 命令 1：`tests/test_llvm_*.py tests/unit/ tests/test_async.py tests/test_generics*.py` + 4 个新增测试文件 | **1116 passed, 8 skipped, 237 subtests passed, 0 failed** | `_taskC3_directed.txt` |
| §3 命令 2：`tests/e2e/test_llvm_pipeline.py` | **5 passed, 1 skipped, 0 failed** | `_taskC3_e2e.txt` |
| 新增 C3 测试汇总（llvm_c3_expr 22 + generics 5 + parser_codegen 7 + capability 4） | **38 passed** | `_taskC3_newtests.txt` |
| `tests/test_frontend_blockers_run.py`（C3-7 改判据后复跑） | **61 passed, 0 failed** | `_taskC3_frontend_run2.txt` |
| `tests/test_p3_comprehensive.py`（LLVM 真跑综合） | **20 passed** | `_taskC3_p3.txt` |

合计定向绿色：**1202 passed / 9 skipped / 0 failed**。所有新增 C3 用例全部真跑断言 stdout，无字符串断言式假测试。

### 逐条判定新红（§4.10）

C3-1 让表达式层从「静默错编成 0」变成「响亮报错」后，逐条判定所有被影响的存量用例：

| 新红 | 归属 | 处置 |
|---|---|---|
| `test_frontend_blockers_run.py::test_a23_异步文件三名字仍是一个词且失败得响` | **改判据**（C3-7 把失败从运行期 `NameError` 提前到编译期，product 腿原断言 `compile` 成功必红） | product 腿改走新增 `_run_product_allow_compile_fail`；核心判据「必须失败 rc!=0 + 报错含完整名字」**不变**。复跑 61 全绿 |
| `tests/e2e/test_llvm_pipeline.py`（任务书 §3 预告「会如实变红」） | **实测未红** | 该用例覆盖已支持路径（IR 存在性/入口块），不触发会抛错的表达式 → 无需处置，如实记录 |
| 存量用例是否依赖「表达式静默编 0」 | **零命中** | grep `DictLiteral\|StringInterpolation\|RangeExpr\|整除` 在存量 `tests/test_llvm_*.py` 与 `tests/unit/` 零命中（只在新增 `test_llvm_c3_expr.py`），无「靠补实现/改判据救回」的存量红 |

**兜底守位（不许改回 `return 0`）用静态断言三重钉死**：`test_llvm_c3_expr.py::test_二元运算符链尾不是静默加法`、`test_llvm_stmt_coverage.py::test_分派链尾有兜底`、`test_native_leg_capability.py::test_兜底拒绝函数还在`——删掉链尾 else 或把 `NotImplementedError` 改回返回 0 都会打红。

### C3-7 词法白名单普查清单（§4.9）

`src/lexer.py` `COMMON_COMPOUND_WORDS` 全表逐项判定「有映射 / 有 stdlib 实现 / 故意留名防错编」三者之一：

- **三个 `异步*文件`**：无实现，**故意留名防错编**（理由见 `src/lexer.py:245-254`：删名会把 `异步读取文件("a")` 切成 `await 异步` + 被丢弃的 `读取文件('a')`，静默错编方向相反）。本轮 C3-7 选方案 B → **编译期报错指路**，不再是「写了 NameError」的现状，已闭环。
- **5 个异步/并发原语**（`异步睡眠`/`并发等待`/`限时`/`创建任务`/`首个完成`）：有 codegen `builtin_map` → `asyncio.*` 真映射。
- **中文异常类型名**、**正则函数名**（`正则匹配`/`编译正则表达式`）：有 codegen/内置映射。
- **其余复合标识符**（`记录列表`/`行列表`/`当前环节`/`测试_生成问候语` 等）：普通标识符，防 pre-scan 前缀误拆，非内置承诺，无实现义务。
- **结论**：除三个 `异步*文件` 外，**无其他「有名无实」**。A2-3 删掉的 9 个死名字复核仍在表外（0 处使用依据不变）。

---

## 三、移交清单 ｜ 光明写不出什么 → 临时绕法 → 期望的语言能力

- **异步文件 IO（`异步读取文件`/`异步写入文件`/`异步追加文件` 只有词法名、无实现）** → C3-7 编译期报错指路同步 `读取文件`/`写入文件`/`追加文件`（`stdlib/` 是 A3/B3 地盘，本轮不碰） → stdlib 侧纯光明线程池实现，把同步文件 IO 包成可等待（方案 A）
- **原生腿切片（`表[1:2]` → `SliceExpr`）** → 专用报错指路 `截取`/`子串` 或转译后端 → 原生腿切片原语
- **Lambda / 列表字典推导 / `Pipeline` / `UnwrapExpression` / `SetLiteral` / `TupleLiteral`** → 链尾报错指路转译后端（`python -m cli.light_unified run`） → 原生腿实现（本轮明确不做，边界表已登记）
- **字典展开 `{**展开}`（DictExpand）** → 报错指路 → 原生腿展开原语
- **字符串插值真 format** → 降级实现「拆段 + dv_concat 拼接」，只做 `{expr}` 替换、无格式说明符/转换 → 原生腿真 format 机制
- **泛型注解** → 形参/返回注解已真发射（C3-5）；但段落级泛型 `段落 首个[T]` 刻意不在 `def` 头发 `[T]`（Python 3.12 语法，产物要兼容老解释器） → 若产物允许 3.12+，可发射 `def 首个[T]`

---

## 四、已知未完成 / 未实测项（POSIX 标注）

- **POSIX 未实测**：本会话全程 Windows 10 + PowerShell。LLVM 原生腿所有新增能力（DictLiteral / StringInterpolation / `整除` / 泛型注解 / 错误路径）的 `.light → IR → clang → exe → stdout` 真跑只在 Windows 验证；POSIX 分支（SIGTERM→SIGKILL 升级、进程组、信号语义）未在 POSIX 系统实测，沿用第二轮既有结论。
- **切片 / Lambda / 推导式 / Pipeline / Unwrap / SetLiteral / TupleLiteral / DictExpand**：明确不做（写进能力边界表，链尾报错 + 指路），非遗漏。
- **字符串插值降级实现**：无 format 说明符/转换（`f"{x!r}"`、`{x:>5}` 等不支持，会按表达式段拼接）。
- **llvmlite MCJIT 路径**：未验证（本机无 llvmlite，见 §五 skip 分析）。
- **原生腿 `整除` 混合类型**：双整数走 sdiv（截断整除），浮点走 fdiv，与 runtime `dv_div` 双整数分支一致；混合类型边界未单独铺测试。

---

## 五、skip 清单与掩盖分析（每条：跳过原因 → 掩盖了什么）

| # | skip 位置 | 跳过原因 | 这条跳过掩盖了哪个未验证事实 |
|---|---|---|---|
| 1 | `test_llvm_tls.py:286` | POSIX 桩反向钉住，Windows 有真后端不适用 | POSIX 上 TLS 桩行为（与本轮无关） |
| 2 | `test_llvm_net.py:854` | 缺 llvmlite | IR→llvmlite MCJIT→进程内执行路径（clang 腿覆盖不到 IR 合法性这一面） |
| 3 | `test_llvm_net.py:862` | 缺 llvmlite | 同上 |
| 4 | `test_ir_verify.py:327` | 缺 llvmlite | IR 被 LLVM 官方 parse_assembly + verify 接受 |
| 5 | `test_ir_verify.py:311` | 缺 llvmlite | 同上 |
| 6 | `test_light_examples_run.py:278` | 字典操作函数以关键字开头，parser 暂不支持直接调用 | 字典**内置函数**直接调用形态（本轮 C3-2 只加了字典字面量 + 索引，未动函数调用形态） |
| 7 | `test_light_examples_run.py:286` | 类代码生成不完整 | 转译后端类特性的部分形态（非原生腿） |
| 8 | `test_light_examples_run.py:373` | 字典操作函数以关键字开头，parser 暂不支持 | 同 6 |
| 9 | `test_llvm_pipeline.py:41`（e2e） | 缺 antlr4 | 旧 antlrparser 路径 IR 生成；`src/llvm/` 新路径由 `test_llvm_net.py` 覆盖 |

**核心声明**：本轮 C3 新增能力（DictLiteral / StringInterpolation / RangeExpr / `整除` / 泛型注解 / 表达式兜底 / `pass` 空操作 / `对于` 指路 / 异步文件编译期报错）**全部真跑断言 stdout，零 skip、零字符串断言式假测试**。8 条定向 skip 全部是既有基建缺失（llvmlite / antlr4 / 平台桩），无一掩盖本轮改动。

---

## 附：验收判据核对（§4.1–4.11）

1. ✅ `_gen_expression` 链尾与未知 `ParagraphCall` 不再返回 0，改抛 `NotImplementedError` 带真实行号（取不到明说，未伪造 0）
2. ✅ 未定义段落名报错列出候选（>10 截断 + 总数）
3. ✅ 适配层未知**表达式**节点揭穿报原 v3 类型名（参数化用例，11 种类型各一条）
4. ✅ `DictLiteral` 原生腿真编真跑断言 stdout（`.light → IR → clang → exe`）
5. ✅ 能力边界表落地 + 源码级断言测试守单点（4 条）
6. ✅ `PassStmt` 编成空操作不报错
7. ✅ 泛型形参/返回注解真发射，`exec` 后 `get_type_hints` 解析出 `T`
8. ✅ `对于 每个 … 在 …` 给指路文案 + `docs/syntax.md` 写明不支持 + 两套介词并列说明
9. ✅ 三个 `异步*文件` 编译期报错 + 指路；词法白名单普查清单见 §二
10. ✅ 新红逐条判定见 §二；**零条靠「把兜底改回返回 0」转绿**（三重静态断言钉死）
11. ✅ 本报告五段齐全 + skip 清单与掩盖分析
