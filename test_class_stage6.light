打印 "=== 阶段6：类方法 + 静态方法 测试 ==="。

类 数学工具：
    属性 PI。
    
    构造 接收 pi_val：
        己PI 为 pi_val。
    
    段落 类圆面积 接收 r：
        返回 r。
    
    段落 静相加 接收 a b：
        返回 a 加 b。
    
    段落 静最大值 接收 a b：
        如果 a 大于 b：
            返回 a。
        否则：
            返回 b。

类 计数器：
    属性 当前值。
    
    构造：
        己当前值 为 0。
    
    段落 计数：
        返回 己当前值。
    
    段落 静描述：
        返回 "这是一个计数器类"。

打印 "--- 测试1：静态方法调用（类名.静态方法()）---"。
打印 "相加(3, 4): " 加 转文本(数学工具.静相加(3, 4))。
打印 "最大值(10, 7): " 加 转文本(数学工具.静最大值(10, 7))。

打印 "--- 测试2：类方法调用（类名.类方法()）---"。
打印 "类圆面积(5): " 加 转文本(数学工具.类圆面积(5))。

打印 "--- 测试3：实例方法仍然正常（不退化）---"。
设 c 为 新建 计数器()。
打印 "计数: " 加 转文本(c.计数())。

打印 "--- 测试4：静态方法描述 ---"。
打印 计数器.静描述()。

打印 "=== 阶段6测试完成 ==="。
