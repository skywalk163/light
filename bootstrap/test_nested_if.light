段落 测试嵌套如果 接收 x：
  如果 x 等于 1：
    设 y 为 x 加 10
    如果 y 大于 10：
      返回 "大"
    返回 "小"
  如果 x 等于 2：
    返回 "二"
  如果 x 等于 3：
    返回 "三"
  返回 "其他"

打印(测试嵌套如果(1))
打印(测试嵌套如果(2))
打印(测试嵌套如果(3))
打印(测试嵌套如果(99))
