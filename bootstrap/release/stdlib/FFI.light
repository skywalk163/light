# 段言 C FFI 模块 - 外部函数接口
# 提供动态库加载、类型映射、函数调用等功能

段落 加载库 接收 路径：文本, 别名：文本：
    从 FFI 加载 加载库(路径, 别名)
    。

段落 获取库 接收 别名：文本：
    从 FFI 加载 获取库(别名)
    。

段落 获取类型 接收 类型名：文本：
    从 FFI 加载 获取类型(类型名)
    。

段落 声明函数 接收 库别名：文本, 函数名：文本, 参数类型列表, 返回类型：文本：
    从 FFI 加载 声明函数(库别名, 函数名, 参数类型列表, 返回类型)
    。

段落 调用函数 接收 库别名：文本, 函数名：文本, 参数列表：
    从 FFI 加载 调用函数(库别名, 函数名, 参数列表)
    。

段落 编码文本 接收 甲：文本：
    从 FFI 加载 编码文本(甲)
    。

段落 解码文本 接收 甲：
    从 FFI 加载 解码文本(甲)
    。

段落 内存分配 接收 大小：整数：
    从 FFI 加载 内存分配(大小)
    。

段落 内存释放 接收 指针：
    从 FFI 加载 内存释放(指针)
    。

段落 取地址 接收 变量：
    从 FFI 加载 取地址(变量)
    。

段落 创建结构体 接收 结构体类, 字段值：
    从 FFI 加载 创建结构体(结构体类, 字段值)
    。

段落 获取字段 接收 结构体实例, 字段名：文本：
    从 FFI 加载 获取字段(结构体实例, 字段名)
    。

段落 设置字段 接收 结构体实例, 字段名：文本, 值：
    从 FFI 加载 设置字段(结构体实例, 字段名, 值)
    。

段落 创建回调 接收 回调类型, 回调函数：
    从 FFI 加载 创建回调(回调类型, 回调函数)
    。

段落 关闭所有库：
    从 FFI 加载 关闭所有库()
    。