# 段言标准库 - 历法模块
#
# 提供公历农历转换、节气查询、天干地支、生肖、农历月日名等功能。
# 依赖: lunardate
#
# 用法：
#   从 历法 导入 公历转农历, 干支纪年, 生肖。

# 公历转农历
段落 公历转农历(年, 月, 日):
    从 历法 加载 公历转农历(年, 月, 日)。

# 农历转公历
段落 农历转公历(年, 月, 日, 闰月=假):
    从 历法 加载 农历转公历(年, 月, 日, 闰月)。

# 节气
段落 节气(年, 序号):
    从 历法 加载 节气(年, 序号)。

# 节气列表
段落 节气列表(年):
    从 历法 加载 节气列表(年)。

# 查找节气
段落 查找节气(年, 名称):
    从 历法 加载 查找节气(年, 名称)。

# 天干
段落 天干(序号):
    从 历法 加载 天干(序号)。

# 地支
段落 地支(序号):
    从 历法 加载 地支(序号)。

# 干支
段落 干支(序号):
    从 历法 加载 干支(序号)。

# 干支纪年
段落 干支纪年(年):
    从 历法 加载 干支纪年(年)。

# 干支纪月
段落 干支纪月(年, 月):
    从 历法 加载 干支纪月(年, 月)。

# 干支纪日
段落 干支纪日(年, 月, 日):
    从 历法 加载 干支纪日(年, 月, 日)。

# 生肖
段落 生肖(年):
    从 历法 加载 生肖(年)。

# 农历月名
段落 农历月名(月, 闰月=假):
    从 历法 加载 农历月名(月, 闰月)。

# 农历日名
段落 农历日名(日):
    从 历法 加载 农历日名(日)。

# 农历完整日期
段落 农历完整日期(年, 月, 日, 闰月=假):
    从 历法 加载 农历完整日期(年, 月, 日, 闰月)。

导出 公历转农历 农历转公历。
导出 节气 节气列表 查找节气。
导出 天干 地支 干支。
导出 干支纪年 干支纪月 干支纪日。
导出 生肖。
导出 农历月名 农历日名 农历完整日期。
