# 段言标准库 - 排版模块
#
# 提供中英文混排空格、全半角转换、标点规范、引号规范、排版检查等功能。
# 参考《中文文案排版指北》。
#
# 用法：
#   从 排版 导入 插入中英文空格, 自动排版, 检查排版。

# 中英文之间插入空格
段落 插入中英文空格(文本):
    从 排版 加载 插入中英文空格(文本)。

# 转全角
段落 转全角(文本):
    从 排版 加载 转全角(文本)。

# 转半角
段落 转半角(文本):
    从 排版 加载 转半角(文本)。

# 规范化标点
段落 规范化标点(文本):
    从 排版 加载 规范化标点(文本)。

# 规范化引号
段落 规范化引号(文本):
    从 排版 加载 规范化引号(文本)。

# 检查排版
段落 检查排版(文本):
    从 排版 加载 检查排版(文本)。

# 自动排版
段落 自动排版(文本):
    从 排版 加载 自动排版(文本)。

导出 插入中英文空格 转全角 转半角。
导出 规范化标点 规范化引号 检查排版 自动排版。
