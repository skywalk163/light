# 段言标准库 - 列表工具模块（纯段言实现）
#
# 用法：
#   从 列表工具 导入 求和, 最大值, 反转列表。

# 列表求和
段落 求和。
  参数 列表。
  变量 总和 = 0。
  变量 计数 = 0。
  变量 长度值 = 长度(列表)。
  当 计数 < 长度值：
    总和 = 总和 + 列表[计数]。
    计数 = 计数 + 1。
  返回 总和。
结束。

# 列表最大值
段落 最大值。
  参数 列表。
  变量 结果 = 列表[0]。
  变量 计数 = 1。
  变量 长度值 = 长度(列表)。
  当 计数 < 长度值：
    如果 列表[计数] > 结果：
      结果 = 列表[计数]。
    计数 = 计数 + 1。
  返回 结果。
结束。

# 列表最小值
段落 最小值。
  参数 列表。
  变量 结果 = 列表[0]。
  变量 计数 = 1。
  变量 长度值 = 长度(列表)。
  当 计数 < 长度值：
    如果 列表[计数] < 结果：
      结果 = 列表[计数]。
    计数 = 计数 + 1。
  返回 结果。
结束。

# 列表平均值
段落 平均值。
  参数 列表。
  变量 长度值 = 长度(列表)。
  如果 长度值 == 0：
    返回 0。
  变量 总和 = 求和(列表)。
  返回 总和 / 长度值。
结束。

# 反转列表
段落 反转列表。
  参数 列表。
  变量 长度值 = 长度(列表)。
  变量 结果 = []。
  变量 索引 = 长度值 - 1。
  当 索引 >= 0：
    追加(结果, 列表[索引])。
    索引 = 索引 - 1。
  返回 结果。
结束。

# 判断包含元素
段落 包含。
  参数 列表 元素。
  变量 计数 = 0。
  变量 长度值 = 长度(列表)。
  当 计数 < 长度值：
    如果 列表[计数] == 元素：
      返回 真。
    计数 = 计数 + 1。
  返回 假。
结束。

# 查找元素索引
段落 查找索引。
  参数 列表 元素。
  变量 计数 = 0。
  变量 长度值 = 长度(列表)。
  当 计数 < 长度值：
    如果 列表[计数] == 元素：
      返回 计数。
    计数 = 计数 + 1。
  返回 -1。
结束。

# 计数元素出现次数
段落 计数。
  参数 列表 元素。
  变量 次数 = 0。
  变量 索引 = 0。
  变量 长度值 = 长度(列表)。
  当 索引 < 长度值：
    如果 列表[索引] == 元素：
      次数 = 次数 + 1。
    索引 = 索引 + 1。
  返回 次数。
结束。

# 列表连接
段落 连接。
  参数 列表甲 列表乙。
  变量 结果 = []。
  变量 索引 = 0。
  变量 长度甲 = 长度(列表甲)。
  当 索引 < 长度甲：
    追加(结果, 列表甲[索引])。
    索引 = 索引 + 1。
  索引 = 0。
  变量 长度乙 = 长度(列表乙)。
  当 索引 < 长度乙：
    追加(结果, 列表乙[索引])。
    索引 = 索引 + 1。
  返回 结果。
结束。

# 范围生成
段落 范围。
  参数 起始 结束。
  变量 结果 = []。
  变量 当前 = 起始。
  当 当前 < 结束：
    追加(结果, 当前)。
    当前 = 当前 + 1。
  返回 结果。
结束。

导出 求和 最大值 最小值 平均值 反转列表。
导出 包含 查找索引 计数 连接 范围。
