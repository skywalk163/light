# _task3_R60_ 地板搬迁 __all__ 收口 + 截取语义误用族 修复报告

> 轮次：R60 · 任务3（在任务书「串行 C 路」位置执行）
> 执行机：本机 Windows，`light-merge/.venv` Python 3.13.14（仅定向，未跑全量；全量留路 M）
> 红线遵守：未 commit、未 push；未跑全量；未改 `src/`；未改 `截取` 定义本身。

---

## 0. 结论速览

| 项 | 任务书预期 | 实际 |
|---|---|---|
| 3A 红 `test_十个段落全部导出且两版都可调用` | 1 条转绿 | **转绿**（128/128 S2 列表文件全过） |
| 3B 截取误用族 | 6 处雷，逐处核对 | **实修 11 处**（任务书列 6，全语料扫描后补修同族遗漏 5 处），手机号 start=0 两处确认无误用 |
| 3B 定向测试 | 3~5 个，挂 tests/unit/ | **17 个用例**（5 个测试函数，参数化），全过；反跑证实非空转 |
| 新增红 | 0 | 0 |

---

## 1. 3A：地板搬迁 `__all__` 模块边界收口（1 条红）

### 目标与复现
- 目标：`tests/unit/test_地板搬迁_列表_S2.py::test_十个段落全部导出且两版都可调用`。
- 复现（修前）：`assert sorted(光明.__all__) == sorted(_十个段落)` 失败——
  - 实际 `__all__` = 14 项（导出行 L14 照抄）；
  - 期望 `_十个段落` = 11 项（10 个列表函数 + `副本`，止于 `副本`）。
  - 多出 `浅拷贝`/`深拷贝`/`冻结` 3 个 L-083 通用容器原语。

### 根因复核
`__all__` 由导出行符号直接生成（`code_generator.py:4665` `__all__ = [...]`），无跨模块收集。
真正越界在导出行本身：L-083 把通用容器原语 `浅拷贝/深拷贝/冻结` 也写进了「列表组搬迁」的
对外契约，而 S2 测试把该模块对外契约**冻结**在 11 项（`builtins.py:719-779` 列表组段 +
787 `副本`；`浅拷贝/深拷贝/冻结` 在 builtins.py 字典工具段、且 `深拷贝/冻结` 真身走
`copy.deepcopy`，不属本搬迁组对外契约）。

### 修复（stdlib/内置核心列表.light）
- L14 导出行去掉尾部 `浅拷贝 深拷贝 冻结`，收口为 11 项；并补 3 行注释说明边界。
- **段落本体仍保留**（L73/87/101 仍定义 `浅拷贝/深拷贝/冻结`）：builtins.py:793 经直接属性
  `内置核心列表.浅拷贝(原)` 调用，不依赖 `__all__`（`__all__` 只影响 `import *`）。
- 取证：全仓无任何 `from 内置核心列表 import *`；.light 侧无从该模块具名导入这 3 名。

### 结果
- 目标用例 `1 passed`；整文件 `test_地板搬迁_列表_S2.py` **128 passed**。
- `tests/test_pure_light_hook.py` + `test_L055_L061_L062.py`（副本浅拷贝）**79 passed, 1 xfailed**，
  确认删导出未影响 builtins.py 经直接属性调用。

---

## 2. 3B：截取语义误用族（预防性）

### 语义基线
`截取(文本, 起始, 结束)` = `文本[起始:结束]`（内置核心字符串.light L13-15）。
旧 `(起始, 长度)` 意图在 start>0 时：`[start:len]` 因 start>end 得**空串**，静默错。

### 逐处核对结论

**stdlib/身份证校验.light（实修 8 处，任务书列 4）**

| 行 | 修前（误用） | 修后（end） | 语义 |
|---|---|---|---|
| 198 | `截取(号码,6,4)` | `截取(号码,6,10)` | 年 [6:10] |
| 199 | `截取(号码,10,2)` | `截取(号码,10,12)` | 月 [10:12] |
| 200 | `截取(号码,12,2)` | `截取(号码,12,14)` | 日 [12:14] |
| 208 | `截取(号码,14,3)` | `截取(号码,14,17)` | 顺序码 [14:17]（提取性别，任务书未列，同族补修） |
| 232 | `截取(号码,6,4)` | `截取(号码,6,10)` | 年（校验身份证内联，任务书未列，同族补修） |
| 233 | `截取(号码,10,2)` | `截取(号码,10,12)` | 月（同上） |
| 234 | `截取(号码,12,2)` | `截取(号码,12,14)` | 日（同上） |
| 235 | `截取(号码,14,3)` | `截取(号码,14,17)` | 顺序码（任务书列） |

修前这些路径生日得 `---`、性别取空串；修后与 `.py` 参考逐值一致。

**stdlib/中文数字转换.light（实修 3 处，任务书仅点名 L188 核对）**

| 行 | 修前 | 修后 | 说明 |
|---|---|---|---|
| 68 | `截取(文本,1,长(文本)减1)` | `截取(文本,1,长(文本))` | 对齐 .py `chinese_num[1:]`；旧版多切末字（零十二→10） |
| 74 | `截取(文本,1,长(文本)减1)` | `截取(文本,1,长(文本))` | 同上（十开头） |
| 188 | `截取(文本,0,点位置)` | **不动** | 已是 `文本[0:点位置]`，正确 end 语义（任务书要求核对，确认无误用） |
| 189 | `截取(文本,点位置+1,长(文本)减点位置减1)` | `截取(文本,点位置+1,长(文本))` | 小数段 end；旧版少截尾位（三点一四→3.1） |

**stdlib/手机号校验.light（确认无误用，未动）**
- L92 `截取(号码,0,4)`、L129 `截取(号码,0,3)`、L156 `截取(号码,0,3)`：start=0，
  `[0:k]` 与「从 0 取 k 个」天然等价，按任务书指示核对后**不改**。

### 定向测试（新增 `tests/unit/test_R60_截取语义误用族.py`，17 用例）
- 对拍口径：纯光明 .light 真实现 vs 同名 `.py` 参考实现（与 S2 同款），断 光明==期望==py。
- 覆盖：身份证 年月日/顺序码/性别/校验结果（3 号码 ×2 函数）、手机号 前3运营商/前4归属地（3 号码）、
  中文浮点小数不丢尾位（三点一四/一百零二点五/零点三）、中文整数零/十开头不丢末字（零十二/零一百/十二/二十/一百零五）。
- **反跑验证非空转**：把 L189 临时改回旧版，3 条浮点用例当场红（`0.0 != 0.3000…`），恢复后转绿。

---

## 3. 与任务书红线的如实声明（偏差）

1. **实修范围大于任务书清单**：任务书列身份证 4 处、中文数字 1 处核对；全语料逐行核对后，
   身份证补修 L208/L232/L233/L234 同族 4 处，中文数字补修 L68/L74/L189 共 3 处。
   均为同一 `(start,len)` 误用族，已逐处对照 `.py` 真身确认意图，未越界改语义。
2. **中文数字 L188 经核对无误用**（任务书要求核对），真正误用在紧邻的 L189；L68/L74 亦为旧 (start,len) 遗留。
3. **既存缺口（非本任务引入，未改）**：`中文数字转换.light` 调裸名 `去除空格`（native 腿 codegen 别名，
   见 `src/llvm/codegen_typed.py:2646`），Python import 腿 builtin 表只绑 `去除空白`，故走 Python hook 时
   该函数在 L167 即 NameError。定向测试在文件内显式 `中文数字.去除空格 = str.strip` 桥接（已注释说明）；
   原生腿（R11B 子进程）下不受影响。此缺口不属「截取语义」，留待后续，不在本任务擅改。
4. **工作区存在 R60 任务2 的 `src/parser_expr.py` 未提交改动**（异步修饰符当值报错），非本任务产物，未触碰。
5. 未跑 0.82 远程定向（按编排，全量/远程由路 M 唯一执行）；本机定向结果如上。

---

## 4. 改动文件清单（行号级）

- `stdlib/内置核心列表.light`：L14 导出行去 `浅拷贝 深拷贝 冻结`，新增 L12-14 边界注释。
- `stdlib/身份证校验.light`：L198/199/200/208/232/233/234/235 共 8 处 `截取` 第三参改 end。
- `stdlib/中文数字转换.light`：L68/L74/L189 共 3 处 `截取` 改 end；L188 不动。
- `stdlib/手机号校验.light`：未改（start=0 等价）。
- `tests/unit/test_R60_截取语义误用族.py`：新增，17 用例。

## 5. 本机定向结果（汇总）
- `test_地板搬迁_列表_S2.py::test_十个段落全部导出且两版都可调用`：FAIL → **PASS**
- `test_地板搬迁_列表_S2.py`：**128 passed**
- `test_R60_截取语义误用族.py`：**17 passed**
- `test_pure_light_hook.py` + `test_L055_L061_L062.py`：**79 passed, 1 xfailed**（魔数护栏未红）
- 反跑：旧版 L189 → 3 浮点用例红；恢复 → 全绿。
