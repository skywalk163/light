# 第六轮完成报告

执行：2026-08-24，Windows 10 / PowerShell 5.1、Python 3.14。
起点锚点 `f9eba912`（标签 **`pre-round6`**，即第五轮收尾）。
收尾 main = **`8c97c51b`**，gitea CI **run 86 = success**，
00:33:34→00:44:45 共 671s（push main 预算 1200s）。

本轮不分四路：第五轮留的四条账加上一条已在第五轮做完，剩下的由我一人顺序做完
（顺序 3 → 2 → 1 → 4 由用户裁定），所以没有合并提交，五个提交都是直接落在 main 上。

全轮合计 17 files changed，+490 / -59。

## 一、做了什么

- `c1fa9bd5` **第 2 条**：`获取JSON` 非 200 抛 `HTTP状态错误`，不再吞成 `None`
  （用户裁决：抛异常，不返回哨兵值）
- `abe9bd41` **第 1 条**：零参调用不许丢括号 —— `ParagraphCall` 加「带括号」字段
  （用户裁决：认改 AST 节点形状这个代价）
- `3d19daab` **第 3 条**：假测试门禁豁免编译器生成的 `.py`
  （用户裁决：exempt）
- `53356b1d` **第 4 条**：真实 DeepSeek 通道做成常驻可选后端，并补跑实测
  （用户裁决：permanent_switch —— 不许再造一次性文件）
- `8c97c51b` 合并点收尾：假测试基线 471 → 469

第 5 条（跨平台 sep-guard 探针复跑）在第五轮已完成，本轮无需再做。

## 二、四条账各自的落地与反跑

### 第 1 条：`目标` 与 `目标()` 在 AST 里终于分得开（`abe9bd41`）

根因是零参歧义：裸引用 `目标` 与真调用 `目标()` 都被包成 `args == []` 的
`ParagraphCall`，codegen 只能靠 `.args` 是否为空猜，于是
「局部绑定名 + 零参调用」一律被当裸引用，括号丢掉。

做法：`src/ast_nodes_v3.py` 的 `ParagraphCall` 加第三个字段 `带括号`，
**默认 False**，所以 16 个构造点里 11 个裸名/元数驱动的位置字节不变；
只在 5 个真括号路径显式置 `True`（运算符动词括号路径、`函数/段落 段名(...)`、
标识符带括号、`_parse_postfix` 的括号后缀、`《名》(...)`）。
读侧 `src/code_generator.py` 的判据改成
`if shadowed and not expr.args and not expr.带括号: return py_name`。

顺带把第五轮为绕这个坑留的容器写法删掉，恢复直白写法：
- `stdlib/并发.light` 的 `带限流`：`设 结果 为 等待 函()`
- `stdlib/重试.light` 的 `重试`：`设 结果 为 等待 目标()`

两向反跑：新增 `Test零参调用不丢括号` 三例（零参调用得 `7`、裸引用仍是函数值得
`True`、文言体 `异步 运行 主()。` 路径得 `7`）；把 `带括号` 判据变异掉，
三例连同并发/重试的定向批次同时转红，改回即绿。

### 第 2 条：`获取JSON` 不再把错误吞成空值（`c1fa9bd5`）

`stdlib/lightpub/HTTP客户端.py` 的 `获取JSON` 原本在非 200 时 `return None`，
正是「零静默降级」口径要拦的形态。改成抛 `HTTP状态错误`。
这是行为变更，第五轮合并点没自裁，本轮拿到用户裁决后才动。

### 第 3 条：门禁豁免编译器生成物（`3d19daab`）

`tools/ci/assert_quality.py` 加 `GENERATED_MARK = "由光明编译器生成"`，
扫描时首行命中就整份跳过。理由：生成物里的
`assert _x is not None, "尝试解包空值"` 是 codegen 发的解包保护，
不是人写的假绿断言；不豁免的话，每个「测试编译真文件」的用例都得自己记着写临时目录。

两向反跑：`tests/unit/test_ci_gates.py` 加
`test_编译器生成的py整份豁免` + `test_标记不在首行则照扫`（反向守卫），14 passed。

### 第 4 条：真实通道成常驻后端 + 真机实测（`53356b1d`）

- 新增 `examples/harness/真实通道.light`：**每次调用新建 `大模型客户端`**。
  一个客户端绑一条 socket，多协程共用同一实例会 `WinError 10038`。
  受语言层三条限制约束（类体不接受 `异步 段落`、名字不许以「异步」开头、
  类内裸调用被隐式改写成 `己.同名`），所以异步生成器放模块级、
  再用一个不同名的桥接段落 `取真实流式` 接进类里。
- `examples/harness/评测驱动.light` 按 `HARNESS_CHANNEL` 选通道，
  **默认 mock，CI 永不联网**；`real` 缺 `DEEPSEEK_API_KEY` 时
  `sys.exit(2)` 且不落报告 —— 不许降级回 mock 假装成功。报告里写明通道。
- `cli/light.py` 加 `--channel {mock,real}`，并把 `HARNESS_CHANNEL` 放进参数表首位。
- `tests/test_harness_real_channel_light.py`：本地 `ThreadingTCPServer` 假 DeepSeek，
  零联网覆盖三件事 —— 6 条请求各占一条连接、鉴权头是非空 Bearer、并发比串行快。

真机实测（用户 key，跑完即恢复默认 mock）：**3/6 = 0.5**，逐条与 D5 一致
（首都 ✓ 太阳 ✗ 水果 ✓ 编程语言 ✗ 邮箱 ✓ 手机号 ✗）；
**并发 2 = 3.23s vs 串行 10.04s**，D5 那次没有并发，这次是 A5 异步内核合入后的真并发。

## 三、自查出来的一个 P0：形参遮蔽类属性时写侧误发 `self.x`

排查 401 时发现的，不在四条账里。

`src/code_generator.py` 判断标识符是不是类属性，**读侧**
`_resolve_identifier_name`（约 3483 行）会排除 `_current_method_params`，
**写侧** `_generate_var_decl`（约 1265 行）不排除。于是「方法形参名恰好与类属性同名」时，
`设 密钥 为 ...` 被编成 `self.密钥 = ...`：赋值打到实例上，后续读取拿的是局部名，
静默错译。表现是「密钥明明传进去了，请求头里却是空 Bearer」→ DeepSeek 报 401。

修法是把写侧判据补齐成与读侧对称，并在注释里留下这次事故的归因。
两向反跑：变异回去，真实通道立刻复现 401；改回即通。
定位手段值得记住 —— 这类静默错译不要在光明层猜，直接 dump 生成的 Python。

## 四、合并点验证（都在 `8c97c51b` 上实跑，门禁未与测试并发）

- 全量：**4344 passed / 23 failed / 86 skipped / 1 xfailed / 237 subtests**，687s
- 假测试门禁：**469 条**存量冻结、无新增，通过
- 自举率门禁：**22/74 = 29.73%** 持平、影子 53 持平、
  `stdlib` 引 Python 逃逸 0、转手造假 0，通过
- CI run 86 = success，671s

### 基线 471 → 469 的账

减掉的两条都是编译器生成物，被第 3 条的豁免规则整份跳过：

- `bootstrap/bootstrap_v3_gen.py:111:not-none`
- `lsp/light_lsp_server.py:95:not-none`

第三条失效项 `tests/test_http_client.py:591:lower-bound` 是**行号平移**：
同一条 `assert len(chunks) > 0` 被 `c1fa9bd5` 新增的用例顶到 599 行，
一进一出不改总数。按口径基线只在合并点重建一次，重建后复跑确认绿。

### 那 23 条红全部有归因，无一与本轮改动相关

- **14 条缺库**：`matplotlib` ×4（L4 demo 的 `test_duan_run` /
  `test_duan_compile_and_run_product`）、`lunardate` ×8（农历与节假日）、
  `aiohttp` ×2（`test_async_client`、`test_async_client_context_manager`）
- **8 条 Windows 控制台 GBK 乱码**：`TestCrossPlatformCLI.test_encoding_cli`、
  `TestCompilerFullChain.test_full_pipeline_run`（实际输出 `Hello, 娈佃█!`）、
  终端组 `Test基本退出` ×3 / `Test有界输出与spill` ×2 / `TestWindowsConPTY` ×1
- **1 条历史存量**：`TestLSPProtocol.test_initialize与shutdown协议`，
  `TypeError: object of type 'NoneType' has no len()`

按「本机装库转绿不算修好」的口径，这些库我没装。

## 五、留给第七轮的账（本轮**未**动）

1. **`编排.light` 的 `带重试对话` 只分类 `TimeoutError` / `ConnectionError`**。
   意外异常（比如 HTTP 401）会把整场 harness 打死且不落报告 —— 本轮实地撞见过一次。
2. **`code_generator.py:3323 / 3336` 的 `_looks_callable_arg` / `_looks_data_arg`
   仍只看 `.args` 是否为空**，没吃新加的 `带括号`。同一个零参歧义在这两处还在。
3. **`src/compiler.py:432 _convert_paragraph_call` 不传播 `带括号`**
   —— v3 AST → 类型化 AST 这一跳会把本轮加的信息丢掉。

## 六、本轮踩到、值得写进规程的两条

1. **测「并发比串行快」时，延迟必须注入到响应体中段**。
   `stdlib/大模型客户端.light` 的诚实边界是 connect / TLS / send / 读响应头全同步，
   唯一 await 点是流式读体。把 `sleep` 放在发响应头之前，并发与串行几乎同耗时
   （实测 2.62s vs 2.79s，像是没提速）；切进响应体后差距立刻出来（3.23s vs 10.04s）。
2. **读写两侧的判据必须逐条比对**。凡涉及
   `_class_attr_names` / `_current_method_params` / `_local_variables` 的改动，
   只改一侧就是缺陷 —— 本轮那个 P0 就是这么来的。
