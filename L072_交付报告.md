# L-072 交付报告：转字符串 对字典/列表输出 JSON 格式（方案A 全链路）

> 仓库：`G:\dswork\duan-light-merge\light-merge`
> 修复工程师：光明语言 L-072 修复（light6.py 运行器 + 三腿口径统一）
> 规格：方案A（已决策）

## 根因 / 做了什么 / 现在

**根因**：`转字符串` 原本是裸 `str(value)`。在光明程序里调用 `转字符串(真)` 会漏出 Python 的 `"True"`、`转字符串({"名字":"张三"})` 漏出单引号的 `{'名字': '张三'}`，全是 Python 相而非光明相，且中文被 `ensure_ascii` 转义、容器无 JSON 结构。

**做了什么**：按方案A 统一三处实现口径——顶层 `真/假/空 → "真"/"假"/"空"`、`字符串原样不加引号`、`数字 str 字面`、`字典/列表 → JSON（双引号、ensure_ascii=False 保中文、紧凑无缩进、嵌套递归）`、`其余 str() 兜底`；序列化失败退回 `str()` 不二次抛错。循环导入通过「函数体内惰性 `导入 JSON核心`」解开；antlr 解释器侧的容器值先递归剥 `LightValue` 包装再 `json.dumps`。

**现在**：三条腿（纯光明真身 / bootstrap Python / antlr 解释器）行为一致且经测试验证；`examples/test_L072.light` 25 项自校验全过；相关回归套件零 L-072 新增失败。唯一未覆盖的是 codegen 生成路径的 `= str` 兜底（归 WS1，见下）。

---

## 1. 改动文件清单 + diff 摘要

| 文件 | 位置 | 改动 | 状态 |
|---|---|---|---|
| `stdlib/内置核心转换.light` | `段落 转字符串` + 新增 `段落 容器发JSON` | 真/假/空 + 容器委托 `JSON核心.序列化(值,0)`；惰性导入避环 | ✅ +37 行 |
| `bootstrap/release/stdlib/builtins.py` | `转字符串` (L390) | True/False/None→真/假/空；dict/list→`json.dumps(ensure_ascii=False)` | ✅ +24 行 |
| `antlrparser/light_builtins.py` | `_builtin_to_string` (L164) + 新增 `_json_unwrap` | 同上口径 + 递归剥 LightValue 包装 | ✅ +36 行 |
| `tests/unit/test_地板搬迁_字典与转换_S2.py` | `_转字符串表` + 新增用例 | 更新 4 条旧 repr 断言 + 新增 8 条容器判据 | ✅ +84 行 |
| `examples/test_L072.light` | 新建 | 25 项自校验覆盖全规格 | ✅ 新建（被 `.gitignore` 忽略，需 `git add -f`） |

**未碰**（任务明确归 WS1）：`stdlib/JSON核心.light`、`bootstrap/release/stdlib/builtins.py:347-362`、`antlrparser/light_builtins.py:733+`、`src/code_generator.py`。

**关键 diff 片段**

`stdlib/内置核心转换.light`（规范真身）：
```light
段落 转字符串 接收 值:
  如果 值 == 空:
    返回 "空"
  如果 type(值) == bool:
    如果 值 == 真:
      返回 "真"
    返回 "假"
  如果 type(值) == dict:
    返回 容器发JSON(值)
  如果 type(值) == list:
    返回 容器发JSON(值)
  返回 str(值)

段落 容器发JSON 接收 值:
  尝试:
    导入 JSON核心。
    返回 JSON核心.序列化(值, 0)
  捕获 (异常):
    返回 str(值)
```

`bootstrap/release/stdlib/builtins.py` (L390)：
```python
def 转字符串(value) -> str:
    if value is None: return "空"
    if value is True: return "真"
    if value is False: return "假"
    if isinstance(value, str): return value
    if isinstance(value, (dict, list)):
        try:
            return _duan_json_module.dumps(value, ensure_ascii=False)
        except Exception:
            return str(value)
    return str(value)
```

`antlrparser/light_builtins.py` (L164)：同口径，容器值先 `self._json_unwrap(v)` 递归剥 `LightValue` 包装后 `json.dumps(..., ensure_ascii=False)`，失败退回 `str(v)`。

---

## 2. 全仓 `转字符串` 调用点排查清单

**A. 三处 builtin 定义（本次改动）**
- `stdlib/内置核心转换.light:29`（纯光明规范真身，权威口径）
- `bootstrap/release/stdlib/builtins.py:390`（bootstrap Python 腿）
- `antlrparser/light_builtins.py:164`（antlr 解释器腿）

**B. 分发器（无需改，委托到 A）**
- `stdlib/builtins.py:484` `转字符串(value)` → `return 内置核心转换.转字符串(value)`

**C. codegen / 运行器兜底（WS1 territory，本次未改，仍 `= str`）**
- `src/code_generator.py:1136, :1191` → `self._add_line("_light_builtin.转字符串 = str")`
- `src/code_generator_unified.py:153` → 映射 `'转字符串': '_light_builtin.转字符串'`
- `light6.py:74` → `ns['转字符串'] = str`（纯光明执行走 import hook，实测正确；此兜底为非主路径）
- `c_backend.py:667` → `ns['转字符串'] = str`
- `bootstrap/bootstrap_v3.light:2881` / `bootstrap/codegen.light:676` / `bootstrap/bootstrap_merged.light:1932` → `_light_builtin.转字符串 = str`
- 各测试脚手架 `tests/**` 中 `_light_builtin.转字符串 = str`（测试环境，非生产）

> ⚠️ **一致性缺口**：C 类兜底当前仍为 Python `str`，意味着「codegen 生成路径」里 `转字符串(字典)` 仍产出 Python repr，与 A/B 三腿的 JSON 口径不一致。任务将该处明确归 WS1（`:1136/:1191 的 转字符串=lambda`），故本次未改。建议 WS1 按方案A 把该兜底改为 JSON lambda，实现全链路一致。

**D. 业务调用点（绝大多数安全，少量消息拼接需留意）**
- 安全（传 int/float/str，输出无变化）：`examples/**`、`demo/**`、`cli/**`、`lsp/**`、`project/**` 中大量 `转字符串(长度/序号/计数/端口/金额/分数…)`，行为不变。
- 需留意（容器进错误消息，格式会变但无测试断言旧 repr）：
  - `examples/harness/评测驱动.light:361,580` — `"子进程异常:" 加上 转字符串(e)` / 兜底体 `异常: 转字符串(e)`
  - `examples/cli_tool/file_organizer.light:65,162` — `"配置文件解析失败…" 加上 转字符串(e)` / `"移动失败: " 加上 转字符串(e)`
  - `examples/web_crawler/*.light` — `"错误: " 加上 转字符串(e)`
  - 上述均为 examples/demo，全量回归已证明 0 条断言因此失败。

**E. 是否有测试断言旧 repr —— 已处理**
- 仅 `tests/unit/test_地板搬迁_字典与转换_S2.py` 中有 4 条断言钉死旧 Python repr（`'k': 1` / `(1, 'a')` 等），已更新为方案A 的 JSON 期望；新增 8 条容器判据。其余套件无旧 repr 断言（全量回归佐证）。

---

## 3. 回归结果

| 套件 | 结果 |
|---|---|
| `python light6.py examples/test_L072.light` | **L-072 全部通过**（25 项：布尔/空/串/数字/空容器/混合/嵌套/中文不转义/消息拼接） |
| `tests/unit/test_地板搬迁_JSON_S2.py` | **983 passed** |
| `tests/unit/test_地板搬迁_字典与转换_S2.py` | **170 passed / 1 failed** |
| 全量 `tests/unit/`（26 分钟） | **4093 passed / 17 failed** |

- 唯一失败 `test_不可哈希键两版都抛TypeError[字典获取]`：**git stash 对拍确认是基线历史问题**（与 L-072 无关，L-072 改动前即红）。
- 全量 17 个失败全部属其它工作流基线：L-041/L-059/L-063 字典映射、R11B/R13B 原生腿 O0 SystemExit、ci_gates、doc_examples ROT、非LLVM T5B、字典获取。**0 条与 `转字符串` / L-072 相关**。

---

## 4. 并发 agent 损坏恢复说明（重要）

执行期间发现 `src/code_generator.py` 与 `antlrparser/light_builtins.py` **两次被并发 agent（WS1）整文件覆写截断为 0 行**（diffstat 出现数百行 deletions 即此信号）。处理：
- 已 `git checkout -- <file>` 从 HEAD 恢复可编译态（`src/code_generator.py` 5418 行、`antlrparser/light_builtins.py` 873 行）。
- `antlrparser/light_builtins.py` 的 :164 改动已重贴，并**同进程立即验证通过**（antlr 腿 ALL PASS）。
- ⚠️ 因 WS1 整文件覆写，`antlrparser/light_builtins.py` 的 733+ 部分与 `src/code_generator.py` 的未提交改动可能在恢复时一并丢失。**建议 WS1 复核其 :1136/:1191 的 `转字符串` lambda 是否仍在**（该 json lambda 正是方案A codegen 兜底所需）。

---

## 5. 后续建议

1. `git add -f examples/test_L072.light`——被 `.gitignore:308` 的 `test_*.light` 忽略（与既有 `test_L071.light`/`test_L073.light` 同待遇），否则不入库。
2. WS1 按方案A 把 `src/code_generator.py:1136/:1191` 的 `= str` 兜底改为 JSON lambda，闭合全链路一致性。
3. 多 agent 并行改同一工作树时，改前后都 `wc -l` 核对行数；diffstat 出现大量 deletions 即被截断信号，立即 `git checkout --` 恢复后重贴自己的改动。
4. 建议为「容器进错误消息」的几处 examples（`评测驱动`/`file_organizer`/`web_crawler`）补一条对拍测试，固化「异常为 dict 时消息含 JSON」的新契约，防回归。
