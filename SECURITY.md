# 安全政策（Security Policy）

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| `r97-dev` 及之后主干 | ✅ 安全更新 |
| `r95-dev` / `r94` | ⚠️ 仅接受高危漏洞，建议升级主干 |
| ≤ v6.3.0 及更早 | ❌ 不再维护 |

> 语言本体（light-merge）当前以 `release/r9x` 分支 + `r9x-dev` 标签形式演进；请优先基于最新主干提报与修复。

## 漏洞报告方式

**请勿在公开 Issue 中披露安全漏洞细节。**

请通过以下任一私密渠道提交（均不会公开）：

- **GitHub Security Advisories**（推荐）：仓库 → Security → Report a vulnerability；
- 创建标记为「secret / 私密」的 Issue，由维护者转为私密跟进；
- 在 Discussions → Help 中私密留言（仅维护者可见）。

报告请尽量包含：

- 受影响版本 / 提交号；
- 复现步骤（最小用例）或概念验证；
- 潜在影响评估；
- 是否已有缓解方案。

> 本项目**不设专门安全邮箱**；任何以本项目名义索取凭据、要求转账或索要私钥的邮件/消息均为不实，请忽略并联系维护者核实。

## 响应期望

- 首次响应：尽力在 **7 个工作日内**确认收到并给出初步评估；
- 高危漏洞：确认后尽快安排修复并协调发布补丁版本；
- 修复发布后会在 Release notes 或 Security Advisory 中公开说明（不点名报告者，除非其同意）。

## 安全相关边界

下列内容**不属于**本政策的安全漏洞范畴，请走常规 Issue / Discussion：

- 文档笔误、示例缺失、构建告警；
- 第三方依赖本身的漏洞（请直接向其上游报告，必要时在本项目提依赖升级 Issue）。
