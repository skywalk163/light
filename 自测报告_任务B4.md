# 自测报告_任务B4.md — 沙箱安全硬化（第四轮 B4）

- 分支：task-B4（基线 main = e1dbfc3a）
- 日期：2026-08-23
- 跑测环境：Windows 10+，PowerShell 5.1 语义（git-bash 执行），Python 3.x
- 跑测口号：`env -u PYTHONUTF8 -u PYTHONIOENCODING -u LANG -u LC_ALL -u LC_CTYPE python -m pytest <四文件> -q`
  （带 PYTHONUTF8 跑 进程树/PTY 会假红，任务书预判命中；清环境后全绿）

---

## 1. 改了哪些文件（5 个，逐路径）

### stdlib/代理工具集.light（B4-1 / B4-4 / B4-5 / B4-6 主战场，+99 行）
- **B4-1 原子写越界修复**（write_file / edit_file）：
  - 新增「目标 == 沙箱根目录（"." / "/" / 根路径本身）→ 明确拒绝」分支；
  - 临时文件将建于 `dirname(实路径)`，该父目录**必须再次过 护栏.核准**——堵死「path=. → dirname(根)=沙箱外 → 裸建临时文件」的真越界写；
  - 临时文件名从「`_taskB3_tmp_` + 微秒时间戳（可预测）」改为「`_taskB4_tmp_` + 微秒 + `os.urandom(6).hex()`（不可预测）+ .tmp」，O_EXCL 创建；
  - 写入失败 / os.replace 失败不再吞错：先 `os.close` 再返回明确错误文案（对应文件头 :17「不许吞掉一切」口径）。
- **B4-4 grep 三处**：
  - 单文件读取失败不再静默吞掉：回显 `[读取失败: <原因>]`（原 `捕获 错误: 返回` 吞一切）；
  - 匹配 0 条且输出非空时，不再丢弃 `状态["输出"]`，追加 `[未找到匹配。模式: …]` 一起返回；
  - 目录遍历逐层过 护栏（与 list_dir 已修形态一致），junction 指向外部时回显 `[指向沙箱外，未展开]`，**不递归、不外泄**。
- **B4-5 sh -c 参数映射 bug**：
  - POSIX 分支 `["sh","-c"] + 命令数组` → `["sh","-c"] + [" ".join(命令数组)]`，整条数组拼一个命令串交付；`; / $() / 反引号` 从此是 shell 语义本身（用户显式 允许shell 的自担行为，有注入面用例佐证）；
  - Windows 分支 `["cmd","/c"] + 命令数组` 保留（cmd 逐参数拼接，实测不丢参）。
- **B4-6 敏感环境变量**：`_构建敏感拒绝` 从「endswith 精确后缀」改为「下划线分段 + 敏感段单词（key/token/secret/password/passwd/passphrase/credential/credentials/auth）+ database_url 特例 + 无下划线单段特例」关键词包含式，堵住 AWS_SECRET_ACCESS_KEY / SECRET_KEY / *_PASSPHRASE / PGPASSWORD / AWS_ACCESS_KEY_ID / GOOGLE_APPLICATION_CREDENTIALS / DATABASE_URL 透传。

### stdlib/路径护栏.light（B4-2 / B4-3，+116 行）
- **B4-2 硬链接真防护**：新增 `疑似硬链接` 判定——`os.stat` 成功、非目录、`st_nlink > 1` 即拒（检查判拒、读取/写入抛错）。只对文件查；目录不查（POSIX 目录 st_nlink 随子目录数增大是常态，不构成逃逸）。
- **B4-3 TOCTOU 收窄**：`段落 打开` 里 stat 快照 → 测试钩子 →（POSIX）`O_NOFOLLOW` → `os.open` → **打开后 `os.fstat` 复核 st_dev+st_ino 与快照一致**，不一致即 `os.close` + 抛 路径护栏错误。`同一文件` 在 inode=0（个别文件系统）时退回只比设备号，方向偏保守放行。
- **安全声明同步**（硬性约束 3）：头部注释与 `段落 安全声明` 机读串均改写为「TOCTOU 窗口已通过打开后 os.fstat 复核收窄（读取/写入/续写三入口）」，**不再残留「存在 TOCTOU 窗口」字样**。

### tests/test_path_guard_light.py（+111 行变化）
- `test_硬链接指向根外被拒`：**xfail → 正向断言**（Windows 用 `mklink /H` 实测造链，本机成功、真实执行；含误伤面检查：根内普通文件 nlink==1 不受影响）。
- `test_TOCTOU复核抓住判定后替换`：**xfail → 正向断言**（测试钩子在判定后替换文件，断言 打开 抛出 路径护栏错误）。

### tests/test_agent_tools_light.py（+197 行变化）
- 新增 B4-1 用例（写根目录本身被拒：`/`、根路径、根路径+`/` 三种退化全试）；
- 新增 B4-4 用例 3 个：junction 指向外部不递归不外泄（pattern 与外部内容解耦）、外部 6 文件不膨胀「遍历文件数」、单文件读取失败回显 `[读取失败: …]`；
- 新增 B4-5 用例 3 个：shell 多参数 `echo hi there`、分号注入面、`$()`/反引号注入面（POSIX skip）；
- 新增 B4-6 用例 2 组（参数化）：7 个旧漏网名逐一喂（透传即 FAIL）、5 个对照名 PATH/HOME/LANG/TMPDIR/USERPROFILE 不误伤；
- **假绿清理 2 处**（任务书 §8）：
  - `test_命令不存在有明确错误`：旧断言 `退出码 or 失败` 近似恒真（成功输出也有 `[退出码: N]`）→ 只认 `启动失败/不存在/未找到`，且断言不出现 `退出码: 0`；
  - `test_两点穿越被拒`：旧断言三选一含「不存在」兜底（Windows 上被放行也返回「文件不存在」而过关）→ 只认 `越界/护栏`，并新建真实外部文件对照「内容不得读出」。

### 自测报告_任务B4.md（本文件）

未触碰（越界即停）：stdlib/代理循环.light、stdlib/事件总线.light（A4）、stdlib/流式.light（C4）、src/、tools/ci/、workflows（D4）。

---

## 2. 定向测试结果（汇总行直抄）

```
tests/test_agent_tools_light.py   69 passed, 1 skipped
tests/test_path_guard_light.py    25 passed
tests/test_process_tree_light.py + test_pty_light.py   20 passed, 4 skipped
────────────────────────────────────────────────────────
合计（同一条命令四文件连跑）：
114 passed, 5 skipped, 22 warnings in 19.65s
```

回归基线对照：改动前 `test_agent_tools_light.py` 52 passed → 改动后 69 passed（+17 用例）。
全目录 `pytest tests/` 会因 **test_tls_light.py 缺 cryptography 第三方依赖**收集报错中断——该文件不属 B4 范围（别的工程师文件），与本次改动无关。

---

## 3. 反跑证明（六条，改坏→变红→恢复）

方法：每个修复点把实现改坏一处（脚本精确替换）→ 跑对应用例 → 必须 FAILED → `git` 外备份逐字节恢复 → 校验恢复一致。全部六条实现为一次脚本跑完，输出落盘 UTF-8。

| # | 改坏点 | 用例 | 结果 |
|---|--------|------|------|
| B4-1 | 删除「目标==根目录拒绝」+「父目录再次核准」两道防线（write_file） | TestWriteFile::test_写根目录本身被拒 | **FAILED**（1 failed）→ 恢复 OK |
| B4-2 | 疑似硬链接 改为恒放行（st_nlink>1 不再拒绝） | Test口径与已知缺口::test_硬链接指向根外被拒 | **FAILED**（1 failed）→ 恢复 OK |
| B4-3 | 删除 打开 里 fstat 复核块 | Test口径与已知缺口::test_TOCTOU复核抓住判定后替换 | **FAILED**（1 failed）→ 恢复 OK |
| B4-4 | grep 单文件读取失败改回吞掉（跳过） | TestGrep::test_单文件搜索读取失败回显不吞 | **FAILED**（1 failed）→ 恢复 OK |
| B4-5 | cmd /c 分支改回丢参数旧病（只取首元素）——与 sh -c 旧病同型 | TestRunCommand::test_shell多参数不再被当参数0丢弃 | **FAILED**（1 failed）→ 恢复 OK |
| B4-6 | 敏感段清空为 []（关键词包含式失效） | TestRunCommand::test_敏感变量逐个不漏传 | **FAILED**（5 failed / 2 passed）→ 恢复 OK |

B4-6 改坏后仍绿的 2 个参数（DATABASE_URL、PGPASSWORD）不是假绿：DATABASE_URL 走独立的 database_url 特例、PGPASSWORD 是单段名走单段特例，二者与敏感段数组无关，属独立防线，合理。
B4-5 改坏点选在 Windows 分支（`["cmd","/c"] + [命令数组[0]]`）：POSIX sh -c 分支的 join 改坏在 Windows 上不可见（测试走 cmd /c），故以同型旧病在 Windows 分支复现「丢参数」，判据本质一致；POSIX 分支的真实反跑需 POSIX 环境（未实测项见 §5）。

---

## 4. 移交清单（光明写不出什么 → 临时绕法 → 期望能力）

| 光明写不出 | 临时绕法（本轮） | 期望语言能力 |
|---|---|---|
| 在 .light 源码内做安全的大批量行级替换 | python 脚本按「精确文本替换 + assert 命中 + 逐字节备份恢复」执行，全程未用编辑器 | 编辑器/工具链不破坏 .light 缩进与字符串（编辑器会吃缩进、字符串真实换行截断） |
| Windows 上实测 O_NOFOLLOW、sh -c、POSIX 硬链接语义 | 用例 skip + 反跑改坏点选同型 Windows 分支 + 报告标注 | POSIX（Linux）实测环境 |
| pytest 子进程输出按正确编码读取 | 反跑脚本以 bytes 抓取、utf-8 replace 解码，乱码仅显示层，落盘内容正确 | 无（显示层问题） |
| PowerShell 5.1 的 && 与 heredoc | 用 `;` 串联、消息用 `-F 文件`、长脚本 write 文件再执行 | 无 |

---

## 5. 已知未完成 / 未实测项（POSIX 分支必须标注）

1. **O_NOFOLLOW（路径护栏 段落 打开）**：`sys.platform != "win32"` 分支加 `os.O_NOFOLLOW`（try 包裹，能力缺失则跳过）。Windows 无此标志，**POSIX 未实测**。
2. **sh -c join（代理工具集 run_command 非 win32 分支）**：`["sh","-c"] + [" ".join(命令数组)]` 语义正确但 **POSIX 未实测**；`$()`/反引号注入面用例在 Windows skip，同因。
3. **硬链接 POSIX 链路**：Windows 用 `mklink /H` 实测通过（本机 NTFS 支持）；POSIX 是同一套 `os.stat().st_nlink` 语义但链路未实测。
4. **进程树 POSIX 信号组、PTY 的 POSIX 分支**（进程树 137 行、PTY 151/167/182 行 skip）：既有 skip，非本轮新增，见 §6。

---

## 6. skip / xfail 清单与掩盖分析

| 用例 | 跳过原因 | 掩盖了哪个未验证事实 | 是否属「本轮已裁决要修」 |
|---|---|---|---|
| test_shell命令替换与反引号注入面（agent_tools:611，本轮新增） | win32 无 `$()`/反引号语义（cmd /c） | sh -c 下 `$()`/反引号注入面未实测 | 否——POSIX 环境不足，语义本身已由 join 修复覆盖 |
| test_posix信号语义跳过占位（process_tree:137，既有） | win32 | os.setsid/负组号杀整树未实测 | 否 |
| PTY 三个 POSIX 分支（pty:151/167/182，既有） | 需 WSL/Docker | openpty/forkpty/setsid、TIOCSWINSZ、优雅关闭（SIGTERM/SIGKILL）未实测 | 否 |

**无 xfail 残留**：硬链接与 TOCTOU 两条本轮裁决要修的 xfail 已全部翻成正向断言（25 passed 不含 xfailed）。
（test_硬链接 自身保留 `sys.platform != "win32"` 的 skipif——那是 POSIX 平台侧覆盖问题，win32 平台真实执行。）

---

## 7. 硬链接封死方案：代价与误伤面（硬性约束 4）

**选定方案：拒绝 `st_nlink > 1` 的文件（os.stat 成功时；仅文件，目录不查）。**
- 代价：沙箱内「合法多链文件」被误伤——NTFS 上 `mklink /H` 产物、部分去重存储/备份软件建的副链，会被判拒（检查 False、读取/写入抛错）。
- 误伤面控制：目录不查（POSIX 目录 st_nlink 随子目录数增大是常态，nlink≥2 不构成逃逸，误伤不可接受）；stat 失败（不存在/权限不足）不按硬链接处理，走原有路径判定；仅 stat 成功且非目录才查。
- 未选「同设备 + 遍历沙箱验 inode 可达」：每次核准 O(文件数) 遍历 + 每文件 stat，成本高且遍历自身仍有竞态，收益不匹配。
- 残余竞态兜底：检查与使用之间若被换入硬链接，由 B4-3 的「打开后 fstat(st_dev+st_ino) 复核」拦截——fstat 反映真实打开对象，与核准快照不一致即抛错。两道防线叠加后未发现可绕路径。

---

## 8. §5.2 路径自查表（逐项回答「这个路径过核准了吗」）

| 位置 | 调用 | 路径来源 | 过核准？ | 结论 |
|---|---|---|---|---|
| 路径护栏.light:208 | `os.open(实, 标志组, 模式)` | 读取/写入/续写 内 `核准(路径)` 的实路径 | **是** | 调用方核准 + stat 快照 + POSIX O_NOFOLLOW + 打开后 fstat 复核（B4-3） |
| 代理工具集.light:283 | `os.open(临时路径, …)`（write_file） | `join(父核准, "_taskB4_tmp_"+随机名)` | **是** | 目录已核准（B4-1 父目录防线），文件 O_EXCL 新建、名不可预测 |
| 代理工具集.light:294 | `os.replace(临时路径, 实路径)` | 目标=核准实路径 | **是** | 原子替换只发生在沙箱内 |
| 代理工具集.light:358 / :369 | edit_file 同 write_file | 同上 | **是** | 同上 |
| 代理工具集.light:590 | `tempfile.gettempdir()`（溢出目录） | 系统临时目录 | **否** | **设计内豁免**：子进程输出溢出不写回沙箱（防塞爆）；文件名 `_taskD2_spill_`+计数+微秒、O_EXCL 撞名重试防 symlink，生命周期随 进程树 |
| 进程树.light:128 | `os.open(路径, 标志, 384)`（溢出文件） | `join(己.目录, 随机名)` | **否** | 同上豁免（溢出逃生通道，非用户路径） |
| 进程树.light:267 | `tempfile.gettempdir()` | 系统临时目录 | **否** | 同上豁免 |
| 进程树.light:304/307 | `subprocess.Popen(..., cwd=工作目录, ...)` | run_command 的 `解析路径(护栏, 目录入参)` | **是** | 显式 cwd 过护栏；未传时 None=继承解释器 cwd（与护栏语义无关，非终端越界） |
| 伪终端.light | 无 os.open/open/os.replace/tempfile/cwd | — | — | 无命中 |

---

## 9. 其余硬性约束落实

- **护栏对外语义不变**（约束 1）：`核准(路径)` 仍为「越界抛 路径护栏错误、通过返回归一化实路径」；读取返回 bytes、写入/续写返回 真（异常抛错），返回类型未改。新增的是内部加固（打开/疑似硬链接/同一文件）与 write/edit 入口的显式拒绝文案（文案属行为补充，非类型变更）。
- **安全声明同步**（约束 3）：路径护栏.light 头部注释（:14-18 区域）与 `段落 安全声明`（:85 区域）均已改写为「fstat 复核收窄」口径，无「存在 TOCTOU 窗口」残留。
- **六注册点副作用第五参**（约束 5）：开工时 main 上 `代理循环.light:280 段落 注册工具 接收 名称, 描述, 参数模式, 实现` 仍是**四参**，无副作用位。按任务书裁决：**按四参提交并在本报告标明**，未擅改 代理循环.light（越界文件）。read_file/list_dir/grep="只读"、write_file/edit_file="写"、run_command="执行" 的标注留待 A4 的第五参支持落地。
- **xfail 纪律**（约束 6）：硬链接、TOCTOU 两条已翻正向；剩余 xfail 为 0；skip 仅钉 POSIX 环境不足项（§6）。
- **引 Python 计数 = 0**（约束 7）：grep 四个 stdlib 文件，`引 Python` 命中 0。
- **临时文件清理**（约束 7）：本轮所有临时文件使用 `_taskB4_` 前缀（inject_tests.py、backrun 脚本与备份、反跑输出、junction 测试目标），已全部删除；`git status --porcelain` 仅剩 5 个预期交付文件。
- **跑法**（约束 8）：进程树/PTY 测试全程 `env -u PYTHONUTF8 …` 跑；带编码环境会假红（裸跑 2 failed，清环境 20 passed，非代码回归）。
- **提交白名单**（约束 9 + §10）：逐路径 `git add`，绝不用 `-A`；commit message 用 `-F 文件`。

## 10. 附：第三轮 D3 退回点对照

D3 被退回的教训是「代码修了声明没改」。本轮检查：头部注释、机读安全声明、测试内 xfail 注释、报告四处的口径已统一为修复后状态，无一处残留旧声明。