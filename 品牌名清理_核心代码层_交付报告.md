# 核心代码层「段言」→「光明」品牌名清理交付报告（任务 PGB）

- 分支：`task-PGB-brand`（本地分支隔离，未建 worktree —— 本仓 39k 文件，`worktree add` 需 10+ 分钟易超时成半成品，且历史上有 `prune` 误删事故）
- 日期：2026-09-07
- 范围：`src/`、`tools/`、`benchmark.py`、`scripts/`、`.github/`

---

## 一、根因

项目已从「段言」更名为「光明」，但核心代码层仍残留旧品牌名，集中在三类：

1. **用户可见文本**（最该修）：错误消息 / 修改指引（`src/errors.py`、`src/enhanced_errors.py`）、
   交互式教程（`src/interactive_tutorial.py`）、包注册中心 Web 页面（`tools/registry_web/app.py`）、
   各工具 `--help` 与运行横幅。
2. **文档字符串 / 注释**：模块头、函数 docstring。
3. **附带问题**：若干处写作「段言（Duan）」——只改中文不改进英文名会变成「光明（Duan）」这一自相矛盾的写法。

**重要前提核查**：替换前先确认测试没有断言这些字符串。经查 `tests/` 下的「段言」全部是
文档字符串 / 测试用数据（如 `test_filesystem.py` 把「段言」当文件正文），
`test_chinese_errors.py` 只遍历 `CHINESE_DUAN_ERROR_HINTS` 的键值（键未改），
**无一处断言错误文案本身**，因此替换不会打挂测试。

---

## 二、修改文件列表与替换处数

共 **25 个文件**，**137 处**「段言」→「光明」，外加 **11 处**英文名 `Duan/DuanLang` → `Light/LightLang`。

### src/（11 个文件，88 处命中）

| 文件 | 替换处数 | 说明 |
|---|---|---|
| `src/enhanced_errors.py` | 21 | 用户可见错误解释 / 示例片段 |
| `src/interactive_tutorial.py` | 32 | 用户可见教程文案、demo_code、expected_output（三者同步改，保持一致） |
| `src/elastic_syntax.py` | 13 | 模块头、docstring、注释 |
| `src/errors.py` | 6 | 用户可见修改指引 |
| `src/chinese_units.py` | 5 | 模块头、docstring |
| `src/ast_nodes_v3.py` | 3 | 类型注解 docstring |
| `src/package_manager.py` | 2 | 自动生成的包文档文案 |
| `src/parser_core.py` | 2 | 关键字提示文案 |
| `src/lexer.py` | 2（改写）+1（标注） | 见「保留项」 |
| `src/version.py` | 2（历史改写） | 见「保留项」 |
| `src/parser_expr.py` | 1（历史改写） | 见「保留项」 |

### tools/（11 个文件）

| 文件 | 替换处数 | 说明 |
|---|---|---|
| `tools/registry_web/app.py` | 14 | 页面标题、`"author": "段言团队"` ×10、启动横幅 |
| `tools/ai_copilot/code_converter.py` | 9 | 模块头、docstring、`--help` description |
| `tools/package_offline_model.py` | 4 | 模型清单 description、状态打印、`--help` |
| `tools/run_all_tests.py` | 2 | 模块头、运行横幅 |
| `tools/dataset_audit.py` | 3 | 模块头、注释 |
| `tools/gen_api_docs.py` | 2 | 模块头、`--help` |
| `tools/ci/bootstrap_rate.py` | 1 | 注释 |
| `tools/ci/python_direct_calls.py` | 1 | 注释 |
| `tools/registry_web/static/style.css` | 1 | 文件头注释 |
| `tools/lightpub_bridge.py` | 1（历史改写） | 见「保留项」 |
| `tools/installer/windows/setup.iss` | 0（保留） | 见「保留项」 |

### 其它（3 个文件）

| 文件 | 替换处数 |
|---|---|
| `benchmark.py` | 6 |
| `scripts/migrate_docs.py` | 6（+1 处功能性匹配串保留并加注释） |
| `.github/ISSUE_TEMPLATE/bug_report.md` | 3 |
| `.github/workflows/eval.yml` | 1（任务书未列，为注释里的「段言语言本体」） |

---

## 三、替换前后示例（典型 5 处）

1. **错误消息（用户最直接可见）** — `src/enhanced_errors.py`
   - 前：`'DuanError': '段言运行时错误，请根据具体错误信息进行排查和修复。'`
   - 后：`'DuanError': '光明运行时错误，请根据具体错误信息进行排查和修复。'`
   - 前：`'IndexError': '请检查索引是否在有效范围内，段言列表索引从 0 开始…'`
   - 后：`'IndexError': '请检查索引是否在有效范围内，光明列表索引从 0 开始…'`

2. **Web 页面作者与标题** — `tools/registry_web/app.py`
   - 前：`"author": "段言团队"`（×10）、`print(f"\n📦 段言包注册中心 Web 界面")`
   - 后：`"author": "光明团队"`（×10）、`print(f"\n📦 光明包注册中心 Web 界面")`

3. **交互式教程（demo/hint/expected 三者须同步，否则自测不过）** — `src/interactive_tutorial.py`
   - 前：`hint='打印 "段言，你好！"' / demo_code='打印 "段言，你好！"'`
   - 后：`hint='打印 "光明，你好！"' / demo_code='打印 "光明，你好！"'`
   - 前：`expected_output="语言：段言，版本：6"`
   - 后：`expected_output="语言：光明，版本：6"`

4. **英文配套名（本次附带修正，避免「光明（Duan）」自相矛盾）**
   - `benchmark.py`：`光明（Duan）性能基准测试` → `光明（Light）性能基准测试`
   - `src/interactive_tutorial.py`：`光明（DuanLang）交互式教程引擎` → `光明（LightLang）交互式教程引擎`

5. **过期标识符引用顺手修正** — `src/lexer.py`
   - 前：`examples/chat_bot/主.light:42 `.段言到Python(...)``
   - 后：`.光明到Python(...)`（该示例文件里的标识符**早已**改名为 `光明到Python`，注释是过期引用）

---

## 四、刻意保留项（8 处，均为历史引用或功能性串）

按任务书「历史说明可保留，建议加（现名光明）注释」执行，全部已加标注：

| 位置 | 内容 | 保留理由 |
|---|---|---|
| `src/version.py:35` | `光明 v7.0 是原「段言」（Duan）与「光明」（Light）双线合并后的首个统一版本（合并后统一称「光明」）` | 双线合并史实，改写为「原…」并补统一说明 |
| `src/version.py:39` | `- 双线合并：原段言 v6.3 与光明 v6.0 …` | 历史版本号 |
| `src/lexer.py:305` | `含 π 的导出名在段言（现名光明）侧完全不可用` | 合并前代码归属说明 |
| `src/parser_expr.py:2050` | `本方法…来自段言（现名光明）侧，比 light 原先…宽松` | 与「light 原先」对举，改掉会丢失对比语义 |
| `tools/lightpub_bridge.py:313` | `函数名对齐上游 duanpub（原段言时期）…` | 上游历史包名 |
| `tools/installer/windows/setup.iss:14` | `保持不变，装 v7 才会覆盖升级旧的段言安装` | 解释 AppId 为何不能改，改掉会让读者误判 |
| `scripts/migrate_docs.py:92` | `is_duan = in_fence and ('段言' in fence_lang or 'duan' in fence_lang)` | **功能性匹配串**：本脚本专扫改名前的旧文档 fence 标签，改掉会漏扫（已加 2 行注释说明） |
| `scripts/migrate_docs.py:90` | 上条新增的说明注释 | 同上 |

**未动（非品牌文案，改了会破坏代码）**：
- 类名 / 常量：`DuanError`、`CHINESE_DUAN_ERROR_HINTS`、`Java2DuanConverter`、`C2DuanConverter`、`DuanLinter`
- C 类型别名：`src/llvm/runtime_typed.c` 的 `DuanValue` / `DuanMethodFunc`
- 文件名、模块名、`duan` CLI 子命令、`duanpub` 上游包名

---

## 五、验证结果

| 项 | 命令 / 方法 | 结果 |
|---|---|---|
| 语法编译 | `py_compile` 全部 22 个改动 .py | **COMPILE_OK** |
| 导入验证 | `sys.path.insert(0,'src'); import lexer; import parser_core; import errors` | **IMPORT_OK**（含 `enhanced_errors`、`version`） |
| 版本验证 | `from version import __version__; get_full_version_string()` | `7.0.0` / `光明 LightLang v7.0.0「v7.0 双线合并与品牌统一版」— 2026-08-14` |
| 运行期文案 | 遍历 `enhanced_errors` 全部 dict + `CHINESE_DUAN_ERROR_HINTS` 值 | **残留项：无**；抽样 `TypeError/IndexError/NameError/LexerError` 均已显示「光明」 |
| 错误消息实证 | 故意写缺冒号的 `.light` 跑 `python -m cli.light run` | 输出全链路无「段言」，教程指引指向 `30分钟入门光明.md` |
| CLI 冒烟 | `python -m cli.light --version` | `光明编译器 v7.0.0` |
| 示例冒烟 | `python -m cli.light run examples/my_first.light` | 正常输出「你好，光明！/及格/1到10的总和：55」 |
| 单元测试 | `python -m pytest tests/unit/ -q` | 见下 |

### 单元测试结果

**全量跑**：`python -m pytest tests/unit/ -q -rf --junitxml` → **总计 4363，失败 9，错误 0，跳过 8**。

失败分两类，逐条做了「改前 / 改后同口径复跑」比对（同一条命令、同一组用例、同一台机器）：

| # | 用例 | 判定 | 依据 |
|---|---|---|---|
| 1 | `test_doc_examples_gate::test_不许新增ROT` | **预存失败** | 改前改后均 FAILED；断言的是 `docs/` 文档噪声块，本次未动 docs |
| 2 | `test_doc_examples_gate::test_噪声类总数不许涨` | **预存失败** | 同上（噪声块 18→19，出自 docs 侧） |
| 3 | `test_lightpub_doc_importability::test_标成text的导入块必须真的导不进来` | **预存失败** | 与 CIC 任务中记录的同一条：`HTTP客户端.md` 文档围栏过时；改前改后均 FAILED |
| 4 | `test_原生腿_R13B_能力扩展::test_O0_行政区划代码_对拍与扩展` | **预存失败** | `AttributeError: 'ChinaRegion' object has no attribute '_region_code_map'`，原生腿代码缺陷，与品牌串无关 |
| 5–9 | `test_原生腿_R13C_对拍扩展` 的 5 条（分割与连接 / Base64往返 / URL编码解码 / 分词族 / 字符串相似度） | **非本次引入（并发冲撞）** | 见下方说明 |

**关于 5–9 的判定过程（避免误判，特记录）**：全量跑里这 5 条报 `NameError: name '列表' is not defined` / `name '编码解码' is not defined` / `TypeError: 'int' object is not callable`。
为定性，把这 9 条**单独拎出**做 A/B 复跑：

- 原代码（`git stash` 取回）：**4 failed, 5 passed**（失败的正是上表 1–4）
- 本次改动（恢复后）：**4 failed, 5 passed** ← **完全一致**

即这 5 条在隔离运行下改前改后**全绿**；它们在全量跑中失败，是因为我误在旧的全量进程尚未结束时又启动了一个全量进程，两个 pytest 共用 `pytest-of-skywalk` 临时目录与原生腿编译产物，导致产物被互相覆盖。**与品牌名替换无关。**

**结论**：本次改动**零回归**——4 条失败在改前改后逐条一致（且全部落在本次未触碰的 `docs/` 与原生腿代码缺陷上），另 5 条为并发冲撞假象。

---

## 六、红线遵守

- ✅ 只动 `src/`、`tools/`、`benchmark.py`、`scripts/`、`.github/`；未动 `stdlib/`、`tests/`、`docs/`、`playground/`
- ✅ 未重命名任何文件 / 模块 / 类 / 函数（`import` 路径全保留）
- ✅ 未改代码逻辑：唯一改动逻辑相邻处是 `scripts/migrate_docs.py`，仅**新增 2 行注释**，匹配串原样保留
- ✅ 显式 `git add` 限定任务目录，暂存名单经 `git diff --cached --name-only` 核对为 25 个文件；
  工作区里其它 agent 的无关改动（`stdlib/*`、`playground/static/index.html` 等）**未混入**
- ✅ 临时文件 `_taskPGB_*` 用完即删

---

## 七、遗留建议（本次范围外，供决策）

1. **`tools/ai_copilot/sft_dataset.jsonl`**：约 200+ 条训练样本的 `instruction` 写「将Python翻译成段言」。
   属训练语料而非用户可见文案，且任务书 tools/ 的 37 处统计未包含它，故本次**未动**。
   若决定统一，建议整批替换并同步重训/回归评测，不要与本次混提。
2. **`DuanError` 等旧名类名/常量**：跨 `src/` 与 `tests/`（如 `CHINESE_DUAN_ERROR_HINTS` 被
   `tests/test_chinese_errors.py` 引用），重命名需同步改测试，属独立任务。
3. **`src/llvm/runtime_typed.c` 的 `DuanValue`/`DuanMethodFunc`**：C ABI 侧类型别名，重命名需全仓 C 侧同步，风险较高。
4. **`examples/` 与 `docs/`** 仍有大量「段言」（历史文档按任务书不在范围内）。
