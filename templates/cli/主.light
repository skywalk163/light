# 命令行工具入口
导入 系统

段落 显示帮助：
  打印("用法: duan run 主.duan [选项]")
  打印("")
  打印("选项:")
  打印("  --help      显示帮助信息")
  打印("  --version   显示版本信息")

段落 显示版本：
  打印("版本 0.1.0")

段落 主程序 接收 参数：
  如果 参数 长度 等于 0：
    打印("你好，世界！")
    返回

  设 第一个 参数 为 参数[0]

  如果 第一个 参数 等于 "--help" 或 第一个 参数 等于 "-h"：
    显示帮助()
    返回

  如果 第一个 参数 等于 "--version" 或 第一个 参数 等于 "-v"：
    显示版本()
    返回

  打印("未知选项: ", 第一个 参数)

# 获取命令行参数
设 参数列表 为 系统.获取参数()
主程序(参数列表[1:])