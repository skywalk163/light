// 基准测试：类系统（类定义、方法调用）
类 形状：
  属性 名称
  函数 构造 接收 名称：
    己.名称 为 名称
  段落 面积：
    返回 0
  段落 介绍：
    打印 己.名称

类 矩形 继承 形状：
  属性 宽
  属性 高
  函数 构造 接收 宽, 高：
    己.宽 为 宽
    己.高 为 高
    己.名称 为 "矩形"
  段落 面积：
    返回 己.宽 乘 己.高
  段落 周长：
    返回 己.宽 加 己.高 乘 2

类 圆形 继承 形状：
  属性 半径
  函数 构造 接收 半径：
    己.半径 为 半径
    己.名称 为 "圆形"
  段落 面积：
    返回 己.半径 乘 己.半径 乘 3.14
  段落 周长：
    返回 2 乘 3.14 乘 己.半径

类 Triangle 继承 形状：
  属性 底
  属性 高
  函数 构造 接收 底, 高：
    己.底 为 底
    己.高 为 高
    己.名称 为 "Triangle"
  段落 面积：
    返回 己.底 乘 己.高 除 2

设 矩形1 为 新建 矩形(3, 4)
设 圆形1 为 新建 圆形(5)
设 Triangle1 为 新建 Triangle(6, 8)

矩形1.介绍()
打印 矩形1.面积()
圆形1.介绍()
打印 圆形1.面积()
Triangle1.介绍()
打印 Triangle1.面积()
