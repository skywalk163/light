# R65 任务1：stdlib 旧式「接收」语法现代化·第一批（41 模块 / 566 行）

- 轮次：R65 ｜ 日期：2026-09-19 ｜ 执行：主 agent
- 背景：R62 只清了 tests/（287 行），全量 warning 只降 162（53617→53455）。
  实测根因：**warning 大头是 stdlib 的 1668 处旧式写法**（每份 stdlib 被每个测试重复编译）。

## 1. 可行性量化（改前扫描）

复用 R62 改造器的 `transform_line`（`lightharness/docs/历史存档/R62探针/_任务3_R62_应用.py`）
扫 `stdlib/*.light`（109 个文件 / 总命中 1629 行）：

| 分类 | 行数 | 说明 |
| --- | --- | --- |
| **SAFE** | **1370** | 纯参数 + 可选内嵌 `: 类型` + 空参数 —— 两分支等价，可机械替换 |
| SKIP-DEFAULT | 250 | 含默认值（`等于 5` / `= 5`）——括号分支会把 `等于` 当参数名吞掉 |
| SKIP-STAR | 6 | 含 `*args` / `**kwargs` |
| SKIP-CTX | 3 | 注释/文档（`代理工具集.light:4`、`数学.light:8`、`网络请求.light:336`） |
| SKIP-SPTYPE | 0 | 空格式类型（stdlib 里不存在） |

**第一批选取原则**：只取「**整模块全部 SAFE**」的模块 —— 改完无残留形态混杂，
可按模块整块回归。共 **41 个模块 / 566 行**（占 SAFE 总量的 41%）。

Top15：数据结构轻量 39、向量 26、路径护栏 25、农历 25、HTTP服务端 25、流式 24、
大模型客户端 21、JSON编解码 21、统计 20、进程树 18、中文编码 18、Base64 18、
插件 17、内置核心字符串 17、中国传统节日 17 …（完整 41 个见 `_r65_batch1.json`）

## 2. 改动

```diff
-段落 _词典合并 接收 甲, 乙:
+段落 _词典合并(甲, 乙):
-段落 分词 接收 文本:
+段落 分词(文本):
-段落 归并排序 接收 数据:
+段落 归并排序(数据):
```

最终落盘：**41 文件 / 562 行**（首轮 566 行，后按 §5 的签名对拍回退 4 行）。
git 自证：`git diff --numstat stdlib/` → **41 files changed, 562 insertions(+), 562 deletions(-)**
（严格 1:1 行替换，无整文件改写）。

## 3. 护栏（4 道，全部执行）

| 护栏 | 结果 |
| --- | --- |
| 白名单（41 模块之外一个字节不碰） | ✅ 只写白名单内文件 |
| CRLF 保持（`io.open(newline="")`） | ✅ 无整文件 diff |
| **魔数护栏**：改动文件里「纯光明实现」只允许在首两行 | ✅ **新增行 0 处含该字样**；<br>脚本报的 12 处均为**改前既有注释**（数据结构轻量:4/8/9、路径护栏:7、进程树:13、Base64:70、插件:4、事件总线:9、内置核心判型:78、日期时间轻量:4/10、伪终端:11），本轮未触碰 |
| 残留自检：改后模块内不应再有 SAFE 形态 | ✅ **残留 0 处** |

## 4. warning 量化（核心指标）

探针 `light-merge/_probe_R65_stdlib_warn.py`：**每个 .light 精确解析一次**，
计数确定、可比（不用 pytest 汇总——它按 (message, category, module, lineno) 去重，
且 stdlib 被重复编译，计数随测试集漂移）。

| 指标 | 改前 | 改后 | 变化 |
| --- | --- | --- | --- |
| stdlib 单次解析 DeprecationWarning | **1321** | **969** | **−352（−26.6%）** |
| 命中文件数 | 72 | 40 | −32 |
| 解析异常（仅登记） | 109→17 | 17 | 持平 |

> 注：改前「解析异常 109 个」是探针首版用错了 parser 类（`parser_core.LightParserCore`
> 是抽象基类）；改用 `light_parser_v3.LightParser` 后异常降到 17 个（这些是 decl 空壳/
> 依赖缺失模块，与本次改动无关）。

## 5. 验证

### 5.1 首轮 0.82 全量：**13 新增红，门 FAIL**（已定位并修复）

失败分布：`test_async_io_light.py` 8 条、`test_harness_real_channel_light.py` 3 条、
`test_codegen_safename_multimodule_O0.py` 1 条、`test_原生腿_R11B_中文工具.py` 1 条。

**根因（关键发现，R62 的等价性分析漏了这一条）**：
参数名**含关键字子串**时，括号式会被切分成两个参数：

```
段落 取异步流式 接收 客户端对象, 消息列表:   →  def 取异步流式(客户端对象, 消息列表)   ✅
段落 取异步流式(客户端对象, 消息列表):        →  def 取异步流式(客户端对象, 消息, 列表)  ❌
段落 加载词典 接收 词列表:                   →  def 加载词典(词列表)                  ✅
段落 加载词典(词列表):                       →  def 加载词典(词, 列表)                ❌
```

`列表` 是 builtin 关键字，在括号上下文里 `消息列表` / `词列表` 会被切成 `消息`+`列表` / `词`+`列表`。
于是调用方传 2 个实参对 3 个形参 → `TypeError: 取异步流式() missing 1 required positional argument: '列表'`，
连带 11 条异步/真实通道用例全红；`中文分词_O0对拍` 因 `加载词典` 签名坏掉而 `assert 7 == 4`。

> **这是 R62「只改 SAFE 形态就等价」结论的一个反例**：SAFE 判据只比对了
> **parser 的 params 结构**，没比对**词法在括号上下文里的切分行为**。

**修复**：
1. 回退 4 行（`大模型客户端.light` 3 行、`中文分词.light` 1 行）→ 566 行变 **562 行**；
2. `tests/unit/test_codegen_safename_multimodule_O0.py:181-182` 的断言
   `assert "段落 最小值 接收 数据:" in content` 改为**语法无关**的 `assert "段落 最小值" in content`
   （它断言的是「定义了该段」，不是「用什么参数语法」）。

**新增护栏（下轮必须遵守）**：**产物函数签名逐模块对拍** ——
对每个待改模块，用 `cli.light compile` 分别编译 HEAD 版与改动版，
比对所有 `def 名(参数)` 签名；有差异即回退。本轮正是靠它把 41 个模块收敛到 2 个问题模块。

### 5.2 修复后

- **本机定向**（3 组失败用例 + 统计相关）：
  `test_中文分词_O0对拍` + `test_async_io_light.py` + `test_harness_real_channel_light.py`
  + `TestWorkaroundRemoved` → **24 passed, 0 failed**。
- **逐模块编译抽检**（13 个模块）：全部 rc=0。
- **stdlib 定向测试组**：`tests/integration/test_stdlib.py` + `tests/test_stdlib_phase*.py`
  → **554 passed, 10 xpassed, 0 failed**（33.2s）。
- **0.82 全量（门整改复跑）**：见 `_taskM_R65_收口总报告.md`。

## 6. 下轮计划（剩余）

| 类别 | 数量 | 处理建议 |
| --- | --- | --- |
| SAFE（散布在**混合模块**，与 DEFAULT/STAR 同文件） | **804** | 逐条人工确认，按模块拆批 |
| SKIP-DEFAULT（含默认值） | **250** | ⛔ 括号分支不支持 → 需先给括号分支加默认值支持，或保留旧式 |
| SKIP-STAR（`*args`） | **6** | ⛔ 括号分支不支持 → 同上 |
| SKIP-CTX（注释） | 3 | 保留 |

⚠️ **下轮风险提示**：
1. **参数名不得含关键字子串**（`列表`/`字典`/`文本`/`整数`/`输出`/`长度`…）——
   括号式会把它切成两个参数（本轮踩中，见 §5.1）。**下轮改造器必须加这条黑名单**，
   并强制跑「产物签名逐模块对拍」。
2. 剩下 802 处都在「同一文件里既有 SAFE 又有 DEFAULT/STAR」的混合模块，
   机械替换会让**同一模块出现新旧两种写法并存**，可读性下降；建议要么整模块推进
   （先把该模块的 DEFAULT/STAR 形态人工改写），要么先给括号分支补上默认值/星号支持。
2. `stdlib/*.light` 有同名 `.py`（Python 腿实现）；部分 `.light` 是 **decl 0 空壳**
   （如 `正则表达式.light`），改 decl 时要确认两侧签名一致。
3. 魔数护栏必须每轮自检（见 §3）。

## 7. 交付物

| 文件 | 改动 |
| --- | --- |
| `light-merge/stdlib/**`（41 文件 / 566 行） | 旧式 `接收` → 括号式参数 |
| `light-merge/_任务1_R65_stdlib应用.py` | 新增：stdlib 改造器（白名单 + 4 道护栏） |
| `light-merge/_probe_R65_stdlib_warn.py` | 新增：stdlib warning 计数探针 |
| `light-merge/_r65_batch1.json` | 新增：第一批 41 模块清单 |
| `light-merge/_task1_R65_stdlib接收现代化首批.md` | 本报告 |
